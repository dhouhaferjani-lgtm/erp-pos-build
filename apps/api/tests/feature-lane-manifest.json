{
  "note": "Assignment of every tests/Feature top-level group to a CI lane, an exclusion, or a deferral. Enforced by tools/feature-lane-manifest-check.php, which runs as a discrete step in the backend-architecture job (no `if:` guard, in all-checks-pass `needs`). A new directory has no entry here and FAILS the check - that is the whole point: before this file, a new Feature class in an unlisted directory ran nowhere, forever, and nothing said so. `classes` is an ENFORCED non-growth CEILING for `deferred`/`excluded` groups AND for groups laned into a lane that is still PARKED behind an execution gate: such a group may shrink freely and may NEVER grow, so a new class there fails CI until the ceiling is raised deliberately or the lane actually executes. It is NOT documentation. O-29 (2026-08-21): the 71 formerly `deferred` groups now carry real lanes, 8 self-hosted jobs partitioned by module family; those jobs are guarded by `vars.SELF_HOSTED_RUNNER_READY` and execute on no event until the owner registers the runner and flips it. Full design: docs/handoff/DESIGN-f2-feature-lane-execution-2026-08-21.md.",
  "dispositions": {
    "lane": "Runs automatically in the named CI lane. New classes in the group are picked up with no edit here — UNLESS the lane declares an `execution_gate`, in which case the lane is wired but parked, the group keeps its enforced ceiling, and the parked total is reported loudly on every run until the gate is flipped.",
    "excluded": "Genuinely cannot run in CI (needs a device, an external service, a manual fixture). Requires `reason`.",
    "deferred": "CAN run, but no lane runs it yet. Requires `reason`. Counted loudly on every CI run. After O-29 exactly one group is left here — `(root files)`, which no whole-directory selector can address."
  },
  "debt_ceiling": 1,
  "gated_ceiling": 1283,
  "lanes": {
    "security-regression": {
      "job": "security-regression",
      "step": "Security regression suite (module gating + kill-switches)",
      "selector": "./vendor/bin/phpunit tests/Feature/Security",
      "runs_on_pr_dev": true,
      "events_note": "EVERY event that starts the workflow — PR->dev, PR->main, push->main, workflow_dispatch. The job has NO `if:` guard, by the parent ruling of 2026-08-19 on brief F-2's sub-decision. It was previously a step inside the `if:`-gated backend-test job and therefore did not run on PR->dev.",
      "note": "Whole directory, not a filter list, so new classes are gated the moment they land."
    },
    "treasury-spine-pgsql/feature-treasury": {
      "job": "treasury-spine-pgsql",
      "step": "PG-only invariants - Treasury Feature suite",
      "selector": "./vendor/bin/phpunit tests/Feature/Treasury",
      "runs_on_pr_dev": true,
      "events_note": "PR->dev, PR->main, push->main, workflow_dispatch.",
      "note": "Real Postgres: the forbid-balance-write trigger, repository_movements immutability, GL advisory locks and savepoints all markTestSkipped on sqlite."
    },
    "treasury-spine-pgsql/feature-accounting": {
      "job": "treasury-spine-pgsql",
      "step": "PG-only invariants - Accounting Feature suite",
      "selector": "./vendor/bin/phpunit tests/Feature/Accounting",
      "runs_on_pr_dev": true,
      "events_note": "PR->dev, PR->main, push->main, workflow_dispatch.",
      "note": "Same PG rationale as the Treasury directory."
    },
    "feature-lane-pos/POS": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/POS",
      "selector": "./vendor/bin/phpunit tests/Feature/POS/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group POS of the Sales & POS lane."
    },
    "feature-lane-pos/Cart": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Cart",
      "selector": "./vendor/bin/phpunit tests/Feature/Cart/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Cart of the Sales & POS lane."
    },
    "feature-lane-pos/Voucher": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Voucher",
      "selector": "./vendor/bin/phpunit tests/Feature/Voucher/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Voucher of the Sales & POS lane."
    },
    "feature-lane-pos/Loyalty": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Loyalty",
      "selector": "./vendor/bin/phpunit tests/Feature/Loyalty/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Loyalty of the Sales & POS lane."
    },
    "feature-lane-pos/Coupon": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Coupon",
      "selector": "./vendor/bin/phpunit tests/Feature/Coupon/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Coupon of the Sales & POS lane."
    },
    "feature-lane-pos/Promotion": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Promotion",
      "selector": "./vendor/bin/phpunit tests/Feature/Promotion/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Promotion of the Sales & POS lane."
    },
    "feature-lane-pos/Progression": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Progression",
      "selector": "./vendor/bin/phpunit tests/Feature/Progression/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Progression of the Sales & POS lane."
    },
    "feature-lane-pos/Pricing": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/Pricing",
      "selector": "./vendor/bin/phpunit tests/Feature/Pricing/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Pricing of the Sales & POS lane."
    },
    "feature-lane-pos/SmartPrompts": {
      "job": "feature-lane-pos",
      "step": "Feature lane Sales & POS — tests/Feature/SmartPrompts",
      "selector": "./vendor/bin/phpunit tests/Feature/SmartPrompts/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group SmartPrompts of the Sales & POS lane."
    },
    "feature-lane-fiscal-finance/Fiscal": {
      "job": "feature-lane-fiscal-finance",
      "step": "Feature lane Fiscal & Finance — tests/Feature/Fiscal",
      "selector": "./vendor/bin/phpunit tests/Feature/Fiscal/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Fiscal of the Fiscal & Finance lane."
    },
    "feature-lane-fiscal-finance/Compliance": {
      "job": "feature-lane-fiscal-finance",
      "step": "Feature lane Fiscal & Finance — tests/Feature/Compliance",
      "selector": "./vendor/bin/phpunit tests/Feature/Compliance/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Compliance of the Fiscal & Finance lane."
    },
    "feature-lane-fiscal-finance/Taxation": {
      "job": "feature-lane-fiscal-finance",
      "step": "Feature lane Fiscal & Finance — tests/Feature/Taxation",
      "selector": "./vendor/bin/phpunit tests/Feature/Taxation/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Taxation of the Fiscal & Finance lane."
    },
    "feature-lane-fiscal-finance/Expense": {
      "job": "feature-lane-fiscal-finance",
      "step": "Feature lane Fiscal & Finance — tests/Feature/Expense",
      "selector": "./vendor/bin/phpunit tests/Feature/Expense/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Expense of the Fiscal & Finance lane."
    },
    "feature-lane-fiscal-finance/Income": {
      "job": "feature-lane-fiscal-finance",
      "step": "Feature lane Fiscal & Finance — tests/Feature/Income",
      "selector": "./vendor/bin/phpunit tests/Feature/Income/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Income of the Fiscal & Finance lane."
    },
    "feature-lane-inventory/Inventory": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Inventory",
      "selector": "./vendor/bin/phpunit tests/Feature/Inventory/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Inventory of the Inventory & Supply lane."
    },
    "feature-lane-inventory/BatchExpiry": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/BatchExpiry",
      "selector": "./vendor/bin/phpunit tests/Feature/BatchExpiry/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group BatchExpiry of the Inventory & Supply lane."
    },
    "feature-lane-inventory/Procurement": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Procurement",
      "selector": "./vendor/bin/phpunit tests/Feature/Procurement/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Procurement of the Inventory & Supply lane."
    },
    "feature-lane-inventory/Replenishment": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Replenishment",
      "selector": "./vendor/bin/phpunit tests/Feature/Replenishment/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Replenishment of the Inventory & Supply lane."
    },
    "feature-lane-inventory/PurchaseHub": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/PurchaseHub",
      "selector": "./vendor/bin/phpunit tests/Feature/PurchaseHub/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group PurchaseHub of the Inventory & Supply lane."
    },
    "feature-lane-inventory/Uom": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Uom",
      "selector": "./vendor/bin/phpunit tests/Feature/Uom/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Uom of the Inventory & Supply lane."
    },
    "feature-lane-inventory/Location": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Location",
      "selector": "./vendor/bin/phpunit tests/Feature/Location/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Location of the Inventory & Supply lane."
    },
    "feature-lane-inventory/Partner": {
      "job": "feature-lane-inventory",
      "step": "Feature lane Inventory & Supply — tests/Feature/Partner",
      "selector": "./vendor/bin/phpunit tests/Feature/Partner/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Partner of the Inventory & Supply lane."
    },
    "feature-lane-catalog/Catalog": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Catalog",
      "selector": "./vendor/bin/phpunit tests/Feature/Catalog/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Catalog of the Catalog & Product lane."
    },
    "feature-lane-catalog/Product": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Product",
      "selector": "./vendor/bin/phpunit tests/Feature/Product/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Product of the Catalog & Product lane."
    },
    "feature-lane-catalog/Channel": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Channel",
      "selector": "./vendor/bin/phpunit tests/Feature/Channel/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Channel of the Catalog & Product lane."
    },
    "feature-lane-catalog/Marketplace": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Marketplace",
      "selector": "./vendor/bin/phpunit tests/Feature/Marketplace/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Marketplace of the Catalog & Product lane."
    },
    "feature-lane-catalog/Vehicle": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Vehicle",
      "selector": "./vendor/bin/phpunit tests/Feature/Vehicle/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Vehicle of the Catalog & Product lane."
    },
    "feature-lane-catalog/Menu": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/Menu",
      "selector": "./vendor/bin/phpunit tests/Feature/Menu/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Menu of the Catalog & Product lane."
    },
    "feature-lane-catalog/ModelUpdates": {
      "job": "feature-lane-catalog",
      "step": "Feature lane Catalog & Product — tests/Feature/ModelUpdates",
      "selector": "./vendor/bin/phpunit tests/Feature/ModelUpdates/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group ModelUpdates of the Catalog & Product lane."
    },
    "feature-lane-documents/Document": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Document",
      "selector": "./vendor/bin/phpunit tests/Feature/Document/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Document of the Documents & Service lane."
    },
    "feature-lane-documents/DocumentIngestion": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/DocumentIngestion",
      "selector": "./vendor/bin/phpunit tests/Feature/DocumentIngestion/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group DocumentIngestion of the Documents & Service lane."
    },
    "feature-lane-documents/Workshop": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Workshop",
      "selector": "./vendor/bin/phpunit tests/Feature/Workshop/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Workshop of the Documents & Service lane."
    },
    "feature-lane-documents/Service": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Service",
      "selector": "./vendor/bin/phpunit tests/Feature/Service/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Service of the Documents & Service lane."
    },
    "feature-lane-documents/Services": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Services",
      "selector": "./vendor/bin/phpunit tests/Feature/Services/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Services of the Documents & Service lane."
    },
    "feature-lane-documents/Scheduling": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Scheduling",
      "selector": "./vendor/bin/phpunit tests/Feature/Scheduling/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Scheduling of the Documents & Service lane."
    },
    "feature-lane-documents/Contact": {
      "job": "feature-lane-documents",
      "step": "Feature lane Documents & Service — tests/Feature/Contact",
      "selector": "./vendor/bin/phpunit tests/Feature/Contact/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Contact of the Documents & Service lane."
    },
    "feature-lane-tenancy/Identity": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Identity",
      "selector": "./vendor/bin/phpunit tests/Feature/Identity/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Identity of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Tenant": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Tenant",
      "selector": "./vendor/bin/phpunit tests/Feature/Tenant/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Tenant of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Company": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Company",
      "selector": "./vendor/bin/phpunit tests/Feature/Company/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Company of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/CompanyConfig": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/CompanyConfig",
      "selector": "./vendor/bin/phpunit tests/Feature/CompanyConfig/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group CompanyConfig of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Admin": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Admin",
      "selector": "./vendor/bin/phpunit tests/Feature/Admin/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Admin of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/SupportAccess": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/SupportAccess",
      "selector": "./vendor/bin/phpunit tests/Feature/SupportAccess/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group SupportAccess of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Permissions": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Permissions",
      "selector": "./vendor/bin/phpunit tests/Feature/Permissions/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Permissions of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Authorization": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Authorization",
      "selector": "./vendor/bin/phpunit tests/Feature/Authorization/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Authorization of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Subscription": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Subscription",
      "selector": "./vendor/bin/phpunit tests/Feature/Subscription/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Subscription of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Billing": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Billing",
      "selector": "./vendor/bin/phpunit tests/Feature/Billing/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Billing of the Tenancy & Identity lane."
    },
    "feature-lane-tenancy/Modules": {
      "job": "feature-lane-tenancy",
      "step": "Feature lane Tenancy & Identity — tests/Feature/Modules",
      "selector": "./vendor/bin/phpunit tests/Feature/Modules/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Modules of the Tenancy & Identity lane."
    },
    "feature-lane-data-console/Console": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Console",
      "selector": "./vendor/bin/phpunit tests/Feature/Console/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Console of the Data & Console lane."
    },
    "feature-lane-data-console/Seeders": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Seeders",
      "selector": "./vendor/bin/phpunit tests/Feature/Seeders/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Seeders of the Data & Console lane."
    },
    "feature-lane-data-console/Import": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Import",
      "selector": "./vendor/bin/phpunit tests/Feature/Import/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Import of the Data & Console lane."
    },
    "feature-lane-data-console/Migration": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Migration",
      "selector": "./vendor/bin/phpunit tests/Feature/Migration/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Migration of the Data & Console lane."
    },
    "feature-lane-data-console/Migrations": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Migrations",
      "selector": "./vendor/bin/phpunit tests/Feature/Migrations/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Migrations of the Data & Console lane."
    },
    "feature-lane-data-console/Schema": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/Schema",
      "selector": "./vendor/bin/phpunit tests/Feature/Schema/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Schema of the Data & Console lane."
    },
    "feature-lane-data-console/CountryDefaults": {
      "job": "feature-lane-data-console",
      "step": "Feature lane Data & Console — tests/Feature/CountryDefaults",
      "selector": "./vendor/bin/phpunit tests/Feature/CountryDefaults/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group CountryDefaults of the Data & Console lane."
    },
    "feature-lane-platform-misc/PlatformIntegration": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/PlatformIntegration",
      "selector": "./vendor/bin/phpunit tests/Feature/PlatformIntegration/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group PlatformIntegration of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Api": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Api",
      "selector": "./vendor/bin/phpunit tests/Feature/Api/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Api of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Http": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Http",
      "selector": "./vendor/bin/phpunit tests/Feature/Http/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Http of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Architecture": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Architecture",
      "selector": "./vendor/bin/phpunit tests/Feature/Architecture/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Architecture of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Bootstrap": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Bootstrap",
      "selector": "./vendor/bin/phpunit tests/Feature/Bootstrap/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Bootstrap of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Broadcasting": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Broadcasting",
      "selector": "./vendor/bin/phpunit tests/Feature/Broadcasting/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Broadcasting of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Communication": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Communication",
      "selector": "./vendor/bin/phpunit tests/Feature/Communication/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Communication of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Notification": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Notification",
      "selector": "./vendor/bin/phpunit tests/Feature/Notification/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Notification of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Dashboard": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Dashboard",
      "selector": "./vendor/bin/phpunit tests/Feature/Dashboard/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Dashboard of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/EventSourcing": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/EventSourcing",
      "selector": "./vendor/bin/phpunit tests/Feature/EventSourcing/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group EventSourcing of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Events": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Events",
      "selector": "./vendor/bin/phpunit tests/Feature/Events/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Events of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Jobs": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Jobs",
      "selector": "./vendor/bin/phpunit tests/Feature/Jobs/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Jobs of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Performance": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Performance",
      "selector": "./vendor/bin/phpunit tests/Feature/Performance/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Performance of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Precision": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Precision",
      "selector": "./vendor/bin/phpunit tests/Feature/Precision/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Precision of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/Shared": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/Shared",
      "selector": "./vendor/bin/phpunit tests/Feature/Shared/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group Shared of the Platform & Misc lane."
    },
    "feature-lane-platform-misc/T2": {
      "job": "feature-lane-platform-misc",
      "step": "Feature lane Platform & Misc — tests/Feature/T2",
      "selector": "./vendor/bin/phpunit tests/Feature/T2/",
      "runs_on_pr_dev": false,
      "execution_gate": "${{ vars.SELF_HOSTED_RUNNER_READY == 'true' && (github.event_name == 'workflow_dispatch' || github.base_ref == 'main' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}",
      "events_note": "PR->main, push->main, workflow_dispatch — AND ONLY once the repository variable SELF_HOSTED_RUNNER_READY is set to \"true\". Never PR->dev: these are minute-expensive suites and PR->dev stays the cheap gate (O-29 design §4).",
      "note": "Whole directory, not a filter list. Runs on the self-hosted `autoerp-heavy` runner against a real PostgreSQL 16 + Redis service pair, so PG-only behaviour executes instead of markTestSkipped-ing. Group T2 of the Platform & Misc lane."
    }
  },
  "groups": {
    "(root files)": {
      "deferred": true,
      "classes": 1,
      "reason": "STRUCTURALLY UN-LANEABLE, and the only surviving coverage debt: a lane disposition requires a whole-DIRECTORY selector ending in tests/Feature/<Group>, and loose files at the root of tests/Feature have no such directory. The single class here is ExampleTest.php (the Laravel scaffold). Fix by moving any real root-level Feature test into a laned directory; the ceiling of 1 means a second root file fails CI."
    },
    "Accounting": {
      "lane": "treasury-spine-pgsql/feature-accounting",
      "classes": 101,
      "raise_note": "DELIBERATE RAISE 100 -> 101 (2026-08-30, lane fix/je-number-company-scope): OpeningBatchNumberingCompanyScopeTest is the end-to-end second-of-everything proof that sibling companies in one tenant can each post their first real AR opening item and first real opening-stock movement, yielding OB-<year>-000001 and INV-OB-<year>-000001 in both companies. Run BY PATH on SQLite AND PostgreSQL 16 (private DB autoerp_test_j4, port 5433); Accounting is a live whole-directory lane, so this ceiling is informational and does not increase gated_ceiling.",
      "note": "Informational for lane groups: the ceiling is enforced only for deferred/excluded groups; a laned directory picks up new classes automatically. 86 on disk at the O-27 tip: 81 at the 2026-08-21 accepted tip, plus ChokepointUnbalancedGuardTest (P3 M1) and SeededChartManifestRequiredPurposeCompletenessTest (P3 M2) = 83, plus ChartPurposeBackfillSeederParityTest, LiveTenantChartValidationParityTest and BackfillChartRequiredPurposesO27MigrationTest (O-27 F-4); 87 with JournalEntryNumberingTenantScopeTest and 88 with InventoryGlNumberingTenantScopeTest (Session B2 lane B2-1, LEDGER C-27 + fix round r1)."
    },
    "Admin": {
      "lane": "feature-lane-tenancy/Admin",
      "classes": 9,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Api": {
      "lane": "feature-lane-platform-misc/Api",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Architecture": {
      "lane": "feature-lane-platform-misc/Architecture",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Authorization": {
      "lane": "feature-lane-tenancy/Authorization",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "BatchExpiry": {
      "lane": "feature-lane-inventory/BatchExpiry",
      "classes": 21,
      "note": "DELIBERATE MERGED-TREE RAISE 16 -> 17 (2026-09-02): DefaultLotExpiryDriftRegressionTest arrived with the local-dev K67 merge; the checker recomputed this group from the merged filesystem. This lane remains parked behind `vars.SELF_HOSTED_RUNNER_READY`, so its class ceiling remains enforced.",
      "raise_note_2026_09_10_wlota1a": "DELIBERATE RAISE 17 -> 21 (2026-09-10, W-LOT-A-1a plan rev 10 section 0). The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY; its ceiling remains enforced. Twelve PostgreSQL classes are explicitly selected in backend-test-pgsql, while BatchActionPermissionsTest and RoleProvisioningSourceSchemaTest remain outside that allowlist as ruled. Local per-file evidence is recorded in docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md; no observed CI run is claimed."
    },
    "Billing": {
      "lane": "feature-lane-tenancy/Billing",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Bootstrap": {
      "lane": "feature-lane-platform-misc/Bootstrap",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Broadcasting": {
      "lane": "feature-lane-platform-misc/Broadcasting",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Cart": {
      "lane": "feature-lane-pos/Cart",
      "classes": 6,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Catalog": {
      "lane": "feature-lane-catalog/Catalog",
      "classes": 35,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 33 -> 34 (union at merge; lane fork base 33) (2026-08-30, lane G-3a feat/g3a-company-scoped-sku): VariantIndexScopeTest pins sibling-company SKU allowance, tenant-wide barcode refusal, and create/restore 422 mapping against the repaired indexes. It is also named in the live backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-30; gate r2 G3A-R2-2) because this lane is PARKED, so that filter is the only live gate that can run it. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped.",
      "raise_note_2026_09_05": "DELIBERATE MERGED-TREE RAISE 34 -> 35 (2026-09-05, merge of teammate PR #215, 3259d49db): AddAttributeValueEndpointTest (attribute-value uniqueness scoped to real UUIDs — non-UUID attribute id is a 400 not a PG 22P02 500; second-company case per rule 22; 5 tests / 21 assertions green on sqlite AND PostgreSQL in gates r1-r2) arrived without the accompanying raise. Also added to the backend-test-pgsql --filter allowlist in the same commit (the PG 22P02 regression is the whole point and only executes on PostgreSQL) so it has a live CI gate while this lane is parked behind vars.SELF_HOSTED_RUNNER_READY."
    },
    "Channel": {
      "lane": "feature-lane-catalog/Channel",
      "classes": 9,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Communication": {
      "lane": "feature-lane-platform-misc/Communication",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Company": {
      "lane": "feature-lane-tenancy/Company",
      "classes": 34,
      "note": "DELIBERATE RAISE 33 -> 34 (2026-08-29, Lane F1 fix/f-bug-1-import-location): BackfillDefaultLocationCodeF1MigrationTest — the ONLY pin on the collision-safe, idempotent default-location MAIN code backfill. Named in the backend-pgsql --filter allowlist (same precedent as FiscalPeriodCloseEndpointTest below) because feature-lane-tenancy/Company is PARKED; the parked lane does not execute it until SELF_HOSTED_RUNNER_READY is enabled. DELIBERATE RAISE 32 -> 33 (2026-08-25, Session B2 lane C-24 (i) fix/sb2-c24-manual-period-close): FiscalPeriodCloseEndpointTest — the only pin on the five manual-close guards (Locked terminal / not Open / fiscal year closed / open predecessor / period not ended), the permission deny paths, and the INTEGRATION pin that reopen -> manual close releases the auto-lock hold on the fiscal year (the defect Q-10 knowingly left open). Named in the backend-pgsql --filter allowlist for the same reason as its reopen sibling below. DELIBERATE RAISE 31 -> 32 (2026-08-25, Session A merge of fix/campaign-w21-signup-stale-company — W2-1 CompanyContextMiddlewareTypedErrorsTest; gate record docs/superpowers/reviews/2026-08-25-w21-signup-gate-r1-tenancy.md). Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 30 -> 31 (2026-08-24 at merge, Session B lane Q-10 fix/sb-q10-fiscal-period-quickfixes): FiscalPeriodReopenEndpointTest — the only pin on the four reopen guards (Locked terminal / not Closed / fiscal year closed / Closed-or-Locked successor) + permission deny paths; gate docs/superpowers/reviews/2026-08-24-sb-q10-fiscal-periods-gate-r1.md. Named in the backend-pgsql --filter allowlist (gate C-3, B-3/Q-6/Q-7 precedent) because feature-lane-tenancy/Company is PARKED."
    },
    "CompanyConfig": {
      "lane": "feature-lane-tenancy/CompanyConfig",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Compliance": {
      "lane": "feature-lane-fiscal-finance/Compliance",
      "classes": 23,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. RAISED 22->23 (LEDGER C-6 item 3 / F-5, 2026-08-21): Nf525CanonicalZGrandTotalPeriodTotalsTest. Deliberate, and NOT a silent class — the same commit names it in the backend-test-pgsql --filter allowlist so it executes on PR->dev today; when this lane is unparked, drop that allowlist entry and lower this ceiling back."
    },
    "Console": {
      "lane": "feature-lane-data-console/Console",
      "classes": 18,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 15 -> 16 (2026-08-30, lane G-6a feat/g6a-import-claim-reaper, gate r3 SG-6-FU): ImportSchedulerRegistrationTest pins the two import maintenance sweeps to their exact cron expressions (`imports:reap-stuck` at `*/5 * * * *`, `imports:purge-expired` at `0 0 * * *`), so a silent edit to routes/console.php cannot leave abandoned imports stuck in `importing` or source files retained past the 90-day window with no failing test. It reads the in-memory Schedule and touches no database, so it needs no PostgreSQL leg and is deliberately NOT added to the backend-test-pgsql --filter allowlist. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped.",
      "raise_note_2026_09_10_wlota1a": "DELIBERATE RAISE 16 -> 18 (2026-09-10, W-LOT-A-1a plan rev 10 section 0). The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY; its ceiling remains enforced. Twelve PostgreSQL classes are explicitly selected in backend-test-pgsql, while BatchActionPermissionsTest and RoleProvisioningSourceSchemaTest remain outside that allowlist as ruled. Local per-file evidence is recorded in docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md; no observed CI run is claimed."
    },
    "Contact": {
      "lane": "feature-lane-documents/Contact",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "CountryDefaults": {
      "lane": "feature-lane-data-console/CountryDefaults",
      "classes": 28,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Coupon": {
      "lane": "feature-lane-pos/Coupon",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Dashboard": {
      "lane": "feature-lane-platform-misc/Dashboard",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Document": {
      "lane": "feature-lane-documents/Document",
      "classes": 94,
      "raise_note_2026_09_05": "DELIBERATE RAISE 92 -> 93 (2026-09-05, teammate PR #212 fix round 1, lane gate/pr-212): DocumentDueDateGuardTest — the only guard on DEV-QA-008/057, that a document can never be persisted with a due (or valid-until) date preceding its own document date. It pins all four holes this lane closed: the create path with the frontend's `issue_date` alias, the update path (which carried no relational rule at all), the PARTIAL `PATCH {due_date}` that Laravel's `after_or_equal:document_date` silently waves through when the referenced field is absent from the request (gate r1 F2), and the keystroke draft auto-save that wrote both dates straight onto the row (gate r1 F3) — plus the equality boundary, `valid_until`, and a SECOND COMPANY via `X-Company-Id` (CLAUDE.md rule 22). Red captured on sqlite before the fix (6 of 18 failed, `Expected response status code [422] but received 200`); green BY PATH on sqlite AND on PostgreSQL 16 (private throwaway DB autoerp_test_f212, port 5433). ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which does run on every event — the parked lane is where the class belongs, the allowlist is where it actually executes today; same precedent as CorrectingEntryEndpointTest / ProformaOutputTest / PurchaseOrderUnpricedLineConfirmTest above. Remove the allowlist entry and lower this ceiling when this lane's gate is flipped, not before. gated_ceiling 1245 -> 1246 (this one Document class; every other group's entry taken from local dev fa000edc3 verbatim). Handback docs/superpowers/reviews/2026-09-05-dhouha-pr-212-fix-round-1-handback.md; gate record docs/superpowers/reviews/2026-09-05-dhouha-pr-212-gate-r1.md.",
      "raise_note": "DELIBERATE RAISE 91 -> 92 (2026-08-29, lane fix/doc-number-company-scope): DocumentNumberingCompanyScopeTest proves the real two-company invoice create -> confirm -> post path, identical first legal numbers, and last_number=1 in both company sequences. Run BY PATH on SQLite AND PostgreSQL 16 (private DB autoerp_test_j, port 5433); paired with the Migrations raise below, gated_ceiling 1212 -> 1214, following G-3a.",
      "note": "DELIBERATE RAISE 90 -> 91 (2026-08-27, Session E lane fix/r2-deferred-document-numbering, LEDGER D-T9-1 / residual R-2): DeferredDocumentNumberingTest — the only guard on the deferred document-numbering contract: a draft is born with NO document_number and burns nothing (the wave-4 PO-2026-0001…0009 orphan shape), nine abandoned drafts leave QT-<year>-0001 for the next confirm, confirm allocates and successive confirms are sequential, an idempotent re-confirm does not re-allocate, a rollback mid-confirm returns the number to the sequence, a legacy numbered draft keeps its number, a draft that dies to Cancelled spends nothing, and the FISCAL INVARIANT that a delivery note is numbered BEFORE its hash input is serialized (a NULL in a sealed hash is unrecoverable). Run BY PATH on sqlite (8 tests, 35 assertions). NOT named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. gated_ceiling 1194 -> 1195 (this one Document class; every other group's entry taken from dev verbatim). === previous note carried forward === DELIBERATE RAISE 89 -> 90 (2026-08-26 at merge into dev by Session D; Session C lane feat/sc-f0w-web-proforma-parity, fiscal gate r1 F-1): ProformaResourceTest — the only test that pins `is_proforma` across nine document shapes (including the two the policy exists to refuse), that the proforma projection is attached iff the predicate holds, that the proforma figures are tax-inclusive and close over the estimated total, and that a definitive invoice keeps `subtotal`/`tax_amount`. Run BY PATH on sqlite (20 tests, 40 assertions) and against PostgreSQL via php artisan test tests/Feature/Document/ProformaResourceTest.php; NOT named in any live --filter allowlist before this fix round, so it executed nowhere. FIX ROUND r1 (gate r1 F-1): ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which does run on every event (PRs and main pushes; NOT push->dev — S-14 promotion leg owed) — the parked lane is where it belongs, the allowlist is where it actually executes today. Without it, `DocumentData::fromModel()` (~20 call sites, refactored often) could silently drop `is_proforma` or revert to a status heuristic and every unsealed invoice would render VAT and a `Posted` chip again with no CI signal. Same precedent as the C-F0 (ProformaOutputTest) / C-QR0a (StagedDeploymentBootTest) entries below. Remove the allowlist entry and lower this ceiling when this lane's gate is flipped, not before. gated_ceiling 1187 -> 1188 at merge (dev moved under this entry again: dev raised 88 -> 89 for ArApOpeningLedgerTest while this lane raised 88 -> 89 for ProformaResourceTest; union = 90). Gate record docs/superpowers/reviews/2026-08-26-sc-f0w-gate-r1-fiscal.md. === dev note carried forward === DELIBERATE RAISE 88 -> 89 (2026-08-25, Session A, W4-3/W4-4 gate r3 conditions): ArApOpeningLedgerTest, on top of dev's own count. Re-taken from the FILE SETS at this merge, as every round of this lane has been — the merged working tree holds 89 files matching tests/Feature/Document/*Test.php, dev holds 88, and the one-file delta is this lane's ArApOpeningLedgerTest. gated_ceiling is dev's 1185 + 1 = 1186. Do NOT add 1 to a stale number: dev has moved under this entry in FIVE consecutive rounds (84/1173 -> 86/1182 -> 88/1185), which is exactly why it is recomputed and never carried forward. ArApOpeningLedgerTest pins the AR/AP opening arc, that aged AR/AP net an opening credit note against an opening invoice (gate r1 C-1), and that an AR/AP batch refuses when a GL opening already stated its control account (I-3). Runs BY PATH on sqlite AND PostgreSQL 16 (throwaway autoerp_test_w43, port 5433). Handback docs/superpowers/reviews/2026-08-25-w43-handback.md. === dev note carried forward === DELIBERATE RAISE 84 -> 86 (2026-08-25, Session C lane feat/sc-f0-proforma-output, SPEC §2.4 / F-95): ProformaOutputTest and ProformaTemplateCensusTest — the confirmed-unposted rendering is a VAT-free proforma (no VAT/TVA/tax/TTC/HT label or amount, no rate column, no tax breakdown, no seal/hash/chain/QR, no 'posted' wording; 'Estimated total' + 'Proforma — non-fiscal document' in en/fr/ar), and the POSTED rendering is pinned unchanged against snapshots captured on the base commit. Run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_scf0, port 5433); NOT named in any live --filter allowlist, so they execute nowhere until this parked lane's gate is flipped. gated_ceiling 1176 -> 1178 at merge (union arithmetic re-derived at the merge commit: dev's 1176 + this lane's +2; every other group's entry taken from dev verbatim). FIX ROUND r1 (gate r1 F-3): BOTH classes are ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which does run on every event — the parked lane is where they belong, the allowlist is where they actually execute today. Without it the ONLY guards against re-introducing the Code TVA Art. 18 exposure on the first tenant's invoice printout would run on no CI event at all. Same precedent as CorrectingEntryEndpointTest / SupplierGoodsReturnNoteTest / PurchaseOrderUnpricedLineConfirmTest above. Remove the two allowlist entries when this lane's gate is flipped, not before. Handback docs/superpowers/reviews/2026-08-25-sc-f0-handback.md. DELIBERATE RAISE 82 -> 83 (2026-08-25, Session A lane fix/campaign-w26-po-price-default, gate r2 finding 2 / C2): PurchaseOrderUnpricedLineConfirmTest — the only guard that a purchase order with an UNPRICED line refuses to confirm (typed 422 PO_LINE_UNPRICED). W2-6's draft autosave deliberately writes '0' for a line the operator never priced, and past that persistence boundary nothing could tell it from a deliberate zero: CreateDocumentRequest's lines.*.unit_price => required|numeric ACCEPTS '0' (probed: '' and null rejected, '0' accepted), and PurchaseOrderService::confirm checked only type and draft status — so the detail page's Confirm button, which no FE guard can cover, turned an unpriced draft into a firm commitment that receives goods at Dr 37 0.000 and drags WAC down, with the three-way match raising only an ADVISORY price_variance under the shipped 'warn' policy. document_lines.is_bonus_line (the explicit zero-value flag) is the one exemption. Red captured on sqlite: 2 of 5 failed, 'Failed asserting that 200 is identical to 422'; green on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w26, port 5433). ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml — this parked lane would otherwise run it on no event, and a stock-valuation guard on the first tenant's purchasing path is not something to leave unarmed (same precedent as CorrectingEntryEndpointTest / SupplierGoodsReturnNoteTest). Remove the allowlist entry when this lane's gate is flipped, not before. gated_ceiling 1168 -> 1169 (this one Document class; the same lane's Product 57 -> 58 raise carried 1167 -> 1168). Gate record docs/superpowers/reviews/2026-08-24-w26-po-price-gate-r2.md. DELIBERATE RAISE 80 -> 82 (2026-08-24, Session A, N-6 fix round r1): PostingMarkerPrintTest (the owner-ruled not-yet-posted print marker, which shipped in r1 with no test at all — fiscal gate F-5) and DocumentStatusCheckConstraintParityTest (pgsql-only guard that chk_documents_status_enum and DocumentStatus::cases() describe the same value domain — treasury gate M-3; the CHECK is materialised at migration-run time and does NOT track the enum). gated_ceiling 1164 -> 1167 at merge (union arithmetic re-taken at the 2026-08-25 dev merge: dev's 1164 + this lane's +3). Handback docs/superpowers/reviews/2026-08-24-n6-handback.md §fix round r1. DELIBERATE RAISE 79 -> 80 (2026-08-24, Session A): RepairPaidNeverPostedDocumentsCommandTest — lane fix/campaign-n6-payment-advance (N-6/B-20 Phase 1, the `documents:repair-paid-never-posted` console command that moves invoices stuck at paid-with-no-fiscal_hash back to Confirmed and re-books their payment from the receivable (411) to the customer advance (419)); handback docs/superpowers/reviews/2026-08-24-n6-handback.md. Runs BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_n6, port 5433); NOT named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. gated_ceiling 1163 -> 1164 at merge (union arithmetic: dev's 1153 + this one Document class; every other group's entry taken from dev verbatim). Note the sibling N-6 classes do NOT land here: N6PaymentOnUnpostedInvoiceTest is tests/Feature/Treasury, DocumentStatusMachineTest is tests/Unit, and DocumentStatusWriteOnlyViaStatusServiceTest is tests/PHPStan — none of those is a ceilinged Feature group. DELIBERATE RAISE 77 -> 78 (2026-08-24, Session A): MissingStockLevelConfirmRefusalTest — lane fix/campaign-n2-stocklevel (N-2, absent stock_levels row refuses as a typed INSUFFICIENT_STOCK 422 on sales-order confirm, delivery-note confirm and the two invoice convenience endpoints, instead of a raw 404); handback docs/superpowers/reviews/2026-08-24-n2-handback.md. Runs BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_n2, port 5433); NOT named in any live --filter allowlist (grep-verified over .github/workflows/ and this manifest), so it executes nowhere until this parked lane's gate is flipped. gated_ceiling 1152 -> 1153 at merge (union arithmetic, gate r1 I-2: dev's 1152 after Session B's Q-2 and Q-6 raises + this one Document class; every other group's entry taken from dev verbatim). Gate record docs/superpowers/reviews/2026-08-24-n2-stocklevel-gate-r1-inventory.md (ACCEPT-with-conditions r1). DELIBERATE RAISE 78 -> 79 (2026-08-24, Session A merge 77610a8bb): AutoSaveDraftTaxConfigurationRateTest-family class from lane fix/campaign-n1-vat-resolution (N-1; autosave resolves line tax via DocumentLineTaxResolver, config outranks echoed rate); gate records docs/superpowers/reviews/2026-08-24-n1-vat-gate-r{1,2,3}-fiscal.md + -r1-treasury.md. Prior: Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. 74 -> 75 raised by lane r2f4-correcting-documents (LEDGER O-26 / S-15) for CorrectingEntryEndpointTest, the only guard on the admin-only `documents.correct` permission. Because this lane is parked, that class is ALSO named in the pgsql --filter allowlist, which does run on every event - the parked lane is where it belongs, the allowlist is where it actually executes today. DELIBERATE RAISE 75 -> 77 (2026-08-23, merge-discipline catch-up: both classes landed on dev WITHOUT the accompanying ceiling raise, which is why dev's own checker was red). (1) GoodsReceiptConversionUnbalancedGlNarrowingTest — lane fix/r10-catch-narrowing (R-10 per-site catch narrowing, the two live InvalidArgumentException arms), authored 1b459e876, merged d0e8b3cd9; gate record docs/superpowers/reviews/2026-08-23-r10-catch-narrowing-gate-r1.md (treasury gate r1 CHANGES -> comment-only fix round, parent-verified). (2) AutoSaveRouteHardeningTest — lane fix/p1-autosave-route-hardening (P1 auto-save hardening: authz + persisted-type binding + draft-only guard + validated payload + currency fix), authored d3dc9122b, merged 81e0948b9; gate records docs/superpowers/reviews/2026-08-23-p1-autosave-gate-authz-r1.md + -fiscal-r1.md + -r2.md + -r3.md (dual gates r1/r2 CHANGES -> r3 ACCEPT + docs micro). Unlike CorrectingEntryEndpointTest above, NEITHER of these two is named in any live --filter allowlist (verified by grep over .github/workflows/ and this manifest), so both genuinely execute nowhere until this parked lane's gate is flipped. DELIBERATE RAISE 83 -> 84 (2026-08-25 at merge, Session A lane W2-7, fix/campaign-w27-phantom-default-batch): BatchTrackedSalesOrderConfirmFefoTest — the ROUTE-level pins for campaign W2-7 and for wave-4 W4-5 / gate-r3 R3-1 / gate-r4 R4-1 / gate-r5 R5-1 (SO confirm reserves the FEFO lot and mints no phantom DEFAULT; DN confirm consumes FEFO lots with one inventory_batch_movements leg per lot; the strict-refusal shapes and their atomic rollback; the inbound restore that puts returned units back on the lot they left, on GRN-sourced lots, provenance-first). Run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w27, port 5433); not named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Handback docs/superpowers/reviews/2026-08-24-w27-handback.md. dev's W2-6 82 -> 83 entry above is taken VERBATIM (gate r5 R5-3: the union is a MERGE-TIME value — this is the third round dev moved under it; re-derive again if dev moves before the squash).",
      "raise_note_2026_09_05b": "DELIBERATE MERGED-TREE RAISE 93 -> 94 (2026-09-05, merge of teammate PR #214 F-STG-4, d56d62535): CreditNotePaidInvoiceSourceTest (credit-note source guard Posted OR Paid, computed-outstanding clamp, creditable filter, second-company case; 10 tests / 58 assertions green on sqlite AND PostgreSQL in gates r1-r3) arrived without the accompanying raise — the checker was not part of that PR's gate list, caught by the PR #212 r3 gate. Also added to the backend-test-pgsql --filter allowlist in the same commit so it has a live CI gate while this lane is parked behind vars.SELF_HOSTED_RUNNER_READY."
    },
    "DocumentIngestion": {
      "lane": "feature-lane-documents/DocumentIngestion",
      "classes": 6,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "EventSourcing": {
      "lane": "feature-lane-platform-misc/EventSourcing",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Events": {
      "lane": "feature-lane-platform-misc/Events",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Expense": {
      "lane": "feature-lane-fiscal-finance/Expense",
      "classes": 23,
      "note": "DELIBERATE RAISE 22 -> 23 (2026-08-25, Session A lane fix/campaign-w42-opening-cash-float, W4-10): ExpensePaidFromRepositoryTest — the only guard that an expense declared PAID IN CASH names the treasury repository the money left. ExpenseService::create() defaults is_paid to true, so a payload naming no repository minted an expense born paid with no till: posting credited the default cash GL account and wrote NO repository_movements row, and /pay (the only endpoint that accepts a repository) then refused it as already paid — GL cash fell 45.000 while the drawer never moved, permanently and irreversibly. The class also pins the positive shape (a 200.000-float drawer pays 5.000 and ends at 195.000, with the GL credit on the drawer's OWN gl_account_id) and the non-cash carve-out. Red captured on sqlite (4 of 6 failed: two expectException misses, one 201-where-422, one missing binding) and green on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w42, port 5433). Because this lane is PARKED behind vars.SELF_HOSTED_RUNNER_READY, the class is ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml — same precedent as CorrectingEntryEndpointTest, SupplierGoodsReturnNoteTest and ResolveLineEntryCodeCostRedactionTest: a money-lane regression pin that executes on no event is not a pin. Remove the allowlist entry when this lane's gate is flipped, not before. gated_ceiling 1175 -> 1176 at merge (UNION arithmetic, RE-TAKEN a THIRD time at the 2026-08-25 condition-round dev merge: dev has moved 1169 -> 1173 -> 1175 while this lane was in review, and the resolved value is always dev's CURRENT ceiling plus this lane's one Expense class — never the number the lane last wrote. Document 84 / Inventory 115 / POS 155 and every other group are taken from dev verbatim; only Expense 22 -> 23 is this lane's. Do NOT resolve this line to 1170, 1174 or 1175. Document and Inventory entries are taken from dev verbatim; every other group's entry likewise. The W4-2 sibling class tests/Feature/Accounting/OpeningCashFloatSeedsRepositoryTest does NOT land here — Accounting is a live lane and carries no ceiling). Handback docs/superpowers/reviews/2026-08-25-w42-handback.md. Prior: Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Fiscal": {
      "lane": "feature-lane-fiscal-finance/Fiscal",
      "classes": 83,
      "note": "DELIBERATE RAISE 81 -> 82 (2026-08-25, Session A lane W4R-2, fix/campaign-w4r2-live-pos-projection-lots): PosCoreReceiptProjectionBatchLotTest — the LIVE POS receipt projection had no FEFO call and no batch write of any kind, so every device-authored sale decremented stock_levels and left the lot ledger untouched (wave-4 re-run tenant: 27 units of drift, 100 % of it live POS sales/refunds; pos_receipt_line_batch_allocations held ZERO rows, so BatchTraceabilityController's recall trail and ReceiptReturnService's per-line lot provenance were both reading an empty table). W2-7/W4-5 HAD built the FEFO arms — on ReceiptCreationService, which serves POST /pos/receipts, retired at 410. The class pins the fixed contract: FEFO draw + per-lot inventory_batch_movements leg + per-lot allocation snapshot on a sale; provenance-first lot credit on a refund (the lot the sale took, not the lot that shipped last); Sigma lots == stock_levels after every receipt AND refund; replay writes no second leg; a lot shortfall never REJECTS an already-signed event (Model 1 §4.1); disposition=scrap credits NO lot (DPA V10 parity — its write-off leg writes no lot leg either); sealed canonical bytes and chain hash byte-identical. SEVEN of its EIGHT tests are PostgreSQL-ONLY by construction and skip honestly elsewhere: FEFOInventoryService::consumeBatchesAtomically() selects FOR UPDATE OF ibs SKIP LOCKED, which IS the concurrency contract and is not something to branch away for a test driver. That is why the class is ALSO named in the live backend-test-pgsql --filter allowlist in .github/workflows/ci.yml — same precedent as SaleReceiptV5PostRemiseVatBaseTest (D-1) and PosReceiptFrozenStateImmutabilityTriggerTest (Q-6 gate r2): while this lane is PARKED behind vars.SELF_HOSTED_RUNNER_READY the allowlist is the ONLY place it executes, and for this class the pgsql lane is also the only place it can. Remove the allowlist entry when this lane's gate is flipped, not before. Green on PostgreSQL 16 (throwaway autoerp_test_w4r2, port 5433) 8/8; on sqlite 1 passed + 7 skipped. gated_ceiling 1186 -> 1187 (fix round r1, gate F-5): RE-TAKEN at the 2026-08-25 fix-round `merge dev` because W4-3 landed on dev in the meantime and claimed the SAME 1185 -> 1186 step (Document 88 -> 89). Both lanes could not have 1186; the second to merge resolves the union, and that is this lane. dev cb6a86529 carries gated_ceiling 1186 / Document 89 / Fiscal 81, so the union is dev's CURRENT 1186 plus this lane's ONE Fiscal class = 1187. Re-take it AGAIN at every subsequent dev merge and never resolve it to a number this lane wrote in an earlier round (1186 is now a stale value from round 0). Handback docs/superpowers/reviews/2026-08-25-w4r2-handback.md. Prior: DELIBERATE RAISE 80 -> 81 (2026-08-25, Session A lane D-1, fix/campaign-d1-pos-vat-discount-base): SaleReceiptV5PostRemiseVatBaseTest — the v5 payload contract for the post-remise VAT base (owner ruling 2026-08-25 (a)): the worked example accepted, the pre-discount base REFUSED at v5 and still ACCEPTED at v3 (forward-only), a base that is not a true split of the sealed lines refused, and the 100 %-comp empty-payments carve-out present at v5 / absent at v3. Runs BY PATH on sqlite and on PostgreSQL 5433 in-lane; red-proved by pinning SALE_RECEIPT_POST_DISCOUNT_BASE_VERSION out of reach (8 of 12 fail). GATE r1 FINDING 6 CLOSED: it IS now named in the live backend-test-pgsql --filter allowlist in .github/workflows/ci.yml (Q-7 / B-3 precedent), so it executes on PR->dev on real PostgreSQL; the parked lane is its second home. gated_ceiling is UNION arithmetic re-taken at EVERY dev merge — dev moved 1173 -> 1174 -> 1176 -> 1180 while this lane was in review, so the resolved value is dev's CURRENT ceiling plus this lane's TWO classes (one Fiscal, one POS) = 1182 at the r2 condition round. Never a number this lane wrote in an earlier round. Prior: Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 79 -> 80 (2026-08-23, Session B lane Q-6): PosReceiptFrozenStateImmutabilityTriggerTest — lane fix/sb-q6-voided-receipt-trigger (pos_receipts immutability trigger becomes whitelist-with-ELSE-RAISE), authored ad2c13325, which landed the class WITHOUT the accompanying raise and left the checker red; caught and attributed in the same lane's gate-r1 fix round. Gate record docs/superpowers/reviews/2026-08-23-sb-q6-trigger-gate-r1.md. The class is NOT named in either live --filter allowlist in .github/workflows/ci.yml (grep-verified; its sibling PosReceiptImmutabilityTriggerSealedHashAlgorithmTest IS), so it executes nowhere until this parked lane's gate is flipped — flagged to the parent as a CI-reachability residual, since it is the only regression pin on the fiscal receipt trigger's whitelist. gated_ceiling 1151 -> 1152 at merge (union: dev's 1151 after Q-2 merge 850e86217 + this one Fiscal class; POS/Inventory entries taken from dev verbatim; gate r2 docs/superpowers/reviews/2026-08-24-sb-q6-trigger-gate-r2.md). The class IS now named in the backend-pgsql --filter allowlist (gate r2 F-7, B-3 precedent)."
    },
    "Http": {
      "lane": "feature-lane-platform-misc/Http",
      "classes": 2,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Identity": {
      "lane": "feature-lane-tenancy/Identity",
      "classes": 40,
      "note": "DELIBERATE RAISE 31 -> 32 (2026-08-29, P0 micro-lane fix/migration-echo-p0): RegistrationResponseIsPureJsonTest pins the real POST /api/v1/auth/register response as parseable JSON and deterministically re-drives Stancl MigrateDatabase after forgetting only the eight census migrations, proving synchronous migration output stays out of PHPUnit/HTTP buffers. RED captured with the first 120 characters beginning `[W4-1] invented DEFAULT-lot expiries found: 0`; GREEN after routing all census emission through MigrationOutput. Run BY PATH on SQLite and PostgreSQL in-lane; not named in a live --filter allowlist. gated_ceiling 1212 -> 1213 is exactly this one parked Identity class. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped.",
      "raise_note_2026_09_10_wlota1a": "DELIBERATE RAISE 32 -> 39 (2026-09-10, W-LOT-A-1a plan rev 10 section 0). The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY; its ceiling remains enforced. Twelve PostgreSQL classes are explicitly selected in backend-test-pgsql, while BatchActionPermissionsTest and RoleProvisioningSourceSchemaTest remain outside that allowlist as ruled. Local per-file evidence is recorded in docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md; no observed CI run is claimed.",
      "raise_note_2026_09_10": "DELIBERATE RAISE 32 -> 33 (2026-09-10, lane lane/rbac-w0a-s1-permission-cache, gate r1 fix round 1): PermissionCacheTenantScopingTest — the only pin that the Spatie permission cache is keyed PER TENANT under database-per-tenant. Before RBAC-W0a-S1 every tenant read and wrote the one shared `spatie.permission.cache` entry (CacheTenancyBootstrapper swaps the container's `cache` binding, but PermissionRegistrar resolves its store from its own injected CacheManager, so tenant tags never applied), so tenant B could be served tenant A's permission snapshot from a shared Redis store — a P0 cross-tenant authorization defect. The class pins the HTTP/resolver path (compat mode stays on the shared base key; two provisioned tenants each get `spatie.permission.cache.<uuid>` and central is restored on end), the real database-queue path (two dispatched jobs observed under two real `queue:work --once` runs), and — added in this fix round — the DATA MEANING: a permission created only in tenant A's database is invisible to the registry under tenant B on a shared non-array cache store, and visible again on return to A. Red captured by unregistering the two listeners (tenant B saw tenant A's permission); green with them. This class is DARK in this parked lane until `vars.SELF_HOSTED_RUNNER_READY` flips, so it is ALSO named in the LIVE `t6-phase0b-pgsql` --filter allowlist in .github/workflows/ci.yml (same precedent as ExpensePaidFromRepositoryTest and SpreadsheetParserDateCellTest, but on the Phase 0b gate rather than backend-test-pgsql, whose filter two in-flight lanes are already editing): it executes on PR->dev, PR->main, push->main and workflow_dispatch on real PostgreSQL 16 with real per-tenant databases — i.e. it is NOT dark in CI. Remove the allowlist entry when this lane's gate is flipped, not before. Run BY PATH in-lane on private PostgreSQL 16 `autoerp_test_r` (port 5433). gated_ceiling 1253 -> 1254 is exactly this one parked Identity class; recompute (do not textually merge) it against whichever of lane/w-lot-a-1a or lane/t2-receipt-spine lands first. Gate record docs/superpowers/reviews/2026-09-10-rbac-w0a-s1-gate-r1.md."
    },
    "Import": {
      "lane": "feature-lane-data-console/Import",
      "classes": 40,
      "k7_note": "UNION AT K67/K89 MERGE (2026-09-02): ImportTimeEnrichmentTest raises Import 38 -> 39. ProductBarcodeUniquenessTest, one BatchExpiry class, and five Inventory classes also enter the merged tree; the checker-recomputed gated ceiling is 1243. The warning-code contract unions the enrichment cases with the K-89 multi-location and numeric-boundary cases without touching ImportErrorCode.",
      "rebase_note": "UNION AT REBASE (2026-08-31): G-4 adds eight Import classes on top of dev's G-3b/G-6a ceiling of 30, so the merged Import ceiling is 38; all G-4, G-3b, and G-6a parked-lane proofs share the one anchored PostgreSQL allowlist.",
      "note": "DELIBERATE RAISE 25 -> 27 (2026-08-30, lane G-3b feat/g3b-import-company-pin): ImportCompanyPinTest and ImportModuleEntitlementTest pin upload attribution/hash, all eight company-mismatch surfaces, current-company plus unattributed history, queued company/tenant arguments, and all ten CompositeItems entitlement surfaces. Both are named in the live backend PG allowlist because the Import lane remains parked. === prior note === DELIBERATE RAISE 18 -> 23 (2026-08-29, lane G-7 feat/g7-import-coverage-baseline): ProductsRoundTripTest, PartiesRoundTripTest, OpeningBalancesRoundTripTest, CompositeItemsRoundTripTest, and ProductImagesZipRoundTripTest — the first true HTTP round-trip per live import type; composite_items had zero coverage. All five are named in the backend-test-pgsql --filter allowlist (gate r1 fix round 1, M-3) for the same reason as SpreadsheetParserDateCellTest below: the feature lane is PARKED, so that filter is the only live gate that can run them — and this lane is the acceptance net the eleven following imports-hardening lanes are graded against. DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 23 -> 24 (union at merge; lane fork base was 18) (2026-08-30, lane G-12 feat/g12-units-invariant): UnitsNotSeededRefusalTest — pins the coded upload refusal before job creation and the no-CompanyContext worker re-check for unit-bearing import types. Named in the backend-test-pgsql --filter allowlist because Import is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` and otherwise executes on no event. === prior note === DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 24 -> 25 (union at merge; lane fork base 18) (2026-08-30, lane G-3a feat/g3a-company-scoped-sku): ProductSkuCompanyScopeImportTest pins same-file sibling-company imports and second-location stock upserts without duplicate products. It is also named in the live backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-30; gate r2 G3A-R2-2) because this lane is PARKED, so that filter is the only live gate that can run it. === prior note === DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 27 -> 30 (2026-08-30, lane G-6a feat/g6a-import-claim-reaper): ImportJobClaimConcurrencyTest, ReapStuckImportsTest, and PurgeExpiredImportArtifactsTest pin atomic claim/CAS finalization, two-clock reaping, source retention/download, and discard DELETE. All three join the live backend PG allowlist because the Import lane remains parked. === prior note === DELIBERATE RAISE 25 -> 27 (2026-08-30, lane G-3b feat/g3b-import-company-pin): ImportCompanyPinTest and ImportModuleEntitlementTest pin upload attribution/hash, all eight company-mismatch surfaces, current-company plus unattributed history, queued company/tenant arguments, and all ten CompositeItems entitlement surfaces. Both are named in the live backend PG allowlist because the Import lane remains parked. === prior note === DELIBERATE RAISE 18 -> 23 (2026-08-29, lane G-7 feat/g7-import-coverage-baseline): ProductsRoundTripTest, PartiesRoundTripTest, OpeningBalancesRoundTripTest, CompositeItemsRoundTripTest, and ProductImagesZipRoundTripTest — the first true HTTP round-trip per live import type; composite_items had zero coverage. All five are named in the backend-test-pgsql --filter allowlist (gate r1 fix round 1, M-3) for the same reason as SpreadsheetParserDateCellTest below: the feature lane is PARKED, so that filter is the only live gate that can run them — and this lane is the acceptance net the eleven following imports-hardening lanes are graded against. DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 23 -> 24 (union at merge; lane fork base was 18) (2026-08-30, lane G-12 feat/g12-units-invariant): UnitsNotSeededRefusalTest — pins the coded upload refusal before job creation and the no-CompanyContext worker re-check for unit-bearing import types. Named in the backend-test-pgsql --filter allowlist because Import is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` and otherwise executes on no event. === prior note === DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 24 -> 25 (union at merge; lane fork base 18) (2026-08-30, lane G-3a feat/g3a-company-scoped-sku): ProductSkuCompanyScopeImportTest pins same-file sibling-company imports and second-location stock upserts without duplicate products. It is also named in the live backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-30; gate r2 G3A-R2-2) because this lane is PARKED, so that filter is the only live gate that can run it. === prior note === DELIBERATE RAISE 17 -> 18 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): SpreadsheetParserDateCellTest — the only pin that an XLSX cell Excel typed as a Date is read as Y-m-d rather than as a raw Excel serial. Read raw, the serial fails expiry_date's date_format:Y-m-d rule and marks the WHOLE product row invalid, which was the default outcome for any operator who edited the official template in Excel. Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 16 -> 17 (2026-08-23, Session B): ImportReExecutionGuardTest — lane fix/sb-q1-import-reexecution-guard (Q-1, import execute re-arm closed: typed 422 precondition, Pending-before-dispatch, worker conditional claim, is_imported row filter), authored 649514944; imports gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md. Runs BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips.",
      "imp1_note": "DELIBERATE RAISE 39 -> 40; gated union 1254 -> 1255 (IMP-1). ImportRowExportTest covers correction CSV/XLSX, mapping injectivity, source layout, reimport and company isolation on SQLite and PostgreSQL. Named in backend-test-pgsql while the Import lane remains parked."
    },
    "Income": {
      "lane": "feature-lane-fiscal-finance/Income",
      "classes": 4,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 3 -> 4 by Session B2 lane B2-1 (LEDGER C-27), with the class renamed in fix/doc-number-company-scope round 1 from IncomeNumberingTenantScopeTest to IncomeNumberingCompanyScopeTest without changing the 4-class ceiling; it now pins independent sibling-company sequences plus company-keyed advisory locking. Run BY PATH on SQLite AND PostgreSQL 16 (private DB autoerp_test_j, port 5433)."
    },
    "Inventory": {
      "lane": "feature-lane-inventory/Inventory",
      "classes": 141,
      "raise_note_2026_09_08_qa_bug_09": "DELIBERATE RAISE 126 -> 127 (2026-09-08, QA-BUG-09 / DEV-QA-077, lane fix/count-movement-reference-id): CountingMovementReferenceTest pins the two contracts of a count movement's traceability — the LABEL (a replay count-correction AND an onboarding first-count opening both carry the bare counting number, both audit events carry it, and a caller supplying no label still falls back to COUNT_REPLAY) and the LINKAGE (GET /api/v1/stock-movements exposes reference_id and resolves source_document_id/type = the counting), plus a SECOND-COMPANY negative (company B's payload contains neither company A's counting id nor its number), a SECOND-LOCATION case (two countings, two distinct references) and a listener-rerun idempotency case. Verified on PostgreSQL 16 via the local docker stack (127.0.0.1:5433, autoerp_lane_test) — the PG run is what caught a locations.code varchar(20) overflow that sqlite silently accepts. The Inventory group is PARKED behind vars.SELF_HOSTED_RUNNER_READY, so without this raise the checker is red on every CI run. The class IS named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml (same precedent as CountingIndexStatusFilterTest and StockTransferIdempotencyCollisionPostgresTest), so it has a live CI gate on real PostgreSQL while this lane stays parked. CAVEAT (gate r1 Minor-1): `backend-test-pgsql` runs on workflow_dispatch / base_ref==main / base_ref==dev / push->main — NOT on push->dev, so this gate is live on PR runs of that workflow and on pushes to main, not on a direct dev promotion. Remove the allowlist entry and lower this ceiling when the lane's gate is flipped, not before. gated_ceiling 1249 -> 1250.",
      "raise_note_2026_09_07_pr218": "DELIBERATE RAISE 125 -> 126 (2026-09-07, PR #218, lane fix/counting-list-status-filter-sync): CountingIndexStatusFilterTest pins the status/overdue filter vocabulary of the counting index endpoint (the `status=active` alias resolving to the three `count_N_in_progress` statuses via scopeActive(); `overdue=true` / `status=overdue` = active AND past scheduled_end, with overdue winning precedence over an exact status; exact `CountingStatus` values filtering verbatim; unknown values tolerated -> unfiltered list; plus a second-company pin that none of those branches leaks another company's rows, gate r1 MAJOR-2) against the production PostgreSQL driver. The Inventory group is PARKED behind vars.SELF_HOSTED_RUNNER_READY — without this raise the checker would be red on every CI run. The class IS wired into the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml (same precedent as StockTransferIdempotencyCollisionPostgresTest, W4-1, G-12, G-7, G-3a), so it has a live CI gate on real PostgreSQL even while this lane remains parked. Remove the allowlist entry and lower this ceiling when the lane's gate is flipped, not before. gated_ceiling 1248 -> 1249.",
      "merge_note": "DELIBERATE MERGED-TREE RAISE 118 -> 123 (2026-09-02): GoodsReceiptBatchExpiryConflictTest, GoodsReceiptErrorMessageHygieneTest, GoodsReceiptExpiredLotPolicyTest, GoodsReceiptReceiveAllBatchDataTest, and GoodsReceiptVariantBatchScopingTest arrived with the local-dev K67 merge; the checker recomputed this group from the merged filesystem.",
      "note": "DELIBERATE RAISE 116 -> 118 (2026-08-26, campaign W4-1 fix/campaign-w4-1-opening-lot-expiry, gate r1 fix round): OpeningLotExpiryW41Test (opening lots are minted UNDATED when nobody supplies an expiry, FEFO ranks an undated lot LAST and still SEES it, and the opening wizard's expiry_date column validates/maps/posts) and NullInventedDefaultLotExpiryMigrationTest (the backfill predicate in BOTH directions across five lot shapes — its PostgreSQL branch runs a different SQL string from its SQLite branch, so without the CI entry the branch that executes on every real tenant migrate ran nowhere). Named in the backend-test-pgsql --filter allowlist (added in fix round 2, 2026-08-26 — round 1 asserted this before the edit existed; gate r1 OPEN-1) for the same reason as the W2-7 lot-provenance pins: the feature lane is PARKED, so that filter is the only live gate that can run them. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. 106 -> 108 raised by lane dpa-v8-supplier-goods-return (DPA V8) for SupplierGoodsReturnNoteTest and SupplierGoodsReturnNotesSchemaTest, the only guards on the new supplier goods-return note document. BOTH are also named in the backend-pgsql job's --filter allowlist (gate P2-2), for the same reason r2f4 named CorrectingEntryEndpointTest: while this lane is parked they would otherwise execute on no event, and SupplierGoodsReturnNoteTest carries the two deliberately failing-visible C-11 residual pins (:601, :656) whose whole purpose is to go red when F-9 derives the ceiling from a real cost basis. An unarmed tripwire is not a tripwire. Remove the allowlist entries when the lane's gate is flipped, not before. DELIBERATE RAISE 108 -> 111 (2026-08-24, Session B lane Q-2, fix/sb-q2-counting-finalize-lock): CountingFinalizeLockTest + CountingSubmitCountRaceTest (authored 1ec42fc43, which landed on the lane branch WITHOUT the accompanying raise) + CountingTerminalStateGuardTest (fix round 600ccbc28; gate r1 terminal-state probes A/B/C for submitCount/triggerThirdCount/cancel plus the FOR-UPDATE structural sentinel). Gate records docs/superpowers/reviews/2026-08-23-sb-q2-counting-gate-r1.md + 2026-08-24-sb-q2-counting-gate-r2.md. None of the three is named in any live --filter allowlist, so all three execute nowhere in CI until this parked lane's gate is flipped — they are run BY PATH on sqlite and on PostgreSQL (DB_PORT=5433) in-lane, per the Session B lane protocol. gated_ceiling 1148 -> 1151 at merge (union arithmetic per gate r2 NEW-1: dev's 1148 after Session A's N-5 raise + these three Inventory classes; POS entry taken from dev verbatim). DELIBERATE RAISE 111 -> 113 (2026-08-24, Session A lane W2-7, fix/campaign-w27-phantom-default-batch): ImplicitReservationFefoLotTest (the implicit-reservation policy — DEFAULT lot carries only the untracked remainder stock_levels.quantity - Sigma(real lots), clamped at zero; the lot is then chosen FEFO) and RepairPhantomDefaultBatchesCommandTest (the inventory:repair-phantom-default-batches dry-run/execute repair path, its scope refusals, and its document-per-action stock_movements + inventory_batch_movements pair). Both run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w27, port 5433) in-lane; neither is named in any live --filter allowlist, so both execute nowhere until this parked lane's gate is flipped. Handback docs/superpowers/reviews/2026-08-24-w27-handback.md. DELIBERATE RAISE 113 -> 114 (2026-08-24, Session A lane W2-7 addendum): SiblingSeamsUntrackedRemainderTest — one pin per sibling seam that commit 9162dfdb0 gave the same phantom-DEFAULT shape (StockAdjustmentService::receive, ::adjust, the inventory-counting completed listener, and the ProductController requires_batch_tracking backfill), all four now routed through BatchStockService::ensureDefaultBatchForUntrackedRemainder(). Run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w27, port 5433); not named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Handback docs/superpowers/reviews/2026-08-24-w27-handback.md (addendum). DELIBERATE RAISE 114 -> 115 (2026-08-25, Session A lane W4-6, fix/campaign-w46-counting-basket-window): CountingVarianceAppliedTest — the W4-6 contract that a finalized count APPLIES its variance (basket_window annotates instead of suppressing; one adjustment movement per varying line and none for an agreeing line; a batch-tracked shortage draws the lot ledger down FEFO; the report carries expected/counted/variance/applied and a summary that agrees with its own rows; the Session B Q-2 finalize lock still refuses the second attempt and the now-applied variance posts exactly once). Run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_w46, port 5433); not named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Handback docs/superpowers/reviews/2026-08-25-w46-handback.md. DELIBERATE RAISE 115 -> 116 (2026-08-25, Session A lane P-1, fix/campaign-p1-count-correction-gl-default): CountCorrectionGlPostingDefaultTest — the P-1 SETTING contract (owner ruling 2026-08-25, superseding the OQ-12/H-5 deploy blocker): the seeded per-country `count_correction_gl_posting_enabled` default, the seeder's pin, the company -> country -> system resolution chain and its `source`, the backfill migration's predicate (raises a country row still on the pre-ruling false; NEVER writes the nullable company override, so a tenant that set false keeps false and an untouched tenant keeps NULL), and the settings surface (GET reports resolved/source/override; PATCH takes a nullable boolean where null clears the override). The GL SHAPE at the shipped default lives in the existing [PG] CountCorrectionGlPostingTest, which gained two re-pinned cases and NO new class. Run BY PATH on sqlite AND on PostgreSQL 16 (throwaway autoerp_test_p1, port 5433); not named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Handback docs/superpowers/reviews/2026-08-25-p1-handback.md. gated_ceiling union at merge: dev had already moved 1174 -> 1175 for its own two classes, so this lane's +1 always lands on top of whatever dev last set, never re-claiming the same slot: dev reached 1179 (W2-1 among them), so the union at the final merge is 1180. Gate r1 F-6 called this out as the silent failure mode; it fired for real, as a genuine merge conflict on this line, and was resolved as a union rather than a pick-one.",
      "raise_note_2026_09_05": "DELIBERATE MERGED-TREE RAISE 123 -> 125 (2026-09-05, request-hygiene Phase A promotion reconciliation): SubmitCountQuantityScaleTest arrived on origin/dev with lane N-1 (6e47bde17, 2026-09-02) WITHOUT the accompanying raise, so the checker has been red on every PR→dev run since; StockTransferIdempotencyCollisionPostgresTest arrived with Phase A Task 13 (`lane/rh-t13-transfer-adjustment-idempotency`, merged f85b7c0e9) and IS named in the backend-test-pgsql --filter allowlist (real dual-index PG collision at transaction levels 0 and 1), so it is the one class in this raise with a live CI gate. This lane remains parked behind `vars.SELF_HOSTED_RUNNER_READY`, so its class ceiling remains enforced.",
      "raise_note_2026_09_09_t1": "DELIBERATE RAISE 127 -> 129 (2026-09-09, T-1 transfers/requests edge campaign). StockTransferEdgeCasesTest and StockTransferCompleteConcurrencyPostgresTest added. The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY. All three T-1 classes are explicitly selected in backend-test-pgsql; completion locking and the reopen partial unique are PostgreSQL-only. Four known failing behavior tests remain ticket-linked skips, reproducible with T1_RUN_KNOWN_REDS=1. Local per-file evidence: docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md. No observed CI run is claimed.",
      "raise_note_2026_09_09_t2t3_s1": "DELIBERATE RAISE 129 -> 141 (2026-09-09, lane T-2/T-3 slice S1, lane/t2-receipt-spine). Twelve classes added: StockTransferReceiveTest, StockTransferReceiveDamageTest, StockTransferReceiveLotsTest, StockTransferReceiveValidationTest, StockTransferCloseTest, StockTransferReceiveConcurrencyPostgresTest, TransferReceiptEventStreamTest, TransferReceiptGlBoundaryTest, TransferReceiptPatternQueryPostgresTest, TransferLegacyCompletionBackfillTest, TransferReceiptSecondOfEverythingTest, TransferReceiptAuthorityGateTest. The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY, so without this raise tools/feature-lane-manifest-check.php is red on every CI run. All twelve are explicitly selected in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which is their only live gate while the lane is parked. TWO of the twelve are wholly PostgreSQL-only by construction: StockTransferReceiveConcurrencyPostgresTest (advisory-lock and row-lock races across two OS processes) and TransferReceiptPatternQueryPostgresTest (jsonb operators, aggregate FILTER clauses, bool_or). A THIRD, TransferLegacyCompletionBackfillTest, is cross-lane: it runs on both lanes and exactly ONE of its methods, test_interrupted_backfill_rolls_back_and_the_next_run_completes, is PostgreSQL-only and is mark-skipped on SQLite. TransferReceiptSchemaRerunPostgresTest is NOT an Inventory class: it is in the Migrations group and is named in that group's own raise note. CAVEAT: backend-test-pgsql runs on workflow_dispatch / base_ref==main / base_ref==dev / push->main - NOT on push->dev, so this arms the classes on PR runs and main pushes, not on a direct dev promotion. Remove the allowlist entries and lower this ceiling when the lane's gate is flipped, not before. Local per-file evidence: docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md. No observed CI run is claimed. gated_ceiling 1253 -> 1267."
    },
    "Jobs": {
      "lane": "feature-lane-platform-misc/Jobs",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Location": {
      "lane": "feature-lane-inventory/Location",
      "classes": 6,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Loyalty": {
      "lane": "feature-lane-pos/Loyalty",
      "classes": 18,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 17 -> 18 (2026-08-23): RedemptionDoubleSpendTest — Session B lane Q-3 (fix/sb-q3-loyalty-redemption-lock), closing the unlocked absolute-value redeem write (POS retail-till sub-report HIGH). The class runs BY PATH on both sqlite and PostgreSQL in-lane; its PG-only FOR UPDATE assertion markTestSkipped-s on sqlite, which is exactly the behaviour the parked lane exists to un-park."
    },
    "Marketplace": {
      "lane": "feature-lane-catalog/Marketplace",
      "classes": 8,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Menu": {
      "lane": "feature-lane-catalog/Menu",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Migration": {
      "lane": "feature-lane-data-console/Migration",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Migrations": {
      "lane": "feature-lane-data-console/Migrations",
      "classes": 16,
      "raise_note": "DELIBERATE RAISE 13 -> 14 (2026-08-30, lane fix/je-number-company-scope): JournalEntryNumberCompanyScopeMigrationTest follows the DocumentNumberCompanyScopeMigrationTest precedent and pins census-before-drop refusal, exact named unique replacement, non-unique tenant lookup preservation, sibling-company reuse, savepoint-isolated same-company 23505 rejection, rollback refusal, and idempotent re-entry. Run BY PATH on SQLite AND PostgreSQL 16 (private DB autoerp_test_j4, port 5433); this parked Migrations class raises gated_ceiling 1223 -> 1224. === prior raise === DELIBERATE RAISE 12 -> 13 (2026-08-30 at merge into dev, lane fix/doc-number-company-scope): DocumentNumberCompanyScopeMigrationTest on top of dev's 12 (G-3c + G-3b unions re-taken from the file set). Lane note: DELIBERATE RAISE 10 -> 11 (2026-08-29, lane fix/doc-number-company-scope): DocumentNumberCompanyScopeMigrationTest pins lifetime census refusal, exact named index replacement, sibling-company uniqueness, rollback refusal, and idempotent re-entry. Run BY PATH on SQLite AND PostgreSQL 16 (private DB autoerp_test_j, port 5433); paired with the Document raise above, gated_ceiling 1212 -> 1214, following G-3a.",
      "rebase_note": "UNION AT REBASE (2026-08-30): G-3b ImportJobCompanyBackfillMigrationTest and G-3c BackfillCompanyPaymentRepositoriesMigrationTest both extend the prior ceiling of 10, so the merged ceiling is 12; the G-3c PostgreSQL proof is also kept live in the backend allowlist.",
      "note": "DELIBERATE RAISE 10 -> 11 (2026-08-30, lane G-3b feat/g3b-import-company-pin): ImportJobCompanyBackfillMigrationTest pins guarded, idempotent, forward-only M4 and unanimous-evidence attribution on SQLite and PostgreSQL; it is named in the live backend PG allowlist because this lane remains parked. === prior note === DELIBERATE RAISE 6 -> 7 (2026-08-28 at merge, Session E I-1 follow-ups): the payment_methods unique re-scope census/migration pin (company_id, code). Runs BY PATH on sqlite AND PG; parked lane, ceiling enforced. Gate record docs/superpowers/reviews/2026-08-28-session-e-i1-followups-gate-r1-treasury.md. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 5->6 (2026-08-23, triage lane F4): DocumentsSourceDocumentIdIndexTest, the forward/rollback/idempotency proof for the `documents.source_document_id` index. It is PG-only and CONCURRENTLY-bearing, so it cannot use RefreshDatabase and skips on SQLite — this parked lane's real-PostgreSQL runner is the only place it will ever execute. It was run green by hand against PG 16 on the lane worktree; see the F4 commit body. DELIBERATE RAISE 7 -> 10 (union at merge; lane fork base 7) (2026-08-30, lane G-3a feat/g3a-company-scoped-sku): three migration classes pin collision-first, idempotent company-scoped product SKU, variant SKU, and partner VAT repairs on SQLite and PostgreSQL. === prior note === DELIBERATE RAISE 6 -> 7 (2026-08-28 at merge, Session E I-1 follow-ups): the payment_methods unique re-scope census/migration pin (company_id, code). Runs BY PATH on sqlite AND PG; parked lane, ceiling enforced. Gate record docs/superpowers/reviews/2026-08-28-session-e-i1-followups-gate-r1-treasury.md. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 5->6 (2026-08-23, triage lane F4): DocumentsSourceDocumentIdIndexTest, the forward/rollback/idempotency proof for the `documents.source_document_id` index. It is PG-only and CONCURRENTLY-bearing, so it cannot use RefreshDatabase and skips on SQLite — this parked lane's real-PostgreSQL runner is the only place it will ever execute. It was run green by hand against PG 16 on the lane worktree; see the F4 commit body.",
      "raise_note_2026_09_10_wlota1a": "DELIBERATE RAISE 14 -> 15 (2026-09-10, W-LOT-A-1a plan rev 10 section 0). The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY; its ceiling remains enforced. Twelve PostgreSQL classes are explicitly selected in backend-test-pgsql, while BatchActionPermissionsTest and RoleProvisioningSourceSchemaTest remain outside that allowlist as ruled. Local per-file evidence is recorded in docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md; no observed CI run is claimed.",
      "raise_note_2026_09_09_t2t3_s1": "DELIBERATE RAISE 14 -> 15 (2026-09-09, lane T-2/T-3 slice S1, lane/t2-receipt-spine). One class added: TransferReceiptSchemaRerunPostgresTest. The feature lane feature-lane-data-console remains PARKED behind vars.SELF_HOSTED_RUNNER_READY, so without this raise tools/feature-lane-manifest-check.php is red on every CI run. The class IS named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which is its only live gate while the lane is parked. It is wholly PostgreSQL-only by construction: it reads pg_constraint, pg_indexes and information_schema.columns catalog dumps, which SQLite does not expose. CAVEAT: backend-test-pgsql runs on workflow_dispatch / base_ref==main / base_ref==dev / push->main - NOT on push->dev, so this arms the class on PR runs and main pushes, not on a direct dev promotion. Remove the allowlist entry and lower this ceiling when the lane's gate is flipped, not before. Local per-file evidence: docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md. No observed CI run is claimed. gated_ceiling 1253 -> 1267."
    },
    "ModelUpdates": {
      "lane": "feature-lane-catalog/ModelUpdates",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Modules": {
      "lane": "feature-lane-tenancy/Modules",
      "classes": 53,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Notification": {
      "lane": "feature-lane-platform-misc/Notification",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "POS": {
      "lane": "feature-lane-pos/POS",
      "classes": 157,
      "note": "DELIBERATE RAISE 156 -> 157 (2026-08-26 at merge into dev by Session D, catch-up: the class landed on dev in the O-30 merge without the accompanying ceiling raise): CloseOrphanedShiftCommandTest — the only pin on `pos:shift:close-orphaned` (eligibility predicate, typed exits, dry-run default, release-bounded expected-cash window, cash account-collection term). Runs BY PATH on sqlite AND PostgreSQL 16 (port 5433). NOT named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Gate record docs/superpowers/reviews/2026-08-26-o30-orphaned-shift-gate-r1-fiscal.md (r1 CHANGES -> r2 -> r3 mergeable). gated_ceiling carried in the union below. === prior note === DELIBERATE RAISE 155 -> 156 (2026-08-25, Session A lane D-1, fix/campaign-d1-pos-vat-discount-base): PosReceiptV5DiscountVatBaseProjectionTest — the read model behind the DGI declaration (post-remise base + ventilated shares mirrored verbatim, v3 still projecting with discount_allocated NULL, a 100 %-comp projecting with ZERO pos_receipt_payments rows and the CHECK (amount > 0) proven still in force by probe) plus the forward-only version-gate matrix. PG-MEANINGFUL BY CONSTRUCTION and run on PostgreSQL 5433 in-lane as well as sqlite; red-proved on PG by removing the pos_receipts_totals widening migration (SQLSTATE 23514). GATE r1 FINDING 6 CLOSED: it IS now named in the live backend-test-pgsql --filter allowlist in .github/workflows/ci.yml (Q-7 / B-3 precedent) — which matters most for this class, since it is the one that proves the widened pos_receipts_totals CHECK and the pos_receipt_payments amount>0 CHECK are both still in force. DELIBERATE RAISE 152 -> 153 (2026-08-24, Session A merge 77610a8bb): ReceiptProductTaxConfigurationRateTest — lane fix/campaign-n1-vat-resolution (N-1; POS seals the configured product rate); gate records docs/superpowers/reviews/2026-08-24-n1-vat-gate-r{1,2,3}-fiscal.md. DELIBERATE RAISE 149 -> 150 (2026-08-24, Session A): PosAuthHasPinsTest — lane fix/campaign-n5-haspins (N-5, has-pins honours company scope + offboarding belt via shared pinHolders()), authored 7aef37494/32a9b2e0b, merged d1dfa30cd; tenancy gate r1 ACCEPT, record docs/superpowers/reviews/2026-08-24-n5-haspins-gate-r1.md. Runs BY PATH on sqlite; not in any live --filter allowlist. Prior: Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational the moment the gate is flipped. DELIBERATE RAISE 147 -> 149 (2026-08-23, Session B merge-discipline catch-up, ground-truth attributed): ZReportRefundVatDisclosureTest + ZReportVatDeclarationReconciliationTest — lane fix/b6ii-xz-refund-vat-display (Session A, B-6ii X/Z refund-VAT), authored b2ee12f8d, merged ce59503a9 without the accompanying raise; gate records docs/superpowers/reviews/2026-08-23-b6ii-xz-gate-r2.md. Neither is in any live --filter allowlist (both execute nowhere until the parked lane's gate flips). Prior raise history: DELIBERATE RAISE 143 -> 147 (2026-08-23, merge reconciliation of two concurrent raises): (1)+(2) TerminalLocationPosEnabledTest + Migrations/BackfillLocationPosEnabledB3MigrationTest — lane fix/b3-pos-enabled-claim-enforcement (owner ruling B-3: server-side locations.pos_enabled enforcement at terminal acquisition + evidence-based backfill), merged 457457911; gate records docs/superpowers/reviews/2026-08-23-b3-pos-enabled-gate-r1.md + -r2.md; both classes run BY PATH on sqlite and PostgreSQL in-lane. (3) ReceiptReturnUnbalancedGlNarrowingTest — lane fix/r10-catch-narrowing (R-10), authored 1b459e876, merged d0e8b3cd9; gate record docs/superpowers/reviews/2026-08-23-r10-catch-narrowing-gate-r1.md. (4) CashCountDispatchGuardTest — lane fix/r8-shift-variance-queue (R-8; the class is the gate-added guard on the enqueue leg), authored 7db5fe467, merged 62fefcf16; gate records ...r8-sv-queue-gate-r1/r2/r3.md. (3)+(4) were a merge-discipline catch-up: both landed on dev without the accompanying raise, which is why dev's checker was red. None of the four is named in any live --filter allowlist (grep-verified), so all four genuinely execute nowhere until the parked lane's gate is flipped. DELIBERATE RAISE 150 -> 152 (2026-08-24 at merge; lane-authored as 149 -> 151 on a stale base, Session B lane Q-8 held-order recall hardening, branch fix/sb-q8-held-order-recall), TWO classes: (1) HeldOrderRecallContractTest — the recall/discard WIRE contract added by Q-8 (terminal_id is an OPTIONAL body field, honoured when supplied and left optional for backward compatibility because the one live client, apps/web/src/features/pos/api/heldOrderApi.ts:106, POSTs bare — so the audit's 'recall is not terminal-scoped' item is NOT closed by this lane and the web follow-up is booked by the parent; 409 HELD_ORDER_RECALL_CONFLICT whenever another actor already consumed the basket, raised BOTH from the guard after the locking read — the production site on PostgreSQL, where the loser's SELECT ... FOR UPDATE re-reads the winner's committed row version under EvalPlanQual — and from the conditional-UPDATE zero-row backstop, which is the only defence on SQLite; 422 RECALL_FAILED reserved for a basket that lapsed on its own TTL, which no actor consumed; 422 HELD_ORDER_DISCARD_REFUSED on a recalled basket, soft-delete + actor on discard). The lost-race case is staged against a REAL SECOND DATABASE CONNECTION on PostgreSQL (skipped on SQLite), not from a same-connection retrieved hook; the retrieved-hook variant survives, honestly renamed, as the SQLite backstop case; (2) PosHeldOrdersStatusCheckTest — the pgsql-only pos_held_orders_status_check + soft-delete/actor column assertions + the MIGRATION-BEARING pre-flight-abort probe for migration 2026_08_23_163000_harden_pos_held_orders_status_and_discard. Both run BY PATH on sqlite and on PostgreSQL 5433 in-lane (red captured by reverting HeldOrderService for (1) and by removing the migration for (2)); neither is named in any live --filter allowlist, so both execute nowhere until the parked lane's gate flips. gated_ceiling 1153 -> 1155 at merge (union: dev's count + these two POS classes; gate r1 docs/superpowers/reviews/2026-08-24-sb-q8-held-orders-gate-r1.md, fix round 872e13662 parent-verified). DELIBERATE RAISE 153 -> 155 (2026-08-24 at merge; lane-authored as 149 -> 151 on a stale base, Session B lane Q-7, fix/sb-q7-terminal-claim-hardening): TerminalClaimHardeningTest + Migrations/PosTerminalsIdentityLifecycleConstraintsTest — terminal-claim hardening (conditional-UPDATE claim settlement, partial unique on hardware_identifier, type + is_active/deactivated_at CHECKs, release endpoint). Both are named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, so both DO execute on PR->dev on real PostgreSQL; the parked POS lane is their second home. gated_ceiling 1160 -> 1162 at merge (union: dev + these two POS classes; gates docs/superpowers/reviews/2026-08-24-sb-q7-terminal-gate-r1-tenancy.md, -r1-fiscal.md, -r2-fiscal.md)."
    },
    "Partner": {
      "lane": "feature-lane-inventory/Partner",
      "classes": 22,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 20 -> 21 (2026-08-23): PartnerTunisianMatriculeTest — lane fix/p0-tn-matricule-regex-convergence (TN matricule regex convergence, merged 929de7b34); gate records docs/superpowers/reviews/2026-08-23-p0-tn-mf-regex-gate-r1.md + -r2.md (fiscal gate r1/r2 CHANGES -> r3 parent-verified incl. PG leg). Run BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips. DELIBERATE RAISE 21 -> 22 (union at merge; lane fork base 21) (2026-08-30, lane G-3a fix round 1): PartnerVatLifetimeScopeTest pins same-company deleted VAT create/update 422s, sibling-company allowance, lifetime service resolution, and truthful coded Parties-import row errors. It is also named in the live PostgreSQL allowlist because this lane is parked. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 20 -> 21 (2026-08-23): PartnerTunisianMatriculeTest — lane fix/p0-tn-matricule-regex-convergence (TN matricule regex convergence, merged 929de7b34); gate records docs/superpowers/reviews/2026-08-23-p0-tn-mf-regex-gate-r1.md + -r2.md (fiscal gate r1/r2 CHANGES -> r3 parent-verified incl. PG leg). Run BY PATH on sqlite and PG in-lane; not in any live --filter allowlist, executes nowhere until the parked lane's gate flips."
    },
    "Performance": {
      "lane": "feature-lane-platform-misc/Performance",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Permissions": {
      "lane": "feature-lane-tenancy/Permissions",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "PlatformIntegration": {
      "lane": "feature-lane-platform-misc/PlatformIntegration",
      "classes": 11,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Precision": {
      "lane": "feature-lane-platform-misc/Precision",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Pricing": {
      "lane": "feature-lane-pos/Pricing",
      "classes": 8,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Procurement": {
      "lane": "feature-lane-inventory/Procurement",
      "classes": 25,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Product": {
      "lane": "feature-lane-catalog/Product",
      "classes": 59,
      "raise_note": "DELIBERATE UNION RAISE 58 -> 59 (2026-09-02, K67/K89 merge): ProductBarcodeUniquenessTest adds the company-scoped live-barcode partial unique, oldest-wins twin cleanup, idempotence, and read-only dry-run census proof. Together with K-7's new Import class, one BatchExpiry class, and five Inventory classes, the checker-recomputed gated ceiling is 1243.",
      "note": "DELIBERATE RAISE 57 -> 58 (2026-08-25, Session A lane fix/campaign-w26-po-price-default, gate r1 finding 2): ResolveLineEntryCodeCostRedactionTest — the only guard that `line-entry/resolve-code` redacts cost fields (purchase_price / cost_price / last_purchase_cost, plus the variant's cost_override) for callers lacking `pricing.view_cost_prices`, the way ProductController::index/show already do. Both endpoints sit behind the same `can:products.view` route middleware and both feed the document line editor, so before this fix a typed product search redacted and a barcode scan did not. W2-6 raised the impact from a JSON-payload leak to an on-screen one (the scanned purchase_price pre-fills the PO price cell and is announced by the new price-provenance hint). Red captured on PostgreSQL 16 (throwaway autoerp_test_w26, port 5433): 3 of 4 failed with '10.000' / '8.000' returned where null was required; green after LineEntryController::productPayload. Because this lane is PARKED, the class is ALSO named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml — same reason CorrectingEntryEndpointTest and SupplierGoodsReturnNoteTest are: an authz regression pin that executes on no event is not a pin. Remove the allowlist entry when this lane's gate is flipped, not before. gated_ceiling 1167 -> 1168 at merge (UNION arithmetic, gate r2 finding 3: dev's 1167 after N-6 raised Document 80 -> 82 / gated 1164 -> 1167, plus this one Product class; every other group's entry taken from dev verbatim. The lane authored 1164 -> 1165 on its earlier base — do NOT resolve this line to 1165). Gate record docs/superpowers/reviews/2026-08-24-w26-po-price-gate-r1.md. DELIBERATE RAISE 55 -> 57 (2026-08-24, Session A merge 77610a8bb): ProductTaxRateDerivationTest + Migrations/BackfillProductsTaxRateN1MigrationTest — lane fix/campaign-n1-vat-resolution (N-1; MIGRATION-BEARING products-only backfill + worklist census); gate records docs/superpowers/reviews/2026-08-24-n1-vat-gate-r{1,2,3}-fiscal.md. Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Progression": {
      "lane": "feature-lane-pos/Progression",
      "classes": 6,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Promotion": {
      "lane": "feature-lane-pos/Promotion",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "PurchaseHub": {
      "lane": "feature-lane-inventory/PurchaseHub",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Replenishment": {
      "lane": "feature-lane-inventory/Replenishment",
      "classes": 9,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped.",
      "raise_note_2026_09_09_t1": "DELIBERATE RAISE 7 -> 8 (2026-09-09, T-1 transfers/requests edge campaign). ReplenishmentEdgeCasesTest added. The feature lane remains PARKED behind vars.SELF_HOSTED_RUNNER_READY. All three T-1 classes are explicitly selected in backend-test-pgsql; completion locking and the reopen partial unique are PostgreSQL-only. Four known failing behavior tests remain ticket-linked skips, reproducible with T1_RUN_KNOWN_REDS=1. Local per-file evidence: docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md. No observed CI run is claimed.",
      "raise_note_2026_09_09_t2t3_s1": "DELIBERATE RAISE 8 -> 9 (2026-09-09, lane T-2/T-3 slice S1, lane/t2-receipt-spine). One class added: TransferCloseReplenishmentSettlementTest. The feature lane feature-lane-inventory remains PARKED behind vars.SELF_HOSTED_RUNNER_READY, so without this raise tools/feature-lane-manifest-check.php is red on every CI run. The class IS named in the backend-test-pgsql --filter allowlist in .github/workflows/ci.yml, which is its only live gate while the lane is parked. It is NOT PostgreSQL-only: it runs on both lanes. CAVEAT: backend-test-pgsql runs on workflow_dispatch / base_ref==main / base_ref==dev / push->main - NOT on push->dev, so this arms the class on PR runs and main pushes, not on a direct dev promotion. Remove the allowlist entry and lower this ceiling when the lane's gate is flipped, not before. Local per-file evidence: docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md. No observed CI run is claimed. gated_ceiling 1253 -> 1267."
    },
    "Scheduling": {
      "lane": "feature-lane-documents/Scheduling",
      "classes": 23,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Schema": {
      "lane": "feature-lane-data-console/Schema",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Security": {
      "lane": "security-regression",
      "classes": 18,
      "note": "17 -> 18 (2026-08-23, membership-offboarding lane): UserOffboardingCascadeTest, the regression for the offboarding privilege hole (a deactivated user kept every ACTIVE user_company_memberships row, and with it their POS override PIN). Deliberately placed HERE and not under Identity/UserManagement: `security-regression` is a whole-directory selector on a job with no `if:` guard, so the class runs on every PR->dev from the moment it lands, whereas `feature-lane-tenancy/Identity` is still PARKED behind `vars.SELF_HOSTED_RUNNER_READY` and would have given a launch-relevant security regression ZERO CI coverage (and cost a gated_ceiling raise). This count is informational, as for any live lane."
    },
    "Seeders": {
      "lane": "feature-lane-data-console/Seeders",
      "classes": 26,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Service": {
      "lane": "feature-lane-documents/Service",
      "classes": 7,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Services": {
      "lane": "feature-lane-documents/Services",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Shared": {
      "lane": "feature-lane-platform-misc/Shared",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "SmartPrompts": {
      "lane": "feature-lane-pos/SmartPrompts",
      "classes": 3,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Subscription": {
      "lane": "feature-lane-tenancy/Subscription",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "SupportAccess": {
      "lane": "feature-lane-tenancy/SupportAccess",
      "classes": 13,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "T2": {
      "lane": "feature-lane-platform-misc/T2",
      "classes": 1,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Taxation": {
      "lane": "feature-lane-fiscal-finance/Taxation",
      "classes": 33,
      "note": "DELIBERATE RAISE 32 -> 33 (2026-08-26 at merge into dev by Session D, catch-up: the class landed on dev in the B-19 merge without the accompanying ceiling raise): SupplierInvoiceVatDeclarationTest — the only declaration-level pin that a TN tenant's supplier invoices and supplier credit notes reach the deductible side (B-19 P0). Runs BY PATH on sqlite AND PostgreSQL 16 (phpunit-pgsql.xml). NOT named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Gate records docs/superpowers/reviews/2026-08-26-b19-supplier-invoice-vat-gate-r1-{treasury,fiscal}.md + 2026-08-26-b19-handback.md. === prior note === DELIBERATE RAISE 31 -> 32 (2026-08-24, Session A merge of fix/campaign-fe-contract): VatReportSummaryContractTest — N-4 contract pin (gate record docs/superpowers/reviews/2026-08-24-fe-contract-gate-r1.md). Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Tenant": {
      "lane": "feature-lane-tenancy/Tenant",
      "classes": 31,
      "note": "DELIBERATE RAISE 29 -> 31 (2026-08-30, Session I lane I-2): FreshTenantCensusInvariantsTest and DayOneCensusCommandTest. Both are explicitly wired into the live backend-test-pgsql allowlist while the whole-directory Tenancy lane remains parked. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Treasury": {
      "lane": "treasury-spine-pgsql/feature-treasury",
      "classes": 124,
      "note": "DELIBERATE RAISE 123 -> 124 (2026-08-30, lane SG-3c-FU feat/sg3c-fu-repository-normalisation): RepositoryNormalisationTest pins the read-only census, refusal-first metadata normalisation, second company, second location, re-run idempotency, and PostgreSQL partial-unique transitions. gated_ceiling 1226 -> 1227. === prior note === DELIBERATE RAISE 122 -> 123 (2026-08-29, P1 micro-lane fix/payrepo-index-company-scope): PaymentRepositoryCompanyScopeTest — the second-of-everything HTTP pin that each active company lists only its own G-3c-provisioned CASH-01/SAFE-01 repositories and a sibling-company show remains 404. Runs BY PATH on SQLite and PostgreSQL 16; the live treasury-spine-pgsql lane also picks up the whole Treasury directory automatically. === prior note === Informational for lane groups: the ceiling is enforced only for deferred/excluded groups; a laned directory picks up new classes automatically. 122 on disk = 119 at the Session-B Q-11 merge tip plus TreasuryOrphanCensusCommandTest (Session B lane Q-12, triage F5 / DS-1 census half) and SliceDBatch1CheckConstraintsTest (Session B2 lane B2-4 / Slice D batch 1, 2026-08-25) — the liveness pin on the 13 money/fiscal enum CHECK constraints (23514 rejection, NULL still accepted on the four nullable columns, pg_constraint.convalidated true). It lives here rather than in Feature/Architecture because treasury-spine-pgsql runs this directory WHOLE on a real PostgreSQL service and gates on PR->dev, which is what a PG-only CHECK pin needs; no --filter append is required. DELIBERATE RAISE 121 -> 122 (2026-08-25, same lane, r1 fix round, treasury C1 / fiscal F-2): SliceDBatch1EnumFreezeTest — the DRIVER-FREE freeze pin on the 13 value sets those migrations materialised. Both r1 gates proved that EnumCheckParityTest cannot catch an enum gaining a case (it runs on a freshly migrated schema against a cases()-derived CHECK, so the two agree by construction); this class pins the case lists as string literals and is the only thing that goes red. It needs no database, so unlike its sibling it does NOT self-skip on the SQLite suite. Template for Slice D batches 2..N."
    },
    "Uom": {
      "lane": "feature-lane-inventory/Uom",
      "classes": 9,
      "rebase_note": "UNION AT REBASE (2026-08-31): G-4 adds UnitCatalogQueryTest, raising Uom 8 -> 9 and gated_ceiling 1223 -> 1232 together with the eight Import classes.",
      "note": "DELIBERATE RAISE 6 -> 8 (2026-08-30, lane G-12 feat/g12-units-invariant): UnitsProvisioningTest and UnitsInvariantTest — pin company-creation provisioning, the shared visible-unit predicate, half-state refusal policy, and the self-guarding census/backfill migration. Both are named in the backend-test-pgsql --filter allowlist because Uom is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` and would otherwise execute on no event; UnitsProvisioningTest was appended in fix round 2 (2026-08-30, gate r2 G12-R2-03) because it carries the R1-01 acceptance proof that company creation survives a units-provisioning failure. === prior note === DELIBERATE RAISE 5 -> 6 (2026-08-26 at merge into dev by Session D, T9 batch item N-9): the units-of-measure seeding pin — a fresh tenant must own the base unit set at provisioning and the self-guarding backfill migration must seed unit-less existing tenants (companies-row guarded). Runs BY PATH on sqlite AND PostgreSQL 16; NOT named in any live --filter allowlist, so it executes nowhere until this parked lane's gate is flipped. Gate records docs/superpowers/reviews/2026-08-26-session-d-t9-batch-gate-r1-{imports,authz}.md. === prior note === Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Vehicle": {
      "lane": "feature-lane-catalog/Vehicle",
      "classes": 16,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    },
    "Voucher": {
      "lane": "feature-lane-pos/Voucher",
      "classes": 11,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped. DELIBERATE RAISE 10 -> 11 (2026-08-24 at merge, Session B lane Q-5 fix/sb-q5-voucher-void-service): VoucherVoidServiceTest — the single void write path (VoucherVoidService: lock + status precondition + GL reversal + idempotency), authored 94ec615bf which never registered the raise; gate records docs/superpowers/reviews/2026-08-23-sb-q5-voucher-gate-r1-{treasury,fiscal}.md, micro-round 12ee4c81d parent-verified. Not in any live --filter allowlist; run BY PATH on sqlite + PG 5433 in-lane."
    },
    "Workshop": {
      "lane": "feature-lane-documents/Workshop",
      "classes": 46,
      "note": "Laned by the O-29 / F-2 execution ruling of 2026-08-21, into a lane that is PARKED behind `vars.SELF_HOSTED_RUNNER_READY` until the owner registers the self-hosted runner. While parked the `classes` ceiling stays ENFORCED (a new class here still runs nowhere); it becomes informational, as for any live lane, the moment the gate is flipped."
    }
  },
  "gated_ceiling_raise_note_2026_09_07_pr218": "DELIBERATE RAISE 1248 -> 1249 (2026-09-07, PR #218): Inventory 125 -> 126, see groups.Inventory.raise_note_2026_09_07_pr218. One class (CountingIndexStatusFilterTest: `status=active` alias, `overdue=true` / `status=overdue`, exact `CountingStatus` values, unknown tolerated, second-company scoping), wired into the backend-test-pgsql --filter allowlist; not a phantom.",
  "gated_ceiling_raise_note_2026_09_05_pr212": "DELIBERATE RAISE 1245 -> 1246 (2026-09-05, teammate PR #212 fix round 1): Document 92 -> 93, see groups.Document.raise_note_2026_09_05. One class, one raise; every other group taken from local dev fa000edc3 verbatim.",
  "gated_ceiling_raise_note_2026_09_05": "DELIBERATE RAISE 1243 -> 1245 (2026-09-05): Inventory 123 -> 125, see groups.Inventory.raise_note_2026_09_05. Union arithmetic: origin/dev had already reached 1244 (N-1's SubmitCountQuantityScaleTest without a raise), local dev adds Phase A's StockTransferIdempotencyCollisionPostgresTest.",
  "gated_ceiling_raise_note_2026_09_05b": "DELIBERATE RAISE 1246 -> 1247 (2026-09-05): Document 93 -> 94, see groups.Document.raise_note_2026_09_05b.",
  "gated_ceiling_raise_note_2026_09_05c": "DELIBERATE RAISE 1247 -> 1248 (2026-09-05): Catalog 34 -> 35, see groups.Catalog.raise_note_2026_09_05.",
  "gated_ceiling_raise_note_2026_09_10_wlota1a": "DELIBERATE RAISE 1250 -> 1264: BatchExpiry +4, Console +2, Identity +7, Migrations +1 for W-LOT-A-1a. Feature lanes remain parked; see group notes and twelve explicit backend-test-pgsql selections. Local verification is by file only. No observed CI run is claimed.",
  "gated_ceiling_raise_note_2026_09_09_t1": "DELIBERATE RAISE 1250 -> 1253: Inventory +2 and Replenishment +1 for T-1. See group notes; explicit backend-test-pgsql selection keeps the new tests armed while feature lanes are parked.",
  "gated_ceiling_raise_note_2026_09_09_t2t3_s1": "DELIBERATE RAISE 1253 -> 1267. Fourteen S1 classes across Inventory, Replenishment and Migrations: StockTransferReceiveTest, StockTransferReceiveDamageTest, StockTransferReceiveLotsTest, StockTransferReceiveValidationTest, StockTransferCloseTest, StockTransferReceiveConcurrencyPostgresTest, TransferReceiptEventStreamTest, TransferReceiptGlBoundaryTest, TransferReceiptPatternQueryPostgresTest, TransferLegacyCompletionBackfillTest, TransferReceiptSecondOfEverythingTest, TransferReceiptAuthorityGateTest, TransferCloseReplenishmentSettlementTest, TransferReceiptSchemaRerunPostgresTest. The lanes remain PARKED behind vars.SELF_HOSTED_RUNNER_READY, so without this raise the checker is red on every CI run. All fourteen are in the backend-test-pgsql --filter allowlist. CAVEAT: workflow_dispatch / base_ref==main / base_ref==dev / push->main - NOT push->dev. Remove the allowlist entries and lower this ceiling when the lane's gate is flipped, not before. Local per-file evidence: docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md. No observed CI run is claimed.",
  "gated_ceiling_raise_note_2026_09_10_rbac_w0a_s1": "DELIBERATE RAISE 1253 -> 1254 (2026-09-10, lane lane/rbac-w0a-s1-permission-cache, gate r1 fix round 1): Identity 32 -> 33, see groups.Identity.raise_note_2026_09_10. One class (PermissionCacheTenantScopingTest), one raise; every other group taken from this lane's base d418a2656 verbatim. The class is dark in the parked `feature-lane-tenancy` lane until the runner flag flips, but it is LIVE via the explicit `t6-phase0b-pgsql` --filter selection in .github/workflows/ci.yml — not a phantom. UNION arithmetic: re-take this number against dev's CURRENT gated_ceiling at merge; lane/w-lot-a-1a (Identity 32 -> 39, gated_ceiling 1250 -> 1264) and lane/t2-receipt-spine are in flight on the same two files.",
  "gated_ceiling_raise_note_2026_09_12_merge_t2s1": "MERGED-TREE RECONCILIATION (2026-09-12, integration owner): lane/t2-receipt-spine (+14: Inventory 129->141, Migrations 14->15, Replenishment 8->9) merged onto dev carrying rbac-w0a-s1 (+1: Identity 32->33). 1254 + 14 = 1268, sum of increments, never a textual merge.",
  "gated_ceiling_raise_note_2026_09_12_merge_wlota1a": "MERGED-TREE RECONCILIATION (2026-09-12, integration owner): lane/w-lot-a-1a (+14: BatchExpiry 17->21, Console 16->18, Identity +7, Migrations +1) merged onto dev at 1268 (T-1 +3, rbac-w0a-s1 +1, T2 S1 +14 over the lane's 1250 base). 1268 + 14 = 1282, sum of increments, never a textual merge.",
  "gated_ceiling_raise_note_2026_09_12_merge_imp1": "MERGED-TREE RECONCILIATION (2026-09-12, integration owner): lane/imp1-history-export (+1: Import 39->40, ImportRowExportTest; see groups.Import.imp1_note) merged onto dev at 1282 (T-1 +3, rbac-w0a-s1 +1, T2 S1 +14, W-LOT-A-1a +14 over the lane's 1254 base). 1282 + 1 = 1283, sum of increments, never a textual merge. ImportRowExportTest and ProcessImportJobStatusTest are LIVE via the backend-test-pgsql --filter allowlist (232 names); the Import feature lane stays parked."
}
