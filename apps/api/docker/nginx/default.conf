# =============================================================================
# Nginx Virtual Host Configuration for AutoERP API
# =============================================================================

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;
    root /var/www/html/public;
    index index.php;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Health check endpoint for load balancers (before php handling)
    location = /api/health {
        access_log off;
        try_files $uri /index.php?$query_string;
    }

    # Alternative health check path (nginx-only, no PHP)
    location = /health {
        default_type text/plain;
        return 200 'healthy';
    }

    # Root path - redirect to API docs or return basic info
    location = / {
        default_type application/json;
        return 200 '{"status":"ok","service":"autoerp-api"}';
    }

    # FPM status (for monitoring)
    location = /fpm-status {
        access_log off;
        include fastcgi_params;
        fastcgi_pass unix:/var/run/php-fpm.sock;
    }

    location = /fpm-ping {
        access_log off;
        include fastcgi_params;
        fastcgi_pass unix:/var/run/php-fpm.sock;
    }

    # Main location block
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP handling
    location ~ \.php$ {
        # Check file exists before passing to PHP-FPM
        try_files $uri =404;

        fastcgi_pass unix:/var/run/php-fpm.sock;
        fastcgi_index index.php;

        # Use $uri instead of $fastcgi_script_name for reliability
        # Script information - MUST come first
        fastcgi_param SCRIPT_FILENAME   $document_root$uri;
        fastcgi_param SCRIPT_NAME       $uri;
        fastcgi_param DOCUMENT_ROOT     $document_root;

        # Request information - use $uri for REQUEST_URI as fallback
        fastcgi_param REQUEST_METHOD    $request_method;
        fastcgi_param REQUEST_URI       $uri$is_args$args;
        fastcgi_param QUERY_STRING      $query_string;
        fastcgi_param CONTENT_TYPE      $content_type;
        fastcgi_param CONTENT_LENGTH    $content_length;

        # Server information
        fastcgi_param SERVER_PROTOCOL   $server_protocol;
        fastcgi_param SERVER_SOFTWARE   nginx/$nginx_version;
        fastcgi_param SERVER_NAME       $server_name;
        fastcgi_param SERVER_ADDR       $server_addr;
        fastcgi_param SERVER_PORT       $server_port;
        fastcgi_param GATEWAY_INTERFACE CGI/1.1;

        # Client information
        fastcgi_param REMOTE_ADDR       $remote_addr;
        fastcgi_param REMOTE_PORT       $remote_port;

        # HTTPS detection (for when behind reverse proxy)
        fastcgi_param HTTPS             $https if_not_empty;
        fastcgi_param HTTP_PROXY        "";

        # Timeouts
        fastcgi_connect_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_read_timeout 60s;

        # Buffer sizes
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    # Deny access to hidden files (except .well-known)
    location ~ /\.(?!well-known) {
        deny all;
    }

    # Deny access to sensitive files
    location ~ /\.(env|git|svn) {
        deny all;
    }

    # Static file caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2|ttf|svg|eot)$ {
        expires 30d;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    # Favicon handling
    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    # Robots.txt
    location = /robots.txt {
        access_log off;
        log_not_found off;
    }
}
