<?php

declare(strict_types=1);

// Workshop Bundle routes are registered in Task 8 (REST endpoints).
// Intentionally empty for now so BundleServiceProvider::boot() can load
// this file without fatal errors at Task-4 scope.
