`UPHOLD REJECT`

I verified every load-bearing claim directly against the branch (diff `fd10632fb...HEAD`), the Rev 2 spec, the Rev 2 plan, and both prior verdicts. Round 2's blocker is real and independently reproduced; one of its majors survives as a docs fix; the other is overruled as plan-authorized design. Note: targeted test execution was permission-blocked for me exactly as it was for all four prior reviewers, so the "someone with execution rights runs the suites" requirement remains open — my adjudication rests on code evidence, which is sufficient for the questions asked.

## B1 — UPHOLD, BLOCKER. Every factual leg is independently confirmed.

- **No direction guard:** `InstrumentLifecycleService.php:596-654` (`cancel()`) gates only on `status !== Received` (`:604`). Its siblings all reject outbound: `custodyTransfer` `:137-139`, `deposit` `:180-182`, `clear` `:202-204`. The spec's own current-state anchor ("the existing inbound actions structurally reject outbound", spec §4.3) was false for `cancel()` — ⑤a built on a premise the code never satisfied.
- **Issued outbound instruments sit exactly in the vulnerable state:** `OutboundInstrumentIssuer.php:78-99` creates via `receive(...)` (status `Received`, `InstrumentLifecycleService.php:104`) and posts the issue JE at `:180-189` (Dr AP / Cr `4035`/`403`). The issuer contains **zero** `Payment` references (whole file read), so at `cancel()` `:608` the payment is `null` → the settle-or-reverse guard `:609-613` is skipped → the JE block `:616-633` is skipped (`$journalEntryId` stays null) → status flips to `Cancelled` `:639` → the event row `:640-652` carries **no `action_key`/`semantic_digest`** → no `InstrumentCancelled` is dispatched (neither in the service nor in `PaymentInstrumentController::cancel` `:337-354` — zero event references in that controller), so `SyncExpenseOnInstrumentLifecycle` never resets `expense_metadata`. The `4035` credit is permanently stranded and AP is understated. The deferred-supplier variant *is* blocked by `:609-613` (it has a completed linked Payment); the live hole is the expense-issued path.
- **Reachable from shipped UI by ordinary roles:** `InstrumentDetailPage.tsx:186` — `canCancel = instrument.status === 'received' && hasPermission('instruments.cancel')`, no direction term (`isOutbound` declared at `:189`, labels only); route `routes.php:141-143` gated on `can:instruments.cancel`, granted to admin (`RolesAndPermissionsSeeder.php:482`) and accountant (`:706`).
- **Pinned wrong:** `OutboundInstrumentGuardTest.php:171-182` (`test_outbound_receive_and_cancel_remain_allowed`) is byte-identical to the design base (empty diff vs `fd10632fb`) and asserts the corrupting call returns `cancelled`. Correct pre-⑤a (outbound carried no GL); wrong the moment `issueExisting` gave outbound a GL footprint.

This violates spec §4.3's `Received → Cancelled` contract (reverse-issue JE, `instrument:{id}:cancel` key, atomic reopen — spec `:76,:83`) and §4.5's expense-metadata reset. Exit-blocking: confirmed.

**However, round 2's prescribed fix is incomplete and would trade one corruption for another.** Registered-but-never-issued outbound instruments are a live state: `PaymentInstrumentController::store` accepts `direction: outbound` (`:148,:176`) and creates a `Received` instrument with **no** issue JE; the deferred-supplier flow also issues pre-registered instruments later via `issueExisting` (`PaymentController` `:1009`). If generic `cancel` simply rejects all outbound, those instruments route to `cancel-outbound` — and `OutboundInstrumentService::cancel` posts `createOutboundInstrumentCancellationEntry` **unconditionally** (`:626-639` region; no issue-event lookup anywhere in `:511-720`), fabricating a Dr payable-purpose / Cr AP reversal for an issue that never happened. The fix set below closes both holes.

## M-A — UPHOLD, MAJOR (docs-only fix, required)

Confirmed end to end. `BackfillPayableInstrumentAccountsCommand.php` correctly detects missing parent, wrong type, and inactive `403`/`4035` (error + `$invalid++`, seen at `:55-95` of the shown range) and returns `FAILURE`. But `vendor/stancl/tenancy/src/Commands/Run.php:54` invokes the child via `$this->call(...)` and **discards the return**; `handle()` (`:32-56`) returns nothing, so `tenants:run` always exits 0 and never aborts on a per-tenant failure. The checklist (`docs/handoff/treasury-phase5a-deploy-checklist.md:22-24`) prescribes exactly this invocation and calls those conditions "a deployment stop" (`:33`) with no detection mechanism beyond an operator eyeballing scrolling multi-tenant output. Given the owner's push-to-dev-auto-deploys discipline, this is a genuine operational trap. **Minimally sufficient detection (docs edit only):** state the non-aborting/exit-0 semantics, then either (a) `tenants:run … 2>&1 | tee backfill.log` followed by a mandatory `grep -E 'inactive|wrong type|missing supplier parent' backfill.log` gate, or (b) loop per tenant with `--tenants=<uuid>` checking `$?`. No code change is required for exit; an aggregate-exit-code wrapper is a follow-up ticket.

## M-B — OVERRULE severity. Not exit-blocking; downgrade to MINOR/ticket.

Round 2's facts are accurate but its authorization analysis is wrong:

1. **The shipped shape is the plan-reviewed design.** Plan Rev 2 Task 2 explicitly prescribes modifying `ExpenseMetadata.php` "(fillable/casts/**relations**)" (plan `:77`), and Task 9 prescribes the listener setting "repository/method **from the instrument**" (plan `:203`). That plan passed three adversarial pre-dispatch reviews (Codex + treasury + tenancy-authz). The plan's rule-6 concern was directional — "Treasury never writes `ExpenseMetadata`" — and that is satisfied.
2. **Pre-existing pattern:** `ExpenseMetadata` already held Treasury model relations (`PaymentMethod`, `PaymentRepository`) before ⑤a — the diff's `paymentInstrument()` (`ExpenseMetadata.php:128-132`) is the third instance of an established in-file pattern, not a new class of breach. The relation is also **never consumed** anywhere in the module (grep: definition only), so dropping it is free — but optional.
3. **Round 2's own fix is rule-8-noncompliant.** "Carry those fields on the event" would restructure `InstrumentCleared`, which is pre-existing and immutable ("never modify once deployed", `InstrumentCleared.php:11-14`; CLAUDE.md rule 8). The compliant minimum, *if* the owner wants it fixed in-branch rather than ticketed: a read port in `Shared/Contracts/Treasury` returning `{repositoryId, paymentMethodId, direction}` for an instrument id (payload shape: three nullable string UUIDs + direction string), consumed by the listener in place of `PaymentInstrument::query()` at `SyncExpenseOnInstrumentLifecycle.php:46-50`. Alternatively a V2 cleared event — heavier, not minimal.
4. `use InstrumentKind` in `ExpenseService.php:27` is an **enum** used for validation (`:612-615`), not a model. Rule 6 prohibits model imports; counting an enum is overreach.

## Authoritative minimal fix set before the next Opus rerun

1. **Backend guard:** add the outbound rejection to `InstrumentLifecycleService::cancel()` mirroring `:137-139`, with an error message pointing callers to `cancel-outbound`.
2. **Issue-conditional reversal:** in `OutboundInstrumentService::cancel()`, look up the issue event (`action_key = "instrument:{id}:issue"`) under the existing instrument lock; post the reversal JE only when it exists, otherwise write the action-keyed `Cancelled` event with `journal_entry_id: null` and still dispatch `InstrumentCancelled`. Add a test proving cancel of a registered-never-issued outbound instrument is GL-free.
3. **Test inversion:** rewrite `OutboundInstrumentGuardTest.php:171` to assert the generic `cancel` route rejects outbound; add a companion asserting an expense-issued instrument cancelled via `cancel-outbound` reverses the issue JE and reopens the expense (the listener path already exists).
4. **FE gating:** condition `canCancel`/`canRemit`/`canTransfer`/`canClear`/`canBounce` on `!isOutbound` at `InstrumentDetailPage.tsx:184-188` (note `canBounce` already renders on outbound *cleared* instruments today; the backend guard at `:328` holds but fails silently — no `onError` in the file).
5. **Deploy checklist:** the M-A detection edit above.
6. Someone with execution rights runs the touched suites + lint/typecheck — still unmet after five reviews, including this one.

## Round-2 claims to treat as non-blocking (do not gate the rerun on these)

- **M-B** in its entirety (downgraded above — ticket the listener read-port; drop or keep the unused relation at the implementer's discretion).
- All round-2 MINORs stand as recorded but none block: `bounce` afterCommit asymmetry, duplicated digest pair, un-normalized digest amount, screenshot-05 clipping, `data-total` assertion shape, `receivedDate` column header, bare invalidation keys, and the locale-enum drift test. Round 2's re-checks clearing three round-1 minors (lock order, scoped replay lookup, bounce-listener-by-design) are correct and should carry forward.

Round 2 contains no false statements of fact — every citation I checked resolved exactly. Its two defects are the incomplete B1 fix prescription (item 2 above) and the M-B severity/authorization error.

ARBITRATION: spec ❌ + gate CHANGES-REQUIRED
