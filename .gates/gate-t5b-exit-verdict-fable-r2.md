The tenant-isolation test deletion is legitimate (it targeted the removed legacy endpoint, now negatively asserted in `BankReconciliationCutoverTest`), the modified instrument/reconcile tests are pure additions, the precision grep over the whole diff is clean, and both e2e reports are complete and honest about their preconditions. I now have everything needed for the verdict.

---

# GATE VERDICT: REJECT

The Phase ⑤ **implementation** is the strongest treasury work on this branch to date — I verified every money-bearing invariant directly in code and found no Critical or Important defect in the shipped logic. The REJECT is driven by the exit contract itself: the mandatory Gate 5 record does not support approval. The last completed Gate 5 verdict (frontend-conventions r7) is a REJECT whose MAJOR finding is verifiably unfixed at HEAD, the treasury Opus lane never recorded a post-fix approval, and the branch's own audit trail mischaracterizes the r7 outcome. Under invariant 10 ("no unresolved Critical/Important finding survives"), that is disqualifying regardless of implementation quality.

A note on the tasking premise: the request states this review runs "after both Gate 5 Opus lanes pass." The committed record contradicts that — no completed APPROVE exists from either Gate 5 lane at HEAD. I reviewed against the record, not the premise. Also, actual branch HEAD is `e07e7e655` (one gates-bookkeeping commit past the stated `6ce6b963f`; it touches only `.gates/` and one handback line).

## Findings by severity

**Critical** — none in the implementation.

**Important**

- **F1 — Unresolved Gate 5 r7 MAJOR: date-localization regression across the entire new surface.** The deleted `BankReconciliationPage` formatted dates for the FR/TN locale; the replacement renders raw ISO at six sites (`StatementListPage.tsx:65`, `ReconciliationWorkspacePage.tsx:115` and `:129`, `LinePanel.tsx:75`, `StatementUploadWizard.tsx:167`, `ManualMatchSearch.tsx:42` — which also slices a UTC timestamp, dropping timezone conversion). Verified at HEAD: `formatDate` from `lib/format.ts` is used zero times under `statements/`. The last implementation commit (`2959571fd`, 05:37) predates the r7 verdict (05:45), so nothing addressed it.
- **F2 — Contradictory audit record for r7.** Commit `6ce6b963f` simultaneously committed the full 61-line r7 REJECT verdict (`gate-t5b-gate-5-verdict-frontend-conventions-r7.md`) and an interruption note claiming r7 produced "empty output" (`...-r7-interrupted.md:5`); `HANDBACK-treasury-phase5.md:68` repeats the "empty output" claim and states the frontend defects were "corrected." In an audit-trail-driven gate process, a completed REJECT recorded as an empty interruption is an evidence-integrity defect that must be corrected before any approval chain built on `.gates/` can be trusted. I make no finding of intent — but the record as committed is wrong.
- **F3 — Gate 5 has no completed approval from either required Opus lane.** Treasury: r2 REJECT (`gate-t5b-gate-5-verdict-treasury-r2.md`), then r3/r4/r5 all quota-interrupted with no output. Frontend: r7 REJECT. I independently verified the treasury r2 closure — C1 smoke fixture fixed (`treasury-phase5b-reconciliation.smoke.ts:298` now destructures `{ request }`), C2 fixed (`ReconciliationWorkspacePage.tsx:110-111,118` gates completion on `bank-statements.reopen` with a translated warning, with render coverage), I1 fixed (`AcquirerFeeService.php:49` uses the injected resolver on replay), I2 fixed (`StatementMatchingService.php:232-237` and `StatementCompletionService.php:144-150` reduce with `bcformatStrict`) — but my verification does not substitute for the contractually required lane approvals; the controller must decide whether this exit review supersedes them.
- **F4 — The ⑤b live smoke is not re-runnable (r7 MAJOR-2, unaddressed).** It permanently consumes an unreconciled GL-linked bank repository with no reset/teardown, and `REPORT.md:40-46` documents three manual out-of-repo preconditions (hand-run account backfill, `QUEUE_CONNECTION=sync`, hand-edited Vite proxy). The evidence honestly discloses this, and the run itself was genuine — but the handback's own controller-verification commands instruct re-running this smoke, which will fail once fixtures are exhausted.

**Minor** (non-blocking, ticket): `BankStatementController.php:75-76` unscoped `findOrFail` (mitigated by `StatementImportService::guardOwnership` before any side effect, and db-per-tenant makes cross-tenant impossible); expense stays `is_paid=true` while its cheque sits in `bounced` (no expense listener on `InstrumentBounced`; recoverable via re-present/cancel; shape approved in the ⑤a exit r3); dead legacy models `BankReconciliation`/`BankReconciliationItem` retained with zero writers; carried r7 minors (pluralization concatenation, `ManualMatchSearch` `min > max` bounds and partial `disabled` propagation, `MODULE_PERMISSIONS` key shape, English-coupled smoke selectors, unchecked casts in the upload wizard); spec §6.5 states a different completion identity than the (correct) code — spec doc never amended; deleted 416-line `tenantScope.test.tsx` only partially replaced by `queryScope.test.ts` (mitigated by the `audit-tanstack-keys.mjs` lint guard).

## Ten-invariant table

| # | Invariant | Result | Key evidence |
|---|---|---|---|
| 1 | Outbound lifecycle replay-safe, append-only, GL-correct; deferred issue = 1 JE, no movement, old writer suppressed | **PASS** | Per-cycle action keys + semantic digests with conflict assertion (`OutboundInstrumentService.php:70-95,223-246,380-406,532-555`); issue JE with `movement_id: null` (`OutboundInstrumentIssuer.php:180-205`); `ExpenseService::settle` returns via `settleByInstrument` before the settlement movement/JE writer (`ExpenseService.php:528-530,613-666`); cancel atomically reopens the payment subledger with negative allocations and bcmath balance recompute (`OutboundInstrumentService.php:654-688`) |
| 2 | Matching is metadata; replay precedes validation; stable lock order; signed sums, rule-19 | **PASS** | Execution replay lookup with `lockForUpdate` precedes `handler->execute` (`StatementMatchingService.php:115-127`); movements locked sorted by ID before totals (`:217-222,303-325`); signed totals and canonical amounts all bcmath at resolver scale (`:243-255,377-408`) |
| 3 | Tier 4 groups by method + fiscal business date, one fee JE/movement; Treasury→Expense synchronous only | **PASS** | Group key `method->id\|businessDate` (`CardBatchResolver.php:117`); exactly-once fee via idempotency key + digest-checked replay (`AcquirerFeeService.php:46-59,122-150`); all five cross-module listeners lack `ShouldQueue`, registered synchronously (`EventServiceProvider.php:77-94`); fail-loud response-channel events (`CreateExpenseHandler.php:41-55`) |
| 4 | Shared signed ignored-total identity; matched/resolved_by_creation/ignored resolve; checkpoint stamped | **PASS** | `signedTotal == (closing − opening) − signedIgnoredTotal` at completion (`StatementCompletionService.php:156-160`) and in the nightly tamper check (`ReconcileTreasuryCommand::reconciledStatementTamperReason`); terminal statuses (`StatementCompletionService.php:107-124`); frontend identity mirrors it (`status.ts:11,26-28`); end-of-period checkpoint in company timezone (`StatementCompletionService.php:165-171,257-264`) |
| 5 | record()/transfer() reject on/behind checkpoint before partial effects; offline uses flag + alert | **PASS** | Guard throws before any insert in both paths (`TreasuryMovementService.php:98-102` before `:123`; `:255-256` before `:278`); replay precedes checkpoint policy (`:72-77,249-253`); only `allowBehindCheckpoint` yields `recorded_behind_checkpoint` + warning (`:735-757,163-170`) |
| 6 | Ownership, middleware, scoped FKs, permission splits; legacy surfaces gone; UI → /treasury/statements | **PASS** | Single route group `['api','auth:sanctum',SetPermissionsTeam,EnforceTokenTenantClaim]` (`routes.php:35`); all permissions seeded (`RolesAndPermissionsSeeder.php:216-238`); all 8 legacy `/bank-reconciliations` endpoints removed and 404-asserted (`BankReconciliationCutoverTest.php:27-48`); UI cutover complete (`routes/index.tsx`, Sidebar, FinanceHub). Minor: upload `findOrFail` (above) |
| 7 | Fiscal method resolution tenant+company scoped, replay-stable, payload untouched | **PASS** | `EloquentPaymentMethodResolver.php:36-49`; fail-closed null handling and read-only projection (`PosCoreReceiptProjection.php:760-780`) |
| 8 | Rules 19/20: no float money; queued projections don't rely on CompanyContext | **PASS** | Diff-wide grep for float-money patterns: only non-money hits and a test-side `Big.toFixed`; frontend is Big.js + decimal strings throughout (r7 independently corroborates); projection consumes explicit event tenant/company IDs |
| 9 | Live evidence honest and complete | **PASS with caveats** | ⑤a: issued→cleared→bounced→re-presented + cancel-from-bounced with expense reopen, 7/7 in 19.3s, screenshots (`treasury-phase5a-e2e/REPORT.md`); ⑤b: upload→Tier 1/3/4→created→ignored+acknowledged→completed, both same-date writes 422, reconcile 0/0/0/0 (`treasury-phase5b-e2e/REPORT.md:13-37`). Caveats: manual preconditions disclosed, smoke non-re-runnable (F4), toolchain counts never independently re-executed in any reviewer session including this one (sandbox denials) |
| 10 | No weakened tests, no unresolved Critical/Important, deploy docs match schema | **FAIL** | No test weakening found (the only deletions target removed legacy endpoints; instrument/reconcile test diffs are pure additions); 12 tenant migrations match the checklists' 3+9 claim; **but** F1–F3: r7 MAJOR unresolved at HEAD, no completed Gate 5 lane approval, contradictory r7 audit record |

## Prior-gate / deviation assessment

Gates ⑤a 1–4 + ⑤a exit (r3), ⑤b 0–4 all carry completed APPROVEs after documented reject/fix rounds. The four binding deviations in the handback (void-aware dedupe, stricter repository validation, ignored-line identity, company-scoped resolver) each cite gate authority; I verified deviations 1, 3, and 4 in code. The handback is otherwise honest — it self-declares PARKED, fabricated nothing, and its migration/permission/deploy claims check out — except for the r7 "empty output" statement (F2).

## Test/evidence assessment

Sampled tests are genuinely adversarial (e.g. `StatementCompletionHttpTest` asserts the accountant/admin permission split, 422s without acknowledgment, and the signed ignored total in the audit payload). No assertion removal, no skips, no status-code softening anywhere in the modified-test diffs. Reported counts (95 backend / 86+32 frontend / 6/6 smoke) remain unreproduced in-session — an environmental limitation shared by every reviewer on this gate, not a code defect.

## MERGE PREREQUISITE: NOT SATISFIED

`multiloc-gate-3a` is not an ancestor of `origin/dev` (cached tip `1c8a5cd94`, 2026-07-20 — the same fetch the handback records; a fresh fetch was sandbox-denied this session, so re-confirm at merge time). The §3 schema/package is absent. This is an external PARK condition, **not** a Phase ⑤ implementation defect — the two must not be conflated.

---

**VERDICT: spec ✅ + quality CHANGES-REQUESTED**

**Controller condition (exact):** Keep `feat/treasury-phase5` PARKED. Before any approval: (1) wire all six date render sites through `lib/format.ts` `formatDate` with proper timezone conversion for `occurred_at`; (2) give the ⑤b smoke a reset/teardown path for the repository it permanently reconciles; (3) correct the audit record — the r7-interrupted note and `HANDBACK-treasury-phase5.md:68` must acknowledge the completed r7 REJECT; (4) either obtain one completed post-fix approval per Gate 5 lane or record an explicit controller decision adopting this exit review as the superseding verdict for the treasury lane's verified C1/C2/I1/I2 closures; (5) re-run the focused guardrails (`pnpm typecheck`, `pnpm lint`, statement Vitest, targeted PHPUnit paths) in a session where they can execute. Merge additionally requires independent confirmation that the complete multi-location §3 package has landed on `origin/dev`. Do not merge, tag, or promote until all of the above hold.
