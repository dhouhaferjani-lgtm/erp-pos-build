GATE VERDICT: REJECT

# Gate t5b-gate-4 — Wave 4 (Tasks 8–9) review

Reviewed `cc68744fd...5fe62aea8` (commits `c66d18af0`, `5fe62aea8`) against spec Rev 2 §§5.1/6.1/6.5–6.7/9–11, the three pre-dispatch reviews, plan Wave 4, and CLAUDE.md rules 1–21. Every file in the diff was read; all four new test suites, the three regression suites, the port, the completion service, the command, the migration, events, subscriber, routes, controller, seeder grants, and the Wave 3 matching service (lock-order consistency) were inspected directly. I could not re-execute tests in this session (sandbox denied `phpunit` invocation), so the committed run evidence stands unre-verified at runtime; every assertion below is from reading the actual code and tests.

## Findings

**[IMPORTANT] `apps/api/app/Modules/Treasury/Application/Services/StatementCompletionService.php:152-158` — the ignored-line acknowledgment path is unreachable for any internally-consistent bank statement — the §6.3 accept-known-discrepancy workflow dead-ends — fold `signedIgnoredTotal` into the delta identity.**
The statement-delta check at :153 (`Σ signed non-ignored lines == closing − opening`) runs *before* the acknowledgment gate at :156, and `$signedIgnoredTotal` (correctly computed at :112-114) is never used in the identity — only shipped in the event payload (:174). A real statement's lines always sum exactly to `closing − opening`; ignoring any line whose signed amount doesn't net to zero therefore makes :153 throw `'Signed statement lines do not equal closing balance minus opening balance.'` regardless of `acknowledge_ignored_total` and the `bank-statements.reopen` permission. The entire §6.3 mechanism ("completing a statement with any ignored line means knowingly accepting a bank≠ledger discrepancy: completion then requires the reopen-level permission and records the ignored total") can only ever fire for ignored sets netting to exactly 0.000. The covering test hides this: `tests/Feature/Treasury/StatementCompletionHttpTest.php:81-111` builds opening `0.000` / closing `0.000` (:162-163) with one ignored `+10.000` IN line — a statement whose own lines sum to +10 against a delta of 0, i.e. internally inconsistent bank data — so the delta check passes for the wrong reason. Spec §6.5's literal sentence matches the code, but it directly contradicts §6.3's normative acknowledgment flow in the same Rev 2; the implementation resolved the conflict in the direction that leaves the operator with no legal path to complete a statement containing one bogus bank line (worst case: repository checkpoint can never advance, or the operator fabricates a fake allocation to force the identity — strictly worse). Fix: compare `Σ signed non-ignored` against `bcsub($expectedDelta, $signedIgnoredTotal, $scale)`, keep the elevated-permission+flag gate, rebuild the fixture with a consistent statement (closing = opening + all lines), and change `ReconcileTreasuryCommand::reconciledStatementTamperReason()` (same identity, `ReconcileTreasuryCommand.php` new check-5 delta comparison) **in lockstep** — today both are consistent-but-wrong together; fixing only completion would make every acknowledged-ignored statement false-alert as tampered on the next nightly run.

**[MINOR] `apps/api/tests/Feature/Treasury/StatementCompletionConcurrencyTest.php:35-61` — the match-vs-complete race test's title over-claims.** Because the line starts `Unmatched`, completion can only succeed *after* the allocation committed; the "allocation attempt against an already-`Reconciled` statement is rejected" interleaving named by the test never occurs, and the assertions accept either parent outcome (:55-58). It still proves no torn state under real two-process contention, and the second test (:63-91) is a genuine, deterministic proof of the shared-movement completion race — the load-bearing Gate 4 requirement is met. Suggested fix: rename, or force the reconciled-then-allocate ordering explicitly (the single-connection guard lives in Wave 3's suite).

**[MINOR] `apps/api/tests/Feature/Treasury/TreasuryCheckpointGuardTest.php:53,68` — checkpoint fixtures use a raw `'2026-07-31 23:59:59'` UTC stamp rather than the completion-produced value (22:59:59Z for Africa/Tunis), so the stored checkpoint's *local* date is 2026-08-01, and no test asserts the permissive side of the boundary (a movement on the first business day after `period_end` is accepted).** I verified `checkpointDisposition` (`TreasuryMovementService.php:735-753`) analytically on both sides — date-truncated inclusive comparison in the repository company's timezone is correct with either fixture — but the boundary matrix is only half-pinned by tests. Add one accepted-day-after case using a completion-stamped checkpoint.

**[MINOR] `apps/api/tests/Feature/Treasury/StatementCompletionHttpTest.php:99-110` — no branch proves that an admin *without* `acknowledge_ignored_total` is rejected**; the flag requirement and the permission requirement (`StatementCompletionService.php:156` requires both) are only tested conflated. One extra request pins it.

## Mandatory checks — verified clean (evidence)

1. **Locking/races:** exact required order — statement :36 → lines :49-53 → allocations :57-61 → executions :63-67 → every allocation-referenced *and* execution-produced movement in stable string-sorted ID order :69-86 → repository :91-96; identical movement-lock discipline as Wave 3 (`StatementMatchingService.php:300-322`, statement-first at :274), so no lock-order inversion. Both PostgreSQL tests are genuine two-process proofs (`pcntl_fork`, child `DB::disconnect`, socket-gated release, `connectionsToTransact()` disabled on pgsql).
2. **Completion semantics:** Reconciling-only :44; earlier-open-period block :240-249 (Voided excluded); terminal/ignored-shape revalidation under lock :104-141; per-line exact signed sums with the normative direction convention :131-136; global per-movement cap under movement locks :143-150; checkpoint = inclusive end-of-day `period_end` in company timezone :252-259 (companies.timezone is NOT NULL, `create_companies_table.php:61`); status+checkpoint+balance in one transaction :161-166; audit event `afterCommit` :167-177.
3. **Reopen:** Reconciled-only :193; route admin-only (`routes.php:286-289` + seeder — reopen in admin block :238, absent from accountant block :714; 403 proven in HttpTest :70-72); no-gap guard with row locks :196-204; checkpoint recompute-or-clear :215-224; durable `audit_events` rows for both events (subscriber wiring + `StatementCompletionHttpTest.php:124-137`).
4. **Port placement:** `record()` — occurrence resolved after repo lock :70, exact replay :75-77 before currency/freeze/checkpoint policy, checkpoint :98-102 before GUC :108 / savepoint :121; `transfer()` — repo locks :212-219 → replay :251-253 → checkpoint on **both** repos with bypass hardcoded `false` :255-256 → GUC/savepoint. `handleIdempotentHit`/`handleTransferIdempotentHit` (:536-654) validate full semantics, so a mismatched "replay" still conflicts loudly.
5. **Interactive vs projection:** canonical `RepositoryCheckpointException` declared on the shared contract (`TreasuryMovementServiceInterface.php:52-55,93-94`); `allowBehindCheckpoint` defaults false (unit-pinned), set only by the three fiscal bridges via `! isServerOnly()`; flag independent of `recorded_while_frozen` (guard test :108-109) with warning log + event/audit payload.
6. **Regressions:** value-dated inbound clear/bounce, outbound clear/bounce (state and movement-count asserted unchanged on rejection), create-from-line atomic rejection (zero executions/allocations/movements/JEs), exact replay wins after checkpoint advances for record *and* transfer, audit + endpoint exposure of the new flag.
7. **Task 9:** tamper check re-derives the full signed model read-only; stale check config-driven (`treasury.statement_stale_days`, clamped ≥1), Voided excluded; statement-specific `audit_events` (`treasury.reconcile.statement_tamper`/`statement_stale`), FAILURE exit, operator notification, and **no freeze anywhere** in the statement path; valid/recent/voided statements proven non-alerting.
8. **Scoping/isolation:** controller fail-closed (`BankStatementController.php:160-171` — UUID guard + tenant + company); command company-scoped with per-company try/catch (statement-check error doesn't abort later companies), `getScaleSafe($currency, 3)` console-safe, notification failure swallowed into `logStatementAlertFailure`.
9. **Schema/deploy:** migration `2026_07_19_110007` additive, `default(false)`, `hasColumn`-guarded both directions, separate from the freeze flag; exposed in model cast/PHPDoc, movement endpoint, and audit payload; deploy checklist lists the migration (§17.8), stale threshold, and checkpoint/projection/reopen/alert runtime verifications.
10. **Hygiene:** no GL/movement writes from matching metadata paths, bcmath-only money, repository writes limited to `last_reconciled_*` (never `balance`), no weakened tests (the `StatementCreationActionTest` count-scoping change preserves the zero-financial-write invariant), plan Task 8/9 boxes genuinely complete.

**VERDICT: spec ❌ (one Important defect — §6.3 acknowledged-ignored completion is functionally dead-ended by the §6.5 delta-check ordering) + quality CHANGES-REQUESTED.**

Before Wave 5: change the completion delta identity to `Σ signed non-ignored == (closing − opening) − Σ signed ignored` (keeping the elevated-permission acknowledgment gate), mirror the same identity in `ReconcileTreasuryCommand`'s tamper check in the same commit, and replace the HTTP fixture with an internally-consistent statement proving a non-zero ignored total actually completes — the Wave 5 workspace completion CTA must not be built on the current dead-ended path.
