GATE VERDICT: APPROVE

# Gate t5b-gate-3 — treasury-reviewer report (Wave 3: Tasks 5–7, commits `90220fced`/`53cf10fb2`/`9d3e460ef`)

I reviewed the full Wave 3 diff (53 files, +4,684) against spec Rev 2 §§5.4/6.1–6.4/9–11, the Codex plans review (B3/B4/H6/H7/H9/H11), the treasury and tenancy-authz reviews, and plan Tasks 5–7. Every mandatory check below was verified by reading the code, not the diff description. **Caveat:** independent test execution and PHPStan re-runs were blocked by this session's permission mode (Bash approval required for `phpunit`); I compensated by verifying the test files assert real DB/GL state at source level. No Critical or Important defect found.

## Mandatory gate checks — all verified

1. **Matching is metadata.** `allocate()` writes only allocation rows (`StatementMatchingService.php:32-45`, `:254-263`); money only via handler execution inside the matcher's single `DB::transaction` (`:110-170`), with `AcquirerFeeService.php:42-44` and `GeneralLedgerService.php` (new builder) both refusing to run outside an enclosing transaction.
2. **Locking and signed math.** Statement aggregate lock first (`:274-291`), movement rows locked in stable ID order (`sort($movementIds, SORT_STRING)` `:221`, `orderBy('id')->lockForUpdate()` `:307-315`) **before** allocation sums are read (`:232-234`); per-movement cap `:235-236` and signed per-line window `0 ≤ Σ± ≤ line.amount` `:240-251`; ownership enforced on tenant/company/repository/currency `:307-311`; manual opposite-direction rejected `:229-231` (`allowOppositeDirection=false` only for `allocate()`), execution groups net `In gross − Out refund − Out fee` via `signedLineTotal()` `:374-390`.
3. **First-allocation race is PostgreSQL-proven.** The empty-allocation race is serialized by the movement-row lock itself; `StatementMatchingConcurrencyTest.php:34-111` is a genuine two-process `pcntl_fork` proof on **two different statements** (so the statement lock can't mask it): exactly one winner, sum stays `60.000`. Reconciled/Voided mutation blocked at `:282-284`; ignore/unignore write shapes satisfy `bank_statement_lines_ignore_shape_check` (migration `110002:67-70`; `ignore()` `:87-91`, `recomputeStatus()` nulls both fields `:362-366`).
4. **Replay before handler.** `executeAndAllocate` looks up `action_key` under `lockForUpdate` and compares the semantic digest **before** any handler/transition runs (`:113-125`); exact replay reuses `produced_repository_movement_ids` (`:125`), mismatch throws (`:123`), and `unallocate()` deletes allocations only — it never touches `bank_statement_match_executions` (`:47-63`). DB backstop: `action_key` unique (`110004:24`). Proven behaviorally with a call-counter handler in `StatementMatchingServiceTest.php:141-192` and digest-mismatch at `:194-236`.
5. **Tiers 1–3 deterministic and read-only.** No writes anywhere in `StatementSuggestionService`; deterministic tie-broken sort `:121-133`; eligibility is `remaining > 0`, not `∃ allocation` (`:203-212`); Bounced outbound instruments are suggested (`:298`) and `OutboundClearHandler.php:43-57` dispatches `Received → clear(valueDate)` / `Bounced → represent()` per the H6/⑤a contract.
6. **Tier 4 card netting.** Groups keyed `method.id|businessDate` from the **fiscal event's business date** (`CardBatchResolver.php:116-117`) — two methods can never combine; only mapped methods (`default_repository_id` = statement repo, `has_deducted_fees`, active, `fee_account_id` set, `:79-88`); refunds join as negative members (`:128-130`); any allocated member excludes the whole group (`:131`, `:137`); multi-day combos are structurally impossible and `assertGroup` requires the exact set (`:152-173`). Fee closure is exact: `calculateFee(gross) == fee` and `gross − fee == line.amount` (`AcquirerFeeService.php:65-71`, `:116-119`).
7. **AcquirerFeeService.** Idempotency probe before any GL (`:46-59`); tenant/company/method/repository/account routing fail-closed incl. active expense-type fee account (`:73-104`); Dr `fee_account_id` / Cr the exact repository `gl_account_id`, `journal_code` BQ (`JournalCode.php:37` maps `acquirer_fee → Bank`), synchronously posted at line value date (`GeneralLedgerService.php` new `createAcquirerFeeJournalEntry` + `postEntryNow`); one Out movement on existing `MovementSourceType::Adjustment` at value date (`:132-149`); non-zero `treasury.acquirer_fee_vat_rate` fails closed (`:106-114`, `config/treasury.php`, test `AcquirerFeeServiceTest.php:168-187`).
8. **Unmatch/reconfirm exactly-once.** `AcquirerFeeServiceTest.php:129-145` asserts 1 execution, 1 fee movement, 1 JE after unmatch+reconfirm; the replay path never re-enters the handler, so no orphan GL can be minted.
9. **Create-expense/income via synchronous events only.** Treasury emits `ExpenseCreationRequestedFromStatement`/`IncomeCreationRequestedFromStatement`/`ExpenseSettlementRequestedFromStatement`; listeners registered non-queued (`EventServiceProvider.php:83-94`) so a listener throw rolls back the whole matcher transaction (proven by `StatementCreationActionTest.php:131-148` — zero movements after rejection). Document/JE/movement dates = line value date, location from line, income requires an explicit **active Revenue** account (`CreateIncomeFromStatement.php:21-29`, asserted at `StatementCreationActionTest.php:95-129`).
10. **Scoping and hygiene.** All six routes behind `can:bank-statements.reconcile`; controller scopes line lookup to CompanyContext tenant+company with UUID guard (`StatementLineController.php:109-122`); matcher verifies the acting user belongs to the statement tenant (`:285-287`); no floats (digest even rejects float params, `StatementActionDigest.php:33-35`); no new `MovementSourceType`; no direct `payment_repositories.balance` writes — everything goes through the movement port.

## Findings (severity order — none blocking)

- **[MINOR]** `StatementSuggestionService.php:179-193` and `CardBatchResolver.php:33-40` — both load the repository's **entire movement history** (plus all allocations / payments / fiscal events) per suggestion GET, with no date bound or SQL-side remaining-capacity filter — on a mature card repository this is an O(history) hot path per line, and arbitrarily old unallocated card days stay suggestible whenever the fee math coincidentally closes — bound the candidate window in SQL (e.g. `occurred_at ≥ period_start − window`) during Wave 5 workspace hardening.
- **[MINOR]** `AllocateStatementLineRequest.php:36` — amount regex `/^\d+(?:\.\d+)?$/` omits the rule-19 per-scale ceiling (`{1,3}`) — excess precision surfaces as a domain error instead of a 422 validation error — the service's exact-scale gate (`StatementMatchingService.php:393-405`) is authoritative, so money is safe; align the FormRequest regex when next touched.
- **[MINOR]** `StatementSuggestionService.php:396` computes tier-4 fee against the line's *remaining* amount while `AcquirerFeeService.php:69` closes against the *full* `line->amount` — on a partially-allocated line a coincidentally-plausible group suggestion would always fail confirm — dead-end suggestion only; all invariants hold at confirm.
- **[MINOR]** `StatementMatchingService.php:47-63` — unallocating a movement that has no allocation silently no-ops; a domain error would be more honest API behavior.
- **[MINOR]** `StatementActionDigest.php:17-25` preserves list order and raw numeric strings — reconfirm must resend byte-identical params or replay throws mismatch rather than reusing — fail-closed and proven workable with verbatim suggestion params.
- **[OBSERVATION — accepted sequencing risk]** `ExpenseService.php:363/:424/:588` and `IncomeService.php:182` now backdate **all** expense/income movements to payment/document date (the spec'd value-date propagation), which widens the pre-Task-8 window in which ordinary expense posting can write movements behind a reconciled checkpoint. Per the binding H7 correction this guard is Task 8's — not a Gate 3 rejection ground — but it makes Task 8 non-negotiable and urgent.

**VERDICT: spec ✅ + quality APPROVED**

Findings in severity order: all Minor (candidate-scan bounds; FormRequest scale regex; tier-4 partial-line suggestion dead-end; silent unallocate no-op; digest byte-strictness) plus the accepted Task-8 sequencing observation.

Before Wave 4: nothing must change in Wave 3 — but Wave 4 must land the H7 checkpoint guard in **both** `record()` and `transfer()` (end-of-day boundary, `recorded_behind_checkpoint` projection flag) reusing this wave's stable-ID movement lock order, since the now-live value-date backdating is otherwise unguarded against reconciled periods.
