GATE VERDICT: APPROVE

Reviewer: Fable (claude-fable-5), whole-branch exit round 3, 2026-07-21, controller-dispatched. Target: HEAD `dd20d479f` on `feat/treasury-phase5` in `/Users/houssamr/Projects/syneriva/apps/erp.treasury-phase5`. Scope since r2 target `e07e7e655`: `3c02ee19a` (.gates ×3 + HANDBACK correction), `b61d7c20e` (9 web files: 5 components, 3 test files, smoke), `dd20d479f` (.gates ×2) — verified by `--stat` that nothing else changed; every money-bearing invariant r2 verified at `e07e7e655` is untouched by this delta.

## r2 controller-condition closures (each independently verified, not taken from the audit trail)

- **(1) Date localization — CLOSED.** All six sites render through `lib/format` `formatDate` at HEAD: `StatementListPage.tsx:65`, `ReconciliationWorkspacePage.tsx:115` (subtitle) and `:129` (line rows), `LinePanel.tsx:75`, `StatementUploadWizard.tsx:167`, `ManualMatchSearch.tsx:42` — the last now passes the full `occurred_at` timestamp (restoring timezone conversion) instead of `.slice(0,10)`. Grep confirms zero remaining raw date renders under `statements/`. The three new tests are genuinely red-first: each asserts the formatted value present **and** the raw ISO absent (`LinePanel.test.tsx:114-118`, `ReconciliationWorkspacePage.render.test.tsx:74-80`, `StatementUploadWizard.test.tsx:85-86`). I ran them: 35/35 pass.
- **(2) Smoke teardown — CLOSED as worded, with one NEW residual (Minor, below).** Step 7 (`treasury-phase5b-reconciliation.smoke.ts:852-873`) POSTs the real `/bank-statements/{id}/reopen` and asserts `last_reconciled_at` is `null`. I traced `StatementCompletionService::reopen()` (`:188-244`): tenant-checked, Reconciled-only, later-statement guarded, checkpoint recomputed from remaining reconciled statements → null. The scarce unreconciled-repository fixture is genuinely released; repository selection (`smoke.ts:345-363`) re-finds it; per-run `RUN_ID` uniqueness (`:20`) defeats the duplicate-file/fingerprint guards. This strengthens, not weakens — teardown runs after all assertions, via a permission-gated real endpoint, loud on failure.
- **(3) Audit record — CLOSED.** `3c02ee19a` commits the recovered 55-line r2 REJECT as `gate-t5b-exit-verdict-fable-r2.md`; both interruption notes carry explicit dated controller corrections **while preserving the original incorrect text** (the right way to fix an audit trail), and `HANDBACK-treasury-phase5.md:68-69` uses strikethrough + correction acknowledging the completed r7 REJECT and r2 REJECT as authoritative. F2 is cured; no finding of intent was made and none is made now.
- **(4) One completed post-fix approval per Gate 5 lane — CLOSED.** `gate-t5b-gate-5-verdict-treasury-r6.md` and `gate-t5b-gate-5-verdict-frontend-conventions-r8.md`, both Opus, both completed APPROVEs at `b61d7c20e`, committed in `dd20d479f`. I sampled both against code (C1 fixture binding at `smoke.ts:700`, C2 acknowledge gating at `ReconciliationWorkspacePage.tsx:110-118` mirroring `StatementCompletionService.php:161-163`, I1 injected resolver at `AcquirerFeeService.php:49`, I2 `bcformatStrict` reductions in `StatementMatchingService.php:232-237` / `StatementCompletionService.php:144-150`) — all hold. No contradiction between lanes.
- **(5) Guardrails re-run in an executing session — CLOSED, reproduced by me this session:** statement Vitest **35/35**; `pnpm typecheck` **clean**; full `pnpm lint` **exit 0** (design-system baseline 750 acknowledged / 0 new / 0 stale; tenant-keys Gate C 0 new / 0 stale); scoped ESLint 0 errors (44 warnings, all acknowledged classes); backend by path **96 passed** (statement set: cutover/matching/completion/checkpoint/isolation) + **58 passed** (resolver/routing/projection/bridge) — matching the claimed 58+96 exactly; targeted PHPStan on the four touched statement services **no errors** (the 3 larastan `TreasuryAlertRecipients` env artifacts did not arise and are per addendum not findings); Pint on `app/Modules/Treasury` **pass**.

## Findings by severity

**Critical** — none.

**Important** — none. The r2 Importants F1–F4 are all closed (above); nothing new of this severity was found in the `e07e7e655..dd20d479f` delta.

**Minor — NEW (ticket, non-blocking)**

- **N1 — ⑤b smoke is same-day re-runnable but blocked across calendar days by its own teardown leftover.** Reopen leaves the statement permanently in `Reconciling` (it can never be voided: `StatementImportService::void():257-258` rejects statements with allocations/executions, which this one has). `assertNoEarlierOpenStatement` (`StatementCompletionService.php:99,245-256`) counts `Reconciling` statements with strictly earlier `period_end` — so a re-run on a later day against the same repository will 422 at step 6 completion ("An earlier active statement must be reconciled or voided first"). Same-day re-runs pass (`period_end` equal, `<` is false; `RUN_ID` uniqueness clears the file/fingerprint dedupe). Failure mode is loud, not false-green; product code is correct (production reopen→fix→re-complete is the intended workflow). The r2 condition as worded — release the permanently consumed repository fixture — is delivered; this is a narrower residual neither Opus lane caught. Fix is small: at setup, skip candidate repositories with active (non-Reconciled/Voided) statements, or re-complete leftovers. Must land before or with the owed DB-backed smoke re-run.

**Minor — carried (acknowledged tickets, not re-blocked per addendum):** r2 Minors (unscoped upload `findOrFail`; expense `is_paid` during bounce; dead legacy models; spec §6.5 identity wording; partial `tenantScope.test` replacement); frontend r8's five (pluralization concatenation; `ManualMatchSearch` `min>max` + partial `disabled`; `MODULE_PERMISSIONS` key shape; English-coupled smoke selectors; unchecked wizard casts); treasury r6's four (I4 `payment_methods_hash` reprojection drift; hardcoded `STATEMENT_DELTA`; dead legacy models; `formatDate` date-only-as-UTC day-shift for far-behind-UTC locales — pre-existing app-wide helper property); ⑤a exit's `SyncExpenseOnInstrumentLifecycle` port item. One observation, not a defect: the `invalidateQueries` filter change to a bare literal prefix (`StatementListPage.tsx:116`) is correct — it prefix-matches the `tenantScopedKey` query and the keys audit passes 0 new.

## Ten-invariant table

| # | Invariant | Result | Basis |
|---|---|---|---|
| 1 | Outbound lifecycle replay-safe, append-only, GL-correct; deferred issue = 1 JE, no movement, old writer suppressed | **PASS** | r2-verified at `e07e7e655` (`OutboundInstrumentService`, `OutboundInstrumentIssuer`, `ExpenseService::settleByInstrument`); zero backend money-path changes since |
| 2 | Matching is metadata; replay precedes validation; stable lock order; signed sums, rule-19 | **PASS** | r2-verified (`StatementMatchingService.php:115-127,217-222,232-255`); I2 `bcformatStrict` reductions re-confirmed this round; 96 statement tests pass |
| 3 | Tier 4 method+business-date grouping, exactly one fee JE/movement; Treasury→Expense synchronous | **PASS** | r2-verified (`CardBatchResolver.php:117`, `AcquirerFeeService.php:46-59`, `EventServiceProvider.php:77-94`); I1 injected-resolver fix re-confirmed at `AcquirerFeeService.php:49` |
| 4 | Shared signed ignored-total identity; three resolving statuses; end-of-period checkpoint | **PASS** | r2-verified (`StatementCompletionService.php:156-171`, `ReconcileTreasuryCommand`); completion HTTP tests re-run green this session |
| 5 | `record()`/`transfer()` reject on/behind checkpoint pre-effect; offline flag + alert only | **PASS** | r2-verified (`TreasuryMovementService.php:98-102,255-256,735-757`); `TreasuryCheckpointGuardTest` re-run green |
| 6 | Ownership, middleware, scoped FKs, permission splits; legacy surfaces gone; UI → `/treasury/statements` | **PASS** | r2-verified; cutover + isolation tests re-run green; C2 admin-acknowledge split confirmed FE+BE |
| 7 | Fiscal method resolution tenant+company scoped, replay-stable, payload untouched | **PASS** | r2-verified (`EloquentPaymentMethodResolver.php:36-49`); resolver + projection + bridge tests (58) re-run green |
| 8 | Rules 19/20: no float money; queued projections independent of CompanyContext | **PASS** | r2 diff-wide grep stands; the only code delta since is FE date *formatting* (display-only) and a query-key filter — no money surface touched; typecheck/lint/PHPStan re-run clean |
| 9 | Live evidence honest | **PASS with caveats** | ⑤a and ⑤b REPORTs unchanged and honest (preconditions disclosed). Honest statement for this round: **the live Playwright smoke was NOT re-run this session** (no DB-backed stack driven); step 7 teardown is committed code, never yet executed live, judged from code as both Opus lanes did. The repository consumed by the original live run predates the teardown and remains stamped — first DB-backed re-run must pick another fixture or manually reopen it |
| 10 | No weakened tests; no unresolved Critical/Important; deploy docs match schema | **PASS** | All deltas since `e07e7e655` are test additions or gates/docs; smoke step 7 adds assertions; r7 MAJORs fixed with completed lane approvals; audit record corrected; r2's 12-migration/deploy-checklist verification stands unchanged |

## Prior-gate / deviation assessment

The gate chain is now coherent end-to-end: ⑤a Gates 1–4 + exit (r3), ⑤b Gates 0–4, and Gate 5 with completed post-fix Opus approvals in both lanes (treasury r6, frontend-conventions r8) at `b61d7c20e`, with every reject/interruption round retained and the two false "empty output" records corrected in place. The four binding deviations (void-aware dedupe, stricter repository validation, ignored-line identity, company-scoped resolver) remain as r2 assessed them: cited to gate authority and code-verified. The tasking-premise defect that drove the r2 REJECT is cured — this round's premise ("both Gate 5 lanes pass") matches the committed record.

## Test/evidence assessment

I re-executed every focused guardrail this session; all reproduce the claimed numbers exactly (35/35 Vitest, 96+58 PHPUnit by path, lint/typecheck/PHPStan/Pint clean, both lint gates 0 new / 0 stale) — the first exit round in which the toolchain claims are independently executed rather than read. The three new date tests assert raw-ISO absence, so they would fail on regression. Sampled backend tests remain genuinely adversarial (permission splits, 422 paths, signed totals in audit payloads); no assertion removals, skips, or status-code softening anywhere in the delta. Outstanding evidence gap, unchanged and disclosed: one green DB-backed run of the amended smoke (now including the never-executed step 7) is still owed, and N1 should be fixed before or with it.

## MERGE PREREQUISITE: NOT SATISFIED

Fresh `git fetch origin dev` this session: tip `1c8a5cd94` (2026-07-20). `multiloc-gate-3a` is **not** an ancestor of `origin/dev` and the §3 schema/package is absent from `apps/api/database/migrations/tenant/`. This is the external PARK condition, distinct from Phase ⑤ quality — the implementation is approved; the branch must remain parked until multi-location §3 lands on `origin/dev`.

---

**VERDICT: spec ✅ + quality APPROVED**

**Controller condition (exact):** Keep `feat/treasury-phase5` PARKED — do not merge, tag `t5b-gate-5`/promote only per your tagging protocol, and do not push to `origin/dev` — until independent confirmation that the complete multi-location §3 package (tag `multiloc-gate-3a` and its schema migrations) is an ancestor of `origin/dev`. Before relying on the ⑤b smoke as regression evidence: (a) fix N1 by making setup leftover-statement-safe (skip candidate repositories with active non-Reconciled/non-Voided statements, or re-complete leftovers) and (b) execute one green DB-backed run of the amended smoke including step 7, on a fresh or manually reopened repository fixture. Ticket N1 and the carried Minors (r2's five, r8's five, r6's four, ⑤a's port item); none block merge.
