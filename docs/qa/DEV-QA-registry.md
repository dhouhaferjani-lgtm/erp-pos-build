# DEV-QA registry — trouvailles de recette ↔ tickets ↔ PR

Registre unique (CLAUDE.md règle 23, point 6). Une ligne par trouvaille, l'ID est définitif et se cite dans la PR (`DEV-QA-0NN`).

- `DEV-QA-001` … `DEV-QA-068` : campagne Codex + tests appairés Claude, consolidés le 2026-09-04 → [`2026-09-04-qa-reconciliation-technique.md`](2026-09-04-qa-reconciliation-technique.md) (verdicts, `file:line`, corroborations). Les IDs de ce document restent la référence ; ne pas renuméroter.
- `DEV-QA-069` … : recette Android + web de Dhouha du 2026-09-07/08 (source `BUGS-CLAUDE-CODE-2026-09-08.md`, IDs d'origine `QA-BUG-01..09`), puis les campagnes suivantes.

Statuts : `OUVERT` (reproduit, non corrigé) · `EN COURS` (lane assignée) · `PR` (correctif en revue, lien) · `MERGÉ` (dev) · `VÉRIFIÉ` (critère de sortie rejoué par le manager) · `PAS-UN-BUG` · `HORS-PÉRIMÈTRE`.

Règle : on n'écrit ici que ce qui est prouvé (scénario rejoué, `file:line`). La colonne « Preuve » pointe la PR, le rapport de gate ou la capture.

## Recette Android + web — 2026-09-08 (build mobile `main@51e3445c`, web `index-DtS1kHol.js`, API `api.erp.otospex.dev`, PharmaBio Tunisie SARL / STORE-TUN1)

| ID | Origine | Prio | Zone | Résumé | Repro | Statut | Lane / branche | Preuve |
|---|---|---:|---|---|---:|---|---|---|
| DEV-QA-069 | QA-BUG-01 | P1 | Mobile · Réception / caméra | Le deuxième scan caméra (retour liste → rouvrir la commande) ne résout plus l'article, aucun message | 2/2 | EN COURS | lane M-A `fix/scanner-lifecycle` | cause : `receiving/[id]/scan.tsx:41-44` `router.navigate` empile un 2e `[id]` au lieu de dépiler le scanner (StackRouter NAVIGATE sans `pop`/`getId`) → CameraView jamais démontée, verrou `scanned` conservé ; correctif `dismissTo` + caméra conditionnée au focus |
| DEV-QA-070 | QA-BUG-02 | P2 | Mobile · Comptage / saisie manuelle | Une saisie manuelle successive est ignorée tant que le scanner n'est pas fermé/rouvert | 2/2 | EN COURS | lane M-A `fix/scanner-lifecycle` | cause : `counting/[id]/scan.tsx:29-31` `scanned=true` jamais remis à faux après `router.push` ; l'écran reste monté → caméra désarmée (`:98`) et modale court-circuitée par le même garde |
| DEV-QA-071 | QA-BUG-03 | P1 | Mobile · Brouillon / saisie manuelle | « Saisir manuellement » n'ouvre aucune modale dans le scanner d'un brouillon ; `product_count` reste 0 | avant/après redémarrage | EN COURS | lane M-A `fix/scanner-lifecycle` | cause : `counting/draft/[id]/scan.tsx:103` `Alert.prompt` (iOS-only, no-op Android) — le correctif `366b852` a oublié ce 3e scanner |
| DEV-QA-072 | QA-BUG-04 | P1 | Mobile · Brouillons / persistance | Brouillon synchronisé listé mais « Brouillon introuvable » à l'ouverture après redémarrage | 2/2 | EN COURS | lane M-B `fix/local-state-offline` (WIP dispo sur `fix/qa-bug-04-drafts-restart`) | cause : la liste (`tasks.tsx:218`) pousse l'UUID serveur, le détail (`draft/[id]/index.tsx:61`) cherche `localId` (`draft_<ts>_<rand>`) → jamais de correspondance ; le redémarrage n'est pas la cause (création → `router.replace(localId)`) |
| DEV-QA-073 | QA-BUG-05 | P1 | Mobile · Comptage hors ligne | Après une première quantité mise en file, les cartes restent en chargement et le second comptage ne s'ouvre plus | 1/1 | EN COURS | lane M-B `fix/local-state-offline` | cause : `onlineManager` jamais relié à NetInfo + axios sans `timeout` (`src/lib/api.ts:8`) → refetch hors ligne bloqué à jamais (`isRefetching` figé, `tasks.tsx:175`) ; `gcTime` 5 min évince la 2e session → spinner infini (`counting/[id]/index.tsx:68`). PR #2 aggrave (test interdit `onlineManager`) |
| DEV-QA-074 | QA-BUG-06 | P2 | Mobile · Réception | Titre « Réception partielle » sur une réception complète 10/10 (API `fully_received`) | 1/1 | PR | erp-mobile [PR #3](https://github.com/otospexsolutions/erp-mobile/pull/3) `fix/qa-bug-06-receiving-title` @ ba57869 (Jest 59/415, tsc) | cause : `app/(app)/receiving/[id].tsx:53` compare `'received'`, l'API renvoie `fully_received` (`GoodsReceiptService.php:1232-1234`) |
| DEV-QA-075 | QA-BUG-07 | P1 | Mobile · Liste Tâches | Nouvelle affectation invisible (onglet, pull-to-refresh) jusqu'au redémarrage complet d'Expo Go | 1/1 | EN COURS | erp-mobile PR #2 `fix/list-freshness-policy` (gate) | PR #2 couvre le retour d'onglet (`useRefreshOnFocus`) ; le pull-to-refresh muet n'est pas prouvé (`tasks.tsx` sans état d'erreur, `refetch()` devrait appeler le réseau) — trace réseau requise |
| DEV-QA-076 | QA-BUG-08 | P1 | Web · Revue de comptage | Aucun champ de note pour justifier une variance auto-résolue ; finalisation possible sans justification | 1/1 | EN COURS | erp `fix/qa-bug-08-variance-note` | cause présumée : `ReconciliationTable.tsx` n'offre l'action que si `is_flagged && final_qty === null` ; `resolveSingleCount` fixe `final_qty` (`CountingReconciliationService.php`) |
| DEV-QA-077 | QA-BUG-09 | P1 | API · Mouvements | L'ajustement de comptage porte `reference=COUNT_REPLAY` et `source_document_id=null` au lieu du n° CNT + lien | 1/1 | PR | erp [PR #221](https://github.com/otospexsolutions/erp/pull/221) `fix/count-movement-reference-id` (PHPUnit sqlite 7/37 + PG 7/37, Vitest, preflight scopé) | cause : `reference_id` n'est PAS perdu (écrit par `recordMovement`, prouvé par le rapport d'écarts `appliedGrains`) ; l'API ne le sérialisait pas et ne résolvait `source_document_*` que pour `Document` ; libellé constante `COUNT_REPLAY` (`StockAdjustmentService.php:69`). La ligne staging existante garde `COUNT_REPLAY` : rejouer sur un comptage neuf |

Critères de sortie (à rejouer tels quels par le manager) : voir la section « Critères de sortie après correction » de `BUGS-CLAUDE-CODE-2026-09-08.md`.

## Recette POS Windows — 2026-09-18 (IziPOS device réel, tenant PharmaBio staging, terminal « En ligne »)

| ID | Origine | Prio | Zone | Résumé | Repro | Statut | Lane / branche | Preuve |
|---|---|---:|---|---|---:|---|---|---|
| DEV-QA-111 | Recette Dhouha 2026-09-18 (erp-28) | P1 | POS · Sync catalogue | Un produit supprimé sur le web reste indéfiniment sur le POS, affiché grisé « Rupture » à côté de son remplaçant homonyme | 1/1 (device Windows) | PR | erp `fix/pos-deleted-products-reconcile` | cause prouvée : le serveur n'émet `deleted_ids` que si `updated_since` est envoyé (`ProductController.php:186-203`) ; les migrations v57/v58 (`apps/pos/src/lib/db/migrations.ts:1788, 1827`) suppriment le curseur `products_last_sync`, donc le pull suivant est sans curseur et ne reçoit aucune pierre tombale ; `pullProductsCore` (`apps/pos/src/lib/sync/syncService.ts:716-800`) était upsert-only et ne réconciliait jamais les absences. Le « Rupture » vient de l'absence de tranche `location_stock` (`gridStock.ts:41-46`), pas d'un changement de statut. Correctif POS-side (option A du rapport `docs/sessions/2026-09-18-pos-deleted-products/root-cause.md`) : sur un pull complet **terminé et non vide**, les ids en cache absents du jeu retourné passent par la **même** cascade que `deleted_ids`. Gate : Vitest POS 370 fichiers / 3738 tests, lint 0 erreur, tsc. **Non vérifié sur device.** |
| DEV-QA-112 | Analyse de cause DEV-QA-111 (§7) | P2 | POS · Sync catalogue | Un produit **désactivé** (`is_active = false`) est toujours livré au POS dans `data` et n'est jamais mis en pierre tombale — le flux variantes fait l'inverse | code-trace (non rejoué sur device) | OUVERT | non assigné — **hors périmètre de `fix/pos-deleted-products-reconcile`** | `PosVariantFeedService.php:66-72` réunit `onlyTrashed()` **et** `is_active = false` dans `deleted_ids` ; le flux produits (`ProductController::index()`) ne connaît que `onlyTrashed()` (`ProductTombstoneService.php:19-25`). Les deux flux ne s'accordent pas sur ce que « disparu » veut dire. Correctif additif (option D du rapport) : il exige un test de va-et-vient désactivation→réactivation (règle 23 §1) et ne doit pas être embarqué dans le correctif de suppression. |
