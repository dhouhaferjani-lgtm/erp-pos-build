# Codex prompt — W-CASH-1 fix round 2 (paste into the SAME W-CASH-1 thread)

---

Fix round 2 for docs/superpowers/plans/2026-09-06-WCASH-1-custody-config-transfer-doc.md. Read the gate review docs/superpowers/reviews/2026-09-06-w-cash-1-slice-codex-gate-r2.md in full and revise IN PLACE to rev 3. Read-only on everything else; no code, tests, migrations or git commands; do not ask questions; reply with the path only.

Non-negotiables for rev 3:
1. B1 (transfer contract cannot represent system-authority callers): extend the transfer/document contract with an explicit actor union — authenticated user (with location scope) OR system intent (named source: fiscal projection, scheduled command) carrying tenant/company/terminal provenance — following the pattern the spec §W1 requires (HTTP adapter authorizes, service takes explicit intent). Name the DTO, the validation per branch, and the tests for both.
2. B2 (three-decimal money vs decimal(15,2) storage): this is a pre-existing precision-contract violation (CLAUDE.md rule 19 requires currency decimal(N,3) floor; payment_repositories.balance / last_reconciled_balance and journal_lines.debit/credit are decimal(15,2) at the cited migrations). Do NOT silently widen in an additive push. Instead: (a) add a named PREREQUISITE task "P0 precision widening" that lists every affected column with path:line, the widening migration (ALTER COLUMN TYPE decimal(15,3), PostgreSQL-safe, with a raw round-trip test using 1.005 and a schema-shape ratchet), and its own deploy note reconciled with the canonical manifest (a widening is NOT additive; it goes in its own push with a host-side backup before and a verified per-tenant migrate-rolling output after); (b) make T3/T4 depend on P0; (c) flag in the change log that this debt also affects existing POS receipt bridges and must be reported to the orchestrator as a program-level finding.
3. Close every MAJOR: M1 keep the old signature as a deprecated adapter until the last caller is migrated in the same task (name the caller and the removal step); M2 convention-09 per task with explicit rerun outcome; M3 repository-history document link with its implementation file set; M4 the live shadow consumer named + POS generated-globals resolution mechanism (how apps/pos imports generated types today — cite it); M5 document-per-action baseline row in the convention-10 matrix and the glossary.
4. Close the MINORs: notes canonicalization rule (trim, NFC, collapse whitespace, case-preserving) stated exactly for the replay comparison; unknown-field rejection via the FormRequest mechanism actually available (cite) with red tests; enum/CHECK parity gate test in T1; refresh the evidence baseline to current HEAD and fix the imprecise anchor.
5. Every claim about current code cites path:line at current HEAD; ≤6 tasks plus the P0 prerequisite; include a round-2 change log closing every gate item.
