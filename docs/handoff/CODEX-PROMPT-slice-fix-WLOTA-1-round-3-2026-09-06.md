# Codex prompt — W-LOT-A-1 fix round 3 (paste into the SAME W-LOT-A-1 thread)

---

Fix round 3 for docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-3.md. Read docs/superpowers/reviews/2026-09-06-w-lot-a-1-slice-codex-gate-r3.md in full and write rev 4 to docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-4.md. Read-only on everything else; no code, tests, migrations or git commands; do not ask questions; reply with the path only.

Close the 3 BLOCKERS exactly:
- B1 idempotency namespaces: root operation UUIDs (client-supplied) and transition UUIDs (deterministic UUIDv5) must NOT share one unique constraint. Use entry-kind-qualified partial unique indexes (or separate columns) so a requester cannot pre-compute a future `recalled` transition id and burn it as another root's operation id. Define replay lookups per kind; keep the status-independent "one open request per (company, batch, location)" constraint; add the adversarial collision test (red first, PG lane).
- B2 recall-writer census: extend to every DIRECT TEST SETUP that writes recall state (`update(['is_recalled'…])`, constructors, factories) — list each file:line, prescribe the replacement (factory state / service call), and add a test-source census with an explicit allowlist so future direct historical-state setups are classified. Production AST ratchet stays strict.
- B3 canonical eligibility interface for W-LOT-B: specify a public typed decision contract (input: company, product/variant, location, quantity, as-of; output: eligible/ineligible with reason set {inactive, globally_recalled, expired, reserved, locally_held}, source revision and snapshot age for D4). Route all three server issue paths (POS FEFO consumption, direct batch transfer, StockTransferService) AND the W-LOT-B snapshot generator through it. Add `companyId` to the atomic FEFO signature and SQL (cite the current signature and the `SKIP LOCKED` query at path:line) or specify exact company derivation; name the compatibility shim for existing callers.

Close the MAJORS: M1 Push 3 inert (no behaviour change until Push 5; rollback must not restore unsafe reads — reads stay scoped under both flag states, only the new mutation gate is flagged); M2 register `product_batches` (and every new table) in the convention-09 catalogue manifests (`TenantOnlyUniqueOnCatalogueTablesRatchetTest` allowlist/classification) with the exact file edit; M3 no Identity model import across the module boundary in public recall contracts — pass actor identity as a Shared/Contracts DTO (rule 6); M4 fix convention-10 rows that say DEFER/ALREADY for work this slice actually performs (they must be MATCH or DIVERGE with the reason); M5 deployment census variable gets executable pass/fail criteria (exact command, exact stdout marker, exit rule).
Close the MINORS: refresh the planning baseline to current HEAD; re-verify Task 5's "exact files" list against that HEAD.
Include a round-3 change log with every gate item CLOSED (plan line) or REJECTED (citation). ≤6 tasks. Every code claim cites path:line at current HEAD.
