# Codex plan gate r3 prompt — RBAC wave 0b plan rev 3 (2026-09-10)

You are the adversarial PLAN gate (round 3) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox; artisan cannot boot here — use the shared checkout /Users/houssamr/Projects/syneriva/apps/erp read-only for route arithmetic, or docs/superpowers/audits/2026-09-09-roles-permissions/02-route-enforcement-sweep.csv). Declare `git rev-parse --short HEAD`. Review ONLY docs/superpowers/plans/2026-09-10-rbac-wave-0b.md (REV 3). Do NOT review the wave-1 plan this round (its own fix round is pending). The plan implements the ACCEPTED spec docs/superpowers/specs/2026-09-10-roles-permissions-catalogue-design.md (rev 9, gate r9 ACCEPT; a concurrent editorial amendment A-1 (rev 9.1) is being recorded that moves item 0a-4 to 0b-15 with ceilings 152/146/298 post-0a and 152/142/294 post-0b-15 — treat that move as SETTLED even if the spec file has not yet been updated when you read it).

FIRST read your round-1 register docs/superpowers/reviews/2026-09-10-rbac-plan-0b1-codex-gate-r1.md ("## Wave 0b" section, cross-plan section, citation rows for 0b) and produce a "Rev-1 closure table" (finding → CLOSED at rev-2 anchor / NOT CLOSED / REJECTED-correctly) before new findings; verify the plan's "Change log rev 1 → rev 2" against code. Rulings applied in rev 2 and SETTLED: admin v0 = all nineteen additions, `--grant-to` = the seven manager/general_manager keys; no knowingly red commit + run list per commit; call-site/method-level PermissionWriterCensus (nikic/php-parser) with LOCKED / LOCK_INHERITED_FROM / INITIALIZATION-ONLY categories and per-method row-order assertions; every FE caller guarded (16-row census incl. AddCompanyModal, ServiceListPage) + `apps/web/e2e/rbac-module-gates.spec.ts` + frontend-conventions-reviewer precondition; `/service-categories` canonical (FE fixes incl. PUT→PATCH); `AdminRoleMissing` → FAILED reason=admin_role_missing on apply and dry-run; `scripts/permissions-ensure-0b.sh` atomic two-invocation wrapper writing the status file; fleet helpers supplied; `Phase 0.2.<task>:` subjects; PG harness on every PHPUnit command. Scrutinise: (a) run the PermissionWriterCensus logic mentally/mechanically against `dev` and `lane/w-lot-a-1a` — does the fixture of 20 (+3 lane) writer methods match a fresh AST-level enumeration (list any missing/extra); is LOCK_INHERITED_FROM proven, not waived; (b) every FE guard's line at dev and after the lane merge (cite lane lines); (c) the e2e spec's assertions cover each UX-visible change with 5xx/console capture; (d) the wrapper script's exit semantics and status file; (e) each task's red test compiles at dev tip and fails for the stated reason; implementations complete; (f) 0b entry condition and overlap statements vs CURRENT lane tips (re-measure); (g) the section "Signature changes for wave 1" — is it accurate (none) and are the three re-pin notes correct; (h) M0b-2 partial closure — acceptable as execution-discipline note, or must tasks be re-sequenced? If only MINOR findings remain, the verdict MUST be DISPATCH-READY — then list the minors as an editorial list. Output heading: "# Codex plan gate r2 — RBAC wave 0b plan rev 2 (gpt-5.6-sol, high, read-only, 2026-09-10)", sections "## Rev-1 closure table / ## BLOCKER / ## MAJOR / ## MINOR / ## Citation audit / ## Rejected false positives / ## Preserve / ## Owner decisions required / ## Dispatch assessment", LAST LINE exactly `VERDICT: DISPATCH-READY` or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Print in full.

Round-3 additions: FIRST read your round-2 register docs/superpowers/reviews/2026-09-10-rbac-plan-0b-codex-gate-r2.md and produce a "Rev-2 closure table" (finding → CLOSED at rev-3 anchor / NOT CLOSED / REJECTED-correctly); verify the "Change log rev 2 → rev 3" against code. Rulings applied in rev 3 and SETTLED: `lane/t2-receipt-spine` merged is a whole-wave 0b entry condition (check `git merge-base --is-ancestor lane/t2-receipt-spine dev`); row-order assertion applies only to multi-row `roles` lockForUpdate chains (no equality on roles.name/roles.id; whereIn forces multi-row; fail-closed on unresolvable chains), provisionGeneralManager documented as the single-row exception; AST-bounded method extraction; snapshot passed into grantMissing; complete Task 4 recipe from post-lane bodies; 22-row FE census incl. service-category guards, AddCompanyModal component guard, useCategoryTree gated at callers; e2e specs assert redirect-to-/dashboard + request absence (RequirePermission redirects without fallback); map regeneration moved into Task 6 step 3(f); `Http` manifest group 2→3; `LOCK_INHERITED_FROM` is now `array<string, list<string>>` (helper → every entry point) with `PermissionWriterCensus::methodSource()`/`callersOf()`; TemplateDeltaApplier acquires nothing, two callers (sync + reapplyTemplate). Scrutinise: (a) run the row-order rule against every lockForUpdate chain in the lane's LotActionPermissionDelta and confirm the test is green on the post-lane tree; (b) each FE guard line at the post-lane tree; (c) each task's red test compiles and fails for the stated reason; (d) entry-condition table vs CURRENT lane tips; (e) Task 6 typecheck viability after map regeneration; (f) any residual placeholder. If only MINOR findings remain, the verdict MUST be DISPATCH-READY (noting the wave cannot START until both lanes merge) — then list the minors as an editorial list. Output heading: "# Codex plan gate r3 — RBAC wave 0b plan rev 3 (gpt-5.6-sol, high, read-only, 2026-09-10)".
