DESIGN PRE-APPROVED — non-interactive run: do NOT stop to ask for approval; do the edits now.

Fix round 7 for slice plan W-LOT-A-1a, performed as IN-PLACE EDITS. docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-8.md already exists and is a byte-for-byte copy of rev 7 (docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-7.md) with only the three header lines changed. Edit rev-8.md with targeted patches; NEVER rewrite, regenerate or re-emit the whole file; never delete or paraphrase a subsection you are not required to change; do not touch any other file. No tests, no git commands, no questions. Final message = a short list of the subsections you edited.

Read in full first: docs/superpowers/reviews/2026-09-06-w-lot-a-1a-slice-codex-gate-r7.md (findings) and docs/superpowers/plans/2026-09-09-parapharmacy-staging-topology-U1-resolution.md.

Close every finding with the gate's minimum correction:
- B-C1 (tenant database names): never synthesise `tenant_${uuid}`. Add to §11.2/§11.3 an executable producer that runs INSIDE the API container and prints one `<uuid>\t<database>` line per tenant using the canonical resolver `Tenant::getDatabaseName()` (apps/api/app/Modules/Tenant/Domain/Tenant.php:264; config prefix at apps/api/config/tenancy.php:59-63; persisted override per vendor/stancl/tenancy/src/DatabaseConfig.php:66) — e.g. `php artisan tinker --execute='foreach (\\App\\Modules\\Tenant\\Domain\\Tenant::query()->orderBy("id")->get() as $t) { fwrite(STDOUT, $t->getTenantKey()."\t".$t->getDatabaseName()."\n"); }'` (verify the exact class/key accessor names at the checkout and cite them) — persist it per phase as `/root/wlota1a/manifests/<PHASE>/tenant-databases.tsv` via `docker cp`, validate it one-to-one against the UUID manifest (same count, same UUID set, no empty database column) with an explicit fail_gate, and make EVERY `psql`/`pg_dump` loop (capture_permission_snapshot, backup-phase.sh, rollback, registration-pause verification) read the database name from that mapping.
- B-B3a: in run_delta()/run_reseed() (and any other per-tenant loop), capture each command's status and full output to per-tenant files WITHOUT exiting (no reject_bad_output inside the loop; temporarily disable errexit for the command or use `|| status=$?`), aggregate every raw prefixed marker across all tenant outputs into the aggregate file, copy the raw aggregate and per-tenant outputs to the host, and ONLY THEN run status validation, bad-output rejection and the committed-template `cmp`. Add literal expected records + `cmp -s … || fail_gate '<name>'` to every remaining test-only §11 gate: timestamp capture, registration response/resume, maintenance-environment restoration, served asset/fingerprint evidence.
- M-E: keep the observed `Commit: <sha>` correlation but fix ordering: before the push, record the baseline set of existing web deploymentIds to a host file; after the push, select deployments whose description matches AND whose deploymentId is NOT in the baseline (use an inclusive whole-second lower bound only as a secondary filter, never strict `>` on truncated timestamps); pin that deploymentId, poll to `status == done`, compare the record with the committed template; apply the identical logic to the incident fallback.

Allowed subsections: §0 (insert "## 0. Round-7 change log", renumber earlier logs 0A–0F), §1 (planning SHA c76435df4a98193dfacb80ad9c25839169179509, commit subject), §11 (all), §14, §16, §17. Everything else byte-identical; never remove a test-table row; every `public readonly` stays readonly; no `mixed`; every new code claim cites an absolute path:line verified at the checkout.
