# Codex prompt — T-2 S1 receipt spine, fix round 2 (paste into the SAME `T-2 receipt spine` thread)

Fix round 2 on `lane/t2-receipt-spine` (worktree `.worktrees/t2-receipt-spine`, HEAD `d7123fa30`). Authority is now **plan rev 10**: `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-10.md` — read **§0AB** first (it rules every item below; §0AA still rules round 1), then the four gate-r2 registers in full: `docs/superpowers/reviews/2026-09-10-t2-s1-impl-gate-r2-tenancy.md` (T-5, T-9..T-15(r2)), `…-r2-stock-gl.md` (ACCEPT; G-9..G-12(r2); nest-guard note), `…-r2-inventory.md` (I-11..I-14(r2)), `…-r2-frontend.md` (F-1..F-12; "Exact T-9 revert (diff-level) and badge decision"; convention-11 census). Every round-1 item is verified closed — do not touch them again except where an r2 item names the same line. One commit per blocker/major group, one for minors, one for docs; cite `path:line` at current HEAD in a new "## Fix round 2" handback table. **Do NOT edit any file under `docs/superpowers/plans/` or `docs/superpowers/reviews/`** — plan-level decisions go in the handback; the orchestrator transcribes. PG: `DB_PORT=5432`, both DB env vars, one file at a time; the server is PostgreSQL **15.15** (fix the handback's "16").

**BLOCKER T-9(r2) — i18n ratchet red.** Apply exactly the frontend register's "Exact T-9 revert": `git checkout 93b106461 -- apps/web/src/lib/i18n.ts` (the file needs NO other change — the namespace is already registered for en/fr and in `ns`), `git rm apps/web/src/locales/ar/stock-transfers.json`, delete the `ar` case (and its `act`/`i18n` imports) from `StockTransferStatusBadge.test.tsx`. Proof: `I18N_BASELINE_PROTECTED_BLOB=fd6dbe3952cc3daaec15dc432e6b99e007f50dd6 node tools/audit-i18n-completeness.mjs` → exit 0, "2816 known gap(s) held at the baseline". NEVER regenerate/re-pin the baseline (the auditor's "1 baseline entry now translated" line is a reclassification artefact, F-12 — ignore it). Ticket `docs/superpowers/tickets/2026-09-10-stock-transfers-arabic-namespace.md` for the 121-key Arabic coverage.

**MAJOR group A — frontend convention 11 (F-1, F-10) + badge (F-2 / T-14).** `apps/web/src/features/stock-transfers/types/index.ts`: replace the hand-rolled `StockTransfer`, `StockTransferLine`, `StockTransferLineBatchAllocation` with `export type StockTransfer = App.Modules.Inventory.Application.DTOs.StockTransferData` (and `…LineData`, `…LineBatchAllocationData`); keep the request/UI shapes (`Create…Input`, `StockTransferListFilters`, `StockTransferListResponse`); leave `StockTransferType` / `TransferCostDistribution` for S4. Patch the fixtures in `__tests__/StockTransferListPage.test.tsx` and `__tests__/StockTransferDetailPage.batchAllocations.test.tsx` (the census lists the missing fields), delete the dead `?? '—'` at `StockTransferDetailPage.tsx:248`, `transfer.lines ?? []` already handles null. `StockTransferStatusBadge.tsx`: revert `:2` and `:21` to `93b106461` (`Badge` atom + `BadgeVariant`), keep the map as `Record<StockTransferStatus, BadgeVariant>` with exactly `partially_received: 'warning'`, `closed_with_writeoff: 'danger'`, `closed_returned: 'default'` added. Pixel-identical; `pnpm audit:design-system` 0 new.

**MAJOR group B — partial-receipt Complete (inventory I-11(r2) with frontend F-3/F-4).** (a) `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` S1 disclosures: "until S4, a short shipment is closed via `POST /close` (`disposition=write_off|return_to_source`); the web Complete on a `partially_received` transfer books the entire outstanding remainder as received-good" + the R5 masking obligation for `StockTransferDetailPage` (S4 masks sent/remaining behind `canSeeExpected`/`blind` on this page; `StockTransferData` needs a receiver-shape discriminant in S2). (b) `detail.confirmComplete.description` becomes LINE-COUNT bearing, rendered only when `status === 'partially_received'`: en `"{{shortLines}} of {{totalLines}} lines have not fully arrived. Confirming books the entire outstanding quantity as received at the destination; use Close to write off or return a short shipment."`, fr `"{{shortLines}} lignes sur {{totalLines}} ne sont pas entièrement arrivées. Confirmer comptabilise la totalité du reliquat comme reçu à destination ; utilisez Clôturer pour perte ou retour."`; `shortLines` = lines whose `quantity_remaining !== '0.0000'` (string compare — NO `parseFloat`/`Number()`); the `in_transit` copy is unchanged. NO remaining column, NO remaining total, NO receive/close UI. (c) backend test in `StockTransferCloseTest` (or `InventoryTransferServiceTest`): receive 5 of 12 → `POST /complete` → destination `12.0000`, status `completed`, `freight_uncapitalized === '0.0000'`, `Σ allocated + freight === transfer_cost`.

**MAJOR group C — tenancy minors that touch code (T-5, T-12).** T-5: `StockTransferService.php:312,328,397` predicate `tenant_id` AND `company_id` on the identity lookup (or accept the controller's already-scoped `StockTransfer`); one test. T-12: keep the nest guard EXACTLY as written (`StockTransferReceiptService.php:97-99`); in `StockTransferEdgeCasesTest` and `InventoryTransferServiceTest` replace the per-test `RefreshDatabaseState::$migrated = false` with once-per-class migration (or truncation-based isolation) so 27 × migrate becomes 1 × migrate; no assertion change; add "never `--parallel` this step" to the `ci.yml` comment block above the PG command; disclose the mechanism + measured runtime in the handback.

**Minors.** Inventory I-12 (drop `total_sent…total_remaining` from `TransferReconciliationSummaryData`, keep `lines`, `lines_with_discrepancy`, `freight_uncapitalized`; `php artisan typescript:transform`; handback note for §7.12), I-13 (`StockTransferLineData.php:67` → `QuantityScale::decimalPlacesForUnit(...)`), I-14 (ratchet message names the expected count). Stock-gl G-9 (close-form `GL_ACCOUNTS_UNMAPPED` case: unmap the shrinkage purpose, `POST /close disposition=write_off` → 422 + seven-table snapshot), G-10 (`self::assertSame('3.0000', $this->destinationQuantity())` beside the batch assertion in `StockTransferReceiveDamageTest`), G-11 (comment at `StockTransferService.php:365` naming `canBeCancelled()` as the precondition + a test that `partially_received` is refused on cancel), G-12 optional. Tenancy T-13 (handback: PostgreSQL 15.15), T-15 (T-6 line in the checklist's S1 disclosures). Frontend F-5 (`STATUS_FILTERS … as const satisfies Record<StockTransferStatus, true>`), F-6 (enum order: draft, in_transit, partially_received, completed, closed_with_writeoff, closed_returned, cancelled), F-7 (move the badge test to `__tests__/`), F-8 (typed per-test fixture copy, no shared mutation), F-11 (checklist line: "no `hasModule('Inventory')` FE counterpart to the backend `module:Inventory` group gate"), F-12 (pin untouched; optional one-line auditor comment).

**Manifest:** no new test CLASS is expected; if you add one, raise the manifest with the four-statement note and add it to the `ci.yml` alternation. `php tools/feature-lane-manifest-check.php` exit 0.

Verification (by file only, never the full suite; PG one file at a time on `autoerp_test_u` @ 5432 with BOTH `DB_DATABASE` and `DB_CENTRAL_DATABASE`): every touched PHP test class on PG incl. `StockTransferCloseTest`, `StockTransferReceiveDamageTest`, `TransferReceiptAuthorityGateTest`, `StockTransferEdgeCasesTest` (record the new runtime), `InventoryTransferServiceTest`, `StockTransferCompleteConcurrencyPostgresTest` (exclusive); SQLite `DocumentPerActionBaselineRatchetTest` (blob `1381983d…`) + `TransferInTransitReadersUseRemainderTest`; `pint --test` + scoped PHPStan; `php artisan typescript:transform` byte-identical after commit; web (Node 20.19.4): `pnpm typecheck`, `pnpm audit:keys`, `pnpm audit:design-system` (0 new), `pnpm audit:quantity`, the i18n auditor exit 0, scoped ESLint 0 errors, `pnpm lint:ratchet` from the repo root (warning count may not grow), Vitest one file at a time (`StockTransferStatusBadge.test.tsx`, `__tests__/StockTransferDetailPage.test.tsx`, `__tests__/StockTransferDetailPage.batchAllocations.test.tsx`, `__tests__/StockTransferListPage.test.tsx`), then `./scripts/preflight.sh` to exit 0 (T-10) — if the laptop cannot afford the full preflight, run every family it contains individually and say so. Update the handback ("## Fix round 2" table; refreshed verification; PG version; ledger). Stop with `status: review`. Do not merge, do not push.
