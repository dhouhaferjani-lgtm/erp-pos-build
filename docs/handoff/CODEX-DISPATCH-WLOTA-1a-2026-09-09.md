# Codex Desktop dispatch — slice W-LOT-A-1a (paste as a NEW thread named `W-LOT-A-1a permissions/roles/web gating`)

Repo: `/Users/houssamr/Projects/syneriva/apps/erp`. Create your worktree first: `git worktree add .worktrees/w-lot-a-1a -b lane/w-lot-a-1a dev` (base = local `dev`, currently `c5dddaddb` or later). Work ONLY inside that worktree. PostgreSQL test DB for this lane: `autoerp_test_w` (set `DB_DATABASE` and `DB_CENTRAL_DATABASE` to it for every PG leg; port 5433 container; see `scripts/run-feature-lane-local.sh --dry-run` for the env the CI lane builds). Never run the full PHPUnit or Vitest suites — by file only.

Plan (authority, gate r8 = DISPATCH-READY with zero BLOCKER/MAJOR/MINOR): `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-8.md`. Gate: `docs/superpowers/reviews/2026-09-06-w-lot-a-1a-slice-codex-gate-r8.md`. Read in this order: §1 (run the `DISPATCH_SHA` block and the repin procedure on drift — documentation-only ancestry does not block), §2 (outcome + hard scope; A-1b/A-1c deferrals are NOT yours), §3 (owner rulings), §6 (shared contracts + named-symbol census — re-run it), §7 Task 1, §8 Task 2, §9 Task 6, §10 (source-push ledger — each file appears exactly once), §13 (verification commands), §15 (boundaries).

YOUR SCOPE = the code of Tasks 1, 2 and 6 only (§16 steps 1–2, 10–19, 32–35). §11 (deployment variables and fail-closed fleet protocol), §12, §14's reviewer dispatch and every Push 1–5 promotion step in §16 (3–9, 20–31, 36–54) are ORCHESTRATOR-OWNED staging operations: do not execute them, do not touch Dokploy, do not push to origin. The feature flag `LOT_ACTION_PERMISSIONS_ENFORCE` stays `false` everywhere you run.

Rules: `CLAUDE.md` rules 2 (TDD — every test red first, capture the first failing assertion in the handback), 3, 4 (no scope creep), 6 (BatchExpiry imports no Document/POS persistence models for trace queries), 7 (`php artisan typescript:transform`; never hand-edit `packages/shared/types/generated.d.ts`), 9, 12, 13, 17, 18, 19 (batch totals are four-decimal strings from the scoped eager-loaded relation; no floats); `docs/conventions/09-SECOND-OF-EVERYTHING.md` (second-company, second-location, re-run tests as the plan names them); `docs/conventions/11-ONE-SURFACE-PER-CONCEPT.md` (glossary rows in `docs/glossary.md` per §8).

Deliverables, one commit group per push so the orchestrator can promote them in the plan's five-push order — keep the ledger boundaries of §10 exactly:
1. **Push-2 group** — the additive self-guarding migration `apps/api/database/migrations/tenant/2026_09_06_205000_add_provisioning_source_to_roles.php` + `tests/Feature/Migrations/RoleProvisioningSourceSchemaTest.php` (PG and SQLite green).
2. **Push-3 group, Task 1** — API action permissions and location-safe batch reads (§7): middleware/permissions on the uncovered BatchExpiry read/trace/deactivate/recall actions, `null` scope unrestricted / `[]` fail-closed, trace DTOs + owning-module adapters, provider wiring; tests per §7 and §13 (`BatchActionPermissionsTest`, `BatchReadLocationScopeTest`, `BatchExpiringLocationScopeTest`, `BatchTraceReaderContractTest`, `BatchTraceabilityModuleBoundaryTest`). Prove create/update/transfer/write-off authorization is unchanged.
3. **Push-3 group, Task 2** — role delta and guarded `general_manager` assignment (§8): enums, `LotActionPermissionDelta`, `GeneralManagerAssignmentGuard`, `permissions:apply-lot-action-delta {--apply} {--verify}` under `tenants:run` with marker `WLOTA1A-PERMISSIONS`, `RolesAndPermissionsSeeder` fresh/unmarked legacy-off branch + `markedTenantCarriesWlota1aDelta()` preservation branch BEFORE the destructive `syncPermissions()` seam, snake-case readonly `RoleData` with marker-derived `is_provisioned_read_only`, Roles API protection of the marked role, `Log::channel('stderr')` registration marker; every §13 Task-2 test green on PG (the two marked-tenant preservation tests and the stderr-marker test are mandatory), then `php artisan typescript:transform`.
4. **Push-5 group, Task 6** — web route/action gating + deterministic permission map (§9): `RolesPage.tsx` consumes generated `is_provisioned_read_only` (marker-derived, never name-derived), protected edit/delete affordances absent + fail-closed modal backstop, batch route/action gating with the exact API permission names, regenerated permission map and `generated.d.ts` byte-identical to the committed artifacts, split Playwright spec `e2e/batch-permissions.spec.ts` (only the `pre-activation web gating` project runs locally); §13 Task-6 Vitest files + `pnpm typecheck` + `pnpm typecheck:e2e` + `pnpm lint`.
5. Handback `docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md`: `DISPATCH_SHA`, per-task red-first evidence (test → first failing assertion → green command), the §10 ledger with the commit SHA that carries each file, anything owed, resume recipe. Do NOT merge into `dev`, do NOT push to origin. Stop with `status: review` — the orchestrator session gates with `tenancy-authz-reviewer` + `inventory-costing-reviewer` after Tasks 1/2, adds `frontend-conventions-reviewer` after Task 6, then runs all three on the combined diff (§14), merges, and runs the §11 staging protocol itself.
