# Codex prompt — W-LOT-A-1a fix round 3 (Codex CLI, read-only, gpt-5.6-sol high; orchestrator saves the reply verbatim as rev 4)

---

DESIGN PRE-APPROVED — non-interactive run: do NOT stop to ask for approval or confirmation; produce the full deliverable now.

Fix round 3 for docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-3.md (rev 3). Read docs/superpowers/reviews/2026-09-06-w-lot-a-1a-slice-codex-gate-r3.md in full. Produce rev 4 of the slice plan. Your sandbox is READ-ONLY: do not attempt to write files, run tests, or run git commands; do not ask questions. Your FINAL MESSAGE must be the complete rev 4 document and nothing else (no preamble, no summary, no code fences around the whole document) — the orchestrator saves it verbatim to docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-4.md.

Rev 3 is the verbatim base. Copy every section that the gate does not fault unchanged. Do NOT touch design sections (§2 scope, §3 rulings, §4 baseline, §6 contracts, §7.2–§7.5, §8.2–§8.8 design content) except where a gate item names a line inside them. Do not add tasks (Tasks 1/2/6 only). Every code claim cites an absolute path:line at current HEAD; re-verify every citation you keep and every citation you add.

Close the 3 BLOCKERS with the gate's minimum corrections verbatim:
- B-B1 migration regexes (rev 3:2297, 2300): replace the doubled-backslash ERE with fixed-string exact-line matching — `grep -Fxc 'Rolling tenant migrations across 1 tenant(s).'` and `grep -Fxc 'Done. 1 tenant(s) migrated, 0 failed.'` (source: RollingTenantMigrationCommand.php:73 and :163) — and add a literal-output shell proof (a committed heredoc of the expected stdout compared with `cmp`, or an equivalent executable gate test) so the pass can never reject valid output again. Audit EVERY other regex in §11 for the same defect and fix each the same way.
- B-B2 topology branch (rev 3:1995 vs manifest §0 and manifest:190): add an executable U-1 preflight that runs BEFORE source-push dispatch and resolves the live topology (separate Dokploy applications vs the checked-in compose stack), with the exact commands and exit rule. Branch explicitly on both outcomes: (a) separate applications → update each applicable Dokploy application environment (worker, API, scheduler) with readback proof; (b) compose stack → enumerate `docker-compose.staging.yml` in a sixth source push that adds `LOT_ACTION_PERMISSIONS_ENFORCE` to `x-api-env` (docker-compose.staging.yml:17-57) and verify all required service readbacks. Update the push inventory (§10), SHA gates, rollback, and the per-slice variable row (§11) for BOTH outcomes. Remove the sentence that forbids editing the compose file unconditionally.
- B-B3 prose/cross-shell checks (rev 3:2146/2190, 2355, 2580, 2850): every fail-closed check must be executable in the shell where it runs. (1) Define or source `assert_exact_marker_set` in the HOST shell (full function body in the plan) and show its exact invocation in the host backup gate. (2) Flag-off and flag-on evidence: capture the OFF and ON records into aggregate files and compare them byte-for-byte with committed heredoc expectations using `cmp`; cardinality, order, and unexpected-line checks are executed, not stated. (3) Push-4 day-one execution: supply the complete command sequence, or invoke a named, versioned helper defined earlier in the SAME shell — never "rename the earlier loop". (4) Rollout-window recensus: same — complete commands, no manual variable substitution by the operator. Every §11 gate must exit non-zero on failure and the plan must show the exit rule.

Close the MAJORS:
- M-C sole writer / read-only Roles page (rev 3:244, 1020, 1485, 1650): the task contract must implement what §5/N-A claims. Backend (Task 2): `RoleController::update()` (RoleController.php:223, :245) and destroy reject name, permission, and delete mutations for the marker-derived protected role (`provisioning_source` non-null) with a stable error code/status; prove grants remain unchanged after the rejected call. Web (Task 6): add `apps/web/src/features/settings/RolesPage.tsx` and a colocated test to the file inventory; consume a marker-derived protected/read-only field from the roles API response (name it, add it to the DTO and generated type), suppress edit/delete affordances for that role (RolesPage.tsx:35, :178, :253), and prevent modal submission. Add exact red-first entries incl. `GeneralManagerRoleProtectionTest::test_marked_general_manager_permissions_cannot_be_synchronized_through_role_update` (and the name/delete siblings) and a Roles-page read-only Vitest, each with first failing assertion, exact command, and lane (PG for the backend). Update file inventories, source-push ledger, reviewer checklist, and rollback.
- M-D `tenants:run` VALUE_NONE normalisation (rev 3:1366 vs 2418; Run.php:40/:50; DayOneCensusCommand.php:83): the delta command must normalise `--apply`/`--verify` as truthy (string '1'/'true'/true) for both options; add a PostgreSQL red-first integration test through `tenants:run`, `ApplyLotActionPermissionDeltaCommandTest::test_tenants_run_apply_string_one_selects_apply_mode`, asserting the exact `mode=APPLY` marker, plus the verify-mode sibling, each with its exact PHPUnit command.

Close the MINOR:
- N-B: one identical A-1b ownership list in BOTH locations (rev 3:113 and 3036): POS sale, direct batch transfer, and StockTransfer allocation. Delivery/write-off/count/return eligibility is deferred to named later lanes.

Also: refresh the planning-baseline SHA to current HEAD and re-verify the named-symbol census (§6.5) and every `path:line` against it.

Include a "## 0. Round-3 change log" (renumber the earlier logs to 0A/0B) with every gate item CLOSED (plan line/anchor) or REJECTED (citation), keep all prior closures with no regression, and keep the gate's Preserve list intact.
