# LEDGER — Outstanding Debts (single source of truth)

> **This file is the ONLY authoritative owes list.** Sessions must update it IN PLACE when a debt is created, discharged, or re-scoped. Any owes/debt list in any other document (handoffs, specs, memory files, checklists) is a pointer at best — **where another document conflicts with this ledger, the other document is stale.** Do not fork per-project owes lists; link here instead.
>
> Seeded 2026-08-12 from the memory-file mine; wording verified against cited repo docs where they exist. Items whose only source is a memory paraphrase are marked `src: memory`. Status vocabulary: **OPEN** / **BLOCKED-ON-x** / **CLOSED (date, evidence)** / **STALE-IN-MEMORY** (memory says open, cited doc says otherwise) / **UNVERIFIED** (could not confirm against any repo doc).

---

## 1. Owner owes (decisions / rulings / sign-offs)

| ID | Item | Source | Status | Action owner |
|---|---|---|---|---|
| O-1 | Push the mobile branch | `src: memory` (MEMORY.md active-work) | OPEN | Owner |
| O-2 | Pre-filing VAT skip runbook (execute/confirm before first VAT filing) | `src: memory` | OPEN | Owner |
| O-3 | Launch gate **E-10** — production environment decision | `docs/handoff/DECISION-HANDOVER-prod-env-choices-2026-08-06.md`; memory `project_first_tenant_launch_program` | OPEN | Owner |
| O-4 | Launch gate **E-1** — production secrets | memory `project_first_tenant_launch_program` | OPEN | Owner |
| O-5 | Launch gate **E-2** — device smoke test | memory `project_first_tenant_launch_program` | OPEN | Owner |
| O-6 | Launch gate **E-3** — migration rehearsal | memory `project_first_tenant_launch_program` | OPEN | Owner |
| O-7 | Launch gate **E-4** — TN legal (also blocks impersonation usage) | memory `project_first_tenant_launch_program`, `project_tenant_impersonation_support_access` | OPEN | Owner |
| O-8 | Launch gate **E-9** — staging-runbook execution | memory `project_first_tenant_launch_program` | OPEN | Owner |
| O-9 | Launch gate **E-7** — evidence, **NON-WAIVABLE**; interim policy = **NO REFUNDS** until satisfied | memory `project_first_tenant_launch_program` (wave 3 owed) | OPEN | Owner |
| O-10 | Order Hetzner **AX42-1** (critical path of prod-env plan) | memory `project_production_environment_and_cat_b_2026_08_05` | OPEN | Owner |
| O-11 | Confirm launch staffing = **TWO humans** | `src: memory` | OPEN | Owner |
| O-12 | UI open item **OI-8** (SO losing-path) | `docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:90` | **CLOSED (2026-08-12: REFUSE, batch-atomic)** — STALE-IN-MEMORY | — |
| O-13 | UI open item **OI-10** (two-lane interaction principle) | `docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:81,90` | **CLOSED (acknowledged 2026-08-12)** — STALE-IN-MEMORY | — |
| O-14 | UI open item **OI-14** | `src: memory` (not found in OWNER-DECISIONS-ui-audit-2026-08-10.md) | UNVERIFIED — locate ruling or re-ask | Owner |
| O-15 | **NEW-Q1** — `/finance` hub delete-vs-refresh (research ordered, decision pending report-back) | `docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:50` | OPEN (awaiting research 11) | Owner |
| O-16 | **NEW-Q2** — receipts web view | `docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:51` | **CLOSED (RULED: BUILD)** — STALE-IN-MEMORY | — |
| O-17 | **NEW-Q3** — DN-consolidation billing UX (research+advise; owner lean: partner-page un-invoiced DN filter) | `docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:52` | OPEN (awaiting research 13) | Owner |
| O-18 | ES-remediation owner decision sheet **D-1…D-17** (17 items; memory's "10 open" count is superseded by the sheet) | `docs/handoff/OWNER-QUESTIONS-es-remediation-2026-08-11.md` | OPEN — answer in place on that sheet | Owner |
| O-19 | SV design items **SV-namespace E-2 / E-3 / E-4 / E-5 / E-8** + **SV-16 (= sheet D-17)** — float shape, DEPOSIT/PAYOUT GL typing, negative-till policy, two-stage deposit follow-on, TND thresholds, SAFE_DROP-vs-CASH_OUT. **Do NOT confuse with launch E-gates (O-3…O-9): same letters, different namespace.** | `docs/handoff/FINDINGS-shift-variance-gl-2026-08-11.md:43,326-330` | OPEN | Owner |
| O-20 | Owner-ack owed: **no in-product country/currency correction path** (settings-identity-guards close-out) | memory `project_document_per_action_remediation` | OPEN | Owner |
| O-21 | `fiscal:backfill` **register-or-delete** ruling | `src: memory` | OPEN / UNVERIFIED (no repo doc located) | Owner |
| O-22 | Device **v67-vs-v64 E-8 row** ruling (gates whether v67 ships — see D-1 in §3) | `src: memory` | OPEN | Owner |
| O-23 | **B5** flagged-for-review mobile state — Option A recommended, **not ruled** | `src: memory` | OPEN | Owner |
| O-24 | **Cash-rounding enable sign-off** — either the refund-rounding track lands (`docs/superpowers/specs/2026-07-27-refund-rounding-research.md`, approved NOT implemented) **or** owner signs the acceptance blank ("who accepted, when") | `docs/handoff/cash-rounding-phase2-deploy-checklist.md:224-229` (both boxes unticked as of 2026-08-12) | OPEN — blocks gate G-2 in §5 | Owner |
| O-25 | **Country-code mutable authz** ruling — who may mutate country code and under what authority | `docs/superpowers/tickets/2026-08-10-country-code-mutable-authz-authority.md` | OPEN | Owner |
| O-26 | **Lineless-document cancellation vs chained balance (P3-M1 R-4)** — `AccountingService::reverseDocumentGl` knowingly seals a ONE-LEGGED entry so a lineless document stays cancellable (prior L1 gate deferral, upheld for P3 by parent ruling 2026-08-21). Decide: keep cancellability (accept the recorded unbalanced-entry class in the chain) OR forbid lineless cancellation (close the last genuine class-(c) imbalance). Evidence: `docs/handoff/reviews/enforcement-p3/M1-census.md` R-4; code `AccountingService.php:900-950` | P3-M1 census + parent ruling | **RULED (2026-08-21, relayed via session de182ed3): PHASE OUT lineless-document posting** — block posting of zero-line documents UPSTREAM (validation refusal BEFORE any fiscal/GL write) so the `reverseDocumentGl` lineless carve-out becomes dead code; cancellability preserved because nothing lineless gets posted anymore; one-time correcting entries for existing broken entries = owner-side disposition per tenant. Implementation = gated lane (treasury + fiscal-pos lenses). **LANDED IN CODE (2026-08-22, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev** — zero-line Invoice/CreditNote posting REFUSED at the L1 preflight inside DocumentPostingService::post()'s transaction (nothing sealed, no number burned, doc stays Confirmed; empirically probed); the reverseDocumentGl lineless carve-out re-documented LEGACY-ONLY with a delete condition (pre-O-26 documents + the ArApOpening designed bypass are the whole residual population — cancellability preserved, pinned by a seeded-legacy test); TWO credit-note-producing authoring paths refused at CREATION (amount-0 CN; full-credit of a lineless invoice) closing the stranded-Confirmed-CN hole both gates chased; WO mappable-line guard; type-aware remedies; 41-method fixture census (no assertion weakened, 2 honest re-pins). SIX live zero-line authoring paths audited — the 2026-08-05 ticket's "unreachable via API" premise was FALSE; at-author hardening ticket: `2026-08-22-lineless-authoring-paths-hardening.md` incl. a P1: the auto-save route has NO validation and NO can: middleware and burns invoice numbers unauthenticated-by-permission. Dual gates: r1 CHANGES ×2 → 9-item round, parent-verified at merge. One-time correcting entries for EXISTING broken entries = still owner-side per tenant (see C-8/B-14(iii): r2f4 vehicle is prospective-only). **B-14(iii) RULED 2026-08-23: NO dedicated repair command — greenfield, nothing previous needs repair; disposition vehicle = the PLANNED per-tenant accountant role (manual entries); tenant #1 legacy population ~nil.** | Owner (accountant-side, per tenant) |
| O-27 | **Live-tenant chart validation is a 14-of-28 subset of manifest-REQUIRED (P3-M2 D-2, the F-4 surface)** — `requiredPurposes()` checks 14 of the 28 purposes `ProvisioningRequiredPurposesV1` classifies REQUIRED (incl. `inventory`); a brownfield tenant missing one reports valid:true then hard-fails at runtime. Exposure brownfield-only (all 3 seeded charts proven complete by the new CI gate). Any tightening = F-4: needs the backfill/seed story per newly-required purpose, routed through the country-defaults authority. Evidence: `docs/handoff/reviews/enforcement-p3/M2-reconciliation.md` D-2 | P3-M2 reconciliation | **RULED (2026-08-21, relayed via session de182ed3): GO on the F-4 backfill** — extend `BackfillChartPurposesCommand` to the 14 manifest-REQUIRED purposes `requiredPurposes()` doesn't check (list from M2-reconciliation D-2; authority = `ProvisioningRequiredPurposesV1`, NEVER edited), run the backfill, THEN widen `requiredPurposes()` to the full 28. Routed through the country-defaults authority; backfill must be self-guarding (origin/dev push auto-runs `tenants:migrate` on staging). **LANDED IN CODE (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev — gate ACCEPT (9 tuples spot-verified incl. TN class-5 root + the 7091 code-reuse trap; deptrac re-run PASS; zero branch-only regressions on the full PG Accounting suite) + 7-item fix round (fail-open magic-string closed via an ADDITIVE accessor on the authority — read-path only, data untouched, distinction documented at the import site; TN end-to-end repair case; dual-writer pins; skipped-status token). MIGRATION-BEARING: `2026_08_21_140000_backfill_chart_required_purposes_o27.php` rides the next promotion, self-guarding proven on PG (savepoint containment, idempotent, chartless-skip). Residual: FR-plan backfill maps 18/28 (the 10 unmapped = exactly the pre-lane-checked set, already failing loudly — no new unrepaired failure); extension = follow-on if wanted. Rule-6 exception recorded: first circular cross-module Domain import (Accounting↔CountryDefaults), owner-review item.** | — |
| O-28 | **Today's-Sales definition — RULED (2026-08-21, relayed via session de182ed3): the headline figure is NET, EXCLUDING REFUNDS, and must be LABELED accordingly** (e.g. "Net sales (excl. refunds)"). Every Today's-Sales surface (owner dashboard, POS today view, reports) must be audited for BOTH figure and label; prior art = the G-1 `netOfReturns`/`-ABS` consumer sweep (verified 2026-08-12). This recording satisfies the D-3 rider (b) precondition ("ruling must be RECORDED before SV-9 promotes" — ticket `2026-08-17-today-sales-blind-count-derivation.md`); the audit/fix lane is queued in session 0578e8d8. | Relay 2026-08-21; LEDGER row D-3 rider (b) | **LANDED IN CODE (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev + PLAYWRIGHT-VERIFIED LIVE** (owner dashboard renders "Net sales (excl. refunds)" headline, net-derived trend, Returns counter, and ALL gross drill-downs labeled "(gross)" en+fr+ar). Both genuinely-gross headlines fixed (POS Today panel: per-row -ABS era-safe netting + full pagination [was ≤20 receipts online] + training/void filters both paths; owner KPI tile: netSales bound + prior≤0 pct guard per the ruled dashboard precedent). 13-surface audit census committed in-tree (`2026-08-21-o28-todays-sales-audit.md`) with open questions O-28-a..f (breakdown scope, shift-vs-day naming, X/Z net-of-VAT label collision, POS Analytics inverted naming, dead delta fields). Gate: 2 rounds + final micro round, all closures re-verified. | — |
| O-29 | **F-2 laneless Feature tests — RULED (2026-08-21): line them up to EXECUTE.** The ~1,130 Feature classes in groups no CI lane runs get REAL lanes (extend `apps/api/tests/feature-lane-manifest.json` + `.github/workflows/ci.yml`). Design consequence (owner constraint: monthly GitHub Actions quota EXHAUSTED): (a) design + wire the lanes now; (b) heavy Feature lanes target a SELF-HOSTED runner (Hetzner VPS / future AX42 — zero Actions minutes), light static gates stay on hosted runners; (c) until the runner/quota exists, execute the suites in the local Docker stack, containerized and resource-limited — never the bare full suite on the laptop. NOTE: ci.yml is a workflow-touching surface — the S-14 dispatch-verification leg applies at promotion, deferred while quota is out (see S-17). Design lane dispatched in session 0578e8d8. | Owner ruling 2026-08-21 (direct) | **LANDED IN CODE (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev** — 8 executable lanes over 70 groups/1,130 classes wired in ci.yml behind `vars.SELF_HOSTED_RUNNER_READY` (queue-vs-skip semantics verified; jobs no-op until the owner flips it); `all-checks-pass` REWRITTEN fail-closed (survived 14 hostile-context executions); manifest checker gains execution-gate honesty rules (whole-if equality + case-insensitive 12-context allowlist + absolute no-step-if rule — 4 gate rounds: Codex r1, Opus r2/r4, each round's executed bypasses pinned as liveness tests, 86/483 green); quarantine mechanism (shrink-only, reflection-validated); local lane runner `scripts/run-feature-lane-local.sh` (destructive-run guards probe-verified incl. DB_CENTRAL_* family, --dry-run harness test in CI). First real lane run surfaced 3 pre-existing PG defects (design doc §8) — burn-down begins at first execution. Owner ops = sheet B-11 (VPS, 4 ephemeral runners, variable flip, S-14 dispatch leg at promotion). Tickets T-1 (needs/EXPECTED_JOBS two-list edit for non-lane jobs) + T-2 (allowlist boundary) in the design doc §8. | Owner (runner provisioning) |
| O-30 | **Q7-OWES-1 — forced terminal release orphans an OPEN shift; no surface can close it (non-waivable before the first forced release in production)**: `POST /pos/terminals/{id}/release` with `force=true` deliberately orphans the OPEN `pos_shifts` row; every `ShiftStatus::Closed` writer is the device projection (`ZSessionLifecycleProjection.php:251`) or `ShiftManagementService.php:163,178` whose callers hard-409 `SHIFT_DEVICE_AUTHORITY_REQUIRED` for every v3 terminal; nothing in `app/Console`. Until resolved the REPLACEMENT device's SESSION_OPEN is silently dropped (`:147-153`), SESSION_CLOSE retries to exhaustion (`:203-217`), Z report cannot land (`pos_z_reports.shift_id` FK RESTRICT). Fiscal chain intact; projections are not. OWED: (a) runbook step — find the orphan via `SELECT payload->>'open_shift_id', payload->>'reason', occurred_at, user_id FROM audit_events WHERE event_type = 'terminal.released' AND (payload->>'forced')::boolean IS TRUE ORDER BY occurred_at DESC;` and close that `pos_shifts` row by hand (status, closed_at, closed_by, variance/expected-cash pair satisfying `pos_shifts_variance_calc`), named accountable role; (b) RULING whether a first-class orphan-resolution surface (`device_loss_incidents` — schema-only, no writer/endpoint) is required before tenants may use release unattended; (c) RULING whether an NF525 device re-binding (`terminal.claimed`/`terminal.released`) is a reportable JET terminal event (`Nf525DataProvider.php:292-297` whitelists 3 types). | Q-7 fiscal r2 (`docs/superpowers/reviews/2026-08-24-sb-q7-terminal-gate-r2-fiscal.md` §Q7-OWES-1) | **(a) runbook + command DELIVERED `docs/handoff/RUNBOOK-orphaned-shift.md` / `pos:shift:close-orphaned` (merge `8c8316ab7`); (b) and (c) STILL OWED (owner)** | Owner |
| O-31 | **Arm the owner-pinned protected blob on the enum↔CHECK parity baseline BEFORE the first Slice D CHECK batch** (D-1 tenancy gate ruling (4)): without it, matched growth — a new uncovered enum-backed column plus a hand-added baseline entry in the same diff — passes the ratchet and is caught only by review. Model: `DocumentPerActionBaselineRatchetTest.php:114-149` (fail-closed, repository variable + pin tag); seed = the merged `apps/api/tests/Architecture/baselines/{enum-check-parity-baseline.json (189 keys), <central baseline> (11), enum-check-parity-acknowledgements.json}` — the pin MUST cover all three (r2 R2-2: an acknowledgement can otherwise be deleted for free). Also needs the S-14 workflow leg to wire `EnumCheckParityTest` into `treasury-spine-pgsql` (PG) + `backend-architecture` — today NO job runs it. **READY FOR THE OWNER (Session B2, 2026-08-25):** reader side merged `19f9e61bb`; steps + verified seed `da5ae1379` (blobs pinned) in `docs/handoff/OWNER-O31-enum-check-parity-pin-2026-08-25.md` — set variable `ENUM_CHECK_PARITY_PROTECTED_SEED` + push tag `ci-pin/enum-check-parity-r1`. ARMING ORDER (fiscal gate cond. 2): promote → owner sets variable AND pushes tag → then wire ci.yml (the seed is not an ancestor of `origin/dev` today). | D-1 tenancy gate r1 | OPEN — owner action, steps handed over | Owner |
| O-32 | **RULING (not just ack) on the JE / income numbering contract**: since C-27 (`0613e2ce8`) `JE-YYYY-NNNNNN` and `INC-YYYY-NNNNNN` are ONE sequence per TENANT-year — the companies of a tenant interleave (A `…0001`, B `…0002`, A `…0003`) and each company's own register shows gaps (expenses already do this since Q-11). This is the only allocation consistent with the shipped unique index `(tenant_id, entry_number)`; FEC is unaffected (`FecExporter.php:62-90` mints its own `EcritureNum`) and nothing asserts per-company contiguity. Alternative = widen the unique to `(tenant_id, company_id, entry_number)` — migration on the hottest fiscal table, `entry_number` is an externally-quoted identifier; reversible later if ruled. Please rule: (a) accept interleaving (default, shipped) or (b) schedule the per-company unique. | C-27 treasury gate r1 cond. 3 | OPEN | Owner |
| O-33 | **E-1 rotation: the `sk_live` production platform key (`SYNERIVA_PLATFORM_API_KEY`) sits in ERP STAGING env (Dokploy api `x5wfthp8-7cVbiUfI6Hq7` + worker `KKYDsAvk4UpYfJXVmsDj2`).** Since 2026-08-29 the push gate (`SYNERIVA_PLATFORM_PUSH_ENABLED=false`) stops staging writing to production, but the key is still a production credential on a non-production box: rotate it on the platform side and give staging a staging-scoped key (or none — reads only). Platform census 2026-08-29: the key is `sk_live_HEXK` = api key "Testkey2" on partner **Izipos** (`019ee1a6…`); plan + key hygiene steps in root `docs/superpowers/plans/2026-08-29-platform-purge-erp-test-submissions.md`. | Session J push-gate lane `1326a96c1`; root `docs/03-ERP-INTEGRATION/REALIGNMENT-LOG.md` 2026-08-29 entry | OPEN | Owner (platform side) |
| O-34 | **Ask the platform team to purge the 1 enrichment submission that leaked from ERP staging before the gate landed** — tenant `019ee4d7…`, 1 product. Platform census 2026-08-29: 11 `barcode_submissions` total on prod (6 Izipos-partner/staging key, 5 older "Otospex ERP" partner), ALL test data, NONE produced a platform product; submissions carry no tenant id (attribution gap). Owner decision (A) purge all 11 / (B) only the 6 — brief: root `docs/superpowers/plans/2026-08-29-platform-purge-erp-test-submissions.md`. | Session J handover (erp-4f); REALIGNMENT-LOG 2026-08-29 entry | OPEN | Owner (platform side) |
| O-35 | **Arm the onboarding campaign on push: set GitHub repo variable `ONBOARDING_CAMPAIGN_ON_PUSH=true`** — lane I-1 ships `.github/workflows/onboarding-campaign.yml` whose push→dev trigger is INERT until the variable exists (workflow_dispatch works immediately, needs no secrets, registers its own tenant). Rule-22 promotion gate = a green campaign run; until armed, the gate is satisfied by the local run recorded in the I-1 lane report. | Session I lane I-1 (`008c2a199`), 2026-08-29 | OPEN | Owner |

## 2. Staging owes (deploy / ops steps)

| ID | Item | Source | Status | Action owner |
|---|---|---|---|---|
| S-1 | Run `channels:reconcile` | `src: memory` | OPEN | Staging operator |
| S-2 | Explicit `permission:cache-reset` | `src: memory` | OPEN | Staging operator |
| S-3 | Negative-repo + negative-line detection SQL, per tenant | `src: memory` | OPEN | Staging operator |
| S-4 | Custom-grant drift check | `src: memory` | OPEN | Staging operator |
| S-5 | Per-tenant migrate log check (country-defaults promotion `7d85232cc`) | memory `project_country_defaults_super_admin` | OPEN | Staging operator |
| S-6 | Impersonation seeder + role reseed + permission cache-reset | memory `project_country_defaults_super_admin`, `project_tenant_impersonation_support_access` | OPEN | Staging operator |
| S-7 | Country-defaults **§F.3** checks | memory `project_country_defaults_super_admin` (spec §F.3) | OPEN | Staging operator |
| S-8 | `DemoPharmacySeeder` rerun (UoM display-precision deploy debt) | memory `project_uom_display_precision` | OPEN | Staging operator |
| S-9 | Replenishment staging seeder | memory `project_replenishment_requests` | OPEN | Staging operator |
| S-10 | Treasury phases **②③④⑤** stacked deploy checklists (launch Lane D2 is consolidating them — consolidation does not discharge the debt) | memory: treasury phase project files; `docs/handoff/CODEX-treasury-phase*` handbacks | OPEN | Staging operator |
| S-11 | **9 deploy checklists 0-ticked** in the production-env file | memory `project_production_environment_and_cat_b_2026_08_05` | OPEN | Staging operator |
| S-12 | `failed_jobs` purge follow-up | memory `project_staging_2026_08_04_upload_500_and_cross_tenant_jobs` | OPEN | Staging operator |
| S-13 | Live tenant: **126 cash repositories share one gl_account with `location_id` NULL** — tenant #1's 4 branch registers need `location_id` set before branch journal legs mean anything | `src: memory`; code-side fix landed via N-12 (Session D, merged `4807f0045`) — migration `2026_08_26_100000_backfill_payment_repository_location_n12` | OPEN — migration provisions branch drawers on deploy; staging must then run the operator `RepositoryTransfer` per branch named by the migration's `drawer-provisioned-transfer-owed` census line (see D-N12-1) | Staging operator |
| S-14 | **Enforcement-guards remote-CI verification (PRE-promotion gate — gate-r2 R2-C-2/R2-H-2 rewrite)** — the enforcement-guards executor never pushes (gate-r1 H-7 ruling), and the CI workflow never triggers on a `dev` push (`.github/workflows/ci.yml:3-8`, `:740-747`), so a post-promotion dev push would produce NO run to observe. The executor's local evidence (actionlint or a generic YAML parse) is explicitly INSUFFICIENT for the Actions job/event graph — syntax only. The promotion sequence (gate-r5 R5-H-4: UNIVERSAL for every enforcement package; the `workflow_dispatch` leg fires only when the accepted branch touches `.github/workflows/**`) is — with NOTHING committed to the package between final-gate ACCEPT and merge (gate-r3 R3-C-2) and **the parent performing NO other local-dev merge of ANY lane during steps 0–5 (single-writer serialization, gate-r5 R5-C-3)**: (0) freshness assert `git merge-base --is-ancestor <current-dev-tip> A` — fast-forward only; (1) the PARENT ITSELF runs the final gate via `scripts/adversarial-review-final.sh` against a SEALED detached worktree at the handed-over tip (gate-r6 R6-C-1, gate-r7 R7-C-1, gate-r8 R8-C-1 — one-dirty-entry handover: status shows exactly the untracked handback, which the bridge copies+hashes itself (gate-r9 R9-H-2); the bridge is RECEIPT-DRIVEN — inputs from the parent dispatch receipt at ~/.claude/projects/-Users-houssamr-Projects-syneriva-apps-erp/memory/dispatch-receipts/enforcement-<pkg>.receipt.yaml, created at dispatch and named here per package (gate-r10 R10-C-1); PyYAML required on the parent host (gate-r10 R10-H-1); verdict must be the final non-empty register line (gate-r10 R10-H-3); control-file preflight incl. the manifest and both bridges; every control input sha256-verified against `docs/handoff/enforcement-control-manifest.yaml` and lenses/max_fix_rounds sourced from it, never candidate YAML — gate-r8 R8-H-1; the bridge computes the handback digest itself, digest never in the prompt, content-derived census evidence required, pre-tag re-hash equality — gate-r8 R8-H-2; substantive CHANGES-REQUIRED = executor fix round, tool_error = parent-owned same-A retry bounded at 3 — gate-r8 R8-H-3) and, on ACCEPT, immediately digests the register from ITS OWN invocation (gate-r5 R5-C-1); (2) *(ratchet packages)* preflight the pre-allocated tag name is free, THEN set the repository variable(s) to the gate-reviewed seed blob hash and VERIFY the read-back value (gate-r5 R5-H-2); for P2, SEND the final merge announcement; (3) the owner pushes the package's PRE-ALLOCATED **annotated** closing pin tag at exactly A — name reviewed in A's mirror block for ALL packages (P3: p3_closing_pin_tag — gate-r6 R6-H-4), annotation binding `{A, manifest_sha256, sha256(register(s)), sha256(handback), control_sha256 lines}` (gate-r7 R7-C-1, gate-r10 R10-H-2), tag verified to resolve to A, **tags NEVER deleted or reused** (gate-r5 R5-C-1/R5-C-2) — and, for workflow-touching packages, pushes A to a throwaway ref + runs `workflow_dispatch`, verifying head == A, GREEN, new job(s)/step(s) executed; (4) **step 4a: repeat the freshness assert AND re-verify tag target/annotation/variable AFTER any dispatch run, and reject candidates whose workflow diff grants contents-write (gate-r5 R5-C-3, gate-r6 R6-H-5)**, then fast-forward EXACTLY A — promotion SHA == accepted SHA == dispatched head; (5) ONE closing admin commit C — THE first-parent child of A, C^ == A (gate-r6 R6-H-2) — lands ALL closing evidence (digested register bytes, handback, final YAML status incl. the structured closing_receipt — which contains ONLY pre-commit-knowable data, never C's own SHA or post-commit output (gate-r6 R6-H-4, gate-r7 R7-C-2), receipts, this LEDGER row), verified by the **named closing check** (preflight before commit; amend authorized only while C is the unpushed dev tip — gate-r6 R6-H-3): `git rev-list --count <A>..C` == 1 (C = the first-parent child of A, `C^ == A`) AND every `git diff --name-only <A>..C` path inside the CURRENT package's closing set only (its own progress YAML · its own `docs/handoff/reviews/enforcement-<pkg>/**` · its own `HANDBACK-enforcement-<pkg>-*.md` · this LEDGER) — another package's evidence or any production/workflow path = closing FAIL (gate-r5 R5-H-3), output recorded via the annotated CLOSE TAG ci-close/enforcement-<pkg>-r<n> pushed AT C after the check passes ({closing_commit: C, closing_check: pass, accepted_sha: A} — gate-r7 R7-C-2, attestation form gate-r8 R8-M-1; every consumer verifies the tag resolves to C, and the parent read-back after EVERY close-tag push verifies target + all three annotation fields incl. closing_check == pass (gate-r10 R10-M-2); never deleted/reused). **Re-gate protocol (only route back; triggers broadened gate-r5 R5-H-2): step-0/4a failure, red dispatch, or ANY side-effect failure (tag collision/push failure/wrong target, variable read-back mismatch, ref/dispatch API failure, announcement failure) → STOP, restore last known-good variable, record partial state here, then retry the SAME A idempotently (an existing tag exactly matching {A, digests} IS step 3 done — gate-r6 R6-H-3) or return to an in-progress final milestone (rebase → rerun invalidated evidence → hand over for the PARENT-invoked whole-package bridge → new A′ → re-send announcement → restart from 0). For workflow-touching packages, no green run on exactly the promoted SHA = PROMOTION BLOCKED.** A post-promotion-discovered workflow defect blocks the NEXT promotion until a green dispatch run on the fixed revision exists. | `docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md` §5, §6 F-7/F-8 (gate-r2 R2-C-2/R2-H-2; gate-r3 R3-C-1/R3-C-2; gate-r4 R4-C-1/R4-C-2/R4-H-1/R4-H-2; gate-r5 R5-C-1..3/R5-H-1..4; gate-r6 R6-C-1/R6-H-1..5; gate-r7 R7-C-1..2/R7-H-1..2; gate-r8 R8-C-1/R8-H-1..3/R8-M-1; gate-r9 R9-C-1/R9-H-1..4/R9-M-1, gate-r10 R10-C-1/R10-H-1..4/R10-M-1..2 — bridge rewritten: parent-held base + expected-manifest-sha256, one-dirty-entry seal, neutral-cwd review with brief injected, exact verdict parse; consumers verify closing_check == pass + manifest identity) | OPEN (armed per package at acceptance; must be satisfied BEFORE each enforcement-guards promotion). **P1 SATISFIED 2026-08-21 with a recorded inherited-red waiver**: dispatch run 32407809263 at exactly A=e55a1c8ae — new dpa-guard job GREEN; 4 red jobs proven inherited-at-base by control dispatch 32427893316 at base 82a032e04 (PG-only 51≡51, Treasury Spine 78≡78, Vitest base⊂A+1 run-local flake; full-suite covered structurally — additive-only candidate diff; control job pending at close). Waiver recorded under the owner's 2026-08-20 blanket delegation + fix-as-we-go ruling; full record in `docs/handoff/progress/enforcement-p1.progress.yaml` `pre_promotion_ci_dispatch`. Row stays armed for P2/P3 — a green-or-waived run on exactly each accepted SHA remains mandatory per package. **P2 SATISFIED 2026-08-21 with the same waiver class**: dispatch 32465616707 at exactly A=d8a631133 — ALL new P2 surfaces GREEN (i18n gate + liveness + event ratchets + manifest checker; dpa-guard green on P2's tree); 4 red jobs inherited (PG-only 51≡51, Treasury 78≡78, Vitest = the 4 known, security-regression's 8 ≡ the identical base full-suite subset — pre-existing .env.testing kill-switch env defect, fix lane queued post-close; full-suite pending, structurally covered). Full record in `docs/handoff/progress/enforcement-p2.progress.yaml` `pre_promotion_ci_dispatch`. Armed now for P3 only. **P3 SATISFIED 2026-08-21 via the R5-H-4 NO-WORKFLOW path**: zero .github/** paths in base..A (verified), so no dispatch leg; parent bridge round-2 ACCEPT at A=8c8c37358; pin tag ci-pin/enforcement-p3-r1 (r12 schema). ALL THREE ENFORCEMENT PACKAGES LANDED — this row's per-package obligation is DISCHARGED; the S-14 promotion sequence itself remains the standing protocol for any future enforcement-class package. | Owner |
| S-16 | **3C inventory-GL cutover: per-tenant duplicate `(source_type, source_id)` count MUST run before the promotion that carries `2026_08_11_000100_unique_journal_entries_source_inventory_movement.php`** — the migration fails closed on any tenant with duplicate inventory rows, and origin/dev pushes run `tenants:migrate` unattended. The wave's local R-11 probe was VACUOUS (zero denominator); the ticket's query is the gate. HARD pre-promotion step for the batch containing merge `1e8c0fa03`. | `docs/superpowers/tickets/2026-08-18-inventory-gl-cutover-duplicate-precondition.md` (severity HIGH); treasury merge-review F-6 (2026-08-19) | CLOSED (2026-08-21: all 8 staging tenant DBs queried live over the Dokploy-exposed port — 0 duplicate (source_type,source_id) rows, denominator 0 on every tenant for all five InventoryGlSourceTypes; this IS the deploy-target set, so the unique index builds over an empty population; port verified re-closed after; tenant-by-tenant record in the parent session ledger, executed under the owner's 2026-08-20 delegation) | Owner/staging operator + promotion gatekeeper |
| S-17 | **CI-BLIND WINDOW (from 2026-08-21): GitHub Actions monthly quota exhausted — NO CI runs on pushes/PRs; the enforcement gates are temporarily blind.** Interim discipline (owner-directed): every local-dev merge gets LOCAL verification — Docker stack + Playwright end-to-end, by-path tests, and the local authority scripts for the i18n/DPA ratchets (`scripts/i18n-baseline-authority.sh`, the DPA ratchet test with `DPA_BASELINE_PROTECTED_BLOB` fetched via pin tag); every merge in this window is recorded CI-UNVERIFIED; **before ANY promotion to origin/dev after quota/runner returns, re-run the gates on GitHub against the promotion candidate.** Session 0578e8d8 merges under this regime are listed in its owner sheet. Staging Dokploy pipeline separately down (owner fixing). | Owner ruling 2026-08-21 (direct) | OPEN — armed until quota or self-hosted runner exists; discharge by a green GitHub run on the promotion candidate | Orchestrator + owner |
| S-18 | **r2f4 correcting-documents deploy step (rides the promotion carrying it)**: per tenant — `php artisan tenants:run db:seed --option="class=RolesAndPermissionsSeeder"` then `php artisan tenants:run permission:cache-reset`. NOT a migration: the staging auto-deploy will NOT run it; without both, admins 403 on the new `documents.correct` routes. | r2f4 lane deploy note 2026-08-21 | OPEN — armed at the promotion carrying r2f4 | Staging operator |
| S-15 | **−19.000 stranded unbalanced GL entry on the demo tenant** — W-6 D1b: the campaign's own DOC06 probe via a since-fixed VAT-zeroing bug; disposition = forward correcting entry OR evidence-pack annotation, owner-side on the demo tenant. NOT code scope of any lane; the enforcement-guards P3 handback points here and carries no status of its own (gate-r1 H-10). | `docs/handoff/PLAN-p0-fix-lanes-pre-production-2026-08-05.md` §W-6 (`:53`); `docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md` §4 3(a) item 6 | OPEN | Owner |
| S-19 | **Session B tenant migrations `2026_08_23_{120000,140000,150000,160000,163000}` — green-field note (owner 2026-08-24: migrations are fine, this is NOT a blocker)**: each carries a pre-flight scan that aborts only that tenant if pre-existing rows would violate the new CHECK/unique (all local censuses = 0; fresh tenants apply cleanly). The single post-step: after `tenants:migrate`, `grep -E 'status=(BLOCKED|FAILED)' <migrate log>` on token `POS TERMINAL IDENTITY/LIFECYCLE HARDENING:` — the Q-7 migration logs instead of throwing on a DDL failure, so a hit means a manual re-apply of that leg. Census SQL, if ever needed on a non-empty tenant, lives in C-14/C-15/C-16/C-18 + the Q-7 tenancy gate record. | Session B merges 2026-08-24 | OPEN (one grep) — Session A promotion checklist | Orchestrator |
| S-20 | **Q-10 deploy steps (rides the promotion carrying `2026_08_24_140000`)**: per tenant — (1) `tenants:migrate` (additive-only, cannot fail on rows); (2) re-run `RolesAndPermissionsSeeder` (idempotent) so `fiscal-periods.reopen` **and `fiscal-periods.close` (C-24 `bd03bd8cb`)** exist and are granted to accountant/admin — without it the new endpoint is a silent 403; (3) permission cache reset. FE map `permissionsMap.generated.ts` already regenerated in-lane (CI hash check). | Q-10 merge `f5cae1f12` | OPEN — Session A promotion checklist | Orchestrator |
| S-21 | **C-27 fleet census BEFORE the promotion carrying `0613e2ce8`** (no data repair implied — the index already refused every bad write): per tenant DB run the Q-11 gate §C queries #1–#3 (`docs/superpowers/reviews/2026-08-25-sb-q11-supplier-invoice-gate-r1.md`) + the `INC-` shape; the local run was NOT a fleet census — port 5433 hosts no `tenant_*` / `synerivia_central` DB, only seeded singletons (0 two-company tenants). A tenant with >1 company whose company B has `own_max < tenant_max` was blocked until now and simply starts working; nothing to backfill. | C-27 gates | OPEN — Session A promotion checklist | Orchestrator |
| S-22 | **Slice D batch 1 deploy steps (rides the promotion carrying `42f7f8bad`; 5 tenant migrations `2026_08_25_1301..1305`)**: (1) PRE-flight per tenant: the 13 census queries in `docs/sessions/session-B-2026-08-23/REPORT-D2-implementer.md` §5 (all must return zero rows; a hit = a raw writer to find — the migration would abort that tenant with the value named); (2) run with **`php artisan tenants:migrate-rolling --force`** (continue-and-collect, `entrypoint.sh:141` already does) — NEVER bare `tenants:migrate` (fail-fast on the first dirty tenant); each table is ACCESS EXCLUSIVE for census+ADD+VALIDATE — trivial on green-field tenants, maintenance window otherwise; (3) POST-flight per tenant: `SELECT conname, convalidated FROM pg_constraint WHERE conname IN (<the 13 names, listed in the report>)` → 13 rows all `t` (a NOT VALID constraint reads COVERED in the parity gate, so this is the only proof VALIDATE ran); (4) `grep -E 'status=(BLOCKED|FAILED)'` on the rolling-migrate log for these five. | batch-1 gates | OPEN — Session A promotion checklist | Orchestrator |
| S-23 | **Run `vat:backfill-tax-details` per tenant (B-19 supplier-document leg)** — supplier invoices and supplier credit notes POSTED BEFORE B-19 carry ZERO `document_tax_details` rows, so their deductible VAT is absent from every TN declaration and CANNOT self-heal (`post()` is idempotent: a re-post returns at the step-3 no-op and never rewrites). Owner-executed, never wired into a deploy step. Procedure: `tenants:run vat:backfill-tax-details` (DRY-RUN by default) → review the printed census per tenant → re-run with `--apply`. Then act on any printed CLOSED-PERIOD reopen/re-close instruction. Documents inside an ALREADY-FILED period are refused unless `--include-filed` (escalate to the accountant instead); documents inside a CLOSED period that cannot be reopened (a successor period is already closed/filed) are refused UNCONDITIONALLY and need a manual accountant correction. Local demo tenant dry run: 43 documents, 12 in scope, 313.884 TND deductible VAT, 0 skipped. | `docs/superpowers/reviews/2026-08-26-b19-handback.md`; gates `2026-08-26-b19-supplier-invoice-vat-gate-r1-{fiscal,treasury}.md` (§6 backfill procedure); `PROMOTION-CHECKLIST-2026-08-26.md` §3 | OPEN | Staging operator + Owner |
| S-24 | **Platform push gate live on staging (rides `1326a96c1`, no migration)**: `SYNERIVA_PLATFORM_PUSH_ENABLED=false` set on Dokploy api + worker envs 2026-08-29 (via `application-update`, the `saveEnvironment` wrapper 400s — see root `claude/deploy-runbook.md`); redeploy both AFTER the auto-deploy of `1326a96c1` settles, then VERIFY: a product submit / enrichment trigger on any staging tenant logs `platform push disabled (SYNERIVA_PLATFORM_PUSH_ENABLED=false): …` and the platform receives nothing. | Session J; gate records `docs/superpowers/reviews/2026-08-30-platform-push-gate-r{1,2}-fiscal.md` | OPEN — env set + api/worker redeployed 2026-08-29 16:14Z (health OK); FUNCTIONAL verification (POST /purchase-hub/orders → 502 ORDER_FAILED, submit → nothing reaches the platform) pending a staging tester login — on the 2026-08-30 tester checklist | Orchestrator (Session J) |
| S-25 | **Batch 1a promoted 2026-08-29 21:2xZ: origin/dev `1326a96c1 → 0cbaa1457`** (P0 registration-JSON echo fix, G-3c payment repositories per company, G-3b import-job company pin + composite gating, H Phase-1 cleanup, tester checklist). VERIFIED on staging (read-only tenant DBs, health OK): `2026_08_30_100200_add_company_to_import_jobs` + `2026_08_30_100800_backfill_company_payment_repositories` present on 9/9 tenants; second companies hold CASH-01/SAFE-01 (019ee4d7: 01a04cd2/01a04cef ×2; 019fc714: 01a04cbc ×2; 019f23cb: 019f23e5 ×3); import_jobs attributed/total 019ee4d7 2/3 · 019fc714 3/15 · 019fcbe5 2/18 · 01a04d1c 1/6. Migrate-log lines not read (no SSH; DB state is the evidence). | Session J | CLOSED (2026-08-29, this row) — functional retest on the 2026-08-30 tester checklist | Orchestrator |
| S-26 | **Batch 1b promoted 2026-08-29 ~22:00Z: origin/dev `0cbaa1457 → 0e5d28705`** (documents numbering per company incl. Expense/Income; payment-repositories index company-scoped; I-1 onboarding campaign + inert workflow; P0-2 registration time-limit hotfix with php-fpm 300 s / nginx 330 s; ledger rows through D-J0-11). VERIFIED on staging 2026-08-30 04:5xZ (read-only tenant DBs, health OK): `2026_08_30_100500_enforce_company_scoped_document_numbers` applied 9/9, `documents_company_id_type_document_number_unique` present 9/9, tenant unique dropped 9/9, lookup index present 9/9 (pre-push census: 0 cross-company collisions). Campaign run 23 on this content: L0a–L8 PASS, findings = {I2-F2}. | Session J | CLOSED (2026-08-30, this row) — functional retest on the 2026-08-30 tester checklist | Orchestrator |
| S-27 | **Batch 2 promoted 2026-08-30 ~04:2xZ: origin/dev `0e5d28705 → 0d97dc37e`** (G-6a d94706a95: atomic claim / stuck-job reaper `imports:reap-stuck` */5 / `imports:purge-expired` daily / cancel-DELETE; M6c `2026_08_30_100400_add_lifecycle_columns_to_import_jobs`; scheduler registrations pinned by `ImportSchedulerRegistrationTest`; + tester checklist + S-26). VERIFIED on staging 05:01Z (read-only tenant DBs, health OK): migration + 6 lifecycle columns 9/9; import_jobs statuses — no `importing` rows anywhere (reaper has nothing to do), `worker_lost` 0, `source_purged_at` NULL everywhere (expected until sources age 90 d). Observation: 019fcbe5 has 3 jobs in `validating` (not reaper scope) — G to read. **Today's staging build = 0d97dc37e.** | Session J | CLOSED (2026-08-30, this row) — functional retest on the 2026-08-30 tester checklist §D line 13 | Orchestrator |

## 3. Device owes (POS builds + seeder/cache steps)

> Namespace note: the `D-1…D-3` ids in THIS section are DEVICE owes. Session D's B-19 rows are `D-B19-n` in §6 — same letter, different namespace (same convention as the O-3…O-9 launch-gate note in §1).

**One device update ships all of the below** — the versions are cumulative, not separate rollouts.

| ID | Item | Source | Status | Action owner |
|---|---|---|---|---|
| D-1 | Device build stack: **v60/61** (replenishment) → **v62** (UoM) → **v63** (cash rounding) → **v66** (Lane C) → **v67** (SUBJECT TO owner ruling O-22) | memory: `project_replenishment_requests`, `project_uom_display_precision`, cash-rounding + Lane C files | OPEN | Device/deploy operator |
| D-2 | `RolesAndPermissionsSeeder` + cache-reset stack alongside the device update | `src: memory` | OPEN | Device/deploy operator |
| D-3 | **SV-11 device build MUST deploy fleet-wide BEFORE the SV-9 migration reaches any origin/dev promotion** (owner-ruled 2026-08-18 at sv-stage1 M5, with fail-closed option 1). SV-9 flips `require_blind_cash_count` on for every tenant via unattended `tenants:migrate`; a pre-SV-11 device shows no expected figure and no whole-drawer instruction → false variance ≈ opening float on every close, manager-PIN wall fleet-wide. HARD precondition on the promotion that carries SV-9, joins the v60-67 cumulative stack. **Two riders added at terminal audit (2026-08-19): (a) release-build devtools verification — the `devtools` cargo feature is compiled unconditionally (`apps/pos/src-tauri/Cargo.toml:16`); build a release POS and attempt to open the inspector — if reachable, gate the feature to dev-only in the SV-11 build (SV-10 concealment is render-level; `expected_cash` sits in component state). (b) Today's-Sales derivation ruling (ticket `2026-08-17-today-sales-blind-count-derivation.md`) must be RECORDED before SV-9 promotes — owner leaning presented 2026-08-19, option 4 recommended.** | sv-stage1 M5 gate (`.worktrees/sv-stage1/docs/handoff/reviews/sv-stage1/M5-round1.md`); owner ruling 2026-08-18; terminal-audit register (parent, 2026-08-19) | OPEN — armed at sv-stage1 merge | Device/deploy operator + promotion gatekeeper |
| D-J0-1 | **Rulings record — platform push gate (owner-ruled: staging must never enrich the production platform).** `SYNERIVA_PLATFORM_PUSH_ENABLED` (`services.platform.push_enabled`, default `true`) gates EVERY outbound platform write — submit/bulk submit/trigger enrichment, enrichment feedback, brand-mapping push, upload-URL + photo upload, and Purchase Hub `placeOrder` (gate r1 F1: typed `ORDER_FAILED` refusal, never silent success); reads (barcode lookup, catalog browse, vehicle query, webhook verify) stay ungated; jobs complete cleanly on the no-op (no retry storm); default `true` is byte-identical to pre-lane behavior (r1 F2: pre-existing tests restored verbatim). Lane `4ff7954c9` + fix `6b44f4512` (Codex) merged `1326a96c1`, promoted to `origin/dev` 2026-08-29. | Codex gates r1 CHANGES → r2 APPROVED (`docs/superpowers/reviews/2026-08-30-platform-push-gate-r{1,2}-fiscal.md`) | RULED + MERGED + PROMOTED 2026-08-29 (`1326a96c1`) | Orchestrator (Session J) |

## 4. Expert-comptable queue

| ID | Item | Source | Status | Action owner |
|---|---|---|---|---|
| E-1 | Rulings **R-b, c2, c3** (+ stamp) | `docs/superpowers/tickets/2026-08-08-expert-comptable-rulings-rb-c2-c3-stamp.md` | **RECEIVED 2026-08-08** — STALE-IN-MEMORY (memory listed as owed); verify downstream lanes consumed them | Orchestrator |
| E-2 | **c1-bis** (perpetual-inventory open question in the R-b/c2/c3 ticket) | `docs/superpowers/tickets/2026-08-08-expert-comptable-rulings-rb-c2-c3-stamp.md` §c1-bis; `HANDOVER-document-per-action-remediation-2026-08-08.md:26,33` | PARTIALLY RULED per handover ("PERPETUAL inventory as…") — confirm closure in the ticket | Owner ↔ expert |
| E-3 | **6354 confirmation** (TN 6654 vs FR 6354 seeded-account question) | `src: memory` (country accounting seeded-settings rule) | OPEN / UNVERIFIED (no repo doc located naming 6354) | Owner ↔ expert |
| E-4 | **D4 final confirm** | `src: memory` | OPEN / UNVERIFIED | Owner ↔ expert |
| E-5 | **EQ-1 (+Q5b/Q6)** cleared-instrument reversal — "SEND NOW, long turnaround, gates only Path B" | `docs/handoff/HANDOVER-dpa-session2-2026-08-09.md:90-92` | OPEN — send if not yet sent | Owner ↔ expert |
| E-6 | **FR analogs** of the 2026-08-10 rulings (PCG 487 / délivrance-vs-débits) — needed before FR launch only | `docs/handoff/HANDOVER-dpa-session2-2026-08-09.md:92-93` | OPEN (FR-launch-gated) | Owner ↔ expert |
| E-7 | SEEDS gate **8-item account-confirmation list** (603-vs-6037 + liasse-607, 628-vs-65x, 7097, TN 606/625x post-H-1, 409/419, 418, FR 666/766, FR supplier-invoice fallback) | `docs/handoff/HANDOVER-dpa-session2-2026-08-09.md:93-96` (gate-seeds-review.md §expert) | OPEN | Owner ↔ expert |
| E-8 | Deferred-revenue / VAT-issuance rulings | `docs/superpowers/tickets/2026-08-10-expert-rulings-deferred-revenue-vat-issuance.md` | **RECEIVED 2026-08-10** — precondition recorded (no `allow` seed until 472 machinery exists) | — |

## 5. Hard pre-enable gates (BLOCK enabling a feature/launch)

| ID | Gate | Source | Status | Action owner |
|---|---|---|---|---|
| G-1 | **Sign-blind `SUM(pos_receipts.total)` aggregates** must be receipt_type-aware before `EnableV4RefundAuthoringCommand` runs on any real tenant | `docs/superpowers/tickets/2026-08-01-positive-refund-total-consumers.md` | **CLOSED (2026-08-18: code verified 2026-08-12 per evidence below; gate ticket carries its close-out header "Status: CLOSED — pre-enable gate satisfied", verified 2026-08-17 against origin/dev 7d85232cc)** | — |
| G-2 | **Cash-rounding enable blocked** — refund short-change up to D/2 in either direction on refunds of rounded receipts; do not run the Enable step until refund-rounding track lands OR owner signs acceptance (O-24) | `docs/handoff/cash-rounding-phase2-deploy-checklist.md:224-229` (verified 2026-08-12: both condition boxes unticked; refund-rounding spec approved, NOT implemented) | BLOCKED-ON-O-24 | Owner |
| G-3 | **TRAINING receipts move real money in Treasury/GL** | `docs/superpowers/tickets/2026-07-31-treasury-bridge-training-money-legs.md` (superseded by the 2026-08-21 lane evidence) | **SATISFIED (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17) — Treasury-side money containment only.** Three sealed-`training_flag` guards: `TreasuryReceiptBridge::apply()` (tender legs, GL, drawer movement, instruments, §4.6; sales+refund shapes; every event_version), `TreasuryAccountPaymentBridge::apply()`, `PosCoreReceiptProjection::redeemVouchers()` (an asymmetry the first commit created and the lane closed). All three surfaces are PRE-ENABLEMENT DEFENSE-IN-DEPTH (FiscalEventEngine refuses training_flag on any operational chain — device cannot author these today); they cover legacy/replayed/quarantine-repaired/direct-DB rows and the moment training authoring is enabled. **Stock NOT contained** — training receipts still move real inventory/COGS: `2026-08-21-training-stock-movement-gap.md` (P2, inventory-costing lane). G-3 must NOT be read as "training is contained". Gates: fiscal-pos r1 CHANGES + treasury r1 CHANGES → fix round → treasury r2 ACCEPT + fiscal r2 CHANGES (2 P2) → final round, parent-verified. Evidence: 16 tests/71 assertions on MERGED dev (3 containment classes, all in the Treasury whole-dir CI lane), red-first at c6d6308ae; regression 105/479 PG; pre-enable census (expected zero, non-zero = unmodeled path, STOP) in `2026-08-21-training-latent-surfaces-deposit-and-exchange.md` §Census. Merged local dev `4a11abff4`. Raised NOT fixed: **C-5** voucher/CompanyContext P1 (non-training path, own lane in flight). | — (census = owner at deploy) |
| G-4 | **TN VAT declaration must net POS refunds before refunds enable** — `EloquentVatDataRepository` has no `receipt_type` discrimination on the POS arm (**OP-03**, owned by launch **E1** refund-enable block) | `docs/handoff/FINDINGS-other-problems-2026-08-11.md:67` | **SATISFIED IN CODE (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17)** — per-row `-ABS` CASE on both SUMs normalizes BOTH writer sign eras (canonical positive / legacy negative); treasury gate round 1 CHANGES-REQUIRED → fix round → merged local dev `efa2282dd`. Red-first proof by gate revert-probe (700/133 vs 900/171); PG + sqlite green; evidence test added to the pgsql CI allowlist (ci.yml, workflow-touching under S-17); voided-return cell pinned non-vacuously. Residuals: `document_count` semantics = owner sheet B-6; NEW pre-existing ticket `2026-08-21-vat-period-last-day-boundary.md` (last day of every period silently dropped from the POS arm — own lane). | — |
| G-5 | **`treasury.shift_variance_gl_enabled` flip conditions** — boxed condition: SV-3+SV-4 (float + DEPOSIT/PAYOUT booked), SV-5, SV-2 (v3 `CashCountRecorded` — absolute on an all-v3 fleet), SV-7+SV-8, G-2/G-3/G-4 (SV-17). Stage-1 items are NOT preconditions and must not delay (or be delayed by) the flip. | `docs/handoff/FINDINGS-shift-variance-gl-2026-08-11.md:366-377` | OPEN — flag stays false | SV lane + owner (via O-19) |

### G-1 evidence (verified 2026-08-12, working tree at branch `dev`)

Every consumer named AT-RISK by the gate ticket now carries per-row, receipt_type-aware handling (`CASE WHEN receipt_type='return' THEN -ABS(col) ELSE col END` — per-row, never `ABS(SUM(...))`, so mixed legacy-negative/v4-positive eras stay correct):

- `apps/api/app/Modules/POS/Application/Services/PosAnalyticsService.php:38-43,70,111,139,174,203,337` — all sums via `netOfReturns` / `netOfReturnsQualified` helpers (defined `:457`, `:475`); discount arms filter `receipt_type = Sale` (`:242,257,274`).
- `apps/api/app/Modules/POS/Domain/Services/GrandtotalService.php:230-238` — perpetual totals apply `-ABS(...)` per row; docblock `:214-223` cites the gate ticket explicitly.
- `apps/api/app/Modules/POS/Application/Services/ReportGenerationService.php:528,546` — `pos_receipt_payments.amount` summed with per-row `-ABS` CASE on `receipt_type='return'`; change_due excludes returns.
- Already-safe sites re-confirmed: `SalesReportService.php:72,81` and `topSkus` `:122` (filter `receipt_type = Sale`); `LiveSalesReportService.php:63`; `OwnerSalesSummaryService.php:105-114` (CASE-split gross/returns with per-row ABS).
- Guard test asserting the blended-sum trap: `apps/api/tests/Feature/POS/ReceiptReturnRefactorV3Test.php:556,819-828`.

Verdict: **satisfied in code.** The three-way disagreement (MEMORY.md "open gate" / readiness register "satisfied-in-code needing close-out" / UI session "appears-remediated") resolves to the readiness register's version. Remaining action: close-out header on the gate ticket; MEMORY.md line is stale.

## 6. Lane-owed code debts (ticketed, awaiting a lane — not enable-gates)

| ID | Item | Source | Status | Action owner |
|---|---|---|---|---|
| C-2 | **Device Z/X/EOD/SESSION_CLOSE SALE-branch per-rate VAT decomposition treats gross line_total as net** — LIVE on every device build incl. staging; three consumer sites (`apps/pos/src/lib/offline/zReportService.ts:~905-915`, `endOfDayPreview.ts:~301-308`, `apps/pos/src/api/reportApi.ts:~492-497` — M0 re-derivation 2026-08-21; the old endOfDayPreview cites were +4 stale) feeding THREE signed event types (Z_REPORT, X_REPORT, **SESSION_CLOSE** — M0 finding F-1, one `vatBreakdown` input feeds all three builders); refund branches already fixed (Lane C). ~~Headline totals correct~~ **STRUCK 2026-08-21 (M1 ruling, V-5/V-6): the headline premise was FALSE — `cartTotals.ts:36` sums GROSS line_totals into `subtotal`, so signed `net_sales == gross_sales` on every taxed shift, exported as NF525 `<VentesNettes>` — that sibling defect is C-6.** Secondary: same expressions call bcadd/bcsub with no scale arg. **Lane DISPATCHED 2026-08-21** (brief gated r1, 6 findings applied; branch `codex/z-sale-branch-decomposition-2026-08-18`): M0+M1 done, **ruling = OPTION B in-place correction, unconditional** (`docs/handoff/reviews/z-sale-branch-decomposition/M1-ruling.md`, owner-review item), M2 in flight. Rides device build stack D-1/D-3; must ship before tenant #1's first signed shift. | `docs/superpowers/tickets/2026-08-01-device-z-sale-branch-gross-as-net.md`; M0/M1 records 2026-08-21 | **FIXED IN CODE (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17): wave COMPLETE, merged local dev** — 5 milestones, whole-lane gate ACCEPT; ruled Option B implemented exactly (15 production lines = three identical blocks, net = gross − vat at explicit scale); SESSION_CLOSE lockstep asserted byte-identical; real-writer E2E with the live symptom netting to exactly zero; every gate finding closed in-branch. **DEVICE-BUILD-GATED: reaches terminals only via the D-1 build stack; the M1 ruling's reversal window closes at DEVICE BUILD ROLLOUT, not merge — owner surfaces this before the build ships. Sibling C-6 (headline) MUST ride the SAME build — the coupling became MANDATORY (not "prefer") when C-6's F-4 tripwire landed: a C-2-only device build would author Zs whose corrected per-rate rows disagree with the gross-as-net headline, and once F-4 is server-side every such taxed Z quarantines permanently. CONFIRMED 2026-08-22: no C-2-only build exists or can exist — C-2 lives only on unpushed local dev and no device build has been cut since; fully-legacy (pre-C-2) builds PASS F-4 by its disagreement-only design.** | Device/deploy operator (build) |
| C-6 | **Signed Z/X/SESSION_CLOSE headline `net_sales` EQUALS `gross_sales` on every taxed shift** — `apps/pos/src/lib/payment/cartTotals.ts:36` (path corrected 2026-08-23; the row previously cited `lib/pos/`, which does not exist) defines `subtotal` as Σ GROSS `line_total`s; `receiptService.ts` stores it; `zReportService.ts:899-901` sums it into the signed headline; NF525 exports it as `<VentesNettes>` (wrong for every taxed shift). Discovered by C-2's M0/M1 (finding F-2), CONFIRMED by the ruling gate (V-5/V-6). **CORRECTED 2026-08-22 (lane + gate superseded the row's early sketch): the implemented identity is `subtotal − tax_amount` per receipt (NOT `total − tax_amount` — that would fail the lane's own F-4 tripwire on discounted/rounded shifts; `subtotal − tax_amount` reproduces the sealed canonical `subtotalNet` byte-for-byte and makes device-Z and server-Z agree), and the lane DOES carry two server changes (F-4 validator tripwire + F-5 NF525 GrandsTotaux). SEQUENCING IS NOW MANDATORY, not prefer-don't-block: once F-4 is server-side, a C-2-only device build would quarantine every taxed Z permanently — see the C-2 row's coupling clause (no such build exists or can exist; fully-legacy builds pass F-4's disagreement-only design). F-4 lands AFTER the headline fix in commit order (done in-lane).** Also related: R-01 readiness audit ranks this defect class the #1 tenant-#1 blocker. | C-2 lane M0/M1 records + M1-ruling.md §sub-item F-2 | **FIXED IN CODE (2026-08-22, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev** — headline = `subtotal − tax_amount` at all three consumers + SESSION_CLOSE (device-Z and server-Z now agree; sealed-corpus-tied identity); F-6 scale args; F-5 NF525 GrandsTotaux now exports real period totals (was literal 0.00); F-4 disagreement-only vat_breakdown tripwire (1-ulp group slack, money-guarded, ValueError-safe on the verify-chain path, refund-count fail-closed). Gate round 1 CHANGES (incl. the empirically-demonstrated over-strict identity + the retroactive-corpus attack) → fix round, parent-verified at merge. DEVICE halves ride the SAME build as C-2 (MANDATORY coupling — see C-2 row); SERVER halves (F-4/F-5) land at promotion; ci.yml touched → S-14 dispatch leg per S-17. Post-merge: validator 151/319, e2e 10/10, manifest reconciled (gated_ceiling 1132). | Device/deploy operator (build) |
| C-3 | **`CopiesDocumentData.php:309-310` hardcoded bcmath scales on the tax-rate conversion** (PHPStan precision-guard findings) — inside the fiscal conversion surface (VAT-zeroing history, multiply gated); the correct fix is a judgment call for the owning lane, not a hot-patch. Currently an inherited-preflight waiver line for every lane whose brief forbids touching the file (DN NG-4). | DN wave handback 2026-08-19; parent read of the site | OPEN — assign to the next conversion-surface lane | Orchestrator |
| C-4 | **Web lint ratchet inherited red: baseline 6449, dev at 6451 (+2) — the drift rode the `c6d6308ae` promotion itself** (CI never runs on dev pushes, so the last green Frontend Lint was at P2's A=d8a631133; something in the later promotion commits added 2 warnings undetected). Verified 2026-08-21: current-session merges innocent (the only session-touched web file's 4 warnings are all pre-existing lines). Every web-touching lane now trips this on `lint:ratchet`. Fix = small mechanical lane: diff eslint outputs at d8a631133 vs dev, fix the 2 new warnings (never bump the baseline). | Session 0578e8d8 diagnosis; POS-screens lane first reporter | **CLOSED (2026-08-23: DOES NOT REPRODUCE — controlled identical-environment measurement, fresh `pnpm install` from lockfile at BOTH commits, node v25.2.1/pnpm 9.14.2: dev `fa807a699` = exactly 6449 (`lint-ratchet.mjs` PASS, raw eslint 6449 warnings/0 errors); baseline `d8a631133` = 6452; dev has 3 FEWER warnings and ZERO new ones, removals pinned to the deleted ImportTypeCard.tsx (G9 lane) + i18n.ts. The original "+2" was environment noise — stale node_modules/version skew — matching the row's own flip-flop caveat. No code change, no baseline touch.)** | — |
| C-5 | **Voucher-paid receipts cannot project on a real Horizon worker** — `VoucherRedemptionService::redeem()` → `GeneralLedgerService::createVoucherLedgerEntry():~2616` → bare no-arg `getScale()`; `ApplyFiscalEventProjectionJob` binds no CompanyContext → `UnboundCompanyContextException`, whole SALE_RECEIPT projection rolls back and retries forever, for ANY ordinary receipt tendering a store voucher. Found by the G-3 fix round's control arm (which masks it by binding context, docblocked). Rule-19/20 fix: pass entity currency into the voucher GL scale resolution. | `docs/superpowers/tickets/2026-08-21-voucher-redemption-unbound-company-context.md` | **FIXED (2026-08-21, session 0578e8d8; CI-UNVERIFIED per S-17): merged local dev** — entity currency (ledger row → voucher fallback, empty→throw) threaded through the voucher GL path incl. the RoundingAdjustment leg; mutation-verified scale discrimination (4 seam mutations each caught, incl. value-invisible hardcode-3 via resolver-call attribution); G-3's masking control arm CONVERTED into an independent C-5 regression guard (revert probe: exactly one error, the converted arm). Treasury gate r1 CHANGES → final round, prescribed items verified at merge. Post-merge 6/55 green. NEW owes recorded: (a) ProvisioningRequiredPurposesV1 line-pinned evidence registry drift deepened +44 (inherited-red conformance/ratchet tests, count unchanged — registry owner re-syncs from a LIVE scan, no partial fix); (b) see C-7. | — |
| C-7 | **`PosCoreReceiptProjectionTest` (PG) is nondeterministically red in multi-class runs** — 35-41 failures across IDENTICAL code/runs, 31/31 green standalone; root cause = unordered, unscoped `DB::table('pos_receipts'/'pos_receipt_lines')->first()` assertions picking up rows leaked across tests in one process. Independently confirmed by THREE lanes this session (fiscal-bytea, C-5, earlier bleed reports). Directory-level PG pass/fail counts for this area are UNUSABLE as a gate until fixed. Fix = scope/order the assertions (committed-state-bleed test-infra class). | fiscal-bytea + C-5 lane reports 2026-08-21 | OPEN — test-infra lane queued | Orchestrator |
| C-8 | **Sanctioned chained-imbalance class #2 — r2f4 correcting entries (BY DESIGN, census delta)**: with r2f4 merged, an individually-unbalanced correcting entry (supplying a broken original's missing leg, whole-footprint invariant) joins the P3-M1 census's class-(c) count — the census closed on "exactly 1" (the O-26 lineless carve-out); the count is now 2 by designed mechanism. The sealed census record stays untouched (evidence integrity); THIS row is the delta of record. Owner ack = sheet B-14(i). Guards: admin-only `documents.correct`; advisory lock; cancelled-target refusal; partner-axis validation; DPA-linked. RESIDUAL of record: a raw-DB re-point of a SEALED correction's `source_document_id` migrates its footprint between documents (measured by test, not asserted) — closing it = widening `trg_document_immutability` (schema lane, deliberately outside the zero-schema r2f4). | r2f4 merge 2026-08-21 + both gates | OPEN (trigger-widening lane) — **B-14(i) ack RECEIVED 2026-08-23 (greenfield framing); B-14(ii) deptrac 176→182 ack RECEIVED same message.** **2026-08-22 composition note: with O-26 landed, class #1 (the lineless carve-out) is CLOSED-FOR-NEW-DOCUMENTS and shrinking-only (legacy population); class #2 (r2f4 corrections) is the sole live-by-design member. The two records compose: new lineless posting refused; corrections individually-unbalanced by design; legacy carve-out shrinking to zero.** | Owner + schema lane |
| C-9 | **Import execute re-arm residuals (audit #23 closed PARTIAL by Q-1)**: (i) `ImportController::updateOptions` (`:375`) re-arms a dispatched job — PATCH accepted while `Pending` (which post-Q-1 also means "queued"); `validateJob()` then writes `Validating`/`Validated` over a worker's `Importing` and the wizard re-shows Execute → second dispatch claims successfully. Fix shape: distinct `Queued` status or `dispatched_at` discriminator. (ii) Sync twin `ImportService::executeImport()` (`:327-336`) has no worker-style claim — two concurrent executes on a ≤100-row (sync-branch) job both apply; 6-line mirror of `ProcessImportJob`'s conditional claim. | Q-1 gate r1 (`docs/superpowers/reviews/2026-08-23-sb-q1-import-gate-r1.md` findings 1-2) | OPEN — small follow-up lane (Session B queue tail or program WS-I) | Orchestrator |
| C-10 | **Two sqlite-masks-PG test defects in `tests/Feature/Import` (pre-existing, proven on base commit)**: `ResultWorkbookTest.php:84` binds `'product-1'` into a uuid column (sqlite-only fixture); `PartiesImportBalancesTest.php:130` `assertSame`s jsonb key ORDER. Both fail on PG, green on sqlite; directory-level PG counts for Import are polluted until fixed. | Q-1 gate r1 PG leg | OPEN — test-infra micro-lane (pair with C-7 class) | Orchestrator |
| C-11 | **Loyalty redemption wiring lane (endpoint gated 501 by Q-3)**: `POST /loyalty/pos/redeem` is hardened (lock + conditional debit + idempotency capability, merged `faa39d7ef`) but returns 501 `LOYALTY_REDEMPTION_NOT_WIRED` until a wiring lane ships: (i) redeem response carrying `reward_value`/`points_spent` + FE discount application (client currently fabricates the type and voids the value; no `RewardRedeemedV2` listener exists); (ii) client sends `idempotency_key`; (iii) `quantity_available` never decremented anywhere; (iv) `quantity_per_member` unenforced (`isEligible(...,0)` hardcoded); (v) no `reward.program_id === enrollment.program_id` assertion (cross-program spend within a tenant); (vi) rule-19 `<` on decimal strings at `RewardRedemptionService.php:41`; (vii) FE hides the dead Redeem affordance. | Q-3 gate r1/r2 (`docs/superpowers/reviews/2026-08-23-sb-q3-loyalty-gate-r{1,2}.md`) | OPEN — wiring lane, post-launch unless owner wants loyalty live for tenant #1 | Orchestrator |
| C-12 | **Promotion/coupon recordUsage wiring obligations (Q-4 residuals)**: (i) `PromotionManagementService::recordUsage()` (`:57-66`) has no txn/lock/idempotent catch while the NEW `promotion_usages` unique index is live — a replay 23505s uncaught the day promotion redemption is wired (zero callers today, grep-verified twice); ship the coupon-shaped lock+catch WITH the wiring. (ii) OWNER RULING OWED before wiring coupon `recordUsage` to checkout: post-seal refusal vs record-and-flag (discount is already sealed on the fiscal receipt; gate + implementer both recommend record-and-flag). (iii) Seal durability is conditional on the caller catching the refusal (documented contract). (iv) pre-existing no-arg `getScale()` at `CouponValidationService.php:128-130`. | Q-4 gate r1/r2 (`docs/superpowers/reviews/2026-08-23-sb-q4-coupon-gate-r{1,2}.md`) | OPEN — rides the checkout-wiring lane; owner ruling flagged | Orchestrator + Owner |
| C-13 | **N-5 has-pins residuals (all Minor, none blocked merge)**: (i) `pos_pin` is NEVER cleared on offboarding — only `UserController.php:704` explicit endpoint clears it; ghost PINs make `setupPin`'s tenant-wide, unscoped uniqueness scan (`PosAuthController.php:133-144`) refuse valid new PINs (nuisance, not lockout); (ii) device `pruneOperatorsExcept` (`apps/pos/src/lib/sync/syncService.ts:1320`) skips on an EMPTY pin-data response, so a terminal whose last operator was offboarded keeps the stale cached PIN and can still auth OFFLINE while the server answers `has_pins=false` — device sync lane; (iii) CLOSED by W2-1 `14748365f` — `Str::isUuid` guard + typed 400/403; (iv) optional: mixed-population `has_pins=true` test + `whereExists` rewrite of `pinHolders()`. | N-5 gate r1 (`docs/superpowers/reviews/2026-08-24-n5-haspins-gate-r1.md`) | (i) CLOSED 2026-08-26 — `pos_pin` now cleared on both offboarding cascades (`deactivate()`/`destroy()`), T9 batch merged `16088fdb7` (see D-T9-9); (ii) OPEN — device `pruneOperatorsExcept` still skips on an EMPTY pin-data response, device-sync lane pre-rollout of device build D-1 (authz gate r1 finding 2); (iii) CLOSED by W2-1 `14748365f`; (iv) optional, platform backlog | Orchestrator |
| C-14 | **Q-2 counting residuals + promotion obligation (merged `850e86217`, MIGRATION-BEARING)**: (i) STAGING CENSUS before the promotion carrying `2026_08_23_120000_unique_stock_movements_counting_apply`: per tenant run the docblock duplicate-grain scan on `stock_movements` (`reference_type='inventory_counting'`, grouped by counting/product/location/variant, HAVING COUNT>1) — any hit ⇒ split the index into a follow-up migration; index is non-CONCURRENTLY (ACCESS EXCLUSIVE window, fine at current sizes). (ii) `InventoryCountingService::manualOverride()` (`:1086`) writes item quantities with neither the header lock nor a terminal-state guard, and `CountingItemController` (`:288-310`) has no status check (sibling `setOpeningCost` `:222-236` does) — 2-line `lockCounting`+`assertNotTerminal` follow-up. (iii) `finalize()` still 500s for unresolved items (`:1131`, pinned by `ReconciliationTest.php:494`) while its sibling refusal now 422s — align to the typed 422. (iv) the 422 `COUNTING_TRANSITION_REFUSED` message is untranslated. (v) MINOR-8 restated: 5 counting test files cannot run on PG — `counting_number` fixtures overflow `varchar(20)` (ReplayFinalizeTest 11, OnboardingFirstCountTest 7, OnboardingLifecycleTest 6, PreFinalizeReplayPreviewTest 6, ZoneScopedCountingTest 4 = 34 PG errors, all pre-existing); the file covering both index-guarded listener paths has never executed on PG. | Q-2 gate r2 (`docs/superpowers/reviews/2026-08-24-sb-q2-counting-gate-r2.md`) | (i) rides Session A's promotion checklist; **(ii)/(iii)/(iv) CLOSED 2026-08-26 by B2-6 (merged `9d0d08ae5`, see D-B26-1)**: `manualOverride()`/`activate()`/`activateDraft()` now lock+guard, unresolved-items is a typed 422, and the translated message now actually reaches the FE toast; (v) OPEN, small follow-up lane | Orchestrator |
| C-15 | **Q-6 pos_receipts trigger residuals + promotion obligation (merged `681c7bf29`, MIGRATION-BEARING)**: (i) STAGING CENSUS before the promotion carrying `2026_08_23_160000` — per tenant `SELECT fiscal_status, COUNT(*) FROM pos_receipts WHERE fiscal_status IN ('voided','pending_sync','synced','sync_failed') GROUP BY 1;` (informational — the migration only replaces a function, but any non-zero row is now FROZEN and must be known). (ii) **F-6 [Important, pre-existing]** the `fiscalized→voided` edge guards only 7 columns (migration `:198-207`): `previous_hash`, `canonical_bytes`, `sealed_hash_algorithm`, `vat_breakdown_hash` can be rewritten ON the void edge and the row is then frozen in the forged state — the widest remaining hole on the fiscalized arm; follow-up lane widens the edge to `to_jsonb − {fiscal_status,is_voided,voided_at,voided_by,void_reason,updated_at,…}` (brief mandated verbatim carry, so not folded). (iii) F-3 no `CHECK (sealed_hash_algorithm IN (...))` — bogus algo ⇒ NF525 verification ValueError DoS (Slice D candidate). (iv) F-5 `pos_receipts` has no BEFORE TRUNCATE guard while `fiscal_events`/`repository_movements` do. (v) F-2 asymmetry: `contacts` FK cascade refusal unpinned (only `partners` pinned). (vi) F-8/F-9 docblock nits: says 10 local DBs (11); "byte-identical down()" is executable-body-identical (comment lines differ). (vii) CI: the class is now in the backend-pgsql `--filter` allowlist — S-14 dispatch-verification leg applies to the promotion carrying it. | Q-6 gate r2 (`docs/superpowers/reviews/2026-08-24-sb-q6-trigger-gate-r2.md`) | OPEN — (i)+(vii) ride Session A's promotion checklist; (ii) small follow-up lane (fiscal-pos gated); (iii) Slice D | Orchestrator |
| C-16 | **Q-8 held-order residuals + promotion obligation (merged `5de9affe2`, MIGRATION-BEARING)**: (i) STAGING/PROD CENSUS before the promotion carrying `2026_08_23_163000`: per tenant DB (`datname LIKE 'tenant%'`) `SELECT COALESCE(status,'<null>'), COUNT(*) FROM pos_held_orders WHERE status IS NULL OR status NOT IN ('held','recalled','expired') GROUP BY 1;` — any row aborts THAT tenant's migrate (pre-flight throws before DDL). (ii) **Terminal-scope follow-up (web lane)**: the audit item "recall is not terminal-scoped" is NOT closed — `terminal_id` is optional on the wire and the one live client (`apps/web/src/features/pos/api/heldOrderApi.ts:106`) POSTs bare; follow-up = send `terminal_id` from the web POS on recall/show, then flip the FormRequest to required; `HeldOrderController::show()` (`:109-111`) unscoped by terminal rides the same lane. (iii) `routes_held_orders.php:15-20` has NO `permission:` middleware although `pos_held_orders.view/create/delete` are seeded (`RolesAndPermissionsSeeder.php:375-377`) — discard is now an audited actor-attributed action authorised to any company member (tenancy-authz lens). (iv) discarded tombstone keeps `status='held'` under `deleted_at` — any raw query on `pos_held_orders` must carry `deleted_at IS NULL`. | Q-8 gate r1 (`docs/superpowers/reviews/2026-08-24-sb-q8-held-orders-gate-r1.md`) + fix round `872e13662` | OPEN — (i) rides Session A's promotion checklist; (ii)+(iii) small follow-up lane (web + authz) | Orchestrator |
| C-17 | **Q-7 terminal-claim residuals (merged `43a2dad14`; none blocked merge)**: (i) device-side binding invalidation — `apps/pos/src/stores/terminalStore.ts:614-673` `initialize()` never compares the refreshed `hardware_identifier` to `getDeviceId()`; shift open/close and fiscal ingestion carry no binding check → an admin releasing a LIVE device's terminal re-creates two-tills-on-one-chain until the `fiscal_events` chain key parks the loser as `sequence_conflict` (POS lane; owner-visible). (ii) `zChainState` `z_hash_sequence` is `count()` (`TerminalController.php:~785`) vs MAX for `z_number` — diverges when any Z row is absent (exactly the O-30 population); value is sealed into the Z_REPORT payload (`legacyReportReference.hash_sequence`), Z hash unaffected; fix = derive from latest row. **CLOSED 2026-08-26** — `TerminalController::zChainState()` now returns `$latestZReport->z_number` (commit `66c4f265f`, already on dev; verified unaffected-Z-hash claim + regression-pinned by `TerminalClaimHardeningTest` in the O-30 lane, merge `8c8316ab7`). (iii) no web `releaseTerminal` client (`terminalApi.ts`) — curl-only until a FE lane; FE must branch on `error.code` not status. (iv) non-UUID `{id}` → 500 across the whole terminal route group (`POS/routes.php:63-77`) — fix group-wide (`whereUuid`). (v) per-company hardware grain RULED deliberate (one device may hold a terminal in two companies of one tenant). (vi) no-op release (already unbound) indistinguishable in the response; also hides an existing orphan. (vii) `generateTerminalCode()` (`:967-974`) derives the next code from `count()` — TOCTOU behind the ≥500 pin. (viii) open-shift probe in `release()` is check-then-act without a lock (`:540-561`). **CLOSED 2026-08-26** — both shift-open paths (`ZSessionLifecycleProjection::projectPosShiftOpen()`, `ShiftManagementService::openShift()`) now take the `pos_terminals` row lock (`lockForUpdate()`, same order as `release()`: `pos_terminals` → `pos_shifts`) before deciding whether the terminal already has an OPEN shift — closing the race that produced an orphan unresolvable by `pos:shift:close-orphaned` because the audit row never named it (merge `8c8316ab7`). Residual: a `SESSION_OPEN` synced AFTER the release commits is still unnameable by any audit row — see LEDGER D-O30-1. (ix) stale docblock line refs + "every terminal is v3" over-claim (`getOrCreateWebTerminal` is v2). | Q-7 gates r1-tenancy / r1-fiscal / r2-fiscal | OPEN — (i)+(iii) POS/web follow-up lanes; (iv)/(vii) small backend lane; (ii)/(viii) CLOSED 2026-08-26 (O-30 merge `8c8316ab7`) | Orchestrator |
| C-18 | **Q-5 voucher-void residuals + promotion obligation (merged `b851db749`, MIGRATION-BEARING)**: (i) STAGING CENSUS per tenant BEFORE the promotion carrying `2026_08_23_150000`: duplicate census `SELECT voucher_id, COUNT(*) FROM voucher_ledger WHERE event='voided' GROUP BY voucher_id HAVING COUNT(*) > 1;` — any hit ⇒ split the partial unique into a follow-up migration (immutability trigger rejects DELETE); #22 money-hole census `SELECT COUNT(*), SUM(ABS(amount)) FROM voucher_ledger WHERE event='voided' AND gl_journal_entry_id IS NULL AND amount <> 0;` — any hit ⇒ forward correcting JE per tenant (historical NULL rows UNREPAIRABLE in place). Local censuses are clean-but-VACUOUS (all 11 local ledgers empty). (ii) **PROGRAM TICKET (treasury R1, mandatory)**: the fraud counter never records attempts against ACTIVE vouchers — brute-force against a live voucher is invisible; remediation is a freeze/hold edge (does not exist), never the void edge. (iii) **PROGRAM TICKET (treasury R2)**: nothing writes `VoucherStatus::Expired`; the future expiry lane owns breakage GL treatment (never the void edge) and must decide skip-vs-block for cascade BEFORE the engine ships. (iv) fiscal F-1 pinned obligation: Nf525 legacy verify arm lacks the `is_voided=false` filter — owed by the future `ReceiptVoided`-emitter lane. (v) `VoucherSource` Loyalty/GiftCard/Promotional voids would 500 at the GL account map the day those sources ship (currently unreachable) — note for the loyalty/gift-card lane. (vi) F-8 `VOUCHER_NOT_FOUND`→404 ships without a dedicated test (unreachable past the controller pre-check except in a race). | Q-5 gates r1 treasury/fiscal + micro-round `12ee4c81d` | OPEN — (i) rides Session A's promotion checklist (add to S-19 bundle: migration `150000`); (ii)-(v) program spec §workstream V | Orchestrator |
| C-19 | **Q-9 kitchen/order gating residuals (merged `5e4a0af3e`)**: (i) ~~NO `module:Tables` gate exists anywhere in the backend~~ **CLOSED by Q-13 `0a3b629ff`** — `routes_tables.php:15` is ungated while the FE gates on Tables (the brief's premise was false; rule-12 both-layers violation on the tables cluster) → small tenancy lane. (ii) KDS swallows the new 422: no `onError` in `useKitchenOrders.ts:33-56`, bare `.mutate()` at `KitchenDisplayPage.tsx:21-27`, no global MutationCache; and there is NO `OrderCancelled` event, so a cancelled card lingers ≤30 s — dead button, no feedback (web lane). (iii) `KitchenDisplayController.php:85-86/:115-116/:145-146` return bare `{message}` not the house `{error:{code}}` envelope (sibling `OrderController` uses the envelope) — not machine-readable/translatable. (iv) `PosHubPage.tsx:28-62` gates Orders/Tables/Kitchen tiles on `permissionModule:'pos'` not `Menu` → Orders tile is now a dead link for retail (bounces to /dashboard); kitchen already was. (v) `RequireModule.php:62-63` `abort(403)` = plain Laravel 403, not the envelope (repo-wide idiom). (vi) 5 PRE-EXISTING PG reds in `PosStabilizationTenantIsolationTest` (`shift_number` string into integer column) sit on exactly the 5 order/kitchen cross-tenant probes — that evidence is sqlite-only until fixed; 9 pre-existing PHPStan errors in the same file. (vii) `tests/Architecture/AuthLifecycleTest` pre-existing red at `POS/routes.php:94` (non-literal middleware array), zero delta. (viii) new class pins module-deny but not permission-deny; FE deny path is a source-string assertion, not the behavioural `Sidebar.test.tsx:603-616` idiom. (ix) `OrderStatus` adjacency map / transition service = program workstream A (SM-2/SM-4), not built. | Q-9 gates r1 tenancy/fiscal | OPEN — (i) tenancy lane; (ii)/(iv) web lane; (iii)/(v) envelope lane; (vi) test-fixture lane | Orchestrator |
| C-20 | **Q-13 tables-gate residuals (merged `0a3b629ff`)**: (i) `OrderManagementService.php:158,446-452,510-516` still writes table state behind the `Menu` gate, not `Tables` (inert today; a Menu-without-Tables tenant — coffee_shop default — can reach table-state writes through order routes) → fold into the tables/menu follow-up. (ii) allow-side probes cover only 2/10 tables routes; no `runFullSync` case for a non-Tables tenant. (iii) `logSyncOperation` status is binary (`syncLogRepository.ts:9`) — a deliberate skip is logged as `success`+detail; a `skipped` tristate is a device migration with no beneficiary today. (iv) cross-describe `productStore` state leak in `apps/pos` sync tests (`vi.clearAllMocks` does not drain `mockResolvedValueOnce` queues) — latent fragility for any future pull-phase module gate. (v) Q-9 C-19 (i) is CLOSED by this lane. | Q-13 tenancy gate r1 | OPEN — (i) small POS lane; (ii)-(iv) test-hygiene | Orchestrator |
| C-17 | **N-2 residuals**: (i) 5 further `firstOrFail()` on `StockLevel` in reservation release/fulfil paths (`StockReservationService.php:111-114` + census M-5 in handback) — reachable only after a reservation exists, so not day-one, but same 404 shape; (ii) `lang/ar/documents.php` absent — the new `documents.stock.insufficient` key has EN+FR only (AR namespace gap pre-exists); (iii) gate observations O-1/O-2/O-3 pre-existing on dev (see gate record §obs); (iv) M-6 not taken (rationale in handback). | N-2 gate r1 + handback (`docs/superpowers/reviews/2026-08-24-n2-*.md`) | OPEN — (i) micro-lane with the reservation hardening; (ii) rides the AR-locale lane | Orchestrator |
| C-21 | **N-1 residuals**: (i) composite items carry the same sealed-wrong-rate defect (`CompositeItemController.php:93-94`, hash-chained arm) — own lane; (ii) the unposted-docs worklist is REPORT-ONLY: the 7 campaign lines (4 docs) stay at 19 % until the operator re-picks the product; confirmed invoices additionally need `vat:backfill-tax-details --apply` because no update path refreshes `document_tax_details` (treasury CRITICAL 3 survives onto the operator — pinned in the migration log line); (iii) no per-rate 4457x sub-accounts anywhere (pre-existing, informational); (iv) worklist override-predicate false positives (deliberately not narrowed). | N-1 gates r1–r3 + treasury r1 (`docs/superpowers/reviews/2026-08-24-n1-vat-gate-*.md`) | OPEN — (i) lane; (ii) deploy checklist step; (iii)/(iv) backlog | Orchestrator + Owner (deploy step) |
| C-19 | **FE-contract residuals**: (i) rule-7 DTO family for opening-batch preview + VAT summary/period payloads (≈11 classes, 3 modules) so `typescript:transform` owns the FE types — drift is pinned by 2 contract tests meanwhile; (ii) server-authored English strings shown to fr tenants on those pages (needs an API contract change); (iii) repo-wide missing plural forms (pre-existing); (iv) missing OBE offset in the ACCOUNTING preview + N-8 GL currency (campaign P2s); (v) inherited red `FranceTaxConfigurationSeederTest:30`. | FE-contract gate r1 + handback (`docs/superpowers/reviews/2026-08-24-fe-contract-*.md`) | OPEN — (i) post-launch lane; (iv) P2 tail wave | Orchestrator |
| C-22 | **Credit note reachable at `status=paid` FORKS its fiscal chain (Session C research find, live on dev)**: `canTransitionToPaid()` includes CreditNote (`DocumentType.php:123-130`); `PaymentController::store()` `allocations.*.document_id` is a bare `ScopedExists` with no type/status predicate (`:401-405`) so a posted CN id reaches the Paid write at `:1023-1024`; chain-head read (`DocumentPostingService.php:462-469`) and verifier (`VerifyFiscalChainsCommand.php:279-283`) key on `status=Posted` ⇒ the next CN chains onto an older head and verification breaks; money booked as cash IN. Also: applied CN cancellable (`hasBlockingAllocations` asks `credit_note_allocations.invoice_id` `:312-315`, always empty ⇒ orphan allocation + phantom `balance_due` cut). Supplier side: supplier payments persisted `PaymentType::DocumentPayment` (`PaymentController:889-891`) so the `SupplierPayment` reversal refusal (`PaymentType.php:186`) never fires. | `docs/sessions/session-C-lifecycle-2026-08-24/RESEARCH-R-returns.md` §2, `RESEARCH-P-purchases.md` §11 OQ-2 | OPEN — N-6 gate (Session A) must confirm the classifier refuses CN allocations (422 `DOCUMENT_NOT_ALLOCATABLE`) and that chain-head/verifier are keyed on SEALED, else Session C lane C-2b closes it; supplier-payment typing → Session B Q-12 census | Session A (N-6 gate) · Session C (C-2b) · Session B (Q-12) |
| C-1 | **Audited duplicate receipt PDF vs register disagree on a legacy return's total** — `ReceiptPdfService.php:132-141` renders the raw (negative) stored total while `ReceiptDetailData` emits the magnitude and relabels the type REFUND; accountant-visible discrepancy between the fiscal document and the register on migrated pre-v4 returns. Surfaced by the receipts wave's own M2-round4 self-flag; carried here at merge per terminal-audit fiscal P3-7 (2026-08-18). | `.worktrees/receipts-build/docs/handoff/reviews/receipts-build/M2-round4.md:26-28`; terminal-audit fiscal register (parent session 2026-08-18) | OPEN — fold into the next receipts follow-up lane or the PDF-surface lane | Orchestrator |
| C-23 | **W2-3 residuals**: (i) `CompositeItemImportService.php:52-61` same silent category drop + rule-6 direct `Category` import at `:12` (one-line fix = call `resolveCategoryByName`); (ii) FE import result page does not declare/render row warnings (`category_*` codes exist in the workbook only); (iii) W2-5 blank tax selector on imported products — import's pre-set `tax_rate` means `category_id` still never feeds `getDefaultTaxForNewProduct`. | W2-3 gate r1 + handback (`docs/superpowers/reviews/2026-08-24-w23-*.md`) | (i) OPEN — micro-lane; (ii) OPEN — P2 tail; (iii) FIXED-MERGED 2026-08-26 — import path only: imported products now inherit the category tax rate + `default_tax_configuration_id` with a coherence guard evaluated on every product write, T9 batch merged `16088fdb7` (see D-T9-5, D-T9-9). The manual `PATCH /products` path is still ungated (D-T9-5) | Orchestrator |
| C-24 | **Q-10 fiscal-period residuals (merged `f5cae1f12`)**: (i) **NO manual single-period close endpoint exists** — and since the scheduler now skips reopened periods AND holds their fiscal YEAR open, a reopened period keeps its year open indefinitely until a human close lands (deliberate: an un-closed period is recoverable, a wrongly-Locked one is not) → build `POST /fiscal-periods/{id}/close` (permissioned, stamps `closed_by`, clears nothing) as the next Company lane. (ii) `CountryFiscalRulesProvider` is hardcoded PHP — no seeded fiscal-window settings table exists (CLAUDE.md country-accounting rule not yet met; TN=FR=1 month so the per-country window VALUE is unpinned). (iii) no FE surface calls `reopen` (permission + map exist; no page/i18n). (iv) reopen emits `Log::info` only, no domain event (sibling `VatPeriodFiled` does); audit columns are last-transition-only. (v) sibling VAT reopen route `Taxation/routes.php:111` lacks `whereUuid`. (vi) fiscal years have no reopen edge; `closeFiscalYears` is one-way. (vii) `fiscal_periods.status` CHECK = Slice D; `PeriodStatus` adjacency map = program. | Q-10 gates r1/r2 + fix round `8a285c495` | **(i) CLOSED — `POST /fiscal-periods/{id}/close` MERGED local dev `bd03bd8cb` (2026-08-25, Session B2 B2-5; treasury r1 ACCEPT-w/-cond `2026-08-25-sb2-c24-period-close-gate-r1-treasury.md`; permission `fiscal-periods.close` → S-20; residuals C-42)**; (ii) settings lane; (vii) Slice D | Orchestrator |
| C-25 | **N-6 Phase 1 residuals → Session C program**: [2026-08-25 DPA follow-up merged: compensating DELETEs replaced by savepoint containment; residual: lock-retention docblock at `GeneralLedgerService.php:1912-1919` false on the failure path (locks release with the rolled-back savepoint) — comment fix; `InvoiceController.php:739-741` 422-vs-500 mapping gap pre-existing]; R-14 eleven unrouted non-birth status writers (RefundService, DN/RN seal writes, 5 confirm sites) — Phase 2 single write path for ALL types; R-15/OQ-3 partial refund of an advance-backed payment refuses (proration not invented) — Phase 2 payment dimension; R-16 nine inherited `TreasuryDepositBridgeTest` reds (arity 3 vs 4, pre-existing on dev); R2-M2 Document→Treasury `PaymentAllocation` reach at `DocumentPostingService.php:204/250/280/489` → Shared contract; R-4 Arabic PDF glyph shaping on the print marker; converter clear-at-conversion timing (OQ-48); `paid` still a lifecycle value until Phase 2 removes it. | N-6 gates r1–r3 + handback (`docs/superpowers/reviews/2026-08-24-n6-*.md`) | OPEN — Session C Phases 2/3 | Session C |
| C-26 | **C-F0 residuals (merged `0dd6caa0e`)**: (i) **FE follow-up lane REQUIRED** — `apps/web` `DocumentTotals.tsx:107-163` still renders per-rate VAT client-side for confirmed-unposted documents; `CreditNoteDetail.tsx:53-55,74-78,92-98` prints VAT DOM, uses a status heuristic, and reads the retired marker copy; `locales/{en,fr,ar}/sales.json` `creditNotes.postingMarker.*` stale — the web detail page now CONTRADICTS the PDF; spec in `2026-08-25-sc-f0-gate-r1-conventions.md` §3.4 (API prerequisite: `is_proforma` from `ProformaOutputPolicy` via `typescript:transform`, never a status heuristic); (ii) R-2 email covering note calls a proforma an "Invoice" + pre-existing `(float)` on money; (iii) R-3 banner follows request locale not company locale (pre-existing split); (iv) R-4/F-C6/R-12 Arabic PDFs: zero `dir=rtl` under `resources/views/documents/`, physical `text-align`, ASCII `-` before ICU-formatted discount — priority raised now that real Arabic text sits in the totals table; (v) R-13 `getDocumentTitle()` en/fr only, no `__()` — Arabic proforma headed "Invoice"; (vi) R-11/F-C7 proforma keeps the brand band; banner is the only visual differentiator (owner: watermark/title?); (vii) R-6 `formatMoney()` float cast (pre-existing); (viii) R-9 intermittent `DocumentPdfRenderTest` error, uncaptured, class runs on no CI event today — capture the message next time; becomes live when `SELF_HOSTED_RUNNER_READY` flips; (ix) R-10 unit×qty drift bound `qty × 0.5 × 10^-scale` accepted (fiscal r3); (x) minor F-C8..C12 (docblocks, 2 keys without literal assertion, mid-line `@if` shape). | handback `2026-08-25-sc-f0-handback.md`; gates fiscal r1/r2/r3 + conventions r1/r2 | OPEN — (i) FE lane (Session A surface — announce in §E before dispatch); (iv)(v) Arabic print lane; (viii) triage | Session C · owner for (vi) | 
| C-27 | **C-QR0a residuals (merged `46130f8f1`)**: (i) staging/promotion: a per-tenant migration abort does NOT fail the deploy (`docker/entrypoint.sh:141-145` swallows the rolling command's exit) and an aborting tenant stops receiving ALL later tenant migrations — read the `tenants:migrate-rolling` log for `C-QR0a cannot apply on this tenant` after every promotion (tenancy gate T-2); local census: 0/13 tenants can hit the abort, 7 stale tenants get the prerequisite first (proven on a clone); (ii) C-QR0b preflight MUST assert `new_columns_present = 4` per tenant via information_schema before its backfill (fiscal F-4/R-2); (iii) the F-101 freeze of `fiscal_authority_status` once sealed is NOT enforced yet — C-3a1a owns the trigger; C-QR0b (the first writer) must not land after C-3a1a is skipped (fiscal F-5); (iv) docs: "four of its six" → "six of its eight" at `ci.yml:997` + manifest note; handback §8 R-1 stale (tenancy T-3/T-5); (v) `generated.d.ts` carried 7 inherited entries from other lanes — expect conflicts with parallel regenerating lanes (T-4); (vi) `CountryDocumentDefaults` still encodes FR's provisional status in a comment (C-QR0b replaces with `policy_expertise_status`). | handback `2026-08-25-sc-qr0a-handback.md`; gates fiscal r1 + tenancy r1 | OPEN — (i) promotion runbook; (ii)(iii)(vi) C-QR0b/C-3a1a briefs; (iv)(v) housekeeping | Session C |
| C-26 | **Slice D preconditions before the FIRST CHECK-adding batch (D-1 gates, merged 2288299bf)**: (i) `EnumCheckParityTest` runs in NO CI job — wire into `treasury-spine-pgsql` (PG service) + `backend-architecture` (S-14 workflow leg); until then the gate is CI-dead and the baseline is honoured only by review. (ii) parser: NOT VALID blindness (F-2) and cross-column value-set CHECKs (F-4) DISCHARGED in fix round 33e26cd69; STILL OPEN **R2-1 (Important)**: an AND-compound CHECK (`status IN (...) AND origin IN (...)`) yields a FALSE COVERED — greedy `(.*)` at `PgValueSetCheckReader.php:145,161` spans `] … ARRAY[`; reproduced live on `payments`; no such CHECK exists on dev today, so fix the reader (split on top-level AND, refuse multi-column) BEFORE batch 1 writes any compound CHECK. **R2-2 (Important)**: the INTENDED_NARROWER acknowledgement is deletable for free (widen the quarantine CHECK + delete its `enum-check-parity-acknowledgements.json` entry in one diff = green) — closed only by the owner pin in (iii). (iii) OWNER (see O-row): arm an owner-pinned protected blob covering ALL THREE artifacts — `enum-check-parity-baseline.json` (189), the central baseline (11) and `enum-check-parity-acknowledgements.json` — so matched growth AND acknowledgement deletion cannot pass. (iv) `fiscal_event_quarantine.integrity_exception_class` NARROWER = INTENDED partition (`IntegrityExceptionClass::isAdmissibleToLedger()`) — never widen; register labels it so. (v) un-gateable by construction → cast/model lane, not CHECK lane: `bank_reconciliations` (no Eloquent model), `fiscal_event_quarantine.payload_parse_status` (no enum cast). (vi) `super_admins.role` (central auth table: no cast, no CHECK) + `migrations/manual/` unscanned — disclosed limits. (vii) Program spec erratum: §#26's 90/76/14 is superseded by D-1's register — **245 tenant enum-governed columns, 54 COVERED / 189 MISSING / 1 INTENDED / 1 COMPOSITE → baseline 189 at the fix round, **188 at merge** (N-6 constrained `documents.status`; ratchet caught it as STALE); central 20 (9 covered, baseline 11)**. Slice D batches burn down from 188 — **batch 1 merged `42f7f8bad`: 175 (C-40)**. | D-1 gates r1 fiscal + tenancy (`docs/superpowers/reviews/2026-08-25-sb-d1-parity-gate-r1-{fiscal,tenancy}.md`) | **(ii) R2-1 + R2-2 CLOSED — MERGED local dev `19f9e61bb` (2026-08-25, Session B2 B2-2, test-only, fiscal-pos r1 ACCEPT-w/-cond `2026-08-25-sb2-c26-parity-preconditions-gate-r1-fiscal.md`)**: parser refuses top-level AND (quote-aware) + `[^\]]*` bodies, planted-AND PG arm; reader side of the owner pin landed (`ENUM_CHECK_PARITY_PROTECTED_SEED` seed COMMIT over all THREE artifacts, mirror `docs/handoff/progress/slice-d-parity.progress.yaml`, fail-closed, 9/9 tamper reds; named-ack rot guard). (iii) → owner steps in `docs/handoff/OWNER-O31-enum-check-parity-pin-2026-08-25.md`. (i) STILL OWED — S-14 leg: the ci.yml hunk in `REPORT-C26-implementer.md` §5 has DEFECTS (C-37) — hand-place, do not `git apply`. Residuals → C-37. | Orchestrator / Owner |
| C-27 | **🚨 `journal_entries.entry_number` scope mismatch — a SECOND company in a tenant cannot post ANY GL entry (found by Q-11's collision test, 2026-08-25; REPRODUCED on PG by the Q-11 treasury gate: company B's expense post 500s on `journal_entries_tenant_id_entry_number_unique` `JE-2026-000001`; ALL 43 GL-minting paths affected; `IncomeService.php:206-224` carries the IDENTICAL bug)**: unique index is `(tenant_id, entry_number)` (`2025_11_30_100000_create_journal_entries_table.php:41`) but `GeneralLedgerService::generateEntryNumber` scans and advisory-locks per COMPANY (`GeneralLedgerService.php:5339-5356`) → company B's first JE collides with company A's `JE-2026-000001` → 23505/500 on every JE-minting flow (expense post, invoice post, payment, counting apply…). Single-company first tenant: NOT hit; any multi-company tenant: launch-blocking. Fix shape = exactly Q-11's expense fix (`1f76745e8`) applied to BOTH `GeneralLedgerService::generateEntryNumber` and `IncomeService` — tenant lock taken BEFORE the company chain lock (`:3742` ordering); 4 census queries in the gate record: tenant-keyed `pg_advisory_xact_lock` + tenant-scoped max+1, both call sites inside the txn. Census for existing rows: `SELECT tenant_id, entry_number, COUNT(*) FROM journal_entries GROUP BY 1,2 HAVING COUNT(*)>1;` (must be empty — the index already enforces it, so live data cannot be wrong; the defect is write-time refusal, not corruption). | Q-11 implementer + treasury gate r1 | **FIXED for JE + INC — MERGED local dev `0613e2ce8` (2026-08-25, Session B2 B2-1, CI-UNVERIFIED S-17)**: `GeneralLedgerService::generateEntryNumber(tenantId, companyId)` tenant-scoped scan + `journal_entry_number:{tenant}` xact lock taken BEFORE the company chain key on every mint AND seal path (`takeTenantNumberingLock()` also called in `sealAndPersistEntry` — gate r1 F-1 AB-BA reproduced 40P01, closed r2); `IncomeService` + `JournalEntryController` same shape; 43 sites pass entity `tenant_id`, zero context lookups; gates: stock-gl r1 ACCEPT-w/-cond → r2 ACCEPT-w/-cond, treasury r1 ACCEPT-w/-cond (`2026-08-25-sb2-c27-*`). Row stays OPEN for the siblings + owner items in C-34 / O-32 / S-21. | Orchestrator → Owner (O-32) |
| C-28 | **Q-11 residuals (merged `0f775b6f7`)**: (i) FE `SupplierInvoiceDetailPage.tsx:353-361` renders the Re-match button unconditionally (sibling Post is `{!isPosted && …}`) — after Q-11 a click on a posted invoice gets an error toast, not silent harm; one-line web fix. (ii) CI reachability: `SupplierInvoiceApiTest` + `ExpensePostTest` are in parked groups and no live allowlist — gate ruling: append ExpensePostTest ONLY (done at merge, B-3 precedent); do NOT append SupplierInvoiceApiTest — 3 inherited PG reds incl. a phantom journal_lines.company_id query + float money format at :1260-1265. (iii) `substr($lastNumber, -6)` wrap-around at 999999 (`ExpenseService.php:~1045`), unchanged. (iv) `documents.match_status` CHECK = Slice D; post-time match-snapshot immutability = program WS-B. (v) the JE numbering defect is C-27 (own lane). | Q-11 treasury gate r1 | OPEN — (i) web lane; (ii) per ruling | Orchestrator |
| C-29 | **W2-6 residuals**: (i) **MAJOR authz** — `line-entry/pricing-context/bulk` returns cost/margins to any `products.view` holder and the editor renders them (`DocumentLineEditor.tsx:391-405,863-869`); needs a product decision on who sees cost, then the same redaction shape as `LineEntryController`; (ii) no supplier-specific purchase price source exists (price lists have no purchase/sale discriminator) — capability gap; (iii) `purchase_price = 0.000` treated as a real price on non-PO surfaces; (iv) `AddQuickProductModal.tsx:224` parseFloat on money (P3). | W2-6 gates r1/r2 + handback (`docs/superpowers/reviews/2026-08-24-w26-*.md`) | OPEN — (i) owner product decision + lane; (ii) backlog | Orchestrator + Owner |
| C-30 | **Q-12 treasury orphan census — residuals + the FK-lane go/no-go input (merged `524e2f477`)**: local census over DS-1's nine bare-uuid columns: 4 directory tenants visited / 0 skipped / **0 orphans across 32 checked column-slots** (`complete: true`, exit 0); 4 slots `target_table_missing` — **`payment_methods.default_journal_id` targets a `journals` table that does not exist anywhere in the repo** (no migration, no model; `PaymentMethodController:90` already documents the removed `exists:journals,id` validator) ⇒ the DS-1 FK lane can ship at most **8 of 9** FKs, the 9th needs an Accounting journals table first. Detector liveness proven (wrong-target probe returns all rows). Local zeros are on a tiny seeded fleet — NOT evidence for staging/prod.. STAGING/PROD: run the command per tenant (`tenants:run treasury:orphan-census --json`) BEFORE the future FK lane; any non-zero orphan class ⇒ that FK is NOT addable until a remediation doc exists (document-per-action). Residuals: (i) **`PaymentMethodController.php:95/:147/:206` accepts and STORES an arbitrary UUID into `payment_methods.default_journal_id` although NO `journals` table exists** (the `exists:journals,id` validator was removed because it 500'd) — a live writer to a phantom target; either drop the column/field or build the Accounting journals table before the FK lane (own small lane). (ii) 3 sibling bare-uuid columns NOT in DS-1's nine and not censused: `payments.location_id`, `payment_instruments.location_id`, `payments.fiscal_event_id` — extend the census before the FK lane decides. (iii) **8 local tenant DBs on PG 5433 with NO row in the central `tenants` directory** (12 in `pg_database` vs 4 in the directory; dev debris) — a directory-less DB is never migrated by `tenants:migrate` nor censused; check staging for the same shape before the FK lane. (vii) test-hygiene hazard: a read-only/coverage pin written as a substring match over `not exists` SQL reads a subquery table name as coverage — always compare the OUTER `from` target (Q-12 C3 probe). (iv) Exit-code contract (gate-ACCEPTED deviation): orphan findings ⇒ exit 0; incomplete coverage (skipped/errored tenant OR empty directory) ⇒ `complete:false` + FAILURE. (v) soft-delete ruling: no `deleted_at` on any of the 9 source/target tables today — no `dangling_soft_deleted` state needed. (viii) N-1 a mistyped `--tenant=` reports `no_tenants_in_directory` (zero-visited check precedes the non-SUCCESS check; fails closed EXIT=1); N-2 with `--tenant=<absent>` the `--json` stdout is not parseable (not-found line precedes the payload). (vi) `ConsoleCommandTenantContextTest` inherited red (12 pre-existing commands; this one is not among them). Local census is near-vacuous (~18 non-null values fleet-wide) — the staging run is the real go/no-go. | Q-12 treasury gate r1 | OPEN — census rides the promotion checklist; FK lane = program | Orchestrator |
| C-31 | **W2-7 residuals**: (i) **P1** a returned COMPOSITE never re-enters stock at all (leaf aggregate + lots stay decremented — consistent, no drift; 0 composites on tenant #1) — must close before the first combo sale; (ii) R6-3 the four provenance pins live only in PARKED CI lanes — no green gate protects the return-provenance fix until the self-hosted runner flips; (iii) R5-10 eleven inherited `ArgumentCountError` tests execute nowhere; (iv) R6-1 spurious `0.0000` ledger leg (balances correct); R6-2 second lock ordering (PG-deadlock-safe today); R6-4 `pos_receipt_line_batch_allocations.quantity` is decimal(10,3) not (N,4); (v) repair command drift report covers phantom rows + delivery-caused drift — `--execute` policy: never unattended, maintenance window only. | W2-7 gates r1–r6 + handback (`docs/superpowers/reviews/2026-08-2{4,5}-w27-*.md`) | OPEN — (i) lane before combos; (ii) runner flip; rest backlog | Orchestrator |
| C-32 | **W4-9 residuals**: (i) `ProvisioningRequiredPurposesV1` AST ratchet stale on dev (2 reds pre-existing) — dedicated regen commit: ADD the 3 `writePosRevenueAndVatLines`/allocator sites, REMOVE 3, re-pin; (ii) no per-category revenue account in the schema — every POS revenue credit hits `ProductRevenue`; (iii) census minors: 'never reached the GL' label, per-leg `gross_base` message, 1:N instrument attribution; (iv) D-1 (device VAT base) and G3-A (zero-tender comps) tracked in the D-1 lane. | W4-9 gates (`docs/superpowers/reviews/2026-08-2{4,5}-w49-*.md`) | OPEN — (i) regen commit; (ii) backlog | Orchestrator |
| C-33 | **W4-6 residuals**: (i) R-8 the pre-count half of the ±window now resolves by posting (owner-facing: sales made before the counter reached the shelf are baseline, not neutralised) — rides the P-1 flag flip; (ii) R-12 `LiveCountingSchemaTest` never ran on PG (literal PRAGMA + jsonb key-order assertion); (iii) F-7/F-8 not taken (rationale in handback); (iv) `count_correction_gl_posting_enabled` seeded true by P-1 — expert reviews 6586/7586 at onboarding. | W4-6 gates r1/r2 + handback (`docs/superpowers/reviews/2026-08-25-w46-*.md`) | OPEN — (ii) test-infra micro-lane; rest tracked | Orchestrator |
| C-34 | **C-27 residuals (merged `0613e2ce8`)**: (i) **C-27b** `Procurement/…/UninvoicedDeliveryNoteService.php:516-527,576-588` is a THIRD `JE-` minter — company-scoped, lockless, and with NO prefix filter (`whereYear` + `orderByDesc('entry_number')`, so `'OB-' > 'JE-'` makes it mint off the OB counter); dormant today (zero production callers, AST-enforced at `ProvisioningRequiredPurposesV1.php:78`) — route it through `GeneralLedgerService` or delete before any caller lands. (ii) **Sibling minters still company-scoped against tenant-wide uniques — Session W (openings) territory**: `AccountingOpeningService.php:474-492` (`OB-`), `ArApOpeningService.php:448-472` (`HIST-*`), `Inventory/…/OpeningBalancePostingService.php:290-306` (`INV-OB-`) and `ResetOpeningBalanceService.php:263-272` (`INV-OBR-`) — the two `INV-` ones are stock↔GL exposures (second company's opening 23505s); they also use 32-bit `hashtext` vs the 64-bit `hashtextextended` family used by GL/EXP/INC (unify when touched). (iii) `Document/Domain/Services/DocumentNumberingService.php:47,84` — per-company `DocumentSequence` against the tenant-wide `documents` unique: a second company's `INV-YYYY-0001` collides too; its one-shot retry re-reads the same company sequence. (iv) 999999 wrap: `sprintf('%06d')` + `substr(-6)` restarts silently at 1,000,000 JE/tenant-year (now shared tenant-wide, so N× sooner) — needs an explicit throw; same in `IncomeService`. (v) `InventoryGlVoucherLockOrderTraceTest.php:159-180` (I-1 guard) matches only `bindings[0] === companyId`, so the tenant key is invisible to it — property measured true today (2/30 with the key rewired); extend the assertion in the next inventory-touching lane. (vi) `TreasuryMovementService::transfer()` takes the company key without the tenant key — safe ONLY because `RepositoryTransferService` mints first (comment landed, nothing enforces the cross-class order) — a `withEntryNumberLock()` helper + ordering architecture test would make it structural. (vii) `IncomeNumberingTenantScopeTest` sits in the parked Income lane (runs nowhere in CI — B-3 allowlist precedent applies if wanted). | C-27 gates r1/r2 (stock-gl F-3..F-8, treasury 1-5) | OPEN — (i) Procurement micro-lane; (ii) Session W; (iii)-(vi) Accounting lane; (vii) S-14 | Orchestrator |

| C-35 | **W4-2 zero-float affordance — the precondition for a stricter opening rule (treasury gate r2, R-9 ruling condition (d))**: the opening-balance wizard refuses a cash debit that reaches NO till (`OPENING_CASH_NOT_FULLY_SEEDED`), but it deliberately ACCEPTS a merged `Dr 53 1200.000` row naming only `CASH-01` on a tenant whose drawer AND safe both hang off `53` — drawer ends at 1200.000, safe at 0.000. It is money-neutral (Σ tills on the account == GL Dr on the account, `treasury:reconcile` green — pinned by `test_a_merged_cash_row_over_seeds_one_till_and_is_deliberately_not_refused`) and correctable in-product by a same-GL-account Treasury transfer, which writes no journal entry (PROBE R-9-REMEDY; now in RUNBOOK §1). The stricter rule — one row per repository on every debited cash account — is UNIMPLEMENTABLE today because `AccountingOpeningService::validateRow` rejects a zero-amount row, so an operator cannot declare "SAFE-01 opens empty"; requiring it would hard-refuse a CORRECT greenfield opening whose safe is genuinely empty. **Ask: ship an explicit zero-float declaration** (a `0.000` row naming a repository, or a "this till opens empty" checkbox in the wizard). Once it exists the validator CAN and SHOULD require one row per repository, and the merged-row test must be rewritten to expect a refusal — its docblock says so. | W4-2 treasury gate r2 §RULING R-9 + `docs/superpowers/reviews/2026-08-25-w42-handback.md` §2.1a | OPEN — wizard/UX lane, then tighten the validator | Orchestrator + Owner (operator flow) |
| C-36 | **W4-2 residuals**: (i) G-2 an opening CREDIT on a repository-backed account (e.g. bank overdraft) has no treasury representation and reconcile check 2 cannot see it — unreachable day-one (no repository on 512 yet); (ii) merged row (one Dr 53 line naming one till) silently over-seeds that till — legal by design (R-9), remedy = same-GL Treasury transfer, RUNBOOK'd; zero-float affordance owed; (iii) R-7 `TREASURY_SHIFT_VARIANCE_GL_ENABLED` still false (SV-3 float half closed only); (iv) web lint baseline 6449 vs dev 6450 (+1 inherited, not re-baselined). | W4-2 gates r1/r2 + handback (`docs/superpowers/reviews/2026-08-25-w42-*.md`) | OPEN — (i) openings lane; (ii)/(iii) backlog | Orchestrator |
| C-37 | **C-26 residuals (merged `19f9e61bb`, fiscal-pos gate r1)**: (i) **the reported S-14 ci.yml hunk (`REPORT-C26-implementer.md` §5) is defective — fix before dispatch**: the `backend-architecture` liveness step's `grep -qE 'OK \([1-9][0-9]* test'` FAILS on every sqlite run because the class self-skips one case (`OK, but some tests were skipped!`) — use a pattern that admits the skipped form there (PG arm keeps the strict form as the skip detector); `git apply --check` = corrupt hunk counts; anchors off by one (`:237`, `:289-303`, `:307-311`, `:1214`); DPA pin-tag fetch lives in `backend-dpa-guard`, not `backend-architecture`. Hand-place. (ii) **F-4**: the ceiling compares acknowledgements by `kind` + `predicate` only — rewriting `intended_set` / `enum_cases_at_acknowledgement` / `pinned_set` / `constraint` stays green (proven), so the fiscal quarantine partition is still movable through the production-code door; extend the comparison to the whole entry. (iii) **F-5 authoring rule for every Slice D batch**: `CHECK (c IS NOT NULL AND c IN (…))` now reads MISSING (over-rejection by design) — write nullable columns as `(c IS NULL OR c IN (…))`, single-column value sets only, never compound. (iv) **R2-3 (grown)**: both new pin tests sit behind `EnumCheckParityTest`'s class-wide pgsql skip — when the S-14 leg lands, move the skip per-method and run the ceiling in `backend-architecture` (no `if:`, more events). (v) `]` inside a value literal now reads MISSING (safe direction; none exist). (vi) unchanged from D-1 r2: R2-4, R2-5 (`not_validated` ungated), R2-6, F-9 production half, T-5/T-6/T-7. | C-26 fiscal-pos gate r1 | OPEN — (i)+(iv) S-14 leg; (ii) test lane; (iii) rule for B2-4+ | Orchestrator |
| C-38 | **W2-1 residual**: POS client (`apps/pos/src/lib/api.ts:76`) still attaches `X-Company-Id` to bootstrap calls — harmless now that the server exempts `user.companies`/`auth.me`, but the client should stop sending it (next POS build, ride with D-1). | W2-1 gate r1 + handback | OPEN — next POS build | Orchestrator |
| C-39 | **P-1 residuals**: (i) F-1 D-e detector has no flip watermark — every pre-flip count-correction movement is a permanent finding (census SQL in the gate record); (ii) the ±window has TWO horns: the same-second one is closed (marker) and its ledger consequence withheld when ambiguous; the pre-count horn (R-8) is documented behaviour; (iii) expert-comptable review of 6586/7586 at onboarding (Settings lever, no deploy); (iv) R-9 `requirePerpetual` throws where the chart case fail-softs. | P-1 gate r1 + handback (`docs/superpowers/reviews/2026-08-25-p1-*.md`) | OPEN — (i) census at cutover; (iii) onboarding | Orchestrator + Owner | 
| C-40 | **Slice D batch 1 residuals (merged `42f7f8bad`; baseline 188 → 175, COVERED 55 → 68) + the enum-freeze coupling to §R-D**: (i) **13 enum value sets are now FROZEN in the DB** (`chk_{table}_{column}_enum` on vouchers ×4, journal_entries ×2, payments ×3, documents.type, instrument_events ×3). Adding a case = widening migration; REMOVING a case (spec §R-D3 `JournalEntryStatus::Reversed`, §R-D8 dead `PaymentOrigin`/`PaymentStatus` cases) = narrowing migration + per-tenant census, and the O-31 ceiling forbids baselining it away. `SliceDBatch1EnumFreezeTest` (driver-free) pins the 13 sets + 4 PG defaults and is the ONLY enforcement — `EnumCheckParityTest` cannot see an unmigrated enum change on a fresh schema (treasury C1, proven). Every R-D ruling that touches these enums must budget the migration. (ii) NOT VALID/VALIDATE inside `$withinTransaction` (default) holds ACCESS EXCLUSIVE across census+ADD+VALIDATE — deliberate (atomic abort → zero constraints); on a non-small tenant use a maintenance window or a future `$withinTransaction=false` opt-out (then the abort is no longer atomic). (iii) census throws on the FIRST bad value of the first bad column (aggregate later); the 13 census queries are a hand-transcribed second copy of the value domain (machine-checked at r1). (iv) writer docblock should warn that a symlinked `vendor` collapses the registry population 245 → 2. (v) no PG CI lane runs `tests/Feature/Voucher|Document|POS`; parity gate itself still in NO workflow (C-26(i)). (vi) `ApplyFiscalEventProjectionJobTest` pre-existing PG red (FK + bytea identity; green sqlite) — not this batch. | batch-1 gates fiscal + treasury r1 (`2026-08-25-sb2-d2-slice-d-batch1-gate-r1-{fiscal,treasury}.md`) | OPEN — (i) rides every R-D ruling; (ii)-(iv) batch-2 template; (v) S-14 | Orchestrator / Owner (R-D) |
| C-41 | **D-1 residuals**: (i) **POS BUILD** carrying event_version 5 (+ C-2/C-6 device-Z, W2-7 device residuals, W2-1 header hygiene) — until rollout tills seal v≤4; (ii) the 4 refund pins run in no live CI filter until the 3 inherited `ReceiptReturnRefactorTest` PG reds are fixed; (iii) on-invoice remise has no GL trace under v5 (turnover net) — discount reporting must read sealed `discount_allocated`; (iv) ACCOUNT_CHARGE remise ventilation (currently refused on v5 chains) — own lane; (v) F-5/F-7 minors in the handback; (vi) turnover jumps by the VAT half of every remise at cutover (pinned). | D-1 gates (`docs/superpowers/reviews/2026-08-25-d1-*.md`) | OPEN — (i) build lane; (ii) test-infra; (iv) lane | Orchestrator + Owner (build rollout) |
| C-42 | **C-24(i) manual period close residuals (merged `bd03bd8cb`)**: (i) **I-2** the oldest-first invariant is not lock-protected across rows — `FiscalPeriodCloseService::hasOpenPredecessor()` (`:149`) is an unlocked `exists()` and `lockForUpdate()` covers only the row being closed, so two concurrent closes of adjacent periods can each see the other as settled; same latent shape in the reopen's `hasClosedOrLockedSuccessor()`; fix idiom = per-company `pg_advisory_xact_lock` as at `GeneralLedgerService.php:3796`, in BOTH services. (ii) **I-3** no minimum age on the manual close (scheduler waits `CountryFiscalRulesProvider::periodAutoLockMonths`; a human may close the day after `end_date`) — deliberate (the endpoint finishes a correction) but an unpoliced divergence from the country window; rule when the fiscal-window settings table lands (C-24 ii). (iii) **I-4** a Draft JE inside a manually closed period is blocked immediately (`FiscalPeriodResolverService:290` treats Closed = Locked) and becomes PERMANENTLY unpostable once the nightly run locks the period (reopen then refuses `PERIOD_LOCKED`) — accepted on the Q-10 precedent; a pre-close Draft-JE census/warning is the humane follow-up. (iv) `reason` validated (3..500) + logged, not persisted — no `close_reason` column (additive migration, Slice-D-adjacent). (v) refusal precedence is now CONTRACT (LOCKED → NOT_OPEN → FISCAL_YEAR_CLOSED → NOT_ENDED → PREDECESSOR_OPEN, pinned) — FE remedies must key on `error.code`. (vi) refusal strings literal English (inherited from reopen); no domain event (iv of C-24 stands); no FE page (iii of C-24 stands). (vii) withdrawn: the `FISCAL_YEAR_CLOSED` "dead end" self-heals via nightly STEP 3 (Open→Locked). (viii) ci.yml allowlist append for `FiscalPeriodCloseEndpointTest` (parked Company lane) → S-14 leg. | C-24 treasury gate r1 | OPEN — (i) small Company lane (both services); (ii) with C-24(ii); (iii) program | Orchestrator |
| C-43 | **W4-3 residuals**: (i) the allocation PREVIEW still offers a mis-typed row (predicate not mirrored in `getOpenInvoices()`) — wastes an offer, moves no money; (ii) aged AP blind to non-historical supplier invoices (pre-existing; widening would double-count); (iii) `payment_type` never records `supplier_payment`; (iv) `InvoiceController.php:739-741` 422-vs-500 mapping gap (pre-existing); (v) campaign tenant holds one inverted `HIST-INV-2026-00002` — greenfield, re-provision. | W4-3 gates r1–r3 + handback (`docs/superpowers/reviews/2026-08-25-w43-*.md`) | OPEN — (i) micro-lane; rest backlog | Orchestrator |
| C-44 | **W4R-2 residuals**: (i) **W4R-3 P1 (Menu tenants only)** composite POS sales move NO stock — `flattenMenuToProducts` ids `<sellable_id>_<category_id>` are skipped by `Str::isUuid()`; needs a fiscal-payload ruling + leaf-arm design; 0 composites on tenant #1; (ii) lot drift on the local test tenants (27 units) is forward-only — re-provision; (iii) `ConsoleCommandTenantContextTest` inherited red (12 unrelated commands); (iv) shortfalls in the sale arm are logged + census-visible, never refused (projector contract). | W4R-2 gates r1/r2 + handback (`docs/superpowers/reviews/2026-08-25-w4r2-*.md`) | OPEN — (i) lane before any Menu tenant | Orchestrator |
| C-45 | **Re-check residuals (2026-08-25)**: (i) **W4R2-2 P2** dashboard 'Payments Received' tile over-counts — `PaymentController::store():915-918` never emits `SupplierPayment` and `TreasuryReceiptBridge:1461` tags refunds `POS`, both `isIncoming()==true`; ledger correct, tile wrong; (ii) W4R2-1 P3 over-refund: aggregate credits full while the lot arm caps → drift (census-visible); (iii) `pos:census-vat-legs` must run under `tenants:run` (bare `--tenant` reports a false 'chart not provisioned') — deploy-checklist wording; (iv) W4R-1 P3 opening-batch Lock button always 422s. | `docs/handoff/PLAYWRIGHT-w4r2-w43-recheck-2026-08-25.md` | (i) CLOSED 2026-08-26 — fixed by W4R2-2 (merged `4ae7c68a8`; see D-W4R2-1); (ii)-(iii) OPEN, backlog; (iv) FIXED-MERGED 2026-08-26 — opening-batch Lock step now idempotent (`OpeningBalanceBatchService::lockBatch()` early-returns when the batch is already immutable, before any hash/previous-hash recompute), T9 batch merged `16088fdb7` (task-9-report.md §(a)) | Orchestrator |
| C-46 | **Consolidation residuals (2026-08-25)**: (i) I-5 `PosCoreReceiptProjectionLoyaltyEarnTest` ×3 red on PG (`25P02`), inherited, outside the CI allowlist — test-infra micro-lane; (ii) web lint ratchet +4 (6449→6453) accumulated over the week — re-pin or burn down; (iii) CI must supply `DPA_BASELINE_PROTECTED_BLOB` (guard fails closed without it). | `CONSOLIDATION-2026-08-25-r{1,2,3}.md` | OPEN — backlog | Orchestrator || D-B19-1 | **Expense input VAT is forfeited silently when `tax_amount` is left NULL — VERDICT: not a code defect, an onboarding/UX gap** (Session D, B-19 deliverable 4: "fix if same root cause, else file"). The owner sheet's "149/156 expense documents lack tax-detail rows" was investigated and reproduced: on `tenant019fbe86-…` ALL 7 posted expenses carrying positive VAT DO have their snapshot row (7/7). The 149 without rows are 64 drafts (48 soft-deleted, never posted) + 85 posted expenses whose `tax_amount` is NULL. `ExpenseService::writeDeductibleVatSnapshot()` (`ExpenseService.php:631-633`) early-returns on a null/zero VAT amount by design, after the unconditional stale-row delete at `:620-626`, and is called from BOTH `post()` branches (`:440` generic, `:477` linked-cost). **Different root cause from B-19 — NOT fixed, per the brief.** The residual risk is real though: an operator who omits VAT on a VAT-bearing expense forfeits the deduction with no warning anywhere in the product. Wanted: an onboarding check, and/or a soft warning when an expense with a VAT-registered supplier is posted with no `tax_amount`. | `docs/superpowers/reviews/2026-08-26-b19-handback.md`; gates `2026-08-26-b19-supplier-invoice-vat-gate-r1-{fiscal,treasury}.md` (§7 expense arm) | OPEN — UX/validation micro-lane | Orchestrator + Owner (onboarding) |
| D-B19-2 | **A tenant-configured stamp row with an EMPTY `applicable_document_types` would leak PURCHASE timbre into the SALES stamp-duty declaration line** (Session D, B-19 observation; latent, not live). `TaxConfiguration::scopeForDocumentType()` (`:122-128`) matches any configuration whose `applicable_document_types` is an empty array, and `TunisiaVatStrategy::getSpecialLineItems()` (`:96-109`) counts `is_stamp_duty` rows with NO document-type and NO `deleted_at` filter — so such a row would put purchase timbre into `stamp_duty_count` / `stamp_duty_total`, which is the SALES timbre collected for the state (per PCG-TN a purchase timbre is a non-recoverable class-6 charge that must never enter that line). Verified NOT live: every local tenant's stamp rows are `["TAX_INVOICE"]` / `["FISCAL_RECEIPT"]` / `["CREDIT_NOTE"]`. B-19 closes the PURCHASE half by construction — `PostedLineTaxSnapshotBuilder` derives line tax only and can never emit an `is_stamp_duty` row — but the seeded-config hole and the unfiltered `getSpecialLineItems()` count remain. Adjacent to the already-known `stamp_duty_count` P2 (rows-not-distinct, no `deleted_at`, company-wide vs per-branch). | `docs/superpowers/reviews/2026-08-26-b19-handback.md`; gates `2026-08-26-b19-supplier-invoice-vat-gate-r1-{fiscal,treasury}.md` (§3 builder contract) | OPEN — fold into the `stamp_duty_count` lane | Orchestrator |
| D-B19-3 | **Cancelling a POSTED supplier invoice reverses no GL and drops no VAT snapshot — the deduction keeps being declared** (Session D, B-19; RULED to file, not to fix here). `DocumentPostingService::cancel()` (`:428-433`) takes the non-fiscal branch: `AccountingService::reverseDocumentGl()` returns null for anything that is not an Invoice/CreditNote, and nothing deletes `document_tax_details`. `EloquentVatDataRepository` applies no status predicate, so the row keeps declaring. **Verified the SALES side behaves identically** (a cancelled invoice on the demo tenant still carries its tax-detail row and is still declared — there the GL *is* reversed, so it is a different mechanism with the same symptom), which is why B-19 did NOT diverge for the purchase side alone: the backfill leg was changed to MATCH live behaviour (cancelled-with-a-journal-entry is in scope, cancelled-without-one is not) rather than ship a writer and a backfill that disagree. Direction matters: on a sales invoice the residual row over-states OUTPUT VAT (over-pay, conservative); on a supplier invoice it over-states DEDUCTION (under-pay — the direction a DGI audit penalises). Latent today: there is no cancel route or service caller for supplier invoices (`Document/Presentation/routes.php` exposes cancel for invoices `:224` and credit notes `:269` only). **Must be answered before a supplier-invoice cancel lane ships**, and the answer must cover both families at once. | `docs/superpowers/reviews/2026-08-26-b19-handback.md`; gates `2026-08-26-b19-supplier-invoice-vat-gate-r1-{fiscal,treasury}.md` (§8 item 5); fiscal r1 I-1; treasury r1 F4 | OPEN — gates any supplier-invoice cancel lane | Orchestrator + Owner (policy) |
| D-B19-4 | **A supplier invoice has NO edit, cancel or delete path before posting — so a period-locked one is stranded, and the block is on the AP ledger, not just the declaration** (Session D, B-19 fix round 2; RULED: keep the refusal, make it actionable, file the gap). `Procurement/Presentation/routes.php:84-120` exposes index / duplicate-reference / show / store / match / link-receipts / post — **no update, no PATCH, no delete**. B-19 added a VAT-period guard to `SupplierInvoicePostingService::post()` (correctly: posting now writes the deductible row the TN declaration reads, and `issue_date` is unvalidated operator input, so a back-dated invoice could otherwise rewrite a CLOSED or FILED month silently). But `VatPeriodBackdatingGuard`'s own docblock justifies a bare refusal on the RETURN-NOTE path precisely because *"its refusal is recoverable by a single PATCH of the draft's `document_date`"* — **that premise does not transfer here**. The ordinary AP case (last month's supplier invoice arrives after the month is closed) leaves the document permanently in Draft with no way to correct the date and no way to remove it, and the refusal blocks the whole `Dr 408 / Dr 4456 / Cr 401` recognition — a bookkeeping obligation that exists independently of the VAT declaration. **Mitigated, not closed:** the 422 now names the period (id + bounds + an honest `reopenable`, false for FILED and for CLOSED-with-a-closed/filed-successor because `reopenPeriod()` rejects both) and carries two remedies (`reopen_period` when genuinely reopenable; `recreate_with_open_issue_date` always). The guard also consults `fiscal_periods` Closed/Locked, so any tenant closing accounting months monthly is affected — **release-note item**. The same trap exists for a legacy/imported line with a NULL `tax_rate` (unpostable until the rate is set; the refusal message now says so). **Wanted:** a `document_date` correction path on a Draft supplier invoice, or refusal at CREATE time so the operator is never handed a dead document, or an explicit owner acceptance of the trap. | `docs/superpowers/reviews/2026-08-26-b19-handback.md`; gates `2026-08-26-b19-supplier-invoice-vat-gate-r1-{fiscal,treasury}.md` (§5 period safety); fiscal r2 N-1/N-2; treasury r2 N1/N2 | OPEN — needs an owner ruling on the correction path | Owner + Orchestrator |
| D-N12-1 | **N-12 branch cash-drawer provisioning — deferred minors + owner lines** (Session D, merged `4807f0045` after 2 fix rounds; treasury r3 + fiscal r3 both APPROVED/mergeable). Backfill migration `2026_08_26_100000_backfill_payment_repository_location_n12.php` PROVISIONS a branch drawer for every already-claimed/`pos_enabled` branch location instead of arming a refusal, resolves a NULL terminal location to the default location's drawer (never a company-wide set), censuses ambiguous companies (2+ same-typed candidate repositories → skipped, logged, counted), and a new partial unique index `payment_repositories_one_drawer_per_location_type` refuses a second drawer per location/type with a typed 422 (`LOCATION_DRAWER_ALREADY_EXISTS`) instead of a raw 500. **DEPLOY (rides the promotion carrying this lane):** the migration provisions the drawers; the operator must then run a `RepositoryTransfer` per branch to move the historical balance off the old shared repository — the exact branches are named by the migration's own `drawer-provisioned-transfer-owed` census log line (`default_location_drawer_balances` payload). **Not test-pinned:** no test asserts that census log line or its balances payload exists (gate r3 R2-4 — code-complete, un-pinned; a future refactor of `drawerBalances()` could silently drop it). Cosmetic: `RepositoryWriteRefusal`'s docblock references a Presentation-layer class by backtick note only, not a real import. **Owner lines owed:** (a) confirm the product rule this backfill now enforces in the DB — one active drawer per location/type; (b) no per-branch GL dimension exists yet — branch drawers still post to one shared `gl_account`, acknowledged, not fixed by this lane; (c) a non-cash tender with no settlement repository degrades to a strict PREFERENCE for the drawer, not a hard refusal (day-one tenants own only CASH-01/SAFE-01 — a hard refusal would dead-letter card sales on a tenant with zero settlement repositories) — owner should confirm this is the intended default rather than a refusal. | Session D gates `2026-08-26-n12-branch-drawer-gate-r1-{treasury,fiscal}.md` r1–r3 | OPEN — minor test-pin + owner ack | Orchestrator + Owner |
| D-W4R2-1 | **W4R2-2 dashboard 'Payments Received' tile fix — CLOSES C-45(i)** (Session D, merged `4ae7c68a8` after 2 fix rounds; treasury r2 ALL ADDRESSED). Two migrations retype historical rows so `PaymentController::store()` and `TreasuryReceiptBridge` no longer tag deferred/instrument supplier payments and POS refunds as `document_payment`; `Payment::scopeIncoming()`/`scopeOutgoing()` now derive from `PaymentType::isIncoming()`/`isOutgoing()` instead of a hand-list, closing the double-count the tile was showing. **Owner-ack row (ruled, reversible only by owner say-so): retyping a historical supplier payment off `document_payment` removes its cash-reversal eligibility** (`PaymentReversalRefusalTest.php:222-263` pins the refusal as correct — the old type was a mis-tag) — revert only if the owner wants legacy supplier payments to stay cash-reversible via the generic path. **DEPLOY (rides the promotion carrying this lane): migration `2026_08_25_150000` (widen `chk_payments_payment_type_enum`) THEN `2026_08_25_150100` (retype) MUST run BEFORE the API image rolls** — the widen migration's own docblock carries this window note. Deferred minors: `Payment.php:330` `scopeOutgoing()` now inherits `PaymentType::isOutgoing()`'s non-exhaustive `default => false` match, so a future enum case silently lands outside the outgoing scope with no compile error (the exact drift class this fix removed, half-closed); `PaymentDetailPage.tsx:412-419` "Reverse" button is still unconditional for any `completed` payment (a backfilled supplier payment now surfaces the correct refusal as a 422 toast instead of a disabled control); the `…150100` migration's residual-census comment overstates emptiness (a historical IMMEDIATE supplier payment posted with a NULL `repository_id` legitimately still appears in the census — the census is still the correct catcher). | Session D gate `2026-08-26-w4r2-2-payments-tile-gate-r1-treasury.md` r1–r2 | OPEN — minors backlog; C-45(i) itself CLOSED 2026-08-26 | Orchestrator |
| D-B26-1 | **B2-6 residual sweep — closes C-14(ii)/(iii)/(iv); new minors from the fix diff** (Session D, merged `9d0d08ae5`, inventory r2 APPROVED). `manualOverride()` (C-14(ii), r1) and `activate()`/`activateDraft()` (this round) all now take `lockCounting()` as the first statement of their transaction, guarded `assertNotTerminal()`; the unresolved-items 500 is now a typed 422 (C-14(iii)); `CountingTransitionException`/`CountingUnresolvedItemsException` are backend-translated (C-14(iv)) AND the FE now actually surfaces the message — `queries.ts` `useFinalizeCounting`/`useManualOverride` route through `getErrorMessage()` instead of discarding it into "Request failed with status code 422" (the r1 IMPORTANT-1 finding that left C-14(iv) inert). **New, from this round's own fix diff — not yet closed:** (1) `queries.ts:131` `useActivateCounting`'s `onError` still reads raw `error.message` — the fix round's own new `activate`/`activateDraft` refusal (typed above) is invisible on this one handler, a one-line `getErrorMessage()` fix; (2) narrow deadlock risk — `activateDraft()`'s new header row lock (`:627`) is taken BEFORE the company-scoped numbering-range lock in `generateCountingNumber()` (`:1498-1502`); (3) post-lock writes (`$counting->save()`, item generation, `transitionTo()`) still go through the caller's stale `$counting` handle rather than the locked one — a concurrent non-terminal `activateDraft()` can still mint a duplicate `counting_number` (caught as a 23505, not corrupted, but not closed); (4) `InventoryCountingController::activate()` still 500s on a fresh-loaded terminal counting (no pre-check mirroring `activateDraft()`'s 422); (5) the original five-path lock sentinel (`CountingTerminalStateGuardTest.php:761-771`) is still presence-only while its failure message claims ordering; (6) `CountingItemController::setOpeningCost()` (`:233`) remains check-then-act, untyped and unlocked — explicitly out of this round's scope, sibling of the paths just fixed. | Session D gate `2026-08-26-sb2-b26-residual-sweep-gate-r2-inventory.md` (§ADMIN, §3, §4); r1 `2026-08-25-sb2-b26-residual-sweep-gate-r1-inventory.md` | OPEN — minors backlog | Orchestrator |
| D-CF0W-1 | **C-F0w web proforma-parity lane — rulings + open tickets** (Session D, merged `9d0d08ae5` then `8664d180c`; fiscal r1 clean, conventions r2 ACCEPT). Two rulings from the gate: **W-4** — settlement surfaces (`DocumentOutstandingCallout`, payments tab, `CloseWithWriteoffSection`) on a not-yet-sealed invoice are KEPT, not a fiscal defect — settling an unsealed invoice is a first-class supported flow, every figure shown is GROSS with no rate beside it so nothing is VAT-derivable, and the proforma totals box (customer-facing PDF mirror) and the rest of the page (internal working surface) are correctly different things; a docblock note recording this split at `DocumentTotals.tsx:91-106` is still owed. **W-8** — `isProformaOutput()`'s predicate is CORRECT as shipped (a DRAFT invoice renders as a proforma too, by design — narrowing it would reopen the paid-never-sealed / posted-never-sealed holes the lane exists to close); the "staff can't check VAT before posting" concern is real but is served by the `/sales/invoices/:id/edit` authoring surface (`DocumentLineEditor` shows per-line Tax % and a bcmath-computed Tax total), which is a different surface than §2.4 governs. Recommendation to the spec owner (not applied in this lane): narrow §2.4's wording from "no `apps/web` surface" to "no read/rendering surface" and record the edit screen as the sanctioned pre-posting VAT check. **Ticket N7** (`docs/superpowers/tickets/2026-08-05-l4-web-followups.md`) — F-6 accepted deferral: document-scale server tax figures vs. company-scale client formatting is a closure risk, plus the `CreditNoteDetail.tsx` divergence; needs its own lane (two-currency viewport). **W-7 owed**: `CreditNoteDetail` is unreachable (unmounted) with a softened docblock saying so, but the "mount-or-delete" ticket itself was never filed (`grep -rn "mount-or-delete" docs/superpowers/tickets/` = nothing) — still needs filing. Minors: both `is_proforma` docblocks (`types/document.ts:98`, `types/creditNote.ts:88`) still instruct `=== true` while the two live fiscal pages now correctly use `!== false` (a reader following the docblock reopens the F-3 hole this round closed) — needs reconciling to "fiscal READ surface: `!== false`; elsewhere: `=== true`"; no handback paragraph exists for fix commit `088cb9d74`. Carried, unchanged: F-4 (test docblock), F-5 (un-mock one settlement component / scope the "whole page" claim), F-7 (fr/ar rendering scan), F-8 (VAT-derivable credit-note modal), F-9 (docblock-only import), and S-14's promotion-leg gap (`backend-test-pgsql` does not run on `push→dev`). | Session D gates `2026-08-26-sc-f0w-gate-r1-fiscal.md`, `2026-08-26-sc-f0w-gate-r2-conventions.md` | OPEN — minors + two tickets (§2.4 wording, mount-or-delete) | Orchestrator + Owner (§2.4 wording) |
| D-B13-1 | **B-13-S1 (structural, P2, owner-ruling required) — POS device credentials are owner-scoped: the PIN gate is an operational control, not a security boundary.** An IziPOS/Otospex terminal is signed in once with a back-office account — in the ordinary single-account deployment the owner's — and its Sanctum bearer token is then held by the physical device for as long as it stays bound. Every POS endpoint authorizes `$request->user()`, i.e. that token's user: the X report and Z history on `pos.view_reports` (`ReportController.php:59, 218, 261, 327, 366, 424`), POS analytics (`AnalyticsController.php:29-145`), shift open/close on `pos.manage_shifts` (`ShiftController.php:49, 106`), cash-drawer deposit/payout on `pos.operate_terminal` (`CashDrawerController.php:42, 111`), and the PIN roster itself (`PosAuthController.php:172`). **The PIN operator identity is never transmitted on any of them** (`apps/pos/src/lib/api.ts` sends only `X-Client-Type`, `Authorization: Bearer`, `X-Company-Id`). Consequence: anyone with physical access to a bound terminal can `curl` every endpoint the owner can reach — including non-POS tenant endpoints — regardless of which PIN is active or whether any PIN is active. B-13 (merged) closes the *UI* leak by composing the manager gate from the PIN operator only (`apps/pos/src/lib/auth/roles.ts:51-53`); it does not and cannot close this, and the first-run `setupPin` path (`operatorStore.ts:329-361`) additionally lets a virgin terminal mint a PIN carrying the login account's roles. Closing it requires per-operator device credentials — PIN verification exchanging the provisioning token for a server-issued, operator-scoped, short-TTL token that POS endpoints authorize instead — plus a decision on what the device may do while offline with no live token. Program-level; blocks nothing today; must be a conscious owner acceptance before tenant #1 hands a terminal to non-owner staff. The offline TTL residual this row originally named is now IMPLEMENTED (`lib/auth/operatorAuthorityFreshness.ts`, 7 days, clocked off `sync_metadata.operators_last_sync`); the cash-drawer server-side permission gap remains open — see D-B13-4. | Session D task-7-report.md ("LEDGER rows to file", Row 1); gates `2026-08-26-b13-pos-manager-gate-gate-r1-authz.md` r1–r3 | OPEN — program-level, needs an owner ruling before tenant #1 hands a terminal to non-owner staff | Owner + Orchestrator |
| D-B13-2 | **B-13 (structural, carried): the POS manager gate is a device-side operational control, not a security boundary.** The PIN operator identity is never transmitted (`apps/pos/src/lib/api.ts` sends only `X-Client-Type` / `Authorization: Bearer` / `X-Company-Id`); every POS report endpoint authorizes the **device token user** (`ReportController`, `AnalyticsController`). On the ordinary single-account deployment that token is the **owner's**, so anyone with physical access to the terminal reaches every endpoint the owner can reach, PIN or no PIN. Closing this requires per-operator device tokens (PIN verification issuing a server-side scoped token) — program-level, out of B-13. Accepted for tenant #1 because (a) `cashier` lacks `pos.view_reports`, so a correctly-provisioned cashier terminal is server-refused, and (b) for `fiscal_schema_version >= 3` the server X path is retired and the device builder is the primary path. **Two residuals ride on this row:** (1) blind-count concealment is now ALSO applied to `/sales` (this round closed the gap where its per-receipt total + tender label bypassed concealment on the open shift) — what remains is the owner headline/reprint call, see D-B13-3; (2) POS cash-drawer deposit/payout authorize `pos.operate_terminal`, which **cashiers hold** — manager-only there is client-side only, see D-B13-4. **Amendment — accountant read parity (needs owner confirmation):** `pos.view_reports` is seeded to both `manager` AND `accountant` (`RolesAndPermissionsSeeder.php:619,837`), and the PIN-holder check applies no role filter, so an accountant holding a till PIN now clears the POS READ surfaces (X report, Z history, `/reports`, `/shift`, opening-float tooltip) — deliberate parity with what the server already authorizes them for server-side, NOT extended to anything destructive (terminal lifecycle keys on `pos.manage_terminals`, the cash drawer on `pos.approve_cash_drawer_control`, neither of which accountant holds). The code implements this ruling; whether an accountant with a till PIN *should* read POS reports on the device is a product call the seeder already answered server-side and the device now matches — owner should confirm the read parity is intended. | Session D task-7-report.md ("LEDGER rows to file", Row 2 + round-2 amendments); gates `2026-08-26-b13-pos-manager-gate-gate-r1-{fiscal,authz}.md` r1–r3 | OPEN — accountant read-parity ack owed | Owner + Orchestrator |
| D-B13-3 | **Combined owner product call — POS blind-count concealment is re-derivable on three surfaces at once** (Session D, B-13). Must be put to the owner as ONE item, not surface by surface: (1) `/reports` — the net headline plus visible non-cash rows still reconcile exactly, so the concealed cash is re-derivable; (2) the X report — same shape, `Σ(tender rows) === gross_sales − refunds_amount` by construction, and the headline cards stay visible; (3) `/sales` — the money is concealed now (this round), but *reprint* still reaches every per-receipt total one document at a time. Closing any of them fully means concealing the headline during trading hours, which makes the reports useless for what they are for — the three surfaces are deliberately kept consistent with each other rather than one going quietly further than the others. | Session D task-7-report.md ("Combined owner product call — residual (i)") | OPEN — owner product decision | Owner |
| D-B13-4 | **`CashDrawerController` deposit/payout authorize `pos.operate_terminal`, which `cashier` holds** (`RolesAndPermissionsSeeder.php:685`) — the manager-only cash-drawer gate B-13 built is **client-side only**; the server accepts the request from any role holding that permission, cashier included. Reported by both the authz and fiscal B-13 gates, not changed in this lane (rule 4 — reported, not silently fixed). Closing it needs either a dedicated `pos.manage_cash_drawer_operations`-class permission scoped to manager+ or a server-side manager-PIN re-check on the two endpoints. | Session D task-7-report.md (Row 2 residual (2), round-2 amendment 3); gates `2026-08-26-b13-pos-manager-gate-gate-r1-{fiscal,authz}.md` | OPEN | Orchestrator + Owner |
| D-O30-1 | **Post-release `SESSION_OPEN` orphan unnamed by any audit row** (Session D, O-30 merged `8c8316ab7`) — a `SESSION_OPEN` authored by the dead device but synced AFTER the release commits projects onto the released terminal with no `terminal.released` row naming it (the forced-release audit event only names the shift that was open AT release time). `pos:shift:close-orphaned` correctly REFUSES this population (exit 4, `EXIT_NOT_ORPHANED`) — there is no evidence distinguishing it from an ordinary open shift, and working around the refusal would be exactly the back-door device-less close the command exists to prevent. Runbook §1c gives the detection query (`pos_shifts` OPEN joined to a `pos_terminals` row with `hardware_identifier IS NULL`) and says to escalate — a fresh forced release naming the shift, or an owner ruling — rather than force it. | `docs/handoff/RUNBOOK-orphaned-shift.md` §1c; task-8-report.md Deliverable 4 residual + fix-round-1 concern 5 | OPEN — escalation path only, no code fix exists | Orchestrator + Owner |
| D-O30-2 | **`CASH_CORRECTION` orphans are unclosable (exit 8) pending a direction convention** (Session D, O-30 merged `8c8316ab7`) — `ShiftExpectedCashService`'s v3 derivation signs every recognised `pos_z_session_events` movement type (`CASH_IN` +, `CASH_OUT`/`SAFE_DROP` −), but `CASH_CORRECTION`'s direction lives in the amount's sign, not its type, and it has no device-authoring path today, so there is no observed convention to encode. The service fails closed (`UnsignableCashMovementException` → exit 8) rather than guessing — a dropped movement would export a balanced-looking count that is short by its value. Live dependency: if `CASH_CORRECTION` ever gets a device authoring path, an orphan carrying one becomes unclosable until this ruling lands. | task-8-report.md fix-round-1 §2 + "New concerns" item 3; runbook §2 exit-code table (exit 8) | OPEN — ruling needed only if/when `CASH_CORRECTION` gets a device writer | Owner |
| D-O30-3 | **Legacy refund cash impact is device-only — the v3 expected-cash derivation is short on a pre-v4 terminal carrying `local_refund_records`** (Session D, O-30 merged `8c8316ab7`) — the device nets legacy local refunds into its own expected-cash arithmetic (`zReportService.ts:277-290`), but the server's v3 derivation (`ShiftExpectedCashService`) nets only v4 refunds off `pos_receipts`; a v3 terminal that used the legacy refund path before an O-30 orphan-close would derive a figure too high by the refunded amount. Not reachable for a terminal that never used the legacy path. Flagged, not fixed. | task-8-report.md fix-round-1 "New concerns" item 2 | OPEN — needs a reviewer/owner look before relying on the figure for a pre-v4-migrated terminal | Orchestrator |
| D-O30-4 | **NF525 JET cannot distinguish an administrative orphan-close from a cashier close — this IS the substance of owner ruling O-30(c)** (Session D, O-30 merged `8c8316ab7`) — once `pos:shift:close-orphaned --apply` sets `closed_at`, `Nf525XmlBuilder::addTechnicalEvents()` emits a `FERMETURE_CAISSE` for that shift like any other, and the JET schema has no field for "closed administratively." Provenance survives only in the `shift.orphan_closed` audit row and `pos_shifts.notes` — do not sanitise either. Still strictly better than a shift left OPEN forever with no closure at all. Whether the JET itself needs a marker is O-30(c), which stays OPEN. | `docs/handoff/RUNBOOK-orphaned-shift.md` §3; task-8-report.md Deliverable 5 + concern 3 | OPEN — same item as O-30(c) | Owner |
| D-O30-5 | **Ruling record — orphan close books `variance = 0` with a stated reason; a later device `SESSION_CLOSE` on an already operator-closed shift UPDATES the figures rather than being dropped** (Session D, O-30 fiscal gate r1 ruling, merged `8c8316ab7`) — nobody counted the drawer that left with a lost/stolen device, so no shortage/overage is asserted against a cashier who was never asked to count one (`expected_cash = actual_cash`, `variance = 0`, satisfying `pos_shifts_variance_calc`). If the device later reconnects and syncs its own `SESSION_CLOSE`, `OrphanedShiftDeviceCloseReconciler` REPLACES the operator-derived figures with the device's real counted figures (including a real variance), records the swap as a new audit event `OrphanedShiftDeviceCloseApplied` (`shift.orphan_device_close_applied`, both sides of the swap in the payload) and appends a second marker line to `pos_shifts.notes`. A shift closed by a device is never upgraded (idempotent on its own audit-event guard + the projector's `z_session_events` short-circuit). | fiscal gate r1 (`docs/superpowers/reviews/2026-08-26-o30-orphaned-shift-gate-r1-fiscal.md`); task-8-report.md fix-round-1 §3 | CLOSED (2026-08-26, merged `8c8316ab7`) — ruling implemented and tested | Orchestrator |
| D-W41-1 | **W4-1 rulings (imports gate r1, merged `98a449d14`)**: past expiry is ALLOWED with a warning `expiry_in_past` (not refused); an unparseable date REFUSES the row outright (never silently dropped); a supplied expiry on an existing UNDATED DEFAULT lot is SET ONCE, and a conflicting supply on an already-dated DEFAULT lot warns `expiry_conflict_existing_lot` rather than overwriting it; a supplied expiry on a non-batch-tracked product warns `expiry_ignored_not_batch_tracked`; a supplied expiry with no DEFAULT lot to attach it to warns `expiry_ignored_no_default_lot`; the `is_expired` flag reset the backfill performs on nulled lots is KEPT (reviewer-backed ruling) and its count is emitted in the migration's own census output. | imports gate r1 (`docs/superpowers/reviews/2026-08-26-w4-1-opening-lot-expiry-gate-r1-imports.md`); task-5-report.md §"Findings → resolution" | CLOSED (2026-08-26, merged `98a449d14`) | Orchestrator |
| D-W41-2 | **W4-1 deploy ordering is load-bearing (rides the promotion carrying `98a449d14`)**: `2026_08_26_100000_make_batch_expiry_date_nullable.php` (nullable) MUST run BEFORE `2026_08_26_100100_null_invented_default_lot_expiries.php` (backfill) — filename order guarantees this on a normal `tenants:migrate`, and the backfill self-guards (checks `information_schema`/`PRAGMA table_info`, skips with a warning rather than aborting if it finds the column still `NOT NULL`) — but a partial/manual run applying only the second file is a SILENT no-op, not an error. Every tenant with opening stock must print a `[W4-1] invented DEFAULT-lot expiry census` line (`invented DEFAULT-lot expiries found: N` / a SKIPPED warning if the schema migration hasn't run / `RESIDUAL (not modified): M` if any remain / the resurrection count on a second run) — absence of the line on such a tenant means the guard skipped and needs investigating. | task-5-report.md §5 "Migration predicate + census" + §6 concern 1; `PROMOTION-CHECKLIST-2026-08-26.md` §2 | OPEN — promotion checklist step, watch the per-tenant migrate log | Staging operator |
| D-W41-3 | **W4-1 residuals (merged `98a449d14`)**: (i) `expiry_notices` is a POST-response field only, read back off `mapped_data` and returned once — opening rows have no persisted per-row warnings column, so a durable warnings channel remains a separate lane; (ii) no FE consumer renders `expiry_notices` — the wizard's post step does not display it, only the PRE-post preview badge is operator-visible today; (iii) `ExpiryStatus` has no "unknown" case — a NULL expiry maps to `ExpiryStatus::OK`, so an undated lot shows a green OK chip with no day count (behaviourally correct, visually ambiguous against "expires far away"); adding the case would ripple into the FE status config + POS chips; (iv) i18n baseline re-pin (B-10) owed — `scripts/i18n-baseline-authority.sh` reports 0 new gaps but notes 1 baseline entry now translated (burn-down); the implementer deliberately did NOT regenerate `i18n-completeness-baseline.json` to avoid a repo-wide artifact conflict with parallel lanes. | task-5-report.md §6 concerns 1/2/5, fix-round-2 concerns 1/2 | OPEN — (i)/(ii)/(iii) UI follow-up lane; (iv) rides the B-10 re-pin (`PROMOTION-CHECKLIST-2026-08-26.md` §3) | Orchestrator |
| D-W41-4 | **Process note — W4-1 round-1 CI-allowlist claim was FALSE; corrected in round 2** — the round-1 fix response claimed `ci.yml`'s `backend-test-pgsql` allowlist had been extended; it had not (a stray edit had instead landed on the MAIN worktree's `ci.yml`, confessed and reverted; main tree verified clean). Round 2 actually appends the three classes: `OpeningLotExpiryW41Test`, `NullInventedDefaultLotExpiryMigrationTest`, `SpreadsheetParserDateCellTest`. Recorded so the false round-1 claim is never mistaken for history. | task-5-report.md "⚠ Correction: round 1 reported a change that did not exist"; W4-1 imports/inventory gate r2 | CLOSED (2026-08-26, merged `98a449d14`, verified in r3) | Orchestrator |
| D-MANIFEST-1 | **Manifest catch-up at the W4-1 merge — POS 156→157, Taxation 32→33, gated union 1193** — merging W4-1 (`98a449d14`) the orchestrator raised the POS manifest ceiling 156→157 (`CloseOrphanedShiftCommandTest`, O-30) and the Taxation ceiling 32→33 (`SupplierInvoiceVatDeclarationTest`, B-19) BECAUSE the O-30 (`8c8316ab7`) and B-19 (`c94d23043`) merges had landed without raising them themselves. Gated union after all three lanes: 1193. | task-5-report.md "Task 5: complete" line (`.superpowers/sdd/PLAN/progress.md`); W4-1 merge commit `98a449d14` | CLOSED (2026-08-26, merged `98a449d14`) | Orchestrator |
| N-9 | **Fresh tenant provisioning seeded ZERO `units`/`unit_categories`** — `products.unit_id` FKs into this table and `units.decimal_places` drives every quantity display/precision the operator sees or types; `UomSeeder` was reachable only from the demo seeders, never from `TenantInitializationService::seedReferenceData()`. | task-9-report.md §(c); authz gate r1 finding 3 (`docs/superpowers/reviews/2026-08-26-session-d-t9-batch-gate-r1-authz.md`); imports gate r1 "(c) N-9" section | FIXED-MERGED 2026-08-26 — `seedUnitsOfMeasure()` added to tenant provisioning + a self-guarding backfill migration (`2026_08_26_100000_seed_base_units_for_unit_less_tenants.php`) for already-provisioned unit-less tenants, both halves demanding BOTH tables empty before seeding, T9 batch merged `16088fdb7`. Residual: see D-T9-3 (pre-promotion census for hand-worked tenants) | Orchestrator (code) / Staging operator (D-T9-3 census) |
| N-14 | **Opening a document form, or an abandoned lineless auto-save, allocated a `document_sequences` number before any document existed** — reproduced live as the wave-4 `PO-2026-0001…0009` orphan drafts ahead of the real `PO-2026-0010`. | task-9-report.md §(d); imports gate r1 "(d) N-14" section + r1 ledger note; residuals ticket `docs/superpowers/tickets/2026-08-23-autosave-residuals.md` (R-2, R-7) | PARTIAL 2026-08-26 — the lineless class (form-open / empty-payload auto-save) is CLOSED: `DraftPersistenceService::saveDraft()` now returns null and burns no number when the payload carries no line, T9 batch merged `16088fdb7`. NOT closed: a draft that reaches exactly one line and is then abandoned still holds its number — that is residual R-2 (defer allocation to confirm/first posted line), ruled a follow-on lane, not implemented (see D-T9-1). Register must not read N-14 as fully closed | Orchestrator |
| D-T9-1 | **R-2 follow-on lane (N-14 residual, explicitly NOT taken in the T9 batch)** — defer document-number allocation from "first line saved" to "confirm / first posted line", so a one-line-then-abandoned draft does not permanently burn a sequence number. Owns the sequence-gap audit story, the "what does the UI show before a number exists" question, and every consumer that assumes a draft's `document_number` is non-null. This is the residual that still reproduces the wave-4 `PO-2026-0001…0009` orphans (one-line-then-abandoned, not lineless — the T9 fix closes only the lineless class). | task-9-report.md §(d) "EXPLICITLY NOT TAKEN"; ticket `docs/superpowers/tickets/2026-08-23-autosave-residuals.md`; imports gate r1 ledger note | OPEN — design lane, not started; ruled a follow-on lane, not a tenant-#1 blocker (sequence gaps are auditable) | Orchestrator |
| D-T9-2 | **Role strip is not an offboarding path — `pos_pin` survives it and the demoted operator stays in the PIN roster.** `DELETE /api/v1/users/{userId}/roles` (`RoleController::removeRole()`, `RoleController.php:390-424`) and the role-sync branch of `UserController::update()` (`:381-389`) leave `pos_pin` set; `pinHolders()` (`PosAuthController.php:46-57`) keys on account status + membership, never role, so a demoted operator keeps appearing in `pin-data`/`has-pins` and can still be selected via `verify-pin`. Not a privilege leak (permissions recompute live, approval scopes empty) but operator identity + terminal access persist. NOT introduced by T9 — C-13(i) as briefed covers deactivate/delete only. | authz gate r1 finding 1 | OPEN — extend the cascade to a role change that drops `pos.operate_terminal`, or rule role-strip is deliberately not offboarding and record it | Orchestrator |
| D-T9-3 | **N-9 backfill's all-or-nothing guard skips the most likely affected tenant.** `2026_08_26_100000_seed_base_units_for_unit_less_tenants.php` runs only when BOTH `units` and `unit_categories` are empty; a tenant that hit N-9 and hand-created one unit via `POST /uom/units` as a workaround is silently excluded forever and stays without the 5 system categories / 19 base units. **DEPLOY: run the migration's docblock census per tenant before promotion** (`SELECT (SELECT COUNT(*) FROM companies) AS companies, (SELECT COUNT(*) FROM units) AS units, (SELECT COUNT(*) FROM unit_categories) AS categories`) and hand-seed any tenant reporting `companies > 0` with a nonzero-but-tiny units/categories count. | authz gate r1 finding 3; task-9-report.md §(c) census block | OPEN — pre-promotion census step, added to PROMOTION-CHECKLIST-2026-08-26.md §2 | Staging operator |
| D-T9-4 | **`DraftController.php:214-219` fabricates a uuid on silent failure, contradicting the N-14 "no ghost draft id" premise.** Answers 200 with `'draft_id' => $draftId ?? Str::uuid()->toString()` on any non-`DraftNotEditableException` throwable; the FE (`useDraftAutoSave.ts:176-181`) only suppresses `onSuccess` for a literal `null`, so the fabricated uuid is still reported to the editor as a saved draft that does not exist. Pre-existing, harmless in data terms (the next save with that id misses the lookup and authors a fresh document), but contradicts the lane's own premise. | authz gate r1 finding 4 | OPEN — Minor, fold into residual R-2/R-5 | Orchestrator |
| D-T9-5 | **`PATCH /products {tax_rate}` can still store a mismatched (rate, configuration) pair — the F-1 coherence ruling holds on the import path only.** `ProductController::applyTaxRateFromConfiguration()` returns early when the caller omits `default_tax_configuration_id` (`ProductController.php:1141-1143`), while `UpdateProductRequest` allows `tax_rate` as `sometimes`; `ProductController.php` is untouched by the entire T9 batch. A manual `PATCH` on a product carrying a 7% configuration can still write `tax_rate=19.00` and leave the stale 7% configuration standing — the exact N-1 shape, reached through the API instead of the import. | imports gate r1 "Deliberately NOT taken in code" #2; imports gate r2 "Scope note" | OPEN — ticketed, out of T9 scope; the F-1 ruling must not be recorded as globally satisfied | Orchestrator |
| D-T9-6 | **`ImportType::Products.tax_rate` has no percent ceiling (CLAUDE.md rule 19), migration-shaped.** `ImportType.php:178` — `'tax_rate' => ['nullable','numeric','min:0','max:100']`, no regex, unlike its `margin` sibling (`:169`, `/^-?\d+(\.\d{1,2})?$/`). Adding the ceiling would make the T9 docblock's "mirrors the FormRequest ceiling" claim true on the import path too, but it turns a file accepted today into a row error — an operator-communication call, not a fix-round one. | imports gate r1 "Deliberately NOT taken in code" #1; imports gate r2 NEW-1 fix note | OPEN — migration-shaped, needs an operator-communication plan before adding the regex | Orchestrator |
| D-T9-7 | **bcmath exponent reach at `ImportService.php:530`, pre-existing.** Passes the file's raw rate into `ProductPriceResolver::resolve()` → `taxFactor()` → `CurrencyScale::bcformatStrict()`, which is `is_numeric`-gated only, so an exponent form (`1e2`) reaches `bcadd()` and throws — caught as a failed *row*, not a dead job (`ImportService.php:349-364` catches `\Throwable` per row). Pre-existing and outside the T9 diff. | imports gate r1 "Deliberately NOT taken in code" #3; imports gate r2 F-6 correction | OPEN — ticketed | Orchestrator |
| D-T9-8 | **`ProductPriceResolverTest::test_tax_default_contract_can_be_faked_without_database` is order-fragile in combined runs, pre-existing.** Appears only in a combined run as a `BindingResolutionException` on Spatie `EventSubscriber` — a Laravel app booted by a sibling test leaks into this plain-`TestCase` file's `new Company`. Green 8/8 in isolation; verified identical on dev with none of the T9 branch applied, so test-ordering pollution, not a T9 regression. | imports gate r2 (round-2) verification table | OPEN — pre-existing test-isolation debt, not a T9 defect | Orchestrator |
| D-T9-9 | **Rulings record (T9 batch, merged `16088fdb7`).** N-9 units are seeded two ways — at fresh-tenant provisioning (`TenantInitializationService::seedReferenceData()` → `seedUnitsOfMeasure()`) and via a self-guarding backfill migration for already-provisioned unit-less tenants (both halves demand BOTH `units`/`unit_categories` empty before running `UomSeeder`, which writes with bare `create()`). W2-5's default-tax ladder is ruled: **category configuration → category `default_tax_rate` → company rate → `'0.00'`**, with a coherence guard evaluated on EVERY product write (not just create) — an existing agreeing configuration is kept, a disagreeing one is re-resolved category→company, else nulled; never a mismatched pair. Unrelated to the same-day W4-1 lot-expiry work (`98a449d14`) — different lane, no shared code path. | task-9-report.md §(b)/(c); imports gate r1 F-1/F-2 fixes + r2/r3 scoped re-reviews; authz gate r1 §(c) | RULED + FIXED-MERGED 2026-08-26 (`16088fdb7`) | Orchestrator |
| D-H0-1 | **Rulings record — B-18 party/contact program, OQ1-9 + acks 1-5 RULED 2026-08-29 (owner: "I agree with all the rulings, 1-9").** OQ1 person attributes = 4 nullable columns on `partners` gated on `party_kind=person` (CIN never required) · OQ2 delete the standalone CRM Contacts surface, fold into the organization detail Contacts tab, keep table+API · OQ3 facture issuance: missing TN matricule = HARD BLOCK for `organization`, exempt for `person` · OQ4 1 receipt → 1 facture at launch, exclusion logic shaped for N:1 later · OQ5 cashier "Demander une facture" at launch, non-fiscal endpoint, Document created server-side · OQ6 nature field labelled **Nature** (Individual/Company · Particulier/Société), role field keeps "Type" · OQ7 sole trader with a matricule = `organization` + `legal_form=personne_physique`; `person` never carries a tax id · OQ8 any cashier, no PIN, behind POS company setting `pos.facture_on_request_enabled` seeded ON for TN/FR, audit trail · OQ9 launch scoping: Phase 1 + Phase 2 (`party_kind`) PRE-launch; Phase 3 (facture on request) first post-launch lane unless tenant #1 confirms daily counter requests; Phases 4-5 after. ACKS: (1) Phase-2 backfill heuristic (suppliers + any B2B datum → organization, rest → person, operator-correctable); (2) AR i18n structurally broken on these screens → fixed inside the program, machine-authored AR keys need native read (B-7 caveat); (3) audit a4 NOT shipped as written → replaced by nature-required-on-create + NULL heuristic; (4) sealed `customer_category:'retail'` literal on ACCOUNT_PAYMENT (`apps/pos/src/lib/fiscal/payloads/AccountPaymentPayload.ts:116`) left byte-stable, routed to the fiscal gate; (5) future marketing CRM standing rule — never a third person table, audiences = read models over partners(person) ∪ contacts, all outbound customer communication via the `Communication` module behind a consent check; Phase 2 folds in reservations R-A..R-D (`docs/handoff/ASSESSMENT-crm-seam-party-model-2026-08-29.md`). Sheet: `docs/sessions/session-H-party-model-2026-08-29/OQ-SHEET-2026-08-29.md` (local). Phase 1 dispatched to Codex Desktop via `docs/handoff/CODEX-DISPATCH-session-H-phase1-2026-08-29.md`. | Owner, 2026-08-29 (Session H) | **CLOSED (2026-08-29, recorded)** — Phases 2-5 entry gate OPEN | Session H writes the Phase 2 brief |
| D-H1-1 | **Session H Phase 1 lane `fix/h1-shape-neutral-cleanup` MERGED local dev `8d629ac89` (2026-08-29, NO migration, NO sealed-byte change — PROMOTABLE).** Scope: a3 `tax_id→vat_number` + generated `PartnerData` consumed (Tax ID column no longer blank; AddPartnerModal persists VAT + named address fields); a4′ Nature required on create (labelled *Nature* / *Particulier·Société*, AR «الطبيعة» = D-5) + B2B block NULL heuristic + decimal-safe `CreditLimitWarning`; a5 `/crm/companies` retired → redirects to `/sales/customers`, partner edit routes gated `partners.update` (was `contacts.update`), create routes `partners.create`, list/detail `partners.view`; a6 route-scoped Type select; a7 Parties import `code` max:50; a8 POS mirror `customer_category:'retail'`→null (device build, not staging deploy); a9 vehicle-owner picker customers-only; merge-gate G1.1–G1.12 incl. create-path duplicate-code 422, code uniqueness aligned to `(company_id, code)` index with `code_held_by_deleted_partner` message, dead Tax-Status/exemption write path HIDDEN (`PHASE2_TAX_STATUS_WRITE_PATH=false`; Phase 2 M2.2 wires it), shadow interfaces removed. Gates: Codex Desktop self-review M1 (codex-fallback), M2 (Opus), M3 (Opus r1-2, codex-fallback r3) → parent re-gate tenancy-authz + frontend-conventions (Claude) ACCEPT-WITH-CONDITIONS, all conditions landed (registers `docs/superpowers/reviews/2026-08-29-session-h-h1-cleanup-merge-gate-r1-*.md`). Evidence: PHPUnit 59/59 on `autoerp_test_h`, PHPStan/Pint clean, manifest exit 0, web+POS typecheck, Vitest 660, ESLint 0 errors, Playwright `e2e/session-h` 10 passed / 1 named Otospex skip (`--workers=1`), react-doctor at dev baseline. **OWES (parent-recorded):** (i) quick-create modal has no Nature field → Phase 2 M3 (`party_kind` on AddPartnerModal); (ii) AR copy review of D-5; (iii) tax-status/exemption write path → Phase 2 M2.2; (iv) FE minors: source-text guard test `sessionHM3BrowserGateSource.test.ts`, Playwright runtime imported into a Vitest helper test, tinker-based e2e credential fixture (not CI-wired), `PartnerData.php:94` unreachable null branch, B2B gate flicker on the last signal, generated.d.ts picked up 5 unrelated stale enums; tenancy minors: `['partner', id]` query-key shape collision via PartnerPicker, POS test fixtures still assert `'retail'`, gate test allow-actor uses a UI alias; (v) lane Playwright specs mutate the demo tenant when run under the default config — keep them out of CI until moved to a seeder-owned fixture. Tester retest lines for staging (F): blank Tax ID column on customers/suppliers lists; Add-partner modal keeps VAT/address; creating a customer as *Fournisseur* no longer vanishes from the list (Type constrained per route); vehicle owner picker shows customers only; Nature required on new partner. | Session H, 2026-08-29 | **CLOSED (2026-08-29, merged local dev `8d629ac89`)** — promotion = tonight's batch (J) | Session J promotes; F retests |
| D-J0-3 | **G-14 (P1, Session G queue; G scoping: new parties → a NEW opening batch importable across files until the L5 lock; duplicate rows against the posted batch → refused at PREVIEW with the count; wizard must not say "completed" when zero documents were created — depends on G-4's outcome model; brief 2026-08-30) — second parties-with-balances import after the first AR/AP opening batch is posted silently drops the balances.** Partners are created, completion panel says `N rows completed with warnings: balance_not_posted`, ZERO historical documents; the `PartiesBalancesPhase` batch_conflict guard (posted batch refuses further rows) surfaces only as a row warning. Baseline (Odoo/ERPNext): opening items importable across several files until the migration lock (L5); refuse only duplicate rows against the posted batch or open a new batch for new parties, as a BLOCKING preview decision (G-4 duplicate-policy shape). Workaround on the 2026-08-30 tester checklist: one parties file per company with all balances. Repro: local campaign tenant `01a04f0f-e9b7-7361-b7b8-9fa5d1335110`. | I-1 campaign run 11 finding L1 (Session I, 2026-08-29) | OPEN — routed to G (G-14), not in tonight's batches | Session G |
| D-J0-4 | **P3 — company creation is only discoverable via the header company dropdown → "Add Company" (`CompanySelector.tsx:113-120` → `/company-onboarding`); `AddCompanyModal` is orphaned (zero usages, flagged by both F2 gates); no Settings→Companies entry.** Not blocking (path verified by F on dev 1e37bc575, role-agnostic; checklist §A step 1 spells it out). Follow-up lane: delete `AddCompanyModal`, add Settings → Company → "Add company" link. | I-1 campaign findings (Session I) + F verification 2026-08-29 | OPEN — P3, unassigned | Orchestrator |
| D-J0-5 | **I-3 follow-up — campaign leg L9 (cash count + Z) is NOT_SCRIPTABLE**: Z-session authoring needs a device-side canonicaliser leg the I-1 wrapper does not cover; declared gap in the I-1 lane report. | Session I I-1 declared gaps | OPEN — brief I-3 after I-2 | Session I |
| D-J0-6 | **I2-F2 — products import never writes `products.unit_id`** (units stored as free text; the campaign's L10 findings gate stays red on it). Same root as G-13 (unit scoping / resolver) and G-4's `products.unit_id` backfill census — both slipped tonight (G-4 gate r1 FAIL). Tester-visible 2026-08-30 as known issue R11 on the checklist. | I-1 campaign finding I2-F2; G-4 gate r1 | OPEN — lands with G-4/G-13 | Session G |
| D-J0-7 | **P0-2 — registration exceeds the request time limit under load and leaves an orphan tenant.** `TenantProvisioningService::provisionForRegistration()` runs CreateDatabase + MigrateDatabase (576 tenant migrations) synchronously in-request (`:118-120`); a PHP time-limit fatal skips `compensate()` (`:231`). Local: 500 after 30 s at load avg 10–11 (campaign 2026-08-29 20:45Z). Staging image ceilings are 60 s on php.ini / php-fpm `request_terminate_timeout` / nginx `fastcgi_read_timeout`. HOTFIX lane `fix/registration-timeout-p0` (Session J, tonight): time-limit seam 300 s + fpm/nginx ceilings 300 s + Throwable/shutdown compensation + tests. **PROPER FIX (architecture, owner call): queued provisioning** — register returns immediately, tenant in `provisioning` state, web shows "preparing your workspace", worker migrates, failure compensates and notifies; also consider a pre-built tenant template DB (Stancl `template`/`CREATE DATABASE … TEMPLATE`) to cut 576 migrations to a copy. | I-1 campaign finding P0-2 (Session I) | OPEN — hotfix tonight; queued-provisioning lane owner-gated | Orchestrator → Owner |
| D-J0-8 | **P1 tenancy leak — `GET /api/v1/payment-repositories` is TENANT-scoped** (`PaymentRepositoryController::index()` :33-47 filters tenant only; `show()` :50-65 filters tenant + company). Company B lists company A's drawers/safes; with G-3c every second company sees both sets — tester-visible on tomorrow's G-3c line. Micro-lane `fix/payrepo-index-company-scope` (Session J, Codex, tonight): company filter on index + audit of other Treasury list endpoints + second-of-everything test. | I-1 campaign gate r1 (Session I) 2026-08-29 | **FIXED-MERGED 2026-08-29 (local dev `1e5ff8c5c`, Codex gate r1 APPROVED)** — rides batch 1b | Orchestrator (Session J) |
| D-J0-9 | **Write-side follow-up to D-J0-8 — `payment_repositories.code` uniqueness VALIDATORS are tenant-only** (`PaymentRepositoryController.php:75-81` store, `:165-173` update) while the DB index is `(company_id, code)` (`2025_12_30_195300_fix_multi_company_unique_constraints.php:31-35`). Effect: a manual repository create/update in company B with a code company A already uses gets a spurious 422; the G-3c provisioner bypasses the validator so day-one CASH-01/SAFE-01 are unaffected. Fix = scope both `Rule::unique` to company. | payrepo gate r1 §4 | OPEN — small, second-of-everything test | Orchestrator |
| D-J0-10 | **`pnpm lint:ratchet` is RED on local dev independent of any single lane** — I-1's frontend reviewer measured dev at 6458 ESLint warnings vs the 6449 baseline (2026-08-29 ~23:30 Tunis). Candidates: H cleanup 8d629ac89 (web partners screens) or G-3b (import wizard). Owner of the raise re-baselines or fixes; CI is blind (S-17) so nothing caught it. | I-1 frontend gate r2 (Session I) | OPEN — morning hygiene, assign after `git bisect`-style lane check | Orchestrator |
| D-J0-11 | **Test-hygiene: running `tests/Unit/Modules/Tenant` (PG-only, `markTestSkipped` in setUp) BEFORE `tests/Feature/Identity/RegistrationResponseIsPureJsonTest.php` in one SQLite process fails the latter with `no such table: permissions`** (RefreshDatabase state after a skipped-in-setUp unit class). Each file passes alone on SQLite and PG. Fix: move the skip to a data provider / `@requires` or make the unit tests driver-agnostic. | Session J verification 2026-08-29 | OPEN — small | Orchestrator |
| D-J0-12 | **deptrac is RED on dev — 185 violations vs baseline 183** (I-2 tenancy reviewer measured the merged tree 2026-08-30; none in DayOne/Census); and the `backend-architecture` CI job has no `if:` guard, so any PR on today's dev shows red. Owner of the +2 (G-6a / G-4 rebase / numbering lane) re-baselines or fixes; pair with D-J0-10 (lint ratchet 6458 vs 6449). | Session I gate r1 on I-2 | OPEN — morning hygiene | Orchestrator |
| D-J0-13 | **Staging census 2026-08-30 05:1xZ (evidence, no action): no `payment_repositories.gl_account_id` NULL on any tenant; the G-3c-created second-company CASH-01/SAFE-01 rows carry GL account + location.** `location_id` NULL only on first-company BANK-*/VIRT-01 (location-less by design) and 019ee4d7's original CASH-01/CASH-02/SAFE-01 (pre-existing S-13 class). The I-2 treasury finding (a NULL-GL drawer would read CLEAN in the day-one census) stands as an I-2 fix item, not a staging defect. | Session J | CLOSED (evidence) | Orchestrator |
| SG-1 | **Session G lane G-7 — import round-trip coverage baseline MERGED local dev `7eb2c0e3a`** (commit `8964e404d`, Codex-implemented). Five `tests/Feature/Import/RoundTrip/*` classes drive the real `POST /imports → /execute` pair per live type (products + placement sub-path, parties full mapped field set, opening balances GL lines, composite items, product-images ZIP) with EU/US fixtures and bccomp-only money assertions; 4 RED-BY-DESIGN pins for later lanes: composite `base_price` rule-19 ceiling (G-8; guard arms on signed or unsigned form) + CompositeItems module entitlement on upload/template/execute (G-3b; live bypass pinned = rule-12 gap on `ImportServiceProvider.php:67` vs `Catalog/Presentation/routes.php:33`). Manifest Import 18→23 / gated 1197→1202; classes added to the pgsql `--filter` allowlist. **Pre-existing defect found, NOT fixed:** `CompositeItem::casts()` (`CompositeItem.php:103-113`) leaves `base_price`/`tax_rate` uncast → SQLite hydrates floats (PG masks) — owned by G-8/G-4. Gates: imports-reviewer r1 CHANGES (3 MAJOR) → fix round → Codex gate r2 PASS (`docs/superpowers/reviews/2026-08-29-g7-coverage-baseline-gate-r1-imports.md`). Program record: spec `docs/superpowers/specs/2026-08-29-imports-hardening-design.md` (r4.1), gap matrix `docs/superpowers/audits/2026-08-29-imports-hardening-gap-matrix.md`. | Session G (`docs/sessions/session-G-imports-hardening-2026-08-29/`) | MERGED local dev 2026-08-29; promotion with the next batch **PROMOTED origin/dev `3ac0de893` 2026-08-29**; staging post-checks (Session F, DB evidence): four `2026_08_30_10*` migrations on 9/9 tenants, company-scoped uniques present 9/9 (products/product_variants/partners), 0 collision groups, all companies see 19 global units (no seed paths fired), API healthy. Pending tester-login checks: 2nd-company import on 019fc714; `units_not_seeded` 422 path. | Session G orchestrator |
| SG-2 | **Session G lane G-12 — units invariant MERGED local dev `b53ccb731`** (lane commit `4f113e923`, Codex-implemented). `UnitsProvisioningService` (one visibility predicate; seeds the canonical set only when `units` AND `unit_categories` are both empty; half-states logged never seeded) called from every company-creation path; tenant migration `2026_08_30_100300_ensure_units_visible_per_company` censuses + seeds fully-empty tenants atomically (never aborts the fleet migrate; local census 8/15 DBs seeded); `ImportErrorCode` created (`units_not_seeded`); `POST /imports` products → coded 422 when the company has no visible units, worker re-verifies. Manifest union Uom 8 / Import 24 / gated 1206. **Residuals:** (i) a tenant whose seed fails logs `units.seed_failed` and is NOT retried by the next `tenants:migrate` (migration recorded as run) — manual seed / company creation repairs; (ii) wizard toasts the generic Axios 422 instead of the backend message → G-1/G-6b; (iii) inherited red `ProcessImportJobStatusTest:218` (red on dev before this lane). Gates: Codex r1 CHANGES → fix 1 → Opus r2 CHANGES (missed handle() caller) → fix 2 → r3 PASS (`docs/superpowers/reviews/2026-08-29-g12-units-invariant-gate-r1-imports-tenancy.md`). | Session G | MERGED local dev 2026-08-29; **migration-bearing — promote only with the explicit promotable line to erp-4f** **PROMOTED origin/dev `3ac0de893` 2026-08-29**; staging post-checks (Session F, DB evidence): four `2026_08_30_10*` migrations on 9/9 tenants, company-scoped uniques present 9/9 (products/product_variants/partners), 0 collision groups, all companies see 19 global units (no seed paths fired), API healthy. Pending tester-login checks: 2nd-company import on 019fc714; `units_not_seeded` 422 path. | Session G orchestrator |
| SG-3 | **Session G lane G-3a — company-scoped SKU / variant SKU / partner VAT uniqueness MERGED local dev `2066e8082`** (lane commit `4ee68be5f`, Codex-implemented; RUL-2). Three census-then-refuse tenant migrations `2026_08_30_1000{00,100,200}` (products (company_id,sku) lifetime; variants live-partial (company_id,sku), barcode untouched; partners (company_id,vat_number) lifetime) — each censuses and THROWS on collision before dropping (local 0/0/0); index-name constant classes; lifetime lookups with coded deleted-holder refusals `sku_held_by_deleted_product:` / `vat_held_by_deleted_partner:`; Product+Partner FormRequests company+lifetime; ProductImages job carries companyId + relative key. Fixes F-BUG-1 §1d (2nd-company import) and pins RUL-2c (2nd-location = stock upsert). Merge-resolution: manifest union ceiling 1212 / Migrations 10 / Catalog 34 / Import 25 / Partner 22; ci.yml filter union + `ProductUpsertKeyPrecedenceTest` (was CI-dark); two G-3a×G-12 fixture interactions + one G-7 ZIP-payload pin fixed inside the merge (assertions strengthened). Verified on merged dev: SQLite 127/0/14, PG 46 passed/5 skipped. Gates: Codex r1 CHANGES → fix 1 → Opus r2 CHANGES → fix 2 → r3 PASS (`docs/superpowers/reviews/2026-08-29-g3a-sku-scope-gate-r1-imports-tenancy.md`). Residual: inherited red `ProductServiceUpsertTest::test_upsert_ignores_nonexistent_category_name` (Unit, W2-3, red on dev before). | Session G | MERGED local dev 2026-08-29; **migration-bearing — promote only with the explicit promotable line; a census refusal on any staging tenant = STOP, read the logged census** **PROMOTED origin/dev `3ac0de893` 2026-08-29**; staging post-checks (Session F, DB evidence): four `2026_08_30_10*` migrations on 9/9 tenants, company-scoped uniques present 9/9 (products/product_variants/partners), 0 collision groups, all companies see 19 global units (no seed paths fired), API healthy. Pending tester-login checks: 2nd-company import on 019fc714; `units_not_seeded` 422 path. | Session G orchestrator |
| SG-4 | **Session G micro-lane G-3c — payment repositories seeded for every company MERGED local dev `50d71e4fa`** (lane commit `9badbe294`, Codex-implemented; closes Session I finding I2-R1-03: a 2nd in-tenant company had no cash register/safe). `PaymentRepositoryProvisioningService` (one implementation; seeder delegates): CASH-01 + SAFE-01 per company at its default location (N-12), idempotent, nested savepoint so provisioning can never roll back company creation, no balance/movement/JE side effect (asserted); called from `CompanyController::store` + registration path; scalar Shared contract (deptrac baseline). Migration `2026_08_30_100800_backfill_company_payment_repositories` (per-company census + backfill of missing day-one codes, failure-contained, idempotent, forward-only, console-only census output). Rule-22 tests (2nd company / 2nd location / re-run / brownfield). Verified on merged dev: SQLite 304/0/3 (incl. whole Company suite), PG 18/105 private DB; manifest ceiling 1214 / Migrations 11 / Treasury 123. Gates: Codex r1 CHANGES (3 MAJOR) → fix round 2 → Codex r2 PASS (`docs/superpowers/reviews/2026-08-29-g3c-company-repositories-gate-r1-treasury-tenancy.md`). Tester retest: create a 2nd company → Treasury shows a cash register + safe at MAIN; a POS shift can open with cash. Per-tenant migrate log: `payment-repositories-seeded company_id=…` for companies that lacked them. | Session G | MERGED local dev 2026-08-30; **migration-bearing (additive, never throws) — promotable** | Session G orchestrator |
| SG-5 | **Session G lane G-3b — import job company pin + CompositeItems entitlement MERGED local dev `0d2ef6a92`** (lane commits `b514c26d6`, `33ab24192`, `069ffab58`; Codex-implemented). M4 `2026_08_30_100200_add_company_to_import_jobs` (additive company_id + source_hash + indexes; evidence-based backfill: company set only when ALL `import_rows.imported_entity_id` links agree, else NULL; never throws; census via `MigrationOutput::info` — lines `import-job-company-attributed|ambiguous|none`). Every job-loading route → 409 `IMPORT_COMPANY_MISMATCH` on company switch; NULL-company jobs adopt-on-execute (atomic with the status flip); `GET /imports` full contract (page/per_page≤100/status/type/q, `{data,meta}`, current company + unattributed flag); `ModuleEntitlementCheck` (RequireModule parity) on all 10 surfaces in both controllers incl. template; wizard status/order hide composite_items when unentitled; G-7 entitlement pins flipped to 403. Verified on merged dev: SQLite 133/0/2 incl. echo guards; PG (private DB) green; manifest 1217 / Import 27 / Migrations 12 / Identity 32. Gates: imports r1 + tenancy r1 CHANGES → fix round (post-merge with dev) → Codex r2 PASS; rebase patch (M4 → MigrationOutput) (`docs/superpowers/reviews/2026-08-29-g3b-company-pin-gate-r1-{imports,tenancy}.md`). Tester retest: 2nd company products import end-to-end; company switch mid-wizard → 409; history shows current company + unattributed badge; company without Composite Items module → 403 on composite tile/template/upload and status/order omit it. FE deferrals to G-6b: composite tile hiding, coded-422 message display. | Session G | MERGED local dev 2026-08-30; **migration-bearing (additive, never throws) — promotable** | Session G orchestrator |
| SG-6 | **Session G lane G-6a — atomic claim, two-clock reaper, purge, cancel/DELETE, M6c MERGED local dev `103605dc6`** (lane commits `5c7fa0693`, `5e601ecae`, `0676c01cc`; Codex-implemented). `ImportJobClaimService`: controller performs ONE compare-and-set claim (pending|validated → importing, `claimed_at`, and G-3b's adopt-on-execute `company_id = COALESCE(company_id, ?)` in the same UPDATE) BEFORE any row read; loser → 409; workers claim-if-unclaimed, `markWorkerStarted` CAS, no-op only on a true duplicate delivery; every terminal write is a CAS `WHERE status='importing'` publishing status + counters atomically; zero-valid-row release to the exact prior status; NO ownership token; one-shot (failed jobs never resumed). Reaper `imports:reap-stuck` (*/5 min, per tenant, `lazyById`) fails jobs whose `claimed_at` (unstarted) or `worker_started_at` is > 90 min old with `error_code=worker_lost`; purge `imports:purge-expired` (daily) deletes source files/exports > 90 days, stamps `source_purged_at` (ProductImages ZIPs purged immediately, relative key end-to-end). `DELETE /imports/{id}` (row discard; only when `status != importing AND successful_rows = 0`, else 409 `IMPORT_HAS_EFFECTS`) and `GET /imports/{id}/source-file` — both gated by the company pin (409) and `ModuleEntitlementCheck` (403). M6c `2026_08_30_100400_add_lifecycle_columns_to_import_jobs` (additive: error_code, error_detail DTO, claimed_at, worker_started_at, source_purged_at, skipped_rows; forward-only; notice via `MigrationOutput`). `ImportErrorCode::worker_lost` added. `tests/Feature/Console/ImportSchedulerRegistrationTest` pins both cron expressions from the in-memory Schedule (SG-6-FU). Verified on merged dev: SQLite 200/0/3 incl. both echo guards; PG (private DB) claim/reaper/purge/pin/opening-batch/registration green incl. the real two-connection single-winner test; manifest union 1223 / Console 16 / Import 30 / Migrations 13. Gates: imports+tenancy r1 CHANGES (BLOCKER worker no-op) → fix 1 → Codex r2 CHANGES (ZIP key) → rebase onto G-3b + patch → Codex r3 CHANGES (3 mechanical) → fix 2 → Codex narrow r4 (only a stray artifact) (`docs/superpowers/reviews/2026-08-29-g6a-claim-reaper-gate-r1-{imports,tenancy}.md`). Residuals: `skipped_rows` stays 0 and status derives from failed rows until G-4's outcome model; branch history carries a 6.8 MB stray SQLite blob (deleted; `.gitignore` added). Tester retest: double-click "start import" → one run + 409; DELETE a pending job → gone, DELETE a completed job → 409 "has effects"; source-file download on a fresh job; a job stuck "importing" > 90 min → failed `worker_lost` (only via logs/DB until G-6b). | Session G | MERGED local dev 2026-08-30; **migration-bearing (additive, never throws) — promotable; post-deploy: `schedule:list` shows `imports:reap-stuck` */5 and `imports:purge-expired` 0 0** | Session G orchestrator |
| SG-5-FU | **G-3b follow-up — 12 COMPLETED import jobs with imported rows stayed `company_id` NULL after the M4 backfill** (staging 2026-08-29 after 0cbaa1457: 019fcbe5 ×6, 01a04d1c ×5, 019fc714 ×1; `null_with_rows` census). Visible effect = "unattributed" badge only (adopt-on-execute cannot touch completed jobs). Follow-up: read those 12 — likely older jobs whose rows predate `imported_entity_id` links or types with no entity link — and, if so, a second-pass backfill keyed on the creating user's company (one guarded migration, never throws). Ruled (a) acceptable for the 2026-08-30 test. | Session J staging census + Session G ruling 2026-08-29 | OPEN — not a blocker | Session G |
| SG-6-FU | **G-6a follow-up — Feature test pinning the scheduler registrations** `imports:reap-stuck` (`*/5 * * * *`) and `imports:purge-expired` (`0 0 * * *`) on the Console Schedule, because the orchestrator has no shell on the staging host to run `schedule:list`. Effect-side staging checks (expect zero rows tonight — silence is consistent, not proof): `SELECT id, status, error_code, claimed_at, worker_started_at FROM import_jobs WHERE status='failed' AND error_code='worker_lost'`; `SELECT id, source_purged_at FROM import_jobs WHERE source_purged_at IS NOT NULL`; `SELECT status, count(*) FROM import_jobs GROUP BY 1` (any `importing` older than 90 min after the first reaper tick = reaper not running). | Session G / J 2026-08-29 | OPEN — lands in G-6a's fix round if gate r3 = CHANGES, else follow-up commit 2026-08-30 | Session G |
| SG-6-FU2 | **Reaper gains a `validating` arm.** Validation runs synchronously inside the upload request, so a killed/timed-out request (same FPM-limit class as P0-2) strands the row in `validating` and the G-6a reaper only watches `importing` — staging 019fcbe5 holds 3 such pre-G-6a rows. Fix: `updated_at` older than N min → `failed` with `error_code = validation_lost`, CAS `WHERE status='validating'`, same scheduler-test pin; goes with G-6b. Interim: those jobs are DELETE-able (non-importing, zero imported rows) via the API today / history page once G-6b lands. | Session J staging census + Session G 2026-08-30 | OPEN — with G-6b | Session G |

---

*Update protocol: edit rows in place; when closing an item, change Status to `CLOSED (date, evidence)` and leave the row for one review cycle before moving it to a "Discharged" appendix. Never delete a row without evidence in the Status cell.*
