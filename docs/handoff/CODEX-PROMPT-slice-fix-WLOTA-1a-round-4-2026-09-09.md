# Codex prompt — W-LOT-A-1a fix round 4 (Codex CLI, read-only, gpt-5.6-sol high; orchestrator saves the reply verbatim as rev 5)

---

DESIGN PRE-APPROVED — non-interactive run: do NOT stop to ask for approval or confirmation; produce the full deliverable now.

Fix round 4 for docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-4.md (rev 4, 3933 lines). Read in full: the gate docs/superpowers/reviews/2026-09-06-w-lot-a-1a-slice-codex-gate-r4.md, and the NEW fact sheet docs/superpowers/plans/2026-09-09-parapharmacy-staging-topology-U1-resolution.md (manifest U-1 is RESOLVED: staging = separate Dokploy applications with exact application IDs and container names; no compose stack; `SYNC_PERMISSIONS_ON_BOOT=true` on the staging API). Produce rev 5. Your sandbox is READ-ONLY: no file writes, no tests, no git commands, no questions. Your FINAL MESSAGE must be the complete rev 5 document and nothing else (no preamble, no summary, no code fence around the whole document); the orchestrator saves it verbatim to docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-5.md.

VERBATIM CONTRACT (the orchestrator enforces it mechanically before gating): rev 4 is the byte-for-byte base. Every subsection you do not need to change for a gate item is reproduced byte-for-byte. Never write "retain the rev-4 …", "as in rev 4", "unchanged from rev 4" or any other by-reference sentence in place of content — the orchestrator diffs rev 5 against rev 4 per `##`/`###` subsection and REJECTS the document if any subsection outside the gate items has removed lines, if any test table is shorter than in rev 4, or if any DTO property loses `readonly` or gains `mixed`. Do not add tasks (Tasks 1/2/6 only); do not change design decisions; do not touch §3 owner rulings.

Close the BLOCKERS:

- B-B2 (deployment topology): U-1 is resolved. REMOVE the compose branch everywhere (the `compose_stack` outcome, conditional Push 6, `docker-compose.staging.yml` edits, `cd` to a compose workdir, the "both outcomes" tables). Rewrite §11.1's U-1 preflight as a confirmation gate against the fact sheet: it reads the three application records through the Dokploy API (`application.one` for `x5wfthp8-7cVbiUfI6Hq7` API, `KKYDsAvk4UpYfJXVmsDj2` worker, `HSXqHvmo_vq7NAYIjrE3T` scheduler; web `mY6P_PHb4pw-2LdG1Y7Ml`), asserts `environmentId=Ree8z_4ixu7vAgBLdI_A_`, `buildType=dockerfile`, the expected `appName` for each, and exits non-zero on any mismatch. Supply the exact executable environment-update, redeploy and readback commands per application: env update = Dokploy tRPC `application.update` with the full `env` text (append `LOT_ACTION_PERMISSIONS_ENFORCE=<value>`; the `saveEnvironment` wrapper is broken per claude/deploy-runbook.md:38-40; API key location per claude/deploy-runbook.md:30), redeploy = `application.redeploy`, readback = `application.one` `env` field AND on the host `docker ps --filter name=<appName> --format '{{.ID}}'` → `docker exec <id> printenv LOT_ACTION_PERMISSIONS_ENFORCE`, compared with `cmp` against a committed heredoc. Assign and persist `PUSH1_SHA`…`PUSH5_SHA` as each push is promoted (host file under `/root/wlota1a/`), and make every later block source that file; every artifact read by a shell must be written by an earlier command in the SAME host shell or transferred with an explicit `scp`/`docker cp` command. Provide executable rollback for the separate-applications topology (env flip to `false` on all three applications + redeploys + aggregate-OFF `cmp`). Update the push inventory (§10), SHA gates, per-slice variable rows, §16 dispatch order and §17 checklist accordingly.
- B-B3 (state crossing destroyed shells/containers; filtered markers): every source push rebuilds and restarts the API, worker and scheduler containers and no container-local file survives. Persist every manifest, expected-tenant file, helper-function definition file and evidence file on the HOST under `/root/wlota1a/`, and for every block that runs inside a container show the explicit `docker cp <host file> <container>:/tmp/...` (or `docker exec … sh -c '…' < host file`) command and the redefinition/`source` of the helpers it uses; no block may reference a variable, function or file defined in an earlier shell without re-sourcing it. Make the before/after web blocks self-contained (redefine `BEFORE`, `AFTER`, `FINGERPRINT_JSON`, the working directory, `set -euo pipefail`) or one continuous shell. Aggregate EVERY raw marker line with the relevant prefix into the aggregate file BEFORE validation (no per-expected-tenant `grep` filter), so an unexpected UUID is visible to `assert_exact_marker_set`. Capture and compare the initial activated registration marker (`mode=ACTIVATED outcome=APPLIED`) with an executable check, not prose. Build the deterministic expected aggregate file for every §11 gate (delta, census, rollout recensus, activation) from the immutable tenant manifest plus committed literal tails, and compare with `cmp`; every gate exits non-zero on mismatch.
- NEW FACT (fact sheet §"Facts that change the slice plans" item 1): `SYNC_PERMISSIONS_ON_BOOT=true` on the staging API means `RolesAndPermissionsSeeder` runs at EVERY API boot (apps/api/docker/entrypoint.sh:157-165). Replace every "prove boot permission synchronization is off" gate (rev 4:2655-2662, §16 step 8, §17) with: record the variable's value from the API application env and prove, with an executable gate, that the seeder is inert for existing tenants while the enforcement flag is off (legacy catalogue, no role/permission delta) and that the explicit Push-4 delta remains the only change to existing tenants; state explicitly that every source push is a seeder run and what that implies for the rollout-window recensus.

Close the MAJOR:
- M-C (generated DTO wire shape): the configured transformer generates PHP property names verbatim (apps/api/config/typescript-transformer.php:34; `emailVerifiedAt` → generated `emailVerifiedAt`). The repository precedent for snake-case wire keys is a DTO with snake_case PHP properties: apps/api/app/Modules/Accounting/Application/DTOs/Reports/TrialBalanceData.php:66-68 → packages/shared/types/generated.d.ts:259-261. Specify ONE strategy and carry it through §8.3/§8.8/§9.2/§9.3/§13/§17: `RoleData` uses snake_case `public readonly` properties that mirror the existing wire keys exactly (`id`, `name`, `guard_name`, `permissions`, `users_count`, `created_at`, `updated_at`, `is_provisioned_read_only`), `RoleController::index()/show()` serialise through `RoleData` (or the DTO's array form) so the wire and the generated type agree, and the web reads `role.is_provisioned_read_only` from the generated type. Add the named backend response-contract test (`RoleIndexResponseContractTest::test_roles_index_wire_keys_are_unchanged_and_is_provisioned_read_only_is_marker_derived`, with first failing assertion, exact PHPUnit command, PG lane) and keep the existing rejection tests and grant snapshots.

Close the MINOR:
- N1: rev 4:256 `RolesAndPermissionsSeeder.php:547` → `:568`; rev 4:259 `:632` → `:653`. Then re-run the SEMANTIC check on every absolute `path:line` citation at the current HEAD (cbcee046a or later): the cited line must contain what the sentence claims, not merely exist.

Add "## 0. Round-4 change log" (renumber the earlier logs 0A/0B/0C) with every gate item CLOSED (plan line/anchor) or REJECTED (citation); update the two header comments to rev 5, fix round 4, 2026-09-09, the audited HEAD SHA, "Rev 4 is the verbatim base", status awaiting gate r5.
