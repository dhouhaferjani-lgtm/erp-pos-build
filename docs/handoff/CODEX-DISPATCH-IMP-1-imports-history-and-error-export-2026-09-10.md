# Codex Desktop dispatch — lane IMP-1: imports history verification + error-line export (paste as a NEW thread named `IMP-1 imports history + error export`)

Repo: `/Users/houssamr/Projects/syneriva/apps/erp`. Create your worktree first: `git worktree add .worktrees/imp1-history-export -b lane/imp1-history-export dev` (base = local `dev`). Work ONLY inside that worktree. PostgreSQL test database for this lane: **`autoerp_test_z`** (set both `DB_DATABASE` and `DB_CENTRAL_DATABASE`; port 5433; check `pg_stat_activity` is empty for it first). Never run the full PHPUnit or Vitest suites — by file only. **Machine note:** the laptop is memory-constrained — run one test file at a time; for the browser leg start ONE API (`php artisan serve --port=8012`) and ONE Vite (`vite.local.config.ts` on a private port, never `:5173`/`:8010`) and stop both when done.

**Why this lane.** The owner reports that exporting the failing lines of a partially failed import returns "the same file you tried to import". The accepted spec already designs the right surface — `docs/superpowers/specs/2026-08-29-imports-hardening-design.md` **§4.10 Error-line export (D4, R1)** and lane **G-1** (`#### G-1 — Error-line export + coded errors`; brief on disk at `docs/sessions/session-G-imports-hardening-2026-08-29/briefs/LANE-G1-error-line-export-coded-errors-BRIEF.md`, gitignored) — but G-1 was never dispatched. Today `FailedRowsExportService` (`apps/api/app/Modules/Import/Services/FailedRowsExportService.php`) emits failed rows with canonical headers and **no status/code/message columns**, excludes warning rows, has no BOM and is unreachable for async jobs; the history page (`apps/web/src/features/import/pages/ImportHistoryPage.tsx:215-240`) offers the **result workbook** (`ResultWorkbookService`: Imported / Skipped / Rejected sheets, an echo of the rows) and, when `failed_rows > 0`, the failed-rows CSV. Benchmark + owner ruling: `docs/superpowers/reviews/2026-09-10-benchmark-import-error-export.md` — **the export is the failing (and warning) rows only, in the operator's own column layout, with appended `_status`, `_code`, `_message`, UTF-8 BOM, re-importable** (spec §4.10 verbatim; not the full file).

Rules: `CLAUDE.md` rules 2 (TDD — Phase A's captured evidence is the red for Phase B), 3, 4 (no scope creep beyond §4.10 + G-1 scope; G-8's `NumberConventionResolver` does NOT exist at HEAD — write CSV in the existing convention (UTF-8 BOM, `,` delimiter, `.` decimal) and file a ticket for the G-8 hookup; do not implement G-8), 6, 7, 9, 11 (every new UI string with en/fr/ar, AR machine-drafted RTL-checked per the session-G ruling), 12, 13, 17, 19 (cells are echoed as stored strings, never reformatted), `docs/conventions/09-SECOND-OF-EVERYTHING.md` (second company + re-run/idempotency on the export and on the `reimport_of` round trip), `docs/conventions/11-ONE-SURFACE-PER-CONCEPT.md` (one export writer: `ImportRowExportService` replaces `FailedRowsExportService`; the result workbook stays a separate "full report" surface).

## Phase A — evidence campaign (commit 1, docs + fixtures only)
1. Build three **products** sample files under `apps/api/tests/Fixtures/Import/imp1/` using the current template (`GET /api/v1/imports/templates/products` or the template route in `ImportServiceProvider.php`): `imp1-success.csv` (10 valid rows), `imp1-hard-fail.csv` (a file that fails outright: e.g. required header missing → 422 at upload, AND a second variant where every row is invalid), `imp1-partial.csv` (12 rows: 8 valid, 3 invalid with distinct reasons — unknown unit, invalid price string, duplicate SKU within the file — and 1 warning-only row). Keep them ≤ 20 rows so they take the synchronous path, plus a fourth `imp1-partial-async.csv` with ≥ 100 rows (mostly valid, 5 invalid) for the async path.
2. Playwright spec `apps/web/e2e/imports/imp1-history-export.spec.ts` (own config `apps/web/e2e/imports/pw.config.ts`, private ports, `testIgnore`d from the default `pnpm test:e2e` like `request-hygiene`): register a fresh tenant/company through the real UI or API, run the four imports through the wizard (`/settings/import/products`), then on `/settings/import/history` assert per job: row present, status badge (completed / failed), the counts (`imported`, `failed`, `warnings`), ordering (newest first), the status filter (`all` / `completed` / `failed`), the empty state on a second company (convention 09), and that the async job appears with its final status after polling. Download BOTH files per job through `authenticatedDownload` (result workbook + failed-rows CSV) and save them under `docs/superpowers/reviews/2026-09-10-imp1-evidence/`; open them and record: sheet names, headers, row counts, whether any status/reason column exists, whether warning rows are present, whether the async job's export is reachable. **Expected RED at HEAD:** no `_status`/`_code`/`_message` columns; warning rows absent; async export unreachable from the wizard. Record every RED verbatim — it is Phase B's first-failing evidence.
3. Evidence file `docs/superpowers/reviews/2026-09-10-imp1-imports-history-export-evidence.md`: one row per assertion → PASS/RED with the artefact path; the exact history-page rendering per job (screenshot paths); the owner's symptom reproduced or refuted with the file diff (uploaded file vs exported file).

## Phase B — implement G-1 (spec §4.10), red-first from Phase A (commits 2..n)
- `ImportRowExportService` (replaces `FailedRowsExportService` incl. its dead `cleanup()`/`getFilePath()`): selection = `is_valid = false OR outcome IN (failed, opening_locked) OR has warnings` via ONE shared portable warning-row scope extracted from `ImportController::countWarningRows()`; columns = the source file's own headers reverse-mapped from `import_jobs.column_mapping` (unmapped source columns dropped, caveat text in the UI); appended `_status`, `_code`, `_message` by the deterministic rule (error outranks warning; first warning in stored order; validation-only failure = `validation_failed` + first validator message); header union across ALL rows; UTF-8 BOM; CSV **and** XLSX; available on async jobs (job-id route, 404 only when nothing to export); mapping injectivity validation on `store` and `updateOptions` (422 `mapping_not_injective`); `reimport_of` on `store` pre-applies the mapping; a code on every remaining refusal path (`internal_error` for raw throwables).
- Web: history page — primary action = **"Download rows to fix"** (the row export, CSV; XLSX secondary) with the reason columns caveat; result workbook demoted to a secondary "Full report" link; wizard completion step links the row export unconditionally (async too); locales en/fr/ar.
- Tests (spec G-1 list, all red-first): the round trip upload → fail → export → re-upload with `reimport_of`; warning rows with `_status=warning`; source headers unioned; BOM + convention; async reachability; `mapping_not_injective`; identical selection on SQLite and PostgreSQL; `ImportRowWarningsTest.php:81` amended; second-company isolation of the export; re-run idempotency (same bytes twice). Feature-lane manifest: raise the `Import` group ceiling and the union with the truthful note, add PG-only classes to the `backend-test-pgsql` allowlist (precedent `ci.yml:1132-1142`), `php tools/feature-lane-manifest-check.php` exit 0.
- Re-run the Phase A Playwright spec at the end: every RED must be GREEN; keep the spec as the lane's browser gate.

Deliverables: the evidence file, the fixtures, the spec, the implementation commits, tickets (G-8 hookup; anything you find in the history page that §4.10 does not cover), REALIGNMENT-LOG line for the new export columns and the `reimport_of` field, handback `docs/handoff/HANDBACK-IMP-1-2026-09-10.md` (Phase A RED table → Phase B green table with `path:line`, manifest numbers, resume recipe). Do NOT merge into `dev`, do NOT push to origin. Stop with `status: review` — the orchestrator gates with `imports-reviewer` + `frontend-conventions-reviewer` and merges.
