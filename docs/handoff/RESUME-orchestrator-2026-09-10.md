# RESUME — orchestrator session handover (2026-09-10, ~13:00 Africa/Tunis)

Previous orchestrator session: Claude Fable 5.1 (`session_01Q63X8ksgVWSsbZ5eYVke49`), started 2026-09-09 17:46 after the IDE-freeze reboot; context at 66% → handed over. **A second Claude session is live on this repo** (worktree `.worktrees/rbac-audit`, branch `docs/rbac-audit-2026-09-09`, roles/permissions catalogue design; it schedules its wave 0 AFTER `lane/w-lot-a-1a` merges) — do not touch its worktree; coordinate only through the plan files.

## State of local `dev` (main checkout `/Users/houssamr/Projects/syneriva/apps/erp`)
- HEAD `29c07d8ad`+ (docs), **~100 commits ahead of `origin/dev`, UNPUSHED**. It now carries CODE from lane T-1 (merge `73f2040c6`: +26/−2 FormRequest guard, 3 test classes, ci.yml pgsql allowlist, lane-manifest ceilings). **Push = staging auto-deploy** (rule 21): promote only as a verified fast-forward, with the promotion note that `StockTransferCompleteConcurrencyPostgresTest` needs an exclusive DB and must never enter a parallel leg.
- Untracked (preserve): `apps/api/docs/sessions/2026-08-04-cross-tenant-scheduled-jobs-plan.md`, `docs/handoff/HANDBACK-enforcement-p1-2026-08-19.md`.
- Pin tag `ci-pin/enforcement-p2-r1` moved to seed `89b508e1f` (pushed; dev CI i18n gate green). BUILD_SHA → ticket `docs/superpowers/tickets/2026-09-09-web-build-sha-from-git-build-context.md` (Dokploy cannot inject the sha; do NOT set a static build arg).

## Lane A — W-LOT-A-1a (lot permissions / GM role / web gating) — IMPLEMENTATION, fix round 1 in the owner's Codex Desktop thread
- Plan authority **rev 10** `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-10.md` (rev 8 gate r8 DISPATCH-READY; rev 9 = NULL-team ruling; rev 10 = gate-r1 rulings: B-2 Option B string totals + expiring/expired narrowing ADOPTED at Push 3 with riders R1–R3, technician NOT granted, manifest raise in the fix round, Push 4 before Push 5).
- Worktree `.worktrees/w-lot-a-1a`, branch `lane/w-lot-a-1a`, HEAD `04e60530c` (8 commits, prod +2025/−593), PG DB `autoerp_test_w`.
- Gate r1 registers (all CHANGES-REQUIRED): `docs/superpowers/reviews/2026-09-10-w-lot-a-1a-impl-gate-r1-{tenancy,inventory,frontend}.md`. Fix-round prompt DISPATCHED by the owner: `docs/handoff/CODEX-PROMPT-WLOTA-1a-fix-round-1-2026-09-10.md`.
- **On `status: review`:** gate r2 with tenancy-authz (only PG runner, `autoerp_test_w`), inventory-costing (SQLite), frontend-conventions — ONE AT A TIME while the laptop is loaded; verify B-1 skip on `phpunit.xml`, R1/R2/R3, the real flag-off contract test, the Playwright evidence at `spec:39:3` from a committed harness, manifest check exit 0. Then merge into LOCAL dev from the main checkout (absolute `cd`), then the §11 five-push staging protocol WITH the owner (Push 1 ops → Push 2 migration → Push 3 dormant backend → Push 4 delta + `permission:cache-reset` → verify `batches.view` on viewer/operator/cashier → Push 5 web). Dokploy app IDs in plan §11 / `docs/superpowers/plans/2026-09-09-parapharmacy-staging-topology-U1-resolution.md`. NEVER copy Dokploy env values into docs.
- Owner rulings on this lane: legacy NULL-team roles NOT adopted; technician NOT granted `batches.view`; B-2 Option B. Ticket owed by the fix round: `2026-09-10-batch-create-update-api-enforcement.md` (A-1b), `2026-09-10-batch-detail-parsefloat-quantities.md`.

## Lane B — T-2/T-3 (transfer receipt, close, blind receiving, notifications) — spec ACCEPTED, plan DISPATCH-READY, S1 implementing
- Spec authority rev 11 `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md` (gate r11 ACCEPT; all owner items ruled incl. R5 on-hand residual "Oracle approach", OD-1, close = reconcile AND close, OD-4).
- Execution plan authority **rev 8** `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-8.md` (gate r6 DISPATCH-READY on rev 6; rev 7 provenance; rev 8 = §6.7b/§7.2 movement-creation amendment ruled on the S1 handback: transfer movements created with final type + linkage inside `StockAdjustmentService::receive()/issue()` behind a backward-compatible param, `markMovementAsTransfer` DELETED, DPA baseline entry REMOVED (shrink only, no re-pin, no scanner change), real event labels after a consumer census, T-1 + ratchet regression gate; reviewed by stock-gl-interaction-reviewer at the S1 gate). Plan gates r1..r6 in `docs/superpowers/reviews/2026-09-{09,10}-t2-t3-plan-codex-gate-r*.md`.
- **S1** (receipt spine) in the owner's Codex Desktop thread `T-2 receipt spine`: worktree `.worktrees/t2-receipt-spine`, branch `lane/t2-receipt-spine`, PG `autoerp_test_u`; packet `docs/handoff/CODEX-DISPATCH-T2T3-S1-receipt-spine-2026-09-10.md`; the ruling paste-back `docs/handoff/CODEX-PROMPT-T2T3-S1-ruling-movement-creation-2026-09-10.md` was RELAYED. On `status: review`: gate per plan §14 (inventory-costing + stock-gl-interaction + tenancy-authz), the stock-gl reviewer must cite the rev-8 amendment; then merge; then S2 (`autoerp_test_v`), S3 (`_x`), S4 (`_y`), S4-mobile (erp-mobile repo @ `51e3445`) — packets in plan §17.1–§17.5, each dispatched only after the previous merges.
- T-1 tickets on dev (`docs/superpowers/tickets/2026-09-09-t1-*.md`) + freight-no-GL ticket are inputs to S1/S2; reviewer follow-ups MINOR-5/6 (scope the journal baseline, reserve via real writer) belong to T-2.

## Standing rules learned this session (also in memory)
- `/private/tmp` is wiped on reboot: Codex `-o` outputs, pid files and drafts die with it; recover from `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl` (last assistant message = the `-o` output); keep drafts under `docs/handoff/` (`reference_codex_rollout_recovery_after_reboot`).
- Codex fix rounds on big docs: byte copy rev N → N+1, targeted edits (Codex `-s workspace-write` or a Claude Opus agent with Edit), per-section census before gating; on every orchestrator rev bump grep `plan-rev-N` and relabel header/§1/§7.16/§17/§18 (gate r5 caught packets naming a superseded rev).
- Lane cap raised to 3 by the owner (2026-09-09) but the laptop is at swap 8+/9 GB with load 25 and no reboot since 2026-09-09 17:40: run reviewer agents ONE AT A TIME; three parallel reviewers stalled at once (watchdog 600 s).
- Blind receiving = hide the received DOCUMENT's quantities only (Oracle approach); on-hand stays visible (R5).
- Plan gates never modelled the feature-lane manifest: every implementation lane needs its ceiling raise + pgsql allowlist (T-1 precedent commit `11a941842`; A-1a numbers in the tenancy register).

## Owner-owed / open
- Reboot the laptop between Codex runs (swap).
- Promote local dev to origin/dev when the owner says so (code now included; staging deploy).
- OD/rulings all closed; no owner question open at handover time.

## Lane C — IMP-1 imports history + error-line export (added 13:3x, owner request)
- Owner symptom: exporting failing lines returns "the same file". Root cause: spec §4.10 / lane G-1 (error-line export with `_status/_code/_message`, source headers, BOM, re-importable) was never dispatched; today's `FailedRowsExportService` + result workbook echo rows without reasons. Benchmark + ruling: `docs/superpowers/reviews/2026-09-10-benchmark-import-error-export.md`.
- Dispatch packet handed to the owner: `docs/handoff/CODEX-DISPATCH-IMP-1-imports-history-and-error-export-2026-09-10.md` (worktree `.worktrees/imp1-history-export`, PG `autoerp_test_z`; Phase A Playwright evidence with 4 sample files → Phase B G-1 implementation). On `status: review`: gate with imports-reviewer + frontend-conventions-reviewer (one at a time), merge into local dev. Owner confirmation pending on one UI point: row export = primary history action, result workbook = secondary "full report".
