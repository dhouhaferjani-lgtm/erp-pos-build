# Codex prompt — W-LOT-A-1 fix round 1 (paste into the SAME thread that wrote the W-LOT-A-1 plan)

---

Fix round 1 for your slice plan docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-1.md. Read the gate review docs/superpowers/reviews/2026-09-06-w-lot-a-1-slice-codex-gate-r1.md in full and write rev 2 to docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-2.md. Read-only on everything else: no code, tests, migrations or git commands. Do not ask questions; when done reply with the path only.

Non-negotiables for rev 2:
1. Q10 is now RULED (docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md, section "Q10–Q13 RULED"): `requested → recalled` or `requested → released`; only the general manager may release or reject; mandatory reason and append-only evidence; the requester never lifts its own hold. Replace the "Q10 OPEN" section and the self-blocking dispatch condition. This slice still implements only `requested → recalled`; release/reject are DEFERRED to slice W-LOT-A-1b under the ruling, and the schema/state machine in this slice must be forward-compatible with the ruled states (M1: the schema must make `recalled` and `released` mutually exclusive terminal outcomes; M2: carry the transition reason in the transport contract now).
2. Deployment: replace the slice's own deployment prose with a reference to the canonical manifest docs/superpowers/plans/2026-09-06-parapharmacy-staging-push-manifest.md exactly as its §5 prescribes, supplying every per-slice variable (migration list, flags with config key + env var, reseed + permission:cache-reset, permission-map regeneration, web changes yes with the web deploy block, device build no, rollback point per push, host-side backup step). Note manifest U-1/U-2/U-6 as promotion preconditions.
3. Close every MAJOR with a plan change: M3 general-manager assignment sequenced against the actual user-creation path (cite UserController and the membership write); M4 distinguish pre-existing vs slice-created `general_manager` on rerun (explicit marker or idempotent delta with asserted outcome); M5 held-stock refusal has a named exception class and HTTP/envelope mapping per endpoint (POS FEFO, direct batch transfer, StockTransferService); M6 exact red-first contracts (file + class::method + first failing assertion + command + lane) and convention-09 tests mapped per task with explicit rerun outcome; M7 generated types via `php artisan typescript:transform` named as a task step with the generated file paths; M8 allowed-location selector with data-source contract and test. Close N1 (do not claim DIVERGE where every comparator is NV; use DEFER or ALREADY with the reason). Or reject any item with a path:line citation in a change log at the top.
4. Keep ≤6 tasks; every claim about current code cites path:line at current HEAD.
