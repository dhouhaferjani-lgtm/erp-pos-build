# Codex prompt — W-LOT-A-1 fix round 2 (paste into the SAME W-LOT-A-1 thread)

---

Fix round 2 for docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-2.md. Read the gate review docs/superpowers/reviews/2026-09-06-w-lot-a-1-slice-codex-gate-r2.md in full and write rev 3 to docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-3.md. Read-only on everything else; no code, tests, migrations or git commands; do not ask questions; reply with the path only.

Non-negotiables for rev 3:
1. B-R2-1 (immediate POS block does not reach the live sale path): state the truth and design to it. Server-side paths (POS receipt projection FEFO consumption, direct batch transfer, StockTransferService allocation) honour the hold immediately because they query server eligibility. The device sells from its own cache and only learns of a hold at the next refresh (owner ruling D4: refresh at session open, cached during the session). Therefore this slice guarantees: no NEW server-side consumption of a held lot after the hold; device-side blocking of a held lot is delivered by W-LOT-B's cache (cite it as the interface: hold status must be part of the branch lot eligibility payload). Add acceptance rows for both halves and remove any claim of device-immediate blocking.
2. B-R2-2 ("sole writer BatchRecallService" is false): census every existing writer of is_recalled / recall state (BatchController, any command, any seeder/import path) with path:line; either route them through the new service in this slice with a named task step, or exclude them explicitly with the reason and a ratchet test that fails if a new direct writer appears.
3. Close every MAJOR: M-R2-1 location-restricted read endpoints (apply LocationScopeResolver on the batch list/detail/traceability reads in Task 1, with tests for the other-branch leak); M-R2-2 the approved viewer/operator seeded-role delta (owner rulings: viewer/operator get batches.view only; manager keeps view/traceability/request; general_manager adds recall); M-R2-3 the capability endpoint complete contract in the task that owns it; M-R2-4 permission-gate activation must follow the manifest's dormant-code push (flag-gated enforcement, default off, activation in Push 5, not at migration time); M-R2-5 every red-first case must actually be red at HEAD — re-verify each, drop or rewrite the ones already green or failing before the named assertion.
4. Close the MINORs: current HEAD baseline; Push 1 either ships a tooling commit or is merged into Push 2 per the manifest; quote the exact permission:cache-reset marker string from the entrypoint; specify the deterministic transition UUID algorithm (namespace UUIDv5 over company_id + request_id + transition kind + sequence); remove convention-10 prose that reopens the ruled role choice.
5. Every claim about current code cites path:line at current HEAD; ≤6 tasks; include a round-2 change log with the closure of every gate item.
