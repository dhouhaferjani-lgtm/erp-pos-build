# RESUME — 2026-07-08 (pre-reboot orchestration state)

Single source of truth for what to work on after the reboot. Everything below is committed +
pushed (verified clean across all worktrees). A fresh session: read this + MEMORY.md, then act.
Companion (stack/container restart): `docs/sessions/CRASH-RECOVERY-INVENTORY-2026-07-07.md`.

## Post-reboot stack bring-up (do first if you need to verify anything)
1. Docker Desktop → `docker start autoerp_postgres autoerp_redis autoerp_minio autoerp_meilisearch syneriva-postgres-parapharmacy syneriva-redis syneriva_minio syneriva-data-acquisition`. Leave agency-* and pgh-monitoring-* STOPPED (memory).
2. App stack is per-worktree (`php artisan serve --port=8010` + `pnpm dev` (:5173) + multi-queue worker `queue:work redis --queue=default,fiscal-projections,enrichment,images,imports`). Recipe: memory `reference_local_db_per_tenant_demo_launch`. Login owner@pharmabio.tn / password.
3. **The local demo tenant DB already has the DocumentIngestion table** (I ran scan-to-doc `tenants:migrate` — additive). It does NOT have the location-hierarchy rename (that stays for the cutover). Swap depends on which worktree's server you run; they share the same tenant DB.
4. Reboot clears the 39.8 GB swap that was the crash risk.

## Track status (most-done first)

### A. Scan-to-Document (OCR) — FINALIZED, E2E VERIFIED + MERGED TO LOCAL DEV ✅ (2026-07-08 post-reboot)
- Branch `feat/scan-to-document` @ `caf9c3ff6`; **merged to LOCAL dev `b1dec1d93` (--no-ff, NOT pushed — origin/dev unchanged, 38 ahead incl. pricing/treasury spec commits)**. Board T-0014.
- **4 fixes found+fixed during finalization E2E, all live-verified + committed:** `debdc2f5c` erp-ml envelope doc_kind/pages mapping (extraction was 100% failing); `61fbdacc2` snake_case line-field normalization at review boundary (unit_price/tax_rate/supplier_ref/line_total — were empty in the form → invoice commit shipped no price; now all 4 lines prefill 8.500/19.200/5.750/14.900 VAT 7); `df45bed15` scan preview rendered as `<img>` not `<iframe>` (media route blocks framing via X-Frame-Options: DENY + CSP frame-ancestors 'none' → preview was permanently blank) + open-in-tab PDF fallback; `caf9c3ff6` accept null `source_bbox` (erp-ml emits it for un-localizable fields — REAL handwritten invoice failed the whole extraction until fixed). First 3 were the snake/camel + fake-fixture-shape class from `1e465fccd`/live-counting; the 4th only surfaced via a REAL messy document. Tested with BOTH a clean synthetic invoice AND a real internet invoice (Wikimedia CC0 handwritten proforma → needs_review, 10 lines, low-conf flagged, commit fail-closed on inconsistent reconciliation). 13 api tests + 8 web tests green, PHPStan L8 + eslint + tsc + pint clean.
- **Live browser E2E PASSED** (owner@pharmabio.tn, `/purchases/scans`): upload PNG invoice → Claude OCR → `needs_review`, avg confidence 0.99, review UI renders supplier/header/4 lines + product auto-match. First real E2E run surfaced + fixed a blocker:
  - **Bug fixed `debdc2f5c`:** `ErpMlExtractionClient` forwarded only erp-ml's `result` sub-object; erp-ml keeps `doc_kind`/`pages` in the TOP-LEVEL envelope → every extraction died `INVALID_EXTRACTION_DATA: doc_kind must be a string`. Client now folds envelope doc_kind/pages into the payload; unit-test fake corrected to mirror the real wire shape (it had wrongly nested them under `result`). 9 tests pass, PHPStan L8 + Pint clean, adversarial review APPROVE.
- **Keys wired (local):** root `.env` ANTHROPIC key copied into `apps/erp-ml/.env` `ANTHROPIC_API_KEY`; generated matching `EXTRACTION_SERVICE_TOKEN` (erp-ml) = `ERP_ML_SERVICE_TOKEN` (`apps/erp.scan-to-doc/apps/api/.env`, also set `ERP_ML_URL=http://127.0.0.1:8002`). **erp-ml venv was missing the `anthropic` package — `pip install anthropic` (0.116.0) required.**
- **Local deploy checklist DONE:** tenants:migrate (nothing pending), permission:cache-reset (flushed), ingestion perms already seeded (`document-ingestions.{create,view,commit,reject}`), Horizon `ingestion` queue in worker.
- **REMAINING (owner-gated):** (1) push local dev → origin/dev (owner promotion decision — dev is 28 ahead incl. pricing/treasury spec commits); (2) PRODUCTION deploy checklist: `tenants:migrate` + `RolesAndPermissionsSeeder --force` + `permission:cache-reset` + Horizon `ingestion` worker; (3) mobile E2E now unblocked (keys live). Minor: source-doc preview iframe not serving locally (MinIO); unit_price/VAT not prefilled into review form (confirm intended). Detail in memory `project_scan_to_document_spec_b`.

### B. Location Hierarchy Phase 1 — DUAL-APPROVED, MERGE-READY ✅
- Branch `feat/location-placement-hierarchy` @ `d8ed46d15`, worktree `apps/erp.location-hierarchy`, pushed. Both reviewer gates APPROVED after fix round.
- **CUTOVER = ONE operation** (merge to dev + `tenants:migrate` [renames location_zones→location_nodes] + local stack restart + FE zones-api shim lands with the route swap). **Sequence AFTER track D (counting report)** — the report branch is based on pre-rename dev; renaming first would break its stack.

### C. Live Inventory Counting — MERGED TO DEV ✅
- On origin/dev `d4784174f`. Owner owes: Tauri-device checks (sell-below-zero, blocking count) + production deploy (tenants:migrate). Memory `project_live_inventory_counting`.

### D. Counting Discrepancy Report — BRIEF READY, NOT STARTED ⏸ (owner → Codex desktop)
- Branch `feat/counting-discrepancy-report` (empty, based on dev), worktree `apps/erp.live-counting`. Brief: `docs/sessions/CODEX-TASK-counting-report.md`.
- Backend `GET /countings/{id}/report` was never built (page 404s); December FE contract exists. Brief covers it + replay deltas, money-as-strings, export DEFERRED to a later task.
- **Codex desktop paste** (open project AT `apps/erp.live-counting`): "Read the EXACT path .../CODEX-TASK-counting-report.md and implement exactly (TDD, tests by path, keep TASK-LOG, commit per task). If missing, STOP." Then I verify + gate. Unblocks B's sequencing.

### E. POS Prepaid Drawdown — BLOCKED on owner §9 answers ⏸
- Branch `feat/pos-prepaid-drawdown` @ `d13adb2d0`. Spec Rev 2, dual reviews reconciled. 6 §9 questions (all have recommendations — "all recommended" is valid) in `docs/superpowers/specs/2026-07-07-pos-prepaid-drawdown-design.md` §9. Answers → plan → Codex waves. Memory `project_pos_prepaid_drawdown`.

### F. Mobile (erp-mobile) — DONE locally, awaits OWNER PUSH ⏸
- Counting + scan-capture features complete, 9 commits ahead of origin/main, owner pushes personally. Their backend ask (typed DUPLICATE_DOCUMENT 422) shipped `638cdaa99`. Handovers updated: `HANDOVER-mobile-scan-capture.md`, `HANDOVER-live-counting-mobile.md`.

## Owner action queue (fastest unblock order)
1. Reboot; bring up containers (above).
2. §9 answers (E) — paper, no machine needed → unblocks a whole track.
3. erp-ml keys (A) → real OCR E2E in the browser + mobile E2E.
4. Paste counting-report brief into Codex desktop (D) → unblocks hierarchy cutover (B).
5. erp-mobile push (F); Tauri-device checks (C).

## Codex desktop protocol (learned 2026-07-07)
Open the project IN the target worktree (not main erp — `docs/sessions/` is gitignored there and Codex will improvise). Pin absolute brief paths + "STOP if the file is missing." CLI-brokered Codex can't write to sibling worktrees (sandbox) — worktree tasks go through desktop.

## Follow-up tickets on the board (non-blocking)
T-0015 (hard-kill strands Committing+null ingestion), T-0016 (widen ForbidHardcodedBcmathScale to Committers dir), T-0017 (POS scanner false-negative from the misfire verdict).
