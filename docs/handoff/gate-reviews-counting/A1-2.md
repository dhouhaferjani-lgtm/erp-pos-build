# A1 Inventory Costing Review — Round 2

- **Reviewer:** `inventory-costing-reviewer`
- **Model:** Opus
- **Scope:** A1 working-tree changes after round-one fixes
- **Mode:** Read-only

## Round-One Finding Verification

- **Resolved:** The selected node is pinned to the item's location at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:789-792`, matching the location pin on the subtree probe at lines 802-806.
- **Resolved:** The loaded node supplies the tenant fallback at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:814`.
- **Resolved:** The current live placement is selected with `lockForUpdate()` at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:793-797` inside the outer transaction opened at line 736.
- **Resolved:** Both the call-site comment at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:740-743` and method docblock at lines 766-771 describe preserve/re-home semantics.
- **Resolved:** The unplaced case at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:297-350` now uses a product that has never been assigned.
- **Verified:** The new cross-location regression at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:456-497` is discriminating: it uses matching `A1` paths at different locations and proves the selected-node lookup must be location-pinned.

The reviewer could not execute PHPUnit, Pint, or PHPStan because its Bash calls were denied. The implementer separately verified 20 tests / 90 assertions, Pint, and PHPStan level 8 before this round.

## Verdict

**VERDICT: spec ✅ + quality APPROVED**

## Findings

- `[Minor] apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:492-495` — After asserting the cross-location exception, also assert that the original local placement survived the transaction rollback.
- `[Minor] apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:335-339` — Remove the `deleted_at` predicate from the precondition so the test proves there is no placement row of any kind, including a tombstone.
- `[Minor] docs/superpowers/specs/2026-07-07-location-placement-hierarchy-design.md:240` — Align the earlier hierarchy design's assign-as-you-count description with the later owner ruling that preserves descendants.
- `[Minor] verification gap` — Reviewer-side command execution was denied; retain implementer-side PHPUnit, Pint, and PHPStan evidence.

## Required Before Merge

Nothing blocking. The implementer elected to fix every actionable minor finding and request a third review. The reviewer command-execution limitation is covered by the implementer's local verification.
