# A2 Fiscal POS Review — Round 2

- **Reviewer:** `fiscal-pos-reviewer`
- **Model:** Opus
- **Scope:** Revised A2 diff after round-one required fixes
- **Mode:** Read-only

## Round-One Finding Verification

- **Resolved — stale snapshot / terminal switch:** the projection publishes in the same synchronous turn as the current-state read, performs no memory write after persistence yields, and reconciles the durable cache with the latest terminal (or removes it after reset).
- **Resolved — persistence before enforcement:** Zustand is updated before best-effort persistence, so storage failure cannot leave the cart gate open or turn an otherwise-successful pull into a failure. The new failure regression test pins this behavior.
- **Resolved — fiscal-regression coverage:** the new test uses a matching terminal id, forces `FiscalRegressionError`, and asserts both in-memory projection and persistence on the preserved-local-head success path.
- The failed-pull test correctly proves that a rejected server pull preserves an existing cached block and performs no persistence write.
- The activation dependency comment is accurate: claim/status payloads eagerly load location and carry the same counting fields while activation runs before publishing the terminal store record.

The reviewer could not execute Vitest or typecheck because its Bash call was denied. The implementer separately verified 84/84 focused tests, typecheck, and `git diff --check` before review. The reviewer independently ran `git diff --check` successfully.

## Non-Blocking Findings

- Add direct assertions for the durable-cache convergence and reset-to-null branches; current switch-during-persistence coverage proves the memory outcome but not the last cache write.
- Pre-existing `terminalStore.refreshTerminalRecord` still persists before publishing to memory. The new sync projector masks that on the same scheduler tick, but the sibling action should be reordered in a separate follow-up.
- The two terminal-state fetch/project paths have no response sequence guard, so out-of-order responses can transiently project older counting state until the next tick. Consolidation would remove that duplication.
- `JSON.stringify` comparison is key-order sensitive, matching the existing store implementation.
- Product policy still needs explicit sign-off for fail-closed offline behavior because a cached active block has no TTL or offline override.

## Verdict

Nothing blocking remains; the reviewer explicitly approved landing the A2 diff as-is.

**VERDICT: APPROVED**
