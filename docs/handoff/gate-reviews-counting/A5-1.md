# A5 frontend conventions review — round 1

## Verification

- `counting.*` leaf-key count EN / FR / AR: 250 / 250 / 250.
- Sorted leaf-key lists were identical; total object-and-leaf paths were 272 / 272 for EN / AR.
- The diff stayed wholly inside `ar/inventory.json`'s `counting` subtree.
- Placeholder-bearing keys matched EN, and A3/A4 keys were present.
- JSON parsed successfully.

## Findings

### Major

1. `ar/inventory.json:90`: `review.lateSalesDetected` only defined `_one` and `_other`. Arabic i18next JSON-v4 plural resolution also requires `_zero`, `_two`, `_few`, and `_many`; missing variants fall through to English for common counts.

### Minor

2. `ar/inventory.json:76,90`: translated replay as restart; use wording for reapplying movements.
3. `ar/inventory.json:91`: mixed POS/till terminology with payment-terminal terminology; standardize on POS till language.
4. `ar/inventory.json:85`: shorten `varianceShort` for the compact table heading.
5. `ar/inventory.json:85,98,100`: standardize opening-cost terminology on opening balance.
6. `ar/inventory.json:94`: `matchedOther` referred to another count rather than another counter.
7. Optional: reflow compact JSON formatting.

The reviewer noted that key parity, placeholder preservation, and structural locality otherwise passed.

**VERDICT: NEEDS REVISION**
