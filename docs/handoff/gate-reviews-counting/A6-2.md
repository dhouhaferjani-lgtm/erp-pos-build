# A6 inventory/costing review — round 2

## Closure

- Round-one Important #1 closed: leaf string validation no longer turns malformed IDs into a whole-batch 422; the item loop emits `invalid_product_id` and preserves valid siblings.
- Round-one Important #2 closed: submitted and database UUID keys are normalized for comparison, while the canonical database ID is persisted.
- Empty-ID barcode fallback is preserved.
- New errors carry stable `invalid_product_id`, `product_not_found`, and `invalid_barcode` codes.
- UUID prefetch is bounded to the 100-item request, company-scoped, and avoids per-ID queries.
- Cross-company, uppercase, non-string, barcode fallback, canonical persistence, and mixed-batch cases are discriminating.
- No costing, movement, quantity, or precision code was touched.

## Non-blocking notes

- The new invalid-barcode branch does not have a dedicated test.
- Defensive non-array guards are unreachable behind current nested validation.
- Barcode-only batches perform an empty `whereIn` query.
- Existing poisoned drafts are not repaired by this prevention change and must remain a rollout note.

The reviewer could not execute package commands in its restricted session. The implementation lane independently verified 7 tests / 46 assertions, Pint, and PHPStan level 8.

**VERDICT: APPROVED**
