# A1 Inventory Costing Review — Round 3

- **Reviewer:** `inventory-costing-reviewer`
- **Model:** Opus
- **Scope:** Final A1 working-tree changes after all actionable findings
- **Mode:** Read-only

## Round-Two Finding Verification

- **Resolved:** The cross-location regression re-reads the live placement after the rejected submission and asserts that the local rack survives at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:502-507`.
- **Resolved:** The never-placed precondition at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:335-338` omits `deleted_at`, excluding live and tombstoned rows alike.
- **Resolved:** All assign-as-you-count statements in `docs/superpowers/specs/2026-07-07-location-placement-hierarchy-design.md:159`, `:193`, and `:240` now describe preserve-descendant / re-home-only-if-unplaced-or-outside semantics.

## Final Reassessment

- The preserve gate at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:799-809` returns only when the selected location-scoped node exists, a live placement exists, and its node is within the canonical subtree.
- `LocationNode::scopeSubtreeOf` at `apps/api/app/Modules/Inventory/Domain/LocationNode.php:150-156` uses an exact path or slash-delimited descendant, preventing `A1`/`A10` collisions.
- The placement row is locked at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:793-797` inside the outer submit transaction, and the existing unique-violation recovery still handles concurrent first placement.
- The production diff adds placement reads and branching only. It introduces no stock, movement, cost, scale, or quantity arithmetic.
- The E2E compares raw stock-level database attributes before and after the counting phase at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:534-540` and `:566-574`.
- The new tests are behavior-discriminating for descendant preservation, outside re-home, prefix collision, and cross-location same-path isolation.

The reviewer could not execute PHPUnit because its Bash call was denied. The implementer separately verified the named path, Pint, PHPStan level 8, and `git diff --check` immediately before this round.

## Verdict

**VERDICT: spec ✅ + quality APPROVED**

## Findings

- `[Minor] apps/api/phpunit.xml:41-42` — SQLite in-memory tests cannot exercise PostgreSQL row-lock contention. The lock query is covered by shape and reviewed for correct PostgreSQL placement, but real contention remains a known coverage limit to record in the final lane report.
- `[Minor — pre-existing, outside A1 scope] apps/api/app/Modules/Inventory/Application/Services/LocationNodeService.php:224` — A node tombstoned mid-count still makes `submitCount` fail, identical to pre-change behavior. Track separately if encountered.
- `[Minor — documentation drift] docs/handoff/HANDOVER-live-counting-mobile.md:218` and `docs/superpowers/specs/2026-07-06-live-inventory-counting-design.md:55,163` still use superseded flat-zone/upsert wording. Sweep in the lane's final reporting pass.

## Required Before Merge

Nothing. All A1-specific actionable findings are resolved.
