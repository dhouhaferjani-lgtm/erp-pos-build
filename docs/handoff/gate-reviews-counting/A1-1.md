# A1 Inventory Costing Review — Round 1

- **Reviewer:** `inventory-costing-reviewer`
- **Model:** Opus
- **Scope:** Uncommitted A1 working-tree changes relative to the current HEAD
- **Mode:** Read-only

## Verification

- Descendant placement preservation is implemented by the early return in `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:795-805`, before `assignProduct()` at line 817, and covered in `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:359-384`.
- Outside and absent placements fall through to the existing assignment path and are covered in `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:296-357` and `:387-422`.
- Prefix collision safety comes from `LocationNode::scopeSubtreeOf` at `apps/api/app/Modules/Inventory/Domain/LocationNode.php:143-150` and is covered by the `A1`/`A10` test at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:425`.
- The production diff changes placement reads/branching only; it does not touch stock or movement logic.
- The hierarchy E2E preserves the bin at `apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:558-562` and compares raw stock rows at `:487-493` and `:519-527`.

The reviewer could not execute PHPUnit, Pint, or PHPStan because its Bash calls were denied. The implementer separately verified 19 tests / 88 assertions, Pint, and PHPStan level 8 before this round.

## Verdict

**VERDICT: spec ✅ + quality APPROVED**

## Findings

- `[Minor] apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:789` — Pin the selected zone lookup to the item's location. Without that constraint, a direct-service malformed scope can confuse identically coded paths at two locations and incorrectly preserve a placement.
- `[Minor] apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:811` — Reuse the already loaded zone for its tenant ID instead of querying the node again.
- `[Minor] apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:790-793` — Lock the live placement read so the preserve/re-home decision and the existing locked write path are atomic for concurrent placement changes.
- `[Minor] apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:740-743` — Refresh the call-site comment, which still described unconditional upsert behavior.
- `[Minor] apps/api/tests/Feature/Inventory/ZoneScopedCountingTest.php:296-357` — Strengthen the unplaced case so it covers a product that never had a placement row, not only a tombstoned prior placement.

## Required Before Merge

Nothing blocking. The implementer elected to fix all minor findings and request a second review.
