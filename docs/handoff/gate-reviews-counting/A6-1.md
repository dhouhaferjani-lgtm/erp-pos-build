# A6 inventory/costing review — round 1

## Verification

- UUID shape and caller-company existence were checked per item.
- Rejections happened before `scope_filters` mutation; mixed batches retained valid products.
- UUID lookup was bounded to the 100-item request and company-scoped.
- The original barcode-only and duplicate paths remained in place.
- The new tests discriminated the old bug for malformed IDs, cross-company IDs, and mixed batches.
- No costing, movement, quantity, or precision code was touched.

## Findings

### Important

1. The outer `string` validator still rejected a non-string `productId` as a whole-request 422 before the new per-item guard. Relax the leaf validator and classify malformed values inside the item loop.
2. UUID existence lookup accepted uppercase UUID syntax, but keyed database results by canonical lowercase values and compared them with the original casing. Normalize lookup keys and persist the database-canonical ID.

### Minor

- Preserve the old empty-`productId` plus barcode fallback.
- Non-array entries could disappear without a per-item error if validation were relaxed that far.
- Stable machine-readable error codes would avoid client string matching.
- Existing poisoned drafts are not repaired by this prevention change and remain a rollout risk.
- Add coverage for uppercase UUIDs and malformed non-string values.

**VERDICT: NEEDS REVISION**
