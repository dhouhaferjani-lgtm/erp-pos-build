## M4 whole-lane gate — round 2

**Scope held to:** brief §M4 ("re-run the full accumulated evidence over the integrated branch with every lens; paste and read `git diff --name-only <BASE>..HEAD`; prove the negatives; record the R-6 sequencing obligation"), as amended by `M1-ruling.md` (Option B, unconditional; conditions 1–5), plus closure of the six round-1 findings.
**Lenses:** `fiscal-pos` — applies (signed `Z_REPORT` / `X_REPORT` / `SESSION_CLOSE` `vat_breakdown` bytes). `general` — applies (rule 19 scale, TS strictness, gates). `tenancy-authz`, `treasury`, `inventory-costing` — **not applicable**: the diff is device-local read-path aggregation; no route, middleware, permission, GL, payment or stock surface appears in it.

**Verified independently at `HEAD` = `23dda4501`, not from the brief, the YAML or the round-1 register:**

- Census re-derived by command: `git diff --name-only 9d14cb8e1..HEAD` → **18** files, **13** under `docs/`, **5** under `apps/pos/`. Working tree clean. Matches the corrected YAML figures.
- Whole-branch **non-comment** production diff is exactly **15 added lines = three byte-identical five-line blocks** (`zReportService.ts:917-926`, `endOfDayPreview.ts:310-321`, `reportApi.ts:497-506`): `lineGross = line.line_total`, `lineNet = bcsub(lineGross, lineVat, <scale>)`, three scaled accumulators. No `bcabs` on the sale side (R-2 honoured). Scale is the injected currency scale in all three (`zReportService.ts:143`, `endOfDayPreview.ts:172`, `reportApi.ts:382-386`). Money is strings throughout; the only `parseFloat` in range is on `tax_rate` (a percent, `reportApi.ts:530-532`, `zReportService.ts:987`) and is untouched by the branch. **Rule 19 holds.**
- Ruled option implemented exactly: no `apps/api/**`, no registry/payload/parser/validator/projection/NF525/`zSessionAuthoring` path, no writer (`receiptService.ts` / `cartStore.ts` / `cartTotals.ts`), no migration/seeder/backfill, no new named queue, no version bump. **Ruling condition 1 holds.** Rule 8 honoured.
- Headline accumulators (`zReportService.ts:899-901`, `reportApi.ts:484-486`) confirmed **unchanged** in the whole-branch diff — brief NOT-IN-SCOPE and ruling condition 4 held. Refund branches unchanged except the disclosed struck comment in `endOfDayPreview.ts`.
- Ruling condition 2 is a real assertion: `saleReportingEndToEnd.test.ts:693-724` asserts the close authoring was actually reached, runs the **real** `buildZReportPayload`/`buildSessionClosePayload` on the captured `AuthorZSessionCloseInput`, compares `vat_breakdown` byte-for-byte via `JSON.stringify`, **and** pins the shared value to the corrected decomposition. Not vacuous.
- Round-1 finding 2 closed on both signed paths: the two new guards (`saleReportingEndToEnd.test.ts:634-665`) bite. Re-derived the arithmetic against `decimal.ts:14,22,26,78`: with the `decimals` argument dropped, `bcsub('10.004','0.001')` → `10.003`, accumulating → `20.006`, `bcformat(...,2)` at `zReportService.ts:988` / `reportApi.ts:533` half-up → `'20.01'` ≠ the asserted `'20.00'`. Round-1 findings 4, 5, 6 also closed (comment corrected to ROUND_HALF_UP with the PHP-vs-device distinction; `updated:` refreshed; brief residue label annotated in place and `owes_parent` narrowed to the ticket).
- Ran the gates myself rather than trusting the paste: Tier-1 by path → **7 files / 88 tests passed**; `tsc --noEmit` exit **0**; `node scripts/lint-ratchet.mjs` → `@autoerp/pos` **held at 84**, `@autoerp/web` 6449→6452 FAIL — inherited, and the inheritance is structurally sound (no `apps/web`, eslint config, ratchet script, `package.json` or shared package in the 18-file list).
- R-6 recorded, never assigned (`progress.yaml:544-563`); ruling condition 5's reversal window correctly stated as closing at build rollout, not merge.

---

### Register

**1 — P2 · CONFIRMED · `docs/handoff/progress/z-sale-branch-decomposition.progress.yaml:465`, `:467`, `:477-479`, `:498-508`, `:564`**
The M4 block is *self-invalidating*: every one of its own truth claims is false at the tip the parent will merge from.
- `status: passed` (`:465`) and `verdict: docs/handoff/reviews/…/M4-round2.md` (`:467`) were committed **before this gate ran**. `M4-round2.md` does not exist at `HEAD` (`ls` → No such file), while `verdict_history` round 2 simultaneously says `register: PENDING` / `result: pending` (`:477-479`). The same file states its own discipline at M1's verdict field — *"Exists at this commit — this field never forward-references."* (`:151`). M2 and M3 both honour it; M4 alone does not.
- The corrected census (`:498-508`) says **18 files / 13 docs** "at the M4 closure commit". `23dda4501` is the *fix-round* commit; the closure commit is the one that adds `M4-round2.md`, at which point the branch is **19 files / 14 docs** — and a round 3 would make it 20/15. Round-1 finding 1 asked for an "as of" qualifier; what landed anchors to a commit that has not happened yet, so the number is wrong again by exactly the mechanism that made it wrong the first time.
- `updated: 27acf2833` (`:564`) is one commit behind `HEAD` again, disclosed but unavoidable in this form.

Failure scenario: the parent closes LEDGER C-2 on this YAML, follows `verdict:` to a file that does not exist, and reconciles a census that disagrees with `git diff --name-only` by one. This is the **fourth** artifact-truth defect in a wave whose M4 exists specifically to prevent them.
**Close-before-merge (P2, not milestone-blocking per the brief's gate rule).** The closure commit must: write `M4-round2.md` and name it in `verdict_history` round 2 with the real result; re-derive the census **at that commit** (19/14, or better — state it as an invariant, "5 under `apps/pos` + one docs register per milestone-round", so it cannot go stale again); refresh `updated:`. Do **not** re-gate for this: another round re-breaks the count by one. The `REPRODUCE:` commands already in the field are the durable part — keep them and make the prose match.

**2 — P3 · CONFIRMED · `apps/pos/src/lib/offline/__tests__/saleReportingEndToEnd.test.ts:654`, `:664`; `endOfDayPreview.test.ts:165`**
R-3 coverage is complete for the **net** derivation on all three consumers, but the **gross accumulator's** scale argument remains unguarded everywhere. All three new/existing precision guards assert `net_amount` only; every `gross_amount` assertion in Tier-1 uses exact 2-dp values (`saleReportingEndToEnd.test.ts:432,450,465,474,501-534,587-610`, `zReportService.test.ts:1046`, `endOfDayPreview.test.ts:98`, `refundReportingEndToEnd.test.ts:332-366`).
Verified arithmetically (big.js, `Big.RM = 1`): `existing.gross = bcadd(existing.gross, lineGross)` **without** the scale argument over two `'10.004'` lines yields `20.008` → `bcformat(…,2)` → `'20.01'`, versus `'20.00'` with it — a one-cent shift in the **signed** `vat_breakdown[].gross_amount`, with all 88 Tier-1 tests still green. (The `vat` accumulator is likewise unguarded but does not drift on this fixture: `0.001+0.001 = 0.002` → `0.00` either way.)
Consequence: `r3_coverage_completed`'s *"All three sites now covered"* (`:492`) is true per-consumer, not per-accumulator; a reader will over-read it. Failure scenario: a later refactor drops `decimals` from the gross accumulator only; the suite stays green; a cash-rounded EUR shift seals a `gross_amount` a cent off in bytes that are immutable forever. Cheapest close: add `expect(...vat_breakdown[0]!.gross_amount).toBe('20.00')` to the two signed guards, or soften the YAML claim to name the residual.

**3 — P3 · CONFIRMED · `docs/sessions/codex-z-sale-branch-decomposition-report.md` (untracked; `.gitignore:58`)**
Deliverable #4 — the session report carrying the fixture-correction register, the R-6 note and **the evidence line the parent uses to close LEDGER C-2** — is gitignored by house rule 15 and therefore is **not part of the branch**. I verified it exists locally and that round-1 finding 1's corrections landed in it (`:322`, `:374`, `:382-384`, `:395` all carry 18/5/13 and "15 added non-comment lines … five-line blocks"). But it survives only on this machine and this worktree. Failure scenario: the worktree is removed after merge and the C-2 evidence line, the R-4 fixture register and the D-1/D-3 note are gone. The YAML duplicates most of it — the parent should confirm that before the worktree is pruned. Structural to the wave design, not an executor defect.

---

### Bypasses I tried that FAILED to find a defect

- **Census re-derivation.** Ran all three `REPRODUCE:` commands rather than reading the numbers: 18 / 13 / 5 and 15 lines, exactly as claimed at this commit. The round-1 P2 is genuinely closed *for this commit*; finding 1 above is about the next one.
- **Guard vacuity (the P1 candidate).** Traced whether the two new signed-path guards can pass for the wrong reason. They cannot: the precision row is the only receipt in a fresh `SqliteTestAdapter` (`beforeEach` at `:368-392`, `afterEach` closes it), so `toHaveLength(1)` fails if the row is filtered out, and the `'20.00'` value is only reachable via scale-2 accumulation. Confirmed the hand-authored row cannot leak into the real-writer decomposition tests — per-test DB.
- **Fixture-as-weakening (R-4 red flag).** `insertSubCentPrecisionRow` is a **new** direct-SQL fixture, not a corrected one, and it is declared as the file's single non-writer-produced row with the reason (`:614-632`). The decomposition claims still rest entirely on `createOfflineReceipt` + real `computeTaxAmount` + real `FiscalEventEngine`. No existing assertion was relaxed to accommodate it.
- **Refund-branch and headline tampering.** Whole-branch non-comment production diff pasted above: not one line of the refund derivations or the headline accumulators is added or removed. The only refund-side edit in the branch is the struck comment in `endOfDayPreview.ts`, disclosed.
- **Brief self-edit.** Diffed the executor's changes to its own acceptance brief across the whole branch: two ruling-ordered annotations (F-2 justification, residue column label) plus the NOT-IN-SCOPE row rewrite from "Already correct" to "Out of scope — but NOT because it is correct". **No acceptance criterion, invariant, required test, lens or gate was weakened.**
- **Rule 20.** The new fixture binds no JS-supplied timestamp into a SQL comparison (it relies on the column default); the shift boundary the Z uses is routed through `toSqliteUtc()` at `zReportService.ts:194`. Held.
- **Rule 8 / scope leak.** No server file, no `event_version`, no migration, no seeder, no new queue, no user-facing string, no `ar` tree. `tsc --noEmit` clean; no `any` in the added tests.
- **Gate paste-trust.** Ran Tier-1 (88), typecheck (exit 0) and the ratchet (pos 84 held) myself; all three match the YAML.

**Net:** the code is correct, the ruled option is implemented exactly, R-3 is now guarded on both signed paths, the negatives hold, and every gate I ran matches what was claimed. The one P2 is bookkeeping in the merge-input artifact that mechanically closes with the commit that records this verdict — re-gating for it would re-break the census by one and buy nothing.

*(Read-only session: I did not write `M4-round2.md`. This register is the round-2 record and must be persisted at `docs/handoff/reviews/z-sale-branch-decomposition/M4-round2.md` by the closure commit, together with the finding-1 corrections.)*

VERDICT: ACCEPT
