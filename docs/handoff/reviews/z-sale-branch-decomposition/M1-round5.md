## M1 round‑5 adversarial review — z-sale-branch-decomposition

**Range:** `9d14cb8e1afae9b9cd3e5afa9d8bfac8dd2aabd3..HEAD` (`464eea67a`). `git diff --name-only` = **7 files, all under `docs/`**; `git diff --name-only … -- apps/` = **0**. No production code, as M1 requires. `git status --porcelain` empty.

**Lens — fiscal-pos:** applicable and central; applied. I re-derived the sealed-bytes path from source rather than from the memo: sale-branch defect at `apps/pos/src/lib/offline/zReportService.ts:907-914` (`lineNet = line.line_total` at `:908`, `bcadd` with no scale arg at `:909`, `:912-914`) → `vat_breakdown` (`:995`) → the three payload builders, `vat_breakdown` present at `zSessionAuthoring.ts:392` (X), **`:440` (SESSION_CLOSE — F-1 confirmed independently)**, `:498` (Z). Sites 2 and 3 re-derived at `endOfDayPreview.ts:301-307` and `reportApi.ts:491-496` (the latter with `bcadd(lineNet, lineVat)` inlined at `:496`, no `lineGross` local — the memo's structural-normalisation note for M2/R-2 is warranted). Rule 8 respected: no backfill is proposed anywhere; the stop state is written.

**Standing checks:** rule 19 / red-first / tenant scoping / constructor injection / en+fr / migrations / Horizon queues — **not applicable at M1**, which the brief's evidence contract defines as "code evidence: none". Met non-vacuously: the diff is 100 % `docs/`, verified by file list, not asserted.

**Round‑4 disposition.**
- **R4‑1 (P2, Option‑A cost table still broken) → CLOSED, verified structurally.** `git diff faa0a3ec1..HEAD` shows the blank line removed. I scanned every markdown table in the memo programmatically: 9 tables, all with a valid delimiter row and uniform pipe counts; the Option‑A table is one contiguous 16‑row block at `M1-signed-bytes-decision.md:205-220`, A11–A14 inside it.
- **R4‑2 (P2, stale "~13 sites … + archive") → CLOSED in substance** at `:365` (§2.3) and `:589-593` (§6 reason 2); A9/A10 named as non‑costs, A14 named as a rollout constraint. Residual arithmetic issue → finding 1.
- **R4‑3 (P3, absolute NF525 framing) → PARTIALLY closed.** §0 (`:23`) and the A9+A10 block (`:240`, `:260`) now say "no *interpretable* value" and the new item 3 (`:266-274`) states the `<HashFiscal>` qualification correctly. Two of the four sites round 4 named were not swept → finding 2.
- **R4‑4 (P3, YAML forward-reference) → CLOSED.** `M1-round4.md` is committed at HEAD (in the diff list); `verdict:` points at it; the round‑5 row is marked `PENDING — not yet run at this commit`.

---

## Findings register

**1. P3 — CONFIRMED — the headline Option‑A cost figure does not match its own enumeration, and the error runs toward the memo's recommendation.**
`docs/handoff/reviews/z-sale-branch-decomposition/M1-signed-bytes-decision.md:365` (§2.3) and `:589-591` (§6 reason 2); also mirrored in `docs/handoff/progress/z-sale-branch-decomposition.progress.yaml` `option_A_real_cost`.
`:365` reads "**9 real code sites** … (A1-A3, A5-A8 + the conditional A4/A7)". That parenthetical enumerates **8** distinct rows (A1, A2, A3, A5, A6, A7, A8 + A4); A7 is counted twice, once inside the `A5-A8` range and again in "the conditional A4/A7". Round 4's own arithmetic reached 9 by *including* A12 and A13 ("the remaining real sites are A1–A3, A5–A8, A12, A13"), but the memo now counts A12/A13 **separately** in the same sentence — so the 9 is a mis-transcription, not a different-but-valid basis. Separately, "8 hardcoded-`1` test sites" matches neither reading of A12 (`:218`), which enumerates **9** line citations across **5** files (`FiscalEventPayloadRegistry.test.ts:80-81`, `FiscalEventPayloadRegistryTest.php:86-87`, `ZReportProjectionTest.php:333,375,422`, `ZSessionLifecycleQuarantineVisibilityTest.php:509,539,561`, `ZReportImmutabilityTest.php:400`). I verified four of those citations exist and are `event_version`/`eventVersionFor` = 1 assertions; the citations are sound, only the totals are not.
**Failure scenario:** the ruling gate reads §2.3 as the at-a-glance comparison and §6 item 2 as one of four weighted reasons, and takes away a cost for Option A one site higher than the memo's own table supports — the same directional bias round 4 flagged at `:356`/`:581`, at smaller magnitude. It cannot plausibly flip a ruling that turns on 8-vs-3 plus a one-way-door argument, which is why this is P3 and not P2. **Fix:** state "8 code sites (6 unconditional + A4/A7 conditional)" and either "9 hardcoded-`1` assertions across 5 files" or drop the integer and point at A12.

**2. P3 — CONFIRMED — the absolute NF525 framing that round 4 flagged survives at three sites, including one round 4 named explicitly.**
`:129-130` (§1.3a: *"the per-rate `vat_breakdown` this lane fixes reaches **no** archive surface at all"*), `:283` (*"the regulator-facing archive … carries nothing from this lane"* — round 4's cited `:274`), `:612` (§6 withdrawn-flip-condition: *"This lane's output reaches no NF525 surface at all"*).
Round‑4 finding 3 named `:23`, `:130`, `:241`, `:274`. The round‑5 commit qualified `:23` and the `:241` block and added the `<HashFiscal>` item, but left `:130` and `:274`/`:283` untouched, and `:612` carries the same absolute. I confirmed the underlying fact myself: `ZReportProjection.php:114` sets `'fiscal_hash' => $event->current_hash` (SHA‑256 over `canonical_bytes`, which contain `vat_breakdown`), and `Nf525XmlBuilder.php:291` emits it as `<HashFiscal>` inside `addZReports()` — so *something* from this lane does reach the archive, opaquely.
**Failure scenario:** low, and lower than round 4's version — §0 (the first table the gate reads) now carries the qualification, and the load-bearing conclusion (archive self-description cannot favour Option A) is untouched because a hash differentiates nothing. The residual risk is the one this memo has already realised three times: a gate that spot-checks a sentence finds it literally false and discounts a section that is otherwise correct. This is the same partial-sweep pattern as round‑4 finding 2 — flagged here so it does not recur a fifth time. **Fix:** one word ("no *interpretable* archive surface") at each of the three sites.

**3. P3 — CONFIRMED — an unrecorded downstream defect in the exact archive path the memo audits: the NF525 `GrandsTotaux` period totals for canonical Z reports are always `0.00`.**
`apps/api/app/Modules/POS/Application/Services/Nf525DataProvider.php:1255-1262` vs `apps/api/app/Modules/Compliance/Services/Nf525/Nf525XmlBuilder.php:335-336`.
`mapCanonicalZReportGrandTotal()` builds `$periodTotals` with keys `receipt_totals`, `refunds_totals`, `voids_totals`, `vat_breakdown`, `payment_method_totals` — there is **no** top-level `gross_sales` or `tax_amount`. `addGrandTotals()` reads exactly `periodTotals['gross_sales']` and `['tax_amount']`, so for every Z-derived grand total `<VentesBrutes>` and `<Taxe>` fall through to the `'0.00'` default. (The other feeder, `mapGrandTotal()` `:1286-1298`, passes `$grandtotal->period_totals` verbatim, so the two mappers produce different shapes for one DTO.) The memo's sentence at `:255-257` is literally true about the builder and its conclusion (`vat_breakdown` is never rendered) is unaffected — this is not a memo error. But the memo is this lane's register of downstream findings (F‑1..F‑4, rule 4 "record and continue"), and this one was found inside the path the memo spent three rounds re-deriving.
**Failure scenario:** a French JET export today reports zeroed period totals in `GrandsTotaux` for every canonical Z; and a gate reasoning "at least `gross_sales` reaches the archive through the grand totals" would be wrong. **Fix:** record it as F‑5 in §5 and in the YAML `findings:`; it belongs to whatever lane picks up F‑2/F‑4, not this one.

---

## Bypasses attempted that FAILED (no finding)

- **Trying to break the NF525 position a fourth time.** Re-derived from source, not from the memo: `addZReports()` (`Nf525XmlBuilder.php:280`) emits `NombreVentes/VentesBrutes/VentesNettes/MontantTaxe/NombreAnnulations` at `:297-301` and no breakdown; `addGrandTotals()` (`:317`) emits only `<VentesBrutes>` `:335` and `<Taxe>` `:336`; `grep -n VersionEvenement` → the single line `:598`, inside `addQuarantineSection()` (`:548`). **The memo's net position is correct.**
- **Trying to falsify §3.1.1's "zero seeder write sites" via the one seeder that mentions `pos_receipts`.** `CoffeeShopSeeder.php:262,266` disables/re-enables `enforce_receipt_immutability` — but the enclosing block is `createTenant()`'s **delete-and-recreate** path (`:256-268`), not a write. **Claim stands.**
- **Falsifying "no Z/X golden vector exists" (A13/§3.2).** `grep -rl Z_REPORT --include='*.json' --include='*.snap'` over `apps/api` + `apps/pos` → zero hits. **Stands.**
- **Falsifying F‑2 independently.** `cartTotals.ts:36` `subtotal = bcsum(item.line_total)`; `:40`/`:52` return `total: subtotal` verbatim with no discount; `receiptService.ts:145-158` re-sums `item.line_total` into the stored `subtotal` (`:547`); `zReportService.ts:900` `netSales = bcadd(netSales, receipt.subtotal)`; `ZReportProjection.php:149` passes `receipt_totals['net_sales']` through to `report_data`, which `Nf525XmlBuilder.php:299` exports as `<VentesNettes>`. **CONFIRMED end-to-end — the brief's "headline totals are correct" premise is false.**
- **Falsifying A3 (Option A's highest-risk line).** `FiscalEventPayloadRegistry.php` `SUPPORTED_VERSIONS` holds only `SALE_RECEIPT => [1,2,3,4]`; `supportedVersionsFor()` falls back to `[$this->eventVersionFor($type)]`; `StrictCanonicalParser.php:630` emits `envelope_event_version_mismatch`. **Stands.**
- **Falsifying F‑4 (no structural validation of `vat_breakdown`).** `validateZReportFamilyPayload()` (`FiscalPayloadConstraintValidator.php:764-771`) checks four UUIDs, `business_date`, `training_flag` — and never iterates the breakdown; the `SALE_RECEIPT` analogue (`Σ net_amount == subtotal`) is documented as a hard invariant at `:1096-1098`. **Stands.**
- **Falsifying B2's "display-only, one consumer".** `grep -rn buildEndOfDayPreview apps/pos/src` → one non-test call site, `EndOfDayPreviewModal.tsx:161-162`; `vat_breakdown` rendered at `:342-357`. **Stands.**
- **Re-checking for a table regression (round 3 and round 4 both died here).** Programmatic scan of all 9 tables: every one has a valid delimiter row and uniform column counts. **No recurrence.**
- **Scope / harness compliance.** 7 files, all `docs/`; no `apps/**`; tree clean; `status: blocked_review`; the gate question verbatim in `blockers:` with `blocks: [M2, M3, M4]`, the five `must_also_answer` items, and `owner_gates.z-signed-bytes-versioning.{ruled_by, chosen_option, record}` all null pending the parent. **No violation.**

---

**Assessment.** Both round‑4 P2s closed with work I verified independently rather than taking the fix commit's word for it — the Option‑A table renders, and the two stale "archive cost" claims are gone. Every M1 deliverable the brief names is present and non-vacuous: byte-level consequence (§1, re-derived from source at three sites plus three builders), both options costed with concrete files and symbols (§2, spot-checked across seven citations, all exact), the `event_version` consumer enumeration split into enforcing / cross-checking / carrying / immutability, real signed-history evidence with an explicit statement of what cannot be established from the worktree (§3.2, Q1–Q4), the tenant‑#1 argument with its four limits (§7), and an advisory recommendation with stated flip conditions and one withdrawn condition kept visible rather than silently edited (§6). What remains is three P3s: an off-by-one in a summary integer, three unswept absolute clauses whose substance is already correct, and one downstream archive defect worth recording before it is lost. None of them changes what the ruling gate is choosing between, and per the brief P3s may ship with a ticket. The memo is fit to be the input to the `fiscal-pos-reviewer` specialist gate.

VERDICT: ACCEPT
