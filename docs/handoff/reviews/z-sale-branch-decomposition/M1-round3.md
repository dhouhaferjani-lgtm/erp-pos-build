## M1 round‑3 adversarial review — z-sale-branch-decomposition

**Range:** `9d14cb8e1afae9b9cd3e5afa9d8bfac8dd2aabd3..HEAD` (`795966c1e`, `0edbf402b`, `63b756784`, `b13536a76`) — 5 files, all under `docs/`. **No production code**, as M1 requires. `git status --porcelain` empty. Base pin verified: `c6d6308ae` is an ancestor of `9d14cb8e1`; drift D-1 benign.

**Lens — fiscal-pos:** applicable and central; applied. I re-derived the sealed-bytes path myself rather than trusting rounds 1–2: sale branch `apps/pos/src/lib/offline/zReportService.ts:906-916` (`lineNet = line.line_total` at `:908`, no scale arg on any `bcadd`) → `vat_breakdown` (`:965-976`, `:995`) → close input `:684-689` → the three builders (`zSessionAuthoring.ts:392/440/498`); X independently via `reportApi.ts`. Rule 8 respected — no backfill proposed anywhere.

**Standing checks:** rule 19 / red-first / tenant scoping / constructor injection / i18n / migrations / Horizon — **not applicable**; documentation-only milestone by design, and the brief's M1 evidence contract is "code evidence: none". Met non-vacuously.

**Round‑2 disposition.** P1 (NF525 flip condition) → **withdrawn, correctly**: `grep -rn "VersionEvenement" apps/api/` returns exactly `Nf525XmlBuilder.php:598`, inside `addQuarantineSection()` (`:548`, `EvenementsQuarantaine` `:554`), fed only by the quarantine table and non-verified `fiscal_events` rows. Struck in §6 rather than silently edited. P2 (identity inversion) → closed as memo §1.3a, and every one of its citations re-derived and CONFIRMED (`zReportService.ts:900`; `receiptService.ts:145-158`, `:547`; `ZReportModal.tsx:158-159` vs `:176-193`; `ZReportProjection.php:149` vs `:154`, both from `$payload['receipt_totals']`; `FiscalPayloadConstraintValidator.php:1097-1098` docblock, enforced `:1139-1166`). P3 (a)(b)(c) → all three citation defects fixed and re-verified (`in_array` at `:628`, block `628-634`; projection passthrough `:154`; A9 path now `apps/api/app/Modules/POS/Application/Services/Nf525DataProvider.php:1247-1281`). YAML `recommendation:` de-staled; stop state written.

**But the round-2 fix substituted one wrong NF525 claim for another, in the opposite direction.**

---

## Findings register

**1. P1 — CONFIRMED — the memo's replacement NF525 claim is also false: the Z's `vat_breakdown` values do NOT reach the archive either.**
`docs/handoff/reviews/z-sale-branch-decomposition/M1-signed-bytes-decision.md:195` (A9), `:212-219` (A10 "in full"), `:299-304` (§2.2), and the YAML mirror at `docs/handoff/progress/z-sale-branch-decomposition.progress.yaml` (`option_A_nf525_not_visible`: *"The vat_breakdown VALUES do reach the archive GRANDTOTAL"*).
The memo now argues: the version stamp does not reach the export, **"while the `vat_breakdown` **values** *do* reach the archive — `mapCanonicalZReportGrandTotal()` … feeds the NF525 GRANDTOTAL section"**. `mapCanonicalZReportGrandTotal()` (`Nf525DataProvider.php:1247`) does put `vat_breakdown` into `Nf525GrandTotalData::$periodTotals` (`:1260`) — but **`Nf525XmlBuilder::addGrandTotals()` (`:319-350`) emits exactly two values from `periodTotals`: `<VentesBrutes>` ← `gross_sales` (`:335`) and `<Taxe>` ← `tax_amount` (`:336`).** `vat_breakdown` is never rendered. The only other Z surface, `addZReports()`, emits five scalars (`:296-302`) and no breakdown. `grep -rn "periodTotals|perpetualTotals" apps/api/app` returns only those four builder lines plus `GrandtotalService`; `Nf525JetExportService::exportJet()` (`:35-74`) is the single export path and XML the single format; the ticket-level `<VentilationTVA>` (`:160-169`) is fed per-receipt from `$receipt->vatDetails` (`Nf525DataProvider.php:661`, `:885` — the SALE_RECEIPT canonical view), **not** from the Z.
**Failure scenario:** the `fiscal-pos-reviewer` ruling gate — a one-way door — weighs "what does the French regulator actually see" from a memo that tells it the corrected/uncorrected per-rate values are archive-visible. They are not: **this lane's per-rate correction has zero NF525 export consequence under either option.** Two load-bearing sentences fall with it: A9's *"A v2 must be handled here or the archive silently mixes conventions"* (nothing can mix — the values are never emitted, so A9 is not an Option-A cost at all), and §2.2's counterweight, which currently reads as "the data is visible, only the label is missing." This is the same defect class round 2 rated P1, restated in the other direction, and it was introduced by the commit that closed it (`b13536a76`).

**2. P2 — CONFIRMED — §1.3a's visibility enumeration omits the one surface that is genuinely regulator-facing, and it is F-2's.**
Memo `:120-122` (*"operator- and server-visible, not theoretical"*).
The Z's headline **does** reach the JET export: `Nf525XmlBuilder.php:298-299` emits `<VentesBrutes>` / **`<VentesNettes>`** from `report_data['gross_sales'] / ['net_sales']`, and that `report_data` is the projection's passthrough of the device payload (`ZReportProjection.php:149`; `Nf525DataProvider::mapZReport()` `:1226-1243`). So the F-2 defect — `net_sales == gross_sales` on every taxed shift — is **exported to a French fiscal archive today as `<VentesNettes>`**, and after this lane's fix with F-2 unfixed it sits in the archive beside a Z whose per-rate rows are now correct.
**Failure scenario:** the gate answers ruling question 3 (*"F-2: ticket it or fold it in?"*) believing the residual is confined to an operator screen and a projection row — the memo's own enumeration — and tickets it. The actual residual is a wrong `<VentesNettes>` in the NF525 JET export for every taxed shift. Combined with finding 1, the correct framing is the inverse of the memo's: **the archive carries F-2's error and carries nothing from this lane** — which is precisely the input ruling question 3 exists to weigh.

**3. P2 — CONFIRMED — the round-2 fix breaks the Option-A cost table; rows A11–A14 no longer render as a table.**
Memo `:196-220`. The A10 prose block (`:198-219`) was inserted **between** the A10 and A11 rows, and `:220` (`| A11 | …`) follows `:219` (`chain, not the archive.`) **with no blank line**. In GFM, `:220-223` become a lazy continuation of that paragraph: A11 (the `CashRoundingCutover` latent trap), A12 (the eight test sites), A13 (no golden vector exists to test "old events keep old semantics" against) and **A14 — the deploy-ordering hazard that round 1's P2 forced into the memo** — render as a run-on paragraph of raw pipes. At `63b756784` the table was contiguous A1→A14 (`git show 63b756784:…:157-161`), so this is new.
**Failure scenario:** the ruling gate reads the rendered memo, sees Option A's cost table apparently terminating at A10, and skims or loses A11–A14 — including the 100 %-quarantine rollout hazard — at the moment of an irreversible ruling. The brief's M1 antidote is explicitly *"both options must list concrete files/symbols"*; the content exists but is delivered garbled. Fix: blank line before `:220` plus a repeated header row, or move the A10 prose below the table exactly as A14's prose already sits.

**4. P3 — CONFIRMED — §1.3a's post-fix ✓ is scenario-scoped; a transaction discount leaves the gross column contradictory too.**
Memo `:109-113` (table row *"After this fix, F-2 unfixed"*: `Σ gross_amount == gross_sales` ✓).
`lines[]` carry the pre-transaction-discount `line_total` (`receiptService.ts:537`), and the transaction discount lives only in the header (`:549`, `:555`); `cartTotals.ts:66` computes `total = subtotal − discount`. The sale loop never subtracts it (`zReportService.ts:905-916`). So on a discounted shift, post-fix `Σ gross_amount − gross_sales = discount ≠ 0`, while `Σ net_amount − net_sales = −VAT` as stated. The table is introduced as a one-line 12.00/2.00 example, so it is not false — but the conclusion the gate takes away ("the contradiction *moves* to the net column") is, on a discounted shift, "one contradiction moves and a second is retained". This **strengthens** §1.3a rather than reversing it, and is worth one sentence given ruling question 3 turns on how coherent the post-fix sealed payload is.

**5. P3 — CONFIRMED — progress-YAML bookkeeping is one commit behind the state it describes.**
`docs/handoff/progress/z-sale-branch-decomposition.progress.yaml`: `verdict:` points at `docs/handoff/reviews/z-sale-branch-decomposition/M1-round3.md`, which **does not exist at HEAD** (a forward reference to this review); `fix_commit: 63b756784` names only the round-1 fix commit while the round-2 fixes are in HEAD `b13536a76` (the inline comment says so, but the field does not); `updated: 63b756784` trails HEAD by one (declared self-reference convention — accepted). `status: blocked_review`, the verbatim gate question in `blockers:`, `blocks: [M2, M3, M4]` and `resume_condition` are all correctly written, and `owner_gates.z-signed-bytes-versioning` is correctly all-null pending the parent's ruling.
**Failure scenario:** a parent resuming off the YAML follows `verdict:` to a missing file and cannot tell whether round 3 ran or the register was lost.

---

## Bypasses attempted that FAILED (no finding)

- **Breaking the round-2 P1 fix (was the NF525 withdrawal itself wrong?).** Enumerated every `Version*` element in the builder: `VersionLogiciel` (`:63`, software), `VersionPrecedente` (`:469`, `payload['previous_version']`), `VersionEvenement` (`:598`) and `VersionSignature` (`:599`) — the last two only inside `addQuarantineSection()`. No section emits `canonical_bytes` (which is where the envelope's `event_version` lives) for verified events; the only `canonical_bytes` references in the NF525 code are re-hash/tamper paths. **The withdrawal is correct.**
- **Finding a device-side version allowlist Option A would trip (a missing A-item).** `grep` for `SUPPORTED_VERSION|supportedVersions|eventVersionFor` across `apps/pos/src` non-test: only `FiscalEventPayloadRegistry.ts:218-246` (`eventVersionFor`, flat `return 1` at `:245`) and its one call site `FiscalEventEngine.ts:572`. No TS mirror of `SUPPORTED_VERSIONS`. **A1–A14 enumeration stands.**
- **Falsifying §1.3a's "Today `Σ net_amount == net_sales`".** The sale loop buckets *every* line including `tax_rate '0'` (`zReportService.ts:905-916`), and `net_sales = Σ receipt.subtotal = Σ line_total` — the identity holds exactly, independent of discounts. **Stands.**
- **Re-deriving all three round-2 citation corrections.** `StrictCanonicalParser.php:628` (`in_array`, block `628-634`); `ZReportProjection.php:154` (`vat_breakdown`), `:149` (`net_sales`), both from `$payload['receipt_totals']`; `Nf525DataProvider.php:1247` (`mapCanonicalZReportGrandTotal`), `:1260` (`vat_breakdown`). Also re-checked A3's anchors: `SUPPORTED_VERSIONS` `:119-127` holds **only** `SALE_RECEIPT => [1,2,3,4]` (`:126`), `supportedVersionsFor()` falls back to `[eventVersionFor()]` (`:152-155`), `PHASE_1_MAP` Z/X at `:93-94`. **All correct.**
- **Residual stale text.** `grep` for `627-632`, `1246-1281`, the old `:151` across the wave's docs and YAML (excluding the historical round‑1/2 registers): **zero hits.** No surviving claim that a v2 Z is regulator-visible.
- **Scope creep / harness violation.** Diff is 5 files, all `docs/`; no production code; tree clean; the run ended at `blocked_review` with the question recorded and `chosen_option: null`. **No violation.**

**Assessment:** the round-2 P1 and P2 were closed with real, verifiable work, and §1.3a is a genuine improvement to the ruling packet. But the correction to the NF525 argument overshot into a second false claim in the same section (finding 1), and it hides the fact that the archive-visible defect is F-2's `net_sales`, not this lane's breakdown (finding 2) — both of which land on ruling question 3 in a memo whose consumer's decision is irreversible. Findings 1–3 must land before the `fiscal-pos-reviewer` specialist gate is dispatched over this memo.

VERDICT: CHANGES-REQUIRED
