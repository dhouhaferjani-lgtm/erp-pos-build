## M1 round‑4 adversarial review — z-sale-branch-decomposition

**Range:** `9d14cb8e1afae9b9cd3e5afa9d8bfac8dd2aabd3..HEAD` (`795966c1e`, `0edbf402b`, `63b756784`, `b13536a76`, `faa0a3ec1`) — 6 files, **all under `docs/`**, verified by `git diff --name-only`. No production code, as M1 requires. `git status --porcelain` empty.

**Lens — fiscal-pos:** applicable and central; applied. I re-derived the sealed-bytes path independently of rounds 1–3 rather than trusting the memo: sale branch `apps/pos/src/lib/offline/zReportService.ts:906-916` (`lineNet = line.line_total` at `:908`, no scale arg on any `bcadd`) → `vat_breakdown` (`:995`) → close input → the three builders, `vat_breakdown` confirmed present at `zSessionAuthoring.ts:392` (X), **`:440` (SESSION_CLOSE — F-1 CONFIRMED)**, `:498` (Z). Rule 8 respected — no backfill proposed anywhere; the stop state is written.

**Standing checks:** rule 19 / red-first / tenant scoping / constructor injection / i18n / migrations / Horizon — **not applicable**; documentation-only milestone by design (brief's M1 evidence contract is "code evidence: none"). Met non-vacuously: the diff is 100 % `docs/`.

**Round‑3 disposition — the substance landed, the delivery did not.**
- **P1 (the replacement NF525 claim was also false) → CLOSED, and I confirmed the correction against code myself.** `Nf525XmlBuilder::addGrandTotals()` (`:317`, body `:319-350`) emits exactly `<VentesBrutes>` ← `periodTotals['gross_sales']` (`:335`) and `<Taxe>` ← `['tax_amount']` (`:336`); `vat_breakdown` is put into `periodTotals` at `Nf525DataProvider.php:1260` and **never rendered**. `addZReports()` emits five scalars (`:297-301`). `grep -rn "VersionEvenement" apps/api/` → the single line `:598`, inside `addQuarantineSection()`. The memo's net position is correct.
- **P2 (F-2 is the archive-visible defect) → CLOSED and verified:** `<VentesNettes>` ← `report_data['net_sales']` at `Nf525XmlBuilder.php:299`; that `report_data` is `ZReportProjection::legacyReportData()` (`:116`, `net_sales` at `:149` from `$payload['receipt_totals']`), a device-payload passthrough. F-2's chain to the JET export holds end-to-end.
- **P3 (discount caveat) → CLOSED and verified:** `receiptService.ts:537` (`line_total`, pre-transaction-discount), header-only discount at `:549`/`:555-557`, `cartTotals.ts:36`/`:66`.
- **P2 (broken Option-A cost table) → NOT CLOSED.** See finding 1.

---

## Findings register

**1. P2 — CONFIRMED — the Option-A cost table is still broken; A11–A14 still do not render as a table, and the fix commit's message asserts otherwise.**
`docs/handoff/reviews/z-sale-branch-decomposition/M1-signed-bytes-decision.md:216-221` (the blank line at `:217`).
Round 3 finding 3 offered two fixes: *"blank line before `:220` **plus a repeated header row**, or move the A10 prose below the table."* `faa0a3ec1` did **both halves of neither** — it moved the prose below (`:241`+, correct) **and** left/added a bare blank line at `:217`. `git show faa0a3ec1` shows the added empty line in the hunk. In GFM a blank line terminates the table, and the block at `:218-221` starts `| A11 | … |` with `| A12 | … |` as its second line — **no delimiter row**, so it is not a table at all: A11 (the `CashRoundingCutover` latent trap), A12 (the eight hardcoded-`1` test sites) and A13 (no Z/X golden vector exists to test "old events keep old semantics" against) render as a paragraph of raw pipes. The failure mode is identical to round 3's, only the mechanism changed (lazy continuation → orphaned block).
**Failure scenario:** the `fiscal-pos-reviewer` specialist gate — whose ruling the memo itself calls a one-way door — reads the rendered memo, sees Option A's cost table apparently terminating at A10 (an entry explicitly labelled *"NOT an Option-A cost"*), and skims or loses A11–A13 at the moment of an irreversible ruling. The brief's M1 antidote is verbatim *"both options must list **concrete files/symbols**"*; the symbols exist but three of the fourteen are delivered garbled. Aggravating: commit `faa0a3ec1`'s subject line claims *"repair the broken Option-A cost table"* and its body claims *"Prose moved below the table beside A14's"* — the register would read as closed to anyone not re-rendering the file. **Fix:** delete the blank line at `:217`, or keep it and repeat the `| # | File:line | Change |` + `|---|---|---|` header rows.

**2. P2 — CONFIRMED — two surviving cost claims still charge Option A for archive-layer work the memo just spent a page proving is zero, and both cut toward the memo's own recommendation.**
Memo `:356` (§2.3 side-by-side: *"~13 sites across device + server + **archive** + 8 test sites"*) and **`:581`** (§6 weighted reason 2: *"~13 sites across three layers **plus an archive format**, versus three expressions"*).
A9 and A10 are now explicitly *"NOT an Option-A cost"* (`:215-216`) with *"No code change"*, and the A9+A10 block concludes *"this lane's per-rate correction has ZERO NF525 export consequence, under either option"* (`:261-264`). Option A's archive-layer code cost is therefore **zero**, and the remaining real sites are A1–A3, A5–A8, A12, A13 (A4/A7 conditional, A14 not code). `:356` and `:581` are round-2-era text that the round-3 fix did not sweep.
**Failure scenario:** §2.3 is the at-a-glance comparison and §6 item 2 is one of the four weighted reasons for the memo's Option-B recommendation. A gate that reads either takes away *"Option A also costs archive-format work"* — inflating the disfavoured option in the recommendation's direction, in a packet whose two prior P1s were both NF525 mis-statements. The `~13` count is also now hard to reconcile: the enumeration is 14 rows of which three (A9, A10, A14) are declared non-costs.

**3. P3 — CONFIRMED — the absolute framing "reaches NO archive surface / carries NOTHING from this lane" over-corrects: the archived `<HashFiscal>` is a hash *over* the bytes this lane changes.**
Memo `:23` (§0 row), `:130`, `:241`, `:274`.
`ZReportProjection::rowFromPayload()` sets `'fiscal_hash' => $event->current_hash` (`apps/api/app/Modules/POS/Application/Projections/ZReportProjection.php:113`), i.e. the SHA-256 over `canonical_bytes`, which contain `vat_breakdown`; `Nf525XmlBuilder::addZReports()` emits that value as **`<HashFiscal>` (`:291`)** and `<HashPrecedent>` (`:292`). So an archive produced before and after this correction differs in the exported hash for every taxed shift, even though no *interpretable* value differs. §1.4 already establishes that the legacy `report_data` hash moves, so the memo is not self-contradictory — but the flat *"carries NOTHING from this lane"* is not literally true, and this is a memo whose NF525 claim has been overstated in both directions already.
**Failure scenario:** low. A hash is opaque and cannot differentiate conventions, so the load-bearing conclusion (*archive self-description cannot favour Option A*) is unaffected; the risk is only that a gate that checks the claim finds it false and discounts a section that is otherwise now correct. One qualifying clause — "no interpretable value; the exported `<HashFiscal>` does change, but a hash differentiates nothing" — closes it.

**4. P3 — CONFIRMED — the progress YAML again forward-references a review file that does not exist at HEAD, and `verdict_history` pre-asserts round 4.**
`docs/handoff/progress/z-sale-branch-decomposition.progress.yaml:113` (`verdict: …/M1-round4.md`), `:128-130` (`- round: 4 … result: see register`).
Round 3's P3 finding 5 flagged exactly this shape for `M1-round3.md`. The mitigation added — the inline *"if absent, round 4 did not complete — see verdict_history"* and the `verdict_history` block — makes it recoverable rather than misleading, so this is materially softer than round 3's version, but the file at HEAD still asserts a round-4 register exists when it does not. `fix_commits: [63b756784, b13536a76]` (`:112`) and `updated: b13536a76` (`:254`) both omit HEAD `faa0a3ec1` under a **declared** self-reference convention stated in-line — accepted, not a finding.
**Failure scenario:** a parent resuming off the YAML follows `verdict:` to a missing path; the inline comment now tells it what that means, so the cost is a confused read, not a wrong conclusion.

---

## Bypasses attempted that FAILED (no finding)

- **Trying to break the round-3 NF525 correction a third time (was *this* direction also wrong?).** Read `addZReports()` (`:288-302`), `addGrandTotals()` (`:317-350`) and the `Nf525GrandTotalData` feed (`Nf525DataProvider.php:1247-1281`) in full. `periodTotals` carries five keys including `vat_breakdown` (`:1260`); the builder consumes exactly `['gross_sales']` and `['tax_amount']`. `<VentilationTVA>` is per-receipt from the `SALE_RECEIPT` view, not the Z. **The correction is right; the memo's NF525 position is now verified.**
- **Falsifying F-1 (is `SESSION_CLOSE` really affected?).** `grep -n vat_breakdown apps/pos/src/lib/fiscal/zSessionAuthoring.ts` → `:392`, `:440`, `:498` — exactly the X / SESSION_CLOSE / Z builders the memo names. **Stands.**
- **Falsifying F-2 (is `net_sales` really gross?).** `cartTotals.ts:36` `subtotal = bcsum(item.line_total)`; `:40`/`:52` return `total: subtotal` verbatim with no discount; `receiptService.ts:145-158` re-sums `item.line_total` into the stored `subtotal` (`:547`); `zReportService.ts:900` `netSales = bcadd(netSales, receipt.subtotal)`. **CONFIRMED — the brief's "headline totals are correct" premise is false, and the memo is right to say so.**
- **Falsifying F-3's "not in the signed bytes".** `zReportService.ts:504` `report_data: reportData` vs `:507` `receipt_snapshots: receiptSnapshots` — siblings; `computeZReportHash` (`:445-451`) takes `reportData` only. **Stands.** The defect itself re-derived at `:1015-1017` with unscaled accumulators at `:1020-1022`.
- **Falsifying §1.1's claim that the third site is structurally different.** `reportApi.ts:490-491` binds `lineVat`/`lineNet` and **inlines** `bcadd(lineNet, lineVat)` inside the accumulator at `:496` with no `lineGross` local — exactly as the memo says. The M2/R-2 normalisation note is warranted. **Stands.**
- **Falsifying B2's "one consumer, render-only, no signed bytes".** `grep -rn buildEndOfDayPreview apps/pos/src` → one non-test call site, `EndOfDayPreviewModal.tsx:161-162`; its `vat_breakdown` is rendered at `:342-357`. **Stands.**
- **Falsifying A3 (the highest-risk Option-A line).** `FiscalEventPayloadRegistry.php` `SUPPORTED_VERSIONS` `:119-127` holds **only** `SALE_RECEIPT => [1,2,3,4]` (`:126`); `supportedVersionsFor()` falls back to `[eventVersionFor()]` (`:154`); `StrictCanonicalParser.php:627-633` is the `in_array` gate emitting `envelope_event_version_mismatch` (`:630`). The memo's `:628-634` is off by one at the block edge — immaterial, the anchor is right. **Stands.**
- **Falsifying the Option-B precedent.** `77283d2a5` exists with the stated subject; the refund branch at `zReportService.ts:867-869` matches the memo's quoted post-image (`bcabs(line_total, decimals)` / `bcsub(gross, vat, decimals)`) exactly. **Stands.**
- **Scope creep / harness violation.** Diff is 6 files, all `docs/`; no `apps/**` change; tree clean; `status: blocked_review` with the gate question verbatim in `blockers:`, `blocks: [M2, M3, M4]`, `owner_gates.z-signed-bytes-versioning` all-null pending the parent. **No violation.**

---

**Assessment.** The round-3 P1 and both P2 *substance* items were closed with real, independently verifiable work — I re-derived every NF525 citation from source and the memo's position is now correct in both directions for the first time in four rounds. What did not land is the third round-3 finding: the Option-A cost table is still split, so A11–A13 still reach the ruling gate as raw pipe text, and the fix commit's message states the opposite. Alongside that, two stale cost lines (`:356`, `:581`) still charge Option A for the archive work the same commit proved costs nothing, and both lean toward the memo's own recommendation. Neither finding touches the memo's analysis — this is a small, mechanical fix round — but both land on how an irreversible ruling packet is read, which is what the M1 gate exists to protect. Findings 1 and 2 should close before the parent dispatches the `fiscal-pos-reviewer` specialist gate over this memo.

VERDICT: CHANGES-REQUIRED
