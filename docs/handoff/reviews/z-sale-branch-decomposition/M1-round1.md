## M1 round‑1 adversarial review — z-sale-branch-decomposition

**Range reviewed:** `9d14cb8e1..HEAD` (`795966c1e` M0.1, `0edbf402b` M1.1) — 3 files, all under `docs/`. **No production code** in the range, as M1 requires. `git status --porcelain` empty (clean tree).

**Lens applied — fiscal-pos:** applicable and central. I re-derived the sealed-bytes claim end-to-end: `aggregateReportData` → `zReportService.ts:684-689` (`vatBreakdown` map) → `appendZSessionCloseAndZReport` (`zSessionAuthoring.ts:664`) → `buildSessionClosePayload` (`:684`, `vat_breakdown` at `:440`) **and** `buildZReportPayload` (`:707`, `:498`); X independently via `reportApi.ts:520-573` → `appendXReport`. Hash-chain integrity reasoning is correct (the defect is semantic, not integrity — `VerifyEventChainCommand.php:780 sealedCoordinateMismatches()` and `Nf525DataProvider.php:1190 isTrustedFiscalEvent()` re-hash and pass either way). No backfill of signed events is proposed anywhere; rule 8 respected.

**Other standing checks:** rule 19 / red-first / tenant scoping / constructor injection / i18n / migrations / Horizon queues — **not applicable**, the milestone is documentation-only by design and contains zero behavioural change. The brief's own M1 evidence contract ("code evidence: none") is met, non-vacuously.

### Verification performed (not taken from the memo)

I re-derived **31 of the memo's citations** at `base_sha`. Confirmed exactly as stated, among others: the three sale-branch defects (`zReportService.ts:908`, `endOfDayPreview.ts:302`, `reportApi.ts:492`) against the corrected refund branch (`zReportService.ts:864-876`); `vat_breakdown` present in all three payload builders; `FiscalEventPayloadRegistry.ts:245` flat `return 1`; PHP `PHASE_1_MAP` Z/X at `:93-94`; `SUPPORTED_VERSIONS` at `:119-127` holding **only** `SALE_RECEIPT`, with `supportedVersionsFor()` falling back to `[eventVersionFor()]` (`:153`) and `StrictCanonicalParser.php:627-632` rejecting on mismatch — A3's "quarantines 100 % of Z/X" is **CONFIRMED**; `validateZReportFamilyPayload` never iterating `vat_breakdown` on **either** side (`FiscalEventEngine.ts:3748-3760`, `FiscalPayloadConstraintValidator.php:764-771`); `ZReportProjection.php:174-179`'s "Z_REPORT stays v1" comment; `Nf525DataProvider.php:1260`; `CashRoundingCutover.php:44,52-55`; the A12 test sites (`FiscalEventPayloadRegistry.test.ts:80-81`, `FiscalEventPayloadRegistryTest.php:86-87`); precedent `77283d2a5`'s exact file list (**no** registry/payload/version file); D4 at `2026-05-22-codex-handover…:290`; audit R-01 at `production-v1-readiness.md:185`; the qa "NO seeder creates `pos_shifts`/`pos_receipts`" quotes; `.gitignore:58`; and every sampled masking fixture (`zReportService.test.ts:123` `line_total '42.00'` under `unit_price '50.00'`; `endOfDayPreview.test.ts:37,50,144,622`).

**F-1 CONFIRMED** (one input object feeds both `SESSION_CLOSE` and `Z_REPORT` payloads). **F-2 CONFIRMED** independently: `cartTotals.ts:36` `subtotal = bcsum(line_total)` (gross), `:40/:52` return `total === subtotal` with no transaction discount, `receiptService.ts:145-158` sums the same into the stored `subtotal` (`:547`), `zReportService.ts:900` sums it as `netSales` → signed `net_sales == gross_sales` on any taxed, undiscounted shift. The brief's "headline totals are correct" premise **is** false, and the memo is right to escalate rather than silently expand scope. **F-3 CONFIRMED** at `zReportService.ts:1015-1017`, and correctly excluded from the signed bytes (`report_data` at `:504`, `receipt_snapshots` its sibling at `:507`).

---

## Findings register

**1. P2 — CONFIRMED — Option A's cost omits the deploy-ordering constraint it creates (server-before-device, 100 % Z/X quarantine if inverted).**
`docs/handoff/reviews/z-sale-branch-decomposition/M1-signed-bytes-decision.md:151` (A3), `:229-241` (§2.3), `:461-486` (§7).
The memo states that *omitting the `SUPPORTED_VERSIONS` code change* quarantines all Z/X, but never states the **rollout** corollary: devices author locally and sync opportunistically to whatever API is deployed. **Failure scenario:** the gate rules Option A; the device build carrying v2 authoring reaches a terminal before the API carrying A3 is deployed (exactly the order R-6/LEDGER D-1 describes for device-side fixes, and the inverse of the D-3 device-before-migration obligation) → every `Z_REPORT`/`X_REPORT`/`SESSION_CLOSE` that device syncs is rejected at `StrictCanonicalParser.php:627-632` as `envelope_event_version_mismatch` and lands in `fiscal_event_quarantine` — 100 % of Z/X for that terminal, on append-only device chains that cannot be re-authored. This is a material, ruling-relevant cost of Option A (Option B has none: `bcsub` in three expressions is order-free). Per the brief's severity contract P2 is close-before-merge, not milestone-blocking — but because the ruling that consumes this memo is a **one-way door** and happens next, this register or the added paragraph must reach the `fiscal-pos-reviewer` ruling gate. Concretely: add one row to §2.1/§2.3 stating "Option A requires the API deploy to precede any device build authoring v2; inverted order quarantines 100 % of that terminal's Z/X."

**2. P3 — CONFIRMED — the recommendation is conditioned on a question the memo itself declares unanswerable, with no stated fallback.**
Memo `:430-431` recommends Option B "conditional on Q1/Q2/Q3 coming back empty", while `:307` states Q2 (*has any demo/pilot device closed a shift?*) is closable **only** by a per-terminal operator inventory and by **no** server query, citing `2026-05-14-pos-phase1-fiscal-event-engine.md:139`. **Failure scenario:** the parent runs Q1 (empty) and Q3, cannot close Q2, and the memo gives no decision rule for the expected state "Q2 unresolved" — neither the recommendation's condition nor the §6 flip condition ("Q2 returns a device with closed shifts") is satisfied, so the ruling gate is left without the memo's guidance at precisely the point it was written to serve. Fix: state the default when Q2 cannot be discharged (and who may decide to proceed on it).

**3. P3 — CONFIRMED — one citation off by one.**
Memo `:413` writes "`receipt_snapshots` is a sibling (`:505` vs `:507`)"; `report_data: reportData` is at `apps/pos/src/lib/offline/zReportService.ts:504` (`receipt_snapshots` at `:507` is right). The semantic anchor — sibling keys, `receipt_snapshots` outside the hashed `report_data` — holds. Sole citation defect found in 31 re-derived.

**4. P3 — CONFIRMED — progress-YAML bookkeeping deviates from the harness in two small ways.**
`docs/handoff/progress/z-sale-branch-decomposition.progress.yaml` last line: `updated: 9d14cb8e1` is `base_sha`, where the harness specifies `git rev-parse --short HEAD` (`795966c1e` at the time of writing). And M1 `commit: null` carries the note "filled below once the memo commit exists" — the memo commit is `0edbf402b` (self-reference was impossible at write time), so it must be filled when the status flips to `blocked_review`. Both are honestly labelled, neither is a truthfulness defect; **failure scenario** is only a stale resume marker if the wave crashes and resumes off the YAML.

**5. P3 — CONFIRMED — "identical" overstates the third site, which matters at M2 under R-2.**
Memo `:36-38` calls the three sale branches identical. `reportApi.ts:492-497` has no `lineGross` local — it inlines `bcadd(lineNet, lineVat)` inside the accumulator — where the other two bind it. Semantically identical, structurally not; R-2 requires the three M2 diffs to read as one pattern, so the M2 executor should normalise the shape rather than transcribe it.

---

## Bypasses attempted that FAILED (no finding)

- **Fabricated/inflated `event_version` enumeration:** I grepped every non-test file in `apps/api/app`, `apps/api/database` and `apps/pos/src` touching `event_version`. The four files the memo does not name — `VirtualAdminFiscalEventService.php`, `TerminalRegistrySnapshotService.php`, `PosCoreReceiptProjection.php`, `TreasuryReceiptBridge.php` — author or branch on `ACCOUNT_STATUS_CHANGED` / `DEPOSIT_RECEIPT` / `TERMINAL_REGISTRY_SNAPSHOT` / `SALE_RECEIPT` only; none is a Z/X consumer, and a v1→v2 Z cannot trip `CashRoundingCutover::applies()` (`>= 3`), which the memo already flags as a latent trap at A11. **Enumeration stands.**
- **"The memo decided instead of presenting":** §6 is explicitly advisory, states flip conditions in advance, and §8 hands four ruling questions upward. No option is foreclosed.
- **"Option B's blast-radius claim is self-serving":** `buildEndOfDayPreview` has exactly one non-test consumer (`EndOfDayPreviewModal.tsx:161-162`), so B2's "changes no signed bytes at all" is true; and the X site **does** reach signed bytes, which the memo does not hide.
- **"No signed history" overclaimed:** the memo asserts only non-demonstrability and separates §3.1 (repo facts) from §3.2 (operator queries). Grep for `Z_REPORT`/`X_REPORT` across all `*.json`/`*.snap` fixtures returns zero; seeder grep for `Z_REPORT|pos_z_reports|pos_shifts` returns zero. Both verified independently.
- **"Precedent inflated":** `77283d2a5`'s file list matches the claim exactly, and the memo itself limits the precedent's weight on blast-radius grounds.
- **Scope creep into production code:** diff is docs-only; F-2/F-3/F-4 are recorded, not acted on (rule 4).

**Assessment:** the memo meets every enumerated M1 requirement — byte-level consequence, both options costed with concrete files/symbols, version-branching consumers enumerated, history evidence real and bounded, tenant-#1 argument presented with its four limits, advisory recommendation — and it surfaces three findings (F-1 `SESSION_CLOSE`, F-2 the false headline premise, F-3 the fourth site) that the brief, the ticket and LEDGER C-2 do not carry. No P1. Finding 1 must reach the A/B ruling gate.

VERDICT: ACCEPT
