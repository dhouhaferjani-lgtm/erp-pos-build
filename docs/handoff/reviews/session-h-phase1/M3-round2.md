## M3 round‑2 adversarial register — `fix/h1-shape-neutral-cleanup` (`bdc228a18..d020cfb03`)

**Lenses.** `tenancy-authz` — APPLIES (route permission gates changed; partner code-uniqueness scope changed from tenant to company). `frontend-conventions` — APPLIES (routes, picker, form, locales, e2e). Rule 19 (money/quantity): **N/A** — no money/quantity path touched (`credit_limit` is only read as an opaque string in `partnerNature.ts:16-21`, never parsed or arithmetic'd). Migrations/queues: **N/A** — none added. Constructor injection: honoured (`CreatePartnerRequest.php:27-31`, `UpdatePartnerRequest` same pattern; no `app()`).

**Gates I ran myself:** `vitest run` over the 6 milestone FE files = **67/67 pass**; `eslint` over the 7 touched source files = **0 errors** (17 pre‑existing warnings); `tools/audit-tanstack-keys.mjs` Gate C = **0 new / 0 stale**; `audit:i18n:local` = **OK, 55 ns, 8 baseline entries burnt down, no new gaps**. Round‑1 findings 3, 4, 5, 7, 8, 11 are **fixed and verified** (evidence doc exists with all four addendum statements; screenshots at 17:09–17:12 postdate the last FE commit 4b8fbcedd at 17:08:46; the spec now creates a disposable user instead of mutating seeded `viewer@`; `nav.companies` + `vehicles:noOwner` removed; `common.clearField` interpolated in en/fr/ar; enum drift recorded in `owes_parent`).

---

### 1. **P2 — CONFIRMED** — `apps/web/src/routes/index.tsx:843-849` — the supplier list gate *replaces* the module gate, leaving four different gates on one entity and a dead-end list for 5 of 7 seeded roles
The evidence doc's ruling (that `moduleKey="purchases"` resolves to the UI alias `purchases.view`, `usePermissions.ts:29-33` → `uiAliasPermissions.ts:6`, not a vertical module gate) is **factually correct** and I am not relitigating it. The unaddressed half of round‑1 finding 1 is the *result*: the brief says "**add** `RequirePermission permission="partners.view"` **matching `/sales/customers`**", and `/sales/customers` is still `moduleKey="sales"` (`routes/index.tsx:596`). Within the same route group the suppliers surface is now gated four different ways — list `partners.view` (`:846`), `new` `purchases.create` (`:856`), detail `moduleKey="purchases"` (`:866`), edit `partners.update` (`:875`).
Failure scenario: `partners.view` is held by `accountant, admin, cashier, manager, operator, technician, viewer` (`hooks/permissionsMap.generated.ts:152`); the `purchases.view` alias only by `admin, purchases, manager` (`uiAliasPermissions.ts:6`). A **cashier, viewer, technician, accountant or operator** — 5 of the 7 seeded roles — opens `/purchases/suppliers` by bookmark/back‑nav (the Sidebar entry is still hidden from them, `Sidebar.tsx:174-178`), gets the full supplier list with balances, clicks any row, and is bounced to `/dashboard` by the unchanged detail gate. This state is now *pinned as intended* by `routes/PartnerRoutes.gates.test.tsx:64-75`. Fix preserving the ruling: `<RequirePermission moduleKey="purchases" permission="partners.view">` (both props compose, `RequirePermission.tsx:49-58`) and assert both legs in the test.

### 2. **P2 — CONFIRMED** — `apps/web/e2e/session-h/m3-dead-crm-partner-vehicle-gates.spec.ts:239-263, 266-276, 430-431, 449-450` — the restricted-user assertion reads back the mock it just installed, and the browser never authenticates as the restricted user
`useRestrictedIdentity` installs `page.route('**/api/v1/auth/me', …fulfill(fabricated body))` (`:267-273`); `restrictedPermissions` then does `window.fetch('/api/v1/auth/me')` **from the same page** (`:249`) and asserts `contains('contacts.update')` / `not.toContain('partners.update')` on the response (`:259-261`). Playwright routes intercept in‑page `fetch`, and the later‑registered handler wins over `loginPage`'s `**/api/v1/**` proxy (`e2e/session-h/helpers.ts:159-172`) — so those three assertions are evaluated against the literal object the test constructed. The permission *set* itself is real (obtained server‑side via `POST /pos/auth/verify-pin`, `:373-383`), but the in‑browser check is circular and the page still carries the **owner's** bearer token, so browser gate items (ii) and (iii) exercise only the client `RequirePermission` guard, never backend authorization. Brief §2 M3 required "log in as a user holding `contacts.update` but NOT `partners.update` … → blocked". The evidence doc discloses this honestly (`M3-implementation-evidence.md`, "Browser and consumer evidence"), which is why this is P2 and not P1 — but the gate as written is not met, and the vacuous assertion should be deleted or replaced with the pre‑injection server response.

### 3. **P3 — CONFIRMED** — production API behaviour change shipped inside an FE‑scoped milestone — `apps/api/.../CreatePartnerRequest.php:65,76` and `UpdatePartnerRequest.php:81,93`
The M3 addendum asked for a **test** ("a Vitest/PHPUnit … case where company B … creates a partner with the SAME `code`"). The lane instead changed the partner `code` uniqueness rule from `tenant_id` to `companyContext->requireCompanyId()`. The change is **correct** — the DB constraint has been `(company_id, code)` since `2025_12_30_195300_fix_multi_company_unique_constraints.php:20-23`, so validation was over‑rejecting — and it is covered (`CreatePartnerTest.php:206-257`, `UpdatePartnerTest.php:128-224`). But it relaxes a published write contract: a POST that previously 422'd now 201s for any client (POS pending‑customer create, Parties import) in a multi‑company tenant. It is not in `owes_parent`, not in the realignment log, and was not gated as an owner decision. Record it for the parent before promotion.

### 4. **P3 — CONFIRMED** — browser gate item (v) skipped, so the one behavioural change with real user impact (a9) still has zero browser evidence — `spec:486`
`test.skip(!availability.available, …)` fires because the Otospex `demo-unlimited` tenant DB is not provisioned locally (503). The brief explicitly permits this ("else skip with a named `test.skip` reason and say so in the register") and the reason is stated in the evidence doc, and `helpers.ts:66-104` correctly hard‑fails *post‑auth* discovery instead of silently skipping. Unit coverage exists (`VehicleForm.tenantScope.test.tsx:186-201` pins `type=customer` on every list request). Accepted as brief‑compliant; flagged so the parent knows the vehicle‑picker change ships unverified in a browser.

### 5. **P3 — CONFIRMED** — the state file is stale and the i18n burn‑down is not in `owes_parent` — `docs/handoff/progress/session-h-phase1.progress.yaml:46-55`
The M3 row records `commit: f91c6066c`, `updated: f91c6066c`, `status: review`, `last_verdict: CHANGES-REQUIRED`, but HEAD is `d020cfb03` — two later commits (one of them adding 97 lines of PHPUnit) are outside the row. Separately, the brief told the lane to record the i18n baseline decision in `owes_parent`; the 8‑entry burn‑down appears only in `M3-implementation-evidence.md`'s trailing section, not in the YAML the parent reads at promotion.

### 6. **P3 — CONFIRMED** — red‑first is narrated, not demonstrable from the commit graph
`M3.1`–`M3.3` and `M3.10` each commit test + implementation together and every commit body is empty; the RED/GREEN evidence lives only in the `## TDD record` section of the evidence doc (including a mutation‑based red for the update‑code case). That is the form the self‑review harness accepts, and the counts are specific enough to be falsifiable, so I am not blocking on it — but nothing in the repository independently corroborates any red run.

### 7. **P3 — CONFIRMED (no live path)** — double‑context collapse in the Type select — `PartnerForm.tsx:139-140, 530-535`
`isCustomerContext` and `isSupplierContext` are computed independently from `partnerType` **or** the pathname, so both are true when `partnerType='customer'` renders under `/purchases/suppliers`. Both option guards then evaluate false and the select offers only `""` and `both`. Reachable today only in the unkeyed BUG‑006 harness — production routes are keyed (`routes/index.tsx:847,876`) — and `__tests__/partnerListRouteType.test.tsx:289-308` now pins the safe consequence (blank value, submit produces no POST). Worth a `key`‑independent guard in Phase 2.

---

### Bypasses I attempted that FAILED to find a defect
- **Company‑scoped uniqueness making a by‑code partner lookup ambiguous elsewhere?** No — every other read is already company‑scoped: `ArApOpeningService.php:153` (`Partner::forCompany(...)->where('code', …)`), `PartnerService.php:65-67` and the `updateOrCreate` criteria at `:83-85`.
- **`requireCompanyId()` in `rules()` 500ing where the old `tenant_id` read degraded gracefully?** No new failure mode — `CompanyContextMiddleware` is in the `api` group (`bootstrap/app.php:147`) and returns typed 400/403 for any authenticated `User` without a bound company, and `PartnerController::store` already called `requireCompanyId()` before this diff.
- **Second‑company PHPUnit case vacuous?** No — `user_company_memberships.status` defaults `'active'` (`2025_11_30_106000_…:47`) so the header company resolves, and the list assertion searches `Shared Code`, a substring of *both* rows, so a company bleed would return 2 not 1.
- **Empty `partner_id` making the picker fetch `/partners/`?** No — `PartnerPicker.tsx:121-123` maps a blank string to `null` and the resolve query is `enabled`‑gated on `selectedPartnerId !== null && !== ''`.
- **A supplier/inactive legacy vehicle owner becoming unrenderable after the `partnerType="customer"` switch?** No — the selected value is resolved by id through the unfiltered `GET /partners/{id}` (`PartnerPicker.tsx:144-153`), independent of `partnerType`/`is_active`.
- **Key deletions tripping a ratchet in the wrong direction?** No — Gate C 0 new/0 stale, i18n audit OK with 8 burn‑down and 0 new gaps, eslint 0 errors.
- **Residual `/crm/companies` or `crm:companies.*` references?** None outside the three test/spec assertions; `CompanyListPage.tsx` is genuinely deleted and the lazy import removed (`routes/index.tsx:279`).
- **Type‑option scoping hiding a legitimate choice on edit?** No — `isEditing` short‑circuits both guards (`:530,533`) and `PartnerForm.test.tsx:197-208` pins all four options on the edit route.
- **New gate tests vacuous?** No — I ran them: 6 files / 67 tests green at HEAD, and `PartnerRoutes.gates.test.tsx:88-93` would render `partner list` (the deleted redirect page) rather than the dashboard fallback if the Companies route had survived.

VERDICT: CHANGES-REQUIRED
