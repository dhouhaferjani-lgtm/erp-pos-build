I reviewed the full range `60df88a01..HEAD` (28 commits) in code, ran the milestone's frontend regressions myself, and re-verified round‑2's disposition against the source rather than the report.

---

## Adversarial merge-gate register — M1 (round 3)

Range: `60df88a01..HEAD` (`95ac2f22a`). Fix round under review: `c517635cb` (code) + `95ac2f22a` (docs-only, verified via `git show --stat`). Amending authority applied: the 2026‑08‑19 re‑pin and `preflight_policy` at `docs/handoff/progress/dn-consolidation-build.progress.yaml:10-42`; the last code commit `c517635cb` is the one covered by `last_comparison` (`:37-42`), which is correct since HEAD adds no code. Brief §3 (ratified OI‑8 conditions 1–4), §5, §6, §7 unamended and binding.

**Lens applicability.** **tenancy-authz** — applies. Re-verified independently: `module:Sales` on both the new `GET /delivery-notes/uninvoiced` and the existing `POST /delivery-notes/consolidate-to-invoice`, with `uninvoiced` ordered before `{deliveryNote}` which now carries `whereUuid` (`Document/Presentation/routes.php:274-294`); the `module` alias really resolves to `RequireModule` (`apps/api/bootstrap/app.php:113`) — not a `moduleKey` alias. Four *independent* per-layer tests exist and are non-vacuous: API+Sales-off on each route (`DeliveryNoteConsolidationAccessControlTest.php:99,137`), FE route under `ModuleGuard` with the user *holding* `invoices.create` so only the module gate can block (`routes/DeliveryNoteConsolidationRoute.gates.test.tsx:29,47-55`), FE action hidden (`DeliveryNoteConsolidation.gates.test.tsx:68-88`). `UninvoicedDeliveryNoteService` is tenant+company scoped on both sides (`:65-73`). The existing `/delivery-notes` `moduleKey="inventory"` residual (F‑3) is correctly untouched. No finding. **treasury** — applies. The fix round adds no GL/payment write and no new partial-write path; the guided lane's direct `invoiced_at` write was replaced by the claim service (`InvoiceController.php:1013-1024`) and `grep invoiced_at apps/api/app` now shows the claim service as the *only* writer. No finding. **inventory-costing** — applies weakly; nothing in the range touches stock movements, WAC, or cost paths (the DN claim is a payload/marker projection only). No finding. **general** — findings below.

### Round‑2 findings — disposition (verified in code and by running the tests)

| R2 | Verdict |
|---|---|
| **1 (P2)** transient toast on the attributed consolidation refusal + a false handback sentence | **CLOSED.** `useDeliveryNotes.ts:154-158` now suppresses the toast only when `parseDeliveryNoteBillingRefusal` matches, preserving the generic branch. The regression is genuinely non-vacuous: `hooks/__tests__/deliveryNotesTenantScope.test.tsx:247-273` drives the **real** hook (only `../../api/deliveryNotes` and `sonner` are mocked, `:24-36`) and asserts both branches. The handback sentence was corrected to *"suppresses only a parsed attributed refusal while retaining the generic error toast"* (`HANDBACK-…` OI‑8 condition 1). |
| **2 (P3)** "Remove these N and retry" never resubmitted | **CLOSED.** `DeliveryNoteConsolidation.tsx:180-190` narrows the selection and then resubmits the remainder; `…billingRefusal.test.tsx:148-172` asserts `mockMutateAsync` was **last** called with `['delivery-note-3']` and that `onSuccess` fired. Backend support is real, not notional: `lockCompleteDeliveryNoteSet` narrows the claim set to DNs whose lines map to the selected order lines and refuses a straddling DN with `partial_selection_incomplete` (`SalesOrderToInvoiceConverter.php:636-700`). Human-initiated, so proposal 7 is not violated. |
| **3 (P3)** `verdict: null` while a register existed | **CLOSED.** `…progress.yaml:96-97` now carries `M1-round2.md` + `CHANGES-REQUIRED`. |
| **4 (P3)** duplicated refusal parser | **HALF-CLOSED** — see finding 1 below. |

I re-ran the milestone's frontend evidence rather than trusting the report: `deliveryNotesTenantScope`, `…billingRefusal`, `…gates`, `DeliveryNoteConsolidationRoute.gates`, `api/deliveryNotes` → **5 files / 12 tests passed**; `SalesOrderDetailPage.tenantScope.test.tsx` → **9 passed**.

---

### P3 — note / ticket (none block)

**1. The SO lane still carries its own refusal parser after the consolidation lane was extracted to a shared module.**
`apps/web/src/features/documents/sales-orders/SalesOrderDetailPage.tsx:70,74,78` vs `apps/web/src/features/documents/deliveryNoteBillingRefusal.ts` · **CONFIRMED**
`c517635cb` extracted `isRecord`/`nullableString`/`parseDeliveryNoteBillingRefusal` for the consolidation lane but left the SO copy in place (it additionally validates `reason` and reads `billed_order_line_ids`). Two parsers still decode one server contract. Failure scenario: a future server change to `details.documents[]` (e.g. a renamed `invoiced_via`) is applied to the shared module and the SO region silently degrades to `—` / *"Billing source unavailable"* on every row, which is precisely the condition‑2 attribution the ruling makes binding. Round 2's rationale (M4 deletes the consolidation page) still applies to the *other* direction, so this is a note, not a blocker.

**2. `generated.d.ts` was committed with three enums this lane does not own, and the YAML asserts the opposite.**
`packages/shared/types/generated.d.ts:842-855`, `docs/handoff/progress/dn-consolidation-build.progress.yaml:138` · **CONFIRMED (claim), PLAUSIBLE (cause)**
The committed diff adds `DeliveryComplianceCode`, `PostingContext` and `PreDeliveryInvoicingPolicy` alongside this lane's four `DocumentData` fields and `DeliveryNoteBillingLane`, while `SystemAccountPurpose` at `:310` still lacks the merged‑3C `inventory_shrinkage_expense`/`inventory_gain_income`. The YAML finding states *"no unrelated enum regeneration was committed by M1"* — the diff contradicts that sentence. Two benign explanations fit (a selectively-committed transform output, or a worktree `typescript:transform` run against the symlinked/stale vendor that could not see the 3C enum); both are additive and type-only, no removals, so nothing regresses at runtime or in `tsc`. Failure scenario: a reviewer reading `findings:` to decide whether the generated artifact is safe to merge alongside the owner lane gets a false negative, and the two lanes conflict on the same generated file. Correct the sentence rather than the file.

**3. The resubmit path bypasses `selectionValidation` and has no pending guard.**
`apps/web/src/features/documents/components/DeliveryNoteConsolidation.tsx:180-190` · **CONFIRMED**
`removeRefusedDeliveryNotes` submits `remainingIds` directly, while `handleConsolidate` (`:171-178`) gates on `selectionValidation.valid`. For a subset of a validated selection this is sound (same-partner/same-currency is subset-stable), so there is no *reachable* invalid-batch submit today. What is reachable: `selectedIds` may retain ids that are no longer in the refetched `deliveryNotes` list, and those are excluded from `selectionValidation` (`:80-83`) yet still resubmitted — an exposure `handleConsolidate` already shares. Separately the button (`:329-338`) has no `disabled={consolidateMutation.isPending}`; a double-click before the region unmounts fires two submissions. Neither can produce a double invoice — the second loses the claim and returns the attributed 422 (`DeliveryNoteBillingClaimService.php:78-96`) — so this is ergonomics, not fiscal integrity.

**4. Non-race refusal codes fall back to an untranslated server-message toast.**
`apps/web/src/features/documents/components/DeliveryNoteConsolidation.tsx:158-167`, `apps/api/.../DocumentConversionController.php:84-92,313-320` · **CONFIRMED**
`PARTIAL_DELIVERY_NOTE_SELECTION_INCOMPLETE` and `CONSOLIDATION_VALIDATION_FAILED` are not matched by either parser (both key on `DELIVERY_NOTE_ALREADY_INVOICED`), so they render as `getErrorMessage(err)` — the raw English exception text, no `t()`, no inline region. Condition 1 governs the *race* refusal only, so this is outside the ratified set; but `partial_selection_incomplete` is reachable from the condition‑4 recovery flow itself, which makes it the least-polished corner of a ratified path. Related and pre-existing on the same file: `useDeliveryNotes.ts:152` `toast.success('Invoice created from delivery notes')` is a hardcoded English string (rule 11), untouched by this range.

---

### Bypasses attempted that FAILED (the build held)

- **Manufacture a red in the fix round's own evidence.** I forced all three DN Vitest files into one process (`--pool=forks --poolOptions.forks.singleFork`) and got 2 failures — `deliveryNotesTenantScope:176` and `…gates:101`. **Refuted as a build defect:** each file passes alone and all five pass under the project's default pool (12/12). The failures were cross-file mock/store bleed created by my own flag, not by the diff. Recording it because it is the kind of artefact that reads like a finding.
- **Defeat the toast suppression through the mocked-module seam.** Round 2's P2 survived because `…billingRefusal.test.tsx` mocks the whole hooks module. The new regression closes exactly that seam — it renders the real `useConsolidateDeliveryNotes` and mocks only the axios-level client and `sonner` (`:24-36`), so deleting the `if (parse… === null)` guard would fail it.
- **Find a writer that reaches invoice creation before its claims.** Repository-wide `grep invoiced_at apps/api/app`: the only production writer is `DeliveryNoteBillingClaimService` (`:66-96`, `:122-133`); the previous direct write in `InvoiceController::createDeliveryAndPost` is gone. The guided lane does call `claim()` with an already-posted invoice (`InvoiceController.php:1013-1024`), but the DN it claims was created moments earlier in the same transaction, so no competing claimant exists — and the handback's inventory states the invariant with exactly that qualifier (*"all required **existing**-DN claims"*), so there is no overstated claim to file.
- **Break the exact-N guard or reach `reserve()`/`finalise()` from outside.** Both halves remain independently count-guarded (`:112-119` marker, `:135-137` payload projection), `claim()` is the sole public entry (`:40`), and `claim()` refuses to run outside a caller transaction (`:44-46`).
- **Escape the claim through the SO partial-recovery path.** Hypothesis: `partial: true` + hand-picked `line_ids` could bill a DN whose remaining lines were never selected. Refuted — `lockCompleteDeliveryNoteSet` requires every line of each candidate DN to map to a *selected* order line and otherwise throws `partial_selection_incomplete` (`SalesOrderToInvoiceConverter.php:679-700`); the marker table is also consulted, not just the payload (`:660-676`).
- **Make the marker migration abort or double-write on an unattended `tenants:migrate`.** Refuted: `up()` short-circuits on a complete table and throws loudly on an incomplete one (`2026_08_18_000002_…php:31-39`), create+backfill share one transaction, and `chunkById(100, …, 'id')` writes only to the marker table so the cursor is stable. The residual malformed-`invoiced_at` cast risk is real but implausible in practice (every historical writer used `now()->toIso8601String()`) and is already disclosed in the handback — I agree with rounds 1–2 that it stays P3.
- **Find a precision violation in the new money path.** `deliveryNoteAggregates` casts `SUM(total)` to `TEXT` in SQL and formats with `bcformatStrict` at the *explicitly resolved* currency scale (`DeliveryNoteController.php:186-198`); `UninvoicedDeliveryNoteService` returns `numeric-string`s and resolves scale from the company's own currency (`:38-51`). The per-row `$this->scale()` used by `DocumentData::fromModel` is an unchanged pre-existing line (confirmed against the file's own diff), not something this range introduced.
- **Find a route-ordering or gating hole.** None: `uninvoiced` precedes `{deliveryNote}`, `whereUuid` constrains the wildcard, the `module` alias is genuinely registered, the FE route manifest was updated in step with `routes/index.tsx:1245-1256`, and the `accountant` seeder block plus `permissionsMap.generated.ts` remain absent from the diff (F‑1 correctly untouched).

---

Round 2's blocking P2 is closed in code with a regression that specifically defeats the seam that let it survive, and its P3 recovery gap is closed on both the client and the server. What remains is four P3 notes, one of which is a one-sentence accuracy fix in the YAML `findings:` rather than a code change. Per §6's disposition rule, P3s may ship with a recorded ticket — all four are recordable, none are merge-blocking, and the M1 named invariants (claim-before-invoice, exact‑N double guard, `claim()`-only entry, both-layer gating with four independent tests, additive self-guarding migrations, ratified OI‑8 conditions 1–4 in en+fr) are present and non-vacuous.

VERDICT: ACCEPT
