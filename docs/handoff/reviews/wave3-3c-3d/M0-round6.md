I ran the full independent verification. Findings below.

## M0 adversarial gate — round 6 register

**Scope / lens applicability.** `git diff 26b63f0ff..HEAD` = 11 files, all `docs/` + `scripts/`. No production code, no money/quantity arithmetic, no migration, no `onQueue`, no user-facing string, no container use. **Rule 19 / i18n / migrations / Horizon queues / tenancy-authz / constructor injection — N/A, verified by inspection of the diff stat and file list.** **inventory-costing** applies (D‑19 register, D‑28 sweep, GR D‑f anchor, R‑11 probe, T11c fixtures) — exercised. **fiscal-pos** applies (POS flush anchor, R‑1 cost basis, transaction depths, sealed-bytes surface) — exercised; nothing in the diff changes what hashes.

### Round‑5 required changes — all three independently verified FIXED

1. **P1‑1 (extraction not exhaustive) — CLEARED.** I wrote my own looser extractor (`[A-Za-z0-9_][A-Za-z0-9_.\-/]{2,}:\d+(-\d+)?`, no backtick or case constraints, digit-leading tokens included) over the dispatch and plan §§0–4 and diffed it against the committed CSV: **considered = 266, uncaptured = 0**, exactly matching `N_extracted=266`. I separately swept for citations with file extensions outside the script's list (`.sql`, `.sh`, …) — none exist. All ten round‑5 citations are present, including the whole `journal_entries` partial-unique table; I verified `2026_06_26_120000:45-47` → `uniq_je_source_procurement` (`…unique_journal_entries_source_procurement.php:44-48`) and the ambiguous `2026_07_12_100000` prefix (two real migrations share it; the alias at `scripts/wave3-citation-inventory.php:73` resolves to the treasury-transfer index, which is the one plan `:194` names — `…source_treasury_transfer.php:22-26`).
2. **P2‑1 (`ParapharmacySeeder`) — CLEARED.** CSV row 238 now maps `:971 → 977-979`, `anchor_kind=comment`; `ParapharmacySeeder.php:977-979` is literally the `cost_price is the WAC field read by MarginService and PostCOGSOnInvoice…` block, the reference T17's deletion makes stale. The `(comment)`/`(docblock)` annotation is consumed as a required-kind constraint (`:88-90`, enforced at `:155-157`).
3. **P2‑2 (relocations bypass the coherence gate / frozen integers) — CLEARED, and I broke it on purpose to check.** `relocationExpectation()` (`:446-469`) pins each of the 17 keys by symbol + semantic-text needle, enforced at `:149-154`. Bypass attempt: I patched a `/tmp` copy (repo untouched) to move the `DeliveryComplianceGate.php` relocation target to 412, 409, 406, 405, 400 in turn — **every one produced `unresolved=5`, `relocation_semantic_pin_mismatch`, exit 1**. A stale relocation address can no longer certify itself at the mandated pre‑M2 re-run.

### P3 notes (non-blocking)

1. **`scripts/wave3-citation-inventory.php:572-610` (`semanticAnchor()`), `:613-616` (`hasExecutableAnchor()`)** — still zero call sites anywhere in `scripts/` (grep, third round unaddressed). `executableAnchor()` is now reachable only from those two, so three dead functions. Cosmetic in a one-off analysis script.
2. **`scripts/wave3-citation-inventory.php:76`** — the lowercase-symbol and timestamp alternatives require a preceding backtick (`(?<=\`)`). Exhaustive today (0 uncaptured, verified), but an un-backticked lowercase/timestamp citation added to either source document before the pre‑M2 re-run would be silently unextracted — the failure class round 5 rated P1. Both sources are frozen inputs, so this is fragility, not an active gap.
3. **`Product.php:314-317` row** (CSV) maps to `resolveMovementUnitCost()` `322-325` with a signature-only assertion. Plan `:406`'s claim that it reads `weighted_average_cost ?? cost_price ?? '0.00'` is false on the pinned tree (`Product.php:324` = `return (string) ($this->cost_price ?? '0.00');`); the deleted arm survives only in the docblock at `:307-313`. Unchanged from round 5, low risk — the address is right, and R‑1's distinguishing test depends on `cost_price`, which is what the method actually reads.
4. **`scripts/` is outside every static-analysis gate** — `apps/api/phpstan.neon` analyses `app/` only, pint runs from `apps/api`. The two new PHP files get no pint/phpstan/preflight coverage. Mitigated by `scripts/tests/wave3-citation-inventory-test.php`; consistent with M0's "no production code" evidence row.

### Independent re-derivations (the brief's mandated antidote)

I re-derived **two writers of my own choosing** — different from round 5's — and got the evidence table's depths exactly:

- **C‑3, `DeliveryNoteService::confirm()` via `DeliveryNoteController::confirm()`**: controller `DB::transaction` at `DeliveryNoteController.php:308-331`, service call at `:330`, service opens its own `DB::transaction` at `DeliveryNoteService.php:88` → **depth 2** ✓.
- **C‑2, `ReturnNoteService::confirmWithin()` via `RefundService::cancelInvoiceWithDecision()`**: the only transaction is `RefundService::cancelInvoice()`'s arrow-fn `DB::transaction` at `:81-86`; `confirmWithin()` is called at `:237` inside `costLock->acquire()`, and `ProductCostLock::acquire()` (`:40-52`) opens **no** transaction; `confirmWithin()` (`ReturnNoteService.php:531`) opens none either; `RefundController` has zero `DB::transaction` → **depth 1, not the plan's stated 2** ✓. The evidence's disclosure is correct and load-bearing: at depth 1 a writer-tail `flushIfOutermost()` would post before `appendDecision()` at `RefundService.php:242` writes the invoice `documents` row — a live I‑1 violation. The proposed M2 remedy (inner savepoint + block-body root closure) is the right shape.
- Call-site enumeration is exhaustive for my two writers: production callers of `deliveryNoteService->confirm(` are exactly `InvoiceController.php:808` (C‑1), `:995` (C‑5), `DeliveryNoteController.php:330` (C‑3); of `confirmWithin(` exactly `ReturnNoteService.php:510` and `RefundService.php:237`.

Other claims verified against code: C‑1 frame `InvoiceController.php:802 try {` / `803 DB::transaction` / `808 DN confirm` / `818 post` / `829 close` ✓ · POS `apply()` `writePayments:457`, `redeemVouchers:458`, `earnLoyaltyPoints:466`, `applyStockMovementForLines:467`, closure ends `477` ✓ (the R3‑2 `:438`/`:459` record is correctly not used as an address) · `assertOriginalReceiptResolvableForRefundOrVoid` at `PosCoreReceiptProjection.php:617` ✓, POS movements carry `reference_type => 'pos_receipt'` (`:1881`, `:2220`) ✓, `COST_SCALE = 6` (`:157`) ✓, `pos_receipts.original_receipt_id` exists ✓, and R‑1's distinguishing test is genuinely distinguishing because `restockStock()` currently costs via `movementCostSnapshot()` → `Product::resolveMovementUnitCost()` (`:1764`) · R‑11 probe shape matches the brief's `stock_movements ⋈ document_lines ⋈ products / reason='delivery' / is_physical=false`, and `WeightedAverageCostService::recordSale` really writes `MovementReason::Delivery` + `reference_type='Document'` (`:432`, `:441`), so the SQL is not vacuous by construction — only by this dev sample, which the evidence declines to self-close · GR D‑f anchor: `referenceType: 'Document'` / `referenceId: $purchaseOrder->id` at `GoodsReceiptService.php:576-577` and `625-626`, persisted at `WeightedAverageCostService.php:278-279`, per-line identity via the two partial unique indexes at `2026_07_04_100000_create_goods_receipts_tables.php:61-69` written at `GoodsReceiptService.php:701-702` ✓ · D‑19 reconciles 10+3+4+1 = 18 with 4 assigned enumerated (rows 14–17) and the twin pair separate; I spot-checked ten row anchors against source, all correct · both Workshop tickets in `git ls-tree -r 26b63f0ff`, `6974231d0` ancestor of BASE_SHA ✓.

### Bypasses I attempted that FAILED

- **CSV is hand-edited / not reproducible** → refuted: re-ran to `/tmp`, `N_extracted=266 N_mapped=266 relocated=24 unresolved=0`, exit 0, **byte-identical** to the committed CSV; regression `PASS (266 rows)`; `--self-test` PASS.
- **Round‑5's P1‑1 fix is cosmetic / the corpus is still under-counted** → refuted by my own independent extractor: 266 considered, 0 uncaptured, and no citation forms outside the handled extension set.
- **Relocation pins are decorative; a shifted file still certifies** → refuted decisively (five mutated targets, all `relocation_semantic_pin_mismatch`, exit 1).
- **The round‑5 red state is asserted, not real** → refuted by re-running the *reverted* script (`git show e45248fdf:scripts/…` into `/tmp`, `$root` repointed): it yields exactly `N_extracted=256, extensionless=13`, and `2026_06_26_120000:45-47` is absent from that corpus. `git diff 8958d5c85 f0a74d332` is **empty** — the reapply is byte-identical to the fix, so the revert/replay is genuine, not a re-authored commit.
- **`validateDeliveryCompliance:605` was relocated onto a construct that still exists (bad mapping)** → probed and refuted: the *method* is live at `DocumentPostingService.php:625`, but its docblock `:610-616` records that the traversal moved to `DeliveryComplianceGate`, and the body `:627` now just calls `evaluate()`. The cited physical-line copy really is at `DeliveryComplianceGate.php:403`.
- **M0's title still over-claims the V‑10 fix (round‑5 P3‑3)** → refuted: `wave3-3c-3d.progress.yaml:27` no longer lists it; the diff shows it removed and V‑10 remains on M1's row.
- **The base pin was swapped to match HEAD** → refuted: `26b63f0ff` is the dispatch-ratified base, foundations are an ancestor, both tickets present; `HEAD=6dcda175a` is the disclosed metadata-only successor of the recorded milestone commit (`M0-evidence.md:233`).

**Nothing blocking remains.** The three round‑5 required changes are fixed in substance, not in prose; the `unresolved = 0` gate is fail-closed and survives adversarial mutation; the D‑28 depths reproduce independently, including the C‑2 deviation the evidence itself surfaced.

VERDICT: ACCEPT
