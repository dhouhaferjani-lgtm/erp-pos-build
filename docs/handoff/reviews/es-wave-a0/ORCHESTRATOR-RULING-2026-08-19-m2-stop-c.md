STOP C RULED (parent orchestrator, 2026-08-19): the ReceiptHashService fiscal-arm (company_id, chain_context) partition fix is PULLED FORWARD into M2 as a prerequisite of M2's own mandated contract — the brief requires GREEN on the clean two-context v3 fixture, which is unsatisfiable while the fiscal arm flattens operational + z_session into one sequence stream. M2 is authorized and required to partition the fiscal arm per (company_id, chain_context), matching the per-context head-resolution shape already used by OutboxIngestor and ZReportHashService, red-first (the existing in-range characterization at VerifyEventChainCommandTest.php:639-643 flips to asserting TRUE on the clean two-context fixture). M4's ES-09 scope is AMENDED accordingly: it covers TerminalRegistrySnapshotService and VirtualAdminFiscalEventService ONLY — it must not re-fix ReceiptHashService, but its two-context append test still asserts the full end-to-end shape including the receipt-verification arm. M2-round1 findings 1 (restore pending-seal exclusion with covering test), 3 (per-arm verdict/count attribution so a snapshot-row tamper is not misattributed to the receipt lane with count 0), and 4 (correct the false 'no controller behavior changed' evidence claim + add a test covering the live endpoint's widened is_valid verdict) are fix-before-merge in the same round. Finding 5 is recorded. Finding 6 (ReceiptReturnRefactorV3Test 2/9 pre-existing red) is NOT this wave's to fix — record it as an inherited residual with its owning-lane question flagged to the parent.
