## Adversarial register — enforcement-p3 · p3-M1 · round 3

**Scope held to:** brief §4 "3(a)" (`docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md:397-448`), gate-r1 C-2 rescope, gate-r2 R2-H-7 acceptance rule. Amending authority: none.
**Diff reviewed:** `0ca7bbb09..HEAD` (9 commits; 7 app files, 3 test files, 4 docs).

### What I independently verified as CORRECT (not findings)

- **Census completeness (§1) holds under my own sweep.** `JournalEntry::create` = 49, `JournalEntry::query()->create` = 6 (55 raw → 53 real), zero `insert/upsert/forceCreate/firstOrCreate/updateOrCreate`, zero relation-create, and the single `DB::table('journal_entries')` use is an `exists()` probe (`app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:559`). `JournalEntryStatus::Posted` assignment sites = the chokepoint (`GeneralLedgerService.php:3450`) + exactly the 6 enumerated creators. No raw write vector on `journal_lines` either. The "no draft reaches Posted except via `sealAndPersistEntry`" proof stands.
- **The "byte-identical blast radius" claim is real, and I proved it rather than took it.** `tests/Feature/Accounting/GLIntegrationTest.php:471` (`catch (\InvalidArgumentException`) and `GeneralLedgerPostEntryScaleTest.php:75` (`expectException(InvalidArgumentException)`) are absent from the diff (unmodified at base) and pass at HEAD: `OK (31 tests, 123 assertions)`.
- **All four census-derived acceptance commands reproduce, nonzero selection each:** `ChokepointUnbalancedGuardTest` `OK (2 tests, 7 assertions)`; `ShiftCashVarianceAdjustmentTest::test_an_unbalanced_adjustment_entry_is_refused_under_its_own_reason` `OK (1 test, 5 assertions)`; `DocumentConversionScenarioTest::it_pins_the_deferral_…` `OK (1 test, 1 assertion)`. Pint `{"result":"pass"}` on all 10 touched files; PHPStan L8 `[OK] No errors` on the touched `app/` files.
- **Lenses.** *treasury*: the new clause at `PostShiftCashVarianceAdjustment.php:184` is reachable (`postEntryNow`, no `afterCommit`), not shadowed (preceding clauses are `InsufficientRepositoryBalanceException` / `RepositoryFrozenException`, neither an ancestor), fail-closed, and changes only the audit reason at the same `error` level. *fiscal-pos*: no hash/seal/serialization byte changed — the throw is upstream of `$entry->update([...'fiscal_hash'...])`. *stock-gl-interaction*: `ReverseWriteOffService` verified class (b) — mirror legs (`GeneralLedgerService.php:4976-4986`) posted only via `postEntry` → chokepoint; currency passed explicitly. *inventory-costing*: no cost/WAC/movement path touched — lens does not apply beyond the stock-GL check above.
- **Rule 19/13/tenancy/migrations/queues:** no float, no `getScale()` no-arg, no `app()` in production, no migration, no new queue, cross-module import is Application→Domain which `deptrac.yaml` permits (Treasury already imports 28 Accounting Domain classes).

### Findings

**1 — P2 — CONFIRMED — `docs/handoff/reviews/enforcement-p3/M1-census.md:516` (row 3) and `:727` (R-9 ii)**
`BatchWriteOffService.php:122` is `catch (\RuntimeException)`; the chokepoint's refusal is `UnbalancedJournalEntryPostException extends \InvalidArgumentException` (`apps/api/app/Modules/Accounting/Domain/Exceptions/UnbalancedJournalEntryPostException.php:116`). That catch **cannot** intercept a balance refusal under any transaction nesting. The census nonetheless dispositions it as protected by the `$postSynchronously`/`afterCommit` deferral and states both R-9 sites are "one refactor away from becoming live swallows". The same table dispositions rows 6, 8 and 17(`:378`) correctly as "not reached — chokepoint type is not a `\RuntimeException`", so row 3 is internally inconsistent with the table's own rule. This is round-1 text that went stale when `bea3b4936` split the types — the same staleness class round 2 blocked on for the docblocks, not swept here.
*Failure scenario:* the parent actions R-9, and a follow-up lane pins a "nesting invariant" at `BatchWriteOffService` or removes the deferral believing that closes a swallow — spending work on a site that is unconditionally immune, while the register gives no warning about the change that *would* expose it (re-typing/broadening the catch). Fix: reclassify row 3 as type-mismatch/not-reached and reduce R-9 to (i) `PostGrIrOnGoodsReceipt.php:41` (`\Throwable`), whose nesting-dependence I confirmed.

**2 — P2 — CONFIRMED — `docs/handoff/progress/enforcement-p3.progress.yaml:202-208`, `:224`, `:299`**
The operative "M1 EXECUTION RECORD" block still asserts round-0 outcomes in the present tense as delivered: *"It now raises the house type; the type was made a subclass of `\InvalidArgumentException`"* (`:204-206`) — false at HEAD, `UnbalancedJournalEntryException.php:62` extends `\RuntimeException` and the chokepoint raises a **new sibling** — and *"SalesOrderToInvoiceConverter re-throws it ahead of its graceful catch"* (`:207-208`) — withdrawn; that file carries comment-only change. `:224` hands the parent **"SIX REPORTED FINDINGS"**, and the round-1 correction at `:299` says **"Total findings now 9"** while census §0/§6 carry **10** (R-1…R-10). `M1-D1` in the same block still pins the chokepoint at `:3378` (HEAD: throw at `:3418`).
*Failure scenario:* this YAML — not the census — is what the parent and the P3-M3 whole-package gate read. A reader taking the execution record at face value concludes the exception hierarchy was changed (it was reverted), that the converter carries a behavioural guard (it does not), and works a 6-item owes list against 10 open findings. A one-line "SUPERSEDED IN PART by the ROUND-1 RECORD below" header plus the count correction clears it.

**3 — P3 — CONFIRMED — census `:527` (rows 14/15) filed under R-10 (`:728`)**
`PosCoreReceiptProjection.php:506` and `ReceiptReturnService.php:516` are marked **S** (reachable, synchronous in-frame) queued-projection catchers that, on the non-retryable branch, `Log::error` and discard the GL batch while the fiscal projection continues — the exact context brief item 4 names (*"an unbalanced projection silently swallowed is the exact incident class this closes"*). They are filed under R-10, whose framing ("a fiscal imbalance reported as a client-fixable 4xx") and whose remedy ("multi-module change to HTTP error contracts") describe neither site: narrowing there means failing a POS receipt projection, a materially different ruling. The milestone's own stated standard ("fix what is reachable and provable red-first; report what is unreachable") groups them with the fixed row-1 case, not with the unreachable/HTTP ones. Item 4 is explicitly "(scoped, optional)" and "propose/implement", so **reporting rather than fixing is within scope** — but they need their own finding id and projection-discipline framing or the parent will rule on the wrong question.

**4 — P3 — CONFIRMED — census `:726` (R-4); code at `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:900,924,950`**
R-4 is the one genuine class-(c), red-at-base row: `reverseDocumentGl` creates with `'status' => JournalEntryStatus::Posted` (`:924`) and gates both the pre-flight refusal (`:902`) and `assertLegsBalance` (`:950`) off `$balanceAssertable`, knowingly sealing a one-legged entry for lineless documents. Brief item 2 says *close* class-(c) seams; this one is escalated instead, on a prior L1 gate's cancellability deferral. I agree the executor lacks authority to change cancellability under a merge gate, and round 2 already forced the honest reclassification. Flagged so it is not carried into P3-M3 as "balance invariant closed" — it needs the parent's ruling first.

**5 — P3 — CONFIRMED — `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:312`**
The new docblock says *"the ~30 `catch (\InvalidArgumentException)` blocks in `app/`"*; actual count is 23 in that literal form and 53 clauses matching `catch (…InvalidArgumentException…)`. The argument direction is unaffected; correct the number while touching the file for finding 2's sweep.

### Bypasses attempted that FAILED (claims I could not break)

- Hunted a raw-SQL / relation / mass-assignment vector that could flip `journal_entries.status` to posted outside the enumerated six + chokepoint — none exists (only read-side `DB::table` uses; no `JournalLine` update/delete call sites in `app/`). Census §1 stands.
- Tried to show the type split broke a live catcher: ran the two base-content tests that pin the bare type — 31 tests, 123 assertions green, and neither file appears in the diff. Blast-radius claim stands.
- Tried to show the new Treasury catch clause is unreachable or shadowed by an ancestor clause — it is neither, and the acceptance test reproduces green.
- Tried to find a deptrac/module-boundary break from the new cross-module exception import — `deptrac.yaml` enforces hexagonal tiers only; Application→Domain across modules is allowed and already pervasive in Treasury.
- Tried to find Rule 19 drift in the touched paths — no scale resolution added/changed; factory takes `numeric-string`; PHPStan L8 clean.
- **Not run (constraint, not a pass):** mutation testing to prove the three new tests are discriminating. Doing so requires editing the tree, which this read-only gate forbids; I relied on the recorded red pastes plus logical analysis (the shift test asserts a reason string that only the new clause can produce; the deferral test fails if the graceful catch ever swallows).

Code content of this milestone is correct, verified, and green. Both P2 findings are documentation-truth defects in the milestone's own deliverables (the census handed to the parent, and the operative progress record) — the same class the round-2 gate blocked on, left un-swept in the artifacts the parent actually consumes.

VERDICT: CHANGES-REQUIRED
