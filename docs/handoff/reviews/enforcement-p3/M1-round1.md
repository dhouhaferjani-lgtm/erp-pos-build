## Adversarial merge-gate register — enforcement-p3 · M1 · round 1

**Scope reviewed:** `git diff 0ca7bbb09..HEAD` (7 files: 3 production, 2 tests, census + progress YAML), held against §4 "3(a)" of `docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md` and the `p3-M1` milestone line. No amending ruling.

**What I verified independently and found sound** (so it is not re-litigated below): the census's structural completeness proof holds — `grep` for `'status' => JournalEntryStatus::Posted` yields exactly the 7 sites claimed (chokepoint `GeneralLedgerService.php:3448` + 6 direct creators); the negative sweep is correct (the only `DB::table('journal_entries')` use, `TreasuryReceiptBridge.php:559`, is a read-only `exists()` probe); no `insert`/`upsert`/`firstOrCreate`/`updateOrCreate`/relation-create vector exists. Rule 19 is clean (no float; test amounts are strings; no scale resolution added or changed). No migrations, no new queues, no user-facing strings. I re-ran the three named test files myself: `ChokepointUnbalancedGuardTest` `OK (2 tests, 3 assertions)`, `DocumentConversionScenarioTest` `OK (14 tests, 36 assertions)`, `GeneralLedgerPostEntryScaleTest` `OK (2 tests, 3 assertions)` — as reported. **stock-gl-interaction lens:** verified at code — `ReverseWriteOffService.php:202-209` → `reverseInventoryWriteOffEntry` posts only through `postEntry` (`GeneralLedgerService.php:4990-4995`); its `$user !== null` conditional deliberately mirrors the original's posted status, not an unreported orphan draft. **inventory-costing lens: N/A** — no WAC/stock-movement/cost-path code is touched by this diff.

---

**1 · P2 · CONFIRMED · `apps/api/app/Modules/Treasury/Application/Listeners/PostShiftCashVarianceAdjustment.php:183`**
*Deliverable D's own premise is under-censused: a genuinely swallowed unbalanced chokepoint post exists, in the exact context the brief names, and is neither fixed nor listed among §6's six reported findings.*
Brief §4 3(a) item 4 conditions the normalization on the posture diverging "**for QUEUED contexts** … an unbalanced projection silently swallowed is the exact incident class this closes." The census surveyed *creators*, never *catchers*, and picked a synchronous HTTP path whose swallow it then withdrew as non-occurring (M1-D6). Meanwhile: `handle()` calls `$this->adjustmentService->post(...)` (`:310`) → `createRepositoryAdjustmentJournalEntry`, which posts via **`postEntryNow`** (`GeneralLedgerService.php:1308` — no `afterCommit` deferral, verified), so the chokepoint's refusal is raised **inside** the listener's `try` and caught by `catch (Throwable $e)` at `:183`, becoming `refuse($event, 'exception', …, level: 'error')`.
**Failure scenario:** a shift closes with a cash variance whose adjustment entry is unbalanced → chokepoint refuses → listener logs a refusal and returns → the Z-report/shift close reports success, no GL entry exists, no retry, no dead-letter. The delivered type normalization gives this path **zero** protection, because the catch is `Throwable`, not `\InvalidArgumentException`.

**2 · P2 · CONFIRMED · `apps/api/app/Modules/Document/Domain/Services/Conversion/Converters/SalesOrderToInvoiceConverter.php:590-604`**
*The milestone's only production behaviour change beyond the exception type is unreachable on the path it targets, and no test covers it.*
`transferPrepayments()` runs inside `billingConcurrencyRetrier->run(...)` (`:165`), whose `run()` wraps the operation in `$this->db->transaction(...)` (`DeliveryNoteBillingConcurrencyRetrier.php:40`). Therefore `DB::transactionLevel() > 0` always holds when `clearCustomerAdvanceToReceivable` reaches its post (`GeneralLedgerService.php:1749` → `:96-110`), so the post is **always** deferred via `DB::afterCommit` and the throw lands outside the `try`. The census's own red trace (§5.4, the `DatabaseTransactionRecord` frame) is the proof.
**Failure scenario:** the new `catch (UnbalancedJournalEntryException) { throw $e; }` never executes in production; both the red and the green runs of `it_does_not_swallow_an_unbalanced_prepayment_gl_post` exercise the deferred path, so deleting the new catch block leaves both tests green — the test is non-discriminating for it. Per §5 working rules ("failing test first … then implementation"), the re-throw has no red-first evidence; the pasted red proves only the *type* change.

**3 · P2 · CONFIRMED · `docs/handoff/reviews/enforcement-p3/M1-census.md` §5.3 vs `apps/api/app/Modules/Document/Presentation/Controllers/CreditNoteController.php:406`**
*The blast-radius claim used to justify a global exception-hierarchy change is false as stated.*
§5.3 asserts "no `catch (\RuntimeException)` exists on any `createInvoiceGLEntries` / `createCreditNoteGLEntries` / `reverseDocumentGl` caller." True for *direct* callers only. Transitively: `CreditNoteController::post()` (`:374`) → `DocumentPostingService::post` → `DB::afterCommit(… event(new InvoicePosted …))` (`DocumentPostingService.php:507-518`) → `EventServiceProvider.php:95` → `InvoicePostedListener.php:30` (not `ShouldQueue`) → `createCreditNoteGLEntries` → `assertLegsBalance` (`AccountingService.php:350`); the afterCommit callback's exception propagates out of `DB::transaction`, into that controller's `catch (\RuntimeException $e)` at `:406`.
**Failure scenario:** an unbalanced credit-note GL entry previously returned `500 {code: CONFIGURATION_ERROR, message: <detail>}`; it now escapes both catches and renders the global `500 {code: INTERNAL_ERROR, message: <generic>}` envelope (`bootstrap/app.php:938-957`). Not fail-open — still a 500 — but it is a silent API-contract change on a fiscal refusal path, produced by a verification claim that was not tested transitively.

**4 · P2 · CONFIRMED · `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:304-306` (and `UnpostableDocumentGlException.php:26-27`)**
*Re-parenting invalidated an explicit in-tree design contract, which was left asserting the opposite.*
The docblock states the type "stays an unmapped `RuntimeException` (a 500 + alert), never a 422" — that choice was deliberate, so a post-seal imbalance could never be reported as a client validation error. After this diff the type is an `\InvalidArgumentException` (hence a `\LogicException`), and is now matched by the ~30 `catch (\InvalidArgumentException)` blocks in `app/` that render 400/422 `VALIDATION_ERROR`-class responses. Neither docblock was updated, and the diff carries no analysis of that direction of the blast radius (the census checked only the `RuntimeException` direction).
**Bypasses I tried that FAILED** (why this is P2 and not P1): I could not find a currently-reachable post-seal path where the new matching actually downgrades a 500 to a 4xx — all four `DocumentPostingService::post` callers (`CreditNoteController:375`, `InvoiceController:729/825/1011`, `SupplierInvoiceController:342`) catch only `\DomainException`/`\RuntimeException`; `DocumentConversionController`'s IAE catches (`:269/:354/:418`) sit on conversion, and `InvoiceToCreditNoteConverter` performs no post. Also failed: no `catch (\LogicException)` exists in `app/`; no test asserts `CONFIGURATION_ERROR`, so nothing goes red. The exposure is latent, not live — but the guardrail is gone and the file still claims it.

**5 · P3 · CONFIRMED · `SalesOrderToInvoiceConverter.php:592-596` (comment)**
The comment justifies the re-throw as preventing "the customer advance and the receivable permanently diverged." Because the post is deferred past commit (finding 2), the conversion and invoice are already committed when the refusal fires; the advance is still not cleared. What the change buys is loudness, not divergence prevention. The comment should say so.

**6 · P3 · CONFIRMED · `apps/api/app/Modules/Accounting/Listeners/PostGrIrOnGoodsReceipt.php:40-52`**
Same defect class as finding 1, unreported in §6: `catch (\Throwable)` → `Log::error`, explicitly never re-throwing, wrapped around `createGoodsReceiptGrIrEntry`. It is protected today only because `flushPendingGlPostings` runs inside `post()`'s `DB::transaction` (`GoodsReceiptService.php:393,403`), so the GR/IR post defers — i.e. the identical "protected by a transaction-nesting accident" argument the census used to justify deliverable D existing at all. Note the fail-closed twin at `GoodsReceiptService.php:409` calls the GL service directly, outside the listener. Belongs in the census's reported-findings table.

---

**Required to clear the gate:** enumerate the *catchers* of the chokepoint refusal (not just its creators) and either close or formally report findings 1 and 6; either make the converter re-throw reachable/covered or withdraw it and record the deferral as the actual mechanism (finding 2); correct §5.3's blast-radius claim to cover transitive callers and state the `CreditNoteController` response change (finding 3); update the two docblocks that now assert a false contract (finding 4).

VERDICT: CHANGES-REQUIRED
