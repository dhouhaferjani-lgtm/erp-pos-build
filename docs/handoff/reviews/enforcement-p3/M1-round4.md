## Adversarial merge-gate register — enforcement-p3 · p3-M1 · round 4

**Scope held to:** brief §4 "3(a)" (`docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md:397-448`) + the `p3-M1` milestone line (`:448`). **Amending authority: none.** Diff reviewed: `git diff 0ca7bbb09..HEAD` (HEAD `de71689ca`; 10 commits — 6 production files, 3 test files, 4 docs). Round-3 verdict was CHANGES-REQUIRED on two P2 documentation-truth findings; this round verifies their closure at code and re-sweeps for new defects.

### Round-3 dispositions — verified at code, not from the handback

| r3 | claim | my verification |
|---|---|---|
| 1 (P2) row 3 / R-9 staleness | reclassified | **CONFIRMED closed.** `BatchWriteOffService.php:122` is `catch (\RuntimeException $e)` (verified); the chokepoint type is `UnbalancedJournalEntryPostException extends \InvalidArgumentException` (`UnbalancedJournalEntryPostException.php:118`) → unreachable by type. Census `:516` now reads "n/a — TYPE MISMATCH / NOT REACHED under any nesting", the correction note at `:544-552` names the change that *would* expose it (re-typing/broadening), and R-9 (`:743`) is reduced to the one genuinely nesting-dependent site — `PostGrIrOnGoodsReceipt.php:41`, confirmed `catch (\Throwable $e)`. |
| 2 (P2) YAML execution record | superseded + corrected | **CONFIRMED materially closed** (residual in finding 1 below). `enforcement-p3.progress.yaml:185-191` carries the SUPERSEDED-IN-PART header + a true-HEAD one-liner; the two false present-tense claims are struck inline (`:197-206`); count corrected to ELEVEN (`:191`, `:245`); anchors repointed to `:3418` / `:3450` — both verified (`GeneralLedgerService.php:3418` throw, `:3450` Posted write, `sealAndPersistEntry` declared `:3379`). |
| 3 (P3) R-11 split | done | **CONFIRMED accurate at code.** `PosCoreReceiptProjection.php:506` and `ReceiptReturnService.php:516` are both `catch (\Throwable)` around `glBuffer->flushIfOutermost(contained: true)`, retryable→rethrow, else `Log::error` and the GL batch is discarded while the projection completes. `ConcurrencyFault::isRetryable()` classifies on SQLSTATE/deadlock strings only (`app/Shared/Domain/ConcurrencyFault.php:57,63-80`), so the balance refusal deterministically takes the discard branch — the finding understates nothing. |
| 4 (P3) R-4 ruling | recorded | **CONFIRMED.** Census `:735` + §0 both carry "OPEN BY PARENT RULING / must NOT be reported at M3 as balance invariant closed". Code re-verified: `AccountingService.php:902` resolves `$balanceAssertable`, gating the pre-flight throw at `:905` and `assertLegsBalance` at `:952`; the Posted create is `:926`. |
| 5 (P3) "~30" count | corrected | **Corrected, but to numbers that are themselves off — finding 2 below.** |

### Independent verification (not findings)

- **All four census-derived acceptance commands re-run by me, nonzero selection each:** `ChokepointUnbalancedGuardTest` → `OK (2 tests, 7 assertions)`; `ShiftCashVarianceAdjustmentTest::test_an_unbalanced_adjustment_entry_is_refused_under_its_own_reason` → `OK (1 test, 5 assertions)`; `DocumentConversionScenarioTest::it_pins_the_deferral_…` → `OK (1 test, 1 assertion)`. §5.8's per-test assertion split (2 + 5) reconciles exactly with the file total of 7.
- **Byte-identical blast radius re-proved, not taken:** `GeneralLedgerPostEntryScaleTest` + `GLIntegrationTest` (neither in `git diff --stat`, both still asserting the bare `\InvalidArgumentException`) → `OK (31 tests, 123 assertions)`. The only message assertions in the tree (`GeneralLedgerPostEntryScaleTest.php:76`, `GLIntegrationTest.php:472`, `ChokepointUnbalancedGuardTest.php:96`) all match the preserved string.
- **Census anchors re-verified by symbol** after the +2 docblock shift: `:421` create / `:526` assertLegsBalance / `:529` fiscal_hash for row 44; `:373` for the `AccountingService` throw site; `:427/:579/:926` Posted writes. All exact.
- **Rows 6/8/7(`:432`)/17(`:378`) "not reached" holds against BOTH throw sites, not just the chokepoint** — the table models the `\RuntimeException` refusal too (row 5 is dispositioned as reached). I traced the only production callers of `createInvoiceGLEntries`/`createCreditNoteGLEntries`/`reverseDocumentGl`: `InvoicePostedListener.php:30,32` and `DocumentPostingService.php:238`, neither reachable from delivery-note confirm, PO, POS return, or conversion (`grep` over `app/Modules/Document/Domain/Services/Conversion/` for `DocumentPostingService|InvoicePosted|->post(` → **zero hits**, so no converter posts a document).
- **Lenses.** *treasury* — the new clause (`PostShiftCashVarianceAdjustment.php:184`) sits after `InsufficientRepositoryBalanceException` / `RepositoryFrozenException` (neither an ancestor) and before `catch (Throwable)` at `:210`: reachable, unshadowed, fail-closed, changes only the audit reason at the same `error` level; `refuse()`'s `string $level = 'warning'` param (`:520`) supports the named arg. Rule 19/20 intact on the path — `getScale($repository->currency)` with an explicit currency (`:312`) and the test harness clears `CompanyContext` before dispatch (`ShiftCashVarianceAdjustmentTest.php:725`). *fiscal-pos* — the throw is upstream of the chain-sequence/`fiscal_hash` write (`:3418` vs `:3450`); no sealed byte, projection `apply()`, or queue changed. *stock-gl-interaction* — the one stock→GL catcher in the diff's blast radius (`BatchWriteOffService:122`) is immune by type and now correctly censused; `ReverseWriteOffService` remains class (b) via `postEntry`. *inventory-costing* — **N/A**: no WAC, stock-movement, or cost-path code is touched.
- **Standing checks:** no float on money/quantity (every test amount is a string literal; no scale resolution added or changed); no `app()` in production (both are in tests); no migration, no new queue, no user-facing string, no tenant-scoped query added. `pint --test` on all touched production files → `{"result":"pass"}`; `phpstan` L8 on the new/changed app files → `[OK] No errors`; `php tools/deptrac-ratchet.php` → **PASS, 176/176 held**. YAML coherent: `status: review`, `fix_rounds: 3` (≤5), `verdict: null`, `last_verdict: changes_required`.

### Findings

**1 — P3 — CONFIRMED — `docs/handoff/progress/enforcement-p3.progress.yaml:322` (and `:300`)**
The round-3 supersession sweep stopped at the ROUND-0 block. The **M1 ROUND-1 RECORD** still reads *"R-9 (PostGrIrOnGoodsReceipt:41 + BatchWriteOffService:122). Total findings now 9."* — the exact two-site R-9 composition round 3 formally retracted, and the count round 3 corrected to eleven — with no inline marker. The new header at `:186` scopes its own supersession to the round-0 block and tells the reader to *"read the M1 ROUND-1/ROUND-2/ROUND-3 RECORDS BELOW FIRST"*, i.e. it points at the block that carries the stale pair. `:300` likewise still cites the docblocks as `AccountingService:304-306` (HEAD: the contract note runs `:304-320`).
*Failure scenario:* the parent works the owes list from the record the header calls authoritative, carries `BatchWriteOffService:122` forward as an R-9 site, and a follow-up lane spends remediation effort pinning a nesting invariant on a catch that is unconditionally immune by type — the precise waste round-3 finding 1 was written to prevent, reproduced one block down in the artifact round-3 finding 2 was written to fix. Mitigated (not eliminated) by the round-3 record at `:378-384`, which a linear reader reaches later in the same file. One `>>> SUPERSEDED` line at `:322` closes it.

**2 — P3 — CONFIRMED — `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:309-311`**
The corrected counts are inflated by counting the codebase's own prose about catches as catches, and one of them is already stale at HEAD by its own method. Measured over `app/`: `catch (\InvalidArgumentException` → 23 raw, of which **3 are comment lines** (`AccountingService.php:309`, `UnbalancedJournalEntryException.php:34`, `UnbalancedJournalEntryPostException.php:41`) → **20 real catch blocks**; `catch (…InvalidArgumentException…)` → 54 raw, of which **4 are comment lines** (the three above plus `AccountingService.php:311`, added by this very fix) → **50 real clauses**. So the docblock's "53" was true only before it wrote itself, and both figures overstate the blast radius the paragraph is warning about.
*Failure scenario:* a maintainer sizing the R-10 per-site narrowing work from this docblock budgets 23 sites, greps for them, finds 20 and three self-references, and loses confidence in the surrounding contract note — the one piece of prose in the tree whose whole job is to be trusted about which type to catch. Argument direction is unaffected; only the numbers are.

**3 — P3 — CONFIRMED — `apps/api/app/Modules/Treasury/Application/Listeners/PostShiftCashVarianceAdjustment.php:190`**
The new comment cites `postEntryNow (GeneralLedgerService.php:1307)`; the call is at **`:1308`** (`createRepositoryAdjustmentJournalEntry` declared `:1225`). Census §5.5 row 1 and §5.6 both say `:1308` — so the production comment is the only place off by one.

**4 — P3 — CONFIRMED — `docs/handoff/progress/enforcement-p3.progress.yaml:177`**
`commit: bea3b4936` no longer names a tip that contains all of M1's accepted production content: `ebbfca144` and `de71689ca` both edit `app/` (the two contract docblocks and the count correction) and are descendants of it. The field was deliberately repointed to the "type-split tip" at `a81b1c07c`, when rounds 2–3 were expected to be documentation-only — that premise no longer holds.
*Failure scenario:* the gate-r5/r6/r11 evidence-binding protocol pins accepted tips by SHA. If the parent pins or cherry-picks "the M1 commit" from this field, the two docblocks that rounds 2 and 3 blocked the gate to obtain do not travel with it, and P3-M3 re-derives the whole-package register against a tree that still asserts the reverted shared-type contract. Repoint to the round-3 fix tip (or state the field's convention explicitly, since a YAML commit cannot carry its own SHA).

**5 — P3 — carry-forward, not a defect.** Two rulings are now load-bearing for the whole-package gate and must not be silently dropped at M3: **R-4** is open-by-ruling (the one class-(c) row that would be RED at base; brief item 2 says *close* class-(c) seams, and this one is escalated instead on the prior L1 cancellability deferral — a scope question already ruled at rounds 2 and 3, not relitigated here), and **R-11**'s fix lane is the post-P3 fiscal projection-discipline family with projection-error framing, not HTTP catch narrowing. Both are recorded at census `:735`/`:745` and YAML `:386-397`; M3 must reproduce them.

### Bypasses attempted that FAILED

- **Break the type split via exact-class dispatch:** searched `app/` + `bootstrap/` for `get_class($e) ===`, `$e::class ===`, and `::class => <status>` render maps — **zero hits**; `bootstrap/app.php` contains no `InvalidArgumentException` reference at all. Subclassing changes nothing.
- **Break it via a message-keyed classifier:** `ConcurrencyFault::classify()` keys on `DeadlockException`, `causedByConcurrencyError`, and SQLSTATE `40001/40P01/25P02/25006` — the byte-identical unbalanced message matches none, so the POS projections' retryable branch is not accidentally taken.
- **Falsify row 3's new "immune by type" disposition** by finding a `\RuntimeException`-family balance refusal reachable from `BatchWriteOffService` — `assertLegsBalance` is reachable only from `InvoicePostedListener` and `DocumentPostingService::post/cancel`, neither on that path.
- **Falsify "not reached" on rows 6/7(`:432`)/8/17(`:378`)** by finding a transitive route to the `\RuntimeException` throw (the route that made round-1 finding 3 real for `CreditNoteController`) — no converter posts a document, and no delivery-note/PO/POS-return path reaches the three `AccountingService` GL methods.
- **Find a boundary or style regression** from the new cross-module exception import — deptrac ratchet 176/176 PASS, pint pass, PHPStan L8 clean.
- **Find Rule 19/20 drift on the touched queued path** — explicit-currency `getScale($repository->currency)`, no bare no-arg resolve, `CompanyContext` cleared before dispatch in the acceptance test.
- **Not run (constraint, not a pass):** mutation testing to prove the three new tests discriminate. It requires editing the tree, which this read-only gate forbids. I relied on the recorded red pastes (§5.7 carries the verbatim `"reason":"exception"` payload) plus reachability analysis: the shift test asserts a reason string only the new clause can emit, and the deferral test goes red the moment the graceful catch at `SalesOrderToInvoiceConverter.php:589` starts swallowing.

**Assessment.** Both round-3 P2 findings are closed at code, not merely at prose, and I could not break the delivery on seven attempted bypasses. The code content is unchanged this round except one docblock count. All five findings are P3 documentation-truth residue in the milestone's own artifacts — worth sweeping before M3 consumes them, none of them a merge blocker under the milestone's own acceptance contract (census-derived commands, nonzero selection asserted, red-first evidence preserved, zero class-(c) guards correctly justified).

VERDICT: ACCEPT
