## Adversarial merge-gate register — P2-M3, round 4

**Scope reviewed:** brief §3 "2(a)" + the `p2-M3` milestone line (`docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md:306-317,391`) and the M3 YAML title (`docs/handoff/progress/enforcement-p2.progress.yaml:256`). **Amending authority: none.** Round-4 delta = `9364f6eca` + `755be99aa`; M3's whole footprint is four docs/YAML files (`git diff --name-only 0fce1206d~1..HEAD`), no code.

**Lens — frontend-conventions:** M3 adds zero `apps/web` code, so canonical components / design tokens / RHF / `tenantScopedKey` do not apply. Where the lens bites is the i18n contract the checklist asserts to other lanes, and I verified it by execution rather than by reading: `bash scripts/i18n-baseline-authority.sh` from `apps/web` → *"i18n completeness OK — 56 namespaces … 23 ns / 1998 keys served in English"*, and mechanically `ns` = **56** (`apps/web/src/lib/i18n.ts:459`), whole-namespace `aliased` baseline entries = **23**, wired = **33**, and all twelve namespaces named at `ANNOUNCE-…:116-117` are in the 33. §3(a)'s *table* is correct; §3's *prose* is finding 2.

---

### 1. P2 — CONFIRMED — §1's headline says **three** lanes go red; its own table lists **eleven**, ten of them requiring an edit
`docs/handoff/ANNOUNCE-enforcement-p2-ci-contract-change-2026-08-19.md:19` vs the table at `:25-37`

> `## 1. ⚠️ READ THIS FIRST — three in-flight lanes will go RED without a one-line edit`

The table below it carries eleven rows (`dn-consolidation`, `es-wave-a0`, `openapi`, `l6-integration-verify`, `r2d`, `dpa-v8`, `r2f4`, `scan-vat`, `ocr`, `r2f2`, `owner-dashboard`); ten need a ceiling raise or a group disposition, one (`r2f4`) is explicitly no-action. I re-derived every row independently (`git diff --diff-filter=A --name-only dev...<branch> | grep '^apps/api/tests/Feature/'`) — all eleven reproduce exactly, and the roster is complete. `git log -S"three in-flight lanes"` → the string dates to `0fce1206d` and survived all three fix rounds; "three" is the round-0 count (`dn`, `es-wave-a0`, `dpa-wave3-3d`), and `dpa-wave3-3d` has since been retracted entirely (§10, merged into `dev`).

**Failure scenario:** the parent builds the FINAL announcement from this file — that is the milestone's whole purpose — and the single line labelled *"READ THIS FIRST"* scopes the red-on-arrival warning to three lanes. Seven lanes that will hard-fail `backend-architecture` on their next rebase are not in the headline, and the eighth named in it no longer exists as a lane. This is round-3 finding 2's exact family ("the summary that outranks the tables"), now in the most prominent heading of the deliverable itself.

### 2. P2 — CONFIRMED — §3 states a closed list of "lanes touching locales" that is two of seven, contradicted by the document's own §10 table
`ANNOUNCE-…:116-118` vs `:302-310`

> "Lanes touching locales: **ui-wave0 (16 files)** and **dn-consolidation (4 files)**."

Measured (`git diff --name-only dev...<branch> | grep -c '^apps/web/src/locales/'`): `ui-wave0` 16, `dn-consolidation` 4, **`fix/r2f2-cancel-flow-prompt` 3, `feat/scan-vat-configuration` 3, `feat/rafiq-skin-experiment` 3, `l6-integration-verify` 2, `feat/dpa-v8-supplier-goods-return` 2**. Those five counts are *already in this file* — §10's "locale files" column prints 3/3/3/2/2 for exactly those branches, and gives each of them a `§3(a) i18n` action. So the enumeration in §3 was never updated to the round-2/round-3 measurement that produced §10.

**Failure scenario:** §3 is the section titled *"The two behaviour changes that will surprise people"* — the part a lane reads to learn whether the i18n gate concerns it. Five lanes editing `apps/web/src/locales` read a sentence that says the change affects two other lanes, skip the reproduce-locally block at `:120-124`, add an English key without `fr` (or without `ar`, in any of the 33 wired namespaces), and get a red `frontend-lint` on a gate whose whole design goal was "never land cold". `git log -S"Lanes touching locales"` → `0fce1206d`; unchanged through three rounds.

### 3. P3 — CONFIRMED — §4 still says "6 steps + 1 job" after §2 was corrected to five; the round-3 fix claimed all seven findings closed
`ANNOUNCE-…:140` vs the corrected `:88` · `git log -S"6 steps + 1 job"` → `0fce1206d` only

`git diff c97e0d1ad..HEAD -- .github/workflows/ci.yml | grep '^+.*- name:'` decomposes as: 2 steps added to `backend-architecture` (*Check tests/Feature CI-lane manifest*, *Feature-lane checker liveness test*), 3 to `frontend-lint` (*Fetch the pinned i18n baseline revision*, *Run i18n completeness gate…*, *Run detector liveness suites…*) = **five**, plus the seven internal steps of the new `security-regression` job. §2 now says *"**Five** new steps … and **one new job**"*; the §4 reconciliation table fifty lines later still says *"6 steps + 1 job"*. The fix commit message states *"All seven fixed"* and `DECISION` §(95) P3-6 states the corrected count — but the duplicate in the same file was left, which is the fourth instance of the pattern the executor itself named at §(94) ("I fixed the sections it pointed *into* and left the summary that names them"). No lane action changes, hence P3, but the fix-round evidence overstates closure.

### 4. P3 — CONFIRMED — §1 justifies the ceiling with a "990-class hole" while the gate it describes prints 1114, and nothing in the announcement reconciles the two
`ANNOUNCE-…:23` ("the 990-class hole must not grow silently") vs the checker's own output

`php tools/feature-lane-manifest-check.php` → *"⚠ COVERAGE DEBT: **71** group(s) / **1114** class(es)"*, and `feature-lane-manifest.json` `debt_ceiling: 1114` (I verified both: 74 groups, 71 deferred, 0 excluded, 1329 classes total). The 990 figure comes from the `ci.yml:172-176` census comment ("990 of 1316 distinct Feature classes"), which counts classes reachable by *no* job at all — a different measure that subtracts classes individually named in the two `--filter` allowlists. Defensible, but the announcement prints one number as the motivation and the CI step prints another, with no sentence connecting them; a lane comparing the two has no way to tell whether the guard under-counts or the prose does.

---

### Bypasses I tried that FAILED — the round-3 fixes hold under independent derivation

- **The §10 inclusion rule, executed rather than trusted.** `git branch --format='%(refname:short)'` → drop `merge-base --is-ancestor <b> dev` → drop `*-pre-repin*`/`*-pre-rewrite`/`backup/*`/`triage/*`/`worktree-agent-*` → drop `codex/enforcement-p2-ci-guards` yields **exactly 20 branches**, matching §10's 15 individual rows + 5 grouped rows with no omission and no extra. The rule is now complete and self-applying, including the fourth filter round 3 caught.
- **The three stated edges.** `factory/board` really is an orphan (`git ls-tree -r` = **21 files**, nothing under `tests/Feature`, `apps/web/src/locales` or `.github/workflows`) — the "assessed by direct inspection" claim is exact. `l6-integration-verify` really does have multiple merge bases.
- **Every §1 measurement re-derived** with `--diff-filter=A` against `dev`: `dn` 9 `Document`, `es-wave-a0` 6 `Fiscal`, `openapi` 6 `OpenApi`, `enforcement-p1` **0**, `l6` 2/4/2, `scan-vat` 1 `Taxation`, `dpa-v8` 2 `Inventory`, `ocr` 1 `Inventory`, `r2d` 2 `Modules`, `r2f2` 1 `Document`, `owner-dashboard` 1 `Seeders`, `r2f4` 2 `Accounting`. All eleven match, and every printed ceiling matches the manifest (`Document` 65, `Fiscal` 73, `Http` 2, `Modules` 53, `Tenant` 29, `Inventory` 105, `Taxation` 31, `Seeders` 26).
- **The `r2f4` "nothing to do" row — the most plausible place for a wrong exemption.** Verified in code: the ceiling test is gated on `$isDeferred || $isExcluded` (`apps/api/tools/feature-lane-manifest-check.php:331-346`), and `Accounting` carries `lane: treasury-spine-pgsql/feature-accounting` with no count assertion anywhere. A laned group's growth genuinely cannot trip the gate.
- **§8 item 0 re-baseline**, recomputed: `Inventory` 105 → **106** on `dev`, `CountryDefaults` 27 → **28**, base `debt_ceiling` 1114 → 1116. Exactly two deferred groups drifted; the "don't pre-raise in the candidate" rationale holds (the test is `>`-only, so a pre-raise is unreportable slack).
- **Six writers / four `needs:` rewriters**, re-derived from branch tips vs `dev`: writers = P2, `ui-wave0`, `dn`, `es-wave-a0`, `openapi`, `enforcement-p1`; `needs:` rewriters = P2, `ui-wave0`, `openapi`, `enforcement-p1`. §4's table carries all six.
- **§(81) Option 1**, re-derived per job from the parsed `if:` set: jobs admitting PR→dev = `backend-lint`, `chokepoint-gate`, `backend-analyse`, `backend-architecture`, `security-regression`, `backend-test-pgsql`, `t6-phase0b-pgsql`, `treasury-spine-pgsql`, `frontend-lint`, `frontend-typecheck`, `pos-test`, `types-drift` = **twelve**; gated off = `backend-test`, `frontend-test`, `frontend-build`; aggregate `needs` = **thirteen**. There is no thirteenth PR→dev job. The aggregate's `if:` is byte-identical to base — the proposal really is a proposal, and the only `needs` edit is `+security-regression`.
- **§1a's remediation instruction, checked against the checker rather than the prose:** `selector` is the only mandatory lane field (`:360-363`); `job` and `runs_on_pr_dev` are verified when present; `step` is optional. A lane following §1a verbatim passes.
- **§2/§7 mechanics:** all five step names and the `security-regression` job id match `ci.yml` verbatim; `test:eslint-rules` = **6** suites (was 3), `test:tools` = exactly `vitest run tools/__tests__` running **7** files; `git rev-parse HEAD:apps/web/tools/i18n-completeness-baseline.json` == `cb618c12c:…` == `da151bbc51edcd066d2153b51a1df8ae1ab5bd32` == the YAML mirror (`:60`); `ci-pin/enforcement-p2-r1` matches `ci.yml:998` and the YAML pin, and no `ci-pin/*` tag exists yet (owner-created at promotion, correct).
- **§3's "reproduce locally" claim:** `scripts/preflight.sh:189-214` really does add all four of steps 2–5, and `audit:i18n:local` → `scripts/i18n-baseline-authority.sh` really does re-derive the blob without the repository variable. I ran it: green.
- **2(a) still verify-only:** `grep -c 'route-manifest-drift\|check-manifest-drift' .github/workflows/ci.yml` → 0; the round-4 delta touches three docs files. No drift job, no manifest regeneration, no `gen-route-manifest.mjs` edit.
- **Process hygiene:** `status: review`, `fix_rounds: 3` (cap 5), `commit: 9364f6eca`, `verdict: …/M3-round3.md`, `last_verdict: CHANGES-REQUIRED`, recorded in a separate metadata commit (`755be99aa`, YAML only); `git status --porcelain` empty; no parent register committed into the candidate.

### Standing checks
Rule 19 (float on money/quantity, injected scale resolver with explicit currency), tenant scoping, constructor injection, en+fr on user-facing strings, migrations, Horizon queue coverage, red-first evidence: **not applicable** — the round-4 delta is two markdown files and one YAML, no PHP/TS, no behavioural change (the only `apps/web` source touched anywhere in the range, `src/features/treasury/statements/api.ts:113-119`, is an M1 type-reference swap with no `any` and no money path). The milestone's own named invariants — 2(a) verify-only per the M0 snapshot, the aggregate as a proposal only, and one actionable line per open lane — are present and non-vacuous; §10 satisfies the per-lane invariant in full. Findings 1 and 2 are stale enumerations in the two sections a reader hits *before* §10, not absence of the work.

VERDICT: CHANGES-REQUIRED
