## Adversarial merge-gate register — P2-M3, round 5

**Scope reviewed:** brief §3 "2(a)" + the `p2-M3` milestone line (`docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md:306-317,391`) and the M3 YAML title (`docs/handoff/progress/enforcement-p2.progress.yaml:256`). **Amending authority: none.** Round-5 delta = `2f7798975` + `18379edfe`; M3's whole footprint is four docs/YAML files (`git diff --name-only 0fce1206d~1..HEAD`), no code.

**Lens — frontend-conventions:** M3 authors zero `apps/web` code (the range's only web source change, `apps/web/src/features/treasury/statements/api.ts:113-119`, is M1's `Pick<OffsetPaginationMeta,…>` swap — no `any`, no money path), so canonical components / design tokens / RHF / `tenantScopedKey` do not apply. Where the lens bites is the i18n contract this checklist asserts to other lanes, and I verified it by execution: `pnpm audit:i18n:local` → *"56 namespaces … 23 ns / 1998 keys served in English"*; mechanically `ns` = **56** (`apps/web/src/lib/i18n.ts`), whole-namespace `aliased` baseline entries = **23**, wired = **33**, and all twelve namespaces named at `ANNOUNCE-…:123-124` are in the 33. §3(a)'s *table* is correct; §3's *lane list* is finding 1.

---

### 1. P2 — CONFIRMED — the seven locale counts are **changed**-file counts, presented as "same measurement" as a stated rule that counts **added** files only — and the header explicitly retracts the method actually used
`docs/handoff/ANNOUNCE-enforcement-p2-ci-contract-change-2026-08-19.md:126` vs `:8-15`, `:291-292`, and the §10 locale column `:309-322` · restated in `DECISION-…:1952`

The file states one method three times: *"impact counted as **added** files against `dev` (`git diff --diff-filter=A --name-only dev...<branch>`)"* (`:10-11`), *"measure impact as **added** files against `dev` … not changed files"* (`:291-292`), and in the decision record *"impact as **added** files against `dev`"* (`DECISION-…:1952`) — and at `:13-15` it retracts `--name-only` without `--diff-filter=A` as one of *"three earlier methods … that must not be re-used"*. Round 4's fix then wrote at `:126`: *"Lanes touching `apps/web/src/locales` — SEVEN, not two **(counts from §10, same measurement)**"*.

Executed, not trusted: `git diff --diff-filter=A --name-only dev...<branch> | grep -c '^apps/web/src/locales/'` returns **0 for all 20 roster branches** — no lane adds a locale file. The 16/4/3/3/3/2/2 counts reproduce only under `git diff --name-only` (changed files): `ui-wave0` 16 (14 `M` + 2 `D` — `en|fr/marketing.json`), `dn` 4, `r2f2` 3, `scan-vat` 3, `rafiq` 3, `l6` 2, `dpa-v8` 2. The roster of seven and every count are **substantively right** (changed-files is the correct measure for an i18n gate — adding a key modifies an existing file), which is why this is P2 and not P1: the defect is that the document's stated, self-declared-falsifiable method does not reproduce its own column, and the round-4 sentence newly asserts that it does.

**Failure scenario:** `feat/scan-vat-configuration` reads §3 — the section a lane reads to learn whether the i18n gate concerns it — takes the file at its word that the counts come from the §10 rule, runs the printed `--diff-filter=A` command to confirm its own row before rebasing, gets **0**, concludes §3(a) is not for it, skips the reproduce-locally block at `:132-137`, adds an English key without `fr`, and reddens `frontend-lint` on a gate whose stated design goal is "never land cold". The same reader, checking §10's table by the rule §10 states, finds the entire locale column contradicted. One clause fixes it (*"locale impact is counted as **changed** files — `git diff --name-only` — because adding a key modifies an existing bundle; the added-file rule applies to Feature classes"*).

### 2. P3 — CONFIRMED — the new 990-vs-1114 reconciliation states a mechanism that is wrong for 13 of the 124 classes and mixes two counting bases; the arithmetic works by coincidence
`ANNOUNCE-…:39-44`

> "The `ci.yml` census comment says **990** = classes reachable by **no CI job at all**, which subtracts the ~124 individually named in the two `--filter` allowlists."

Measured: the union of the two allowlists (`ci.yml:733`, `:830`) is exactly **124** distinct names — but only **111** of them live in deferred/excluded groups. The other 13 (`CashPositionEndpointTest`, `MaturingInstrumentsTest`, `UpcomingPaymentsTest`, `TreasuryReceiptBridgeNettingTest`, `VoucherLedgerTest`, …) sit in **laned** groups and are therefore not part of the 1114 to begin with. The honest decomposition: `tests/Feature` holds **1329 files / 1316 distinct names**; deferred groups hold **1114 files / 1102 distinct**; subtracting the 111 allowlisted gives **1003 files / 991 distinct** — which is the `ci.yml:177` census's 990 (±1) on the distinct basis. `1114 − 124 = 990` lands on the right number only because the file-vs-distinct error (+12) and the over-subtraction (+13) nearly cancel.

**Failure scenario:** no lane action changes (1114 *is* the enforced ceiling total, and §1 correctly names it as the number to act on), but a reader doing exactly what the paragraph invites — reconciling the two figures — computes 1003 or 991 and is left where the round-4 finding said they were: unable to tell whether the guard under-counts or the prose does.

### 3. P3 — CONFIRMED — the decision record still says **eleven** lanes where the announcement's corrected headline says **ten**, and restates the added-files method for the whole checklist
`DECISION-…:1952-1953` vs `ANNOUNCE-…:19`

§(82) — the durable ruling the parent sequences from — reads *"the ceiling remediation for the **eleven** lanes that actually add Feature classes"*. Both statements are individually defensible (11 lanes add classes; 10 need an edit, `feat/r2f4-correcting-documents` needs none because `Accounting` is laned — verified in code at `apps/api/tools/feature-lane-manifest-check.php:331-346`, the ceiling test fires only for `$isDeferred || $isExcluded`), but nothing connects them, and §(82) is the paragraph round 3 already had to correct for exactly this reason (§(94)). The same sentence carries the added-files method claim covered by finding 1.

### 4. P3 — CONFIRMED — §(81) cites a `ci.yml` line range that matches neither the candidate nor the base
`DECISION-…:1903` (*"the workflow's own comment at `:1136-1143`"*)

The skipped-`needs` reasoning comment lives at `.github/workflows/ci.yml:1240-1247` on the candidate and at `:1096-1103` at `base_sha` (`git show c97e0d1ad:.github/workflows/ci.yml | grep -n 'STRICT SUPERSET'` → 1100; the base file is 1111 lines, so `:1136-1143` never existed). The cited reasoning is correct and load-bearing for the owner proposal; only the pointer is wrong.

---

### Bypasses I tried that FAILED — the round-4 fixes hold under independent derivation

- **The §1 headline, re-counted.** The table `:27-37` carries 11 rows; exactly one (`feat/r2f4-correcting-documents`) is "nothing". **Ten** is now right, and `dpa-wave3-3d` is gone from it.
- **Every §1 measurement re-derived** with `git diff --diff-filter=A --name-only dev...<branch>`: `dn` 9 `Document`, `es-wave-a0` 6 `Fiscal`, `openapi` 6 `OpenApi`, `enforcement-p1` **0**, `l6` 2 `Http`/4 `Modules`/2 `Tenant`, `r2d` 2 `Modules`, `dpa-v8` 2 `Inventory`, `r2f4` 2 `Accounting`, `scan-vat` 1 `Taxation`, `ocr` 1 `Inventory`, `r2f2` 1 `Document`, `owner-dashboard` 1 `Seeders`. **All eleven reproduce exactly**, and every printed ceiling matches `feature-lane-manifest.json`.
- **The §10 inclusion rule, executed.** `git branch --format='%(refname:short)'` → drop `merge-base --is-ancestor <b> dev` → drop the four snapshot patterns → drop `codex/enforcement-p2-ci-guards` yields **exactly 20** branches = §10's 15 individual rows + 5 grouped, no omission, no extra. `factory/board` genuinely has no merge base; `l6-integration-verify` genuinely warns on multiple merge bases.
- **§8 item 0, recomputed against today's `dev` (`41fb478c2`):** `Inventory` 105→**106**, `CountryDefaults` 27→**28**, `Document` 65, `Fiscal` 73 unchanged. The re-baseline warning is live and correct, and the "don't pre-raise in the candidate" rationale holds (the checker fails only on `count > ceiling`, so a pre-raise is unreportable slack).
- **§2's "Five new steps … one new job" (round-4's P3-3 fix).** `git diff <base>..HEAD -- ci.yml | grep '^+.*- name:'` = 12 added step names: 2 in `backend-architecture`, 3 in `frontend-lint`, 7 internal to the one new `security-regression` job → **5 + 1**. §4's table row now agrees (`:152`).
- **§2's other counts, all verified:** `apps/web/eslint-rules/*.test.mjs` = **6** (was 3), `tools/__tests__/` = **7** files, `FeatureLaneManifestCheckerTest.php` = **46** test methods, `tests/Feature/Security` = **17** classes, `frontend-lint` and `security-regression` carry **no `if:`** (I read the job blocks, not a grep — an awk sweep initially mis-attributed `all-checks-pass`'s `if:` to `frontend-lint`; the block itself is clean).
- **The aggregate proposal really is a proposal.** The only `ci.yml` `needs` change in the whole range is `+security-regression`; `all-checks-pass`'s `if:` is byte-identical to base. `needs` = **13** entries. `security-regression` has no `if:`, a strict superset of the aggregate's, so it can never come back `skipped`.
- **§4's six writers / four `needs:` rewriters**, re-derived from branch tips: writers = P2, `ui-wave0`, `dn`, `es-wave-a0`, `openapi`, `enforcement-p1`; rewriters = P2 (`+security-regression`), `ui-wave0` (`+route-manifest-drift`), `openapi` (`+backend-openapi-contract`), `enforcement-p1` (`+backend-dpa-guard`) — and **not** `dn`/`es-wave-a0`. `ci.yml:1249` is the exact line cited.
- **2(a) verify-only, checked against the lane's own YAML rather than the prose:** `codex/ui-wave0-2026-08-11:docs/handoff/progress/ui-wave0.progress.yaml` M4 `status: passed`, `commit: 6747d9042…`, wave `status: review`; that branch carries `route-manifest-drift` (3 refs); `grep -c 'route-manifest-drift\|check-manifest-drift' .github/workflows/ci.yml` → **0** here. No drift job authored, no manifest regenerated, `gen-route-manifest.mjs` untouched.
- **§7 pins:** `git rev-parse cb618c12c:apps/web/tools/i18n-completeness-baseline.json` == `da151bbc51edcd066d2153b51a1df8ae1ab5bd32` == YAML mirror `:60`; `ci-pin/enforcement-p2-r1` == `ci.yml:998` == YAML `:61`.
- **§3's "reproduce locally" claim, executed:** `scripts/preflight.sh:189-214` really adds all four of steps 2–5, and `pnpm audit:i18n:local` runs green without any repository variable.
- **Process hygiene:** `status: review`, `fix_rounds: 4` (cap 5), `commit: 2f7798975`, `verdict: …/M3-round4.md`, `last_verdict: CHANGES-REQUIRED`, recorded in a separate YAML-only metadata commit (`18379edfe`); `git status --porcelain` empty; no round-5 register committed into the candidate.

### Standing checks
Rule 19 (no float on money/quantity, injected scale resolver with explicit currency), red-first evidence, tenant scoping, constructor injection, en+fr on user-facing strings, migrations, Horizon queue coverage: **not applicable** — the round-5 delta is two markdown files and one YAML; M3 adds no PHP, no TS, no migration, no queue, no behavioural change. The milestone's own named invariants are present and non-vacuous: 2(a) is verify-only per the M0 snapshot with the dependency recorded, the `all-checks-pass` PR→dev question is a proposal with the aggregate untouched, and §10 gives every one of the 20 open lanes one actionable line, including the OpenAPI merge-order/reconciliation line (§1a + §4) and the M1 `frontend-lint` step-addition contract change (§2 rows 1–3, §3(a)). Finding 1 is a stated-method contradiction in the section a lane reads to self-select, not absence of the work.

VERDICT: CHANGES-REQUIRED
