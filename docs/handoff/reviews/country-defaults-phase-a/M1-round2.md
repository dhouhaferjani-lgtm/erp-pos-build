## M1 adversarial merge-gate review — round 2

**Wave:** country-defaults-phase-a · **Base:** `7d85232cc` · **Head:** `8b966758d` (M1 impl `07868ba12` + `6df85c92e`, fix round 1 `1a3982d3f`)
**Lens:** treasury — applied (purpose manifest ⇄ live GL throwing sites, protected treasury account literals ⇄ `InstrumentAccountResolver`, timbre capability authority ⇄ tax seeders). No tenancy-authz / fiscal-pos / frontend surface exists in this milestone (no DB, no HTTP, no user-facing strings), so those lenses are not engaged. Rule 19 is not engaged: nothing in the range touches money or quantity.

### Round-1 findings — verified closure

| # | round-1 finding | round-2 verdict |
|---|---|---|
| P1-1 | manifest citations systematically wrong, checked only for non-emptiness | **CLOSED — CONFIRMED.** All 41 citations rewritten to typed `DIRECT:`/`DYNAMIC:`/`CONDITIONAL:`/`NONE:` signatures (`ProvisioningRequiredPurposesV1.php:34-77`) and now machine-verified: `assertConforms` rejects a citation kind that contradicts the classification (`:217-225`), and `test_every_evidence_citation_resolves_to_current_source_semantics` (`ProvisioningRequiredPurposesV1ConformanceTest.php:139-180`) AST-resolves each one to the exact file:line, method, callee and enum argument, following the dynamic→resolver delegation. I re-verified a sample independently of the test: `GeneralLedgerService.php:4215/4216` (Bank/Cash match arms), `:4189`+`:4203` (GeneralExpense fallback), `:2733-2736` (voucher dynamic pair), `AccountingService.php:412/416/420/424`. The three resolution helpers all genuinely throw (`AccountingService.php:980-984`, `ChartOfAccountsService.php:72`, `GeneralLedgerService.php:4849`), so REQUIRED is sound. |
| P1-2 | frozen-seeder guard shelled out to `git show` under a depth-1 CI checkout | **CLOSED — CONFIRMED.** Replaced by three pinned SHA-256 fingerprints (`FrozenSeederDocblockTest.php:22-24`); no `shell_exec`, no git object dependency. I recomputed all three from `git show 7d85232cc:<file>`: `4e5dae62…`, `44d1d721…`, `d0f13078…` — exact match, i.e. no content drift was smuggled behind the new self-contained check. |
| P2-3 | serializer demands a unique `sort_order` no legacy source has | **CLOSED.** The uniqueness rule is spec-mandated, not an addition (spec §3.2 + R3-05: `sort_order` persisted, `UNIQUE(template_id, sort_order)`). The M4 legacy path is now pinned in production code — `CanonicalCoaSerializer::withLegacyInsertionOrder()` (`:27-35`) + `CanonicalCoaSerializerGoldenTest.php:83-99`. |
| P2-4 | capability ⇄ tax-seeder drift test was a whole-file substring conjunction | **CLOSED — CONFIRMED.** Now seeds `CountriesSeeder` + each tax seeder and asserts per-row `country_code + is_stamp_duty + is_active` against `supportsStampDuty()` (`CapabilityAuthoritySingularityTest.php:135-155`). Both the false-pass and false-fail directions are gone. |
| P2-5 | no REQUIRED-direction cross-check | **CLOSED in substance.** The citation-resolution test plus the pinned 27/1/4/9 partition (`:243-245`) plus `test_rebalancing_required_and_soft_counts_cannot_hide_direction_misclassification` (`:120-137`) now make demotion require a compensating promotion whose citation must resolve in real source. Residual noted below. |
| P2-6 | registry lacks a `requires_active` expectation | **REFUTED by the accepted spec — do not re-assert.** `admin_template_accounts` stores no `is_active`; it is a derived constant `true` and is excluded from the canonical projection (spec §3.2 / R2-11, `2026-08-08-country-defaults-super-admin-design.md:99-101`). The "publish a certified template with `is_active=false`" scenario is unrepresentable. The fix records this at `CanonicalCoaSerializer.php:37-43` and pins it with `test_template_activation_state_cannot_change_canonical_content`. |
| P3-8 | `DemoTenantSeeder` mutable typed property | **CLOSED** — `private readonly` constructor injection (`DemoTenantSeeder.php:84-86`) with a reflection test. |

### Register — round 2

**1. P2 — M1's surviving P3 findings have no carry-forward ticket, contrary to the harness gate and the M0 precedent · CONFIRMED**
`docs/handoff/SELF-REVIEW-HARNESS.md:78-79` ("P3 may ship with a ticket recorded in the brief's tree") vs `docs/superpowers/tickets/` — the only ticket is `2026-08-11-country-defaults-m0-p3-hardening.md`, written for M0's P3s. Round-1 P3-7, P3-10, P3-11, P3-12 and the un-fixed half of P3-9 are unaddressed *and* unrecorded; the fix commit `1a3982d3f` touches no ticket or report file.
**Failure scenario (concrete, near-term):** P3-7 is an M3 hazard — `CertificationScope::allowsAssignment()` throws `InvalidArgumentException` on a malformed code (`CertificationScope.php:88-90`, exercised by `CertificationScopeTest.php:76-83`). M3 wires this behind `PUT /assignments/{countryCode}`; without `AssignTemplateRequest` validating the ISO shape first, `PUT …/assignments/FRA` returns a 500 instead of 422. With no ticket, the only record is a round-2 register that M3's implementer is not obliged to read. Same for P3-9's public-API change (`provisionForCompany()` gained a `bool $failLoudOnMissingCountry` parameter — `CompanyTaxProvisioningService.php:28` — rippling through six seeders; the brief's M1 task 1 named only the registry delegation) which round 1 asked to be recorded as a decision. **Remedy is one file**, mirroring the M0 ticket.

**2. P3 — a purpose reached only through a DYNAMIC site can still be demoted REQUIRED→SOFT without a dedicated guard**
`ProvisioningRequiredPurposesV1ConformanceTest.php:247-259` checks the SOFT direction by substring over the registered list; a DYNAMIC-reached purpose (`GeneralExpense`, `SalesReturnsClearing`, `VoucherLiability`, `MarketingGoodwillExpense`, `PosTenderClearing`, `RoundingLossExpense`) never appears by name there, so that assertion can't see it. Attack is not cheap — the pinned partition forces a compensating SOFT→REQUIRED promotion whose citation must AST-resolve to a real registered site with the exact enum at the exact line — but the specific direction is guarded only indirectly.

**3. P3 — M4's obligation to route legacy definitions through `withLegacyInsertionOrder()` is a docblock promise, not an enforced contract**
`CanonicalCoaSerializer.php:17-22`. The method also unconditionally overwrites any pre-existing `sort_order` (`:31`), so calling it on already-persisted template rows silently renumbers them. M4 should assert the exporter's ordering source rather than rely on the comment.

**4. P3 — no red-first evidence for the fix round**
`1a3982d3f` lands implementation and its tests in a single commit; nothing in the commits or the progress ledger shows the new assertions failing first. I compensated by inspection and execution — every new test derives its expectation from live source (AST) or the live resolver/seeder rather than from a restated constant, so none is vacuous — but the harness's red-first rule is unevidenced for this round.

**5. P3 — `ext-intl` still undeclared** (carried from round 1)
`CanonicalCoaSerializer.php:170` hard-depends on `Normalizer::normalize()`; `composer.json` declares no `ext-*` at all. Repo-wide convention, so low risk, but a certification-critical class now depends on it.

**6. P3 — the line-keyed ratchet remains a forward merge risk** (carried from round 1; re-verified, no imminent break)
`ProvisioningRequiredPurposesV1.php:81-84` + exact-equality assert at `ProvisioningRequiredPurposesRegistrationRatchetTest.php:23`.

### Bypasses attempted that FAILED (no finding)

- **Smuggle content drift past the new self-contained freeze guard.** Recomputed all three fingerprints from the frozen bytes at `7d85232cc` — exact match. The guard no longer depends on git history, `shell_exec`, or CI checkout depth.
- **Find a still-wrong citation.** Read `GeneralLedgerService.php:4185-4220`, `:2728-2736`, `AccountingService.php:395-427` directly against the manifest — all correct, including the DIRECT-vs-DYNAMIC distinction. Whole M1 unit directory green locally: `52 tests, 687 assertions`.
- **Merge-time ratchet / partition collision with `dev`.** `origin/dev` is still exactly `7d85232cc`; local `dev` (39 ahead) touches none of `GeneralLedgerService.php`, `AccountingService.php`, `SystemAccountPurpose.php`, `InstrumentAccountResolver.php`, `ExpenseCategorySeeder.php`, and adds no `findByPurposeOrFail`/`getAccountByPurpose`/`findAccountByPurpose` call site. The exact-equality ratchet and the 41-case partition will not red on merge today.
- **Break the CI Unit lane with the new DB-backed capability test.** `--testsuite=Unit` runs on sqlite `:memory:` (`ci.yml:222-226`, `phpunit.xml:41-42`) and 114 existing `tests/Unit` files already use `RefreshDatabase`; the test seeds and passes locally.
- **Break a caller with the constructor-signature changes.** No `new CompanyTaxProvisioningService` / `new CountryTaxConfigurationRegistry` / `new ParapharmacySeeder` / `new DemoTenantSeeder` survives anywhere in `app/ database/ tests/ routes/ config/ bootstrap/` (migrations included). Production callers `CompanyController.php:175` and `TenantInitializationService.php:313` keep the pre-change non-fail-loud behavior; only seeders opt into `failLoudOnMissingCountry: true`, as before.
- **Behavior drift in the delegated timbre predicate.** `supportsStampDuty` was `MAP[…] ?? false` (TN true, FR false, unknown false); it is now `in_array(strtoupper(trim($c)), ['TN'])` — identical on every input. `TaxConfigurationController.php` is byte-unchanged (N-D holds) and pinned by the characterization test with both a real and an injected-fake authority.
- **Protected-registry drift.** `ProtectedAccountCodeRegistry` TN/non-TN/wildcard literals reproduce `InstrumentAccountResolver::accountCode()/accountType()` exactly, and the drift test derives its expectation from the live resolver by reflection rather than restating it (`ProtectedAccountCodeRegistryDriftTest.php:30-53`), same for both `ExpenseCategorySeeder` maps.
- **Static analysis / style.** PHPStan level 8 on every new and changed production path: `[OK] No errors`. Pint: `{"result":"pass"}`. (`database/seeders` reports 8 errors, but `phpstan.neon` analyses `app/` only and every one is pre-existing and unrelated — e.g. `CoffeeShopSeeder.php:298` `TenantSubscription::create` typing.)
- **Standing checks not engaged:** no float/bcmath/scale-resolver surface, no migration, no queue, no route, no translated string, no tenant-scoped query in production code; no `app()` helper anywhere in the new module.

**Blocking:** none. **Fix before merge:** finding 1 (one carry-forward ticket file covering the surviving P3s, with P3-7 explicitly flagged for M3's `AssignTemplateRequest` and P3-9's `provisionForCompany` signature change recorded as a decision). All round-1 code findings are closed and independently verified.

VERDICT: CHANGES-REQUIRED
