I ran the diff, read the M2 brief section, and verified every claim against source. Register below.

---

## M2 adversarial merge-gate review — round 4

**Scope:** brief §M2 (`docs/handoff/CODEX-DISPATCH-country-defaults-phase-a-2026-08-10.md:342-377`), diff `7d85232cc..HEAD`. Round-3 was a recorded tool error (`docs/handoff/reviews/country-defaults-phase-a/M2-round3.md`), so the code delta since round 2 is exactly one commit: `c6e1ee176` "Reject self-parent template cycles". `dc6b42edc`/`1fb13c806`/`2b92626f3` touch only progress/review docs (verified with `git show --stat`). Working tree clean.

**Lenses.** *tenancy-authz* — applies to central-connection pinning and migration targeting only; M2 ships no HTTP surface, so route middleware / `module:` gating / permission checks are genuinely N/A until M3. *treasury* — applies to the publish gate's timbre/absorber semantics only; no payment, GL posting, or partial-write path exists here. *Rule 19 — N/A:* `grep -E '\(float\)|floatval|number_format|bcadd|bcmul|CurrencyScale|QuantityScale'` over `app/Modules/CountryDefaults/` returns nothing; M2 introduces no monetary or quantity value. *Horizon — N/A* (no named queue). *en+fr — N/A* (every message is a developer-facing `DomainException`/`LogicException`).

### Round-2 disposition (verified, not taken on trust)

- **R2-1 (numeric-key coercion breaking the clone self-parent exemption) — FIXED and correct.** `(string) $code` at `TemplatePublishingService.php:341`; I re-ran the coercion myself (`php -r`: `'70' !== 70` → true; `(string) $k` → `'70'`), and confirmed `isset($insertedCodes[$parentCode])` at `:341` coerces the string lookup back to the int key, so the read side was never broken. New guard at `:283-285` rejects `parent_code === code` at publish **and** — via the shared validator called from `TemplateAssignmentService.php:100` — at assignment. Both covered non-vacuously (`TemplatePublishGateTest.php:179-188`, `TemplateAssignmentServiceTest.php:233-259`, the latter recomputing `content_hash` after the tamper so the validator, not the hash check, is what fires). Report `:1111-1126` records genuine red-first: 3 failed / 2 assertions on both engines pre-fix, plus two one-at-a-time mutation/revert cycles.
- **R2-2, R2-3, R2-4, R2-5 (carried P3s)** — correctly routed to `docs/superpowers/tickets/2026-08-11-country-defaults-m2-p3-hardening.md`; the R2-3 ticket text is now actionable and the R2-4 M5 wildcard/timbre gate is recorded there.

### Register

**1 — P2 — `apps/api/app/Modules/CountryDefaults/Application/Services/TemplatePublishingService.php:282-289` — CONFIRMED**
The round-3 fix closed the **self**-cycle (`parent_code === code`) but the publish gate still has no cycle detection of any length. `validateAccounts()` checks only "not self" (`:283`) and "parent resolves inside the template" (`:286`); `CanonicalCoaSerializer::serialize()` (`:56-88`) inspects `parent_code` but performs no reachability walk; and the composite FK `(template_id, parent_code) → (template_id, code)` (`2026_08_11_100100_create_admin_template_accounts_table.php:32-34`) is satisfied in both directions once both rows exist.

I built the cycle in a scratch SQLite database with `PRAGMA foreign_keys=ON` against the exact FK/unique shape from that migration: insert `('70', parent NULL)`, insert `('706', parent '70')`, then `UPDATE … SET parent_code='706' WHERE code='70'` — the update **succeeded**, leaving `70→706→70`. PostgreSQL's non-deferred AFTER-ROW FK trigger accepts it for the same reason. Round 2's "bypass that FAILED" entry for multi-row cycles only probed cycle construction by *insert order*; it did not probe construction by a later *update*, which is the sanctioned draft-editing path — `TemplateImmutabilityTest.php:185-205` proves draft account `create`/`update`/`delete` are all permitted inside a central transaction, and M3 puts an HTTP surface on exactly that.

*Failure scenario:* a curator reorganizing a draft moves `70` under `706` while `706` already sits under `70`. Both rows legal at the DB. `validateAccounts()` passes (neither row is its own parent; both parents resolve). `publish()` certifies the template and freezes `content_hash`/`certified_country_codes`. `assign()` re-runs the same validator and passes. Later, the only sanctioned way to correct a published template (spec §4.1: clone → edit → publish → re-point) calls `cloneInsertionOrder()`, which makes no progress on either row and throws `DomainException('Template account hierarchy contains a cycle or unresolved parent.')` (`:351-353`). The published, assigned template is permanently uncorrectable — the identical outcome R1-1 and R2-1 were each raised to close, now surviving in its general form. Forward, M5's template-backed provisioning walks this same parent hierarchy, so the gate is currently certifying a structurally unprovisionable template as safe. Fix is contained: run the existing `cloneInsertionOrder()` topological pass (or an equivalent reachability walk) inside `validateAccounts()` and reject on no-progress, with a two-row and a three-row negative test at publish and at assignment.

**2 — P3 — `apps/api/app/Modules/CountryDefaults/Application/Services/TemplatePublishingService.php:283` with `Infrastructure/Models/AdminTemplateAccount.php:63-71` — CONFIRMED, latent**
The new self-parent guard is a `===` comparison between two attributes that carry no cast (`casts()` covers `type`, `system_purpose`, `is_system`, `sort_order` — not `code` or `parent_code`). Every M2 call site hydrates rows from the database (`TemplatePublishingService.php:42`, `TemplateAssignmentService.php:99`), so both sides are strings and the guard holds today. But `validateAccounts()` is `public`, and a future caller (M3 draft validation, M4 bootstrap import) passing an **unsaved** model built with `'code' => 70` yields `70 === '70'` → `false`, silently reopening finding 1's narrower form. `(string)` both sides, exactly as `:341` already does.

**3 — P3 — `apps/api/app/Modules/CountryDefaults/Application/Services/TemplateAssignmentService.php:41-45, 110-120` — CONFIRMED**
First-time assignment for a `(country_code, domain)` pair takes no row lock, because `SELECT … FOR UPDATE` over zero rows locks nothing. Two concurrent `assign('FR', ChartOfAccounts, …)` calls therefore both see `null`, both proceed, and both reach the raw `insert()` at `:112`; the loser hits `country_template_assignments_country_domain_unique` and surfaces an untyped `QueryException` (PG `23505`) rather than a domain error. No corruption — the transaction aborts atomically and the audit row rolls back with it — but M3 will render this as a 500 instead of a conflict. The race suite covers publish-vs-assign, stale-draft-vs-publish, repoint-vs-archive and new-assign-vs-archive (`TemplateLifecycleRaceTest.php:48,81,172,205`), not assign-vs-assign.

**4 — P3 — carried forward, unchanged and correctly ticketed.** R1-5 (dead normalization branch, `CountryTemplateAssignment.php:34` — `saving` fires before the unconditional `creating`/`updating`/`deleting` throws, so the assignment can never persist; the malformed-code rejection at `:30-32` is still live). R1-6 (`CentralConnectionUnderTenancyTest` stubs `tenancy.bootstrappers` instead of exercising the real `DatabaseTenancyBootstrapper` swap prescribed at brief `:369`; it still proves the invariant that matters). R1-7 (no index on `country_template_assignments.template_id`, none on `admin_templates(domain, status)`). R1-10 residual (fixed `usleep(400_000)` handshake window). R1-11 (`outsideCentralTransaction()` leaks committed `super_admins`/`admin_audit_logs` rows). R1-12 / R2-3 / R2-4 (typed exceptions deferred to M4/M5; private `REQUIRED` constant; the M5 wildcard-vs-timbre resolver gate). All present in the hardening ticket; none blocks M2.

### Bypasses attempted that FAILED (the code held)

- **Self-parent smuggled past the round-3 fix**, at publish and at assignment, including the numeric-code coercion path — blocked at `:283-285`; the clone-recovery exemption at `:341` still lets a *legacy* self-parent row be cloned and corrected (`TemplateImmutabilityTest.php:355-365`).
- **Two-row cycle built by insert order** — genuinely blocked by the immediate composite FK (my SQLite probe: the dangling-parent insert fails). Only the *update* route works, which is finding 1.
- **Dropping a row from a clone via `$pending[$row->code]` key collapse** — unreachable: `UNIQUE(template_id, code)` makes duplicate source codes impossible, and clone reads only persisted rows.
- **Sort-order corruption through topological reordering** — `cloneInsertionOrder()` preserves each source `sort_order`, so `UNIQUE(template_id, sort_order)` and the canonical hash are unaffected (`TemplateImmutabilityTest.php:310-353`).
- **Mutating a published template's certification, content, or account rows via Eloquent** — `AdminTemplate::updating`/`deleting` plus the `lockCurrentDraft()` re-read under lock defeat both the direct and the stale-instance attack (`TemplateImmutabilityTest.php:24-71,207-227`).
- **Audit row escaping or landing outside the central transaction** — `assertAuditTransaction()` guards the same connection instance, and `AdminAuditLog` carries `CentralConnection`.
- **Archiving/deleting a referenced template, or a phantom assignment inserted after the scan** — post-lock `FOR UPDATE` recheck (`:165-169`, `:214-218`) plus the FK's `NO ACTION`.
- **Removing the pinned wildcard assignment** — `TemplateAssignmentService.php:165-167`.
- **Assigning a draft/archived/wrong-domain/out-of-scope/stale-version/tampered template** — all rejected after the locks (`:65-103`), including the `hash_equals` recompute against locked rows.
- **Central migrations leaking into tenant databases on the staging auto-deploy** — `config/tenancy.php:197` pins `tenants:migrate` to `database_path('migrations/tenant')`, and all three files additionally pin `Schema::connection(central)`.
- **`app()`, `Cache::`, or `config()` in production module code** — grep over `app/Modules/CountryDefaults/` returns nothing; constructor injection throughout, so "provisioning-relevant reads are never cached" holds trivially.
- **Rule 19 drift** — grep for float casts, `number_format`, and bcmath over the module returns nothing.

**Red-first evidence:** present and substantive for the round-3 fix (report `:1119-1126`): pre-fix red on both engines, two independent mutation/revert cycles, honestly asymmetric final counts (SQLite 41/260 + 7 skipped; PostgreSQL 48/334).

**Verification limits:** I did not execute PHPUnit (house rule forbids broad runs) and did not exercise PostgreSQL. The FK/cycle probe ran against a scratch SQLite database in `/tmp`; the PHP coercion checks ran via `php -r`. Everything else is cited to source, schema, or reproduced behaviour. The working tree is unmodified (`git status --porcelain` empty).

VERDICT: CHANGES-REQUIRED
