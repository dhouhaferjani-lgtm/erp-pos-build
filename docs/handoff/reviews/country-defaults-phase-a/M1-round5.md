# M1 adversarial merge-gate review — round 5 (final gate)

**Wave:** country-defaults-phase-a · **Base:** `7d85232cc` · **Head:** `e81f774d2`
**Range:** full `7d85232cc..HEAD`, with the round-4 delta (`e89edba80..HEAD`) read line-by-line.
**Repo untouched:** `git status --porcelain` empty; nothing modified, staged, or committed. My only writes were to `/tmp` (base-tree deptrac reproduction).

**Lens: treasury — applied** (protected treasury instrument literals ⇄ `InstrumentAccountResolver`; purpose manifest ⇄ live GL/Treasury/Expense/Inventory throwing sites; stamp-vs-rounding absorber selection; timbre capability authority ⇄ tax seeders). **Not engaged, with reason:** tenancy-authz (no table, route, middleware, connection, or permission in range), fiscal-pos (no hash chain, sealed bytes, projection — the one fiscal touch is a *test* asserting a document is refused **before** sealing), frontend (no `apps/web` file). **Rule 19 not engaged:** no money/quantity value in range — zero float casts, zero `bcmath` calls, no scale resolver, no FormRequest (verified by reading every new production file). **No migration, no `onQueue`** → Horizon coverage n/a. **No user-facing string** → en+fr n/a. **No `app()`** in production code (provider uses `$this->app->bind` inside `register()`, which is the sanctioned form).

## Round-4 finding — closure

| # | round-4 finding | round-5 verdict |
|---|---|---|
| 1 (P2) | fix rounds 2 and 3 had no §9 report record; no executed mutation proof; no counts at HEAD; stale `Status:` line | **CLOSED — CONFIRMED, and independently re-verified rather than accepted.** `docs/sessions/codex-country-defaults-phase-a-report.md:584-642` (fix round 2), `:644-798` (fix round 3, incl. the `apply_patch` mutation `['TN'] → ['TN','MA']`, the verbatim red output, and the restore green), `:800-873` (fix round 4). `:7` now reads `M0 passed; M1 code green; Opus round 4 CHANGES-REQUIRED (evidence completeness only); Opus round 5 final pending`. I re-ran both engines myself at HEAD: **SQLite 65 passed / 745 assertions**, **PostgreSQL (`autoerp_country_defaults_test`, `-c phpunit-pgsql.xml`) 65 passed / 745 assertions** — byte-identical to the recorded counts. `git diff --stat 86e62e2bd..HEAD` is documentation only (3 files), so the recorded evidence is still the evidence for the code at HEAD. |

Round-4 finding 2 (the purchase/sales stamp asymmetry, P3) is routed as ticket §10 with acceptance criteria (`docs/superpowers/tickets/2026-08-11-country-defaults-m1-p3-hardening.md:190-213`). Correctly closed.

## Register — round 5

### 1. P3 — the capability ⇄ tax-seeder drift test is a snapshot, not a ratchet: it hardcodes `['TN','FR']` instead of deriving from `CountryTaxConfigurationRegistry::MAP` · CONFIRMED
`apps/api/tests/Unit/CountryDefaults/CapabilityAuthoritySingularityTest.php:139-142` vs `apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:28-36`

Round 3 closed the *capability→registry* direction (`CountryAccountingCapabilitiesServiceTest.php:26-41` now reds on any `STAMP_DUTY_COUNTRIES` edit — mechanically certain, `assertSame` over one array literal, and the private-const read is proven live by the green run). The mirror direction is still unguarded: `$seeders` is a literal two-entry map, so a lane that adds `'MA' => MoroccoTaxConfigurationSeeder::class` to the registry `MAP` and seeds an **active** `is_stamp_duty` row for MA reds nothing. `supportsStampDuty('MA')` stays `false`, so `TaxConfigurationController.php:265` refuses stamp-duty configs for a country whose seeded data already has them, and an MA credit note carrying a seeded timbre reaches `AccountingService::residualPlan` with no `SalesStampDutyPayable` account. Fails **closed** (422 unpostable / 4xx refusal, not a silent mis-posting), and v1 has exactly TN + FR, so the test is non-vacuous today — which is why this is a note, not a fix-before-merge. **One-line remedy for the ticket:** derive the iterated country list from the live `MAP` by reflection so an additive registry entry cannot outrun the capability set. Not currently covered by any of the ticket's ten items.

### 2. P3 — the `UninvoicedRevenue` SOFT classification rests on two AST guards that both key on resolved class *names*, so a string-literal container resolution escapes · CONFIRMED (mechanism) / no live exposure
`apps/api/tests/Unit/CountryDefaults/ProvisioningRequiredPurposesV1ConformanceTest.php:262-303` + `apps/api/tests/Unit/CountryDefaults/ProvisioningRequiredPurposesRegistrationRatchetTest.php:85-87`

The ratchet **explicitly skips** `UninvoicedDeliveryNoteService.php`, so its throwing purpose lookup is invisible to the registration scan; the SOFT entry (`ProvisioningRequiredPurposesV1.php:77`) is therefore load-bearing on the companion caller-scan. That scan matches only `Node\Name` after `NameResolver`, so `$container->make('App\Modules\Compliance\Services\UninvoicedDeliveryNoteService')` — a `Node\Scalar\String_` — would satisfy both guards while making the dead code reachable. **No such reference exists today:** grep over `app/ routes/ config/ database/ bootstrap/` returns only the manifest's own citation and two frozen-seeder comments. Worth one sentence in the ticket so the exclusion stays a decision.

### 3. P3 — the repo's deptrac ratchet is RED, identically, at the pinned base: not this branch's debt, and M7 must not attribute it here · CONFIRMED
`apps/api/tools/deptrac-ratchet.php` via `.github/workflows/ci.yml:178`

`php tools/deptrac-ratchet.php --config=deptrac.yaml --baseline=deptrac.baseline.json` at HEAD: **111 vs baseline 99**, `BLOCKER — ModuleDomain on ModuleApplication`, `SharedContracts on ModuleDomain RATCHET (+7)`, `RESULT: FAIL`. I extracted `7d85232cc` with `git archive` into `/tmp` and ran the same command against the base tree: **byte-identical output** — same total, same blocker, same categories. The new module contributes **zero** violations (`deptrac analyse | grep -i countrydefaults` → empty), and the new `Shared/Contracts` interface has no dependencies at all. Pre-existing, matches the known dev-branch ratchet debt, and will bite at the eventual `dev → main` PR rather than at this merge.

## Bypasses attempted that FAILED (no finding)

- **Independently recompute the frozen-seeder pins.** `git show 7d85232cc:…` for all three charts → `4e5dae62…`, `44d1d721…`, `d0f13078…`; then re-hashed the *current* files in Python after stripping exactly the inserted `@deprecated` block → all three match, byte-for-byte. Content is genuinely frozen at the pinned SHA; `FrozenSeederDocblockTest.php:21-23` is honest. No `@deprecated`-driven static-analysis fallout either: no `phpstan-deprecation-rules` is installed, and `phpstan.neon:6-7` analyses `app/` only.
- **Independently recompute the canonical golden.** Rebuilt the two-row projection in Python (NFC, `ensure_ascii=False`, `(',',':')`) → `b93206e4fe2e416c36152f70c6684fd4c95a9865cbf5f1e46b4b13a9e100eb36`, exactly `CanonicalCoaSerializerGoldenTest.php:15`. Field order per spec §5.4, `is_active` genuinely absent from the projection (and pinned absent by `test_template_activation_state_cannot_change_canonical_content`), no trailing newline, the NFC case is real (`cafe\u{301}s`), `parent_id` resolved to `parent_code`, duplicate `sort_order` rejected.
- **Break the treasury protected-code registry.** `ProtectedAccountCodeRegistryDriftTest.php:29-54` derives expectations by reflecting `InstrumentAccountResolver::accountCode/accountType` rather than restating literals, across all three variants — I re-read `InstrumentAccountResolver.php:41-71` and all 9 code/type pairs per variant agree (TN `5312/5313/5314/6275/43666`, non-TN `5112/5113/5114/627/44566`, shared `403/4035/413/416`). Demo codes derive from the live `ExpenseCategorySeeder::categoriesForCountry`, so D-4's seven French-plan and four generic codes cannot drift silently.
- **Find a second timbre authority.** Swept every `'TN'` literal in `app/` (40 hits): chart-plan selection, bank/tax-ID validation, fiscal-period rules, VAT strategies, payment providers, receipt-PDF flags — none is a stamp-duty *capability* predicate. Document-level timbre is data-driven off `tax_configurations.is_stamp_duty`, and `CapabilityAuthoritySingularityTest.php:135-155` pins that data ⇄ the predicate. The AST authority-finder is genuinely semantic (its own fixture proves a renamed `ShadowPolicy::admits` with unrelated vocabulary is still caught, `:68-98`).
- **Make the registration ratchet vacuous.** It is a real AST scan with wrapper discovery to a fixed point (`:176-192`), and its own fixture proves a *renamed* thin wrapper in a non-`app/` root is followed (`:26-60`). `assertSame` against a 101-entry inventory means any added, moved, or renamed throwing lookup reds.
- **Demote a REQUIRED purpose behind a fake gate.** `assertConforms` (`ProvisioningRequiredPurposesV1.php:196-249`) direction-checks evidence kind against classification, forbids gates on non-CONDITIONAL entries, forbids `NONE` call sites outside SOFT, and pins 27/1/4/9 **plus** the exact per-classification name lists in the conformance test — a REQUIRED↔SOFT or REQUIRED↔SCOPE_REQUIRED swap preserves counts but reds the direction check. Two deliberately-misclassified fixtures assert the rejection, not merely a count.
- **Prove a CONDITIONAL purpose is really a 500 in disguise.** Read the sources, not the manifest: `RefundCompensationService.php:186-195` prechecks **both** `RefundWriteOff` and `SalesReturn` before opening the transaction and throws a `DomainException`-derived refusal; `GeneralLedgerService::createRefundCompensationEntry:3960` has exactly one caller, that service, after the precheck. `AccountingService::residualPlan:216-236` uses nullable `Account::findByPurpose` and returns `GlResidualRefusal::NoAbsorbingAccount` — and the suite proves live that the document stays `Confirmed`, `fiscal_hash` null, `chain_sequence` null, **not** sealed. The four CONDITIONAL classifications are correct.
- **Confirm the absorber-by-existence invariant the milestone rests on.** `AccountingService.php:222-226`: a present `SalesStampDutyPayable` account absorbs the residual *before* any rounding account is consulted — so the brief's non-timbre content ban is a real fiscal risk, not a stylistic rule, and `SalesStampDutyPayable` living only in the TN chart is the correct state.
- **Break a caller with the signature churn.** Zero surviving `new CompanyTaxProvisioningService` / `new CountryTaxConfigurationRegistry` / `new`-of-the-five-converted-seeders anywhere outside a historical plan doc. The subtle one: `DemoPharmacySeeder extends ParapharmacySeeder` and calls `parent::run()` — which is precisely why `ParapharmacySeeder` took **constructor** injection while the seeders nobody subclasses took method injection. Production callers (`TenantInitializationService.php:313`, `CompanyController.php:175`) keep the prior non-fail-loud default; seeders pass named `true` exactly where they previously constructed with it.
- **Find behavior drift behind the Taxation delegation.** `MAP[...]['supports_stamp_duty'] ?? false` → `in_array(strtoupper(trim($c)), ['TN'], true)`: identical for every input, the added `trim` strictly safer. `TaxConfigurationController` is not in the diff, and `TaxConfigurationCapabilityDelegationTest.php:44-66` pins it against an **injected fake** authority, so a retained hidden predicate would red.
- **Static + architecture gates.** PHPStan level 8 over `CountryDefaults` + `Shared/Contracts/CountryDefaults` + `Taxation` → `[OK] No errors`. Pint over the new module, contract, all seeders and the new tests → `{"result":"pass"}`. Deptrac: no new violation class (see finding 3 for the pre-existing failure).
- **Attack the durability of the closure evidence.** Both per-milestone reports are gitignored (`.gitignore:58` `docs/sessions/`, `:61` `.superpowers/`), so the tracked provenance is the four review registers, the ten-item ticket, and the progress YAML. That is house rule 15 working as designed, and ticket §8 already owns "make later evidence durable" — I could not turn it into a defect.

---

**Blocking: none. Fix before merge: none.** Round-4's evidence gap is closed with executed proof, and I reproduced the dual-engine counts exactly rather than taking them on report. The invariant kernel survived every bypass I attempted: one capability authority (now coupled set-to-version in the direction that matters), a scope algebra that rejects mixed and malformed sets, a 41-case partition whose classifications I verified against the live throwing sites, a protected-code registry derived from the live treasury resolver, a byte contract I recomputed independently, and three charts pinned to the base SHA. The three P3s above are forward-looking guard notes for the existing hardening ticket, plus one pre-existing repo-wide ratchet failure recorded so it is not misattributed to this branch at M7.

VERDICT: ACCEPT
