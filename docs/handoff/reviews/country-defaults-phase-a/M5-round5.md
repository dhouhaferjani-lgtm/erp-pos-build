## M5 Adversarial Merge Gate — Round 5 (final)

**Range:** `7d85232cc..HEAD` (`f583c8b26`). Round-5 delta under review = `7320b1315` "Close provisioning review hardening" (`6f01c837b` and `f583c8b26` are docs/progress only), against the round-4 register at `docs/handoff/reviews/country-defaults-phase-a/M5-round4.md`.
**Brief section:** `docs/handoff/CODEX-DISPATCH-country-defaults-phase-a-2026-08-10.md:474-545`.
**Lenses:** **treasury** — applies (existing-company chart repair command, frozen-seeder repair baseline, phase-2 deploy checklist). **tenancy-authz** — applies narrowly: the round-5 delta adds no route, permission, module gate, or middleware; it is one console-only Application service plus a test-marker attribute. Both re-checked against code.

Unlike prior rounds I did not stay purely static: I ran the two affected suites, Pint, and PHPStan myself (results below).

## Round-4 P2 dispositions — verified in code, not from the report

- **#1 (19 live suites mislabeled `historical-compat`, one `phpunit.xml` line from silent deletion) — substantively fixed.** A non-excludable class attribute was introduced (`tests/Support/Attributes/UsesFrozenSeederFixture.php:14`) and applied to all 17 fixture-only suites, including `Feature/Treasury/InstrumentAccountResolverTest.php:21`, `Feature/Treasury/PayableInstrumentAccountsTest.php:22`, `Feature/Accounting/InvoiceGLIntegrationTest.php:60`, the four `Backfill*Test` migration suites, and this wave's own gate tests (`Feature/CountryDefaults/ExpenseCategorySeederLoudFailureTest.php:20`, `Unit/CountryDefaults/ProvisioningRequiredPurposesV1ConformanceTest.php:38`). `grep -rn "historical-compat"` now returns exactly two files, both named in the brief. The guard also rejects the label on any other frozen-seeder file (`FrozenSeederProvisioningIsolationTest.php:137-139`). Residual → finding 1.
- **#2 (public committing `previewLegacyExistingChartRepair()` guarded only by "some transaction is open") — fixed.** The method is deleted (`ChartOfAccountsService.php` no longer declares it; `use LogicException` removed). The replacement `LegacyExistingChartRepairPreviewer.php:27-40` owns its own transaction and rolls back in `finally`, returning counts. I verified the rollback cannot be defeated: at level 0 it is BEGIN/ROLLBACK; nested it is a savepoint rollback that undoes only its own writes (`vendor/.../Connection::rollBack`, `$toLevel = level - 1`). I also verified nothing escapes the transaction — all three frozen seeders write **only** `DB::table('accounts')` on the default connection (`GenericChartOfAccountsSeeder.php:38,50,62,76,93`, same shape in `TunisiaChartOfAccountsSeeder.php:42-99` / `FranceChartOfAccountsSeeder.php:41-98`): no Eloquent model events, no observers, no queued jobs, no second connection, no audit-log side channel. Pinned by `SeedChartsCommandTest.php:288-303`. Residual → finding 2.
- **#3 (dry-run reports an unappliable baseline with no marker) — fixed.** `SeedChartsCommand.php:107-113` emits `[LEGACY-ONLY PREVIEW]`, `[NOT ASSIGNED-TEMPLATE PARITY]`, plus the successor procedure, and the docblock now states it (`:39-43,53,60-61`). I confirmed the markers reach **stdout**, as the grep-gate contract claims: `warn()` → `line()` → `output->writeln()` (`vendor/laravel/framework/src/Illuminate/Console/Concerns/InteractsWithIO.php:354`), not stderr. Asserted at `SeedChartsCommandTest.php:249-251,279-281`.
- **#4 (no M5 P3 ticket; harness requires one) — fixed.** `docs/superpowers/tickets/2026-08-12-country-defaults-m5-p3-hardening.md` exists and is non-vacuous: it carries the round-3 #5 config-cache flip/rollback runbook as **deployment-blocking** (rebuild `config:cache`, restart app processes, `horizon:terminate`, verify from the running release, require `country-defaults:verify` zero-exit), the round-3 #4 "Release 1 is not behaviorally inert" note, and an itemized disposition for every round-3/4 P3 with owners and status.

**Evidence I generated (read-only, no writes):**
- `./vendor/bin/phpunit tests/Feature/CountryDefaults/FrozenSeederProvisioningIsolationTest.php` → **OK (14 tests, 49 assertions)**
- `./vendor/bin/phpunit tests/Feature/Accounting/SeedChartsCommandTest.php` → **OK (12 tests, 85 assertions)**
- `./vendor/bin/pint --test` on the four changed files → `{"result":"pass"}`
- `./vendor/bin/phpstan analyse` on `LegacyExistingChartRepairPreviewer.php`, `SeedChartsCommand.php`, `ChartOfAccountsService.php` → **[OK] No errors**

## Findings register

**1. P3 — `apps/api/tests/Feature/CountryDefaults/FrozenSeederProvisioningIsolationTest.php:114-117` + `:129` — CONFIRMED.** The `$historicalCompatFiles` allowlist is unreachable dead code. Neither `tests/Feature/Seeders/DemoSeedersTaxTest.php` nor `tests/Feature/Seeders/SeededProductsHaveTaxRateTest.php` names a frozen chart seeder — I grepped both for `TunisiaChartOfAccountsSeeder|FranceChartOfAccountsSeeder|GenericChartOfAccountsSeeder`, zero hits — so both `continue` at `:129` before the marker check ever runs. The guard therefore does not *reserve* the label for them; it simply never sees them. **Failure scenario:** the five `#[Group('historical-compat')]` methods those files carry (`DemoSeedersTaxTest.php:39,63,90`, `SeededProductsHaveTaxRateTest.php:37,70`) are live current-behavior assertions — FR TVA 20/10/5.5/2.1/0 rows and the company default FK, TN standard/reduced/zero/stamp rows, and the T15 20.00 product rate — and a one-line `<group>historical-compat</group>` added to the existing exclude block at `phpunit.xml:21-25` still deletes them silently and green. Graded P3, not P2: the label is **brief-mandated** (`CODEX-DISPATCH…:496-498`, "TunisianParapharmacySeeder.php (+ its two feature tests re-scoped and labeled historical-compat)"), and the round-4 blast radius — treasury instrument accounts, invoice GL, VAT-period cancel, the four GL backfills, and this wave's own gate tests — is genuinely closed.

**2. P3 — `apps/api/tests/Feature/Accounting/SeedChartsCommandTest.php:288-303` — CONFIRMED.** `test_legacy_preview_collaborator_never_commits_with_or_without_caller_transaction` cannot exercise the "without caller transaction" half of its own name. `RefreshDatabase` (`:24`) already holds an open transaction, so the "direct" call at `:294` begins at level 1 → savepoint at level 2, byte-identical in mechanism to the `DB::transaction` case at `:298`. The production invocation — `tenants:run accounting:seed-charts --option=dry-run=1` — runs at level 0, where `beginTransaction()`/`rollBack()` is a real BEGIN/ROLLBACK. The mechanism is sound at level 0 (`LegacyExistingChartRepairPreviewer.php:29-39`, unconditional `finally` rollback), so this is a coverage-honesty note: the assertion the test name advertises is not the assertion it performs. Same vacuity class as round-4 #2's `transactionLevel()` guard, inherited rather than introduced.

**3. P3 — `apps/api/app/Console/Commands/SeedChartsCommand.php:158-161` — CONFIRMED (doc drift).** The `finally` comment states "The snapshot reads and the per-company reporting sit OUTSIDE the delegate try/catch, so a query failure there would otherwise bypass the rollback…". `7320b1315` moved both snapshot calls **inside** the inner try/catch (`:133-135`); only `$this->line()` remains outside. The behavior change is an improvement — a snapshot query failure is now caught, recorded as `$failure`, breaks the loop, still rolls back at `:162`, and exits FAILURE with an operator message — but the comment now documents a structure that no longer exists, which is exactly the kind of stale rationale that invites a future "safe" revert.

**4. P3 — `apps/api/tests/Feature/CountryDefaults/FrozenSeederProvisioningIsolationTest.php:42` — CONFIRMED.** `assertStringNotContainsString('previewLegacyExistingChartRepair', $source)` pins one deleted identifier by literal name. `ChartOfAccountsService.php` remains on the F-6 file-level allowlist (`:74`), and `LegacyExistingChartRepairPreviewer.php` is now a second allowlisted file (`:75`), so a differently-named committing frozen-seeder seam re-added to either file stays green. Already tracked: ticket, "Round 4 #5 — file-level frozen-seeder allowlist … Status: MITIGATED, hardening OPEN".

**5. P3 — red-first evidence for the round-5 delta — CONFIRMED.** `docs/sessions/codex-country-defaults-phase-a-report.md:1443-1462` records the round-5 remediation and its final green counts (SQLite 60/218, PostgreSQL 60/218, clean PHPStan/Pint) but states **no** failing-first sequence — unlike the round-4 section immediately above it (`:1437`, "The initial focused run failed all seven new/updated expectations") and the round-3 section (`:1428-1429`). The new `test_legacy_preview_collaborator_never_commits_…` therefore has no captured red artifact, and `7320b1315` bundles implementation with tests. Consistent with the wave-wide accepted pattern and ticketed as a PROCESS FOLLOW-UP.

**6. P3 — treasury sequencing, `docs/handoff/treasury-phase2-deploy-checklist.md:19-27` + the M5 ticket — CONFIRMED.** The correction block is accurate and well-placed (it precedes, and conditions, the original "Re-run the locale chart seeder … for every company in every tenant" paragraph at `:28`). But it only says *don't* run §2 once the flag is true; neither it nor the ticket's six-step Release-2 runbook makes "phase-2 portfolio/fee chart repair already applied on every tenant" an explicit **prerequisite** of the flip. **Failure scenario:** the flag is flipped on an environment where §2 was never executed; the apply command is now permanently refused (`SeedChartsCommand.php:99-105`), the dry-run reports a legacy baseline explicitly disclaimed as unappliable, and every affected existing company needs a bespoke reviewed migration to obtain accounts a single documented command could have added an hour earlier. I verified the branch text only; I did not verify staging/production execution state.

## Bypasses I attempted that FAILED (the implementation held)

- **Persist a repair through the new preview seam.** Direct call, and call wrapped in a committing `DB::transaction` — both undone. Verified by reading `Connection::rollBack`'s `$toLevel = transactionLevel - 1` semantics (savepoint rollback when nested, full ROLLBACK at level 0), and by running `SeedChartsCommandTest.php` green (12/12).
- **Escape the rollback via a side channel.** Hunted for Eloquent model events, an `Account` observer, an audit/TimescaleDB write on a second connection, or a dispatched job inside the frozen seeders — none exist; all three write exclusively `DB::table('accounts')` on the default connection. Nothing survives the rollback.
- **Reach a flag-true *write*.** `SeedChartsCommand.php:99-105` fails closed before `Company::query()` is ever issued, and `$legacyOnlyPreview` (`:107`) requires `$dryRun`, so no branch reaches `seedForCompany()` with the flag true. `previewLegacyExistingChartRepair` is gone from `ChartOfAccountsService` and pinned absent by `FrozenSeederProvisioningIsolationTest.php:42`.
- **Poison `tenants:run` with a left-open transaction.** In legacy-only mode the command opens no outer transaction (`:123`) and the previewer's `finally` always rolls back its own; in the flag-false dry run the outer `finally` at `:162` still fires. No path returns the connection with an open (PostgreSQL: aborted) transaction.
- **Push the grep-gate markers onto stderr.** `warn()` → `line()` → `writeln()` on the standard output stream; the `[LEGACY-ONLY PREVIEW]` / `[NOT ASSIGNED-TEMPLATE PARITY]` markers land where the docblock's stdout contract claims.
- **Regress static analysis / style by removing the seam.** `use LogicException` was removed with its only consumer; `DB` and `Seeder` imports are still live (`ChartOfAccountsService.php:54,159`). PHPStan level 8 clean and Pint pass on all four changed files — I ran both rather than trusting the report.
- **Rule 19 / migrations / queues / DI / i18n sweep of the round-5 delta.** No float touches money or quantity (the previewer reads only `is_system`/`parent_id`); no new migration; no new named queue → Horizon coverage N/A; `LegacyExistingChartRepairPreviewer` takes `DatabaseManager` by constructor injection with no `app()`; the command injects it the same way (`:81`); no new user-facing string, so no en+fr obligation added.
- **Vacuity check on the milestone's own required tests.** The eight brief-named M5 files remain present and non-vacuous; the brief's "assert the `ExpenseCategoryProvisioningService` no-op branch is unreachable" obligation is met in substance by `ExpenseCategorySeederLoudFailureTest` (absent COA + missing mapped code both throw; `null` mapping still resolves `GeneralExpense`), with the contract restated at `ExpenseCategoryProvisioningService.php:25`.

---

All four round-4 P2s are closed with real code, and I confirmed each by reading the implementation and running the affected suites, Pint, and PHPStan myself rather than accepting the handback report. What remains is six P3s: one dead allowlist branch in the guard, one test whose name overstates what `RefreshDatabase` lets it prove, one stale rationale comment, one name-literal regression pin, the wave's standing red-first evidence gap, and one unstated deploy-ordering prerequisite. Every one of them is either already itemized with an owner in the M5 P3 ticket or is a documentation/coverage-honesty nit; none can produce a wrong write, a cross-tenant read, an authorization bypass, or a money/quantity defect. The shipped default remains flag-off, and the flag-true paths are correct as written.

VERDICT: ACCEPT
