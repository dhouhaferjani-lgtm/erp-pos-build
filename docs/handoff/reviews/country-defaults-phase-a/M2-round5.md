I ran the diff, read the M2 brief section, and verified every claim against source. Register below.

---

## M2 adversarial merge-gate review — round 5

**Scope:** brief §M2 (`docs/handoff/CODEX-DISPATCH-country-defaults-phase-a-2026-08-10.md:342-377`), diff `7d85232cc..HEAD`. Delta since round 4 is exactly one code commit — `e8c9dde8a` "Reject cyclic template hierarchies" (3 lines of production code + 2 test files + the P3 ticket); `d6f76670b` touches only `docs/handoff/progress/country-defaults-phase-a.progress.yaml` (verified with `git show --stat`). Working tree clean (`git status --porcelain` empty).

**Lenses.** *tenancy-authz* — applies to central-connection pinning and migration targeting only; M2 ships no HTTP surface, so route middleware / `module:` gating / permission checks are genuinely N/A until M3. *treasury* — applies to the publish gate's timbre/absorber and protected-instrument-code semantics only; no payment, GL posting, or partial-write path exists here. *Rule 19 — N/A:* `grep -E '\(float\)|floatval|number_format|bc(add|mul|sub)|CurrencyScale|QuantityScale'` over `app/Modules/CountryDefaults/` returns nothing; M2 introduces no monetary or quantity value. *Horizon — N/A* (`grep onQueue` → nothing). *en+fr — N/A* (every message is a developer-facing `DomainException`/`LogicException`).

### Round-4 disposition (verified, not taken on trust)

- **R4-1 (general hierarchy acyclicity) — FIXED and correct.** `validateAccounts()` now runs the existing Kahn pass at `TemplatePublishingService.php:290`, after the resolve/self checks and before the purpose/protected checks, so a cycle of any length leaves no ready node and raises the typed hierarchy error. It fires on both boundaries — publish (`:57`) and assignment (`TemplateAssignmentService.php:100`). I re-derived the numeric-key path by hand (`'70'`/`'706'`: neither is insertable, no progress, throw) and confirmed no false negative exists from PHP's canonical-integer key coercion — `(string)` round-trips losslessly for every code shape the column can hold (`'0070'`, `'-5'`, and >`PHP_INT_MAX` all stay string keys). Coverage is non-vacuous: `TemplatePublishGateTest.php:207-214` and `TemplateAssignmentServiceTest.php:262-284` build genuine 2- and 3-row cycles by raw `UPDATE` (each individual statement satisfies the immediate composite FK — the exact construction round 4 demonstrated), and the assignment case recomputes `content_hash` after the tamper so the validator, not the hash check, is what fires. Report `:1146-1168` records real red-first: **4 failed / 4 assertions** on both engines pre-fix, plus a removal-mutation that recreated all four.
- **R4-2 (uncast `===` on `code`/`parent_code`) — FIXED.** `:283` now casts both sides and short-circuits on `null`. The new test at `TemplatePublishGateTest.php:194-205` is non-vacuous: without the cast, `70 === '70'` is false, the row survives to `cloneInsertionOrder()`, and the message becomes `cycle` rather than `itself` — the test asserts `itself`. Report `:1157-1159` confirms it red at 1/1 under that mutation.
- **R4-3 (assign-vs-assign unique-key loser) — correctly ticketed to M3** as a typed-conflict obligation (`docs/superpowers/tickets/2026-08-11-country-defaults-m2-p3-hardening.md`). M2 has no HTTP surface, so the raw `23505` is not yet observable by any consumer.
- **R4-4 (carried P3s)** — all present in the same ticket, unchanged.

### Register

**1 — P3 — `apps/api/tests/Feature/CountryDefaults/TemplatePublishGateTest.php:386-395` and `TemplateAssignmentServiceTest.php:347-356` — CONFIRMED, coverage note**
`createParentCycle()` operates on rows built by `validDraft()`, whose codes are `sprintf('R%03d', …)` plus registry literals — the cycle is always built on non-numeric codes (`R001`↔`R002`), which take *string* array keys in `cloneInsertionOrder()`. The realistic chart-of-accounts case — a cycle among numeric codes such as `70`↔`706`, which is exactly the failure narrative R1-1/R2-1/R4-1 were each raised against — never exercises the int-key coercion branch at `TemplatePublishingService.php:333,342,347`. I verified by hand that that branch is correct today, so this is a hole in the signal, not a live defect: a future refactor of the keying (e.g. dropping the `(string) $code` cast at `:342`) could reopen a numeric-code cycle with the cycle suite still green. One extra data-provider case with codes `'70'`/`'706'` closes it.

**2 — P3 — `apps/api/app/Modules/CountryDefaults/Application/Services/TemplatePublishingService.php:253` — CONFIRMED, latent**
R4-2 was closed for `parent_code` but the mirror side is still open. `validateAccounts()` is `public`, and if a future caller (M3 draft validation, M4 bootstrap import) passes an **unsaved** `AdminTemplateAccount` with an integer `code` — `AdminTemplateAccount` casts `type`/`system_purpose`/`is_system`/`sort_order` but **not** `code`/`parent_code` (`AdminTemplateAccount.php:61-69`) — then `trim($account->code)` throws an uncaught `TypeError` under this file's `declare(strict_types=1)` rather than a `DomainException`. The new round-4 test covers only `parent_code => 70` with `code => '70'`, so the symmetric input is untested. Same one-line remedy as `:283`.

**3 — P3 — `TemplatePublishingService.php:342` with `:290` — CONFIRMED, accepted trade-off worth recording**
The clone escape hatch exempts only *self*-parent rows. A template carrying a genuine multi-row cycle can never be cloned (`cloneInsertionOrder()` throws), so if such a template ever reached `published`, spec §4.1's clone→edit→publish→re-point path would leave it permanently stranded. The round-4 fix makes this unreachable *from this codebase* (publish now rejects cycles, and M4's bootstrap import creates acyclic drafts from frozen seeders), so it is theoretical rather than live — but the asymmetry between the self-cycle recovery path and the general-cycle path is deliberate and undocumented in the ticket.

**4 — P3 — `TemplatePublishingService.php:306-319` with `Domain/Registries/ProtectedAccountCodeRegistry.php:44-73` — CONFIRMED, forward note for M4/M5 (treasury lens)**
The protected-code loop iterates every code in the scope and requires all of them, so a multi-country exact scope demands the **union** of per-country registries. `['DE','FR']` is constructible (`CertificationScope.php:41-47` only forbids mixing timbre states, and `STAMP_DUTY_COUNTRIES = ['TN']` at `CountryAccountingCapabilitiesService.php:14` makes `['TN']` the only timbre scope) and would require both `FRENCH_PLAN_DEMO_CODES` and `GENERIC_DEMO_CODES` in one template. Satisfiable, no type conflicts (the overlapping instrument codes `4035`/`413`/`403`/`416` carry identical types in both sets), and correct as a superset rule — but it means no legacy-derived M4 bootstrap template can be certified for a multi-country scope. Worth a line in the M4 gate rather than a change here.

**5 — P3 — `TemplatePublishingService.php:89,140,189,229` and `TemplateAssignmentService.php:135,182` — CONFIRMED, decorative**
`assertAuditTransaction()` is unreachable-by-construction at all six call sites: each sits inside `$connection->transaction(...)`, where `transactionLevel()` is always ≥ 1. The brief invariant "a mutation without its audit row cannot commit" is actually carried by (a) the audit write being ordered inside the same central transaction, (b) `AdminAuditLog` carrying `CentralConnection` (`app/Models/AdminAuditLog.php:33`) so it lands on the same connection, (c) `AdminAuditService::log()` being a synchronous unguarded `AdminAuditLog::create()` with no try/catch and no queue dispatch (`app/Services/AdminAuditService.php:85-115` — I read it rather than assume), and (d) the rollback proof in `TemplateAuditTransactionTest`. The guard is real protection only for the model hooks. Noting so the invariant is not credited to the wrong mechanism.

**6 — P3 — carried forward, unchanged and correctly ticketed.** R1-5 (dead normalization branch, `CountryTemplateAssignment.php:34` — `saving` fires before the unconditional `creating`/`updating`/`deleting` throws; the malformed-code rejection at `:30-32` is still live). R1-6 (`CentralConnectionUnderTenancyTest.php:59` sets `tenancy.bootstrappers = []` and hand-points `database.default` at an in-memory sqlite probe instead of exercising the real `DatabaseTenancyBootstrapper` swap prescribed at brief `:369`; it still proves the invariant that matters). R1-7 (no index on `country_template_assignments.template_id`, none on `admin_templates(domain, status)`). R1-9 / R2-3 (private `REQUIRED` constant vs the `'REQUIRED'` literal at `:293`). R1-10 residual (fixed `usleep(400_000)` handshake). R1-11 (`outsideCentralTransaction()` leaks committed `super_admins`/`admin_audit_logs` rows). R1-12 (typed exceptions deferred to M4/M5). R2-4 (M5 wildcard-vs-timbre resolver gate). R4-3 (M3 typed-conflict obligation). All in the hardening ticket; none blocks M2.

### Bypasses attempted that FAILED (the code held)

- **Cycle surviving the new gate by ordering.** `cloneInsertionOrder()` runs at `:290`, before the REQUIRED-purpose, timbre and protected-code loops — no earlier check can be made to fire first and mask it, and no later check can be reached with a cycle present.
- **Cycle hidden by numeric-string key coercion** (`'70'`↔`'706'`, `'0070'`, `'-5'`, >`PHP_INT_MAX`): traced each through `$pending[$row->code]` / `$insertedCodes[$code]` / `isset($insertedCodes[$parentCode])`. Read and write sides coerce identically; no false negative and no false positive.
- **Row lost or double-inserted by `unset()` inside `foreach`.** PHP iterates the snapshot while `$insertedCodes` updates live, so same-pass parent→child chains still resolve and every entry is visited exactly once; `$pending !== []` terminates correctly.
- **Duplicate-code key collapse in the new validator path.** Rejected at `:256` before `cloneInsertionOrder()` is reached, and impossible on the clone path via `UNIQUE(template_id, code)`.
- **Self-parent smuggled past the clone exemption at `:342`** — pre-rejected at `:283-285` on both the publish and assignment boundaries.
- **Timbre/absorber smuggling** — `SalesStampDutyPayable` is required on a timbre scope and forbidden on every non-timbre and wildcard scope (`:298-304`), re-run at assignment via the shared validator; `['TN','FR']` and `['FR','*']` scope algebra rejected at `CertificationScope.php:31-47`.
- **Mutating a published template's certification or account rows** — `AdminTemplate::updating`/`deleting` plus the `lockCurrentDraft()` re-read under lock (`AdminTemplate.php:45-81`), the `AdminTemplateAccount` guard (`:34-53`), and the `app_path()` bulk-mutation ratchet (`TemplateImmutabilityTest.php:230-251`).
- **Archiving/deleting a referenced template, or a phantom assignment inserted after the scan** — post-lock `FOR UPDATE` recheck (`:165-169`, `:214-218`) plus the composite FK's `NO ACTION`; the pinned `*` row is protected at `TemplateAssignmentService.php:165-167`.
- **Audit row escaping the central transaction, landing in a tenant DB, or being swallowed** — read `AdminAuditService::log()` end to end: synchronous `create()`, no try/catch, no queue, `CentralConnection` model.
- **Central migrations leaking into tenant databases on the staging auto-deploy** — `config/tenancy.php:194-199` pins `tenants:migrate` to `database_path('migrations/tenant')`, `database/migrations/tenant/` contains no `super_admins`, and all three files additionally pin `Schema::connection(central)`. FK ordering holds (`2025_12_01_194614_create_super_admins_table.php` precedes `2026_08_11_100000`).
- **Schema drift vs spec §3** — verified each named constraint in the migration source: `UNIQUE(id,domain)` (`100000:33`), composite FK `(template_id,domain)→(id,domain)` (`100200:24-26`), `UNIQUE(template_id,code|system_purpose|sort_order)` (`100100:28-30`), `UNIQUE(country_code,domain)` (`100200:23`), unique `bootstrap_key` (`100000:21`) with immutability at `AdminTemplate.php:55-57` and absence from `$fillable`.
- **`app()`, `Cache::`, or `config()` in module production code** — grep over `app/Modules/CountryDefaults/` returns nothing; constructor injection throughout, so "provisioning-relevant reads are never cached" holds trivially.

**Red-first evidence:** present and substantive for the round-4 fix (report `:1146-1168`) — dual-engine pre-fix red at 4 failed / 4 assertions, two independent mutation/revert cycles, honestly asymmetric final counts (SQLite 46/269 + 7 skipped; PostgreSQL 53/343), targeted Pint and PHPStan clean, and a fresh scratch-DB replay reporting all three migrations `Ran`.

**Verification limits:** I did not execute PHPUnit (house rule forbids broad runs; this worktree's symlinked vendor would also risk running stale main-repo code) and did not exercise PostgreSQL. `php -l` is clean on the three files touched by `e8c9dde8a`. Everything above is cited to source, schema, or reproduced reasoning over the actual code; the working tree is unmodified.

**Assessment:** every round-4 finding is dispositioned — two fixed in code with genuine red-first evidence, two ticketed. Nothing in this round rises above P3, and none of the P3s touches an M2 named invariant or a required test's substance. M2's scope, invariants, migrations, and test inventory are present and non-vacuous.

VERDICT: ACCEPT
