I re-ran the two baselines this fix round added, executed the full tenant-isolation file the report characterises, and re-derived a fresh set of citations plus the M1 branch extent. Here is the round-5 register.

## M0 review register — sv-stage1, round 5 (lens: fiscal-pos)

**Independently verified at `HEAD = af07af6b9`.** Base check re-derived, not read: `git merge-base --is-ancestor df85d43f4 HEAD` → PASS; `git show HEAD:docs/handoff/ES-CONSOLIDATED-REGISTER-2026-08-11-SNAPSHOT.md | tail -n +63 | shasum -a 256` → `04760455ac…20540`, byte-exact; `git status --porcelain` → empty; `git diff --name-only df85d43f4..HEAD` → **9 docs paths, zero production code**. `git diff --name-only df85d43f4..a5520f23c` → 4 admin docs, so the report's "source files did not change between `df85d43f4` and `a5520f23c`" (`:56`) is true. `af07af6b9` moves exactly one line (the YAML `commit:` pointer to `7975125e7`, the real round-4 fix commit) — the pointer is honest, and the whole `fix_rounds: 0→4` progression traces commit-by-commit with no back-dating.

**Round-4 findings — closure verified by execution, not assertion:**
- **R4-#1 (P2, blocking — undeclared covering tests for the branch M1 deletes) → CLOSED, both ways at once.** All seven enumerated paths are now literal commands (`:208-246`), *and* the narrowing is declared with its rationale and search basis (`:126`). I re-ran the six-file expansion verbatim on a fresh `autoerp_sv_stage1_m0r5_six_test`: **`Tests: 41 warnings (174 assertions)`, exit 0** — identical to `:226`. I then re-ran the completeness grep one term wider than round 4 (`grep -rlE 'cash_counts|buildExpectedPerMethod|expected_per_method' tests/`): **13 hits, 12 test files + 1 fixture, and every one of the 12 is declared.** The claim "every matching covering file found for the M1 removal branch is included below" holds mechanically. Aggregate arithmetic also checks: 63+3+41+1 = **108** cases, 9117+7+174+4 = **9302** assertions, 15+1+6+1 = **23** files — exactly `:135`, `:126`.
- **R4-#2 (P3, unreproducible lock-table rationale) → CLOSED.** `:206` re-qualifies it as a one-off observation under host load, credits my green 66/9124 reproduction, and explicitly disclaims it as a standing narrowing justification.
- **R4-#3 (P3, brief allowlist) → still NOT fixed in the brief** → finding 4, orchestrator-owned, 4th round carried.
- **R4-#4 (P3, incomplete "M0 files touched") → CLOSED** (`:285` now lists `M0-round*.md`).

**Antidote executed — fresh citations rounds 1–4 never touched, all exact:** `CashDrawerService.php:387-413` — the *end* line, not just `:387`: signature at 387, `return $expected;` 412, close 413 ✓ (rule-19 clean, `bcadd`/`bcsub` at `$this->scale()`) · `CashReconciliationSection.tsx:137-157` — `const diff = bcsub(raw, tender.expected_amount, scale)` at exactly 137, memo closes at 157 ✓ (the per-tender magnitude derivation M4 must audit) · `endOfDayPreview.ts:406` opens the four-term whole-drawer comment and `:479` emits `expected_cash` inside the 406-480 span ✓ · `companyFraudSettingsCacheRepository.ts` — `upsertCompanyFraudSettings():35`, `getCompanyFraudSettings():78`, and `:99` `require_blind_cash_count: r.require_blind_cash_count === 1`, which *substantiates* the report's "the SQLite default governs only pre-first-sync behavior" claim rather than merely repeating it ✓ · `pos_shifts.shift_number` is `$table->integer(...)` at `2026_01_08_190641_create_pos_shifts_table.php:33` ✓.

---

### Findings

**1 — P2 · CONFIRMED · `docs/sessions/codex-sv-stage1-report.md:48`**
The binding ruling of record is restated with the grouping dropped, and the natural reading of the result is a **different, wrong formula**. Report: *"Variance is counted total minus opening float plus net cash movements."* Dossier `FINDINGS-shift-variance-gl-2026-08-11.md:30` (quoting R15 §D, `15-…:203`): *"Variance = counted total − (opening float + net cash movements)."* Under standard precedence `counted − float + movements` differs from `counted − (float + movements)` by **2 × net cash movements**, and it inverts the sign of the movements term.
*Failure scenario:* opening float 100.000, net cash movements +50.000 (a cash sale), drawer counted 150.000. The ruling gives variance `150 − (100 + 50) = 0.000` — balanced. The report's sentence gives `150 − 100 + 50 = 100.000` — a fabricated 100.000 overage that, under this wave's own thresholds, trips a mandatory written variance reason and a fraud-alert severity. M2 ships cashier-facing copy derived from this ruling (`Expected in drawer` / `Counted` / `Over / Short / No difference`, brief `:359-368`) and M0 is the record a later session cites.
*Why this is not pedantry:* SV-1 — the entire first milestone — exists because prose in `config/treasury.php:19-25`, a docblock, and a ticket states a cash formula wrongly and the wrong statement became policy. M1's job is to rewrite exactly that class of artefact. The wave's own preflight record cannot carry the defect it was dispatched to remove. **Close:** restore the parentheses (or write `counted − expected, where expected = opening float + net cash movements`). One line, no other change.

**2 — P3 · CONFIRMED · `docs/sessions/codex-sv-stage1-report.md:127`**
The **device** half of the regression set is declared with no search basis and omits two green covering files. `:126` states the API narrowing's search terms; `:127` justifies the device set only as "the six explicit files in the command below". Running the device-side equivalent of the search that round 4 forced on the API side — `grep -rlE 'require_blind_cash_count|requireBlindCashCount|blindCount|isBlind' apps/pos/src --include='*.test.ts*'` — returns **7 files, 2 undeclared**: `src/lib/offline/__tests__/zReportService.test.ts` (`:585` *"stamps report_data.cash_counts with computed variances"*, `:620` `exposes cash_counts on the returned LocalZReport`, `:662` `blindCountUsed: true`) and `src/api/__tests__/fraudSettingsApi.test.ts` (`:54` the `require_blind_cash_count` → `requireBlindCashCount` sync mapping).
*Failure scenario:* M4's "narrow fixes" to the reveal gating touch `CashReconciliationSection.tsx:229`, the committed-payload blind flag. A change there that alters `blindCountUsed` or the `cash_counts` rows the device stamps into `report_data` — i.e. the device-authored fiscal payload under schema v3 — is not executed by the declared six, so the milestone reports green.
*Honest limits, and why this is P3 not P2:* R-4 fences M2/M4 to presentation, no Stage-1 milestone edits `zReportService.ts` or `fraudSettingsApi.ts`, and the declared six *do* cover the components that assemble the payload (`CashReconciliationSection.test.tsx`), the preview formula (`endOfDayPreview.test.ts`), the reveal gate (`CashCountTable.test.tsx`), the v22 DDL and the cache repository. The exposure is one hop removed, unlike round 4's, which sat on the branch M1 is authorised to delete. **Close:** add the two paths at M2, or state the device narrowing's search basis the way `:126` now does for the API.

**3 — P3 · CONFIRMED · `docs/sessions/codex-sv-stage1-report.md:62`, `:270-272`**
The inventory resolves the M1 branch as `233-241 → 233-244` ("calls `buildExpectedPerMethod()` before validation") — correct for what the dossier cites, but the branch itself runs **`ReportGenerationService.php:233-311`, 79 lines**, and the report calls it "the legacy count branch that M1 removes" (`:126`) without recording what else lives inside it: `buildTransactionCountsPerMethod()` (`:235`), the variance-reason and manager-PIN orchestration gates (`:251-265`), **`assertManagerCanOverride($managerOverrideBy, $terminal->tenant_id)` (`:270`)** — a tenant + `pos.close_shift_with_variance` permission check — and four **hash-input** keys: `schema_version` (`:279`), `cash_counts` (`:280`), `variance_summary` (`:292`), `tolerance_summary` (`:306`), the last carrying an in-code warning that "adding/renaming keys would break every existing Z-report hash" (`:299-305`). The "Forward work discovered during M0 review" section records a stale comment and a docblock but not this, the largest M1 hazard.
*Failure scenario:* an M1 implementer takes R-3's delete-with-branch shape literally and removes 233-311, silently dropping the cross-tenant manager-override refusal and four sealed-bytes keys.
*Why P3, not P2:* this fix round's own additions catch it. `HashGoldenByteTest` (Feature + Integration), `HashInputScale4EquivalenceTest`, `ZReportSyncControllerSchema2Test`, `GenerateZReportWithCountsTest` and the filtered cross-tenant `cash_counts.payment_method_id` guard would all go red. Not an M0 failure by the letter either — the brief bounds the inventory to cited `file:line`. Recorded so M1 does not meet it as a surprise.

**4 — P3 · CONFIRMED (carried from rounds 2, 3 and 4 — still unfixed) · brief `CODEX-DISPATCH-sv-stage1-2026-08-11.md:104-107` (orchestrator-side)**
Base-check step 3's allowlist still names `docs/superpowers/reviews/*` while this wave's register is `docs/handoff/reviews/sv-stage1/` and its report lives in `docs/sessions/`. Run mechanically at `HEAD` it returns 9 paths, of which `M0-round1..4.md` and `codev-sv-stage1-report.md` read as "contamination" — the agent's own required deliverables. `:272` resolves it by reading the check as dispatch-time-only; defensible, and I independently confirmed zero production paths in `base..HEAD`, but the check is no longer mechanical. Not the implementer's defect.

**5 — P3 · CONFIRMED · `docs/sessions/codex-sv-stage1-report.md:133-137`**
The results block opens `[PG 127.0.0.1:5432, fresh dedicated autoerp_sv_stage1_m0r2_test]`, then states an aggregate that spans **four** databases (`m0r2`, `m0r3_guard`, `m0r4_six`, `m0r4_tenant_guard`), then labels 63/9117 as "Core command". The DB annotation is true only of the core command it now sits two lines above. Cosmetic; the per-command blocks below each name their own DB correctly.

---

### Bypasses attempted that FAILED (the implementation held)

- **Tried to prove the newly-declared six-file expansion was inflated or never ran:** re-ran the literal command on a fresh `autoerp_sv_stage1_m0r5_six_test` → `Tests: 41 warnings (174 assertions)`, exit 0. Exact to the assertion. Every prior-round DB (`m0r1`, `m0r2`, `m0r3`, `m0r3_guard`, `m0r4_extra`, `m0r4_six`, `m0r4_tenant_guard`) is present on local PG — the claimed runs happened.
- **Tried to prove the "five unrelated failures" in `PosStabilizationTenantIsolationTest` were a convenient story hiding a Stage-1 break:** ran the **whole** file on a fresh DB → `Tests: 5 failed, 44 warnings (154 assertions)`. Exactly five, all `SQLSTATE[22P02] invalid input syntax for type integer: "SHIFT-A-…"/"SHIFT-B-…"`, all raised from helper frames **`:1557` and `:1704`** — the exact line numbers `:248` cites, the exact cause, and none in the cash-count path. `grep -n cash_counts` on that file returns one test method (`:772`), so "the only test in that broad file selected by the M1 payload search" is literally true.
- **Tried to prove the closure grep was still one term too narrow:** widened to `pos_z_report_counts|CashCountRecorded`. Three extra files — `ShiftCashVarianceAdjustmentTest` (already disclosed as a pre-existing red at `:259`), `EloquentPaymentMethodResolverTest`, `OpenFraudAlertForShiftVarianceTest`. All three are *downstream consumers* of the counts table, not covering tests for the M1 branch, and round 3 already cleared the fraud-alert listener as reading only `cash_variance_email_severity`. Not a finding.
- **Tried to break the base check:** all three legs re-derived at `HEAD` — ancestor PASS, digest byte-exact, tree clean, 9 docs paths.
- **Tried to catch production-code drift smuggled into a no-production-code milestone:** docs only, and `base..dispatch` is 4 admin docs, so the inventory's "source files did not change" premise holds.
- **Tried to break the citation inventory on four entries no prior round touched:** `CashDrawerService.php`'s *end* line 413, `CashReconciliationSection.tsx:137`, `endOfDayPreview.ts:406`/`:479`, and the cache-repository symbols — every one resolves to the claimed symbol *and* semantic anchor.
- **Tried to catch YAML bookkeeping inflating the fix rounds:** traced every commit's diff — `fix_rounds` is monotone 0→4, each bump paired with the matching `verdict:` file, and the two "requeue" commits move only the `commit:` pointer.

### Lens applicability
`fiscal-pos` applied and load-bearing: finding 3 is a fiscal finding (the branch M1 may delete carries four sealed-bytes hash-input keys plus a cross-tenant override guard), finding 2 is the device-authored `report_data.cash_counts` / `blindCountUsed` payload, and finding 1 is the whole-drawer variance formula itself. The whole-drawer (`CashDrawerService::calculateExpectedCash`, scope-fenced and untouched) vs takings-only (`buildExpectedPerMethod`) split, the schema-v3 server-authoring chokepoint, the blind-reveal gate (`CashCountTable.tsx:64-65`) and the device-vs-server layering of `require_blind_cash_count` are each accurately characterised. **Not applicable at M0** — rule 19 precision, red-first evidence, tenant scoping, constructor injection, migration safety, Horizon queue coverage and en+fr strings: no production code, no migration, no queue, no DI surface, no user-facing string in the diff.

Finding 1 is the only blocking item and its close is one line of prose. Findings 2–5 may ship, with 4 belonging to the orchestrator. Note for the orchestrator: `fix_rounds` is at 4 of `max_fix_rounds: 5`, so this is the last available fix round.

VERDICT: CHANGES-REQUIRED
