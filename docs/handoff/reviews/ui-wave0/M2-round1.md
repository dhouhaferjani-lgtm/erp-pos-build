## M2 adversarial review register — round 1

**Scope held to:** brief §2 `T2` (`UI-11`), lines 197–225 of `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md`, plus the milestone framing in the EXECUTION MODE banner (M2 = T2) and the M2 entry in `docs/handoff/progress/ui-wave0.progress.yaml:56-66`.
**Range reviewed:** `a6234a7b8..HEAD` (`daf0a8f50`, `57b3fe3e2`) — 4 files, +55/−3.

### Acceptance criteria — verified against code, not the report

| Brief criterion | Result |
|---|---|
| `KeyedByRouteId` added to `WRAPPERS` with a one-line comment naming why, cross-referencing `routes/index.tsx:325` | **CONFIRMED** — `scripts/factory/gen-route-manifest.mjs:34`; the cited line `apps/web/src/routes/index.tsx:325` is indeed `function KeyedByRouteId(...)`, and its body is `<React.Fragment key={id ?? 'none'}>` (`:325-329`) — genuinely keying-only, no visual output, so the allow-list entry is semantically sound, not just convenient |
| After the fix, regeneration yields `component: InvoiceDetailPage` for `/sales/invoices/:id` | **CONFIRMED by independent reproduction** — I generated the manifest to a scratch dir at HEAD: `- path: /sales/invoices/:id / component: InvoiceDetailPage / module_gate: sales / permission: null`. Also confirmed `bash scripts/factory/check-manifest-drift.sh` (exit 1, pre-T3 drift) no longer emits an invoice-detail component hunk |
| **No other manifest entry changes as a result of this task** | **CONFIRMED empirically, not by assertion.** I built a scratch copy of the generator with only the `KeyedByRouteId` line reverted and diffed full regenerated output before-vs-after: exactly one changed line in `routes-web.yaml` (`KeyedByRouteId` → `InvoiceDetailPage`); `routes-pos.yaml` byte-identical. Corroborated by `grep -rn KeyedByRouteId apps/web/src apps/pos/src` → one definition (`routes/index.tsx:325`) + one route-tree use (`:702-704`) |
| Test 1: `KeyedByRouteId` fixture resolves to inner page, **must fail before the change** | **CONFIRMED red-first by re-execution.** Reverted copy: `tests 10 / pass 9 / fail 1`, failing exactly `extractRoutes looks through KeyedByRouteId to the routed page` with `actual: KeyedByRouteId, expected: InvoiceDetailPage`. Matches the handback's "9 passed / 1 failed" claim exactly. At HEAD: 10/10 (`scripts/factory/gen-route-manifest.test.mjs:98-107`) |
| Test 2: unknown local wrapper still resolves to the wrapper (proof of no silent "innermost element" switch) | **CONFIRMED and non-vacuous** — `gen-route-manifest.test.mjs:109-121` asserts `component: 'UnknownLocalWrapper'`; `resolveElement` (`gen-route-manifest.mjs:94-118`) is untouched, the diff is a single Set member. The general-contract fix was *not* taken; the low-risk allow-list entry specified by `00` was |
| No manifest file committed at M2 (T3 owns `scripts/factory/manifests/`) | **CONFIRMED** — `git diff --stat d682b38ec..HEAD -- scripts/factory/manifests/` is empty; committed `routes-web.yaml:754-757` still reads `InvoiceDetailPage`, so M2 *reduces* drift rather than introducing it. Residual drift (6 new routes, 8 permission corrections, 1 `module_gate`) is pre-existing T3 work |
| Commit hygiene / F-6 commit series | **CONFIRMED** — `daf0a8f50` = source + test only; `57b3fe3e2` = docs/YAML only. `Phase 0.2.1` / `Phase 0.2.2` conform to `commit_series: "Phase 0.<task#>.<seq>"` (progress YAML:23). Nothing in the §1 DO-NOT-TOUCH set touched; no `apps/api/**` |

### Findings

1. **P2 — `docs/handoff/HANDBACK-ui-wave0-2026-08-11.md:162-172` — CONFIRMED (evidence contract, not substance).** The regeneration acceptance claim is backed by neither a pasted command nor its output: the report says only "A temporary regeneration (outside the repository) produces:" followed by a hand-transcribed YAML block. §4 binds every acceptance claim to "a pasted command + output, or a `file:line`". **Failure scenario:** the terminal auditor at M8 cannot re-derive that the T2 proof was ever run against the real router — the one artifact that would prove it (the before/after generator diff) is absent, and the same sentence is what stands in for the brief's explicit "paste the before/after diff hunk" instruction. Substance is CONFIRMED true (I reproduced it above), so this is close-before-merge, not milestone-blocking. Fix: paste `node scripts/factory/gen-route-manifest.mjs --out <tmp> && diff -u scripts/factory/manifests/routes-web.yaml <tmp>/routes-web.yaml` with its output.
2. **P3 — `docs/handoff/HANDBACK-ui-wave0-2026-08-11.md:158-161` — CONFIRMED.** The block introduced by "The failure was exact:" is not the `node --test` output. Node prints `+ actual` / `- expected`, i.e. `+ component: 'KeyedByRouteId'` / `- component: 'InvoiceDetailPage'`; the report shows the signs inverted. Read as a unified diff it states the fix *produces* `KeyedByRouteId` — the inverse of the acceptance criterion. **Failure scenario:** a later reader (or the M8 audit) takes the block at face value and concludes T2 regressed the entry. The block is in fact the pre-fix *drift* hunk in the brief's own format; relabel it, or paste node's verbatim assertion output.
3. **P3 — `scripts/factory/gen-route-manifest.mjs:34` — CONFIRMED, arrives at M3, not an M2 defect.** The comment cites `apps/web/src/routes/index.tsx:325`, which the brief itself mandates verbatim. M3 deletes the lazy imports at `routes/index.tsx:98` (`MarketingHubPage`), `:99` (`FinanceHubPage`) and `:284` (`POSShiftsPage`), all above line 325 — so at final branch state the citation will point ~3 lines off. **Failure scenario:** the M8 whole-branch audit follows the citation and lands on `SuspenseWrapper` instead. Cheap to re-anchor during M4/M8; flagging it now because no later milestone owns this file.
4. **P3 — no automated gate executes the new tests — CONFIRMED.** `grep -rn "gen-route-manifest\|scripts/factory" .github/workflows/*.yml` returns nothing, and `scripts/preflight.sh:194` invokes only `check-manifest-drift.sh`, never `node --test scripts/factory/gen-route-manifest.test.mjs`. **Failure scenario:** someone prunes the `WRAPPERS` set and no gate fires. Mitigated once T3(a) commits the regenerated manifest and T3(b) adds the drift job — that job would then catch this class of regression indirectly. T2 did not require CI wiring, so this is a note for M4, not a gap in this milestone.
5. **P3 / informational — divergent wrapper models across the two route parsers, verified NOT a live defect.** `apps/web/tools/audit-listing-census.mjs:44-52` (M1's deliverable) keeps a 7-entry `ROUTE_WRAPPERS` without `KeyedByRouteId`, while the generator now has 9. I checked whether this reopens `UI-11` in the census: it does not — `:549` uses `findLast((name) => !ROUTE_WRAPPERS.has(name))`, i.e. innermost non-wrapper, which yields `InvoiceDetailPage` regardless of the set. No change requested; recorded so the M8 audit does not re-derive it.

### Domain lenses

- **frontend-conventions — NOT APPLICABLE, stated deliberately.** The diff touches zero files under `apps/web/src`; it is build tooling (`scripts/factory/*.mjs`) plus two docs. Design tokens, `t()` i18n (en+fr), `tenantScopedKey`, RHF/zod, canonical component reuse and `RequirePermission`/`hasModule` gating have no surface here. `pnpm typecheck` / `lint` / `test` inputs are unchanged from the M1-accepted state, so their green status carries over unaltered (no web sources modified — verified via `git diff --stat`).
- **general — applied**, results above.
- **Standing checks:** Rule 19 (money/quantity) — N/A, no monetary or quantity path in the diff. Tenant scoping / constructor injection / `app()` — N/A, no PHP and no React data layer. Migrations, named queues + Horizon coverage — N/A, none added. User-facing strings — none added.

### Bypasses attempted that FAILED to break the milestone

1. Tried to prove collateral manifest damage by regenerating both manifests with and without the one-line change — only the single intended line differs; POS byte-identical.
2. Tried to find a second `KeyedByRouteId` mount (or a POS one) that would be silently re-resolved — only two references exist, one definition and one use.
3. Tried to show the red-first claim was retrofitted — reverted the allow-list in a scratch tree and got exactly the claimed 9/1 split with the exact assertion delta.
4. Tried to show a broader "always take the innermost element" fix had been slipped in under the allow-list story — `resolveElement` is untouched and the second fixture pins the old behaviour.
5. Tried to catch a manifest file committed early (which would sabotage T3's single-regen contract) — `scripts/factory/manifests/` is untouched since the wave base `d682b38ec`.
6. Tried to attribute the still-failing `check-manifest-drift.sh` (exit 1) to M2 — the committed manifest already matches post-fix output for the invoice route; all residual drift is pre-existing and is T3's, permitted by §2.

**Milestone gate:** no P1. One P2 (evidence completeness, substance independently confirmed) and four P3s, none blocking per the brief's severity ladder (P1 blocks a milestone; P2 close-before-merge; P3 may ship).

VERDICT: ACCEPT
