# Codex A→Z dispatch — Enforcement guards: DPA cementing ratchet + CI guard liveness + money-lane hardening (2026-08-12)

**Model/effort (owner directive):** Codex SOL/Luna, HIGH effort. Workhorse mode: implement end-to-end, TDD red-first.
**Revision:** r21 — **GATED (owner ruling 2026-08-18: series CLOSED on the r21 evidence — gate r11 = 0C/1H/2M with all gate-r10 findings RESOLVED; the 1H+2M fixed in r20/r21 and verified by focused round-0 runs `...round0-r20.md`/`...round0-r21.md`). NOT yet dispatched** — dispatch remains per-package behind the §⛔ sequencing gates + §6 F-1..F-9 pre-dispatch steps (see `docs/handoff/enforcement-dispatch-checklist.md`). Gate history: 11 adversarial rounds (r1–r11 registers in `docs/superpowers/reviews/`) + 13 round-0 mechanical runs.
**Revision log:** r1 — 2026-08-12, first draft. · r2 — round-0 findings R0-1 (ParserFactory claim narrowed to the 4 actual users) and R0-2 (cementing-guard quote replaced with verbatim source text at file:line) applied; no substantive scope change. · **r3 — 2026-08-12, full gate-r1 fix round** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-gate-r1.md`), orchestrator rulings recorded inline as "(gate-r1 ruling)":
- **C-1** — the nonexistent single nested progress YAML replaced by THREE harness-schema wave files, authored and shipped with this revision: `docs/handoff/progress/enforcement-p1.progress.yaml`, `enforcement-p2.progress.yaml`, `enforcement-p3.progress.yaml` (each top-level: one `base_sha` placeholder pinned at dispatch, one `branch`, ordered milestones, safe conditional-gate encoding copied from the corrected ES/UI YAMLs — no milestone `owner_gate:` fields). P3-after-P1 and P1-after-3C are machine-checkable M0 preconditions in those files, not prose.
- **C-2** — P3(a) rescoped: the GL posting chokepoint already balance-validates; work is now semantic classification of draft-only creators, closing only genuinely unguarded seams, red-first proof mandatory per target (green-at-base disqualifies).
- **C-3** — P3(b) rescoped to a CONSUMER of the country-defaults lane's `ProvisioningRequiredPurposesV1` manifest + conformance suite; sequences after that lane lands (M0 precondition); never redefines the operational purpose set; never touches frozen seeder class bodies.
- **C-4** — ratchet anti-growth mechanism defined (baseline compared against the base branch; any added entry fails CI) + matched-growth tamper acceptance case, applied to the P1 baseline AND the P2 i18n ratchet.
- **H-1** — fold-P1-into-3C exception removed; P1-M0 records the accepted 3C merge SHA, asserts ancestry, re-verifies the S0 seam at that exact SHA.
- **H-2** — 2(a) gates on OWNERSHIP (UI Wave 0 progress YAML), not artifact presence; explicit OpenAPI-lane CI-wiring reconciliation rule added.
- **H-3** — P2-M1 reclassified as a CI-contract change with the same quiet-window treatment.
- **H-4** — P1 tamper-fixture matrix extended to the full write-method vocabulary incl. the real `firstOrCreate` mechanisms, per mechanism per table.
- **H-5** — i18n completeness defined on authored-locale provenance (source translation files), never the merged resource object; production-shaped alias/spread fixtures required.
- **H-6** — 2(b) requires an exhaustive class-to-lane/exclusion manifest + a planted-new-Feature-class negative proof.
- **H-7** — NO push, ever, by the executor; remote-CI evidence moved to an owner-executed post-promotion LEDGER row (S-14); executor acceptance uses local-run evidence.
- **H-8** — final whole-package gate milestone added to every package; lens→agent-contract wiring made explicit (the bridge passes lens labels as prose only); `stock-gl-interaction` + `inventory-costing` added to P3-M1.
- **H-9** — every new CI job must join the `all-checks-pass` `needs` list, stated as deliverable + acceptance grep; already-landed verify branches also check aggregate membership.
- **H-10** — the −19.000 operational item lives in `docs/handoff/LEDGER.md` (row S-15); the handback points to the row and carries no independent status.
- **H-11** — P3 acceptance runs one unbalanced-guard command per guarded path (Treasury AND BatchExpiry AND Accounting/GL).
- **M-1** — census gloss corrected to "other static tests/scans" (`OrphanedTypesCleanupTest` is a filesystem assertion).
- **M-2** — bare `git diff --stat` replaced with `git diff --stat <base_sha>..HEAD` + a path allowlist check.

**r4 — 2026-08-12, round-0 R0-3 fix** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-round0-r3.md`), orchestrator ruling recorded inline as "(r4/R0-3)":
- **R0-3** — the Package 2 gap census claimed `frontend-lint` "runs `pnpm lint`" and 2(c) wired the i18n audit "into the `lint` chain in `package.json` AND therefore into `frontend-lint` — no new CI job needed". **False:** the job (`ci.yml:853-891`) runs **discrete steps** (`pnpm audit:keys` `:876`, `audit:design-system` `:879`, `audit:quantity` `:882`, `lint:ratchet` `:890`) with the `:885` comment stating it *supersedes* a raw `pnpm lint`; the `package.json:10` chain never reaches CI, and `test:eslint-rules` / `tools/__tests__/` run in **no workflow at all**. Fixed: (1) census row corrected to the true step list + supersession comment, and the `test:eslint-rules`/`tools/__tests__` CI-absence added to the runs-nowhere census; (2) 2(c) deliverable 3 rewritten — the i18n audit lands as an **explicit discrete step in the `frontend-lint` job**, never via the `lint` chain; acceptance greps `ci.yml` for the step; H-9 aggregate-membership check applies (`frontend-lint` in `all-checks-pass` `needs`, `:1104` — verify at base, wire if absent); (3) 2(d) gains named deliverable 4 — a discrete CI step or job (executor's choice, justified in the decision doc) running the ESLint RuleTester suite AND the tools tests, with a planted-failing-rule-test tamper proof; (4) the H-3 mechanism sentences (sequencing table, 2(c), P2 milestones, F-6, P2 YAML) updated to the step-addition mechanism — the CI-contract-change classification and quiet-window treatment stand unchanged, now correctly grounded. **Honest note:** the false conjunction predated r3 and survived two round-0 passes because the `:853` job anchor and the `package.json:10` chain contents were each verified as separate true facts — the conjunction ("the job runs the chain") was never itself checked until the r3 round-0 re-run.

**r5 — 2026-08-12, round-0 R0-4 fix** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-round0-r4.md`): three frontend-lint step citations corrected (`:877`/`:880`/`:883` → the re-derived run-lines `:876`/`:879`/`:882` — `run: pnpm audit:keys` at `ci.yml:876`, `run: pnpm audit:design-system` at `:879`, `run: pnpm audit:quantity` at `:882`; the cited lines were the blank separators after each step); numbers had been copied from the r3 round-0 report instead of re-derived from the tree.

**r6 — 2026-08-12, full gate-r2 fix round** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-gate-r2.md`), orchestrator rulings recorded inline as "(gate-r2 …)":
- **R2-C-1** — the branch-name anti-growth comparison (could not bootstrap either new baseline; compared against a moving ref) replaced by the **TWO-PHASE PINNED-BASELINE PROTOCOL** for BOTH ratchets (P1 DPA baseline, P2 i18n baseline): (i) initial baseline generated in a dedicated seed commit in the package branch, reviewed at that milestone's gate against the scanner/census output; (ii) seed commit SHA + seed baseline git BLOB hash pinned in the package's progress YAML at that milestone (`dpa_baseline_seed_commit`/`dpa_baseline_protected_blob` in P1; `i18n_baseline_seed_commit`/`i18n_baseline_protected_blob` in P2); (iii) the CI checker compares PARSED KEY SETS of the working baseline against the pinned protected revision (blob fetched by hash via `git cat-file blob`), never against a branch name — keys may only be REMOVED relative to the pin; (iv) after each promotion the PARENT re-pins the new protected revision (parent-owned pin-update step); (v) the matched-growth tamper case is retained and must fail against the pinned comparison; (vi) the event's before-state IS the pin, making the PR-vs-push before-state ambiguity moot by construction.
- **R2-C-2** — every "every push and PR"/"every lane" claim corrected to the TRUE event graph: PR→`main`/`dev` + push→`main` + `workflow_dispatch` only (`ci.yml:3-8`); a direct push to `dev` never triggers the workflow (`ci.yml:740-747`). Adding `dev` to `on.push.branches` is an **explicitly-declined option** (dev pushes also trigger the staging auto-deploy incl. `tenants:migrate`; widening CI there is an owner cost decision — owner may revisit; see §5). Gap closed instead by a **mandatory PRE-PROMOTION owner gate**: a `workflow_dispatch` run whose head equals the exact accepted candidate SHA, run URL + green result recorded in the package's progress-YAML `pre_promotion_ci_dispatch` pin BEFORE the parent merges to dev; failure = promotion blocked. Event-graph acceptance checks added to P1 and P2 (the recorded dispatch run must show the new job/steps executed).
- **R2-H-1** — P2's announcement state SPLIT: M0 requires the owner-approved quiet window + a PRELIMINARY scope notice (what will change, roughly when); a NEW parent-owned pin `merge_announcement_ack` (filled after M3/M4 acceptance, before promotion) attests the FINAL generated checklist — as-implemented contract changes + per-lane rebase instructions — was actually sent. F-6 updated to match.
- **R2-H-2** — LEDGER S-14 + this brief's §5/§7 rewritten: the remote proof is the R2-C-2 PRE-promotion `workflow_dispatch` run (URL + success BEFORE merge); the local actionlint-or-YAML-parse fallback is explicitly labeled INSUFFICIENT for the Actions job/event graph; failure consequence = promotion blocked; a defect discovered post-promotion blocks the NEXT promotion until a green dispatch run exists.
- **R2-H-3** — the tools tests stay on **Vitest** (every current `apps/web/tools/__tests__/*.mjs` imports its suite API from `vitest`; `node --test` cannot run them): P2 DELIVERABLE adds `"test:tools": "vitest run tools/__tests__"` to `apps/web/package.json`, the CI step runs exactly `pnpm test:tools`, `node --test` removed everywhere, and the SAME command string appears in P2-M1 wiring, P2-M4, and package acceptance. New tools tests follow the Vitest contract.
- **R2-H-4** — P1-M0 additionally requires: `docs/handoff/progress/wave3-3c-3d.progress.yaml` M3 `status: passed` with a parseable ACCEPT verdict artifact, and commit equality/ancestry tying that accepted M3 commit to `dpa_3c_merge_sha`; if the merge commit ≠ the reviewed tip, BOTH `dpa_3c_reviewed_sha` + `dpa_3c_merge_sha` are pinned and reviewed-tip → merge-commit → `base_sha` ancestry verified. New optional pin `dpa_3c_reviewed_sha` in the P1 YAML.
- **R2-H-5** — P3-M0 additionally requires: parent-pinned `country_defaults_landed_sha`; the country wave's top-level `status: complete`; M7 ACCEPT verdict + commit; ancestry from the accepted country tip through the landed SHA to P3's `base_sha`. M1-alone is explicitly insufficient. New pin `country_defaults_landed_sha` in the P3 YAML.
- **R2-H-6** — P3-M2 gains a hard precondition: parent-pinned `p2_m2_landed_sha` with P2-M2 accepted/landed (status + ancestry check), so "per 2(b) outcome" always has a value; reciprocal note added to §3 2(b): any later P2 restructuring must reconcile against the landed P3 wiring.
- **R2-H-7** — the hard-coded Treasury/BatchExpiry/Accounting acceptance trio replaced: acceptance commands are DERIVED from the class-(c) census rows — per row, a named test path/filter with an asserted NONZERO selected-test count (parse the PHPUnit summary; `apps/api/phpunit.xml` sets no `failOnEmptyTestSuite`, so an empty selection exits 0 — exit code alone proves nothing) plus preserved red-first evidence; modules with zero class-(c) rows submit the reviewed census evidence only, no placeholder green command. P3 YAML M1 title updated. Supersedes the gate-r1 H-11 fixed-trio *mechanism* (the per-guarded-path principle stands).
- **R2-M-1** — the three progress-YAML line-5 header comments refreshed to reference r6.
- **R2-M-2** — the 2(b) Option B citation widened to `ci.yml:844-848` (the Accounting run line is `:848`; `:847` is only its step name).

**r7 — 2026-08-12, full gate-r3 fix round** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-gate-r3.md`), orchestrator rulings recorded inline as "(gate-r3 …)":
- **R3-C-1** — the protected-baseline authority moved OUT of candidate-editable state (the r6 YAML pins were themselves ordinary tracked files inside the P1 scope allowlist, so matched growth + a matching pin edit was CI-green). The CI checkers' AUTHORITATIVE protected value is now an **owner-set GitHub Actions repository variable** — `DPA_BASELINE_PROTECTED_BLOB` (P1) / `I18N_BASELINE_PROTECTED_BLOB` (P2) — which the executor cannot write (no push, no repo admin). The progress-YAML pins become **NON-AUTHORITATIVE MIRRORS** (paper trail + drift tripwire). The checker FAILS closed on: variable unset (this deliberately forces the owner bootstrap), unfetchable blob object, **mirror ≠ variable drift**, or any key added relative to the variable's blob. Bootstrap and every re-pin are owner-authenticated variable writes at promotion time — never a candidate diff, never CI, never the executor. Matched-growth tamper case retained against the variable-held blob. **Residual risk accepted on the record:** `ci.yml` (the checker's invocation) remains candidate-editable; that residual is covered by the path-allowlist scope check, the milestone gates reviewing every workflow diff, and the owner's pre-promotion dispatch run — a checker-invocation tamper is a visible workflow diff, not a silent data change.
- **R3-C-2** — post-acceptance receipts moved OUT of the promoted candidate (recording a receipt inside the candidate changed the SHA it attested — an unresolvable cycle). New contract: **promotion SHA == accepted SHA == dispatched head, exactly**; nothing is committed to the package between final-gate ACCEPT and merge. `pre_promotion_ci_dispatch` and `merge_announcement_ack` are recorded AFTER promotion, on dev, in a **POST-PROMOTION ADMIN COMMIT** whose diff touches ONLY the admin allowlist (`docs/handoff/progress/*.progress.yaml`, `docs/handoff/LEDGER.md`) — the house admin-only-delta pattern (harness fix `a5520f23c`). The GATE is the procedural rule — no merge without a green dispatch run whose head equals the exact accepted SHA; final announcement SENT before promotion — owed and audited via LEDGER §2 row S-14; the YAML fields are the durable post-promotion record, never pre-promotion contents of the candidate.
- **R3-H-1** — the alternate 3C proof path now requires EXACT equality: when `dpa_3c_reviewed_sha` is set, `wave3-3c-3d.progress.yaml` M3's recorded `commit` MUST EQUAL `dpa_3c_reviewed_sha`, in addition to the reviewed-tip → merge-commit → `base_sha` ancestry chain. An arbitrary ancestor can no longer stand in for the reviewed tip.
- **R3-H-2** — `p2_m2_landed_sha` replaced by **`p2_landed_sha`** with whole-package proof: `enforcement-p2.progress.yaml` at P3's base must read top-level `status: complete` with M4 (the whole-package gate) `status: passed`, a parseable ACCEPT verdict artifact, and a recorded commit; ancestry `M2.commit → M4.commit (accepted tip) → p2_landed_sha → base_sha` (each hop `git merge-base --is-ancestor`). An M2-passed snapshot alone is exactly the forgery class R2-H-5 already killed for country defaults.
- **R3-H-3** — landed P2 promoted from an M2-time pin to a **P3 DISPATCH/M0 PRECONDITION**: P3 branches from a base that already contains landed P1 AND landed country-defaults AND landed P2; the "parent may pin after dispatch" affordance is DELETED; no mid-wave rebase/merge/integration procedure exists because none is ever needed. Schedule consequence accepted on the record: P3 is strictly last (P1 and P2 may still run in parallel with each other, subject to their own gates).
- **R3-H-4** — deterministic PRE-GATE pin order inside P1-M2 / P2-M1 (the r6 "executor writes pins after the seed commit is reviewed" contradicted the harness's implement→commit→review→ACCEPT-advances order, leaving either the pins unreviewed or the sequence violated): **commit 1** = the dedicated seed commit (baseline generation); **commit 2** = the mirror pins recorded in the YAML + any checker metadata; the milestone bridge review is invoked only AFTER both commits exist and its verdict covers both. Any fix round that regenerates the seed recomputes the blob hash and updates the mirror pins in the fix commit BEFORE re-review. Every "after the seed commit is reviewed" phrase replaced with this order.

**r8 — 2026-08-12, round-0 r7 R0-1 fix** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-round0-r7.md`): the Package 2 acceptance-evidence block still carried the superseded r6 receipt contract ("recorded in `pre_promotion_ci_dispatch` + LEDGER §2 row S-14 BEFORE merge"; "the parent verifies the recorded run … BEFORE merging") — the R3-C-2 rewrite had been applied block-by-block and skipped P2's block while the P1 equivalent was correctly rewritten. Both comment runs aligned with §5 item 4: the run is verified BEFORE merging on exactly the accepted SHA (merged unchanged), the receipt recorded AFTER promotion in the admin commit. No other text changed.

**r9 — 2026-08-13, full gate-r4 fix round** (`docs/superpowers/reviews/2026-08-12-enforcement-guards-brief-gate-r4.md`), orchestrator rulings recorded inline as "(gate-r4 …)":
- **R4-C-1** — the final ACCEPT register could not land anywhere legal (the bridge writes the register file AFTER the reviewed commit exists — `scripts/adversarial-review.sh` copies to `--out` post-review; exact-A promotion forbids committing it into A; the admin allowlist excluded `docs/handoff/reviews/**`; yet P3-M0 demanded P2's M4 verdict artifact resolvable at base). Fixed by **extending the post-promotion admin-commit allowlist** to: `docs/handoff/progress/*.progress.yaml` · `docs/handoff/LEDGER.md` · `docs/handoff/reviews/enforcement-p{1,2,3}/**` · `docs/handoff/HANDBACK-enforcement-*.md`. The final register(s), handback, YAML final status/commit/verdict updates, receipts, and (P2) announcement ack all land TOGETHER in that one admin commit; every register/handback header quotes accepted implementation SHA A (content-bound); the admin commit contains NO production or workflow paths (machine check: its diff ⊆ the allowlist). P3-M0's "verdict path resolves at base" is then satisfiable: `base_sha` is pinned after the admin commit.
- **R4-C-2** — exact-A promotion had no base-freshness or restart rule and deadlocked the second of two parallel P1/P2 promotions. Fixed: **step 0 freshness assert** — `git merge-base --is-ancestor <current-dev-tip> A` (only a FAST-FORWARD to A is legal) — and **promotion critical sections are SERIALIZED** (one package completes steps 0–5 before another begins, even when implementation ran in parallel). **Stale/red re-gate protocol defined:** if step 0 fails, or the dispatch run is red, the package returns to an in-progress final milestone — rebase onto the current dev tip, rerun all invalidated local evidence plus the whole-package bridge gate producing a NEW accepted A′, re-send the P2 final announcement for A′, re-set the variable if the seed blob changed, fresh dispatch on A′, restart from step 0. No other route back exists.
- **R4-H-1** — the "settable only by a repo admin" claim removed as overclaimed. Verified fact recorded: this repo is a PERSONAL private repo (`gh api … .owner.type == "User"`), where UI-path variable writes are owner-only — the gate's cited org-repository write-access rule does not directly apply, but the REST/classic-PAT surface is not proven closed. The trust model is now stated honestly in §6 F-8: variables are the anchor because the EXECUTOR provably lacks them (no push, no credentials); any principal with collaborator write + a qualifying token is inside the trust boundary; the owner enumerates write-access principals and asserts at each dispatch that none authors these candidates; residual accepted on the record, upgrade path = protected environment.
- **R4-H-2** — a raw blob hash had no durable remote reachability once the throwaway ref is deleted. Fixed: at promotion the owner ALSO pushes A to a **durable pin tag** (`ci-pin/enforcement-<pkg>-r<n>`), recorded next to the variable value in the YAML mirror block; the CI checker `git fetch origin tag <pin-tag>` explicitly and then verifies the blob hash from the variable; the throwaway ref may be deleted only AFTER the pin tag exists; the pin tag is retained until a permanent remote branch contains the protected commit; re-pin = new tag + variable update together.
- **R4-H-3** — a seed-changing fix round could not truthfully self-record (one commit cannot contain its own SHA). Fixed: every seed-changing fix round recreates the SAME two-step topology — a seed-revision commit (new baseline) followed by a distinct metadata commit updating `*_baseline_seed_commit` + `*_baseline_protected_blob` — then BOTH are re-reviewed. A single self-identifying fix commit is expressly forbidden.
- **R4-H-4** — the wave's own green acceptance commands failed closed under the designed unset-variable bootstrap. Fixed: every normal green invocation and whole-package rerun is preceded by an explicit **local authority setup**: derive the reviewed seed blob (`git rev-parse <seed_commit>:<baseline-path>`), assert it equals the YAML mirror, `export` it under the SAME env-var name the checker reads; the unset-variable negative case runs in an isolated subshell (`env -u NAME …`) so the exported value survives; the CI step maps `${{ vars.DPA_BASELINE_PROTECTED_BLOB }}` / `${{ vars.I18N_BASELINE_PROTECTED_BLOB }}` into the checker's environment on every workflow event.
- **R4-H-5** — P3's P1 gate accepted an arbitrary ancestor + a candidate-editable top-level status while demanding whole-package proof from P2 and country defaults. Fixed symmetrically: P3-M0 now requires P1 top-level `status: complete`, M3 `status: passed`, a parseable ACCEPT verdict artifact, a recorded M3 commit, and ancestry `M3.commit → p1_landed_sha → base_sha` (each hop `git merge-base --is-ancestor`) — satisfiable because P1's final register lands via the R4-C-1 extended admin commit.

**r10 — 2026-08-13, round-0 r9 R0-1 fix** (`docs/superpowers/reviews/2026-08-13-enforcement-guards-brief-round0-r9.md`): the R4-H-4 repair had been applied asymmetrically — P2-M4's whole-package title carried the LOCAL AUTHORITY SETUP clause but P1-M3's did not (an executor working from P1-M3's title alone would hit the fail-closed unset-variable checker and never reach ACCEPT). P1-M3's title now mirrors P2-M4's clause. Two cosmetic asymmetries fixed in the same pass (both round-0 note-only): §2 Bootstrap line "OWNER-authenticated" → "OWNER-performed … trust boundary per §6 F-8" (no authority overclaim outside F-8), and the P1 YAML pin block "no push, no admin" → "no push, no credentials" (matching P2/F-8 wording). No other text changed.

**r11 — 2026-08-13, full gate-r5 fix round** (`docs/superpowers/reviews/2026-08-13-enforcement-guards-brief-gate-r5.md`), orchestrator rulings recorded inline as "(gate-r5 …)":
- **R5-C-1** — final ACCEPT evidence was forgeable ("header quotes A" bound nothing; the closing admin commit accepted fabricable register/handback files; P3 merely parsed an ACCEPT string). Fixed by the **ANNOTATED CLOSING TAG binding**: at final ACCEPT, the PARENT computes `sha256` digests of the EXACT bridge-output register file(s) and the handback BEFORE anything edits them; at promotion step 3 the OWNER pushes the package's pre-allocated closing pin tag as an **annotated tag at A whose annotation records `{A, sha256(final register(s)), sha256(handback)}`** — the executor cannot push tags, so the binding is owner-authenticated and lives outside every editable payload. Registers are never edited after digest (the informational header stays, but the DIGEST is the binding). **P3-M0 verifies the binding mechanically:** for P1 and P2 it fetches each package's closing tag, reads the annotation, and asserts `sha256` of the landed register file equals the recorded digest (+ annotation's A equals the landed accepted tip) — parsing `VERDICT: ACCEPT` alone is no longer sufficient. Cross-package rewriting is separately blocked by R5-H-3's package-specific closing check.
- **R5-C-2** — the pin-tag lifecycle could not support the first dispatch (A carried a null tag name; the owner tags only after ACCEPT) and later permitted deleting a tag the checker still fetches. Fixed: the tag NAME is **PRE-ALLOCATED and recorded in the milestone metadata commit** (mirror field `*_baseline_pin_tag` is NON-NULL in A and reviewed at that milestone's gate; allocation rule `ci-pin/enforcement-<pkg>-r<n>` with `n` = 1 + highest existing `ci-pin/enforcement-<pkg>-*` tag, collision = pick next `n`, never reuse); at step 3 the owner creates EXACTLY that tag at A and verifies it resolves to A BEFORE touching the variable or dispatching. **Pin tags are NEVER deleted** (they are bytes-cheap; the r10 delete-after-permanent-branch affordance is REVOKED), which removes the deleted-tag-still-fetched contradiction entirely.
- **R5-C-3** — freshness was checked only at step 0 and serialization covered only the three enforcement packages, so any other lane's promotion during the acceptance/dispatch interval broke exact-A fast-forward with no re-gate trigger. Fixed: **the parent performs NO local-dev merge of ANY lane between a package's step 0 and step 5** (single-writer serialization — all local-dev merges go through the parent, so this is enforceable by the one actor holding the pen; recorded as the promotion critical section), AND **step 4a repeats `git merge-base --is-ancestor <current-dev-tip> A` immediately before the fast-forward**; a step-4a failure (or a fast-forward refusal) is an explicit re-gate trigger, same A′ procedure.
- **R5-H-1** — the F-8 trust-model assertion had no field, M0 predicate, or receipt. Fixed: new parent-owned pin **`ratchet_trust_model_ack`** in the P1 and P2 YAMLs (dated, commit-stamped enumeration of repository write principals + the non-authorship assertion, or a pointer to it); **non-null is a P1/P2 M0 precondition**; re-confirmed at promotion if access changed; the final state lands in the closing receipt.
- **R5-H-2** — the re-gate protocol covered only step-0 failure and a red run, not partial external side effects. Fixed with **fail-closed handling per side effect**: preflight the pre-allocated tag name is still free BEFORE mutating the variable; after each write, verify it (tag resolves to A; variable equals the expected blob — a wrong value is repaired by re-setting to the reviewed blob even when the seed did not change); on any failure (tag collision, tag push failure, ref push/dispatch API failure, announcement failure): STOP promotion, restore the last known-good variable value where possible, record the partial state in LEDGER S-14, then either retry the SAME unchanged A from step 2 or enter the re-gate path; the final announcement is re-sent whenever A changes; ALL pre-promotion state is revalidated immediately before dispatch.
- **R5-H-3** — the closing-commit allowlist check was unnamed, unrecorded, and package-blind. Fixed with the **named verifier command**, run by the parent and its output recorded in the closing receipt: `git diff --name-only <A>..<admin-tip>` must show EXACTLY ONE commit (`git rev-list --count <A>..<admin-tip>` == 1) whose every path is inside the CURRENT package's specific closing set — its own `docs/handoff/progress/enforcement-<pkg>.progress.yaml`, its own `docs/handoff/reviews/enforcement-<pkg>/**`, its own `docs/handoff/HANDBACK-enforcement-<pkg>-*.md`, and `docs/handoff/LEDGER.md` — any OTHER path (including ANOTHER package's evidence files) = closing FAIL. **P3-M0 additionally verifies the landed chain shape:** for each of P1/P2, the commits between `p*_landed_sha` and the evidence-bearing state are exactly that package's single closing admin commit with a conforming diff.
- **R5-H-4** — the closing sequence was scoped to workflow-touching packages, leaving a legal no-workflow P3 outcome with no promotion contract. Fixed: **steps 0, 1, 4, 4a, 5, the serialization rule, freshness, the closing tag + digest binding, and the re-gate protocol are UNIVERSAL for every package**; the variable/mirror work in step 2 applies only to ratchet-carrying packages (P1, P2); the throwaway-ref push + `workflow_dispatch` in step 3 applies ONLY when the accepted branch touches `.github/workflows/**` (the closing tag is still created either way); a no-workflow package promotes through the same sequence minus the dispatch.

**r12 — 2026-08-13, round-0 r11 R0-1 fix** (`docs/superpowers/reviews/2026-08-13-enforcement-guards-brief-round0-r11.md`): the R5-H-3 narrowed closing allowlist had been APPENDED next to the surviving r10 wildcard sentence in all three progress YAMLs (each stated two contradictory versions of the rule; a parent following the older sentence could still rewrite another package's landed evidence — verbatim the R5-H-3 failure scenario). The three superseded wildcard sentences are now REPLACED with each package's OWN closing set (`enforcement-p<n>.progress.yaml` · `LEDGER.md` · `reviews/enforcement-p<n>/**` · `HANDBACK-enforcement-p<n>-*.md`; anything else = closing FAIL). One round-0 note adopted as a ruling in the same pass: the closing-tag annotation payload is now SCHEMA-specified in §5 step 3 (`accepted_sha:` / `register_sha256: <path>=<hex>` per file / `handback_sha256: <path>=<hex>`) since P3-M0 must parse it. No other text changed.

**r13 — 2026-08-14, full gate-r6 fix round** (`docs/superpowers/reviews/2026-08-13-enforcement-guards-brief-gate-r6.md`), orchestrator rulings recorded inline as "(gate-r6 …)":
- **R6-C-1** — the annotated tag signed executor-supplied bytes (the executor runs the bridge and controls its output file; the parent hashed a presented file — laundering a fabricated `VERDICT: ACCEPT` through the trust root). Fixed structurally, no new cryptography: **the FINAL whole-package bridge invocation (P1-M3 / P2-M4 / P3-M3) is PARENT-INVOKED.** The executor completes the final milestone's implementation, sets it `status: review`, and HANDS OVER; the PARENT runs `scripts/adversarial-review.sh` itself from its own session against the handed-over branch, captures the register from ITS OWN invocation, and computes the step-1 digests from those parent-observed bytes immediately. Mid-wave milestone reviews remain executor-run (self-review harness economy) — only the ACCEPT that authorizes promotion must be parent-observed. An executor-run "final" register is not acceptance evidence; a handed-over branch whose final bridge the parent has not run is simply not yet accepted.
- **R6-H-1** — P3 verified the register digest but never the handback digest, though P3(a) consumes the handback's census. Fixed: P3-M0 1d/2d now ALSO parse the annotation's `handback_sha256` line, require that exact path to exist at base, and compare `sha256` of the landed handback bytes to the annotation — AND require both register and handback bytes at P3's base to equal the closing commit's blobs, so later rewrites by any lane fail closed.
- **R6-H-2** — step 5's `<admin-tip>` was unbound, so the exactly-one check could be run against a different commit than the evidence-bearing state. Fixed with the **deterministic closing-commit identity**: the closing commit **C is THE first-parent child of A on dev** (`C^ == A`, verified); `git diff-tree C^ C` must contain EVERY mandatory closing artifact (final register path(s), handback path, the package progress YAML final state, the closing receipt content, the LEDGER row) and nothing outside the package's closing set; P3-M0 derives C the same way from `p*_landed_sha` (= A) and verifies base blobs equal C's blobs. C's SHA is recorded in the structured closing receipt (R6-H-4).
- **R6-H-3** — recovery was non-idempotent after tag creation, and a failed closing check had no transition. Fixed: **idempotent tag semantics** — on any same-A retry, an existing tag whose target AND full annotation exactly match `{A, digests}` IS the successful step 3, resume after it; an existing tag that mismatches = re-gate with a newly reviewed name (the old tag stays, never deleted). **Step-5 recovery:** the parent PREFLIGHTS the staged closing set (all mandatory paths present, nothing outside the closing set) BEFORE committing; if the committed closing commit nevertheless fails the check, the parent may AMEND/REDO it ONLY while it is still the unpushed local dev tip (explicitly authorized — local, unpushed, single-writer; recorded in the receipt); once anything else lands on dev, a failed close BLOCKS the next promotion until a corrective closing state is produced and verified.
- **R6-H-4** — P3's no-workflow path contradicted the universal pre-allocation rule and had no receipt field. Fixed uniformly: **every package pre-allocates its closing tag in a reviewed commit** — P3 gains `p3_closing_pin_tag`, recorded by the executor in a small metadata commit BEFORE the final milestone review (reviewed at P3-M3, non-null in A), same allocation rule; and ALL THREE YAMLs gain a structured parent-owned **`closing_receipt`** field (schema: `closing_commit:` C's SHA · `tag:` name · `closing_check:` verbatim rev-list/diff-tree output · `digests:` the annotation payload · for workflow packages a pointer to `pre_promotion_ci_dispatch`) — written in the closing commit itself for the YAML's final state, independent of the CI-dispatch receipt. §5 step 5, the YAML notes, and LEDGER S-14 state the same rule.
- **R6-H-5** — the tag was verified before running the untrusted candidate workflow and never re-verified (a candidate granting itself `contents: write` could retag). Fixed three ways: (i) **step 4a now re-verifies, immediately before the merge and AFTER any dispatch run: the tag object's target == A, the exact annotation bytes, and the repository variable's value** — any mismatch is a named re-gate trigger; (ii) an **owner promotion check REJECTS any candidate whose workflow diff grants content/ref write authority** (`permissions:` containing `contents: write` or equivalent in the changed workflow files = promotion blocked; the repo's `default_workflow_permissions` is `read` — verified via `gh api`, so only an explicit grant matters); (iii) F-8 records the hardening option: protect the `ci-pin/enforcement-*` tag namespace with an owner-controlled repository ruleset so `GITHUB_TOKEN` cannot move those refs.

**r14 — 2026-08-14, round-0 r13 R0-1 fix** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-round0-r13.md`): the R6-H-2 deterministic-C rule had been added BESIDE four surviving `<admin-tip>` placeholders instead of replacing them — the universal closing-sequence note in all three progress YAMLs and LEDGER S-14's check clause each stated the check twice, once bound to C and once unbound (the fourth recurrence of this series' append-vs-substitute failure class). All four now read `git rev-list --count <A>..C` with C defined as THE first-parent child of A (`C^ == A`); the only surviving `<admin-tip>` strings are historical revision-log entries and §5's own negation sentence. No other text changed.

**r15 — 2026-08-14, full gate-r7 fix round** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-gate-r7.md`), orchestrator rulings recorded inline as "(gate-r7 …)":
- **R7-C-1** — "parent-invoked" still ran the CANDIDATE's bridge (the executor could replace `scripts/adversarial-review.sh`, the brief, or the lens contracts in A, making the parent's own invocation emit a fabricated ACCEPT). Fixed with **TRUSTED CONTROL INPUTS**: the parent runs the final bridge from a **parent-pinned trusted blob OUTSIDE the handed-over worktree** (its own checkout of the control files at the parent-pinned control SHA, never the candidate copy), with A supplied ONLY as reviewed data; the brief and every `.claude/agents/*-reviewer.md` lens contract consumed by that run are likewise the parent's pinned copies. **Control-file preflight:** before invoking, the parent machine-checks `git diff --name-only <base_sha>..A` and REJECTS any change to the control surface — `scripts/adversarial-review.sh`, this brief, `docs/handoff/SELF-REVIEW-HARNESS.md`, `.claude/agents/*-reviewer.md` — unless the owner explicitly re-gates those files as their own reviewed change (none of the three packages has any business touching them; P1's/P2's/P3's scope allowlists already exclude them, so any hit is simultaneously a scope FAIL). The final attestation (tag annotation) is extended with `control_sha256:` naming the trusted bridge/brief/contract blobs used.
- **R7-C-2** — the `closing_receipt` required C to contain its own SHA (a fixed-point that can never be truthfully committed; every amend changes the value being recorded). Fixed by **removing all post-C data from C**: the in-C receipt records ONLY what is knowable before commit — `accepted_sha: A` · `tag:` name · `digests:` the annotation payload · `manifest:` the mandatory closing-path list · `preflight: pass`. The actual C SHA and the post-commit check output move to a **SECOND owner-authenticated object outside C**: after the closing check passes, the owner pushes the annotated **close tag `ci-close/enforcement-<pkg>-r<n>` AT C** with annotation `{closing_commit: <C>, closing_check: <sha256 of the verbatim check output>, accepted_sha: <A>}` — same never-delete/never-reuse rules as the `ci-pin/*` tags. Consumers derive C as A's first-parent child regardless and verify the close tag matches; P3-M0 adds that verification for P1/P2.
- **R7-H-1** — a parent-returned final CHANGES-REQUIRED (or tool error) had no harness transition. Fixed with the **final fix-round state machine**, stated in the execution-mode block, all three final milestone titles, and S-14: the PARENT records the round-N register/result in its OWN workspace (never committed into the candidate; the candidate tree must be clean at every handover) and returns the register content to the executor; the EXECUTOR alone implements a scoped fix, commits, increments `fix_rounds`, sets the milestone back to `status: review`, and hands over again; the PARENT invokes EVERY final-milestone round including tool-error retries (exit 3 = CHANGES-REQUIRED, fail closed, parent re-invokes); `max_fix_rounds` exhaustion = `blocked_review` exactly as the harness defines; ONLY the final ACCEPT register's bytes ever land in C.
- **R7-H-2** — the handback bytes the owner authenticates were never a required input of the final review (the bridge reviews `base_sha..A` only; the handback is written post-A). Fixed with the **handback-bound final review**: before invoking the trusted bridge, the parent computes `sha256` of the exact handback file and passes BOTH the path and the digest to the trusted bridge invocation (trusted-copy interface extension: `--handback <path> --handback-sha256 <hex>`); the bridge instructs the reviewer to open and evaluate THAT exact file (census/classification tables are review targets per §7, not appendices) and to ECHO the path+digest in its register; the parent verifies the echoed digest equals its pre-invocation digest BEFORE any ACCEPT is acted on or tagged. A handback edited after the reviewed digest = the ACCEPT does not apply; re-run the final round.

**r16 — 2026-08-14, full gate-r8 fix round** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-gate-r8.md`), orchestrator rulings recorded inline as "(gate-r8 …)". This revision SHIPS the two control-plane artifacts round 8 proved missing: **`scripts/adversarial-review-final.sh`** (the parent-only final-gate bridge) and **`docs/handoff/enforcement-control-manifest.yaml`** (the pre-dispatch control manifest).
- **R8-C-1** — the final review had no sealed snapshot (dirty working-tree bytes could be tested instead of A) and candidate-owned progress fields (`review_lenses`, `max_fix_rounds`) still steered the trusted invocation. Fixed with the **SEALED REVIEW BUNDLE**: step 1 now (i) copies the handback OUT to the parent's immutable workspace FIRST; (ii) verifies the handed-over worktree is otherwise CLEAN (`git status --porcelain` empty after that copy — an operative rule stated in the execution-mode block, all three final milestone titles, and S-14, not a log footnote); (iii) materializes a FRESH READ-ONLY DETACHED WORKTREE at exactly A — all diff inspection and command execution happens there, never in the handed-over checkout; (iv) sources milestone id, FULL lens list, `max_fix_rounds`, and every immutable review control from the **parent-pinned CONTROL MANIFEST**, never the candidate YAML — the candidate YAML is field-checked against the manifest (only run-state fields may vary); (v) runs the reviewer with project-local instruction/settings loading pinned by the manifest. The snapshot + manifest identity are recorded in the attestation.
- **R8-H-1** — "parent-pinned" had no pre-recorded identity and the required bridge interface did not exist on disk. Fixed by SHIPPING both: **`docs/handoff/enforcement-control-manifest.yaml`** (owner/parent-owned, committed BEFORE dispatch so it is part of `base_sha`; added to the control-file preflight list — any candidate change to it = FAIL; contains the control source ref + expected `sha256` for the final bridge, this brief, `SELF-REVIEW-HARNESS.md`, every lens contract, plus each package's immutable review controls) and **`scripts/adversarial-review-final.sh`** (the parent-only final-gate bridge implementing `--manifest`, `--handback`, sealed-worktree checks, lens-contract CONTENT injection, and independent digest computation). New pin `control_manifest` in all three YAMLs (parent fills path+`sha256` at dispatch; non-null at M0). Step 1 verifies every control byte against the manifest BEFORE invocation; P3-M0 and closing verification compare the tag's `control_sha256:` lines to the manifest. The step-3 annotation summaries everywhere now include `control_sha256` (round-0 r15 note 1 harmonized).
- **R8-H-2** — the handback echo could be copied from the prompt, and the tag could bind bytes different from those reviewed. Fixed with the **IMMUTABLE HANDBACK COPY**: the parent's step-1 copy is THE handback — it serves the review, the annotation digest, and C's landed bytes; the expected digest is NEVER placed in the reviewer prompt — the final bridge computes `sha256` of the copy ITSELF and echoes it, and the parent verifies that independently-computed echo against its own pre-invocation hash; the reviewer must additionally quote content-derived evidence from the census/classification tables (row counts + at least the first and last row keys) in the register, proving inspection; the parent re-hashes the immutable copy immediately before tagging and requires exact equality.
- **R8-H-3** — a transient final-bridge tool error had two contradictory owners. Fixed with the **split state machine**: `tool_error` (exit 3 / no parseable verdict / CLI failure) is PARENT-OWNED — retry the SAME A with NO executor involvement, NO candidate commit, NO `fix_rounds` increment, registers kept at unique parent-workspace paths `round<n>-attempt<m>`, bounded at 3 attempts then `blocked_review` with a tool-error note; only a SUBSTANTIVE CHANGES-REQUIRED register transfers control to the executor and increments `fix_rounds` after a new commit; superseded parent registers are quarantined in the parent workspace; exactly the final ACCEPT attempt's bytes land in C.
- **R8-M-1** — consumers verified close-tag annotation fields but not the tag's target, and `closing_check:` hashed an unverifiable transcript. Fixed: every consumer (P3-M0 for P1/P2; the closing verification for every package) requires `git rev-parse <close-tag>^{commit} == C`; the close-tag annotation's `closing_check:` field becomes the owner attestation **`pass`** (the unverifiable output hash is dropped — the owner is the trust root and every consumer independently repeats the C/diff-tree/blob checks); the same complete close-tag schema applies to P3's no-workflow path.

**r17 — 2026-08-14, full gate-r9 fix round** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-gate-r9.md`), orchestrator rulings recorded inline as "(gate-r9 …)". `scripts/adversarial-review-final.sh` REWRITTEN; `enforcement-control-manifest.yaml` gains per-package `progress_path`.
- **R9-C-1** — the review range's lower endpoint came from candidate-editable YAML (an executor setting `base_sha: A` collapsed preflight and review to `A..A`). Fixed: the bridge takes `--base` from the PARENT'S OWN dispatch record (stated rule: never read from candidate state), REQUIRES `BASE` a strict ancestor of A and `BASE != A`, and FIELD-CHECKS the candidate progress YAML (path from the manifest's new `progress_path`) against the projection: `base_sha == --base`, final milestone id matches, its `status == review`, `fix_rounds <= max_fix_rounds`.
- **R9-H-1** — the reviewer ran from the caller's checkout without the pinned brief. Fixed: the `claude` invocation now runs from a NEUTRAL EMPTY temp cwd (neither checkout — no project-local CLAUDE.md/settings/hooks from either tree can load; all prompt paths absolute), the VERIFIED brief and harness CONTENTS are injected into the prompt as the binding acceptance authority, and the snapshot is RE-ASSERTED after the run (`HEAD == A`, clean, handback hash unchanged).
- **R9-H-2** — copy-then-clean was unsatisfiable for the required post-A handback. Fixed with ONE-DIRTY-ENTRY SEAL semantics: `git status --porcelain` must be EXACTLY `?? <handback-source>`; the bridge copies+hashes that file ITSELF to the parent's immutable dest (`--handback-source` + `--handback`); any other entry — or a TRACKED-modified handback — is rejected (the handback must be a new untracked file).
- **R9-H-3** — the manifest had no authenticated identity and no downstream consumer link. Fixed: the bridge requires **`--expected-manifest-sha256`** (parent-held, from the dispatch record) and verifies the manifest BEFORE parsing it; the register and the pin-tag annotation carry a **`manifest_sha256:`** line; **P3-M0 1d/2d now COMPARE the pin-tag's `control_sha256:`/`manifest_sha256:` lines against the package's dispatch manifest at base** (via the `control_manifest` pin), and the closing verification does the same for P3.
- **R9-H-4** — `*ACCEPT*` wildcard verdict parsing accepted `VERDICT: NOT ACCEPT`. Fixed: EXACTLY ONE `VERDICT:` line required and matched by STRING EQUALITY (`VERDICT: ACCEPT` → 0, `VERDICT: CHANGES-REQUIRED` → 2, anything else → 3, fail closed).
- **R9-M-1** — consumers never required `closing_check: pass`. Fixed: P3-M0 1d/2d and the universal producer read-back now require the close-tag annotation's `closing_check` field to exist and equal `pass`.

**r18 — 2026-08-14, round-0 r17 R0-1 fix** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-round0-r17.md`): the R9-H-2 one-dirty-entry seal rule had been added in the r17-bindings sentence while SIX operative locations (step-1 sub-step (ii), the execution-mode exception, all three final milestone titles, LEDGER S-14) still mandated an EMPTY `git status --porcelain` — a handover the rewritten bridge deterministically rejects (the deadlock re-created in the opposite direction; fifth firing of this series' append-vs-substitute class). All six now state the one-dirty-entry rule: status shows exactly `?? <handback-source>`, the bridge copies+hashes it itself, anything else is rejected. No other text changed.

**r19 — 2026-08-17, full gate-r10 fix round** (`docs/superpowers/reviews/2026-08-14-enforcement-guards-brief-gate-r10.md`), orchestrator rulings recorded inline as "(gate-r10 …)". The bridge is REWRITTEN receipt-driven; a receipt TEMPLATE ships at the named parent-workspace location.
- **R10-C-1** — "the parent's dispatch record" had no named artifact, so an honest parent could be steered to a candidate-chosen later base or surrogate manifest. Fixed with the **PARENT DISPATCH RECEIPT**: a per-package YAML at the NAMED durable location `~/.claude/projects/-Users-houssamr-Projects-syneriva-apps-erp/memory/dispatch-receipts/enforcement-<pkg>.receipt.yaml` — OUTSIDE the repo and all candidate control — created by the parent at dispatch (schema: `package`, `base_sha` = the TRUE dispatch base, `manifest_path` canonical ABSOLUTE, `manifest_sha256`, `progress_path`, `final_milestone`, `lenses`, `max_fix_rounds`, `created`). The bridge's interface is now `--receipt` (replacing `--base`/`--manifest`/`--expected-manifest-sha256`): base, canonical manifest path, and expected digest are LOADED FROM THE RECEIPT; the candidate YAML's `base_sha` and structured `control_manifest: {path, sha256}` are field-checked against it; the range preflight additionally REJECTS any added/changed `*control-manifest*` surrogate in `base..A`. The receipt path is echoed in the register and named in LEDGER S-14 at dispatch.
- **R10-H-1** — the bridge's YAML fallback silently skipped every milestone/status/fix-round check when PyYAML was missing (true on this parent host). Fixed: **PyYAML is asserted at startup, fail closed** (`CONTROL FAIL … pip3 install pyyaml`, exit 3); the fallback is DELETED; the field-check now also requires exactly ONE final milestone, top-level `max_fix_rounds` equal to the manifest projection, and the structured `control_manifest` equal to the receipt. F-9 adds PyYAML to the parent-host prerequisites.
- **R10-H-2** — the operative pin-tag schema omitted `manifest_sha256`, breaking P3's promised identity comparison. Fixed: the step-3 annotation schema, S-14, the closing-receipt `digests:` payload, and step-4a's exact-annotation re-verification ALL carry a mandatory **`manifest_sha256: <hex>`** line; P3-M0 parses it from the PIN tag and compares against the package's dispatch receipt + base `control_manifest`; `control_manifest` is defined as structured `{path, sha256}` in all three YAMLs.
- **R10-H-3** — an exact `VERDICT: ACCEPT` followed by a contradictory correction still exited 0. Fixed: the verdict line must ALSO be the FINAL non-empty line of the register (`grep -v blank | tail -1` equality); trailing content = tool_error, fail closed.
- **R10-H-4** — the neutral-cwd reviewer could be handed a relative handback path it cannot open. Fixed: the bridge canonicalizes (`abspath`) and existence-checks the receipt, candidate, handback destination, and register path BEFORE any side effect, requires the manifest path absolute, and rejects handback/register destinations inside the candidate; every prompt path is absolute by construction. §5 ships the exact invocation template.
- **R10-M-1** — the manifest's harness pin had gone stale against a trailing-whitespace drift (fail-closed, but would burn tool attempts). Fixed procedurally: the F-9 pre-dispatch refresh re-derives ALL pins after control inputs are byte-stable (done again for r19), and the refresh gate is stated to cover incidental whitespace.
- **R10-M-2** — the universal close-tag read-back was claimed but not operative for P3. Fixed: step 5 now requires the PARENT, after EVERY close-tag push (P3/no-workflow included), to re-fetch the tag, verify `^{commit} == C`, and parse exactly one each of `closing_commit` (== C), `closing_check` (== pass), and `accepted_sha` (== A) before the package is closed.

**r20 — 2026-08-17, full gate-r11 fix round** (`docs/superpowers/reviews/2026-08-17-enforcement-guards-brief-gate-r11.md` — the series' first ZERO-Critical register; all seven gate-r10 findings RESOLVED):
- **R11-H-1** — P3's operative M0 text asked the CLOSE tag at C for the manifest/control lines the producer places on the PIN tag at A (an unexecutable conjunction). Fixed: P3-M0 1d/2d rewritten as **TWO SEPARATELY NAMED TAG PREDICATES** — d1 = the PIN tag at A (resolves to the landed tip; `accepted_sha`; exactly one `manifest_sha256` matching the dispatch manifest; register/handback digests == landed bytes; the control-name set exactly as the bridge emits, incl. `harness`); d2 = the CLOSE tag at C (derived first-parent child, conforming diff-tree, base blobs == C's blobs, tag resolves to C, annotation EXACTLY `{closing_commit == C, closing_check == pass, accepted_sha == A}` — NO manifest/control lines). The brief's sequencing-row and §4 summaries no longer let "closing tag" name both objects.
- **R11-M-1** — the bridge's header still described the removed `--base`/`--expected-manifest-sha256` options, and the brief's invocation template used the invalid placeholder `<p_n_>`. Fixed: header rewritten receipt-driven (r20); template placeholder now `<p1|p2|p3>`.
- **R11-M-2** — the receipt's four projection fields (`progress_path`, `final_milestone`, `lenses`, `max_fix_rounds`) were never compared with the authenticated manifest, allowing a stale receipt half to persist as a false owner record. Fixed: the bridge cross-checks all four against the manifest's package block after authentication — mismatch = exit 3.

**r21 — 2026-08-17, round-0 r20 R0-1 fix** (`docs/superpowers/reviews/2026-08-17-enforcement-guards-brief-round0-r20.md`): the R11-H-1 two-predicate separation had reached P3-M0's header checks (1d1/1d2, 2d1/2d2) but NOT the M0 TITLE, whose two evidence-binding clauses still attributed the A-tag predicates to a single "closing tag" (sixth firing of the append-vs-substitute class). Both title clauses now name d1 (PIN tag at A) and d2 (CLOSE tag at C) exactly as the header checks do. No other text changed.

**Executor:** Codex desktop session (owner dispatches; long-running Codex work never goes through the CLI — broker conflicts, sandbox write rejections).
**Verification base for every citation in this brief:** the shared `dev` worktree at `/Users/houssamr/Projects/syneriva/apps/erp` on 2026-08-12 (branch `dev`, tip near `a5520f23c` — an **unpushed local-dev state** carrying merged DPA Wave-3 work). Every `file:line` below was re-read in that tree. **Line numbers may drift by dispatch time — re-verify each citation at your base SHA before relying on it.** Claims that could not be cheaply verified are marked **UNVERIFIED**.

---

## ⛔ SEQUENCING CONSTRAINTS — READ FIRST, THESE GATE DISPATCH ITSELF

This brief packages enforcement work that **cannot run right now** because of active parallel sessions. The three packages have **independent dispatch gates** — the owner dispatches each package separately when its gate opens. Do not start a package whose gate is closed. Each gate is encoded machine-checkably in that package's progress YAML M0 (gate-r1 C-1); the table below is the human explanation, the YAML is the operative check.

| Package | Priority | Dispatch gate | Why |
|---|---|---|---|
| **P1 — Document-per-action cementing guard (ratchet mode)** | **P0** | **ONLY after the DPA 3C lane's branch is gated, ACCEPTED, and merged to local `dev`. P1 is NEVER foldable into the running 3C session (gate-r1 H-1 ruling)** — folding an unreviewed new package into 3C would change that lane's accepted scope (its own dispatch defines 3C as a gated branch merged to local dev before 3D exists, `docs/handoff/CODEX-DISPATCH-wave3-3c-3d-2026-08-10.md:612-617`). Per the owner's 2026-08-08 sequencing ruling, only one session works the accounting/GL lane at a time. **(Ruling of record: relayed by the orchestrator; file location UNVERIFIED — the parent must attach the pointer at dispatch.)** **Operative encoding (extended gate-r2 R2-H-4):** the parent writes the accepted 3C merge SHA into `dpa_3c_merge_sha` in `docs/handoff/progress/enforcement-p1.progress.yaml` — and, if the merge commit differs from the reviewed tip, ALSO `dpa_3c_reviewed_sha`; **P1-M0 asserts (i) `git merge-base --is-ancestor <dpa_3c_merge_sha> <base_sha>`; (ii) `docs/handoff/progress/wave3-3c-3d.progress.yaml` M3 — the 3C tail milestone (`:49-56`) — reads `status: passed` with a parseable ACCEPT verdict artifact at its recorded `verdict:` path; (iii) the accepted M3 `commit` EQUALS `dpa_3c_merge_sha`, or — when both pins are set — M3's recorded `commit` EQUALS `dpa_3c_reviewed_sha` (gate-r3 R3-H-1: exact equality, an arbitrary ancestor can never stand in for the reviewed tip) AND reviewed-tip → merge-commit → `base_sha` ancestry holds (`git merge-base --is-ancestor` chain); (iv) the S0 seam is re-verified AT THAT EXACT SHA; and blocks (`blocked_precondition`) otherwise.** Ancestry + seam alone is NOT sufficient — any intermediate/rejected 3C commit also carries the seam (gate-r2 R2-H-4). | The guard's baseline enumerates violators in exactly the code DPA 3C is mutating (`Phase` M1 resumed at `77d07de3c`, treasury gate V-7 open at M4 — state from project memory, may be stale by dispatch). A baseline generated mid-3C is stale on arrival and creates merge conflicts on both sides. **Additional hard prerequisite:** the S0 seam — `reference_type`/`reference_id` on `StockAdjustmentService::recordMovement` — must be on your base. **Verified PRESENT in the current local-dev tree:** `apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:1700-1719` takes `?StockMovementReferenceType $referenceType = null, ?string $referenceId = null` (`:1715-1716`) and calls `assertReferenceLinkagePaired` (`:1719`). But that tree is **unpushed local dev** (Wave-3 merge `bc67530a6`); if the seam is absent at your pinned `dpa_3c_merge_sha` or `base_sha`, **STOP — `blocked_precondition`** — the guard has no reference-linkage contract to enforce without it. |
| **P2 — CI runs the guards that already exist** | **P1** | **Quiet window only.** This package edits `.github/workflows/ci.yml`, which changes CI behaviour for **every open lane** (DPA 3C, country-defaults Phase A, OpenAPI fix rounds, UI Wave 0, DN-consolidation, receipts — all in flight per the progress ledger in `docs/handoff/progress/`). The owner picks the window; the merge must be **announced to all active lanes before it lands**, not after. **Operative encoding (split gate-r2 R2-H-1):** the parent records the approved window + a PRELIMINARY scope notice (what will change, roughly when — sent to all active lanes) in `quiet_window_ack` in `docs/handoff/progress/enforcement-p2.progress.yaml`; null at P2-M0 = `blocked_precondition`. The FINAL announcement — built from the P2-M3-generated checklist with the as-implemented contract changes and per-lane rebase instructions — is SENT after M3/M4 acceptance and BEFORE promotion; the separate parent-owned `merge_announcement_ack` pin records that fact in the POST-promotion admin commit (gate-r3 R3-C-2 — nothing is committed to the accepted candidate between ACCEPT and merge). An unsent announcement at promotion time = promotion blocked, audited via LEDGER S-14. | A new required CI job that an in-flight branch cannot pass turns every open PR red simultaneously. New gates land **observe-first or baselined**, never cold. **This includes P2-M1** — adding the i18n-audit and rule-test/tools-test steps to the `frontend-lint` job in `ci.yml` changes that job's pass/fail contract on every lane (gate-r1 H-3; mechanism corrected r4/R0-3 — the job runs discrete steps, never `pnpm lint`, so the wiring is itself a workflow edit). |
| **P3 — Queued money-lane hardening** | **P2** | **After Package 1 LANDS** (merged by the parent to local dev — same accounting/GL-lane serialization as P1, plus P3's assertions want P1's write-path census as input). **Operative encoding (hardened gate-r4 R4-H-5 — symmetric with the P2/country-defaults whole-package proofs; an ancestor + a candidate-editable status line is the same partial-snapshot forgery class):** P3-M0 requires `p1_landed_sha` (parent-pinned) an ancestor of P3's `base_sha`, AND `enforcement-p1.progress.yaml` at base reading top-level `status: complete` with M3 (the whole-package gate) `status: passed`, a parseable ACCEPT verdict artifact at its recorded `verdict:` path, and a recorded commit, AND ancestry `M3.commit → p1_landed_sha → base_sha` (each hop `git merge-base --is-ancestor`) — satisfiable because P1's final register lands via the gate-r4 R4-C-1 extended post-promotion admin commit. **Plus the gate-r5/gate-r6 authentication layer (R5-C-1/R5-H-3/R6-H-1/R6-H-2; two-predicate split gate-r11 R11-H-1 — the PIN tag at A carries manifest/control/digest lines, the CLOSE tag at C carries exactly {closing_commit, closing_check, accepted_sha}): P3-M0 fetches each of P1's/P2's PIN tags, asserts the annotation's A equals the landed accepted tip, `sha256` of the landed register file equals the annotation's register digest, AND `sha256` of the landed handback equals the annotation's `handback_sha256` (a parseable ACCEPT string alone proves nothing); derives each package's closing commit C as the first-parent child of its landed SHA (`C^` verified), checks `git diff-tree` conformance to that package's specific closing set, and requires the register+handback blobs at P3's base to equal C's blobs.** **P3(b) additionally requires the country-defaults Phase A lane's `ProvisioningRequiredPurposesV1` manifest landed on the base (gate-r1 C-3)** — also a P3-M0 precondition, machine-checked (see §4), with the landed-lane proof hardened per gate-r2 R2-H-5: parent-pinned `country_defaults_landed_sha`, country wave top-level `status: complete`, M7 ACCEPT verdict + commit, ancestry to `base_sha` — an M1-passed snapshot alone is INSUFFICIENT. **AND after Package 2 LANDS (gate-r3 R3-H-2/R3-H-3, superseding the r6 M2-time `p2_m2_landed_sha` pin): P3-M0 requires the parent-pinned `p2_landed_sha` an ancestor of `base_sha`, with `enforcement-p2.progress.yaml` at base reading top-level `status: complete`, M4 `status: passed` + a parseable ACCEPT verdict + a recorded commit, and ancestry M2.commit → M4.commit (accepted tip) → `p2_landed_sha` → `base_sha` (each hop `git merge-base --is-ancestor`) — so "per 2(b) outcome" is a landed fact before P3 branches. There is NO mid-wave pin and NO mid-wave integration; P3 is strictly last (P1 and P2 may still run in parallel with each other, subject to their own gates).** | P3(a) consumes P1's census; doing them out of order duplicates the census and risks divergent lists. P3(b) consumes the country-defaults lane's operational-purpose authority; running before it lands would recreate the manifest that lane owns. |

**Cross-lane overlap you must respect:** UI Wave 0 (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md`) already dispatches, as its **T2/T3(b)**, the route-manifest generator fix (`KeyedByRouteId` missing from `gen-route-manifest.mjs:31-34` `WRAPPERS`) and a manifest-drift CI job. Package 2(a) must resolve **ownership, not just presence** (gate-r1 H-2 — see §3): never duplicate that job, and **never regenerate the manifests before the generator fix is on your base** (regenerating first bakes the wrong component in; the T2 evidence is the live drift hunk `/sales/invoices/:id: InvoiceDetailPage → KeyedByRouteId`). The **OpenAPI Desktop lane** is also live and owns CI drift/coverage wiring for its layer (`docs/handoff/HANDOVER-openapi-lane-orchestration-2026-08-07.md:32-36`) — same ownership rule (§3 2(a)).

---

> ## ⚙️ EXECUTION MODE — SELF-REVIEWING WAVE (read before anything else)
> Each package runs under **`docs/handoff/SELF-REVIEW-HARNESS.md`**. You do NOT hand back to a human between milestones. At the end of every MID-WAVE milestone you run the adversarial review YOURSELF via `scripts/adversarial-review.sh` (Opus bridge), read its register, loop scoped fix rounds until ACCEPT, then move on. **EXCEPTION — the FINAL whole-package milestone (P1-M3 / P2-M4 / P3-M3) is PARENT-INVOKED (gate-r6 R6-C-1) via `scripts/adversarial-review-final.sh` from the parent's control-manifest-verified copies (gate-r7 R7-C-1, gate-r8 R8-C-1/R8-H-1): you complete its implementation, set it `status: review`, and HAND OVER with `git status --porcelain` showing EXACTLY ONE entry — your untracked handback file (gate-r9 R9-H-2; the parent's bridge copies+hashes it itself; anything else dirty, or a tracked-modified handback, is rejected). The parent seals a fresh detached worktree at your exact tip and reviews THAT — never your checkout, with lenses/max_fix_rounds from the manifest, never your YAML. You never run the final bridge, and a register you produced for it is not acceptance evidence. FINAL FIX ROUNDS (gate-r7 R7-H-1; split gate-r8 R8-H-3): on the parent's SUBSTANTIVE CHANGES-REQUIRED, the parent returns the register content; YOU fix scoped, commit, increment `fix_rounds`, reset to `status: review`, hand over again. A TOOL ERROR is the parent's alone — same-A retry, no commit from you, no `fix_rounds` change. Never commit a parent register into your tree.**
> - **Track state in the per-package wave YAMLs (gate-r1 C-1):** `docs/handoff/progress/enforcement-p1.progress.yaml`, `docs/handoff/progress/enforcement-p2.progress.yaml`, `docs/handoff/progress/enforcement-p3.progress.yaml`. One file per package, each in the harness's top-level schema (one `base_sha`, one `branch`, ordered `milestones`, `owner_gates`, `max_fix_rounds`). Read yours first; update after every milestone (status, commit SHA, verdict path, fix_rounds). It is the resume point if you crash. **There is no single nested three-package file — do not invent one.**
> - **Conditional owner gates are encoded SAFELY** (copied from the corrected ES/UI YAMLs): the harness treats any populated milestone `owner_gate:` field as an UNCONDITIONAL STOP (`SELF-REVIEW-HARNESS.md:49-50`), so **no milestone in these YAMLs carries an `owner_gate:` field** — conditional logic (F-2, F-4, T3(b) ownership) lives in the milestone text and the top-level `owner_gates:` list as informational/conditional records. If a listed condition actually fires, THEN you stop `blocked_owner`.
> - The bridge call, once per milestone:
>   ```
>   scripts/adversarial-review.sh \
>     --brief   docs/handoff/CODEX-DISPATCH-enforcement-guards-2026-08-12.md \
>     --milestone <package>-M<n> \
>     --lenses  "<comma list from the milestone's review_lenses in your YAML>" \
>     --range   <base_sha>..HEAD \
>     --out     docs/handoff/reviews/enforcement-<p1|p2|p3>/M<n>-round<r>.md \
>     --round   <r>
>   ```
>   Exit **0 = ACCEPT** · **2 = CHANGES-REQUIRED** · **3 = tool error → treat as CHANGES-REQUIRED (fail closed)**.
> - **Lens → reviewer-contract wiring (gate-r1 H-8).** `scripts/adversarial-review.sh:49-64` passes the `--lenses` labels into the reviewer prompt as **prose only** — it does not load any agent file. The binding contract for each lens is its agent definition, and the reviewer must open and apply it. **REVIEWER INSTRUCTION (the bridge points the reviewer at this brief — this paragraph is part of every milestone's acceptance criteria):** for each lens label, read the matching contract file and apply its full checklist before issuing a verdict — `stock-gl-interaction` → `.claude/agents/stock-gl-interaction-reviewer.md` · `inventory-costing` → `.claude/agents/inventory-costing-reviewer.md` · `treasury` → `.claude/agents/treasury-reviewer.md` · `fiscal-pos` → `.claude/agents/fiscal-pos-reviewer.md` · `frontend-conventions` → `.claude/agents/frontend-conventions-reviewer.md` · `tenancy-authz` → `.claude/agents/tenancy-authz-reviewer.md`.
> - **Lenses:** P1 milestones → `stock-gl-interaction,inventory-costing`. P2 milestones → `frontend-conventions` (add `tenancy-authz` for P2-M2, the PHPUnit-lane change). P3-M1 → `treasury,fiscal-pos,stock-gl-interaction,inventory-costing` (`ReverseWriteOffService` crosses the stock↔GL seam — gate-r1 H-8); P3-M2 → `treasury,fiscal-pos`. **Every package's FINAL milestone is a whole-package gate that re-runs the full accumulated evidence with EVERY lens the package used, over the integrated branch** (harness final-milestone obligation, `SELF-REVIEW-HARNESS.md:66-75`) — P1-M3, P2-M4, P3-M3.
> - **STOP conditions:** (A) fix rounds exhausted (`max_fix_rounds: 5`) → `blocked_review`; (B) an owner gate fires (§6 lists them: F-2 CI-minutes budget, F-4 chart-validation tightening consequence, T3(b)/OpenAPI ownership ambiguity) → `blocked_owner`; (C) architecture contradiction (e.g. a P1 write path that **cannot** carry a document reference without a schema change) → `blocked_architecture`. Set YAML `status` + `blockers` and end the run.
> - **No clock.** Use `git rev-parse --short HEAD` as the `updated:` marker.
> - Branch NOT merged, NOT pushed — and **the executor NEVER pushes any branch, period (gate-r1 H-7 ruling)**. Remote-CI proof of workflow edits is an owner-executed **PRE-promotion `workflow_dispatch` gate (gate-r2 R2-C-2/R2-H-2; receipt cycle fixed gate-r3 R3-C-2)**: a green run on EXACTLY the accepted candidate SHA, which the parent then merges UNCHANGED — the receipt goes into `pre_promotion_ci_dispatch` in the POST-promotion admin commit, tracked against `docs/handoff/LEDGER.md` §2 row S-14; your evidence for workflow changes is local-run only (§5). The parent promotes — and a missing/red dispatch run BLOCKS that promotion.

---

## 0. Read before starting (in this order)

| # | Document | Why |
|---|---|---|
| 1 | `docs/handoff/SELF-REVIEW-HARNESS.md` + your package's `docs/handoff/progress/enforcement-p<n>.progress.yaml` | The execution mode above; the YAML is your operative milestone list and M0 precondition set. |
| 2 | The DPA session file + Wave-3/3C handoffs (parent supplies exact paths at dispatch; the DPA state lives in that session's docs, not here) | Package 1 guards the seam that program is building. Its violation register is your baseline's cross-check. |
| 3 | `docs/handoff/PLAN-p0-fix-lanes-pre-production-2026-08-05.md` §W-6 (`:53`) | The original D1a/D1b finding Package 3(a) closes: DoubleEntryValidator on the auto-posting path; the stranded −19.000 entry is **campaign test money — tracked at `docs/handoff/LEDGER.md` §2 row S-15, not a code fix (gate-r1 H-10)**. |
| 4 | `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md` T2, T3, T7 + `docs/handoff/progress/ui-wave0.progress.yaml` | The manifest-generator bug, the drift CI job Package 2(a) must resolve OWNERSHIP of (gate-r1 H-2), and the C6 detector-rot incident motivating Package 2(d). |
| 5 | For P3 only: `docs/handoff/CODEX-DISPATCH-country-defaults-phase-a-2026-08-10.md` §0 D-6 (`:57-66`) + M1 (`:289-318`) | The operational-purpose authority P3(b) consumes (gate-r1 C-3). |
| 6 | `CLAUDE.md` rules 1–21 (esp. **4** scope, **5** verification, **10** preflight, **13** constructor injection, **19** precision, **20** queued-job/no-CompanyContext reality, **21** branch discipline) | Non-negotiable house rules. |

---

## 1. Mission and scope guard

### Mission

Move enforcement **ahead of remediation**. The DPA program scheduled its cementing guard **last** — after ~25–30 dev-days of remediation (DPA sizing headline; the sweep plan's verbatim words at `docs/superpowers/audits/2026-08-08-document-per-action-violation-sweep.md:136-139`: "**Cementing guard (after fixes):** architecture test forbidding `JournalEntry::create` / `StockMovement::create` / `StockLevel` writes outside document-keyed services with non-self `source_*` linkage (PHPStan rule or deptrac layer + pinned test), so this class can't regrow."). This brief inverts that: the guard lands **first, in observe/ratchet mode**, with every current violator enumerated into a shrink-only baseline — so **new** violations are caught from day one while remediation burns the baseline down. Packages 2 and 3 apply the same principle to CI (guards that exist but never run) and to the queued money lanes (posting paths with no balance assertion).

Three classes of work, nothing else:

1. **P1 (P0):** the document-per-action cementing guard — architecture test + baseline generator + checked-in baseline + CI wiring + tamper test.
2. **P2 (P1):** make CI actually run the cheap guards that exist — and give every detector a liveness test.
3. **P3 (P2):** close the genuinely unguarded Σdr==Σcr seams the census exposes + consume the operational purpose manifest for per-country completeness.

### DO NOT TOUCH

| Area | Why |
|---|---|
| **Any baseline violator's production code.** | Package 1 is guard-only. Fixing violators is the DPA remediation program's job (V1–V10 lanes). A "quick fix while I'm here" creates exactly the cross-lane conflict the sequencing ruling forbids. Enumerate, baseline, move on. |
| Files under DPA 3C's lane (treasury settlement/instrument paths named in that session's brief) while 3C is open. | P1's dispatch gate exists precisely because of this overlap. If your baseline generator must *read* them, fine — reading is not touching. |
| `scripts/factory/manifests/` regeneration, unless UI Wave 0's T2 generator fix is verifiably on your base. | Regenerating with the unfixed generator bakes `KeyedByRouteId` in as a route component. Check `gen-route-manifest.mjs` `WRAPPERS` for `KeyedByRouteId` first. |
| Existing gates' thresholds or baselines (deptrac baseline, design-system baseline, quantity baseline) except where a task explicitly says so. | Never weaken a live gate to make a new one pass. |
| The three frozen country seeder class bodies (`TunisiaChartOfAccountsSeeder`, `FranceChartOfAccountsSeeder`, `GenericChartOfAccountsSeeder` — frozen at `7d85232cc`) and `ProvisioningRequiredPurposesV1`'s classification content. | Owned by the country-defaults Phase A lane (gate-r1 C-3). P3 consumes; it never edits. |
| `apps/pos/**`, mobile, ML services. | No package touches them. |

**If a task appears to need an owner decision, STOP and record it** — do not decide it yourself.

---

## 2. Package 1 (P0) — Document-per-action cementing guard, RATCHET mode

### Context — why this exists and why now

The document-per-action principle: **every stock/GL/cash/fiscal mutation needs its own justifying document.** The DPA audit found 10 violations + 3 GL-gap grays; remediation is a multi-week program; the architecture test that *cements* the principle was scheduled after all of it. That ordering means every week of remediation is a week in which **new** violations can land unguarded. Ratchet mode fixes the ordering: the guard is honest about today (baseline) and merciless about tomorrow (any growth fails CI).

### The contract to enforce

Any **write** (insert/update/delete — reads are out of scope) to these four tables must carry a justifying document reference:

| Table | Model / write surface (verified) | Reference contract |
|---|---|---|
| `journal_entries` | `apps/api/app/Modules/Accounting/Domain/JournalEntry.php`; writers include `AccountingService`, `GeneralLedgerService` (`postEntry` family, `:2874`, `:3450`), `TreasuryReceiptBridge`, `TreasuryAccountChargeBridge`, `RepositoryAdjustmentService`, `InstrumentLifecycleService`, `ReverseWriteOffService` | a `source_type`/`source_id` (or equivalent document linkage — confirm the actual column pair per writer; note `journal_entries(source_type,source_id)` is **not globally unique**, so the check is presence, not uniqueness) |
| `stock_movements` | `apps/api/app/Modules/Inventory/Domain/StockMovement.php`; chokepoint `StockAdjustmentService::recordMovement` (`:1700`) | `reference_type` (`app/Shared/Domain/Enums/StockMovementReferenceType.php`) + `reference_id`, paired — the S0 seam, `assertReferenceLinkagePaired` (`:1719`) |
| `stock_levels` | `apps/api/app/Modules/Inventory/Domain/StockLevel.php` | a level mutation must be traceable to a movement (i.e. flag direct `StockLevel` writes that bypass the movement chokepoint) |
| `inventory_batch_stock` | `app/Modules/BatchExpiry/Domain/Entities/BatchStock.php`; writers in `FEFOInventoryService`, `BatchWriteOffService`, `ReverseWriteOffService`, `GroupedWriteOffService` | same principle: batch-stock mutation traceable to a justifying movement/document |

The **precise** per-table rule (what counts as "has a reference") is a P1-M1 deliverable, written down in the test's docblock and reviewed at the gate — the table above is the starting contract, not the final word. If a table's schema makes the rule unstatable without migration, that is STOP condition (C), not a rule you invent.

### Deliverables

1. **The architecture test** — `apps/api/tests/Architecture/DocumentPerActionWriteGuardTest.php`. Match the house style: PHP-Parser AST traversal, extends `Tests\TestCase`, **no database** (several of the existing `tests/Architecture/*.php` use `ParserFactory` + visitors — 4 of 16: `AuthLifecycleTest`, `BroadcastChannelTenantContextTest`, `TenantScopedExistsRulesTest`, `TenantScopedFindCallsTest`; the rest are **other static tests/scans** (mostly `token_get_all`/regex; one, `OrphanedTypesCleanupTest`, is a pure filesystem non-existence assertion — `tests/Architecture/OrphanedTypesCleanupTest.php:15-29`) — and none use `RefreshDatabase` — verified; gloss corrected per gate-r1 M-1). It statically enumerates write sites against the four tables and classifies each as **linked** (reference present per the per-table rule) or **violation**. **The write-method vocabulary is a stated deliverable, not an example list (gate-r1 H-4):** Eloquent `create`/`firstOrCreate`/`updateOrCreate`/`update`/`save`/`delete`/`increment`/`decrement` on the models, relation-mediated writes, `DB::table('…')->insert/update/delete/upsert`, and raw SQL touching the four tables.
2. **The baseline generator** — a `--write-baseline` mode on the same scan (mirror `apps/web/tools/audit-quantity-display.mjs`: sorted, stable, line-number-free keys so refactors don't churn it; see its `partitionViolationsByBaseline` at `:342-358` — baselined / new / stale three-way split). **Bootstrap-only:** after the initial checked-in baseline, `--write-baseline` is not part of any CI or normal-fix flow (see deliverable 3). The bootstrap run happens in the dedicated SEED COMMIT of deliverable 3's two-phase protocol (gate-r2 R2-C-1).
3. **The baseline, checked in** — `apps/api/tests/Architecture/baselines/document-per-action-baseline.json` (or house-consistent path). **Ratchet semantics, THREE directions enforced (gate-r1 C-4):**
   - (a) a violation not in the baseline → FAIL (growth);
   - (b) a baseline entry with no matching violation → FAIL with "remove the stale entry" instruction (like `audit-quantity-display.mjs:449-461` and the deptrac ratchet, ci.yml `backend-architecture` job `:143-178`);
   - (c) **anti-growth of the baseline file itself — TWO-PHASE PINNED-BASELINE PROTOCOL (gate-r2 R2-C-1 ruling; replaces the r3 branch-name comparison, which could not bootstrap the initial baseline and compared against a moving ref).** (a)+(b) alone are defeatable: the mirror-model's writer simply replaces the baseline with every current violation and exits 0 (`audit-quantity-display.mjs:418-442`), so "land a new violation + its freshly regenerated baseline entry in one commit" is green. Close it:
     - **Phase 1 — seed (pin order fixed gate-r3 R3-H-4):** the initial baseline is generated in a DEDICATED SEED COMMIT (**commit 1**) within the package branch; a SECOND metadata commit (**commit 2**) records the seed commit SHA, the seed baseline's git blob hash (`git rev-parse <seed-commit>:<baseline-path>`), AND the PRE-ALLOCATED closing pin-tag name (gate-r5 R5-C-2 — allocation rule in §5 item 4 step 3; the tag name must be NON-NULL in the accepted candidate) as the NON-AUTHORITATIVE MIRROR pins in `enforcement-p1.progress.yaml` (`dpa_baseline_seed_commit`, `dpa_baseline_protected_blob`, `dpa_baseline_pin_tag`); ONLY THEN is the P1-M2 bridge review invoked, and its verdict covers BOTH commits (the seed reviewed against the scanner output + the DPA-register cross-check, deliverable 4). A fix round that regenerates the seed recreates the SAME two-step topology (gate-r4 R4-H-3 — one commit cannot truthfully contain its own SHA): a seed-revision commit with the regenerated baseline, then a DISTINCT metadata commit updating `dpa_baseline_seed_commit` + `dpa_baseline_protected_blob`, then BOTH are re-reviewed; a single self-identifying fix commit is forbidden.
     - **Phase 2 — ratchet (authority moved gate-r3 R3-C-1 — the r6 YAML pin was itself candidate-editable, inside P1's own `docs/handoff/**` scope allowlist, so matched growth + a matching pin edit was CI-green):** the CI checker takes the protected blob hash from the **owner-set GitHub Actions repository variable `DPA_BASELINE_PROTECTED_BLOB`** — never from the YAML, never from a branch name — and compares **PARSED KEY SETS** of the working-tree baseline against that blob (`git cat-file blob "$DPA_BASELINE_PROTECTED_BLOB"`). Any key present in the working baseline but absent from the variable's blob = FAIL; keys may only be REMOVED. **FAIL CLOSED** on: variable unset/empty (this deliberately forces the owner bootstrap step), blob object unfetchable, or the YAML mirror `dpa_baseline_protected_blob` differing from the variable (**mirror drift is a tamper signal**, never a skip). **Blob availability is a durable-ref contract (gate-r4 R4-H-2; lifecycle fixed gate-r5 R5-C-2), not a hope:** the protected commit is reachable via the owner-pushed pin tag whose NAME is pre-allocated in the metadata commit (non-null in the accepted candidate, so the workflow on A can always discover it from the mirror block); the CI job runs `git fetch origin tag <pin-tag>` explicitly, then verifies the variable's blob hash resolves — a raw blob hash with no retained ref is exactly the unfetchable case that fails closed. **Pin tags are NEVER deleted**, so the fetch can never target a legitimately removed ref. **The CI step maps `${{ vars.DPA_BASELINE_PROTECTED_BLOB }}` into the checker's environment on every workflow event (gate-r4 R4-H-4).** The executor provably cannot write repository variables (no push, no credentials) — the ceiling lives outside the candidate; the honest trust boundary is stated in §6 F-8.
     - **Bootstrap + re-pin on promotion (OWNER-performed, gate-r3 R3-C-1; durable ref gate-r4 R4-H-2; lifecycle + recovery gate-r5 R5-C-2/R5-H-2; trust boundary per §6 F-8):** the variable does not exist until the owner sets it — at first promotion the owner preflights the pre-allocated tag name, sets `DPA_BASELINE_PROTECTED_BLOB` to the gate-reviewed seed blob hash, VERIFIES the read-back value, and pushes the pre-allocated annotated pin tag at A (verifying it resolves to A); after each promotion that legitimately shrinks the baseline, the same preflight-set-verify-tag order with a freshly allocated next-`n` tag name (recorded in that revision's metadata commit). Tags are never deleted or reused. Any side-effect failure = STOP + restore + record + retry-or-re-gate (§5 item 4). Never an executor write, never a CI write, never a candidate diff.
     - **Event-anchoring, stated explicitly (gate-r2 R2-C-1(vi); authority per gate-r3 R3-C-1):** the event's before-state IS the variable-held blob — there is no `origin/<base>`/branch ref anywhere in the mechanism, so the PR-vs-push before-state ambiguity is moot by construction.
     - The matched-growth tamper case (acceptance case 5) is RETAINED and must FAIL against the variable-held blob comparison. Post-bootstrap baseline edits are removal-only in normal flow; a genuine need to add an entry is an owner/parent decision recorded on the record (followed by an owner variable update + a mirror update in the next admin commit), never a CI-green path.
4. **Cross-check the baseline against the DPA register.** Every violator the DPA audit catalogued that your scan does NOT find is a scanner blind spot — investigate each miss and document the resolution (found under a different pattern / already remediated by Wave 3 / scanner gap fixed). A baseline that misses known violators is worse than no guard: it certifies them clean.
5. **CI wiring.** **The Architecture testsuite currently runs in NO automatic CI lane** — verified: `phpunit.xml:17-18` defines suite `Architecture` → `tests/Architecture`, but `ci.yml`'s only `--testsuite` invocation is `Unit` (`:275`); the Feature lanes run `tests/Feature/Security` (`:294`) and the two pgsql `--filter` allowlists (`:629`, `:726`); the full suite runs only on manual `workflow_dispatch`. Deliverable: a lean job (model it on `backend-architecture`, `:143-178` — checkout, PHP, composer, run; no DB, no Redis, static-only, seconds-scale) running `./vendor/bin/phpunit --testsuite=Architecture` with **no `if:` guard** so it runs on **every event that actually starts this workflow — and be precise about what that is (gate-r2 R2-C-2): the true event graph is PR→`main`, PR→`dev`, push→`main`, and `workflow_dispatch` ONLY (`ci.yml:3-8`); a direct push to `dev` NEVER starts the workflow (`ci.yml:740-747`), so the parent's ordinary local-merge + dev-push promotion path produces NO run.** The promotion-path guarantee comes from the mandatory pre-promotion `workflow_dispatch` gate (§5), whose recorded run must show THIS job executed (event-graph acceptance check, §2 acceptance block). **Check first** whether any `tests/Architecture` test secretly needs a DB or env (some carry `#[Group('sweep-progress')]` informational markers — `TenantScopedFindCallsTest` header documents that pattern); if any does, scope the job to the DPA guard class + the provably-static tests, and record which were excluded and why. **AND (gate-r1 H-9): the new job must be added to the `all-checks-pass` aggregate's `needs` list (`ci.yml:1090-1107`)**, with the skipped-job-semantics reasoning comment next to the addition (a no-`if:` job always runs and never sits `skipped`, so it cannot spuriously break the aggregate — read the existing comment block above that `needs` list first; mirror the T3(b) contract at `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:240-247`). A guard visible in CI but absent from the aggregate is not merge-gating.
6. **The tamper test — one fixture per write mechanism per applicable table (gate-r1 H-4).** A fixture-based proof the guard fires (fixture directory, same pattern as `tests/Architecture/fixtures/`). The matrix must cover the REAL write mechanisms in the live tree, not just the two `create` patterns:
   - `JournalEntry::create` without source linkage → reported;
   - `StockMovement::create` without `reference_type` → reported;
   - **`StockLevel::firstOrCreate`** (live mechanism: `apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:1574-1590` — note a zero-valued `firstOrCreate` writes a target row even when a later `update` branch does not run) → covered by a fixture;
   - **`BatchStock::firstOrCreate`** (live mechanisms: `StockAdjustmentService.php:1838-1863`; `apps/api/app/Modules/BatchExpiry/Application/Services/BatchStockService.php:88-105,322-335`) → covered by a fixture;
   - one fixture each for `update`/`save`/`delete`/`increment`/`decrement`, `DB::table()` query-builder writes, and raw SQL against a target table;
   - plus the negatives: a properly linked write per mechanism → NOT reported.
   Acceptance asserts coverage of this matrix explicitly (a table in the handback: mechanism × table → fixture file). **This test is the guard's liveness certificate — it must run in the same CI lane as the guard.**

### Explicitly out of scope

- Fixing ANY baselined violator (DPA program's job).
- Runtime enforcement (DB triggers, model observers). This package is static analysis only; runtime enforcement is a later DPA decision.
- The POS device SQLite layer.

### Acceptance evidence (exact commands, outputs pasted in handback)

```bash
cd apps/api
# LOCAL AUTHORITY SETUP (gate-r4 R4-H-4 — repo vars do not populate a desktop shell; without this the
# checker fails closed by design and no green run is possible pre-bootstrap). Before EVERY normal green
# invocation and the M3 whole-package rerun:
#   seed_blob=$(git rev-parse <dpa_baseline_seed_commit>:<baseline-path>)
#   grep -q "$seed_blob" docs/handoff/progress/enforcement-p1.progress.yaml   # assert == YAML mirror
#   export DPA_BASELINE_PROTECTED_BLOB="$seed_blob"
# The unset-variable negative case runs ISOLATED so the export survives:
#   (env -u DPA_BASELINE_PROTECTED_BLOB ./vendor/bin/phpunit tests/Architecture/DocumentPerActionWriteGuardTest.php; test $? -ne 0)  # must FAIL
./vendor/bin/phpunit tests/Architecture/DocumentPerActionWriteGuardTest.php     # green at final state
# Tamper proof, run and paste ALL FIVE:
#  1. temporarily add an unlinked JournalEntry::create to a scratch fixture-scanned path → test FAILS, paste the failure
#  2. revert → test passes again
# Baseline ratchet proof:
#  3. delete one baseline entry → test FAILS naming the entry as unexpected-new; restore
#  4. add a bogus baseline entry → test FAILS naming it stale; restore
#  5. ANTI-GROWTH proof (gate-r1 C-4, repaired gate-r2 R2-C-1, authority moved gate-r3 R3-C-1): in one
#     scratch change, add a NEW unlinked write to a scanned path AND its matching baseline key → the
#     key-set comparison against the OWNER-VARIABLE-held blob (git cat-file blob "$DPA_BASELINE_PROTECTED_BLOB";
#     locally, export the reviewed seed blob hash under the same env-var name — never a branch name, never
#     the YAML) FAILS (paste the failure); revert both. Also demonstrate fail-closed: YAML mirror edited to
#     a different hash → FAIL (mirror drift); variable unset → FAIL.
# Scope proof (gate-r1 M-2 — bare `git diff --stat` is vacuous on a committed branch):
git diff --stat <base_sha>..HEAD   # guard + baseline + fixtures + ci.yml + progress YAML/handback docs only
# Path allowlist check: every path in that diff must match
#   apps/api/tests/Architecture/**  ·  .github/workflows/ci.yml  ·  docs/handoff/**
# — any OTHER path is a scope FAIL, not a footnote.
# Aggregate membership proof (gate-r1 H-9):
grep -n '<architecture-job-id>' .github/workflows/ci.yml   # shows the job AND its entry in all-checks-pass `needs`
# Event-graph acceptance check (gate-r2 R2-C-2; receipt semantics gate-r3 R3-C-2): the handback NAMES
#   the exact job id(s) the owner's pre-promotion workflow_dispatch run must show executed; the parent
#   verifies the run (green, head SHA == the exact accepted candidate SHA == the SHA that will be merged
#   unchanged, the new job present and executed in the run's job list) BEFORE merging, then records it in
#   pre_promotion_ci_dispatch in the POST-promotion admin commit. No such run = promotion blocked (§5; LEDGER S-14).
```
Plus: the baseline↔DPA-register cross-check table (deliverable 4) and the mechanism×table fixture-coverage table (deliverable 6) in the handback, one row per item, with disposition.

**Milestones (operative list in `enforcement-p1.progress.yaml`):** `p1-M0` = machine-checked preconditions (3C ancestry + S0 seam at the exact SHA + phase series; setup-only, no bridge review). `p1-M1` = scanner + per-table rules + full write-mechanism fixture/tamper matrix (TDD: fixtures first). `p1-M2` = seed-commit baseline generation (commit 1) + mirror pins/checker metadata (commit 2), BOTH before the bridge review (gate-r3 R3-H-4) + the variable-authority anti-growth ratchet (`DPA_BASELINE_PROTECTED_BLOB` — gate-r2 R2-C-1 as re-anchored by gate-r3 R3-C-1) + register cross-check. `p1-M3` = CI wiring + aggregate membership + event-graph acceptance check (gate-r2 R2-C-2) + **WHOLE-PACKAGE gate: rerun the full accumulated evidence with every lens over the integrated branch**. Gate each via the harness.

---

## 3. Package 2 (P1) — CI runs the guards that already exist

**Honest gap census first** — the shorthand "CI skips preflight on every lane" is directionally right but imprecise, and the executor must not re-fix what already runs. **The workflow's TRUE event graph, before any per-job claim (gate-r2 R2-C-2): it starts on PR→`main`, PR→`dev`, push→`main`, and `workflow_dispatch` ONLY (`ci.yml:3-8`); a direct push to `dev` NEVER starts it (`ci.yml:740-747` documents this outright). Every "runs on every lane" phrase below therefore means "on every event that starts the workflow" — never "on every dev push".** Verified state of `ci.yml`:

- **Already running on every workflow event (no `if:`):** `backend-lint` Pint (`:27`), `backend-analyse` PHPStan (`:105`), `backend-architecture` deptrac ratchet (`:143`), `frontend-lint` (`:853-891` — **does NOT run `pnpm lint`** (r4/R0-3): it runs **discrete steps** — `pnpm audit:keys` `:876`, `pnpm audit:design-system` `:879`, `pnpm audit:quantity` `:882`, `pnpm lint:ratchet` `:890` — and the in-file comment at `:885` states outright that the ratchet step *"Supersedes a raw `pnpm lint`"*. The `apps/web/package.json:10` `lint` chain (`lint:eslint && audit:keys && audit:design-system && audit:quantity && test:eslint-rules`) is **LOCAL-ONLY** — editing it changes nothing CI runs. Note `audit:keys` is the **TanStack-key** audit, not i18n), `frontend-typecheck` (`:893`), `types-drift` (`:1012`).
- **Skipped on PR→dev:** `backend-test` PHPUnit (`:180-185`, comment: *"Heavy job… Skipped on PR→dev — local preflight is the gate during dev iteration"*), `frontend-test` Vitest (`:918-922`), `frontend-build` (`:977-981`), and — notably — the **`all-checks-pass` aggregate itself** (`:1090-1103`).
- **Running nowhere in any workflow:** the route-manifest drift check (`scripts/preflight.sh:193-195` invokes it locally; zero workflow references — verified in the UI Wave 0 brief at its base and unchanged since), the **Architecture testsuite** (Package 1 deliverable 5 covers it), and — **added r4/R0-3** — **`pnpm test:eslint-rules`** (the ESLint RuleTester suite, `apps/web/package.json:12`) **and the `tools/__tests__/` Vitest suite** (every current file there imports its suite API from `vitest` — gate-r2 R2-H-3; `package.json` also has no dedicated runner script for it today — adding `test:tools` is a 2(d) deliverable): zero workflow references (`grep -rn 'test:eslint-rules\|tools/__tests__' .github/workflows/` → exit 1). Every existing rule test and tools tamper test is CI-dead today; closing this is 2(d) deliverable 4.

### 2(a) — cheap static legs on PR→dev

**Scope after the census:** the only cheap static leg still missing from PR→dev is the **manifest drift check** — and UI Wave 0 T3(b) already dispatches that job. **Ownership, not presence, decides your branch (gate-r1 H-2 ruling):** a grep of your base cannot distinguish "not yet reached on the active UI branch" from "unowned/relinquished". Decision rule:
- **Read `docs/handoff/progress/ui-wave0.progress.yaml`.** Its M4 owns T3(b).
  - **UI M4 `passed` / wave `complete`, and `check-manifest-drift` present on your base** → **verify-only**: confirm the job runs on PR→dev with no `if:`, **AND confirm it is in the `all-checks-pass` `needs` list** (gate-r1 H-9, mirroring `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:240-247`). A landed job missing aggregate membership is a finding you record AND fix here (that fix is yours: it completes the landed contract, it does not re-author the job).
  - **UI M4 `pending`/`in_progress` (the wave still owns the task)** → **verify-only + record the dependency.** Do NOT author the job — even though it is absent from your base.
  - **UI YAML shows the task relinquished, or the wave complete WITHOUT the job on your base** → implement it yourself, exactly per the T3(b) spec in the Wave 0 brief (lean job, workspace install because `gen-route-manifest.mjs` resolves `typescript` via the web workspace, added to `all-checks-pass` `needs` with the no-`if:`-never-skipped reasoning comment). **Do not regenerate manifests** unless T2's `KeyedByRouteId` generator fix is on your base.
  - **Any ambiguous state** → `blocked_owner`, name the ambiguity.
- **Same reconciliation rule for the OpenAPI lane's CI wiring (gate-r1 H-2):** the OpenAPI Desktop lane is live (`docs/handoff/HANDOVER-openapi-lane-orchestration-2026-08-07.md:32-36`) and owns CI drift/coverage wiring for its layer (`docs/superpowers/plans/2026-08-06-codex-dispatch-openapi-mcp-layer-a-to-z.md:9-16,21-26`). Before you touch the aggregate or the Feature lanes: record that lane's CI-wiring state on your base; any job it owns is verify-only for you; and your P2-M3 merge-announcement checklist must carry an explicit rebase/merge-order line for the OpenAPI lane (whichever of the two `ci.yml`-writing lanes lands second rebases and re-verifies aggregate `needs` reconciliation).
- Also wire `all-checks-pass`'s PR→dev story: today the aggregate is main-only, so branch protection on `dev` (if any) can't key off it — **investigate and propose** (a proposal in the handback, not a unilateral branch-protection change; that's an owner knob).

### 2(b) — a strategy for `tests/Feature` in CI

Today `tests/Feature` coverage on PR→dev is: `tests/Feature/Security` whole-directory (`:294`, main-lane only — actually inside `backend-test`, so **not** on PR→dev), the pgsql lane's hand-maintained `--filter` allowlist — **93 class names** at `ci.yml:629` (the finding that motivated this task said ~49; it has nearly doubled since, which IS the argument: it grows by hand, forever) — and a second 16-entry allowlist at `:726`.

**The substring-shadowing defect, called out explicitly:** PHPUnit `--filter` is an **unanchored regex** over test identifiers. Concretely, allowlist entry `AnalyticsTest` also matches `ExpenseAnalyticsTest` (both happen to be listed — but the mechanism means **any future class whose name contains an allowlisted substring silently joins the lane**, and conversely any new Feature class matching no entry **silently never runs anywhere**). The allowlist's failure mode is silence in both directions.

**Deliverable — a decision document plus the implementation of the chosen option:**
- **Option A: enable the Feature suite on PG in CI** (the pgsql service is already provisioned in `backend-test` "for future Feature-suite coverage that opts into it explicitly", `:226-230`). Cost: CI minutes + flake surface — measure it **locally** (single local run of the lane's commands, record wall time — the executor never pushes, gate-r1 H-7) before choosing. **If the runtime is significant, this is owner-budget territory → §6 F-2, STOP condition (B).**
- **Option B: replace the allowlist with a principled inclusion rule** — e.g. directory-level inclusion (`tests/Feature/Treasury`, `tests/Feature/Accounting` — the pattern `:844-848` already uses for two directories — the Accounting run line is `:848` (gate-r2 R2-M-2)) plus an explicit, tested exclusion list for classes that genuinely cannot run in that environment, each exclusion carrying a reason string. Directory inclusion is anchor-safe: new classes are gated the moment they land (the exact property the `tests/Feature/Security` comment `:284-293` celebrates).
- Either option must kill the unanchored-substring mechanism: if any `--filter` survives, anchor it (`^…Test$` per alternation) and add a CI-side lint (a script test) asserting every allowlist entry matches **exactly one** test class.
- **Exhaustive class-to-lane manifest + planted-class proof, whichever option (gate-r1 H-6):** the deliverable includes a **mechanically checked** assignment of EVERY `tests/Feature` class (or top-level directory) to either an automatic CI lane or a reasoned exclusion with a reason string — checked by a script/test that enumerates the tree and fails on any unassigned class, so the manifest cannot rot. Plus the **negative proof**: plant a throwaway Feature class in a previously-UNCOVERED directory → demonstrate the chosen mechanism automatically selects it (or fails loudly on the unassigned class); revert, paste both outputs. Merely pointing at the already-covered Treasury/Accounting directories (`ci.yml:833-851`) does not satisfy this — the defect is the OTHER directories.
- **Reciprocal reconciliation with P3 (gate-r2 R2-H-6; hardened gate-r3 R3-H-2/R3-H-3):** P3-M2 wires its country-chart completeness test into the lane 2(b) selects — and P3 DISPATCHES only after the WHOLE P2 package has landed (`p2_landed_sha` is a P3-M0 dispatch precondition; P3's base contains this milestone's outcome by construction, so there is no mid-wave pin and no mid-wave integration). Any LATER restructuring of the Feature lanes/allowlist by P2 follow-up work must reconcile against the landed P3 wiring — the landed P3 test must never be silently dropped from CI.

### 2(c) — i18n completeness gate

**Verified state:** there is **no i18n completeness check anywhere** — `apps/web/package.json`'s lint chain has no i18n audit (`audit:keys` is TanStack), and the vitest setup (`apps/web/src/test/setup.ts:2`) imports the **real** `lib/i18n` with `fallbackLng: 'en'` (`i18n.ts:440`) — so a key missing in `fr` silently falls back to English, a key missing everywhere renders as the raw key string, and no generic gate fails. The only guard is `src/lib/i18nRawKeyCoverage.test.tsx`, which pins **specific hand-chosen keys**.

**Deliverables:**
1. `apps/web/tools/audit-i18n-completeness.mjs` (+ **Vitest** unit tests in `tools/__tests__/`, matching the existing tools-test contract — every current file there imports from `vitest`, and they run via the new `pnpm test:tools` script; gate-r2 R2-H-3): for every namespace in the `ns` array (`i18n.ts:442`), compare key sets across en/fr/ar **on AUTHORED-LOCALE PROVENANCE — the source translation files/imports — never the final merged `resources` object (gate-r1 H-5 ruling).** The merged object lies: `i18n.ts` assigns whole namespaces to English for `ar` (`:392-405`, `:416-424`), spread-merges English keys underneath partial Arabic objects (`:429-430`), and the runtime has `fallbackLng: 'en'` (`:435-442`) — a scanner importing `resources` sees every aliased/spread-supplied English key as "present in ar" and reports vacuous full parity. **English-aliased namespaces and spread-supplied keys count as UNTRANSLATED** and belong in the ratchet baseline. Missing key = failure. **Baseline/ratchet if — and only if — the current tree has gaps** (certain for `ar`, per the aliasing above): same ratchet semantics as every other ratchet in this brief, **INCLUDING the anti-growth mechanism in its two-phase pinned form (gate-r2 R2-C-1; authority + pin order re-anchored gate-r3 R3-C-1/R3-H-4): the i18n baseline is generated in a dedicated SEED COMMIT (commit 1) at P2-M1; the YAML mirror pins (`i18n_baseline_seed_commit` / `i18n_baseline_protected_blob` / the PRE-ALLOCATED `i18n_baseline_pin_tag` name, non-null in the accepted candidate — gate-r5 R5-C-2) are recorded in a second metadata commit (commit 2); ONLY THEN does the milestone bridge review run, covering both commits — and every seed-changing FIX ROUND recreates the same two-step topology, never a single self-identifying fix commit (gate-r4 R4-H-3). The CI checker takes the protected blob hash from the owner-set repository variable `I18N_BASELINE_PROTECTED_BLOB` — never the YAML, never a branch name — mapped into the checker environment via `${{ vars.I18N_BASELINE_PROTECTED_BLOB }}` on every workflow event (gate-r4 R4-H-4); compares PARSED KEY SETS against that blob and FAILS on any added key; keys are removal-only; FAIL CLOSED on unset variable, unfetchable blob object, or YAML-mirror drift; blob reachability is guaranteed by the owner's durable pin tag fetched explicitly (`git fetch origin tag <i18n_baseline_pin_tag>` — name pre-allocated in the metadata commit, NEVER deleted; gate-r4 R4-H-2, gate-r5 R5-C-2); bootstrap + every re-pin are owner variable writes at promotion time, with the mirror updated in the post-promotion admin commit (gate-r3 R3-C-2); the before-state is the variable-held blob (PR-vs-push ambiguity moot). LOCAL green runs export the reviewed seed blob under the same env name after asserting it equals the mirror; the unset negative case runs in an isolated subshell (gate-r4 R4-H-4)** — with its own planted violation+matching-baseline-key tamper case, which must fail against the pinned comparison. Decide and record the `ar` policy explicitly: full-parity gate, or ratcheted-toward-parity — do not silently exempt it.
2. **Per-locale CLDR plural-category check.** A flat en↔fr key diff is **structurally blind** to plural-form gaps: the categories come from CLDR per locale, and French requires forms English has no counterpart for. Derive each locale's required categories at runtime — `new Intl.PluralRules(locale).resolvedOptions().pluralCategories` — and for every key family with any plural suffix (`_one`/`_other`/`_many`…), assert every required category's suffix exists in that locale. (Verify the category sets from the API itself at implementation time rather than trusting this brief's memory of CLDR.)
3. **Wire into CI as an EXPLICIT DISCRETE STEP in the `frontend-lint` job** (`ci.yml:853-891`), matching that job's existing step pattern (named step + `run: pnpm audit:…`, like `:875-883`). **NEVER via the `package.json` `lint` chain — `frontend-lint` does not run `pnpm lint`** (it runs discrete steps + `lint:ratchet`, whose `:885` comment says it supersedes the raw `pnpm lint`); a chain-only wiring ships a guard that runs in local preflight and **never in CI** — the exact guard-exists-but-never-runs defect class this package closes (r4/R0-3: the pre-r4 text claimed chain-wiring reached CI "with no new CI job needed" — mechanically false). Also add the script to the local `lint` chain for preflight parity, but that is a courtesy, NOT the CI wiring. **Acceptance greps `ci.yml` for the step** (see the acceptance block). **H-9 aggregate membership applies:** `frontend-lint` is in the `all-checks-pass` `needs` list today (`ci.yml:1104`, verified at this brief's base) — re-verify at your base; if it is absent there, wiring it in is in scope for this deliverable. **This is a CI-contract change (gate-r1 H-3): see the P2-M1 milestone classification below.**
4. Tamper tests (see 2(d)), **production-shaped (gate-r1 H-5)**: (i) delete a key in a fixture → audit fails; (ii) remove a `_many` form in a fixture → plural check fails; (iii) an **alias/spread fixture shaped like the live `ar` graph** (a namespace assigned wholesale to the `en` object; a `{...en, ...partialAr}` spread) → the audit classifies the aliased/spread-supplied keys as gaps, NOT as present.

### 2(d) — guard-liveness convention: every detector gets one tamper test

**Motivating incidents (both real):** the design-system **C6 detector regex rotted silently** — `audit-design-system.mjs`'s `STATUS_RE` never matched the `Tone`/`Tones` suffix, so C6 reported **0** for months while 25+ live violations existed (UI Wave 0 T7 fixes the regex; this package prevents the *class*). And **`no-parsefloat-on-money` has no RuleTester** — verified: `apps/web/eslint-rules/` contains `.test.mjs` files for `no-dead-tailwind-token-interpolation`, `no-hardcoded-step`, `no-literal-decimal-places`, and **none** for `no-parsefloat-on-money.js`, `no-untranslated-literal.js`, `no-hardcoded-entity-route.js`.

**Deliverables:**
1. RuleTester (`.test.mjs`, matching the existing three) for each untested ESLint rule: at minimum one valid case, one invalid case per distinct pattern the rule claims to catch.
2. A tamper test per audit script that lacks one (`audit-tanstack-keys.mjs`, `audit-design-system.mjs`, `audit-quantity-display.mjs` — check what exists in `tools/__tests__/` first; extend, don't duplicate): a fixture containing a known violation → the scanner reports it. The C6 case specifically: a `Record<X, StatusTone>` fixture must be detected (coordinate with UI Wave 0 T7, which writes exactly this test — same ownership rule as 2(a): if the UI YAML shows it landed, verify instead of author).
3. **The convention, written down:** a short section in `docs/conventions/` (new numbered file or the README) stating: *every detector/ratchet/audit script ships with at least one test proving it fires on a planted violation, in the same CI lane as the detector.* Cite both motivating incidents. This is what P1's tamper test and 2(c)'s tamper tests instantiate.
4. **Close the runs-nowhere gap (r4/R0-3 — an explicit NAMED deliverable, not a footnote): wire `pnpm test:eslint-rules` and the `tools/__tests__/` Vitest suite into CI.** Today both run in **NO workflow** (census above) — so every RuleTester and tamper test this package authors, plus the three existing rule tests and the existing `tools/__tests__/` suite, is CI-dead: violating deliverable 3's own convention (*"in the same CI lane as the detector"*) the moment it is written, unless `ci.yml` is edited. **The tools tests STAY ON VITEST (gate-r2 R2-H-3 ruling — every current `tools/__tests__/*.mjs` imports its suite API from `vitest`; `node --test` cannot execute them, and rewriting them to `node:test` would be an unrequested migration):** this package ADDS a `test:tools` script to `apps/web/package.json` — exactly `"test:tools": "vitest run tools/__tests__"` — and the CI step runs exactly `pnpm test:tools`. That SAME command string appears in the P2-M1 wiring, P2-M4, and the package acceptance block; every NEW tools test also follows the Vitest contract. Deliverable: a **discrete CI step** (or a separate job — **executor's choice, justified in the decision doc**) that runs BOTH `pnpm test:eslint-rules` AND `pnpm test:tools` on every event that starts the workflow (gate-r2 R2-C-2 — a direct dev push starts nothing); a step inside `frontend-lint` satisfies the same-lane convention naturally. H-9 applies: a new job joins `all-checks-pass` `needs`; a step inherits `frontend-lint`'s membership (`:1104` — re-verify at your base, per 2(c) deliverable 3). **Tamper proof (planted failing rule test):** plant a deliberately failing rule test (e.g. an invalid case the rule does not flag) → run the exact command the CI step invokes → it fails; revert → green; paste both outputs.

### Package 2 acceptance evidence

```bash
cd apps/web
# LOCAL AUTHORITY SETUP (gate-r4 R4-H-4) — before every normal green invocation and the M4 rerun:
#   seed_blob=$(git rev-parse <i18n_baseline_seed_commit>:<baseline-path>)
#   grep -q "$seed_blob" ../../docs/handoff/progress/enforcement-p2.progress.yaml   # assert == YAML mirror
#   export I18N_BASELINE_PROTECTED_BLOB="$seed_blob"
# Unset negative case, isolated so the export survives:
#   (env -u I18N_BASELINE_PROTECTED_BLOB node tools/audit-i18n-completeness.mjs; test $? -ne 0)   # must FAIL
pnpm lint                                  # green, now including i18n completeness (LOCAL preflight parity only —
                                           #   the lint chain is NOT the CI wiring; r4/R0-3)
node tools/audit-i18n-completeness.mjs     # exit 0; then plant a missing key in a fixture test → the unit test proves detection
pnpm test:tools                            # all tools tests green — script ADDED by this package
                                           #   ("test:tools": "vitest run tools/__tests__" — gate-r2 R2-H-3;
                                           #   the CI step runs this exact command)
pnpm test:eslint-rules                     # green, now covering the previously untested rules
# CI-wiring proof (r4/R0-3) — the guards must run in CI, not just locally:
grep -n 'audit-i18n-completeness' ../../.github/workflows/ci.yml   # → the discrete frontend-lint step (2(c) deliverable 3)
grep -n 'test:eslint-rules\|test:tools' ../../.github/workflows/ci.yml  # → the 2(d) deliverable-4 step/job
                                           #   (pattern updated gate-r2 R2-H-3: the CI step runs `pnpm test:tools`)
# H-9 aggregate membership: frontend-lint (and any NEW job) present in all-checks-pass `needs` (ci.yml:1104 at this
#   brief's base — re-verify at yours; absent = wiring it is in scope)
# For 2(b): paste the decision doc path + the LOCAL timing measurement for the chosen option (single local
#   run of the lane's exact commands, wall time recorded) + the class-to-lane manifest check output + the
#   planted-class negative proof. NO CI run link FROM THE EXECUTOR — the executor never pushes (gate-r1
#   H-7); remote CI verification is the owner's MANDATORY PRE-promotion workflow_dispatch gate
#   (gate-r2 R2-C-2/R2-H-2; receipt semantics gate-r3 R3-C-2): a green run on EXACTLY the accepted SHA,
#   which is merged UNCHANGED — the receipt goes into pre_promotion_ci_dispatch + LEDGER §2 row S-14
#   in the POST-promotion admin commit.
# Event-graph acceptance check (gate-r2 R2-C-2; receipt semantics gate-r3 R3-C-2): the handback NAMES
#   the frontend-lint step ids / any new job id the owner's pre-promotion workflow_dispatch run must
#   show executed; the parent verifies the run (green, head SHA == the exact accepted candidate SHA ==
#   the SHA that will be merged unchanged, those steps/jobs executed) BEFORE merging, then records it
#   in pre_promotion_ci_dispatch in the POST-promotion admin commit.
# For 2(a): paste the OWNERSHIP disposition first (ui-wave0.progress.yaml M4 status + ci.yml grep +
#   OpenAPI-lane wiring state), then per the branch taken: the verify-only record (no `if:` + aggregate
#   `needs` membership grep) OR the new job's YAML + a local red/green drift demonstration (throwaway
#   route, not committed).
```

**Milestones (operative list in `enforcement-p2.progress.yaml`):** `p2-M0` = machine-checked preconditions (`quiet_window_ack` — the owner-approved quiet window + the PRELIMINARY scope notice, gate-r2 R2-H-1 — + ownership snapshot recorded). `p2-M1` = 2(c) + 2(d) — **classified as a CI-CONTRACT CHANGE (gate-r1 H-3; mechanism corrected r4/R0-3):** adding the i18n-audit step (2(c) deliverable 3) and the rule-test/tools-test step or job (2(d) deliverable 4) to the `frontend-lint` job / `ci.yml` changes that job's pass/fail contract for every open frontend branch, so M1 gets the same quiet-window, announcement, and observe-first/baselined treatment as M2/M3 — the previous "pure additions, no behavioural CI change" label was false, and so was the pre-r4 mechanism ("wire via the `package.json` `lint` chain, no new CI job needed" — the chain never reaches CI; the classification stands, now correctly grounded in the workflow edit itself). M1 lands the seed commit (commit 1) + the mirror-pin metadata commit (commit 2) BEFORE its bridge review (gate-r3 R3-H-4; the checker's authority is the owner variable `I18N_BASELINE_PROTECTED_BLOB` — gate-r3 R3-C-1) and the `test:tools` script + `pnpm test:tools` CI wiring (gate-r2 R2-H-3). `p2-M2` = 2(b) decision doc + implementation (P3 dispatches only after the WHOLE P2 package lands — gate-r3 R3-H-2/R3-H-3; the reciprocal never-drop-the-landed-P3-wiring obligation in 2(b) stands). `p2-M3` = 2(a) + the merge-announcement checklist for the parent (which lanes are open, what they must rebase for — incl. the OpenAPI reconciliation line). **The checklist is the INPUT to the parent's FINAL announcement, SENT after M3/M4 acceptance and before promotion; `merge_announcement_ack` records that fact in the POST-promotion admin commit (gate-r2 R2-H-1; gate-r3 R3-C-2 — nothing is committed to the accepted candidate between ACCEPT and merge). The preliminary M0 notice cannot and need not contain it.** `p2-M4` = **WHOLE-PACKAGE gate: rerun the full accumulated evidence with every lens over the integrated branch (gate-r1 H-8)** — including `pnpm test:tools` (the exact command string, gate-r2 R2-H-3) and the event-graph acceptance check hand-off (gate-r2 R2-C-2). Gate each via the harness.

---

## 4. Package 3 (P2) — queued money-lane hardening (after Package 1 lands)

### 3(a) — balance-guard census + closing the genuinely unguarded seams (RESCOPED, gate-r1 C-2 ruling)

**Correction of record: the GL posting chokepoint ALREADY balance-validates — do not re-guard it, and do not present already-guarded paths as new coverage.** `GeneralLedgerService::sealAndPersistEntry` loads every journal line, sums debit and credit with `bcadd`, and throws before sealing when `bccomp` differs (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3480-3510`). Both `postEntry()` (`:2874`, via `postEntryWithOptionalActor`) and `postEntryNow()` (`:3450`) funnel into it. The named treasury bridges/services post via `postEntryNow` — `TreasuryReceiptBridge` (`:473,547,1387`), `InstrumentLifecycleService` (`:274,456,488,847`) — so a grep for `DoubleEntryValidator`/`isSumBalanced`/`Unbalanced` proves nothing about those paths: they are guarded by the chokepoint's own algorithm, just under different names. The earlier framing ("zero validator references ⇒ remaining gap") was false for the chokepoint.

**The work, rescoped:**
1. **Semantic classification (consumes P1's census):** classify every `journal_entries` creator by BEHAVIOR, not by grep —
   - **(a) draft-only creators** — e.g. `createPOSChargeEntry()` returns a Draft without posting (`GeneralLedgerService.php:4072`). For each: who posts the draft, through what guard, and can a draft reach `posted`/sealed state without passing `sealAndPersistEntry`?
   - **(b) posted through the existing chokepoint guard** — characterize as already-guarded; NO new validator, NO "red-first" test (it would be green at base).
   - **(c) bypassing the chokepoint** — writes posted/sealed rows without `sealAndPersistEntry`. These are the genuine gaps.
2. **Close ONLY class (c) seams** (plus any draft-posting flow that seals outside the chokepoint). Prefer routing the writer through the existing chokepoint over adding a parallel assertion; where routing is impossible, a constructor-injected `DoubleEntryValidator` (rule 13, never `app()`). **Never copy a second balance algorithm into `sealAndPersistEntry`.**
3. **Red-first proof mandatory per targeted path (gate-r1 C-2 ruling):** for every path you claim to guard, the deliberately-unbalanced-post test must be demonstrated FAILING at base BEFORE your change. **A green-at-base test disqualifies the target** — reclassify it as (b), record it in the census, and present no new test as evidence for it.
4. **Chokepoint failure-mode normalization (scoped, optional):** if the census shows the chokepoint's `InvalidArgumentException` posture diverges from the house throw+alert pattern for QUEUED contexts (the job must fail loudly and retryably/dead-letter per the existing projection error discipline — an unbalanced projection silently swallowed is the exact incident class this closes), propose/implement exception-and-alert normalization there as its own recorded deliverable — not a duplicate validator.
5. **Rule 19/20 compliance in every touched queued path:** scale comes from the entity currency (`getScale($currency)` / `getScaleSafe`), never a bare no-arg resolve — queued workers have NO CompanyContext; projection tests must `app(CompanyContext::class)->clear()` before `apply()`.
6. **The −19.000 live entry is NOT code scope (gate-r1 H-10 ruling).** It is the campaign's own DOC06 probe via a since-fixed VAT-zeroing bug (W-6 D1b); the disposition (forward correcting entry or evidence-pack annotation on the demo tenant, owner-side) is tracked at **`docs/handoff/LEDGER.md` §2 row S-15** — the single authoritative owes list. The handback POINTS to that row; it never carries its own status for this item.

### 3(b) — per-country purpose completeness as a CONSUMER of `ProvisioningRequiredPurposesV1` (RESCOPED, gate-r1 C-3 ruling)

**Authority ruling of record:** the country-defaults Phase A lane owns the operational purpose set. Its binding decision (`docs/handoff/CODEX-DISPATCH-country-defaults-phase-a-2026-08-10.md:57-66`, D-6): **`requiredPurposes()` is NOT the operational manifest, and conformance must not key off it.** That lane ships `ProvisioningRequiredPurposesV1` — the complete 41-case partition (27 REQUIRED / 1 SCOPE-REQUIRED / 4 CONDITIONAL / 9 SOFT) with per-entry call site, classification, gate kind and evidence — plus its conformance suite and an AST registration ratchet (`:289-318`; `docs/handoff/progress/country-defaults-phase-a.progress.yaml:33-40`), and freezes the three country seeder class bodies (`:314-318`). P3(b) therefore:

- **Sequences AFTER that lane lands — and "landed" means the WHOLE LANE, never an M1 snapshot (gate-r2 R2-H-5).** P3-M0 precondition, machine-checked: (i) the manifest class exists at your base (`git cat-file -e <base_sha>:apps/api/app/Modules/CountryDefaults/Domain/Services/ProvisioningRequiredPurposesV1.php`); (ii) the parent has pinned `country_defaults_landed_sha` in `enforcement-p3.progress.yaml` — the commit at which the accepted country-defaults Phase A branch was merged to local dev; (iii) `country-defaults-phase-a.progress.yaml` reads top-level `status: complete` at base, with M7 — the whole-branch integration gate (`country-defaults-phase-a.progress.yaml:81-88`) — `status: passed`, a parseable ACCEPT verdict artifact at its recorded `verdict:` path, and a recorded `commit`; (iv) ancestry holds from that accepted country tip through `country_defaults_landed_sha` to P3's `base_sha` (`git merge-base --is-ancestor` chain). **Checking M1 alone is explicitly INSUFFICIENT** — a partial cherry-pick can carry the manifest file plus an M1-passed YAML snapshot without the lane having passed M7 or merged, and P3 would then consume classification content that is not the landed authority. Absent/failing → `blocked_precondition` for M2 at minimum; do NOT proceed on prose assurances.
- **CONSUMES the manifest; never redefines it.** P3(b) does NOT extend `requiredPurposes()` on its own authority, does NOT key any new test off `requiredPurposes()`, does NOT edit `ProvisioningRequiredPurposesV1`'s classification content, and does NOT touch the frozen country seeder class bodies.
- **Deliverables:**
  1. **Reconciliation, not census-building:** reconcile `TreasuryReceiptBridge`'s local required-purposes lists (`:446`, `:528`) — and any other local list the P1 census surfaces — against the manifest's classification. A divergence (a purpose a live path resolves-or-fails on that the manifest classifies differently, or omits) is a **REPORTED finding handed to the country-defaults lane / parent**, with evidence — never a unilateral manifest or `requiredPurposes()` edit.
  2. **Per-country seeder/template completeness CI test:** for every seeded country chart (TN, FR, and whatever else `database/seeders`/country packs define — enumerate, don't assume), assert every purpose the **manifest classifies as REQUIRED** maps to an account in that country's chart, AND the mapped account's type matches `expectedAccountType()` (`SystemAccountPurpose.php:~186`). Keyed to the manifest — never to `requiredPurposes()`. Wire it into a CI lane that actually runs (Package 2(b)'s outcome determines where; worst case, the pgsql allowlist — anchored, per 2(b)). **Precondition moved to dispatch (gate-r2 R2-H-6, hardened gate-r3 R3-H-2/R3-H-3): the WHOLE P2 package must be LANDED before P3 dispatches — P3-M0 verifies the parent-pinned `p2_landed_sha` is an ancestor of `base_sha`, `enforcement-p2.progress.yaml` at base reads top-level `status: complete` with M4 `status: passed` + a parseable ACCEPT verdict artifact + a recorded commit, and ancestry M2.commit → M4.commit → `p2_landed_sha` → `base_sha` holds (each hop `git merge-base --is-ancestor`) — so "per 2(b) outcome" is a landed fact before ANY P3 work begins; an M2-passed snapshot alone is INSUFFICIENT (same forgery class R2-H-5 closed for country defaults). P2 carries the reciprocal obligation (§3 2(b)): later lane restructuring must reconcile against the landed P3 wiring.**
  3. Tamper test: remove one purpose mapping from a fixture chart → the test names the country and the purpose.
- **F-4 rescoped accordingly (§6):** any proposal that would tighten validation for EXISTING tenants (e.g. growing what `ChartOfAccountsService::validateCompanyAccounts()` iterates, `ChartOfAccountsService.php:51`) remains an owner gate — every newly-required purpose needs its backfill/seed story (`BackfillChartPurposesCommand` exists; **push-to-origin/dev auto-deploys migrations to staging incl. `tenants:migrate`** — anything with a manual prerequisite must be self-guarding) — but it is now a cross-lane PROPOSAL routed through the country-defaults authority, never a unilateral P3 change.

### Package 3 acceptance evidence

```bash
cd apps/api
# The acceptance command list is DERIVED FROM THE CLASS-(c) CENSUS ROWS (gate-r2 R2-H-7 ruling —
# supersedes the r3 hard-coded Treasury/BatchExpiry/Accounting trio, which could green with zero
# selected tests or demand disallowed already-green work). The RULE is:
#   - for EVERY census row of class (c): name its test path + anchored --filter, run it, and assert the
#     PHPUnit summary shows a NONZERO selected-test count (parse the "OK (N tests, …)" / "Tests: N" line,
#     N >= 1). An EMPTY SELECTION IS A FAIL: apps/api/phpunit.xml sets no failOnEmptyTestSuite, so PHPUnit
#     exits 0 on zero selected tests — the exit code alone proves nothing. Red-first evidence preserved
#     and pasted per row.
#   - every class (b) row appears in the census as 'already guarded — characterized' with NO new test
#     presented as fresh evidence (that would be the C-2 false-positive).
#   - a module with ZERO class-(c) rows submits the reviewed census evidence only — NO placeholder green
#     command may be invented for it.
./vendor/bin/phpunit <census-derived-test-path> --filter '^…UnbalancedGuard…$'   # ONE line per class-(c) census row;
                                                                                 # nonzero selected-test count asserted
./vendor/bin/phpunit <path-to-country-chart-completeness-test>
# Paste: the write-path census table (from P1) with per-path disposition (draft-only / chokepoint-guarded / genuine gap→fixed / N-A with reason)
# Paste: the purpose RECONCILIATION table (bridge local lists ⇄ ProvisioningRequiredPurposesV1 classification, divergences flagged as reported findings)
./vendor/bin/phpstan   # level 8, zero new errors (needs a live-DB env — see §5)
```

**Milestones (operative list in `enforcement-p3.progress.yaml`):** `p3-M0` = machine-checked preconditions (the WHOLE P1 package landed per gate-r4 R4-H-5: top-level complete + M3 ACCEPT + `M3.commit → p1_landed_sha → base_sha` — + country-defaults lane FULLY landed per gate-r2 R2-H-5 + the WHOLE P2 package landed per gate-r3 R3-H-2/R3-H-3: `p2_landed_sha` + P2 top-level `status: complete` + M4 ACCEPT + the M2.commit → M4.commit → `p2_landed_sha` → `base_sha` ancestry chain — + the gate-r5/r6 EVIDENCE BINDING for both, as TWO tag predicates per package (gate-r11 R11-H-1): PIN tag at A (manifest_sha256 + register/handback digests == landed bytes + control-name set) and CLOSE tag at C (derived first-parent child, conforming diff-tree, base blobs == C's blobs, exactly {C, pass, A}) (R5-C-1/R5-H-3/R6-H-1/R6-H-2/R10-H-2) + phase series). `p3-M1` = 3(a) — lenses `treasury,fiscal-pos,stock-gl-interaction,inventory-costing` (gate-r1 H-8: `ReverseWriteOffService` crosses the stock↔GL seam; the landed `.claude/agents/stock-gl-interaction-reviewer.md` contract applies); acceptance commands census-derived with nonzero-selection assertion (gate-r2 R2-H-7). `p3-M2` = 3(b) — lenses `treasury,fiscal-pos` (the P2 dependency is already satisfied at M0 — gate-r3 R3-H-3). `p3-M3` = **WHOLE-PACKAGE gate: rerun the full accumulated evidence with every lens over the integrated branch**. Gate each via the harness.

---

## 5. Working rules (Codex desktop session)

- **Branch/worktree:** fetch first; branch off the base the parent pins in your package's progress YAML. Dedicated `git worktree`; never edit/commit in a shared `dev` worktree; **never `git stash`** (stash stack is repo-global across worktrees); never force-push; the `dev-push-guard` hook enforces part of this. Suggested branches: `codex/enforcement-p1-cementing-guard`, `codex/enforcement-p2-ci-guards`, `codex/enforcement-p3-money-lanes`.
- **Worktree backend env gotchas (they will bite):** symlinked vendor runs stale main-repo code; run tests **by path**; **phpstan needs a live-DB env**; validation errors use the `{error:{errors}}` envelope. See the worktree-gotchas memory file if the parent supplies it.
- **Commit format:** `AGENTS.md:16` requires `Phase <major.minor.patch>: <imperative summary>`. ⚠️ **The phase series for this wave is unassigned — the parent writes it into your progress YAML's `commit_series` before dispatch; null at M0 = `blocked_precondition`. Do not invent one** (§6 F-1).
- **TDD, always:** failing test first, failure pasted, then implementation. For scanners/ratchets the failing test is the fixture-based tamper case.
- **NEVER run the full PHPUnit suite** — it crashes the owner's laptop. Targeted paths only. `PREFLIGHT_SCOPE=full` is forbidden on this machine.
- **Preflight before claiming completion:** `./scripts/preflight.sh` with the default paths scope; report PHPUnit as **skipped** (with `PREFLIGHT_TEST_PATHS` covering your touched paths where feasible), never as green-by-omission; every non-PHPUnit stage must genuinely pass.
- **Scope discipline (rule 4):** guard-only means guard-only. Every "while I'm here" fix is a deviation to be recorded, not committed.
- **CI/workflow changes — NO PUSH, EVER by the executor (gate-r1 H-7 ruling; the no-push contract wins over the old "push the branch to dry-run it" rule, which is hereby deleted).** The executor never pushes any branch. Evidence for every workflow edit is layered (gate-r2 R2-C-2/R2-H-2):
  1. **LOCAL (executor):** YAML validity — `actionlint` if available on the machine, else a YAML parse; record which was used, and record `actionlint`'s absence explicitly if unavailable. **Either way this layer is explicitly INSUFFICIENT for the Actions job/event graph** — it proves syntax, never that jobs trigger, execute, or gate anything.
  2. **LOCAL (executor):** execution of the exact command(s) the job runs, output pasted.
  3. **LOCAL (executor):** written job-graph reasoning (`needs` / `if:` / event analysis, incl. the skipped-job-semantics argument for aggregate entries) — against the TRUE event graph (`ci.yml:3-8`, `:740-747`): a direct `dev` push never starts the workflow.
  4. **THE PROMOTION SEQUENCE — UNIVERSAL for EVERY package (gate-r5 R5-H-4), owner/parent-executed (gate-r2 R2-C-2; receipt cycle gate-r3 R3-C-2; freshness/evidence-landing gate-r4 R4-C-1/R4-C-2; binding/lifecycle/race/recovery gate-r5 R5-C-1..3/R5-H-2/R5-H-3).** In order, with **NOTHING committed to the package between final-gate ACCEPT and merge**. **Serialization (gate-r5 R5-C-3): the parent performs NO local-dev merge of ANY lane between a package's step 0 and step 5** — all local-dev merges go through the parent (single writer), so the critical section is enforceable by the one actor holding the pen.
     0. **Freshness assert:** `git merge-base --is-ancestor <current-local-dev-tip> A` — only a FAST-FORWARD to A is legal. Failure → re-gate protocol.
     1. **The PARENT ITSELF runs the final whole-package gate via `scripts/adversarial-review-final.sh` (gate-r6 R6-C-1; trusted inputs gate-r7 R7-C-1; SEALED gate-r8 R8-C-1)** against the handed-over branch tip **A** (the executor completed the final milestone, set it `status: review`, and handed over — an executor-run "final" register is NOT acceptance evidence). **The sealed sequence, in order:** (i) the parent COPIES the handback to its own IMMUTABLE workspace path — that copy is THE handback for the review, the annotation, and C (gate-r8 R8-H-2); (ii) verifies the handed-over worktree's `git status --porcelain` shows EXACTLY ONE entry — `?? <handback-source>`, the untracked handback (gate-r9 R9-H-2; the bridge itself performs the copy+hash in this step, so the source remaining present is expected — any OTHER entry, or a tracked-modified handback, is rejected); (iii) **control-file preflight:** `git diff --name-only <base_sha>..A` must show NO change to `scripts/adversarial-review.sh`, `scripts/adversarial-review-final.sh`, this brief, `docs/handoff/SELF-REVIEW-HARNESS.md`, `docs/handoff/enforcement-control-manifest.yaml`, or `.claude/agents/*-reviewer.md` (any hit is simultaneously a scope FAIL); (iv) the final bridge — the parent's copy, self-verified against **`docs/handoff/enforcement-control-manifest.yaml`** (gate-r8 R8-H-1: every control input `sha256`-checked; lenses/`max_fix_rounds`/milestone id sourced from the MANIFEST, never candidate YAML, which is field-checked against it) — materializes a FRESH READ-ONLY DETACHED WORKTREE at exactly A and reviews THAT tree with lens-contract CONTENT injected; (v) the bridge computes the handback `sha256` ITSELF and echoes it in the register (the digest is NEVER in the prompt); the reviewer must quote content-derived census evidence (row counts + first/last row keys); the parent verifies the echoed digest equals its own pre-invocation hash. **r19 bindings (gate-r9/gate-r10): the bridge is RECEIPT-DRIVEN — `--receipt` names the PARENT DISPATCH RECEIPT at `~/.claude/projects/-Users-houssamr-Projects-syneriva-apps-erp/memory/dispatch-receipts/enforcement-<pkg>.receipt.yaml`, which supplies the true base, the canonical ABSOLUTE manifest path, and the expected manifest digest (R10-C-1); strict-ancestor + `BASE != A` checks and a FAIL-CLOSED candidate-YAML field-check (PyYAML required — R10-H-1: base_sha + structured `control_manifest: {path, sha256}` vs the receipt, exactly one final milestone at `status: review`, `fix_rounds <= max`, top-level `max_fix_rounds` == manifest); the range preflight also rejects any added `*control-manifest*` surrogate; the seal admits EXACTLY ONE dirty entry, the untracked handback source, which the bridge itself copies+hashes to a canonicalized dest OUTSIDE both checkouts (R9-H-2, R10-H-4); the reviewer runs from a NEUTRAL EMPTY cwd with the verified brief+harness contents injected and all-absolute paths, the snapshot is re-asserted post-run (R9-H-1); verdict parsing is single-line exact-equality AND the verdict must be the final non-empty line (R9-H-4, R10-H-3). Exact invocation template: `scripts/adversarial-review-final.sh --receipt <receipt> --package <p1|p2|p3> --candidate <worktree> --accepted <A> --handback-source <relpath> --handback <abs-dest> --round <n> --attempt <m> --out <abs-register>`.** On ACCEPT, **the parent immediately digests the register its own run produced; before tagging it re-hashes the immutable handback copy and requires exact equality.** Registers are never edited after digest and never committed into A. **Final fix rounds (gate-r7 R7-H-1; tool-error split gate-r8 R8-H-3): a SUBSTANTIVE CHANGES-REQUIRED register transfers control to the executor — it alone fixes, commits, increments `fix_rounds`, resets to `status: review`, hands over; `max_fix_rounds` exhaustion = `blocked_review`. A `tool_error` (exit 3 / no parseable verdict) is PARENT-OWNED: retry the SAME A with `--attempt <m+1>`, no candidate commit, no `fix_rounds` increment, registers at unique parent-workspace paths `round<n>-attempt<m>`, bounded at 3 attempts then `blocked_review` with a tool-error note. Superseded parent registers are quarantined in the parent workspace; exactly the final ACCEPT attempt's bytes land in C.**
     2. *(Ratchet packages P1/P2 only.)* **Preflight first (gate-r5 R5-H-2): verify the PRE-ALLOCATED tag name recorded in A's mirror block is still free on the remote.** Then the owner sets/updates the repository variable(s) to the gate-reviewed seed blob hash and VERIFIES the read-back value equals it (a wrong value is re-set even when the seed did not change). For P2: the parent SENDS the final merge announcement built from the M3 checklist.
     3. **The owner pushes the package's pre-allocated ANNOTATED closing pin tag at exactly A** — name from A's reviewed mirror field (allocation rule: `ci-pin/enforcement-<pkg>-r<n>`, `n` = 1 + highest existing; collision = next `n`, never reuse; gate-r5 R5-C-2), **annotation recording `{A, sha256(final register(s)), sha256(handback)}` (gate-r5 R5-C-1), in the EXACT machine-parseable schema (r12 ruling — P3-M0 must parse it): line 1 `accepted_sha: <A>`; line 2 `manifest_sha256: <hex>` (the dispatch manifest's digest — gate-r10 R10-H-2); `register_sha256: <path>=<hex>` (one line per register file); `handback_sha256: <path>=<hex>`; final lines `control_sha256: <name>=<hex>` for the trusted bridge/brief/lens-contract blobs used by the parent's final invocation (gate-r7 R7-C-1)** — and verifies the tag resolves to A. **Pin tags are NEVER deleted.** *(Workflow-touching packages only:)* the owner also pushes **exactly A** to a throwaway remote ref and runs `workflow_dispatch` on it, verifying: head SHA **== A**, GREEN, the package's new job(s)/step(s) executed (event-graph acceptance check). The throwaway ref may be deleted after the run; the tag stays forever.
     4. **Step 4a — full pre-merge RE-verification (gate-r5 R5-C-3; extended gate-r6 R6-H-5, AFTER any dispatch run):** repeat `git merge-base --is-ancestor <current-dev-tip> A`, AND re-fetch the closing tag verifying its target == A and its EXACT annotation bytes (incl. the `manifest_sha256:` line — gate-r10 R10-H-2), AND re-read the repository variable verifying its value — any mismatch (a candidate workflow with self-granted write authority could have moved them) → re-gate protocol. **Owner workflow-authority check (gate-r6 R6-H-5): if the candidate's workflow diff grants content/ref write authority (`permissions:` containing `contents: write` or equivalent in any changed workflow file), promotion is BLOCKED** (the repo's `default_workflow_permissions` is `read` — verified `gh api` — so only an explicit grant matters). Then the parent merges **exactly A** to local dev (fast-forward) — promotion SHA == accepted SHA == dispatched head.
     5. ONE **CLOSING ADMIN COMMIT** lands ALL closing evidence together — and its identity is DETERMINISTIC (gate-r6 R6-H-2): **C is THE first-parent child of A on dev; `C^ == A` is verified; there is no `<admin-tip>` placeholder.** Contents: the digested final register(s) and handback (exact bytes from step 1), the progress-YAML final status/commit/verdict + top-level `status: complete` **+ the structured `closing_receipt` (gate-r6 R6-H-4; de-self-referenced gate-r7 R7-C-2 — C contains ONLY what is knowable before commit: `accepted_sha:` A · `tag:` name · `digests:` the annotation payload · `manifest:` the mandatory closing-path list · `preflight: pass`; NEVER C's own SHA, NEVER post-commit output)**, `merge_announcement_ack` for P2, and the LEDGER row update. **Named closing check (gate-r5 R5-H-3 / gate-r6 R6-H-2-H-3), run by the parent:** PREFLIGHT the staged set BEFORE committing (every mandatory closing artifact present; nothing outside the closing set), then verify after commit: `git rev-list --count <A>..C` == 1 AND `git diff-tree` shows every mandatory artifact AND every path inside the CURRENT package's closing set — its own `docs/handoff/progress/enforcement-<pkg>.progress.yaml` · its own `docs/handoff/reviews/enforcement-<pkg>/**` · its own `docs/handoff/HANDBACK-enforcement-<pkg>-*.md` · `docs/handoff/LEDGER.md`. Any other path — including ANOTHER package's evidence — = closing FAIL (no production/workflow path, ever; admin-only-delta, `a5520f23c`, narrowed gate-r5). **After the post-commit check PASSES, the OWNER pushes the annotated CLOSE TAG `ci-close/enforcement-<pkg>-r<n>` AT C (gate-r7 R7-C-2) — annotation `{closing_commit: <C>, closing_check: pass, accepted_sha: <A>}` (the check attestation is the owner's — the unverifiable transcript hash is dropped, gate-r8 R8-M-1), same never-delete/never-reuse rules — the second owner-authenticated object holding everything C cannot truthfully contain about itself. Every consumer verifies `git rev-parse <close-tag>^{commit} == C` (gate-r8 R8-M-1) — AND the PARENT, after EVERY close-tag push including P3's no-workflow path, re-fetches the tag and verifies `^{commit} == C` plus exactly one each of `closing_commit` == C, `closing_check` == pass, `accepted_sha` == A before the package is closed (gate-r10 R10-M-2). Step-5 recovery (gate-r6 R6-H-3): a failed post-commit check may be repaired by AMEND/REDO ONLY while C is still the unpushed local dev tip AND before the close tag exists (the amend is recorded in the eventual close-tag annotation); once anything else lands on dev or the close tag is pushed, a failed close BLOCKS the next promotion until a verified corrective closing state exists.**
     **Re-gate protocol (the ONLY route back), triggers broadened gate-r5 R5-H-2, made IDEMPOTENT gate-r6 R6-H-3:** step-0 or step-4a failure, a RED dispatch run, or ANY external side-effect failure (tag collision / tag push failure / tag not resolving to A / variable read-back mismatch / ref-push or dispatch API failure / announcement failure). Fail-closed handling: STOP promotion, restore the last known-good variable value where possible, record the partial state in LEDGER S-14, then either RETRY the SAME unchanged A from step 2 (when A is still fresh and the failure was external) or return the package to an IN-PROGRESS final milestone: rebase onto the current dev tip, rerun every invalidated piece of local evidence, hand over for the PARENT-invoked whole-package bridge → NEW accepted tip A′, re-send the P2 announcement for A′ (re-send whenever A changes), re-verify the variable, fresh dispatch, restart from step 0. **Idempotency (gate-r6 R6-H-3): on a same-A retry, an existing tag whose target AND full annotation exactly match `{A, digests}` IS the successful step 3 — resume after it (a free-tag preflight does not re-fire); an existing tag that MISMATCHES = re-gate with a newly reviewed name (the old tag stays, never deleted, never reused).** Never merge a stale A; never force-move dev; never rebase without re-gating.
     **For workflow-touching packages, no green run on exactly the promoted SHA = PROMOTION BLOCKED** — audited via `docs/handoff/LEDGER.md` §2 row **S-14**; the YAML fields are the durable post-promotion record, never pre-promotion contents of the candidate. A post-promotion-discovered workflow defect blocks the NEXT promotion until a green dispatch run on the fixed revision exists. Never executor acceptance evidence; no acceptance criterion in this brief may demand a CI run link from the executor.
  - **Explicitly DECLINED alternative (gate-r2 R2-C-2 ruling, on the record):** adding `dev` to `on.push.branches` was considered and REJECTED for this program — pushes to `origin/dev` also trigger the staging auto-deploy (incl. `tenants:migrate`), so widening CI onto every dev push is an owner cost/behaviour decision, not something an enforcement package smuggles in. The owner may revisit this separately; until then the pre-promotion dispatch gate above is the promotion-path guarantee.

---

## 6. Flags for the parent session (do not resolve these yourself)

- **F-1 — Phase number unassigned.** Same as every recent wave: parent supplies the `Phase x.y.z` series in each package's progress YAML `commit_series` (or a recorded exception) before the first commit.
- **F-2 — OWNER GATE (conditional): CI-minutes budget for Package 2(b) Option A.** Enabling the Feature suite on PG in CI has a real runtime cost. If the LOCAL measurement shows it is material, the A-vs-B choice is the owner's, with the measured number in front of them. STOP condition (B) if it fires. Encoded in `enforcement-p2.progress.yaml` as a conditional `owner_gates:` record — NOT a milestone `owner_gate:` field.
- **F-3 — Ownership of the manifest-drift job and the OpenAPI CI wiring (gate-r1 H-2).** Resolved by RECORDED OWNERSHIP, not presence: 2(a)'s decision rule reads `ui-wave0.progress.yaml` (and the OpenAPI lane state) and implements only on relinquish/complete-without-artifact; ambiguity = `blocked_owner`. Never both lanes authoring the same CI job.
- **F-4 — OWNER GATE (conditional, rescoped by gate-r1 C-3): chart-validation tightening has live-tenant consequences.** P3(b) itself no longer extends `requiredPurposes()`; but if reconciliation produces a proposal that tightens validation for existing tenants, the owner acknowledges the backfill/seed story per purpose before it goes anywhere — routed through the country-defaults authority. Staging auto-deploy runs `tenants:migrate` on push; anything with a manual prerequisite must be self-guarding. STOP condition (B) if a proposed purpose lacks a self-guarding seed path. Encoded as a conditional `owner_gates:` record in `enforcement-p3.progress.yaml`.
- **F-5 — UNVERIFIED items in this brief, to be pinned at dispatch:** (i) the 2026-08-08 owner sequencing ruling's file location (relayed by orchestrator; content not in doubt, pointer missing); (ii) DPA 3C's live milestone state (memory-derived, may be stale — superseded operationally by the `dpa_3c_merge_sha` pin, gate-r1 H-1); (iii) whether the pinned base carries the S0 seam and the `assertLegsBalance` sites — both MUST be re-verified at the actual base SHA (P1-M0 does the S0 check mechanically), and their absence is `blocked_precondition`, not a reason to improvise.
- **F-6 — Package 2 merge announcement (SPLIT, gate-r2 R2-H-1).** The parent owns telling every open lane before the CI-behaviour merge lands — in TWO parent-owned artifacts, because the final checklist does not exist until M3 has run: (1) **BEFORE dispatch:** the owner-approved quiet window + a PRELIMINARY scope notice (what will change, roughly when) sent to all active lanes — this is what `quiet_window_ack` attests; (2) **AFTER M3/M4 acceptance, BEFORE promotion:** the FINAL announcement, built from P2-M3's generated checklist with the as-implemented contract changes (incl. the `frontend-lint` step additions — gate-r1 H-3; mechanism corrected r4/R0-3) and a one-line "what you must rebase for" per lane — SENT before the merge; the `merge_announcement_ack` pin records the sent fact in the POST-promotion admin commit (gate-r3 R3-C-2 — the pin is the record, not a pre-promotion candidate content). The announcement itself is never the executor's to send.
- **F-7 — LEDGER rows (gate-r1 H-7/H-10; S-14 semantics rewritten gate-r2 R2-H-2, receipt cycle fixed gate-r3 R3-C-2).** `docs/handoff/LEDGER.md` §2 rows **S-14** (owner-executed **PRE-promotion** `workflow_dispatch` verification of every enforcement-guards workflow change — green run on EXACTLY the accepted SHA, which is merged UNCHANGED; receipts recorded in `pre_promotion_ci_dispatch` in the POST-promotion admin commit; no green run = promotion blocked; a post-promotion-discovered defect blocks the NEXT promotion until a green run exists) and **S-15** (the −19.000 demo-tenant disposition) are the authoritative records; handbacks point to them.
- **F-9 — PRE-DISPATCH CONTROL DELIVERABLES (new gate-r8 R8-H-1; PARENT/OWNER-owned, exist in this working tree as of r16).** Two artifacts must be present, tested, and pinned BEFORE any package dispatch: (1) **`scripts/adversarial-review-final.sh`** — the parent-only final-gate bridge (manifest verification, sealed detached-worktree review, lens-content injection, independent handback digest, tool-error exit discipline); (2) **`docs/handoff/enforcement-control-manifest.yaml`** — the control manifest pinning `sha256` for the bridge, this brief, the harness doc, and all six lens contracts, plus each package's immutable review controls. **At dispatch the parent: (i) REFRESHES the manifest's hashes in the pre-dispatch commit AFTER all control inputs are byte-stable (the refresh gate covers incidental whitespace — gate-r10 R10-M-1); (ii) CREATES the per-package PARENT DISPATCH RECEIPT at `~/.claude/projects/-Users-houssamr-Projects-syneriva-apps-erp/memory/dispatch-receipts/enforcement-<pkg>.receipt.yaml` (schema per the shipped TEMPLATE; outside the repo — gate-r10 R10-C-1) and names its path in LEDGER S-14; (iii) fills each package's structured `control_manifest: {path, sha256}` pin — non-null is an M0 precondition.** Parent-host prerequisite: PyYAML (`pip3 install pyyaml` — the bridge fails closed without it, gate-r10 R10-H-1). Both repo files are on the control-file preflight list — no package may touch them; added `*control-manifest*` surrogates in `base..A` are preflight failures.
- **F-8 — OWNER-OPS DEPENDENCY (gate-r3 R3-C-1; trust model honest-stated + durable refs gate-r4 R4-H-1/R4-H-2): GitHub Actions repository variables + pin tags are the ratchet trust anchor.** The DPA and i18n anti-growth checkers read their protected blob hash from owner-set repository variables (`DPA_BASELINE_PROTECTED_BLOB`, `I18N_BASELINE_PROTECTED_BLOB`). **The honest trust boundary (gate-r4 R4-H-1 — "repo admin only" was overclaimed):** the anchor is sound against the EXECUTOR, which provably cannot write variables (no push, no credentials, desktop session only); it is NOT proven sound against every principal with repository collaborator/write access plus a qualifying token. Verified at r9: this is a PERSONAL private repo (`gh api → owner.type: "User"`), where the UI variable path is owner-only — the org-repo write-access rule does not directly apply — but the REST/classic-PAT surface is not proven closed. Therefore, at each package dispatch the OWNER (i) enumerates the principals with write access to the repo and (ii) asserts on the record that none of them authors these candidates; a violated assertion voids the ratchet guarantee. Residual accepted on the record; upgrade path if the collaborator set grows = a protected environment / org-level authority with repository shadowing prevented. **Durable reachability (gate-r4 R4-H-2; lifecycle gate-r5 R5-C-2):** each variable value is paired with an owner-pushed ANNOTATED pin tag whose NAME is pre-allocated in the milestone metadata commit (non-null in the accepted candidate) and whose annotation binds `{A, sha256(final register(s)), sha256(handback)}` (gate-r5 R5-C-1); CI fetches the tag explicitly and verifies the blob; **pin tags are NEVER deleted or reused**; throwaway dispatch refs may be deleted after their run. The owner performs the bootstrap set at each package's first promotion (steps 2–3 of §5 item 4, preflight-set-verify-tag order) and every subsequent re-pin (variable + freshly allocated tag together). **The trust assertion is operative (gate-r5 R5-H-1):** the parent-owned `ratchet_trust_model_ack` pin in the P1/P2 YAMLs holds the dated principal enumeration + non-authorship assertion; non-null is a P1/P2 M0 precondition, re-confirmed at promotion if access changed. Unset variable = the checker fails closed — the designed forcing function (executor green runs use the LOCAL AUTHORITY SETUP export, gate-r4 R4-H-4 — never a silent bootstrap mode). Residual risk also accepted: `ci.yml` (the checker invocation) remains candidate-editable — covered by the scope allowlist, the milestone gates reviewing every workflow diff, the pre-promotion dispatch run, the step-4a post-dispatch tag/variable re-verification, and the owner workflow-authority check (no `contents: write` grants in candidate workflow diffs — gate-r6 R6-H-5). **Recommended hardening (owner-ops option, gate-r6 R6-H-5(iii)):** protect the `ci-pin/enforcement-*` tag namespace with an owner-controlled repository ruleset so `GITHUB_TOKEN` cannot create/move/delete those refs regardless of workflow permissions.

---

## 7. Handback format

Per package, write `docs/handoff/HANDBACK-enforcement-<p1|p2|p3>-2026-08-12.md`:

1. **Header** — base SHA (verbatim `git log -1`), branch, worktree path, final SHA, commit list, the parent's phase-number answer.
2. **Per-milestone sections** — status (`DONE`/`BLOCKED`/`PARTIAL` only), changes as `file:line`, red-first evidence, each acceptance criterion restated with pasted command + output, and **every decision the brief did not specify, flagged prominently**. Operational owes are POINTERS to `docs/handoff/LEDGER.md` rows (S-14, S-15) — the handback never carries its own status for a ledger item (gate-r1 H-10).
3. **The census tables** (P1 baseline↔register cross-check + mechanism×table fixture coverage; P3 write-path classification and purpose reconciliation) — these are the review targets, not appendices.
4. **Deviations** — every one, with justification. A silent deviation found at gate time invalidates the handback.
5. **Verification** — preflight tail with PHPUnit reported as skipped; per-package acceptance blocks from §2–§4; **local** workflow-change evidence per §5 (no CI run links from the executor — remote verification is the owner's PRE-promotion `workflow_dispatch` gate on exactly the accepted SHA, receipts recorded post-promotion in `pre_promotion_ci_dispatch` + LEDGER row S-14; gate-r2 R2-C-2/R2-H-2, gate-r3 R3-C-2).
6. **Completion language:** do not declare a package "complete" until its harness gate ACCEPTs; never declare the *program* complete — Packages dispatch independently and the parent tracks the whole.

Self-certification is not accepted. The harness gates each milestone; the parent runs the final promotion gate. Expect a fix round.
