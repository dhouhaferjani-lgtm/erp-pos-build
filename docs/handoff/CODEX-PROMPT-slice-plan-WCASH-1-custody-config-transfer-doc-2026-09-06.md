# Codex prompt — slice plan W-CASH-1: per-location cash custody configuration + transfer document

Paste into a NEW Codex thread (one slice per thread), or let Fable run it via CLI (`codex exec --sandbox read-only -m gpt-5.6-sol -c model_reasoning_effort=high`).

---

NON-INTERACTIVE SLICE-PLAN TASK — do not ask questions or stop; your final message is the COMPLETE slice plan in Markdown (saved verbatim to docs/superpowers/plans/). Read-only sandbox: read any source; no file edits, no tests, no git writes. Repository: /Users/houssamr/Projects/syneriva/apps/erp (HEAD = local dev; declare the SHA; every code claim cites path:line at that SHA and must be true).

Scope of THIS slice only (≤6 tasks). No shift-event booking, no v2/v3 adapters, no W7, no variance flag — those are later slices. This slice lands the two prerequisites every later cash slice needs:
1. Per-location cash custody configuration on the existing repository surface (not a new UI concept): for each location, the default drawer→safe destination (drops) and safe→bank destination (deposits), plus an activation validator that refuses activation when a location with a drawer has no configured safe. Company-scoped; census command listing unconfigured locations; one-surface-per-concept (a setting on the repository index/detail, mode of the existing surface).
2. One transfer DOCUMENT per repository transfer: today RepositoryTransferService (apps/api/app/Modules/Treasury/Application/Services/RepositoryTransferService.php:64-115) creates a JE plus paired movement legs via TreasuryMovementService::transfer() (:355-396, interface Shared/Contracts/Treasury/TreasuryMovementServiceInterface.php:59-85) but NO justifying document. Add repository_transfer_documents: one document per action, one transfer group, exactly two cross-linked movement legs, zero or one posted JE depending on whether the two repositories share a GL account; stable source key (company_id + operation UUID) for idempotent retry; reversal policy that preserves the original (append-only, linked reversal document); frozen-repository behaviour per TreasuryMovementService.php:91-96; location scoping of source custody per W1 rules (LocationScopeResolver is HTTP-only: adapter authorizes, service takes explicit intent).
3. Wire the existing back-office transfer endpoint (Treasury/Presentation routes + RepositoryTransferController) and the web transfer flow (apps/web features/treasury: useTransferCash, RepositoryListPage, PaymentForm local types → generated DTO types) to the document; module gate on the reused routes (rule 12); JSONB DTOs for any JSON column; PHP enums for every status/type column.
4. Tests (red first): convention-09 second company via real registration, second location, re-run idempotency (same operation UUID → no second document/leg/JE, explicit outcome asserted); same-GL-account transfer posts no JE; cross-GL transfer posts one balanced JE; frozen destination records with alert; location-restricted manager cannot transfer from another branch's drawer (404 scoped-not-found) with unchanged movement/GL snapshots; PG lane for uniqueness and the tenant-only unique ratchet (company-scoped composites).

Mandatory format:
- Convention-10 matrix in the exact format (ID | Guarantee | Odoo | ERPNext | Dolibarr or NV | AutoERP today path:line | Gap | Decision MATCH/DEFER/DIVERGE/ALREADY) with rows for create, duplicate, edit, cancel/reverse, rerun, second company, second location, permission, audit. Facts you may cite: Odoo cash journal internal transfer creates two liquidity entries and books cash in/out with typed reasons; ERPNext Payment Entry "Internal Transfer" between cash/bank accounts; Dolibarr bank transfer between accounts (NV on document shape). Link sources.
- Convention-11 Vocabulary line reconciled with docs/glossary.md (Cash custody configuration, Repository transfer document).
- Owner rulings: copy Q11, Q12, Q13 VERBATIM as OPEN from docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md §Q10–Q13; nothing in this slice may encode a branch of them (no reason-code enum, no drawer-session model, no alignment).
- Per task: exact production files verified at HEAD, full signatures, COMPLETE migration schema (every column with type, nullability, default, FK + on-delete, checks, indexes, company-scoped uniques, JSONB DTO class), enums, red-first tests with exact file + class::method + first failing assertion + exact command + lane, implementation steps, reviewer gate (treasury-reviewer + tenancy-authz-reviewer), rollback.
- Deployment: reference the shared staging push manifest docs/superpowers/plans/2026-09-06-parapharmacy-staging-push-manifest.md; if absent, list the exact steps you need from it.
- End with dispatch order and a verification checklist. Title: "Slice plan W-CASH-1 — per-location cash custody configuration and repository transfer document (rev 1)".
