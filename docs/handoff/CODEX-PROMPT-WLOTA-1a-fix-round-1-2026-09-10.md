# Codex prompt — W-LOT-A-1a fix round 1 (paste into the SAME `W-LOT-A-1a permissions/roles/web gating` thread)

Fix round 1 on branch `lane/w-lot-a-1a` (worktree `.worktrees/w-lot-a-1a`, HEAD `04e60530c`). Authority is now **plan rev 10**: `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-10.md` — read its **§0 Round-9 addendum** first (it rules every item below), then the three gate registers in full: `docs/superpowers/reviews/2026-09-10-w-lot-a-1a-impl-gate-r1-tenancy.md` (B-1, B-2, M-1..M-5, N-1..N-5), `…-inventory.md` (I-1..I-9, "Ruling input for B-2"), `…-frontend.md` (B-1..B-7, minors, "Consumers of the string totals"). Close every item; one commit per blocker/major group, one for minors, one for tickets/docs; cite `path:line` at current HEAD in the handback's new "## Fix round 1" table. Owner rulings applied (not reopenable): `technician` is NOT granted `batches.view`; legacy NULL-team roles stay unchanged.

**RULED DESIGN CHANGE — B-2 / I-2 (Option B, plan §6.1 amended):** the 4-dp string `total_quantity`/`available_quantity` and the location-filtered eager load on `/batches/expiring` and `/batches/expired` SHIP at Push 3 for every tenant, flag-independent. Do NOT flag-gate them. Riders: **R1** load the scoped `batchStock` relation before building `BatchResource` on EVERY response path — `store` (`BatchController.php:211`) and the four mutation responses `update`/`recall`/`transfer`/`writeOff` (`:229,:268,:464,:503`, inventory I-1) use the same scoped closure as `BatchRepository::findVisibleByUuid` driven by `resolvedReadLocationIds()`; never recompute totals from a fresh query. **R2** an unloaded relation is loud (`whenLoaded` omission or `LogicException`), never `"0.0000"` (I-3). **R3** `apps/web/src/features/batches/types.ts:38-39,281,322-324` → `string`, rewrite the false "PHP float aggregate" comments at `:270-272,291-295`, add Vitest rendering `BatchListPage` and `ExpiryWriteOffPage` with `"3.1234"`-shaped strings (frontend B-6). Replace the vacuous `test_flag_off_preserves_pre_activation_access_and_payloads` (tenancy M-1) with a real HTTP contract test at `enforce=false` over `/batches`, `/batches/{uuid}`, `/batches/expired?location_ids[]=`, `/products/{id}/batch-stock`, `POST /batches`, asserting the committed payload shape incl. JSON string types and the two declared exceptions. Add REALIGNMENT-LOG lines for both exceptions.

**Blockers / majors, backend**
- Tenancy B-1: `LotActionPermissionDeltaTest:32-33` → `markTestSkipped` when driver ≠ pgsql; DELETE the `autoerp_test_w` name assertion (no committed test may name a session-private DB); `GeneralManagerAssignmentTest` shares the harness. Re-run both on PG (`autoerp_test_w`) AND on `phpunit.xml` (must skip, not fail).
- Tenancy M-3 / frontend B-4: `RoleData::$permissions` gets `#[LiteralTypeScriptType('Array<string>')]` (precedent `ParapharmacyProductMetadataData.php:60`); `php artisan typescript:transform`; never hand-edit `generated.d.ts`; `RolesPage.tsx:159` spread becomes typed (ESLint `no-unsafe-assignment` warning gone).
- Tenancy M-4: `BatchTraceabilityController::backwardTrace` validates `partnerId` (`Str::isUuid` → 404) and `product_id`/`date_from`/`date_to` (`sometimes|uuid`, `sometimes|date`) — no more PG 22P02/22007 500s.
- Tenancy M-5 / inventory I-5: `productId` AND `documentNumber` become `?string` on both `BackwardDocumentBatchTraceData` and `ForwardDocumentBatchTraceData`; drop the `(string)` casts; `BatchTraceReaderContractTest` gains a Draft-document / null-`product_id` line.
- Inventory I-4: `BatchReadLocationScopeTest` gains (a) restricted actor + foreign `location_id` on `/pos/products/{id}/batches` → 403 with the seven-table snapshot unchanged, (b) unrestricted actor's suggestion JSON identical with `enforce` true/false, (c) a second-company backward-trace case.
- Tenancy M-2: ruled — keep `technician` ungranted; add the seeder comment naming the ruling.

**Majors, web / e2e (frontend register)**
- B-1: re-run `pnpm exec playwright test e2e/batch-permissions.spec.ts --project=chromium --grep 'pre-activation web gating'` against HEAD; attach evidence showing `batch-permissions.spec.ts:39:3`; commit the harness (env-driven `baseURL`/`webServer` port in `apps/web/playwright.config.ts`, or a committed dedicated config) so §13's command is the one that produced the evidence — the gitignored `docs/sessions/wlota1a/playwright.config.ts` is not acceptable evidence.
- B-2: the `post-activation isolation` project must not poison `pnpm test:e2e`: `test.skip(!process.env.WLOTA1A_POST_ACTIVATION, …)` or `testIgnore` + dedicated config (repo precedent: `request-hygiene`).
- B-3: `/inventory/batches/create` is not a route — use `/inventory/batches/new` so the create-route denial is actually exercised.
- B-5: revert the Roles-page card classes to the literal-preserving form (`'rounded-lg border bg-white p-4 transition-shadow'` + background/border tokens only) — do NOT pull in `tokens.card.base` (it changes padding, radius and elevation on every legacy role card; §9.2 forbids presentation drift).
- B-7: RECORDED, not fixed here — the web gates `batches.create`/`batches.update` while the API enforces neither (plan §14 "no Task-1 middleware on create/update/transfer/write-off" is the deliberate slice boundary). Add the asymmetry to the handback's A-1b boundary note and file `docs/superpowers/tickets/2026-09-10-batch-create-update-api-enforcement.md` (A-1b: `BatchActionAccess` allow-list widened to `batches.create`/`batches.update` behind the flag).

**Minors**
- Tenancy N-1 release-note line; N-2 `LOT_ACTION_PERMISSIONS_ENFORCE=false` in `apps/api/.env.example`; N-3 emit the `WLOTA1A-RESEED` stderr marker only on marked/activated paths, not every flag-off boot; N-4 `currentTenantId()` must not throw for `DatabaseSeeder`/`ProductionSeeder` outside tenancy (legacy fallback + logged reason + test); N-5 type the `BatchPermissions.test.tsx` fixture `satisfies Batch` (fixes the `key` warning).
- Inventory I-6 comment + test pinning `getByProduct` quantity>0-only; I-7 widen `BatchTraceabilityModuleBoundaryTest` to `app/Modules/BatchExpiry/Presentation/Controllers/*.php`; I-8 ticket `2026-09-10-batch-detail-parsefloat-quantities.md` (`BatchDetailPage.tsx:273-301`, plus `BatchListPage.tsx:23,219` currency-vs-unit precision); I-9 hoist the duplicate `resolvedReadLocationIds` call.
- Frontend minors: `roles.provisionedReadOnly` key in en/fr/ar instead of "System Role" for the marked role; retitle or refetch-drive the `RolesPage.test.tsx:339` scenario; one-line comment on `handle={{ featureFingerprint }}`.

**Feature-lane manifest + CI allowlist (in THIS round, plan rev 10 §0):** `apps/api/tests/feature-lane-manifest.json` BatchExpiry 17→21, Console 16→18, Identity 32→39, Migrations 14→15, `gated_ceiling` 1250→1264 with the standard four-statement raise notes (precedent: lane T-1 commit `11a941842`); `.github/workflows/ci.yml` `backend-test-pgsql` `--filter` gains the PG-lane classes listed in the tenancy register's "Manifest / CI raise needed" (after B-1 is fixed; NOT `BatchActionPermissionsTest`, NOT `RoleProvisioningSourceSchemaTest`); `php tools/feature-lane-manifest-check.php` exit 0 captured in the handback. Both files join the Push-3 ledger row.

Verification (by file only, never the full suite; one PG file at a time on `autoerp_test_w`): every touched PHP test class on PG; the SQLite legs named above; `./vendor/bin/pint --test` + `./vendor/bin/phpstan analyse` on touched PHP; the six §13 Vitest files one at a time; `pnpm typecheck`; `pnpm typecheck:e2e`; `pnpm audit:keys`; `pnpm audit:design-system`; the pre-activation Playwright project. Update the handback ("## Fix round 1": finding → resolution → `path:line`; manifest numbers; the Playwright evidence line; the consumer list from the frontend register with what changed). Stop with `status: review`. Do not merge, do not push.
