# Codex plan gate r4 prompt — RBAC wave 0a plan rev 4 (2026-09-10)

You are the adversarial PLAN gate (round 4) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox). Declare `git rev-parse --short HEAD`. Review ONLY: docs/superpowers/plans/2026-09-10-rbac-wave-0a.md (rev 4) and, for programme consistency, docs/superpowers/plans/2026-09-10-rbac-programme-execution-plan.md. The plan implements the ACCEPTED spec docs/superpowers/specs/2026-09-10-roles-permissions-catalogue-design.md (rev 9.1 — rev 9 gate r9 ACCEPT plus editorial amendment A-1 recorded in its Amendments section at docs/superpowers/reviews/2026-09-10-rbac-spec-codex-gate-r9.md — its "Preserve" list is binding). Wave item 0a-1 (S-1) is ALREADY MERGED into local dev at 6415062b9 (see docs/superpowers/reviews/2026-09-10-rbac-w0a-s1-gate-r{1,2}.md).

Verify against the CODE at HEAD (and against `dev`, `lane/w-lot-a-1a`, `lane/t2-receipt-spine` via `git show <ref>:<path>` / `git diff --stat dev...<ref>`), never against the plan's own prose:
1. Every path:line in both plans — open and confirm; list wrong/stale citations with the real line.
2. Task by task: is each red test compilable against HEAD as written (namespaces, base classes, traits, helper names that exist), does the stated first failing assertion actually fail for the stated reason, is the minimal implementation complete (no placeholders), do the exact commands run under the repo's harness (phpunit config names, PHPStan paths, Pint paths), and are commit messages/paths exact and path-scoped?
3. The route-coverage ratchet (T2): re-derive the classification from `php artisan route:list --json` if artisan boots (else from docs/superpowers/audits/2026-09-09-roles-permissions/02-route-enforcement-sweep.csv) under the spec §4.4.3 rule; confirm baseline size 294 and post-0a ceilings writes 152 / reads 142; confirm the liveness fixture and shrink-only logic; confirm the self-service allow-list is the exact set and the structural check cannot be satisfied by an unrelated route (spec B-7 lineage).
4. T1 `AllowSelfService` + `authz.self` alias in bootstrap/app.php — alias registration shape vs the existing aliases; applied routes' lines at HEAD in Identity/Notification/SupportAccess route files.
5. T3/T3b: the four Identity read routes gated on `roles.view` (names-only shaping deferred to wave 2 or not — check the plan's claim against spec §4.6.4 and D4); the UsersPage `enabled` guard — verify `UsersPage.tsx:130-137`, the imports at `:38,102`, `permissionsMap.generated.ts:272`, the affected existing test; is adding apps/web to 0a safe vs both lanes' apps/web diffs (list overlaps)? Is there any OTHER frontend caller of GET /roles, /roles/{id}, /permissions, /users/{id}/roles that would break (grep apps/web/src and apps/pos/src)?
6. T4: every gated Promotion/Uom/Menu/Coupon route line and key exists in the seeder; no new key introduced in 0a.
7. T5 docs rewrite: does the new convention text match the spec's enforcement standard exactly (can: only, self-service marker, generated map, permissions:scaffold future)?
8. Disjointness of the 0a file list (incl. apps/web files) from `lane/w-lot-a-1a` and `lane/t2-receipt-spine` at CURRENT tips (re-measure); confirm the plan's claim that the manifest overlap is gone.
9. CI: are the three new Architecture tests really not run on any CI event today (ci.yml:202,215,228) and is deferring their registration to 0b-11 acceptable, or must 0a register them (would that collide)? Recommend.
10. Programme plan: entry conditions, "when can it run" table and owner-owed list consistent with the spec §8 and with reality (lane tips, lane cap 3, S-1 merged).

Output a Markdown register: "# Codex plan gate r1 — RBAC wave 0a plan rev 1 (gpt-5.6-sol, high, read-only, 2026-09-10)", "## BLOCKER", "## MAJOR", "## MINOR", "## Citation audit", "## Rejected false positives", "## Preserve", "## Owner decisions required", "## Dispatch assessment" (which tasks are dispatchable as-is), and the LAST LINE exactly `VERDICT: DISPATCH-READY` or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Print in full.

Round-4 additions: FIRST read your round-3 register docs/superpowers/reviews/2026-09-10-rbac-plan-0a-codex-gate-r3.md and produce a "Rev-3 closure table" (finding → CLOSED at rev-4 anchor / NOT CLOSED / REJECTED-correctly) before new findings; verify the "Change log rev 3 → rev 4" against code. SETTLED by orchestrator amendment A-1 (spec rev 9.1, Amendments section): 0a-4 → 0b-15 with its frontend guards; post-0a 152/146/298; post-0b-15 152/142/294; the r9 Preserve bullet's "all six start-now 0a tasks" half is superseded — do NOT re-raise. Scrutinise: (a) the four tombstone pins — re-derive at least two with the plan's own normalisation and compare byte-for-byte; (b) T5's Promotion example vs `git show dev:apps/api/app/Modules/Promotion/Presentation/routes.php` + Task 4's edits (diff should be exactly the comment block + four `can:promotions.manage`); (c) O-2 runner-flip procedure vs feature-lane-manifest-check.php:774-819 and ci.yml variable usage; (d) every task's red test compiles at dev tip and fails for the stated reason; (e) 0a disjointness at CURRENT tips and Phase 0 check B arithmetic; (f) spec rev 9.1 Amendments section internal consistency with §8 and §4.4.3. This plan has been through three rounds; if only MINOR findings remain, the verdict MUST be DISPATCH-READY — then list the minors as an editorial list for the dispatch brief and give the exact per-task file list. Output heading: "# Codex plan gate r4 — RBAC wave 0a plan rev 4 (gpt-5.6-sol, high, read-only, 2026-09-10)".
