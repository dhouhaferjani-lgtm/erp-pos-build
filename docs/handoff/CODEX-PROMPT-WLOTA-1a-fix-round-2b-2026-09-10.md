# Codex prompt — W-LOT-A-1a fix round 2b: scope ruling on the pre-existing per-lot transfer failure (paste into the SAME thread)

Round 2b on `lane/w-lot-a-1a` (HEAD `5d847b2ba`). This answers the handback's `blocking_decision: scope ruling for pre-existing transfer writer failure` (ticket `docs/superpowers/tickets/2026-09-10-batch-transfer-missing-movement-reference.md`).

**Ruling (orchestrator, 2026-09-10; owner may overturn):** the `BatchStockService::transferBatchStock` defect (`:523`, `:531` → `inventory_batch_movements.movement_id` NOT NULL violated, SQLSTATE 23502, 500) is **pre-existing and OUT OF SCOPE for this lane** — rule 4, and the repair belongs to the inventory movement seam that lane T-2 S1 is changing right now (`StockAdjustmentService::receive()/issue()` transfer-linkage parameters); fixing it here would collide at merge. Do NOT touch `BatchStockService`, the aggregate writer, or the migration.

Apply the repo's known-red precedent (`apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php`, `T1_RUN_KNOWN_REDS`): 
1. `BatchReadLocationScopeTest::test_transfer_controller_response_uses_all_membership_locations` stays exactly as written (real HTTP, no mock, no schema relaxation) but is **skipped unless `WLOTA1A_RUN_KNOWN_REDS=1`**, with `markTestSkipped('Known red — pre-existing per-lot transfer writer omits movement_id; ticket docs/superpowers/tickets/2026-09-10-batch-transfer-missing-movement-reference.md')`. It must never run red in CI (`BatchReadLocationScopeTest` is in the PG allowlist).
2. Add a sibling test that **pins the current behaviour to the ticket**: same fixture, `POST /batches/{uuid}/transfer` → assert the 500 envelope (or the `QueryException` when `withoutExceptionHandling`), assert the seven-table snapshot is unchanged (the service transaction rolled back — prove it: no `inventory_batch_stock`, `stock_levels`, `stock_movements`, `inventory_batch_movements` row changed), and name the ticket in the test name/comment. This is the regression lock that will flip red the day the writer is repaired, so the ticket owner deletes both the pin and the skip together.
3. Ticket: add the ruling ("out of scope for A-1a; owned by the inventory movement seam; schedule after T-2 S1 merges; the two tests above are the acceptance harness"), the note that no first-party client calls the per-lot transfer route today (grep of `apps/web/src` and `apps/pos/src` finds no caller — verify and record the grep), and the priority consequence (API/mobile exposure only).
4. Handback: `blocking_decision: none`, `promotion_blocker` updated (the transfer defect is a disclosed pre-existing residual with a ticket, not a lane blocker); release note gets one line ("per-lot `POST /batches/{uuid}/transfer` is broken pre-lane on PostgreSQL — ticket …"); the Push-3 ledger row is unchanged.

Verification: `BatchReadLocationScopeTest` on PG (`autoerp_test_w`, both DB env vars) → the known-red case reports **skipped**, the pin case green; the same file with `WLOTA1A_RUN_KNOWN_REDS=1` → exactly one failure (the known red) and record its output as evidence; `pint --test` + scoped PHPStan; manifest checker exit 0. One commit for the tests, one for docs. Append a "## Fix round 2b" row block to the handback. Stop with `status: review`. Do not merge, do not push.
