# Codex spec gate r2 prompt — roles & permissions catalogue design rev 2 (2026-09-10)

You are the adversarial spec gate (round 2) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox). Review ONLY this document: docs/superpowers/specs/2026-09-10-roles-permissions-catalogue-design.md (REV 2). Declare the SHA you read (`git rev-parse --short HEAD`). Supporting evidence you may consult: docs/superpowers/audits/2026-09-09-roles-permissions/*.md and the .csv, docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-9.md (or highest rev), docs/handoff/CODEX-PROMPT-WLOTA-1a-ruling-legacy-null-team-roles-2026-09-09.md, docs/superpowers/plans/2026-09-03-request-hygiene-phase-a.md Task 1, docs/conventions/09..11, docs/glossary.md, the branch lane/w-lot-a-1a (git diff dev...lane/w-lot-a-1a).

Owner rulings 2026-09-10 that are SETTLED (do not re-open): Direction B (catalogue-as-code with deploy-time sync) accepted; D1 roles stay tenant-scoped; D2 template-delta policy for seeded roles; D3 ratchet with ceilinged shrink-only baseline, writes first; D4 roles.view on reads; D5 kebab-resource.verb + closed verb enum + rename map; D6 no general per-user overrides; D7 wave 0 after lane/w-lot-a-1a merges except the no-overlap 0a subset; D8 audit events for role mutations and 403 denials; NEW: service accounts + MCP/API tokens are first-class principals whose effective permissions = owner grants ∩ token scope, never wider. Only OQ-1..OQ-4 may appear under Owner decisions.

Verify against the CODE, never against the spec's own prose:
1. Every path:line claim — open the file at HEAD and confirm; list every wrong or stale citation with the real line.
2. Token narrowing claim: the spec says wiring scope into User::hasPermissionTo() (where the impersonation path already narrows) makes every existing call site inherit it. Prove or refute for each idiom: `can:` route middleware (Illuminate Authorize → Gate → Spatie Gate::before in PermissionRegistrar → which User method?), `$user->can()`, `Gate::authorize()`, `->hasPermissionTo()`, `->hasAnyPermission()`, `->getAllPermissions()` (used by AuthUserData for /auth/me and by PosAuthController for the PIN payload), `->hasRole()`, Spatie `role:` middleware if any. Identify any path that bypasses the narrowing.
3. Sync vs W-LOT-A-1a: read that lane's actual diff (roles.provisioning_source column + CHECK, the delta service, RoleController/UserController changes, config/lot_action_permissions.php, seeder contract in plan §8.4/§8.6) and check every sentence of spec §4.3 against it: can `permissions:sync` coexist with the A-1a delta migration and its "non-destructive for marker-carrying tenants" seeder; do template_key/template_version/is_system/customised_at collide with provisioning_source semantics; does the NULL-team ruling (legacy roles tenant_id NULL, no re-homing) break the registry's role lookup/creation; does admin = registry-list (instead of Permission::all()) change what A-1a expects.
4. Ratchet: recompute the baseline numbers (spec claims 326 uncovered / 177 writes / 149 reads, ceilings 167/148) from `php artisan route:list --json` if artisan boots (otherwise from docs/superpowers/audits/2026-09-09-roles-permissions/02-route-enforcement-sweep.csv) under the spec's exact classification rule; state whether the rule is mechanically checkable in a PHPUnit test and whether the `authz.self` alias design lets a developer whitelist a route by mistake.
5. Principals: users.principal_kind on the existing users table — check every constraint the reuse implies (email unique per tenant? password nullable? UserStatus enum, Sanctum guard, login/PIN/`pinHolders()` exclusion, notifications, user counting/seat logic, `TenantInitializationService`, FormRequests that create users, the last-admin floor definition with Human-only principals, `users.manage_location_access`, memberships); list every writer of `users` the spec did not mention.
6. Sanctum: token abilities storage and TTL (`expires_at`, `sanctum.expiration`), `EnforceTokenTenantClaim` ordering, revocation on membership removal — feasible as described? Any place `personal_access_tokens` is central (synerivia_central) vs tenant that the spec gets wrong?
7. Data model §5: every new column has type/null/default/index/FK/enum; migrations tenant-scoped and additive; CHECK constraints valid PG; no ALTER on an enum-typed column; no backfill beyond sync.
8. Static guards: PHPStan custom rule precedent exists in the repo (find the rules dir, e.g. ForbidFloatCastOnDecimalProperty) — is the proposed "no permission string literals outside manifests" rule implementable at level 8 without a mass baseline; enum ⊆ manifest test; label-coverage test against apps/web/src/locales/{en,fr,ar}/common.json; generated TS union vs rule 7.
9. Role-management hardening: last-admin floor — enumerate every write path that could remove the last admin (RoleController::update/destroy/removeRole, UserController update/destroy/deactivate, membership removal, sync itself, impersonation) and check the spec covers each; effective-permissions endpoint authorization (who may read whose); audit events on the audit chain per rule 8 (immutable event classes, versioned names).
10. Conventions: 09 obligations per wave complete (second company, second location, re-run idempotency, tenant-only unique keys on roles/permissions tables); 10 matrix rows carry real path:line and MATCH/DEFER/DIVERGE/ALREADY; 11 glossary rows: one table, one write path, one operator surface each, no synonym drift versus the A-1a "General manager" row.
11. Edge-case register §7: for each row, is the expected behaviour actually decided by the design (not deferred), and is the test lane realistic (PG-only features on sqlite lane etc.)? Add any missing scenario.
12. Waves §8: verify the no-overlap claim for 0a by `git diff --stat dev...lane/w-lot-a-1a`, `...lane/t1-transfers-edge`, `...lane/t2-receipt-spine`; check 0a-6 (delete PermissionSeeder) ordering against ProductionSeeder; check the entrypoint replacement of SYNC_PERMISSIONS_ON_BOOT is safe for a staging auto-deploy on push.
13. Open questions OQ-1..4: genuinely open, or answerable from code/rulings? Add any question the spec should have asked (max 3).

Output a Markdown register with sections: "# Codex spec gate r1 — roles & permissions catalogue design rev 1 (gpt-5.6-sol, high, read-only, 2026-09-10)", "## Rev-1 base SHA", "## BLOCKER", "## MAJOR", "## MINOR", "## Citation audit" (table: claim → verified/wrong with the real line), "## Rejected false positives", "## Preserve" (what must not change in the fix round), "## Owner decisions required", and the LAST LINE exactly `VERDICT: ACCEPT` or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Do not propose scope beyond the settled rulings. Print the register to stdout in full.

Round-2 additions: FIRST read your own round-1 register docs/superpowers/reviews/2026-09-10-rbac-spec-codex-gate-r1.md and produce a "Rev-1 closure table" (finding → CLOSED at rev-2 anchor / NOT CLOSED / REJECTED-correctly) before any new finding; the spec's "Change log rev 1 → rev 2" section is its claim, verify each against code, not against the change log. Orchestrator rulings applied in rev 2 and now SETTLED (audit their exact application, do not re-open): role-name authorization banned by PHPStan rule + shrink-only baseline and converted before tokens are issuable; two resolvers forSubject/forTarget with explicit token selector; registry adopts W-LOT-A-1a's LotActionPermissionDelta as template migration #1 and invokes it; legacy adoption rule = grant-set equality against the embedded template_version 0 snapshot else customised_at=now(); customised_at writer moves into wave 1; single LastAdminFloor service with deterministic locking; permissions:sync-fleet runner with exact per-tenant verdicts, boot continues with a structured marker + /health permissions_sync; permissions:scaffold generator; self-service exact allow-list + structural check; denial events on audit_events (no security_events table); service accounts excluded from human seats with a max_service_accounts plan limit (default 5); SoD hard for new resources + shrink-only ratchet on existing templates with the five baselined manager combinations; confirm is financially decisive. Scrutinise especially: (a) the legacy adoption rule — can the embedded template_version-0 snapshot be wrong for tenants provisioned at different seeder revisions (git log the seeder) and what happens then; (b) sync-fleet + entrypoint ordering relative to `tenants:migrate` and the S-1 cache listeners; (c) whether forSubject/forTarget leave any path where a narrowed token's /auth/me payload disagrees with what `can:` enforces; (d) the adoption of LotActionPermissionDelta — does calling it from sync respect its idempotency and marker CHECK for tenants created after wave 1; (e) the exact 0a list against `git diff --stat dev...lane/w-lot-a-1a`; (f) the scaffold generator's write targets (enum file, three locale JSONs) — file paths real, JSON insertion deterministic; (g) the SoD baseline list matches today's manager grants exactly. Output heading: "# Codex spec gate r2 — roles & permissions catalogue design rev 2 (gpt-5.6-sol, high, read-only, 2026-09-10)".
