# Codex plan gate r1 prompt — RBAC wave 0b + wave 1 plans rev 1 (2026-09-10)

You are the adversarial PLAN gate (round 1) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox; artisan cannot boot here — for route/classification arithmetic use the shared checkout at /Users/houssamr/Projects/syneriva/apps/erp (read-only) or docs/superpowers/audits/2026-09-09-roles-permissions/02-route-enforcement-sweep.csv). Declare `git rev-parse --short HEAD`. Review: docs/superpowers/plans/2026-09-10-rbac-wave-0b.md (rev 1 + the 0b-15 task added by the 0a fix round) and docs/superpowers/plans/2026-09-10-rbac-wave-1.md (rev 1). They implement the ACCEPTED spec docs/superpowers/specs/2026-09-10-roles-permissions-catalogue-design.md (rev 9; gate r9 register docs/superpowers/reviews/2026-09-10-rbac-spec-codex-gate-r9.md — its "Preserve" list is binding). Also consult docs/superpowers/plans/2026-09-10-rbac-programme-execution-plan.md and the wave-0a plan rev 2 (whose T2 ratchet is being fixed separately: known defects = scanner must be restricted to routes whose uri starts with api/, support classes must be PSR-4 named and fully supplied, tombstone proof must pin the closure source; do NOT re-report those against 0b/1, but DO check that 0b/1 consume the corrected names). Known 0b-15 wording defect already ruled: UsersPage /roles query guard = users.assign-roles; RolesPage queries, /settings/roles route and Settings card = roles.view (D4) — verify the plan text and report if not yet applied.

Verify against CODE (dev tip via `git show dev:<path>`, lane/w-lot-a-1a and lane/t2-receipt-spine via `git show <ref>:<path>`, `git diff --stat dev...<ref>`), never against the plans' prose:
1. Every path:line in both plans — confirm; list wrong/stale with the real line. Lane citations must carry the `lane/…:` prefix and match CURRENT tips (re-measure).
2. Task by task (both plans): red test compilable at dev tip (namespaces, PSR-4 paths, base classes, traits, helpers exist), first failing assertion fails for the stated reason, minimal implementation complete (no placeholders/prose stubs), commands run under the repo harness (phpunit configs, PG env, PHPStan/Pint explicit paths), commit subjects `Phase 0.2.<task>:` / `Phase 1.<task>:` per AGENTS.md:15-16, `git add` lists exact and path-scoped.
3. 0b integration with W-LOT-A-1a's ACTUAL code: PermissionWriteLock adopts the delta's key (`hashtextextended('wlota1a:'||tenant)`) and row order (`roles.name, roles.id`) — verify against lane LotActionPermissionDelta.php; wiring of all runtime writers incl. ResetTenantCommand; PermissionWriteLockCoverageTest partition (LOCKED ∪ INITIALIZATION-ONLY = enumerated callers) — is the caller enumeration mechanical and complete against a fresh grep (`syncPermissions|givePermissionTo|revokePermissionTo|assignRole|syncRoles|removeRole|Role::create|Permission::create|firstOrCreate`)?; BatchExpiry route gates vs the lane's BatchActionAccess middleware; feature-lane-manifest ceilings vs whatever the lane raises (recompute rule stated?).
4. 0b TenantFleetRunner + permissions:ensure/ensure-fleet: selector contract (id OR slug, `get(['id','slug'])`, exactly one, duplicates collapsed, unmatched/ambiguous FAILED non-zero), readiness predicate (database exists AND migrations current → else FAILED reason tenant_not_ready), team boundary set/restore, NULL-or-tenant role predicate, `matchesPreWave0b` snapshot-then-batch, two-invocation deploy (Q-0b-4) — check the admin-only vs template-role key split against the seeder so no manager receives an admin-only key; status marker name; that no `tenants:run` form is used.
5. 0b new keys: services/service-categories/channels/categories/progression/purchase-hub/credit-notes.cancel — every gated route line at dev tip, verb choice consistent with D5's closed verb list in spec §4.2 (are `operate`, `manage` allowed?), no FE caller left 403 (grep apps/web/src, apps/pos/src for each affected endpoint), Playwright e2e listed where UI behaviour changes.
6. Wave 1: PermissionVerb closed list (incl. Pay, Close), PermissionDefinition nullable verb + behavesAs stored, manifests per module with EVERY one of the 304 keys assigned (count them), rename map (11) with rename_targets_are_not_sources, deprecation list (22 — check against the spec's authoritative list), per-module enums + enum⊆manifest tests, label coverage test vs apps/web/src/locales/{en,fr,ar}/common.json, SoD ratchet with the 11-row baseline (blocking O-5/OQ-3 stated), permissions:scaffold deterministic writes (anchor, sort, JSON flags), PermissionSyncService steps 1–9 in the settled order with adopt-before-write, marker write only on general_manager creation, exact template diff for uncustomised roles, deprecation-with-replacement as the single custom-role write, admin = activeKeys, after-commit cache reset, mutations vs gauges, dry-run keeps failure exit; sync-fleet via TenantFleetRunner; entrypoint replacement of the SYNC_PERMISSIONS_ON_BOOT seeder block + status file + admin health reader; seeder shim keeping the lane's marked-tenant branch (Q-w1-3); roles schema migration per spec §5 (columns, CHECK, unique(tenant_id, template_key) + partial unique for NULL team, down migration); RoleSyncedV1 + subscriber; generated FE map + label skeleton; PHPStan literal rule + AST route test with constant-expression evaluation + shrink-only baseline; PermissionDeploySequenceTest consuming the versioned YAML runbook; permissions:report-dead FE/POS-aware; deletion of ensure/ensure-fleet with PermissionsSyncSupersedesEnsureTest (first sync adopts → non-zero adoption counters, second run zero mutations).
7. Cross-plan signature consistency: names/signatures 0b PRODUCES vs wave 1 CONSUMES (PermissionWriteLock::acquire/key, LegacyRoleBaseline::matchesVersion0/postA1aGrants/version0/WAVE_0B_ADDITIONS, PermissionRenameMap::canonicalise/to/sources/targets, TenantFleetRunner::run → FleetRunResult/FleetTenantOutcome, DateFactory substitution for Clock).
8. Convention-09 rows per plan (second company via real registration, second location on POST /stock-adjustments, re-run idempotency); the three tenant types in wave 1 (legacy unmarked, marked by lane, provisioned after wave 1).
9. The 7 recorded design points Q-0b-1..4, Q-w1-1..3: are the defaults consistent with the spec and code, or should any be escalated to the owner?

Output a Markdown register: "# Codex plan gate r1 — RBAC wave 0b plan rev 1 (+0b-15) and wave 1 plan rev 1 (gpt-5.6-sol, high, read-only, 2026-09-10)", with per-plan sections "## Wave 0b" and "## Wave 1" each containing "### BLOCKER / ### MAJOR / ### MINOR", then "## Cross-plan consistency", "## Citation audit", "## Rejected false positives", "## Preserve", "## Owner decisions required", "## Dispatch assessment", and the LAST LINE exactly `VERDICT: DISPATCH-READY` (both plans) or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Print in full.
