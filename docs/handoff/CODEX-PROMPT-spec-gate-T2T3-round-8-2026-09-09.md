You are the adversarial spec gate (round 8) for AutoERP. Repository root is the current directory (read-only). Review ONLY this document: docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-8.md (REV 8). Also read your own round-7 register docs/superpowers/reviews/2026-09-09-t2-t3-spec-codex-gate-r7.md and produce a "Rev-7 closure table" (finding → CLOSED at rev-8 anchor / NOT CLOSED / REJECTED-correctly) before new findings. Owner rulings 2026-09-09 that OVERRIDE the brief and your r1 tail: OQ-1 return_to_source IS in scope on close with no GL (benchmark docs/superpowers/reviews/2026-09-09-benchmark-transfer-discrepancy-gl.md), D1 reconcile permission seeded to manager+admin, OQ-3 coarse status accepted, NEW requirement: stored per-line receipt events for pattern detection (see docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md last section) (T-2 destination receipt for stock transfers with partial receipt + discrepancy, event-sourced; T-3 blind receiving). Context you must read: docs/handoff/BRIEF-parallel-transfers-blind-receiving-2026-09-08.md (benchmark + survey; owner CONFIRMED defaults Q1–Q5 on 2026-09-08: Q1 Odoo backorder semantics, Q2 over-receipt refused / short+damaged allowed with reason + alert, Q3 company-level setting, transfers first then PO receipts, Q4 DB notifications + web bell first, Q5 location membership, no receiver assignment), docs/conventions/09-SECOND-OF-EVERYTHING.md, 10-BENCHMARK-FIRST-SPECS.md, 11-ONE-SURFACE-PER-CONCEPT.md, docs/glossary.md, docs/architecture/precision-contract.md, CLAUDE.md rules 8, 9, 12, 13, 19, 20.

Verify against the CODE, never against the spec's own prose:
1. Every `path:line` claim in the spec — open the file and confirm the line says what is claimed (re-derive; never trust copied numbers). List every wrong or stale citation.
2. Blind-mode leakage: enumerate every response shape a receiver can reach (receiver-view, list, show, 422 envelopes, notifications, generated TS types, mobile types) and prove whether sent/expected quantities can leak, including via batch allocations, `partially_received` status, and error messages. Compare with the counting pattern (InventoryCountingController counterView, CountingItemController, countingApi.ts NEVER-INCLUDE contract).
3. In-transit remainder: the spec moves three readers (LocationStockQueryService, StockMatrixQueryService, WeightedAverageCostService) from status-based to quantity-based; check for OTHER readers of `status = in_transit` or of stock_transfers the spec missed (grep), and whether the backfill (received = sent on completed) keeps every reader's historical answer unchanged.
4. Movements + GL: damaged/written-off via TransferIn then issue() with MovementReason::Damage / WriteOff at destination — verify those reasons exist, their `requiresGLEntry()`/GL counter family, that the stock↔GL bridge (InventoryGlPostingBuffer, ReturnScrapWriteOffService precedent) supports it, WAC untouched, precision contract (decimal strings, QuantityScale, no floats, scale-4 storage).
5. Event sourcing: new stored `StockTransferReceivedV1` / `StockTransferClosedWithWriteoffV1` as DomainEvent (Spatie ShouldBeStored) — fields sufficient for replay? idempotency anchors? rule 8 respected (existing events untouched)? afterCommit dispatch? queued listeners without CompanyContext (rule 20)?
6. State machine: `TransferStatus` gains `partially_received`, `closed_with_writeoff` — check every switch/match on TransferStatus in app/ and apps/web for exhaustiveness breaks, `canBeCompleted/Cancelled` semantics, and the claim that the values fit `string(20)`.
7. Idempotency + concurrency: `stock_transfer_receipts.idempotency_key` + payload_hash; two receivers concurrently; replay with different payload → typed 422; PG partial uniques; lock ordering versus existing transfer/batch locks (cite `StockTransferService::completeLocked`).
8. API/authz: routes under the rule-12 middleware set; new permissions (`inventory.transfers.close`, `inventory.transfers.reconcile`) seeded and grantable; deviation D1 (reconcile not `inventory.view`) — judge whether it is necessary given `RolesAndPermissionsSeeder` seeding; module gating (`inventory`), location membership via `canSeeTransfer()`.
9. Conventions: 09 test matrix complete (second company, second location, re-run for EVERY writer), 10 benchmark table present with ≥5 rows and decisions, 11 glossary rows + one primary write path (does keeping `complete` alongside `receive` violate one-surface? the spec says complete delegates — verify the claim is implementable), no hand-rolled FE type beside a generated DTO.
10. Migrations: additive and self-guarding (push = staging deploy), backfill idempotent, PG CHECK constraints valid, no ALTER on enum columns needed.
11. Open questions OQ-1..3 and deviations D1/D2: are they genuinely open, or answerable from code/rulings? Add any question the spec should have asked.

Output a Markdown register with sections: "# Codex spec gate r8 — T-2/T-3 (gpt-5.6-sol, high, read-only, 2026-09-09)", "## BLOCKER", "## MAJOR", "## MINOR", "## Citation audit" (table: claim → verified/wrong with the real line), "## Rejected false positives" (things you checked that are fine), "## Preserve" (what must not change in the fix round), "## Owner decisions required", and the LAST LINE exactly `VERDICT: ACCEPT` or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Do not propose scope beyond the confirmed defaults. Print the register to stdout in full.


Round-5 note: the owner register (docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md, sections dated 2026-09-09) now CONFIRMS freight OQ-4 default, multi-receiver attribution default, accountant out of the PO-revert seed; D1/D2/OQ-1/OQ-2/OQ-3 remain ruled. Do not list any of those as open. Only OD-1, close authority and OD-4 may appear under Owner decisions.

Round-8 note: OD-1, close authority and OD-4 are intentionally owner-open with complete defaults; they do NOT block ACCEPT. If no BLOCKER and no MAJOR remains, the verdict must be `VERDICT: ACCEPT` (minors may be listed for the execution plan).