# T-1 handback — 2026-09-09

status: review

Worktree: `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t1-transfers`  
Branch: `lane/t1-transfers-edge`  
Base: local dev `63e0e5e16`  
No merge or push performed. Whole-lane production delta is **+26/−2** in one FormRequest, explicitly ratified by the orchestrator in fix round 2; round 2 changes zero production lines.

## Delivered

1. `b3c957903` — StockTransferEdgeCasesTest and real two-process StockTransferCompleteConcurrencyPostgresTest.
2. `7d7f02a13` — ReplenishmentEdgeCasesTest plus the source-location access guard: **12 added / 2 removed production lines** in CreateTransferFromRequestsRequest. A restricted processor previously dispatched from a hidden source; initially got 422 before any transfer/request mutation; fix round 1 aligns this to 403 LOCATION_ACCESS_DENIED.
3. `11a941842` — manifest ceiling Inventory 127→129, Replenishment 7→8, aggregate 1250→1253; all three new classes selected by backend-test-pgsql; truthful parked-lane notes. Manifest checker passes.
4. `90d2b4ec9` — four unresolved behavior tickets, linked from skipped tests.
5. [Evidence, all 15 cases and sanitized local curl transcripts](../superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md).
6. This handback.

Case 8 uses the existing StockTransferLocationScopeTest (all 12 tests passed); no gap was found needing changes. No StockTransferService lifecycle change, counting file, frontend source change, DTO change, or migration.

## Owed

Four unresolved behaviors remain **skipped, not green**:

- [Recall during transit](../superpowers/tickets/2026-09-09-t1-recalled-in-transit.md): silently completes with recalled lot.
- [Changed payload under same idempotency key](../superpowers/tickets/2026-09-09-t1-idempotency-payload.md): silently replays original transfer.
- [Settlement event replay](../superpowers/tickets/2026-09-09-t1-settlement-replay.md): settles newly captured demand against an already-used transfer.
- [Cancel in progress](../superpowers/tickets/2026-09-09-t1-cancel-in-progress.md): requester gets 422 although enum says open; needs sourcing lifecycle/product ruling.

The orchestrator must run **inventory-costing-reviewer** and **stock-gl-interaction-reviewer** (this lane adds freight/WAC assertions), resolve any findings, then decide promotion. No CI run has been observed. Do not interpret the parked lane's ceiling raise or allowlist entry as a CI execution result.

## Resume / reproduce

From the worktree, use its installed vendor and node_modules. Local diagnostic logs and helper scripts are gitignored in `docs/sessions/t1/`.

```sh
cd /Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t1-transfers
# Each call selects exactly one file. Helper pins BOTH database families to autoerp_test_t.
sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferEdgeCasesTest.php
sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php
sh docs/sessions/t1/pg-test.sh tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php
# Reproduce known reds rather than skipping them:
T1_RUN_KNOWN_REDS=1 sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferEdgeCasesTest.php
T1_RUN_KNOWN_REDS=1 sh docs/sessions/t1/pg-test.sh tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php
cd apps/api
php tools/feature-lane-manifest-check.php
```

For a fresh checkout without the ignored helper: export `DB_CONNECTION=pgsql`, `DB_HOST=127.0.0.1`, `DB_PORT=5433`, `DB_DATABASE=autoerp_test_t`, and local test DB credentials; set **DB_CENTRAL_HOST/PORT/DATABASE/USERNAME/PASSWORD to the same values**. Export `APP_ENV=testing`, `TENANCY_DB_PER_TENANT=false`, `CACHE_STORE=array`, `QUEUE_CONNECTION=sync`, then run `php vendor/bin/phpunit <one-file>` from apps/api. Create the reserved test DB if absent. The concurrency class requires an **exclusive per-session database**: never use a shared database or parallel database leg. Before each PG invocation, check `pg_stat_activity` for `autoerp_test_t` from a connection to `postgres` and proceed only with zero other clients; both DB and DB_CENTRAL must point to that same reserved database. Never aim RefreshDatabase tests at `iziposcentral` or a demo tenant DB.

SQLite: run each new file by path with no DB overrides. The PG-only completion class and case 12 skip by construction. Known defects additionally skip unless `T1_RUN_KNOWN_REDS=1`.

API probe setup: ignored `apps/api/.env.t1` contains the db-per-tenant local configuration. Export that file's values before starting `php artisan serve --host=127.0.0.1 --port=8011 --no-reload` (the environment must reach the child server). `apps/api/.env` is deliberately empty, so PHPUnit never inherits demo connection settings and artisan's dotenv warning is avoided. Authenticate normally with the demo owner; no bearer token or tenant password is committed.

The original probe fixture **T1-EDGE-092254** / **01a0857a-3761-715c-92c0-de235331737d** has been removed in fix round 1. Identity and expected row counts were checked before deletion; afterwards product=0, stock movements=0, stock levels=0, transfer lines=0, replenishment requests=0, and all three fixture transfers=0. Its earlier evidence is a historical snapshot, not live demo state. A fixture-only backup and cleanup transcript are ignored local diagnostics under `docs/sessions/t1-r1/`. The empty `.env` remains explicitly documented above and is gitignored, so it does not propagate on merge.

## Initial-round verification

Scoped preflight **PASS** after restoring dependencies from the local pnpm cache. Scoped Pint and PHPStan, selected PHPUnit files, TypeScript, ESLint, repository audits, CreateTransferDialog Vitest file, fiscal parity and §14.3 gate all completed. The missing-.env artisan warning was diagnosed and removed with an empty ignored `.env`; a focused artisan rerun passed without warning. No full PHPUnit/Vitest suite was run.

PostgreSQL: transfer edges 16 tests/74 assertions/2 ticket skips; completion concurrency 2/13/no skips; replenishment edges 6/41/2 ticket skips. SQLite: transfer edges 16/74/2 skips; replenishment edges 6/23/3 skips (including PG-only case 12). Existing location scope 12/24 and replenishment actions 16/67 pass. Manifest checker passes. See the evidence for first-run colours and exact case mapping.

Probe server stopped and its exact newly issued token revoked; local bearer files removed. Shared PostgreSQL/Redis remain running for the orchestrator. Fix round 1 removed the labelled synthetic fixture; no T-1 probe stock or demand remains in the shared demo.

## Fix round 1 (historical)

The table and run counts below describe `a2fd11174`. Gate r2 rejected the schema-wipe harness; the Fix round 2 section supersedes its cleanup claims and line references.

status: review

Four major commits, followed by one minors/handback commit. Gate findings are closed in this lane; the four desired-behaviour tickets remain outstanding product work, not falsely green tests. A fifth ticket documents the product-detail incoming visibility/formatting gap.

| Finding | Resolution and current-HEAD `path:line` |
|---|---|
| M1 | Live current recall/expiry pins: `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:114`, `:127`. Live requester-cancel refusal: `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:194`. Desired tests retain their ticket-linked skips. Commit `b506c8dc4`. |
| M2 | Exact three-reader identities and actual side-by-side service transcripts: `docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md:54`. Product-detail PO incoming explicitly distinguished; visibility/scale ticket: `docs/superpowers/tickets/2026-09-09-t1-product-detail-transit.md:1`. Commit `e4a906f0c`. |
| M3 | Same lone source-denial envelope as StoreStockTransferRequest: `apps/api/app/Modules/Replenishment/Presentation/Requests/CreateTransferFromRequestsRequest.php:28` returns 403 LOCATION_ACCESS_DENIED with location/user details. Status, code and details pinned at `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:207`. **14 production lines added in this fix round**, inside the FormRequest; no shared rule/controller changed. Commit `0a6ceea73`. |
| M4 | Company-B source refusal, no writes, unchanged request and stock; positive control on same request and actor using company-A source: `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:220`. Commit `30e2d9ac4`. |
| m1 | Severity/reachability corrected: `docs/superpowers/tickets/2026-09-09-t1-settlement-replay.md:3`. Synchronous listener and reachable quantity-blind settlement stated at `:5`, `:7`; ordinary endpoint current-behaviour pin at `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:91`. T1-10 also explicitly states no second stock decrement. |
| m2 | Case 6 renamed to the owned-quantity denominator it proves: `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:143`. |
| m3 | Four exact desired-method names and required skip/current-pin retirement in T-2 acceptance: `docs/superpowers/specs/2026-09-08-transfer-destination-receipt-and-blind-receiving-design.md:285`. All skip messages keep full ticket paths; method references checked against current source. |
| m4 | Exact synthetic demo fixture deleted and verified absent; cleanup and ignored `.env` status documented at `docs/handoff/HANDBACK-T1-2026-09-09.md:55` and the evidence’s final verification section. No shared actor/company/location deleted. |
| m5 | Framework DatabaseMigrations with full-schema cleanup equivalent: `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:34`, `:63`. Complete schema wiped after each test and RefreshDatabaseState reset; no hand-maintained fixture-table list. Final external check: 0 public base tables. |

Verification: SQLite transfer 17/79/2 skips; SQLite replenishment 9/57/3 skips; existing replenishment actions 16/67/no skips. PostgreSQL transfer 17/79/2 skips; replenishment 9/77/2 skips; real completion concurrency/WAC class **2/15/no skips**. Pint, **PHPStan on all four touched PHP files**, manifest checker, and diff check pass. Initial default migration rollback failed on an existing counting down() migration; the final PG cleanup uses db:wipe instead, and no counting file was touched. Test logs and cleanup transcript: ignored `docs/sessions/t1-r1/`.

The same resume commands above apply. That historical harness left the schema empty; fix round 2 restores tenant-scoped cleanup and preserves the schema. The original demo probe fixture no longer exists. No full test suite, push, or merge was performed. Return to the orchestrator for re-review.

## Fix round 2

status: review

Scope: tests and docs only. The orchestrator explicitly ratifies the **whole-lane +26/−2** production delta in `CreateTransferFromRequestsRequest.php`; this is not a per-round ≤20-line allowance. `git diff a2fd11174 -- apps/api/app` is empty. Three major commits, one minors commit and one tickets/docs commit comprise this round.

| Finding | Resolution and current source reference |
|---|---|
| Inventory MAJOR-1; stock-GL m7 | Restored the `691eacba2` tenant-scoped cleanup; every delete is bounded by the fixture tenant/company/id and each test asserts deletion of exactly its own tenant. No DatabaseMigrations, db:wipe or schema reset. `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:71`; exclusive database contract `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:30`. Commit `58fe90cde`. |
| Stock-GL MAJOR-1 | Zero journals before, during and after freight capitalization, and before/after both WAC adjustments: `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:135`; `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:198`. Missing justifying document and freight GL counterpart ticket: `docs/superpowers/tickets/2026-09-09-t1-freight-capitalization-no-gl.md:5`. Commit `568f3d38f`. |
| Stock-GL MAJOR-2 | Recall and expiry pins assert source batch quantity 6.0000 and equality with source stock_levels.quantity: `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:115`; `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:131`. Commit `e769e768d`. |
| Stock-GL m1 | The raced transfer carries freight 10.000; after the loser refuses, cost is 6.000000 and exactly one Adjustment exists: `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:89`. |
| Inventory MINOR-1; stock-GL m2 | Whole-lane +26/−2 budget explicitly ratified; no production change this round. `docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md:5`. |
| Stock-GL m3 | 403 equivalence limited to hidden-location refusal; unrestricted cross-company source still reaches the controller’s 422. No ScopedExists added: `docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md:288`. |
| Stock-GL m4 | Hidden-location refusal snapshots and compares stock-movement count: `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:207`. |
| Stock-GL m5 | Demand-only replay, no stock/GL/cash compensation, and swallowed per-line failures recorded: `docs/superpowers/tickets/2026-09-09-t1-settlement-replay.md:23`. |
| Stock-GL m6 | Accepted then scrapped recall requires movement plus buffered GL write-off; today transfers book no GL: `docs/superpowers/tickets/2026-09-09-t1-recalled-in-transit.md:19`. |
| Stock-GL m8 | Reserve one source unit; available total is 5.0000 in transit and 9.0000 after either terminal action while physical balances remain pinned: `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:168`. |
| Inventory MINOR-2 | Follow-up for a shared ValidLocationAccess message constant used by both requests: `docs/superpowers/tickets/2026-09-09-t1-location-denial-message-constant.md:7`. |
| Inventory MINOR-3 | Superseded rev-1 spec exactly restored to lane base (no whole-lane diff). Retirement acceptance lives in all four tickets: `docs/superpowers/tickets/2026-09-09-t1-recalled-in-transit.md:17`; `docs/superpowers/tickets/2026-09-09-t1-idempotency-payload.md:17`; `docs/superpowers/tickets/2026-09-09-t1-settlement-replay.md:21`; `docs/superpowers/tickets/2026-09-09-t1-cancel-in-progress.md:17`. |
| Inventory MINOR-4 | Follow-up for replenishment refusal i18n using stock-adjustments mapping: `docs/superpowers/tickets/2026-09-09-t1-replenishment-dialog-refusal-i18n.md:7`. |

PostgreSQL exclusivity check, run from `postgres` before every invocation:

```sql
SELECT count(*) FROM pg_stat_activity
WHERE datname = 'autoerp_test_t' AND pid <> pg_backend_pid();
```

Observed other-client counts: baseline run 1 **0**, run 2 **0**, run 3 **0**; strengthened class **0**; multi-class **0**. Both database connection families use `autoerp_test_t`. Never attach another test session during the run; never schedule this class into a shared/parallel database leg.

Baseline harness evidence at `58fe90cde`, before adding the GL and freight-race assertions:

```sh
sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php
# Three consecutive results: OK (2 tests, 15 assertions)
# Elapsed: 06:54.433 (initial migration rebuild), 00:04.436, 00:03.221.
```

The final strengthened class passes **2 tests / 20 assertions / no skips** (00:53.005): three added journal assertions plus two added race assertions explain the increase from the requested baseline 15. The required multi-class command is:

```sh
sh docs/sessions/t1/pg-test.sh --filter '/(ReplenishmentEdgeCasesTest|StockTransferCompleteConcurrencyPostgresTest|StockTransferEdgeCasesTest)::/' tests/Feature
```

Multi-class result: **exit 0; 28 tests, 187 assertions, 4 ticket skips, 339 PHPUnit deprecations**, 08:12.227. The deprecations are reported, not suppressed or fixed in this scoped round. No errors or failures. Post-run check: **0 clients** and **277 public base tables**; the schema remains available. The multi-class run includes all three changed classes on PostgreSQL; it is the sole explicitly authorized multi-file selection.

| Final file-level verification | Result |
|---|---|
| StockTransferEdgeCasesTest, SQLite | 17 tests / 89 assertions / 2 ticket skips |
| ReplenishmentEdgeCasesTest, SQLite | 9 tests / 58 assertions / 2 ticket skips + 1 PG-only skip |
| StockTransferCompleteConcurrencyPostgresTest, SQLite | 2 PG-only skips / 0 assertions |
| StockTransferCompleteConcurrencyPostgresTest, PostgreSQL | 2 tests / 20 assertions / no skips |
| Pint, three touched PHP test files | PASS |
| PHPStan level 8, three touched PHP test files | PASS, no errors or suppressions |
| feature-lane-manifest-check.php | PASS; 1517 Feature classes, filters uniquely matched across 1928 test classes |
| git diff --check | PASS |
| Production delta since a2fd11174 | Empty |
| Superseded rev-1 spec delta since lane base | Empty |

Minors test commit: `758fabeb1`. All logs and the local repeat-run wrappers are ignored under `docs/sessions/t1-r2/`; the existing helper remains under `docs/sessions/t1/`. Four desired-behaviour defects remain ticket-linked skips. Three new round-2 tickets document freight accounting, the shared denial-message constant and refusal i18n. No full suite, production modification, merge or push. Return to the orchestrator with **status: review**.
