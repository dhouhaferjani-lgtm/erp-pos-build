# Codex prompt — W-LOT-A-1a fix round 2 (paste into the SAME `W-LOT-A-1a permissions/roles/web gating` thread)

Fix round 2 on branch `lane/w-lot-a-1a` (worktree `.worktrees/w-lot-a-1a`, HEAD `2fa724c1d`). Authority is now **plan rev 11**: `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-11.md` — read its **§00 Round-10 addendum** first (it rules every item below; rev 10 §0 still rules round 1), then the three gate-r2 registers in full: `docs/superpowers/reviews/2026-09-10-w-lot-a-1a-impl-gate-r2-tenancy.md` (B-1(r2), B-2(r2), M-1..M-3(r2), N-1..N-5(r2), "Orchestrator rulings"), `…-r2-inventory.md` (B-1(r2) executed, M-1(r2), I-1..I-4(r2), "Consequence assessment"), `…-r2-frontend.md` (B-1(r2), M-1(r2), F-1..F-6(r2), "Assessment of …"). Every gate-r1 item is verified closed — do not touch them again except where a r2 item names the same line. Close every r2 item; one commit per blocker, one per major group, one for minors, one for docs/tickets/ledger; cite `path:line` at current HEAD in a new "## Fix round 2" handback table. Owner rulings applied (not reopenable): `technician` NOT granted `batches.view`; legacy NULL-team roles unchanged; Option B stands.

**BLOCKER 1 — post-write authorization (tenancy B-1(r2) = inventory B-1(r2), closes tenancy M-2(r2)).** `BatchController::loadScopedResourceRelations()` (`:105-110`) → `resolvedReadLocationIds($request)` (`:56-73`) re-reads request input and `LocationScopeResolver::resolve()` THROWS after the mutation has committed; on write-off this commits the stock decrement + GL and then 403s (executed: two calls → lot 10→6→2, two `stock_movements`). Fix: the response scope of every mutation response (`store :223`, `update :241`, `recall :280`, `transfer :477`, `writeOff :516`) derives from membership only — `LocationContext::getAllowedLocationIds()` (`null` = unrestricted) — resolved ONCE before the write and passed into `loadScopedResourceRelations(Batch $batch, ?array $locations)`; the response step must be incapable of throwing; request-input `location_id` is a write target, never a read scope. Delete or narrow `resolvedReadLocationIds()`'s input read to the pure-read endpoints that legitimately accept a query `location_id` (index/show/stock/expiring). Tests in `BatchReadLocationScopeTest` (PG allowlist): (a) restricted actor, `PATCH /batches/{uuid}` and `POST /batches/{uuid}/write-off` with an out-of-scope body `location_id` → 403 BEFORE the write, seven-table snapshot byte-identical (helper at `:163`); (b) unrestricted actor's write-off/PATCH → 200 with COMPANY-WIDE `total_quantity`/`available_quantity`/`batch_stock` (the inventory probe fixture: 10 at `$other` + 7 at `$this->location`, expect `"16.0000"` before / `"15.0000"` after a 1-unit write-off); (c) one successful write-off = exactly one `stock_movements` row (retry semantics recorded). Fixture needs a `stock_levels` row beside `inventory_batch_stock` (see the inventory register's probe).

**BLOCKER 2 — undeclared flag-off change (tenancy B-2(r2)).** `RoleController.php:374-376` (`abort_if($membership === null, 422, …)`) runs for every tenant with the flag false. Ruled: gate the membership requirement behind `$this->activation->enforced()` (constructor-injected, as elsewhere in the controller). Tests: flag-off HTTP case — assigning a role to a tenant user without an active membership in the current company succeeds exactly as at `4373ba2f6`; flag-on case — 422 `Active company membership required.`. If the owner overturns this (OWNER-QUESTIONS 2026-09-10 entry), the orchestrator will relay before you finish; otherwise implement the gate.

**BLOCKER 3 — push-ordering crash (frontend B-1(r2)).** Pre-lane `BatchListPage.tsx:212` calls `.toFixed()` on `available_quantity`; Push 3 turns it into a string → the Batches list crashes for every BatchExpiry tenant until Push 5, flag-independent. Ruled (plan rev 11 §10): `apps/web/src/features/batches/types.ts`, `apps/web/src/features/batches/pages/BatchListPage.tsx` and its Vitest move to the **Push 3** ledger row as the "web string-tolerance slice"; permission-gating web files stay in Push 5. Update the handback's "Commit groups and source-push ledger" (every file exactly once), correct `HANDBACK…:116`, and add the window to `RELEASE-NOTES-WLOTA-1a-2026-09-10.md`. No code change is required for this item beyond the ledger, but see MAJOR 2 which edits the same file.

**MAJOR 1 — unpinned contracts (tenancy M-1(r2), inventory M-1(r2)).** In `BatchReadLocationScopeTest`: one HTTP case per remaining path (`recall`, `transfer`, `writeOff`) asserting the restricted actor's membership-scoped totals (as `:136-148` does for update); plus the flag-off `/batches/expired?location_ids[]=`, `/batches/expiring?location_id=` and create-response `batch_stock: []` assertions duplicated from `BatchActionPermissionsTest` (which stays OUTSIDE the CI allowlist per the owner).

**MAJOR 2 — list display precision (frontend M-1(r2) + inventory I-1(r2) + frontend F-2(r2)), fix now.** `BatchListPage.tsx:23,219` renders a lot quantity at `useCurrency().decimals`. Fix: (i) `loadScopedResourceRelations()` and `BatchRepository::getByCompany()` eager-load `product.unitOfMeasure` so `BatchResource::quantity_decimals` (`:55-57`) stops hard-returning 4 on `/batches` and the mutation responses; (ii) `Batch.product` in `types.ts:62-66` gains `quantity_decimals: number` (mirror `ExpiredBatch.product :332-333`); (iii) `BatchListPage` uses `getQuantityDecimals(batch.product)` from `@/lib/quantityScale`; (iv) the Vitest case at `BatchPermissions.test.tsx:35-38` asserts `'3.1234'` and is renamed to what it asserts; (v) the PHP fixture gets a unit with `decimal_places != 4` so `BatchActionPermissionsTest`'s exact-JSON `quantity_decimals` can distinguish loaded from unloaded. Update ticket `2026-09-10-batch-detail-parsefloat-quantities.md`: remove `BatchListPage`, keep `BatchDetailPage.tsx:273-301`, ADD the three `ExpiryWriteOffPage.tsx:215,222,229` deprecated-`formatQuantity` sites (F-3(r2)) and the `getQuantityDecimals`-silent-default trap.

**MAJOR 3 — declare (tenancy M-3(r2)).** `POST /batches` now emits `batch_stock: []`: add it to the release note and to the parent-repo `docs/03-ERP-INTEGRATION/REALIGNMENT-LOG.md` entry (the parent edit is uncommitted in `/Users/houssamr/Projects/syneriva`; extend the existing 2026-09-10 "batch totals" entry — the orchestrator commits it path-scoped at merge).

**Minors** — tenancy N-1(r2) cite `RolesAndPermissionsSeeder.php:47,52` for N-3; N-2(r2) re-record the PG assertion counts from a fresh run (126 / 108 or whatever HEAD yields); N-3(r2) drop the dead `whenLoaded('batchStock')` guard at `BatchResource.php:60`; N-4(r2) `types.ts:49-54` `batch_stock[]` members → `string`, `types.ts:18` `product_variant_id` → `variant_id: string | null` (API emits `variant_id`, `BatchResource.php:31`; `ExpiredBatch :302` already correct), fix the `satisfies Batch` fixture; N-5(r2) `$this->command?->info(...)` at `RolesAndPermissionsSeeder.php:82`. Inventory I-2(r2) (subsumed by MAJOR 2 iv); I-3(r2) cites (`BatchController.php:344-345,350`; `BatchReadLocationScopeTest.php:216`); I-4(r2) → NEW ticket `docs/superpowers/tickets/2026-09-10-batch-stock-dead-float-accessors.md` (`BatchStock.php:44-71`, file untouched by this lane — do NOT edit it). Frontend F-1(r2) `ExpiredBatchStock.location_id: string` with a truthful comment (`types.ts:274-275`), UUID-shaped `location_id` in the three fixtures (`ExpiryWriteOffPage.test.tsx:104,119,163`); F-4(r2) the Push-5 evidence log's first three lines = `git rev-parse HEAD`, the exact command, `node -v` (regenerate `docs/sessions/wlota1a/r1-browser-pre-node20.txt` → `r2-browser-pre-node20.txt`); F-5(r2) drop `?? 0` at `BatchListPage.tsx:178`; F-6(r2) `test.skip(process.env.WLOTA1A_POST_ACTIVATION !== '1', …)` at `e2e/batch-permissions.spec.ts:82`.

**Manifest:** if any NEW test class is added, raise `apps/api/tests/feature-lane-manifest.json` accordingly with the four-statement raise note; adding cases to existing classes needs no raise. `php tools/feature-lane-manifest-check.php` exit 0 in the handback.

Verification (by file only, never the full suite; one PG file at a time on `autoerp_test_w`, BOTH `DB_DATABASE` and `DB_CENTRAL_DATABASE`): every touched PHP test class on PG; `LotActionPermissionDeltaTest` + `GeneralManagerAssignmentTest` on `phpunit.xml` (skips, not failures); `./vendor/bin/pint --test` + `./vendor/bin/phpstan analyse` on touched PHP; the six §13 Vitest files one at a time + `ExpiryWriteOffPage.test.tsx` + `tenantScope.test.tsx`; `pnpm typecheck`; `pnpm typecheck:e2e`; `pnpm audit:keys`; `pnpm audit:design-system`; `pnpm audit:quantity`; `node scripts/factory/gen-route-manifest.mjs --check`; the pre-activation Playwright project under Node 20.19.4 with the F-4 header. Update the handback ("## Fix round 2": finding → resolution → `path:line`; refreshed verification table; corrected ledger; the "reviewer verdicts" belong to the orchestrator's registers — never assert acceptance inside the handback). Stop with `status: review`. Do not merge, do not push.
