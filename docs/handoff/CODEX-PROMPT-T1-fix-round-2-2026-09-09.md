# Codex prompt — lane T-1 fix round 2 (paste into the SAME `T-1 transfers/requests edge campaign` thread)

Fix round 2 for lane T-1 on branch `lane/t1-transfers-edge` (worktree `.worktrees/t1-transfers`, HEAD `a2fd11174`). Two gate registers this round — read both in full: `docs/superpowers/reviews/2026-09-09-t1-transfers-gate-r2-inventory.md` (MERGE-WITH-FIXES: MAJOR-1, MINOR-1..4) and `docs/superpowers/reviews/2026-09-09-t1-transfers-gate-r2-stock-gl.md` (MERGE-WITH-FIXES: MAJOR-1, MAJOR-2, m1..m8). Close every item below; one commit per major, one for the minors, one for tickets/docs; cite `path:line` at current HEAD in the handback. Tests and docs only — NO production line may change this round (the FormRequest guard stays exactly as it is; its +26/−2 total is ratified by the orchestrator, inventory MINOR-1).

**Majors**
- **Inventory MAJOR-1 — make `StockTransferCompleteConcurrencyPostgresTest` deterministic.** Revert the `DatabaseMigrations` + per-test `db:wipe` harness (`:34,63-97`) to the bounded, tenant-scoped hand-clean of `691eacba2` (which was verified green 2/13 in gate r1) and answer the original m5 by SCOPING the clean-up, not wiping the schema. If you keep any migration-based approach, it must (i) never call `db:wipe` and (ii) set a short `lock_timeout`/`statement_timeout` so a blocked drop fails fast. Evidence required in the handback: **three consecutive green runs** of the class via `sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php` (2 tests / 15 assertions each) **plus one green multi-class run** `--filter '/(ReplenishmentEdgeCasesTest|StockTransferCompleteConcurrencyPostgresTest|StockTransferEdgeCasesTest)::/' tests/Feature`, all on `autoerp_test_t` with no other client attached (check `pg_stat_activity` first and paste the count). Add to the class docblock and to the handback resume recipe that the class needs an exclusive per-session database (stock-gl m7).
- **Stock-GL MAJOR-1 — pin the absent GL side of freight capitalisation.** In `test_freight_capitalization_uses_ten_owned_units_not_fourteen_inside_transaction` and in `StockTransferEdgeCasesTest::test_wac_counts_transit_exactly_once_before_and_after_terminal_action`, assert `journal_entries` count is unchanged (0) across the capitalisation. New ticket `docs/superpowers/tickets/2026-09-09-t1-freight-capitalization-no-gl.md`: `transfer_cost` has no justifying document (document-per-action) and no freight-clearing/expense counterpart, references `StockTransferService.php:631-709`, and states the double-count risk when the carrier invoice is also expensed. Do NOT add any GL posting.
- **Stock-GL MAJOR-2 — source-side lot pins.** In `test_recalled_lot_receipt_pins_current_behaviour_until_ticket_t1_4` and its expiry twin, add the source-side `BatchStock` assertion (`'6.0000'` at the source for the allocated batch) and, for the batch case, that it equals the source `stock_levels.quantity`.

**Minors**
- Stock-GL m1: give the raced transfer in the concurrency class a non-zero freight cost (`$this->transfer('10.000')`) and assert after the loser is refused: `cost_price === '6.000000'` and exactly one `Adjustment` movement for the transfer (a 7.000000 / 2-row outcome is the double-capitalisation failure).
- Stock-GL m3: soften the evidence sentence at `…-t1-transfers-requests-edge-evidence.md:286` — same 403 envelope for the hidden-location case; the cross-company case is the controller's 422 (no `ScopedExists` in the new request; `ValidLocationAccess` checks neither existence nor company for an unrestricted membership). No production change.
- Stock-GL m4: case-15 denial (`ReplenishmentEdgeCasesTest.php:207-218`) snapshots `StockMovement::count()` before the POST and re-asserts it after, like its second-company sibling.
- Stock-GL m5: `2026-09-09-t1-settlement-replay.md` — add that a replay double-settles DEMAND ONLY (`SettleRequestsOnTransferInitiated::settleLine` writes only `replenishment_requests`; `ReplenishmentFulfilled` has no registered listener), no stock decrement, no journal, no cash; and that `handle()` swallows per-line failures (`:33-40`) so a partial settlement failure leaves stock moved and requests open silently.
- Stock-GL m6: `2026-09-09-t1-recalled-in-transit.md` — add the valuation arm: a recalled lot accepted then scrapped is a write-off (movement + GL posting through `InventoryGlPostingBuffer`), and today the transfer path books no GL at all.
- Stock-GL m8: `StockTransferEdgeCasesTest.php:166-170` — `totalOnHand` is `quantity − reserved`; either reserve one unit at the source and assert the distinction, or rename the expectation/comment so the pin says "available".
- Inventory MINOR-3: move the "T-1 regression retirement acceptance" table out of `docs/superpowers/specs/2026-09-08-transfer-destination-receipt-and-blind-receiving-design.md:285-294` (that file is the SUPERSEDED rev 1; rev 9 lives on dev and is not in your branch) into the four tickets (one acceptance line each) and delete it from the rev-1 file so the lane leaves that file untouched.
- Inventory MINOR-2 and MINOR-4: file two follow-up tickets — `2026-09-09-t1-location-denial-message-constant.md` (hoist the `ValidLocationAccess` message literal to a constant used by both FormRequests) and `2026-09-09-t1-replenishment-dialog-refusal-i18n.md` (`CreateTransferDialog.tsx:99-100` surfaces the raw server string; reuse the stock-adjustments `refusals.ts` mapping pattern). No code.

Verification (by file only, never the full suite): every touched test class on SQLite; the PG classes on `autoerp_test_t` with the run counts above; `php tools/feature-lane-manifest-check.php`; Pint + PHPStan on touched PHP. Update the handback with a "Fix round 2" section (finding → resolution → `path:line`). Stop with `status: review`. Do not merge, do not push.
