# Ruling + plan amendment — T-2/T-3 slice S1, movement creation vs the append-only ratchet (2026-09-10; paste into the SAME `T-2 receipt spine` thread)

**Decision: APPROVED, bounded.** Your finding is correct and the direction you proposed is the ruling: **eliminate the post-creation rewrite; do not relocate it, do not weaken the scanner, do not re-pin the protected blob.** The plan is now rev 8 — `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-8.md` — whose §6.7b amendment and §7.2 amendment are the binding text (revs 1–7 must not be read). Summary of the bounds:

1. `markMovementAsTransfer()` is DELETED, not moved. Its entry `StockTransferService::markMovementAsTransfer::stock_movements::update#1` is REMOVED from the working DPA baseline (a shrink; the ratchet permits it). `StockTransferMovementSupport` carries `lockTransfer()`, `capitalizeTransferCost()`, `computeAllocationWeights()`, `restockAtSource()` only.
2. `StockAdjustmentService::receive()` / `::issue()` gain a backward-compatible way to create the row with its FINAL type and transfer linkage at creation (e.g. `?MovementType $movementType = null`, accepted only as `TransferIn` on receive / `TransferOut` on issue, anything else → `InvalidArgumentException`; linkage through the existing `referenceType`/`referenceId` pair or an explicit transfer-linkage parameter if the enum cannot express it). The stored row must be byte-identical to what the rewrite produced: `movement_type` `transfer_in`/`transfer_out`, `reference_type` exactly the value `markMovementAsTransfer` wrote (`StockTransfer::class`), `reference_id` the transfer id. Every existing caller keeps its defaults; `StockAdjustmentService::transfer()` is untouched.
3. `StockTransferService` initiate/cancel paths and the new `StockTransferReceiptService` call the writers with the final type; the six former call sites disappear.
4. Announced events carry the REAL label for transfer rows (`transfer_in`/`transfer_out`, as the `transfer()` path already does) — closes T-1 seam fact 5. FIRST run a consumer census of every listener/subscriber of `StockMovementRecorded` and `StockMovementRecordedV2` (re-grep at `DISPATCH_SHA`; the known set is `ChannelServiceProvider`, `WeightedAverageCostService`, `OpeningBalancePostingService`, `ResetOpeningBalanceService`, `ReturnScrapWriteOffService`, `StockAdjustmentService`) and record per consumer whether it branches on the label; any that does gets a named regression test pinning its output before/after. No event class changes shape (rule 8).
5. Scope + ledger: `StockAdjustmentService.php` and the working DPA baseline file join S1's modified files (Push 3). Red-first: a new test asserting a transfer-initiation movement is created with `movement_type = transfer_out` + linkage and is never updated afterwards (red before the change). Regression gate before any new behaviour: `StockTransferEdgeCasesTest`, `StockTransferCompleteConcurrencyPostgresTest` (exclusive `autoerp_test_u`), `StockTransferIdempotencyCollisionPostgresTest`, `DocumentPerActionBaselineRatchetTest` (both directions, with `DPA_BASELINE_PROTECTED_BLOB` exported from the reviewed seed as the test's message says), `DocumentPerActionWriteGuardTest` (SQLite) — all green, counts in the handback.
6. Then continue the S1 commit order from step (0b) onward exactly as the packet states; the reader baseline you captured on the untouched tree (step 0c) stands. Stop at `status: review`. No merge, no push.
