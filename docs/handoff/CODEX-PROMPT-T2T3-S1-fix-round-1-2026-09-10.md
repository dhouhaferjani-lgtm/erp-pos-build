# Codex prompt — T-2 S1 receipt spine, fix round 1 (paste into the SAME `T-2 receipt spine` thread)

Fix round 1 on branch `lane/t2-receipt-spine` (worktree `.worktrees/t2-receipt-spine`, HEAD `93b106461`; reviewed source `c30dcdf9c`). Authority is now **plan rev 9**: `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-9.md` — read its **§0AA Round-8 addendum** first (it rules every item below), then the three gate registers in full: `docs/superpowers/reviews/2026-09-10-t2-s1-impl-gate-r1-tenancy.md` (T-1..T-8, "Input to the M-2 owner ruling"), `…-r1-inventory.md` (M-1, M-2, I-1..I-10), `…-r1-stock-gl.md` (G-1..G-8, the stock↔GL consequence census). Close every item; one commit per blocker, one per major group, one for minors, one for docs/tickets/ledger; cite `path:line` at current HEAD in a new "## Fix round 1" handback table. Hard scope §2.3 still holds except where §0AA explicitly widens it (M-2 option a). PG port: the lane DB `autoerp_test_u` is on the NATIVE PostgreSQL 16 at `127.0.0.1:5432` (T-8) — pin `DB_PORT=5432` explicitly on every PG command and in the handback.

**BLOCKER T-1 — CI filter dropped.** `.github/workflows/ci.yml:1142-1144`: your three comment lines sit BETWEEN `php artisan test -c phpunit-pgsql.xml \` (`:1141`) and its `--filter='…'` continuation (`:1145`); bash strips `\`+newline before tokenizing, so the `#` starts a comment, the filter is gone, and `:1145` runs as a separate command (`--filter…: command not found`). Move the comment block ABOVE `:1141` (plan §6.7.5). Verify with `bash -n` AND a stubbed `php` on PATH that echoes its argv, run against the extracted step; paste the argv line (must contain `--filter=`) in the handback.

**BLOCKER T-2 — convention-09 ratchet red on PG.** `apps/api/tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php` `EXCLUDED_TABLES` (`:56-127`) gains three parent-scoped entries in the sibling style (`:111-112`): `'stock_transfer_receipts' => 'Sequence numbers are unique within one company-owned stock transfer; the number and idempotency keys already carry company_id.'`, `'stock_transfer_receipt_lines' => 'Transfer-line and movement links are unique within one receipt.'`, `'stock_transfer_receipt_line_lots' => 'Batch-allocation and movement links are unique within one receipt line.'`. No waiver, no ceiling raise. Re-run the class on PG (was: 3 tests / 134 assertions / 1 failure).

**MAJOR group A — authz pins and scope (tenancy T-3, T-5).** T-3: in `TransferReceiptAuthorityGateTest` add (a) an `inventory.view`-only actor → 403 on `GET /stock-transfers` and `GET /stock-transfers/{id}`; (b) a `complete`-only actor → 403 on `GET /stock-transfers/{id}/reconciliation` (reaches the list, not the variance surface). T-5: `StockTransferReceiptService::transact()` (`:98`) and `StockTransferMovementSupport::lockTransfer()` (`:39-42`) predicate `tenant_id` AND `company_id` (or accept a scoped `StockTransfer`); move the `Str::isUuid` guard beside them; one test proving a scoped service call with another company's transfer id is a `ModelNotFoundException`, zero writes.

**MAJOR group B — costing (inventory M-1).** `StockTransferService::cancel()` (`:394-420`) sets `freight_uncapitalized = transfer_cost` after the terminal status write (nothing was capitalised; exact at 4 dp). Test in `StockTransferCloseTest`: initiate with `transfer_cost 140.0000`, cancel, assert `Σ allocated_transfer_cost + freight_uncapitalized === '140.0000'`.

**MAJOR group C — GL seam pins and guards (stock-gl G-1, G-2, G-3).** G-1: in `StockTransferReceiveDamageTest::test_damaged_units_land_then_scrap_with_one_shrinkage_journal` and `StockTransferReceiveLotsTest::test_lot_damage_nets_to_zero_and_requires_perpetual_valuation`, load the entry's lines, map `account_id → system_purpose`, and `assertSame`: `inventory_shrinkage_expense` debit = `qty × cost_price` at the company scale / credit `0`; `inventory` debit `0` / credit the same amount; `JournalEntryStatus::Posted`; `isChained() === true`; `Event::assertDispatched(JournalEntryPosted::class)`. G-2: extend the `:124-133` preflight with the account-mappability check (`GeneralLedgerService::hasInventoryWriteOffAccounts()` / movement accounts for the company) → new `TransferReceiptFailureReason::GlAccountsUnmapped` (or the plan's naming), typed 422 BEFORE any stock or GL write; test: company whose chart lacks the shrinkage purpose posts a damaged receipt → 422, seven-table snapshot unchanged. G-3: at the top of the receipt transaction, `if (DB::transactionLevel() !== 0) throw new \LogicException('StockTransferReceiptService must be the outermost transaction')` (precedent `GeneralLedgerService.php:115,131`); test: wrapping `receive()` in `DB::transaction` throws and writes nothing.

**MAJOR group D — web status widening (inventory M-2, orchestrator default option (a) — the owner may overturn before you reach it; if the orchestrator relays option (d) instead, implement the validation guard described in §0AA and skip this group).** `apps/web/src/features/stock-transfers/types/index.ts:12`: delete the hand-rolled four-member union and re-export the generated `StockTransferStatus` from `packages/shared/types/generated.d.ts` (convention 11; no hand-rolled FE type beside a generated DTO). `StockTransferStatusBadge.tsx:5-10`: add `partially_received`, `closed_with_writeoff`, `closed_returned` variants using `tokens.badge.*` only (rule 18). `StockTransferListPage.tsx:15-21`: filter accepts the three. `StockTransferDetailPage.tsx:38-39`: `canComplete = status === 'in_transit' || status === 'partially_received'`. i18n: `status.partially_received`, `status.closed_with_writeoff`, `status.closed_returned` in `stock-transfers` namespace, en/fr/ar (rule 11). Vitest: badge renders all seven, detail shows Complete on `partially_received`. No other web change (no receive/close UI — that is S4). Add the T-4 line to `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` (backend read gate widened to any-of; FE route guard still `inventory.transfers.view` and not module-gated until S4).

**Minors.** Tenancy T-6 RECORD as intentional in the handback; T-7 delete the stray `apps/api/autoerp_test_u` SQLite file (gitignored) and record the trap; T-8 handback command with `DB_PORT=5432`. Inventory I-1 named 4-dp money constant (or `CurrencyScale::bcformatStrict` at persist) for `freight_uncapitalized` / `allocated_transfer_cost` with the truncation direction stated in a comment; I-2 RECORD the S4 decision (join on `transfer_line_id` for `quantity_decimals`) in the handback and §7.12 note; I-3 per-site ratchet (`LocationStockQueryService` 2 sites, others 1); I-4 resolve the `decimal_places` fallback through the shared helper, not `QuantityScale::SCALE`; I-5 RECORD (SQLite legs are not reader proof); I-6 add `InventoryTransferServiceTest` to the ledger; I-7 explicit null check at `StockTransferReceiptService.php:431`; I-8 mark the Blind receiving / Receiver view / Visibility version glossary rows "planned (S2/S3)"; I-9 two assertions (`products.cost_price` unchanged across the damage path; `BatchStock` nets to the good quantity) in `StockTransferReceiveDamageTest`. Stock-gl G-4 + G-7 one disclosure line each in the promotion checklist; G-5 delete the orphan docblock `StockTransferService.php:799-805`; G-6 replace handback `:209-213` with "reviewer gates: see the orchestrator's registers" — never assert reviewer acceptance inside the handback; G-8 RECORD in the verification section.

**Manifest:** new test CLASSES (if any) raise `apps/api/tests/feature-lane-manifest.json` with the four-statement note and join the `ci.yml` alternation; new cases in existing classes need nothing. `php tools/feature-lane-manifest-check.php` exit 0.

Verification (by file only, never the full suite; one PG file at a time on `autoerp_test_u` @ 5432 with BOTH `DB_DATABASE` and `DB_CENTRAL_DATABASE`): every touched PHP test class on PG incl. `TenantOnlyUniqueOnCatalogueTablesRatchetTest`; SQLite legs `DocumentPerActionBaselineRatchetTest` (protected blob `1381983d…`), `TransferInTransitReadersUseRemainderTest`, the receive/damage/lots/close/edge classes; `./vendor/bin/pint --test` + `./vendor/bin/phpstan analyse` on touched PHP; the `ci.yml` argv proof; for group D: `pnpm typecheck`, `pnpm audit:keys`, `pnpm audit:design-system`, the touched Vitest files one at a time. Update the handback ("## Fix round 1" table; refreshed PG table with port; ledger). Stop with `status: review`. Do not merge, do not push.
