DESIGN PRE-APPROVED — non-interactive run: do NOT stop to ask for approval; produce the full review now.

Adversarial slice-plan gate r8 for docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-8.md (W-LOT-A-1a, rev 8). Read-only audit at current HEAD: no edits, no tests, no git writes; do not ask questions. Your FINAL MESSAGE is the review itself and nothing else (the orchestrator saves it verbatim).

Context to read first, in this order: the rev-7 gate docs/superpowers/reviews/2026-09-06-w-lot-a-1a-slice-codex-gate-r7.md (the findings rev 8 claims to close: B-C1 (tenant database names must come from `Tenant::getDatabaseName()`, never synthesised as `tenant_<uuid>`), B-B3a residue (aggregate-before-validate ordering; heredoc `cmp` on timestamp/registration-resume/maintenance/asset gates), M-E (subsecond ordering → pre-push deploymentId baseline)), the fix prompt docs/handoff/CODEX-PROMPT-slice-fix-WLOTA-1a-round-7-2026-09-09.md (its corrections are the gate r7 minimum corrections), the shared staging manifest docs/superpowers/plans/2026-09-06-parapharmacy-staging-push-manifest.md and the U-1 fact sheet docs/superpowers/plans/2026-09-09-parapharmacy-staging-topology-U1-resolution.md (U-1 is RESOLVED: separate Dokploy applications, exact IDs, per-app env + redeploy; do NOT fault the plan for dropping the compose branch, and DO fault any remaining compose reference), the owner rulings docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md (RD2, Q4, Q10 — all defaults accepted 2026-09-06), and docs/conventions/09-SECOND-OF-EVERYTHING.md, 10-BENCHMARK-FIRST-SPECS.md, 11-ONE-SURFACE-PER-CONCEPT.md.

Note on M-E: the U-1 fact sheet documents the OBSERVED Dokploy deployment record shape (`description == "Commit: <40-hex sha>"`, array unordered, correlate by description + a pre-push baseline of deploymentId values, then poll that deploymentId to `status == done`). Judge the Push-5 web gate against that observed shape; do not fault the plan for not using the `Hash: <sha>` format from Dokploy's canary source.

Note on B-C1: `config/tenancy.php:59-63` sets prefix `tenant` (no underscore) + suffix, and `DatabaseConfig.php:66` honours a persisted per-tenant override; the only acceptable producer of a tenant database name is `Tenant::getDatabaseName()` executed inside the API container. Verify that no §11 loop, snapshot, backup or restore builds a database name from a UUID by string concatenation.

Audit rules:
1. Resolve EVERY absolute `path:line` citation in rev 8 at HEAD; report the count checked and any that do not resolve or whose content does not support the claim.
2. For each rev-7 finding, verify the closure against the actual plan text AND against source: the tenant-databases manifest producer must be copy-pasteable, run inside the current API container, and be validated one-to-one against the immutable UUID manifest before any per-tenant loop consumes it; every per-tenant loop must capture all statuses and outputs without early exit, aggregate raw markers unfiltered, copy evidence to the host, and only then validate; every §11 gate (timestamp, registration pause/request/capture/resume, maintenance restoration, served asset and fingerprint, normal and fallback) must `cmp -s` against a committed heredoc with an explicit `fail_gate` branch; the Push-5 web gate must save the pre-push deploymentId set, select only a `Commit: ${PUSH5_SHA}` record absent from that baseline, pin it, poll it to done, and compare the record with the committed template — the incident fallback identically.
3. Check for REGRESSION of every previously accepted closure (round-1 B1–B5, M1–M10, N1–N2; round-2 B-A, M-A, M-B; round-3 B-B1, M-D; round-4 M-C, N1; round-5 B-B3b, N-B, N-C; round-6 B-B2 non-destructive flag-off seeding for marker-carrying tenants with the two named PG tests, M-F inventory) and of the Preserve list in gate r3. §6, §7, §8 and §8.9 are byte-identical to rev 7 by construction (orchestrator census) — do not re-audit their prose, but DO re-resolve their citations at HEAD.
4. Check dispatchability: Tasks 1/2/6 only, exact files, exact symbols, red-first tests with first failing assertion + exact command + lane, convention-09 evidence, both required reviewers per task, verbatim manifest checklist, no design changes outside what a gate item named.
5. Reject false positives explicitly; do not fault A-1a for hold/eligibility/release/reject work that belongs to A-1b/A-1c.
6. Classify every finding as TASK-CONTRACT (Tasks 1/2/6 code, tests, DTOs, web gating) or STAGING-RUNBOOK (§11 execution mechanics). Add a section `## Split assessment` stating whether Tasks 1/2/6 could be dispatched today with §11 carved out as a separately gated staging runbook, and list exactly which findings would remain in that runbook.

Output format (markdown): `# Adversarial gate result`; audited HEAD SHA + citation count; `## BLOCKER`, `## MAJOR`, `## MINOR` sections — each finding with Plan (rev 8 path:line), Source (path:line), Failure scenario, Minimum correction, Class (TASK-CONTRACT | STAGING-RUNBOOK); `## Rev-7 closure table` (finding | CLOSED / NOT CLOSED / REGRESSED | evidence line); `## Rejected false positives`; `## Preserve`; `## Split assessment`; `## Owner decisions required`; final line exactly `VERDICT: DISPATCH-READY` or `VERDICT: CHANGES-REQUIRED`. DISPATCH-READY only if there are zero BLOCKER and zero MAJOR findings.
