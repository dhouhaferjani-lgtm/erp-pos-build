# SESSION M — Dhouha: continue wave-2 testing & fixing (2026-09-02)

You are taking over the **wave-2 purchase-order testing-and-hardening program** for AutoERP (first tenant: TN parapharmacy, IziPOS) from Session L. Everything below is committed and pushed; read this file plus the two anchors first:

- **Evidence record (the single source of truth for what was measured):** `docs/superpowers/reviews/2026-09-01-wave2-po-evidence.md`
- **Gated scenario matrix (the contract):** `docs/superpowers/audits/2026-09-01-wave2-po-flow/02-scenario-matrix.md` (rev 4 + `[run24]/[r4c]` corrections; appendices `01a-01d`, research `01-research.md`, ledgers `03/04/05-*.md` in the same folder)
- Gate/review trail: `docs/superpowers/reviews/2026-09-01-wave2-po-spec-gate-r1..r4*.md`, `…-lane-l1-code-review-r1/r2.md`, `…-lane-l2-code-review-r1.md`, `…-lane-l1/l2-brief-gate-*.md`

## State at handover (all on `origin/dev`)
- 154-row matrix fully measured on fresh tenants: **part 1 50/50** · **part 2 111/112** (1 deliberate fixme `LAND-5b`: no additional-cost reversal route exists).
- Fix lanes **L-1 (receipt hardening: F-W2-02/09/10/12/27)** and **L-2 (FE hygiene: F-W2-37/38/39)** implemented by Codex, adversarially reviewed (r2 = MERGE both), **browser-re-verified 50/50** with the fixed tolerances removed, merged: `f02a71646`, `ab68fe1ab`, evidence/specs `b39fa6518`.
- Playwright harness committed at `apps/web/e2e-local/` (`wave2-support.ts`, `wave2-shared.ts`, `wave2-po.part1.spec.ts`, `wave2-po.part2.spec.ts`, `pw.config.ts`, `tsconfig.verify.json`) — **run manually only, never in CI**.

## Non-negotiable rigor (owner rules — identical to Session L)
1. **Empirical evidence only.** A row is "confirmed" when a committed evidence line carries `[measured]` beside `[derived]`, with SQL output and screenshots. Every browser leg registers `page.on('response')` 5xx capture AND console-error capture and asserts **zero**, tolerances named one by one (the list lives in `wave2-support.ts` — each is a comment citing its finding; K-10's `/auth/me` 401 on `/login|/register` is still tolerated until that lane merges).
2. **No un-gated fixes.** Finding → `LANE-*-BRIEF.md` → adversarial gate by the matching reviewer agent (`inventory-costing-reviewer`, `stock-gl-interaction-reviewer`, `treasury-reviewer`, `frontend-conventions-reviewer`, `tenancy-authz-reviewer`) → conditions applied → Codex implements → adversarial CODE review (r1, fix round, r2) → orchestrator commits path-scoped → **browser re-verification** → merge to local dev. Expect the gate to find real blockers — L-1's brief took 2 rounds, the spec took 4.
3. **Benchmark-first (conv. 10) + second-of-everything (conv. 09) + one-surface-per-concept (conv. 11)** on every brief; TDD red→green BY PATH (never the full PHPUnit suite — it crashes the laptop); PG legs on your own DBs `autoerp_test_m` / `autoerp_test_m_central`.
4. **Shared dev discipline:** own worktree per lane, path-scoped commits only (`git commit -- <paths>`), merge to LOCAL dev, promote to origin/dev only as clean fast-forward in owner-approved windows (push = **staging auto-deploy incl. tenants:migrate**). Never `git stash` in worktrees. Codex cap: 2 active lanes machine-wide — check `pgrep -x codex` cwds and coordinate with any parallel session.
5. **Triage harness-vs-product before booking anything.** Session L retracted two "findings" (F-W2-37 v1, F-W2-42) that were harness artefacts, and converted several matrix expectations into measured observations. Cite `file:line` for the product side before you write F-W2-xx.

## Environment recipe (yours — do not touch other sessions' stacks)
- Worktree: `git worktree add .worktrees/M-wave2 -b test/M-wave2 dev`; `cp -R apps/api/vendor` + `cp apps/api/.env` in; set `APP_URL=http://localhost:8016`, `QUEUE_CONNECTION=sync`; symlink `apps/web/node_modules`; in-tree `apps/web/vite.local.config.ts` (mergeConfig, port **5179**, proxy `/api` + `/sanctum` → `http://localhost:8016`). Ports 8010-8015 / 5173-5178 are taken by other sessions.
- Run: `cd apps/web && CAMPAIGN_API_URL=http://localhost:5179 ./node_modules/.bin/playwright test --config e2e-local/pw.config.ts e2e-local/wave2-po.part1.spec.ts` (config baseURL must be edited to 5179 in your worktree). `CAMPAIGN_API_URL` is mandatory — `e2e/campaign/journey.ts:9` otherwise fetches the shared `:8010` stack cross-origin.
- Registration throttle: 5/15 min/IP shared across all local stacks via Redis `:6380` — one fresh tenant per run; `php artisan cache:clear` in YOUR worktree between bursts. Oracle: psql `127.0.0.1:5433` `autoerp/autoerp_secret`, DB `tenant<uuid-with-dashes>`.
- Harness pitfalls that each cost a run in Session L (full list in the run-log table of the evidence doc): console-guard tolerances match on `message.location().url`; harness-initiated 4xx are excluded via the `harnessPaths` set (5xx stays guarded); psql `-At` prints command tags after `RETURNING` and empty lines for NULL (`COALESCE(col::text,'NULL')`); `string_agg(numeric)` needs `::text`; ESM has no `__dirname`; draft receive returns 200 with the receipt in `meta.goods_receipt`; supplier-invoice resources expose `number` not `document_number`; `journal_entries` actor column is `posted_by`; goods-receipt list route is `/purchases/receipts` (Drafts tab, `window.confirm` on Post); the company wizard is 4-step cards; `NON_REGISTERED` is uppercase; company update is `PUT` with `name` required.
- Codex dispatch pattern: prompt file + `nohup codex exec --sandbox workspace-write -C "<worktree>" "$(cat prompt)" < /dev/null > log 2>&1 &` + pid file + a monitor. **Prepend `DESIGN PRE-APPROVED — non-interactive run: DO NOT stop to ask for approval; implement fully now`** or gpt-5.6-sol exits at "approve this design". Log <5 KB after 10 min = hung, kill + relaunch. The sandbox **cannot run `git commit` in worktrees** (index.lock denied — commit yourself from the staged files) and **cannot launch Chrome** (you run Playwright, paste verbatim results).

## Your queue, in priority order
1. **F-W2-13 — supplier-payment refund (Q-10 hotfix trigger, P0-grade, measured `W2-EDGE-8`)**: refund of a completed supplier payment returns 201 and books **Dr `customer_receivable` 5.000** on a SUPPLIER payment; `partners.payable_balance` moved 18.800→23.800. Owner ruling Q-10 says an incoherent AP position "gets blocked immediately as a hotfix" — confirm the ruling with the owner, then brief the narrow block (refuse `refund` for supplier-side payments, or make the refund supplier-aware) and run it through the gate. Oracles: `PaymentRefundService.php:1072-1085` (`canRefund()` status-only), 01-research §5 F-W2-13.
2. **F-W2-01 — receive idempotency (P0, measured `W2-IDEM-2`)**: sequential re-POST of the same receive payload double-counts (stock 8.0000 on a 4.0000 PO; 2 receipts/movements/JEs). The concurrent race is already serialised (`W2-IDEM-2b`: 10.0000|1|1|1) — the fix is an `idempotency_key` (the standalone lane at `StandaloneReceiptService` has the pattern to mirror) or a received-state recheck under the existing lock. Brief + gate first.
3. **F-W2-03 — GR-IR posting swallow (P0)**: `PostGrIrOnGoodsReceipt.php:41-52` swallows every `\Throwable`; a committed **Draft** entry survives when `GeneralLedgerService.php:2134` throws, and replay is a no-op because `:2052-2054` early-returns on ANY existing entry regardless of status (status-aware shape exists at `:5224-5229`). Detector queries (three arms incl. `free_movement_id`) are in `W2-WDIL-4` — reuse them in the fix's tests.
4. **F-W2-40/41 — FE match-status UI (P2/P3, measured)**: `SupplierInvoiceDetailPage.tsx:51-56` `matchIconConfig` misses `quantity_variance`/`exception` → **page crash**; the `qty_blocked` key exists nowhere in the backend so `isQtyBlocked:125` never fires and `btn-post` never disables under `match_enforcement=block` (server still 422s). One FE lane; remove the `F-W2-41` tolerance in `wave2-support.ts` afterwards and re-run the MATCH class.
5. **Owner rulings Q-1..Q-12** (in `02-scenario-matrix.md` §OPEN QUESTIONS): each unlocks its lane — Q-1 short-close (F-W2-04/05), Q-2 over-receipt tolerance, Q-3 bonus WAC, Q-5/Q-6 landed-cost repost + freight liability (F-W2-18's phantom PPV income is measured at 30.000/15.000), Q-8 receipt reversal, Q-9 supplier credit-note route, Q-11 supplier defaults, Q-12 confirmed-PO lock. F-W2-14 (cashier can post/pay suppliers — measured `W2-PERM-10/11`) is a confirmed defect awaiting the same batch.
6. **Ticket backlog** (small, gate-lite but still reviewed): F-W2-43 `stored_events.aggregate_uuid` empty for `PurchaseOrderConfirmed`; F-W2-44 stale document page 404s after company switch; F-W2-45 `journal_entries.posted_by` NULL; F-W2-26's SQLSTATE-leaking 500 is FIXED for the receive route (`whereUuid`) but sibling PO routes still lack the constraint; auto-save returns a bare non-enveloped body; `/dashboard/stats` has NO backend `can:` gate (ticket `L-2-FU-dashboard-stats-ungated`); L-1 r2 minors L1-C2-01..07 and L-2 r1 minors L2-C1-01..07 are listed in their review files as tickets.
7. **Staging round** (after the owner's push window): same two specs against `https://erp.otospex.dev` — but FIRST the ops precondition: `php artisan tenants:run db:seed --class=RolesAndPermissionsSeeder` on existing tenants (the new `goods-receipt.receive-expired` permission), or LOT-6's override arm fails for an operational reason. Fresh tenants seed it automatically.

## Ops notes for the owner (relay, don't do)
- Staging deploy auto-runs `tenants:migrate` on push — this range carries **no migrations**, only the seeder change above.
- Local Docker `autoerp_postgres` segfaulted once under multi-stack load (64 MB `/dev/shm`); consider `--shm-size=1g`. A register-500 during a run can be that — check `docker logs autoerp_postgres` before booking a finding.

Keep a running ledger at the bottom of your session file (`docs/sessions/session-M-…/HANDOVER.md`, gitignored) — one timestamped line per event, and commit evidence after every round. Good luck — the matrix is your map; the gates are your safety net.
