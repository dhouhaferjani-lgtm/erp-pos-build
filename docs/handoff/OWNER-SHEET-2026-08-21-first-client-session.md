# Owner sheet — first-client dev session 2026-08-21 (session 0578e8d8)

> Autonomous session under the landed enforcement layer (origin/dev `c6d6308ae`). Working mode:
> local Docker stack + Playwright verification (staging Dokploy down, owner fixing). Decisions the
> session could make under the standing delegation were made and are FLAGGED here for review;
> decisions that are genuinely yours are ASKS. Answer in place.

## A. Decisions made under delegation (review, veto if wrong)

| # | Decision | Rationale / evidence | Status |
|---|---|---|---|
| A-1 | P2 post-hoc full-suite dispatch (run 32465616707) recorded as waiver-consistent: the only red jobs are the 5 documented inherited classes; `tmp-p2-dispatch` branch deleted | Session ledger entry 2026-08-21; matches the recorded waiver expectation | done |
| A-2 | `scripts/adversarial-review.sh` verdict parse hardened (exact final-line match, fail-closed) — the old `*ACCEPT*` glob accepted "NOT-ACCEPT" and verdicts anywhere in the file | Codex brief-gate finding 3, `docs/superpowers/reviews/2026-08-21-z-sale-decomposition-brief-gate-r1.md` | landed local dev |
| A-3 | C-2 (device Z sale-branch gross-as-net) brief GATED (Codex round 1, 6 findings applied) and DISPATCHED; the M1 signed-bytes versioning question will be ruled by the parent-dispatched fiscal-pos specialist gate autonomously (P3-M1 R-4 precedent), recorded here when made | Brief + progress YAML on local dev; LEDGER row C-2 | lane in flight |
| A-4 | Wave-1 fix lanes dispatched on verified premises: fiscal-bytea (OutboxIngestor PARAM_STR→stream), G-3 training-receipt containment, G-4 VAT POS-refund netting, Arabic backfill (145 keys + 9 plurals) | Premise-verification agent report (all four LIVE with file:line) | lanes in flight |

> ~~2026-08-23 early: first answer transmission arrived with every slot EMPTY; nothing recorded.~~ **SUPERSEDED — answers re-sent (voice transcript) and RULED 2026-08-23:**
>
> - **B-1 RULED (a)**: tenant #1 via self-service signup + post-hoc corrections; super-admin provisioning = post-launch build.
> - **B-2 NOT YET RULED — research ordered**: owner wants an industry-standards brief + a concrete risk explanation before choosing; stated lean = "opening cash in a POS machine should be an opening balance for sure" (i.e. toward option (b), the `opening_float` kind). Research lane dispatched 2026-08-23.
> - **B-3 RULED (a)-now**: wire `Location.pos_enabled` properly (terminal claim/open must respect it) — owner believes it used to work; fix pre-launch unless sizing shows it's genuinely large, in which case post-launch is acceptable. Sizing scout dispatched.
> - **B-4 SUPERSEDED BY A BIGGER ASK**: owner saw real issues while testing; wants an investigation of what happened with parties/partners AND a proper contact-vs-company disambiguation (actions relevant to a contact vs to a company must bind to the right entity). Audit DELIVERED 2026-08-23 (`AUDIT-parties-partners-disambiguation-2026-08-23.md`, commit `6b6f4d2ff`). **ESCALATED same day after owner read the findings — now a RESEARCH-FIRST REDESIGN PROGRAM**: (1) retrospective on WHY the drift happened + why building continued on top + prevention guardrails; (2) proper domain research BEFORE the fix — owner's design sketch to engage: POS sells to a contact when a private person, or to a company which always links to a contact person; alternatively POS stays B2C and an invoice request "pulls together" the sale to invoice as a company; determine MINIMUM required contact data (e.g. phone OR email); clean up the dashboard and link entities with zero ambiguity. Both research agents dispatched 2026-08-23. Pre-launch shape-neutral minimum (audit a1 buyer-block fix + a2–a8 cleanup) proceeds in parallel and does NOT wait on the research; the (b) party_kind consolidation is entry-gated on the research spec + owner sign-off.
> - **B-5 RULED (a)+**: runbook is enough for launch; build the settings UI too if cheap (sizing scout dispatched).
> - **B-6(i) RULED include-per-standards**: research the declaration standards (TN focus); anything that must be declared is included — apply to both arms together. Research lane dispatched.
> - **B-6(ii) RULED: CHANGE, not ack**: owner sees no reason X/Z reports exclude refund VAT — "it's probably part of it, we need to do it properly"; correctness items land before the first client. Scoping dispatched (device-signed Z surfaces interact with the C-2/C-6 build coupling — must be scoped before touching).
> - **B-7 RULED defer**: translation review later; Arabic not crucial for this launch.
> - **B-8/B-9/B-14(i)/B-14(ii) ACKED under the GREENFIELD framing** (owner: "8, 9, 10 and so on — we don't need to repair anything previous, we just need to make sure things run properly from now on"): r2f4 imbalance class #2 acked; deptrac 176→182 acked; dpa-v8 whole-branch stands; r2f2 stays discarded. (B-8/B-9 folded under the "and so on" sweep — flagged as inference; veto window still open until promotion.)
> - **B-14(iii) RULED (a)**: NO dedicated repair command. Greenfield: no legacy repair needed; plus a per-tenant ACCOUNTANT ROLE with manual-entry capability is planned ("log in as an accountant and edit whatever needs to be edited") — already-cancelled lineless residue is accountant-side manual disposition, and for tenant #1 the population is ~nil anyway.
> - **General owner principle recorded**: in the greenfield context, backward-repair work can be discarded; forward correctness is what must be right — and correctness items land before the first client.
> - **B-13 RULED fix-properly**: the manager-gate residuals get a real fix (role composition PIN-only + the ungated X-report surface + tooltip plumbing), not conceal-more, not accept-documented. Lane queued behind the current wave.
> - **B-11 runner: NOT YET (owner-confirmed 2026-08-23). Dokploy: NOT YET (same).** Consequences: S-17 CI-blind window stays armed (no GitHub gate re-run possible on the promotion candidate — the conditional promotion go therefore rests on local + Playwright evidence alone); `SELF_HOSTED_RUNNER_READY` stays false so the 8 F-2 feature lanes stay parked; a push to origin/dev will NOT auto-deploy staging while Dokploy is down — the two migrations + S-18 seeder step will instead fire on the FIRST deploy after Dokploy is repaired, so the deploy-day checklist must travel with that repair, not with the push.
> - **B-17 promotion: CONDITIONAL GO** — "we could do that for sure if everything is correct, works fine, and has been tested using Playwright." Recorded as: promotion proceeds after the current lanes gate+merge AND a Playwright first-tenant end-to-end simulation passes; the S-17 GitHub re-run remains impossible while quota is out, so this is an owner-authorized promotion under the CI-blind window on local+Playwright evidence (the S-14 dispatch leg, S-18 seeder step, and B-10 i18n re-pin still execute at/after the push, and the owner-side legs still need the owner).
> - **Operating mode re-confirmed by owner**: Fable orchestrates + final review + merges only; sub-agents implement; session finishes with a Playwright first-tenant simulation covering as many functionalities as possible.

> **2026-08-23 delegated decisions under the B-3/B-5 rulings (A-rows — review, veto if wrong):**
> - **A-5 (B-3 execution)**: sizing found the flag was NEVER enforced (git -S proof: no enforcement site ever existed — the "used to work" memory is unsupported), the claim/request/create half is S and server-only, the shift-open half is unenforceable server-side (v3 terminals open shifts fully offline by design) and needs the TerminalResource payload + a device build → deferred to the D-1 stack as a rider. **Delegated sub-ruling A2**: enforcing with today's data would brick terminal claim on every tenant (all auto-provisioned Main Locations are `pos_enabled=false`, test-pinned "do not flip, see E-8") — ruled: provisioning now creates Main Location `pos_enabled=true`; MIGRATION-BEARING evidence-based backfill enables locations that already have a POS terminal attached; user-CRUD keeps explicit choice. Lane dispatched 2026-08-23.
> - **A-6 (B-5 resolution)**: the settings UI resolved NOT-cheap (M: no HTTP surface exists for `country_payment_settings` at all; ~200 lines of fail-closed money validation locked inside the artisan command need extraction first; the enable flag shares a row with the denomination so a naive card IS the O-24/G-2 bypass; row is country-scoped not company-scoped). Per your "build if cheap" ruling → **runbook for launch, UI deferred post-launch** (revisit when refund-rounding lands and G-2 unblocks).

> **2026-08-23 money-semantics research outcomes** (`RESEARCH-opening-float-and-vat-doc-count-2026-08-23.md`, commit `2a18fe5d7`):
> - **B-2 RESOLVED (research delivered; executing the pre-launch S items under the correctness principle, flagged for veto)**: your lean confirmed on substance — the float IS an opening balance — but the mechanism matters: it must be booked ONCE, in the accounting opening batch (Dr cash / Cr 119), with the till side as a subledger seed with NO journal entry (unanimous across Odoo/ERPNext/Square/Lightspeed + FR caisse practice). **The real risk is worse than double-counting: today's only operator path ("Adjust balance") books the float as REVENUE (7580) — phantom income inflating the IS base** — and seals it immutably into the fiscal hash chain. Pre-launch (dispatched): the runbook section (none existed at all) + a guard refusing adjustment-seeding of a virgin repository. Post-launch: the `opening_float` movement kind (M — the enum case and the reconciler's JE exemption already exist, only the writer is missing); it also closes the float half of SV-3.
> - **B-6(i) RESOLVED — NO CODE CHANGE**: both arms ALREADY count credit notes and return receipts symmetrically; TN law (CDET art. 126 + DGELF doctrine: an avoir is dutiable AS a facture) and every comparative regime (SAF-T PT, Italian RT `NumeroDocCommerciali`) count them. The behavior converts from accidental to pinned (comment + count assertions, in the dispatched closeout lane).
> - **🚨 NEW ASK B-15 — the TN chart seeder is a French PCG chart under a Tunisian label** (verified line-by-line against the official OECT nomenclature): cash seeded at `53` where official TN cash is `54/5411` (real `53` = Banques); `63`↔`66` and `73`↔`75` swapped; `6580/7580` (where every cash adjustment + shift variance posts) mean something materially different in TN; seeded `6354` contradicts the recorded "TN 6654" rule. Charts are per-tenant data and possibly a deliberate FR-shaped starter — needs YOUR ruling + TN-accountant confirmation, and the DECISION is pre-launch (tenant #1's accountant will read this trial balance).
> - **🚨 NEW ASK B-16 — avoir stamp duty seeded stale at 0.600; law says 1.000** (facture rate, décret-loi 2022-79; the sibling invoice row already carries 1.000). Every credit note under-collects 0.400 TND of a duty that IS declared and paid. Fix is a one-line seed change + reseed — held for your/accountant confirmation per the brief.
> - **🚨 NEW ASK B-17 — TN does not net avoirs into declared turnover** (art. 9-I-5 imputation via rubric-4 "déduction additionnelle"; our arms reduce the base instead). Net tax payable IDENTICAL — only declared CA presentation differs. Raise with tenant #1's accountant before the first filing; ticket only, touches G-4-gated code.
> - **Pre-launch verifications queued** (in the closeout lane, read-only): does tenant #1 use the supplier-invoice flow (its input VAT is silently absent from the declaration — potential P0); does anything sell at 0%/exempt through the POS (zero-rated POS turnover is undeclared while the same sale invoiced IS declared); `stamp_duty_count` fragility (rows-not-distinct, no deleted_at, company-wide vs per-branch).

> **2026-08-23 B-4 program: both research deliverables IN** — retrospective `RETRO-partner-party-drift-2026-08-23.md` (`0ca47e61d`) + target-model spec `docs/superpowers/specs/2026-08-23-party-contact-target-model-research.md` (`d80ee2375`). The spec converges on: `party_kind (person|organization)` on `partners` (contacts NEVER billable — unanimous across Odoo/Dolibarr/ERPNext and forced by 24 FK tables + immutability triggers); POS variant (b) B2C-first with facture-on-request escalation as a separate Document (TN art. 18 is literally written for it; zero new sealed keys); minimum data = name + (phone|email), already the till's rule, promoted everywhere with a shared normalizer. **NEW P0 found (lane dispatched)**: three incompatible TN matricule-fiscal regexes — partner validation demands 3 letters, the sealed-payload validator demands 2, so a valid partner MF fails at seal time; live on the account-charge B2B lane today. **a1 re-scoped** (buyer.customer_id = resolved server partner id ONLY, else the projection FK breaks; golden populated-buyer fixture required) and **a4 replaced** (show-B2B-on-NULL was wrong; the nature field becomes required with the Phase-2 heuristic run early). **NEW ASK B-18 — sign off the target model + answer OQ1-5** (each with the researcher's recommendation): OQ1 person attributes as columns on partners, NOT auto-created contacts (disagrees with the audit; recommended); OQ2 delete the standalone CRM Contacts surface, fold into the org detail tab (recommended); OQ3 facture to organization = HARD BLOCK on missing matricule fiscal, person exempt (recommended); OQ4 1 receipt → 1 facture at launch, exclusion logic shaped for N:1 later (recommended); OQ5 cashier gets the "Demander une facture" button at launch (recommended). Phases 2-5 are entry-gated on this sign-off; Phase 0 (regex P0) + Phase 1 (re-scoped a1 + a2/a3/a5-a8) proceed under the correctness principle.

> **2026-08-23 closeout-lane verification results (research recs #7/#7b/#8b)**:
> - **🚨 NEW P0 (B-19) — supplier-invoice deductible VAT is silently absent from the TN declaration.** Double lock proven: `EloquentVatDataRepository` excludes the `supplier_invoice` type AND `CreateSupplierInvoiceService` never snapshots tax details (zero rows to include even if the type were whitelisted; the backfill command can't reach them either). Live data: 43 supplier invoices / **704.401 TND deductible VAT / 0 tax-detail rows** in the demo tenant (reproduced in a second: 6/142.500), all authored through the shipped ungated UI — seeders create none. Bonus unverified: 149/156 `expense` documents also lack tax-detail rows (different writer — if real, input VAT is near-totally undeclared regardless of flow). **Fix lane queued (M): snapshot at the writer + whitelist the type + backfill; lands pre-launch if tenant #1 records purchases** — your call whether tenant #1 uses the supplier-invoice or the expense flow decides priority, but the expense arm needs its own verification either way.
> - **(7b) zero-rated POS turnover: real code defect, currently dormant** — no genuine 0% products exist in any tenant (the only 0% rows are test artifacts), but two live routes can mint them (ProductFactory's unfiltered rate pool; TaxResolutionService's hard `'0.00'` fallback). Ticketed for a small lane; not launch-blocking while the catalog has no 0%/exempt items — worth a one-line runbook check at onboarding ("no product may carry 0% unless deliberately exempt").
> - **(8b) `stamp_duty_count`: armed-latent P2 confirmed + one arm firing today** (a cancelled invoice is being counted); the figure freezes into `vat_periods.special_items` at period close so recomputation can't repair it, and it reaches the DGI via accountant transcription. Ticket ready; per-branch reporting blocked on an owner ruling (art. 126 wants per-établissement; we aggregate company-wide).
> - **B-2 pre-launch items LANDED in the lane (pending gate)**: opening-float runbook written (`RUNBOOK-opening-cash-float-2026-08-23.md`); `REPOSITORY_NOT_SEEDED` guard at the service layer with a fail-open shift-variance carve-out (orthogonal to R-8, either merge order); B-6(i) ruling pinned in code with mutation-proof count assertions.

> **2026-08-24 PLAYWRIGHT FIRST-TENANT CAMPAIGN — VERDICT: NOT PROMOTION-READY (promotion HELD per the owner's own condition).** Full report: `PLAYWRIGHT-first-tenant-campaign-2026-08-23.md`. The rehearsal proved the foundation SOLID (provisioning end-to-end clean incl. the 141-row TN chart + stamp duty + balanced trial balance; EVERY new guard from this session behaved as designed with its message reaching the operator; O-28/B-3/B-4 verified live) and found 5 NEW blockers, all now in dispatched fix lanes: **N-1 P0** silent wrong VAT (non-default product rate discarded on create; wrong rate hash-chained on POS) · **N-2 P0** SO/DN confirm 404s for any product without a stock_levels row — every new tenant's day one, and it breaks the pre-delivery guard's own remedy · **N-3/N-4 P1** the opening-balances wizard and the VAT declaration screen CRASH on API↔FE contract drift · **N-5 P1** has-pins contradicts the just-shipped offboarding parity. **NEW ASK B-20 (N-6)**: a payment can be recorded against an invoice the pre-delivery policy forbids posting → AR goes negative, output VAT unrecognised. Rule: (a) gate payment recording on posted invoices, or (b) book pre-posting payments as customer advances (industry-standard; more work). P2/P3 tail (N-7..N-13: wizard raw i18n keys, $-hardcoded GL amounts, EMPTY units table at provisioning, missing action keys, repo location_id NULL, form-error visibility) + R-1 (now REPRODUCED on the prod build; a dev-only StrictMode bug N-14 masks it in dev) queued as the next wave after the blockers. **Pattern named by the campaign: 3 of 5 blockers are API↔FE contract drift — exactly what the stalled OpenAPI contract lane (`project_openapi_contract_lane`) exists to catch; recommend re-prioritizing it post-launch.**

**RULED 2026-08-24 (owner, verbatim substance): tenant #1 launches with POS first, then purchasing (catalog+inventory), branch transfers (branches of one company), treasury; accounting later; B2B sales later (fix properly, not launch-critical). Session A queue re-ordered accordingly; wave 3 = POS rehearsal.** Also RULED: N-6/B-20 = option (b) advance model within the independent-dimensions lifecycle model (gating payments on posted invoices REJECTED); unposted invoices may be printed with VAT + a 'not posted' line (TN: posted = sealed + MoF QR).

## B. Asks — need your ruling (from the first-client onboarding audit)

Full audit evidence lives in the session transcript; gaps are ranked G1–G13. The asks:

| # | Question | Options / session lean |
|---|---|---|
| B-1 | **Operator-driven tenant provisioning (G2).** Today the ONLY working path to a usable tenant is the public self-service `/register` form (hardcodes 14-day trial, `<slug>.synerivia.tn`, "Main Location"). `php artisan tenant:create` produces a permanently-503 tenant (no DB/migrations/seeds). For tenant #1: do we (a) onboard via the signup form + post-hoc corrections, or (b) build an admin provisioning endpoint/flow first? Session lean: (a) for tenant #1 (fastest, path is battle-tested by the demo seeders), (b) as a post-launch feature. Either way the session queues a small guard lane so `tenant:create` refuses loudly instead of bricking (G1). |
| B-2 | **Opening cash float (G8).** Repository opening cash is entered as a balance *adjustment* (no draft/validate/lock lifecycle) while the ACCOUNTING opening batch debits the same GL cash account — double-count risk is entirely on the operator. Industry standard is an opening-balance kind with reconciliation against the GL opening. Options: (a) ship as-is for tenant #1 with a written runbook line (operator = you), (b) small lane: an `opening_float` adjustment kind excluded/reconciled against the accounting opening batch. Session lean: (a) for launch + (b) queued. |
| B-3 | **`Location.pos_enabled` is a dead flag (G12)** — set by three creation paths, enforced by nothing. (a) wire it (POS terminal claim/open refuses at a disabled location) or (b) remove it from the UI until wired. Session lean: (b) is dishonest-UI removal, (a) is a small lane; recommend (a) post-launch, document meanwhile. |
| B-4 | **`parties` vs `partners` import duplication (G13).** `partners` is a strictly weaker duplicate (no opening balances) — a client picking it loses AR/AP openings silently. Session lean: hide `partners` from the dashboard (keep API for history), or add a deprecation banner steering to `parties`. Cheap; will implement the hide+banner unless you object (flagged, not yet done). |
| B-5 | **Cash-rounding UI (G4)** is artisan-only (`pos:configure-cash-rounding` via `tenants:run`, exit-code swallowed). For TN millime rounding on tenant #1: is a settings UI wanted pre-launch, or is the runbook step enough? (Also gated by O-24 sign-off, unchanged.) |
| B-6 | **G-4 VAT lane sub-questions** (money netting fixed either way): (i) should `document_count` in the declaration count return receipts — and note the DOCUMENT arm counts credit notes the same way, so any ruling should apply to both arms together; (ii) explicit ack wanted that shift X/Z reports *exclude* refund VAT (own positive refunds block) while the declaration now *nets* it — the two numbers deliberately won't reconcile. |
| B-7 | **Arabic legal copy**: the backfill lane machine-authors the `legal.privacy.*` Arabic tree (38 keys). Needs a native/legal read before production exposure to AR-locale users. |
| B-8 | **dpa-v8 vs the F-9 park order — RESOLVED BY THE GATE under delegation (review/veto)**: the inventory-costing gate RULED land-whole-branch-now on verified grounds: the CN-posting wiring has ZERO callers post-rebase (grep-proven — F-3's controller is the only activation path, unchanged), so holding the wiring buys nothing dormancy doesn't already buy, while each out-of-tree cycle measurably costs (this rebase produced 5 conflicts + a genuine branch×dev defect only visible in-tree). The F-9-before-F-3 ordering is PRESERVED by dormancy. Residual pins made executable via the ci.yml allowlist (gate condition). Fix round in flight (both gates found real seam defects the rebase exposed — un-dilution GL leg, D-e over-breadth — all prescribed). Veto = revert the merge before promotion. |
| B-9 | **r2f2 discarded** under ruling #4 — recorded rationale in the dispositions file; tip preserved at tag `archive/r2f2-cancel-flow-prompt-2026-08-21`. Veto = restore from the tag. |
| B-12 | **🚨 C-2 lane M0/M1 discovery — the signed Z headline `net_sales` EQUALS `gross_sales` on every taxed shift** (`apps/pos/src/lib/pos/cartTotals.ts:36` sums GROSS line_totals into `subtotal`), and that figure is exported as NF525 `<VentesNettes>`. This falsifies the C-2 brief's "headline totals are correct" premise; the C-2 lane correctly did NOT fix it (rule 4) — it needs its own sequencing decision: fix before tenant #1's first signed shift (same cheap-window argument as C-2 itself) or after. Also found: SESSION_CLOSE is a third affected signed event type, folded into C-2's scope by the memo. Awaiting your read; the session's lean is to draft a sibling micro-lane brief so both land in the same device build. |
| B-14 | **🚨 r2f4 correcting-entries: two acks + one design consequence** (fiscal gate, probe-proven): (i) **ACK WANTED — the mechanism creates a SECOND sanctioned chained-imbalance class** (an individually-unbalanced correction supplying a broken original's missing leg — inherent to the c4/O-26 design you ruled, but the P3-M1 census closed on "exactly 1" such class; a census row is being recorded so the enforcement layer sees it); (ii) **ACK WANTED — deptrac ratchet +6** (176→182, all one edge shape matching the 3 siblings; the 176 precedent carried your explicit ack); (iii) **O-26 CONSEQUENCE — the correcting-entry vehicle is PROSPECTIVE-ONLY and Invoice/CreditNote-only**: probe-proven that a correction applied BEFORE cancellation balances the subsequent mirror, but an already-cancelled lineless document's one-legged mirror is UNREACHABLE (footprint excludes cancellation mirrors; reversal is idempotent) — "one-time correcting entries for existing broken entries" needs a different vehicle for the already-cancelled subset (accountant-side manual disposition per tenant, or a dedicated repair command — your call; the O-26 upstream-refusal half is unaffected). |
| B-13 | **POS manager-gate leak — AMENDED post-merge (re-gate condition)**: `hasManagerAccess` MAXes the PIN operator's and device user's roles, so a cashier PIN on an owner-logged-in terminal clears "manager-only" routes. WHAT SHIPPED: `/shift` conceals ALL drawer/takings figures under blind-count (fail-closed on malformed policy), and `/reports` now ALSO conceals physical-tender rows+bars whenever a shift is open. REMAINING RESIDUALS for your ruling: (i) exact arithmetic derivability on `/reports` — `cash = net headline − Σ(visible non-cash)` reconciles EXACTLY, so full closure means concealing the headline during trading hours (product call); (ii) the pre-existing X-report discloses per-tender CASH takings to ANY operator with no gate at all (`2026-08-21-xreport-blind-count-gap.md` — the blind regime is already defeated on dev by that surface); (iii) the Header opening-float tooltip (one term, not the secret; needs policy-at-mount plumbing); (iv) the role-composition question itself (PIN-role-only?). The gate ruled ship-deferred acceptable because the lane strictly improves containment and grants nothing X-report doesn't already grant. |
| B-11 | **F-2 runner ops (from the O-29 lane — none agent-doable)**: (1) provision the VPS (8 dedicated vCPU / 32 GB / Ubuntu 24.04 / Docker); (2) register 4 ephemeral repo-scoped runners labelled `self-hosted,linux,x64,autoerp-heavy` (2 waves ≈ 60-70 min wall); (3) confirm in writing the repo stays private (public flip would expose the runner to arbitrary PR code); (4) flip repo variable `SELF_HOSTED_RUNNER_READY=true`; (5) at the promotion carrying the branch, once quota returns, run the S-14 dispatch leg — cheap proof is a dispatch with the variable still off (8 lanes skipped, all-checks-pass green). Lane open questions Q1-Q5 (concurrency budget, PR→dev scope, nightly schedule, ExampleTest deletion, max_locks re-measure) in the design doc. Note: its first real lane run already surfaced 3 genuine PG-vs-sqlite defects (PlatformIntegration sqlite_master introspection, EventSourcing non-uuid fixture, Events max_locks) — the burn-down begins on first execution. |
| B-10 | **Promotion-time i18n re-pin (from the AR gate, P3-6)**: the promotion that carries the Arabic backfill shrinks the i18n baseline 2924→2763 — per the P2 regime you must then update `I18N_BASELINE_PROTECTED_BLOB` + the durable pin tag together in the post-promotion admin commit (CI passes meanwhile since the ratchet is removal-only, but the 161-entry gain isn't locked in until re-pinned). |

## C. Standing open items carried
Four rulings arrived mid-session (2026-08-21, relayed from session de182ed3) and are RECORDED in
LEDGER.md + folded into the queue: **O-26** phase-out lineless posting (lane queued, treasury +
fiscal-pos gates) · **O-27** GO on F-4 backfill (lane queued via country-defaults authority) ·
**O-28** Today's-Sales = NET excl. refunds, labeled (audit lane queued; satisfies the D-3 rider (b)
recording precondition) · **orphan branches** dpa-v8 / r2f2 / r2f4 = evaluate + rebase + full
adversarial gates, merge-or-discard-with-rationale (assessment agent dispatched).
Second ruling batch (direct, 2026-08-21): **O-29** F-2 RULED — laneless Feature tests get real
lanes, heavy lanes target a self-hosted runner (quota exhausted), interim = containerized local
execution (design lane dispatched) · **S-17** CI-BLIND WINDOW recorded — every merge this session is
CI-unverified, locally verified per the interim discipline; GitHub gates re-run on the promotion
candidate before any promotion.
Still open elsewhere: Dokploy deploy pipeline (yours) · staging owes S-1..S-13 once deploys work.

## E. Merges under the CI-blind window (S-17 register — every row locally verified)

> **IN-FLIGHT NOTICE (Session B, 2026-08-24):** lane Q-9 (`fix/sb-q9-kitchen-order-gating`) will make a MINIMAL edit to the shared web files `apps/web/src/routes/index.tsx` (wrap the Orders route in `ModuleGuard module="Menu"`, ~:2976-2984) and `apps/web/src/components/layout/Sidebar.tsx` (Orders entry `module: 'Menu'`, ~:236) — nothing else in `apps/web`. Session A lanes touching those files (`fix/campaign-fe-contract`) should expect a trivial merge; Session B will merge from the fresh dev tip.
> **IN-FLIGHT NOTICE 2 (Session B, 2026-08-24, owner "go ahead"):** micro-lane Q-13 (`fix/sb-q13-tables-module-gate`) adds `module:Tables` to the backend tables/floors route group (`routes_tables.php` — no such gate existed) and, to avoid a per-cycle sync-error row on retail devices, a 3-line module guard inside `apps/pos/src/lib/sync/syncService.ts::pullTables()` mirroring the existing Menu idiom at :1907-1915. No other `apps/pos` or `apps/web` file. Dhouha's unmerged tables chain (#201–206) will see a trivial conflict on the route group line.
| Lane | Branch | Merged at | Local verification |
|---|---|---|---|
| G-4 VAT refund netting | `fix/g4-vat-pos-refund-netting` | `efa2282dd` | treasury gate r1 (revert-probe red-proof) + fix round (probe-verified CI token, voided-return pin); PG 7/31 + sqlite 7/31 by path; manifest checker exit 0 re-run on MERGED dev; actionlint OWED (not installed) at next CI-sighted window |
| Arabic backfill (161 keys + 9 plurals) | `fix/ar-locale-coverage` | `176d2e932` | conventions gate r1 (exhaustive 161/161 placeholder check, empirical tsc causation repro, pure-shrink 1:1 proof) + fix round (3 wording P2s, ar appName guard row); post-merge on dev: i18n suite 51/51, audit:i18n:local exit 0, baseline 2763 held; PLAYWRIGHT live-verified on the local stack (ar locale: RTL + lang=ar through login→dashboard→/treasury/remittances; page h1 AND sidebar both render the corrected حافظات التحصيل, old term absent) |
| G-3 training containment | `fix/g3-training-receipt-containment` | `4a11abff4` | 4 gate rounds total (2 lenses × 2 rounds) + parent-verified final round; 16/71 containment tests green on MERGED dev; regression 105/479 PG in-lane; census SQL committed; stock-side gap deliberately excluded with ticket |
| O-27 chart-purposes backfill | `fix/o27-chart-purposes-backfill` | merged (post-`3d3b76623`) | gate ACCEPT + 7-item fix round; 40/370 PG in-lane; post-merge 8/69 parity + manifest exit 0; MIGRATION-BEARING (self-guarding proven); acks wanted: additive authority accessor + circular Domain import (rule-6 exception, documented at site) |
| O-29/F-2 feature-lane execution | `feat/f2-feature-lane-execution` | merged | 4 gate rounds (Codex r1 + Opus r2/r4, every executed bypass pinned); post-merge checker exit 0 + 86/483 architecture tests green on merged dev; ci.yml workflow-touching → S-14 dispatch leg owed at promotion (B-11 step 5) |
| POS real manager screens (audit G3) | `fix/pos-mocked-manager-screens` | merged | fiscal gate r1 CHANGES + conventions gate r1 CHANGES → fix round → re-gate ACCEPT (2 rulings granted; reviewer independently recomputed the bars-sum-to-net-headline reconciliation and traced legacy/v4 refund disjointness to the writer); post-merge 14 files/125 tests green; device app — Playwright N/A (Tauri surface), evidence = 210-file/2275-test sweep in-gate |
| C-5 voucher projection context | `fix/c5-voucher-projection-context` | merged | treasury gate r1 CHANGES → final round (gate-corrected G-3 arm conversion, revert-probe verified); post-merge 6/55 green; unblocks voucher tenders on real Horizon workers — first-client relevant if vouchers enabled |
| Fiscal-bytea ingestion hardening | `fix/fiscal-bytea-param-lob` | merged | fiscal gate r1 CHANGES (independent PDO probes found the silent backslash byte-collapse) → fix round → r2 ACCEPT (getDirtyForUpdate reasoning proven in vendor by reflection probe) + P3 polish; ci.yml allowlist union-resolved at rebase (manifest checker validates); post-merge 18/90 green; PG Fiscal directory exactly-baseline with 2 previously-red classes now green |
| O-28 Today's-Sales net+labeled | `fix/o28-todays-sales-net-labeled` | merged | gate 2 rounds + micro round; post-merge POS 27/27 + tsc 0 + web i18n baseline held + owner-dashboard 35/35; PLAYWRIGHT live: net headline + 8 gross labels + Returns rendered on /reports; locale rename conflict (totalSales→netSalesExclRefunds) union-resolved, zero surviving consumers verified |
| C-2 device Z decomposition | `codex/z-sale-branch-decomposition-2026-08-18` | merged | full self-review harness: M0-M4, 5 bridge ACCEPT verdicts + the parent-dispatched Option-B ruling; clean rebase; post-merge POS offline+api 27 files/228 tests green; DEVICE-BUILD-GATED (D-1 stack; reversal window closes at rollout — see LEDGER C-2) |
| r2f4 correcting documents | `feat/r2f4-correcting-documents` | merged | 2 full dual-gate rounds (5 probe-proven P1s closed red-first) + prescribed final round (partner axis, supplier fallback, types regen no-op, re-point measured); post-merge 31 passed + 1 correct sqlite skip (PG-verified green), manifest 0, deptrac PASS; escape-hatch ticket CLOSED; census delta = LEDGER C-8; deploy step = S-18 |
| Onboarding hygiene (G1/G9/G11) | `fix/onboarding-hygiene-g1-g9-g11` | merged | tenancy gate r1 CHANGES (route-404 P2) → fix round (incl. an honest revert-probe removing a guard that guarded nothing); post-merge Tenant 9/32 + FE 70/70; `tenant:create` now fail-closed under db-per-tenant; trial-plan absence loud; dead import links gone + permanent route-parity guard. NOTE: the two new backend guards execute only in the parked tenancy lane until `SELF_HOSTED_RUNNER_READY` flips (B-11). |
| C-6 headline + F-4/F-5 | `fix/c6-z-headline-net-sales` | merged | fiscal gate r1 CHANGES (identity choice CONFIRMED against the brief's suggestion; over-strict rule empirically demonstrated) → fix round with revert-replay on the ValueError clause; post-merge validator 151/319 + e2e 10/10; two ci.yml union-conflicts resolved; manifest double-raise seam reconciled deliberately (1132) |
| dpa-v8 supplier goods return | `feat/dpa-v8-supplier-goods-return` | merged | orphan-ruling #4 landing: 926-commit rebase + 3 rounds under dual gates (un-dilution GL leg red-first at the gate's exact 4.762 divergence; D-e predicate hardened to require the backing JOURNAL ENTRY — four arms; both judgment calls ruled); post-merge D-e 28/67, manifest 0 (parked 1134 recomputed), deptrac PASS; MIGRATION-BEARING (greenfield tables, dual-sha deploy note); dormant until F-3 wires the controller (B-8 grounds) |
| O-26 lineless phase-out | `fix/o26-lineless-posting-phaseout` | merged | dual gates r1 CHANGES ×2 → 9-item final round (both gates' premises partially corrected by the lane with evidence); post-merge refusal suites 8/44, manifest 0, types-drift clean; the six-path authoring audit refuted the old ticket's premise; NEW P1 ticket: the unvalidated auto-save route |
| dpa-v8 types regen seam | (dev direct) | `ea5b9dec5` | both v8 gates missed the rule-7 regen; deterministic additive-only, transform now a no-op |
| Membership offboarding / PIN revocation | `fix/membership-offboarding-pin-revocation` | `de14b65f7` (2026-08-23) | tenancy gate r1 CHANGES (F-1 broader than reported: zero-membership users passed verifyPin; vendor-symlink false-green trap documented) → fix round (F-1 company/Active filter on all 3 surfaces; cascade test MOVED into the LIVE security-regression lane; vacuous tests made discriminating) → r2 ACCEPT (merge materialized + checker run on the result). Closes the fired-employee-keeps-manager-PIN hole (sweep HIGH): deactivate/destroy cascade → Revoked memberships (spoof-proof reason rule), reactivation restores only cascade-revoked rows, Active+company belts at every PIN surface. **MIGRATION-BEARING** (`revoked_*` columns) + promotion checklist: F-8 per-tenant `SELECT status, count(pos_pin) FROM users GROUP BY status` spot-check. D-1 residual: offline device PIN cache unbounded (online window 1-5 min). Tickets: setPosPin scoping (owner ruling), orphaned PIN reservation. |
| B-6(ii) X/Z refund-VAT display (Option A) | `fix/b6ii-xz-refund-vat-display` | `ce59503a9` (2026-08-23) | dual gates r1 CHANGES (unreachable unreconciled branch found by BOTH lenses independently; blade sync-lag hole; legacy-blind EOD tile) → consolidated fix round + R-8 dev-merge → r2 ACCEPT (no-cry-wolf proven: sub-resolution wedges annihilated, negative wedge unreachable on device data by construction; CurrencyScaleResolver issue proven TEST-ONLY) → token-stream-proven doc micro (incl. a caught false-pass + negative control). Ships: net-VAT presentation on all X/Z/EOD surfaces, derived refund-VAT disclosure reconciling with the declaration BY CONSTRUCTION (same source + predicate as G-4), server legacy netting, Z↔declaration identity test, zReports/terminal i18n deep-merges (conflict union-resolved at merge, tsc clean, shadow test 4/4). ZERO signed-payload change — device halves ride D-1 with C-2/C-6; server halves land at promotion. Tickets: TND scale-2 test fixtures (8 files), printed-ticket VAT, blade raw-i18n guard gap. |
| Opening-batch lifecycle hardening | `fix/opening-batch-lifecycle-hardening` | `aad47bb7f` (2026-08-23) | treasury gate r1 ACCEPT outright (deviation attacked on four fronts and survived: the brief's blanket unique predicate was proven ILLEGAL on legitimate data — PG rejected it live — and the shipped `OB-%`-scoped predicate's exclusivity was prefix-censused; migration proofs re-executed; 12/12 PG). Closes both sweep HIGHs on tenant #1's first act: double-post race (fresh-row lock in all three postBatch txns + claim-style conditional UPDATEs + partial unique backstop) and LOCKED-batch re-validation (editability guards, Posted excluded from selection AND write path, hash-byte-identical pin). **MIGRATION-BEARING + S-16-STYLE PRE-PROMOTION CENSUS OBLIGATION**: before the promotion carrying `2026_08_23_000100`, run the duplicate-OB query against EVERY tenant DB (query in the migration docblock); non-empty = accounting correction first, never index weakening. |
| P0 TN matricule regex convergence | `fix/p0-tn-matricule-regex-convergence` | `929de7b34` (2026-08-23) | fiscal gate r1 CHANGES (device guard never executed — jsdom URL idiom; country-fallback bricked null-country partner edits, proven over HTTP) → fix round → r2 CHANGES (undisclosed lane-caused red: a fiscal must-reject fixture was exactly the newly-legal 3-letter arm) → r3 parent-verified (151/319 on the seal gate's own suite, sqlite AND PG throwaway). Ships: single-source TN pattern (strict superset of sealed bytes — 40k witnesses + 400k fuzz at the gate), byte-identical-after-normalization grandfather on updates, device mirror widened with cross-language literal guards both sides, company-country fallback closing the self-disabling hole. Residual ticket: `2026-08-23-company-tax-id-no-entry-validation.md` (seller matricule + 3 more). Manifest raise Partner 20→21 in admin commit `c52b53a57`. Unblocks the B2B ACCOUNT_CHARGE lane + the party/contact facture escalation (spec Phase 0 DONE). |
| B-3 pos_enabled claim enforcement | `fix/b3-pos-enabled-claim-enforcement` | `457457911` (2026-08-23) | tenancy gate r1 CHANGES (virtual_admin backfill poisoning — irreversible warehouse enable; FE-invisible refusal) → fix round → r2 CHANGES (AR i18n ratchet + shallow-spread trap) → r3 all items probed, parent-verified (i18n gate exit 0, FE 4/4, PG 17/17); post-merge 29/76. Ships: 422 LOCATION_POS_DISABLED at all four acquisition paths (fail-closed), provisioning flip (5 writers), evidence-based MIGRATION-BEARING backfill (till-terminal types only), FE surfacing + AR keys with deep-merge. **B-3 STAYS OPEN: shift-open half is D-1 device-build-gated** — a device claimed while enabled keeps selling after disable. ci.yml allowlist tokens → S-14 leg at promotion. Residuals in `1c69b70a1` commit body (device picker cosmetic; web-terminal asymmetry; arPos.zReports shadow ticketed to X/Z lane). |
| F4+F6 fold-now (state-machine triage) | `fix/sm-foldnow-f4-f6` | `c106fb809` (2026-08-23) | parent-gated micro-lane per triage §5: F4 partial CONCURRENTLY index on documents.source_document_id (r2f4 hot path, planner-verified both predicate shapes; FK deferred to C-8 lane) + F6 five tracked .bak migrations deleted (inertness proven on the Migrator glob; triage errata: two of five had LIVE bodies, extension-only inertness) + MANIFEST RECONCILIATION: 4 post-fa807a699 classes attributed (R-10 ×2, R-8 ×1, P1 ×1) + B-3's 2 union-resolved at merge → checker EXIT=0 on dev (was red — merge-discipline catch-up; checker now in the standing post-merge protocol). |
| P1 auto-save route hardening | `fix/p1-autosave-route-hardening` | `81e0948b9` (2026-08-23) | dual gates (authz+fiscal) r1 CHANGES ×2 → consolidated fix round → r2 CHANGES (spoofed-type critical caught by re-gate) → r3 ACCEPT (gate re-ran the spoof with attacker lines; leniency arm proven HTTP-unreachable) + docs micro; post-merge 32/131 green. Ships: can:documents.update + per-type create map bound to PERSISTED type, draft-only guard under lockForUpdate, AutoSaveDraftRequest validation, company currency into the fiscal hash (was EUR default), sequence-burn pin, cross-tenant + two-POST characterisations. Residuals ticket: 2026-08-23-autosave-residuals.md (R-1 live line-id defect [own lane queued], R-10 Document.php annotation lie/11 latent errors, R-11 .create-vs-.update divergence ruling). |
| R-8 shift-variance queue (P3 residual) | `fix/r8-shift-variance-queue` | `62fefcf16` (2026-08-23) | treasury gate 3 rounds (r1 CHANGES: producer census + tenancy-on-failed verified in vendor; r2 CHANGES: F-1 reporter reach, F-2 real-scanner guard, F-3 all-or-nothing truth + ordering pins — control proven to discriminate by tamper; r3 ACCEPT with every tamper re-executed) + doc-only micro `f9dfdfab7` parent-verified comment-only; post-merge 21/108 green. Ships: ShouldQueue + retry/backoff + dead-letter (skipped+dead_lettered audit rows), CashCountDispatcher guarding both producers (Redis fault can no longer 500 a sealed close), flag-skew audit row, 2 new architecture guards (Horizon property scan + queued-listener tenancy policing). Inert until `treasury.shift_variance_gl_enabled` flips (G-5); dead-letter surfaces have zero consumers = G-5 checklist item. |
| C-7 projection-test determinism | `fix/c7-poscore-projection-test-infra` | `0e06e1451` (2026-08-23) | fiscal gate r1 ACCEPT (assertion-strength closed arithmetically 140+26+7=173; census honesty independently swept) + micro (F-1/F-2/F-5 + residuals ticket); post-merge 31/173 green. Root cause: `connectionsToTransact()→[]` committed-state sibling + TenantFactory slug collision. **Directory-level PG counts for tests/Feature/Fiscal are now USABLE as a gate: 53F/3S/798P, byte-identical across 3 runs.** Residuals ticket: `2026-08-23-c7-residuals-committed-state-siblings.md` (tearDown cure, 7 sibling files, TenantFactory global fix). |
| B-2/B-6(i) money-semantics closeout | `fix/b2-b6i-money-semantics-closeout` | `076df8e0e` (2026-08-23) | treasury gate r1 CHANGES (code verified SOUND — all guard conjuncts mutation-reproduced, the pre-guard misuse proven previously accepted end-to-end with a 201, R-8 orthogonality read from both worktrees; findings doc-provenance only) → doc-only fix round `ef61b4027`, parent-verified comment-only via php -w byte-compare → merged; 36/227 green on MERGED dev. Ships: opening-float runbook (B-2 still UNRULED — labeled truthfully), REPOSITORY_NOT_SEEDED virgin-till guard (all reason codes, shift-variance carve-out), B-6(i) count-semantics pins. |
| **[Session B]** Q-3 loyalty redemption | `fix/sb-q3-loyalty-redemption-lock` | `faa39d7ef` (2026-08-23) | treasury gate r1 CHANGES (F-2 blocking: resurrected endpoint would debit points and deliver nothing) → fix round → r2 **ACCEPT** (all blockers verified by execution; PG 35/35 0-skips). Ships: locked read + conditional debit (1-row assert, typed 422), EnrollmentStatus guard, idempotency capability (additive `redemption_key` partial unique, MIGRATION-BEARING additive-safe/self-censusing), driver-aware 23505 replay, **endpoint gated 501 LOYALTY_REDEMPTION_NOT_WIRED until the wiring lane (LEDGER C-11)**. P0 side-find fixed in-place: redeem endpoint DEAD since birth (created_at NOT NULL 500) — the audited double-spend was never live; this merge ARMS a dormant path, hardened. Post-merge: 21 green on merged dev, manifest union gated 1146 EXIT=0. |
| **[Session B]** Q-4 coupon cap | `fix/sb-q4-coupon-cap-enforcement` | `6d601a33f` (2026-08-23) | treasury gate r1 CHANGES (Exhausted-seal proven always-rolled-back; 2 untested branches) → fix round `52e656136` → r2 **ACCEPT** (refusal-return + post-commit throw airtight on every exit — gate walked all six; sabotage-probe 25P02 proves the savepoint; unrelated-unique-index probe proves the narrowed classifier). Ships: recordUsage under txn+lock w/ replay idempotency (savepoint-isolated 23505), counter re-check under lock + durable Exhausted seal, validate-time refusal-message fix, MIGRATION-BEARING dual per-receipt unique (dual-table preflight census, abort names both offenders). RECORD CORRECTION: sub-report HIGH premise overstated — `Coupon::isValid()` already refused a capped coupon at validate time; the new enforcement = under-lock re-check + DB index. Owes: LEDGER C-12 (promotion recordUsage unhardened behind the new index; owner ruling record-and-flag vs refuse before wiring). Post-merge: 54 tests green, manifest union gated 1147 EXIT=0. Coupon lane parked ⇒ evidence local-only (S-17). |
| **[Session B]** Q-1 import execute re-arm | `fix/sb-q1-import-reexecution-guard` | merged (2026-08-23, post-`8a5bcc513`) | imports gate r1 **ACCEPT** outright (revert-probe red-proof 6/7→7/7; gate re-ran PG leg on throwaway 5433 — lane 7/7, and proved the 2 remaining Feature/Import PG reds PRE-EXIST on base = LEDGER C-10). Ships: typed 422 `IMPORT_NOT_EXECUTABLE` precondition, `Pending`-write moved BEFORE dispatch on both paths, tenant-scoped conditional worker claim (0 rows ⇒ return), `getValidRows()` +`is_imported=false`. NO migration. Gate attribution note: load-bearing fix = clobbered-`Pending` closure (audit #23's `canBeExecuted()` premise was stale — guard `canStart()` already existed). **#23 closes PARTIAL**: `updateOptions` re-arm + sync-twin claim = LEDGER **C-9**. Post-merge: lane 7/7 on merged dev; manifest reconciled to EXIT=0 (Import 16→17 Q-1; POS 147→149 ground-truth attributed to A's b6ii pair landed without raise; gated 1142→1145). |
| R-10 catch narrowing (P3 residual) | `fix/r10-catch-narrowing` | `d0e8b3cd9` (2026-08-23) | treasury gate r1 CHANGES (all 8 behavioral claims verified TRUE, incl. framework-source proof of the afterCommit nesting; findings doc-only) → fix round `38e4b376c` proven comment-only in production (+1 pin test) → parent re-gate (diff filter + 8/26 green) → merged; 8/26 re-green on MERGED dev. **P3-3 delta of record (sealed census untouched): M1-census row 7's "reachable" label for `DocumentConversionController:418` is REFUTED — the method has no route and the converter path cannot arm fail-closed GR-IR; both R-10 narrow arms now CLOSED; R-10's open tail = the broad sites (`RefundController`, `MultiPaymentController`, `PaymentRefundController`, `TenantScopedCommand`), not dispatched.** | 
| **[Session A]** N-5 has-pins parity (P1, campaign) | `fix/campaign-n5-haspins` | `d1dfa30cd` (2026-08-24) + manifest raise `e70dcaad4` | tenancy-authz gate r1 **ACCEPT** outright (`docs/superpowers/reviews/2026-08-24-n5-haspins-gate-r1.md`): `hasPins`/`pinData`/`verifyPin` share one `pinHolders(tenant, company)` population (active user + active membership + `pos_pin NOT NULL`); three tamper probes prove the tests discriminate; no other consumer on the old population. NO migration. Post-merge on merged dev: `PosAuth{HasPins,VerifyPin,SyncPins}Test` 20/20 by path; manifest POS 149→150 / gated 1147→1148 deliberate raise, EXIT=0. Residuals → LEDGER **C-13**. |
| **[Session B]** Q-2 counting finalize lock | `fix/sb-q2-counting-finalize-lock` | `850e86217` (2026-08-24) | inventory-costing gate r1 CHANGES (`2026-08-23-sb-q2-counting-gate-r1.md`: submitCount/triggerThirdCount/cancel terminal-state probes A/B/C) → fix round `600ccbc28` + docblock `NEW-2` → r2 **ACCEPT-w/-conditions** (`2026-08-24-sb-q2-counting-gate-r2.md`; both conditions discharged at merge). Ships: `finalize`/`submitCount`/`triggerThirdCount`/`cancel` re-read the counting header FOR UPDATE + re-assert (Finalized/Cancelled) before any write; tolerant forward-phase no-op on the last-item race; typed `CountingTransitionException` → 422 `COUNTING_TRANSITION_REFUSED` (bootstrap handler above the generic renderer); **MIGRATION-BEARING** `2026_08_23_120000_unique_stock_movements_counting_apply` = partial unique on `stock_movements` for `inventory_counting` applies (pre-flight duplicate scan in docblock; local census 0 dup grains AND 0 counting movements across 11 tenant DBs — vacuous; staging census = promotion obligation LEDGER C-14; index built without CONCURRENTLY → maintenance window). Post-merge on merged dev: 3 lane classes by path sqlite 9 pass/4 PG-only skips (gate ran PG 13/13 green on 5433); manifest union Inventory 108→111 / POS 150 (dev verbatim) / gated 1148→**1151** EXIT=0 on dev. CI-UNVERIFIED (S-17). Residuals → LEDGER C-14. |
| **[Session B]** Q-6 voided-receipt trigger | `fix/sb-q6-voided-receipt-trigger` | `681c7bf29` (2026-08-24) | fiscal-pos gate r1 CHANGES (`2026-08-23-sb-q6-trigger-gate-r1.md`: F-1 Critical fiscalized→pending_seal FK-cleanup bypass FOLD-RULED; F-2 keep-freeze parent-RULED) → fix round `c85875c73` → r2 **ACCEPT-w/-conditions** (`2026-08-24-sb-q6-trigger-gate-r2.md`; F-1/F-2/F-4 re-proven closed by probe on old vs new body). Ships (migration-only): `2026_08_23_160000_…pos_receipt_immutability_trigger_whitelist` CREATE OR REPLACE `prevent_receipt_modification()` as named-transition whitelist with `ELSE RAISE`; `voided`/`pending_sync`/`synced`/`sync_failed` frozen (strict no-op via `to_jsonb − keys`); FK-cleanup branch now requires same status + same is_voided; fiscalized backfill branch `to_jsonb − keys` (previous_hash etc. no longer rewritable); PG's own `ON DELETE SET NULL` cascade deliberately REFUSED on frozen rows (docblock + pinned). **MIGRATION-BEARING (S-16)**: function replace only, safe on any row count; census `SELECT fiscal_status, COUNT(*) FROM pos_receipts WHERE fiscal_status IN ('voided','pending_sync','synced','sync_failed') GROUP BY 1` = 0 rows on all 11 local DBs; staging census = LEDGER C-15. Parent ruling at merge: F-7 → `PosReceiptFrozenStateImmutabilityTriggerTest` named in the `backend-test-pgsql` `--filter` allowlist (`ci.yml`, B-3/Q-7 precedent — allowlist append only; S-14 dispatch leg applies to the promotion). Post-merge on merged dev: lane class **41/41** on PG 5433 throwaway; manifest union Fiscal 79→80 / gated 1151→**1152** EXIT=0. CI-UNVERIFIED (S-17). Residuals F-3/F-5/F-6/F-8/F-9 → LEDGER C-15. |
| **[Session B]** Q-8 held-order recall | `fix/sb-q8-held-order-recall` | `5de9affe2` (2026-08-24) | fiscal-pos gate r1 **ACCEPT-w/-conditions** (`2026-08-24-sb-q8-held-orders-gate-r1.md`: race safety PROVEN by real two-session PG contention; typed 409 was unreachable on PG because the loser's FOR UPDATE re-read sees `recalled` under EvalPlanQual → 422; the claimed Tauri `holdApi.ts` residual was a PHANTOM — zero importers, device parks carts in local SQLite; terminal-scope audit item NOT closed because the live web client sends no `terminal_id`) → fix round `872e13662` (one-line raise-site change pre-sanctioned by the gate, parent-verified by diff; new PG race test against a REAL second connection RED→GREEN; `down()` symmetric guards; census sweep `LIKE 'tenant%'` — tenant DBs have no separator, `tenant\_%` matches 0). Ships: `recallOrder` in txn + FOR UPDATE + conditional UPDATE, 409 `HELD_ORDER_RECALL_CONFLICT` at BOTH raise sites, 422 `RECALL_FAILED` only for TTL-lapsed baskets; `terminal_id` optional on the wire (backward-compat); `discardOrder` soft-delete + `discarded_by`, refuses Recalled; **MIGRATION-BEARING** `2026_08_23_163000_harden_pos_held_orders_status_and_discard` (pgsql CHECK from `HeldOrderStatus::cases()`, pre-flight THROWS before DDL per `2026_08_11_000100` precedent; local census 10/10 tenant DBs clean; staging census = LEDGER C-16). API-contract note: stale UI recalling a consumed basket now gets 409 (was 422) — web client does no status branching (grep-verified). Post-merge on merged dev: 4 lane files **58/58 on PG 5433** throwaway; manifest union POS 150→152 / gated 1153→**1155** EXIT=0 (⚠ Session A: N-1 r3's "1157 at merge" was computed on 1153 — recompute on 1155). CI-UNVERIFIED (S-17). Residuals → LEDGER C-16. |
| **[Session B]** Q-7 terminal-claim hardening | `fix/sb-q7-terminal-claim-hardening` | `43a2dad14` (2026-08-24) | DUAL GATE: tenancy-authz r1 **ACCEPT-w/-cond** (`2026-08-24-sb-q7-terminal-gate-r1-tenancy.md`: rule-12 route, `pos.manage_terminals` already seeded+granted, every query company-scoped, cross-company 404 probed; C-1 manifest union, C-2 deploy grep BLOCKED+FAILED, C-3 residuals) · fiscal-pos r1 **CHANGES** (`…-r1-fiscal.md`: CRITICAL `release()` with an OPEN shift strands the replacement device — SESSION_OPEN silently dropped at `ZSessionLifecycleProjection:146-153`, no server surface can close a v3 orphan) → fix round `ff2bcfd81`+`f1d34ec99` → fiscal r2 **ACCEPT-w/-cond** (`…-r2-fiscal.md`: closed by independent probe; force truthy variants cannot bypass the mandatory reason; rulings: no-op-before-guard ordering correct; 409 on `TERMINAL_HAS_OPEN_SHIFT` accepted vs the 422 siblings). Ships: `claim()` conditional UPDATE `WHERE hardware_identifier IS NULL` → 409 `TERMINAL_ALREADY_CLAIMED` / 23505 → 409 `DEVICE_ALREADY_BOUND`; same guard in `requestTerminal()`; `findByDevice()` ordered `created_at,id`; NEW `POST /pos/terminals/{id}/release` (`pos.manage_terminals`; refuses on OPEN shift with 409 + `open_shift_id` unless `force=true`+`reason`; clears ONLY `hardware_identifier` — chain columns pinned byte-identical); `TerminalClaimed`/`TerminalReleased` audit events (audit register only — NOT in the NF525 JET whitelist, owner question O-30); FormRequests `max:100`. **MIGRATION-BEARING** `2026_08_23_140000_harden_pos_terminals_identity_and_lifecycle` — 3 legs (partial unique on `(tenant_id,company_id,hardware_identifier)` live rows; `type` CHECK; `is_active`/`deactivated_*` coherence CHECK), each with pre-flight scan; a BLOCKED **or FAILED** leg is logged and permanent-until-manual → LEDGER S-19 (grep both statuses on token `POS TERMINAL IDENTITY/LIFECYCLE HARDENING:`); local census 10/10 tenant DBs clean on all three legs. `ci.yml`: both classes appended to the backend-pgsql `--filter` allowlist (B-3 precedent; S-14 leg applies). Post-merge on merged dev: 7 lane files sqlite 56 pass / 9 PG-only skips (PG leg deferred — Session A test run live; r2 gate ran PG 65/65 on the identical tree); manifest union POS 153→**155** / gated 1160→**1162** EXIT=0. CI-UNVERIFIED (S-17). **OWNER: O-30 (Q7-OWES-1 orphaned-shift runbook — non-waivable before the first forced release in prod)**; residuals → LEDGER C-17. |
| **[Session B]** Q-5 voucher void consolidation (#22/#24) | `fix/sb-q5-voucher-void-service` | `b851db749` (2026-08-24) | DUAL GATE r1: treasury **ACCEPT-w/-cond** (`2026-08-23-sb-q5-voucher-gate-r1-treasury.md`: #22 proven red by revert-probe; GL reversal = true source-keyed mirror; ledger immutability live; R1 auto-fraud alert-only ACCEPTED; R2 cascade fails-closed on Expired ACCEPTED) · fiscal **ACCEPT-w/-cond** (`…-r1-fiscal.md`: sealed payload forward-only-safe; Nf525 `is_voided` asymmetry pinned on the future ReceiptVoided-emitter lane) → consolidated micro-round `12ee4c81d` (T-F1 honest `VOIDABLE_STATUSES` = effectively `{Issued}`, T-F4/T-F5 docblocks, T-F2 #22 money-hole census in migration docblock, F-F3 cascade `receipt_id`/`terminal_id` pin, F-F4 `/vouchers/{id}/void` hard-blocked under impersonation + route-table test, F-6/7/8 folded) parent-verified by diff (all hunks = sanctioned items; mutation-probed reds for F-F4/T-F1/F-F3). Ships: `VoucherVoidService` = the single void write path (lock + status precondition + GL reversal JE via `createVoucherLedgerEntry` + idempotency); controller + `VoucherLookupService` cascade both route through it; **MIGRATION-BEARING** `2026_08_23_150000_unique_voucher_ledger_voided_per_voucher` (partial unique, hard-aborts on duplicates — trigger blocks delete-based cleanup). Local censuses on 11 tenant DBs: duplicate census 0 rows, #22 money-hole census `0 | NULL` — **vacuous** (every local `voucher_ledger` is empty); staging census = LEDGER C-18. API-contract note: FullyRedeemed void now `VOUCHER_NOT_VOIDABLE` (was `VOUCHER_ALREADY_TERMINAL`); no FE branches on voucher codes. Post-merge on merged dev: 6 lane files sqlite **70 pass / 5 PG-only skips** (finisher ran PG 75/75 on the lane tree); manifest Voucher 10→11 / gated 1162→**1163** EXIT=0. CI-UNVERIFIED (S-17). Residuals + program tickets → LEDGER C-18. |
| **[Session B]** Q-9 F1 kitchen/order gating + SM-1 guard | `fix/sb-q9-kitchen-order-gating` | `5e4a0af3e` (2026-08-24) | DUAL GATE: tenancy-authz r1 **CHANGES** (`2026-08-24-sb-q9-kitchen-gate-r1-tenancy.md`: rule-12 both layers verified on all 12 live routes via `route:list`; Menu-vs-Tables confirmed from `config/verticals.php` and probed live (parapharmacy 403×12, coffee_shop 200); C-1 = isolation-suite fixture `Restaurant`+`Loyalty` product-invalid) → parent-applied prescribed fix `7cc3842d0` (`CoffeeShop`, product-valid strict superset) → fiscal-pos r1 **APPROVED** (`…-r1-fiscal.md`: C-1 discharge recomputed; SM-1 exploit walked over HTTP on sqlite AND PG — create→line→send→cancel→PATCH ready = 422, no OrderReady, fiscal_events/pos_receipts/stock_movements 0→0; guard inside the txn after the scoped lock; Tauri device calls NONE of the gated routes). Ships: `module:Menu` on `routes_kitchen.php` + the live order routes in `routes_orders.php` (410 `/close` tombstone deliberately ungated); `updateLineStatus` refuses non-active orders (→422), `checkAndTransitionOrderToReady` only when active and not Ready; web Orders route `ModuleGuard module="Menu"` + Sidebar `module: 'Menu'` + `routes-web.yaml` regenerated (one line); NEW `Security/PosOrderKitchenModuleAccessControlTest` in the LIVE `security-regression` lane (no ceiling); four existing suites re-fixtured onto F&B verticals (retail tenants now 403 by design). NO migration. Deploy note: non-F&B tenants lose `/pos/orders*` + `/pos/kitchen*` (intended — structurally unreachable for parapharmacy). Post-merge on merged dev: backend 3 files 37 pass/1 skip; FE ratchets in-lane after dev merge: vitest routes 22/22 + Sidebar 50/50, typecheck 0, ESLint 0 errors, TanStack audit 0 new, route-manifest drift 0; manifest EXIT=0 (no ceiling moved). CI-UNVERIFIED (S-17). Residuals/tickets → LEDGER C-19. |
| **[Session B]** Q-13 `module:Tables` backend gate + device pull guard (owner go-ahead 2026-08-24) | `fix/sb-q13-tables-module-gate` | `0a3b629ff` (2026-08-24) | tenancy-authz gate r1 **APPROVED** outright (`2026-08-24-sb-q13-tables-gate-r1-tenancy.md`: `route:list` shows `RequireModule:Tables` on all 10 floors/tables routes and no second ungated door; FE already gated on `Tables` (`routes/index.tsx:2991`, `Sidebar.tsx:237`) so rule 12 is now both-layers; coffee_shop fixture reconcile-stable by construction; both re-fixtured suites audited hunk-by-hunk, zero assertions weakened; device skip rulings: fail-open on null config correct, stale-config acceptable (cache is not a security boundary), `success`-log honest — `sync_log` has zero production readers and `tablesPulled` no consumer). Ships: `routes_tables.php:25` `'module:Tables'` on the group (no tombstone in the file); `apps/pos/src/lib/sync/syncService.ts::pullTables()` skips the `/pos/floors` pull when `hasModule(config,'Tables')` is false (Menu-gate idiom) so retail devices no longer log a per-cycle error; NEW `Security/PosTablesModuleAccessControlTest` (5) in the live `security-regression` lane; NEW pos vitest `syncService.pullTablesModuleGate.test.ts` (3). NO migration, NO new permission, no seeder re-sync. Deploy note: retail/parapharmacy tenants lose `/pos/floors*`+`/pos/tables*` (intended). Post-merge on merged dev: backend 3 files by path **24 passed (102 assertions)**, pos vitest 3 files 88/88, manifest EXIT=0 (no ceiling moved), route-manifest drift EXIT=0. CI-UNVERIFIED (S-17). Residuals → LEDGER C-20. Dhouha's unmerged tables chain (#201–206) will see a one-line conflict on the route group. |
| **[Session B]** Q-10 fiscal-period quick fixes | `fix/sb-q10-fiscal-period-quickfixes` | `f5cae1f12` (2026-08-24) | treasury gate r1 **ACCEPT-w/-cond** (`2026-08-24-sb-q10-fiscal-periods-gate-r1.md`: country resolution honest — reads `company.country_code`, unregistered country SKIPPED+logged, never TN-defaulted; I-1 the nightly job re-closed a human-reopened period) → PARENT RULING C-2: reopened periods are excluded from auto-close → fix round `8a285c495` (red run proved STEP 3 previously LOCKED a reopened period — terminal; all three scheduler arms now exclude reopened-Open periods; typed refusal codes `FISCAL_PERIOD_REOPEN_{LOCKED,NOT_CLOSED,FISCAL_YEAR_CLOSED,SUCCESSOR_SETTLED}` in the house envelope; `whereUuid`) → treasury r2 **ACCEPT** (`…-gate-r2.md`). Ships: `FiscalPeriodAutoLockService` per-company country rules with fail-safe skip + per-row audit stamps (`status_actor='system:auto-lock'`, `status_changed_from`, `closed_at/locked_at`) replacing three blind bulk updates; NEW `POST /api/v1/fiscal-periods/{id}/reopen` (`FiscalPeriodReopenService`: Closed→Open only; Locked terminal; refuses inside a closed fiscal year or with a Closed/Locked successor; same-company only) behind NEW permission `fiscal-periods.reopen` (accountant + admin; manager excluded per 2026-08-06 I-1 precedent) — FE permissions map regenerated (1 line). **MIGRATION-BEARING (additive-only)** `2026_08_24_140000_add_fiscal_period_transition_audit_columns` — 7 nullable columns, cannot fail on existing rows; no census. Deploy: `tenants:migrate` → per-tenant `RolesAndPermissionsSeeder` (idempotent) → permission cache reset. Post-merge on merged dev: 4 lane files by path **41 passed (130 assertions)** on sqlite (r2 gate ran the same set on PG 5433 green); manifest union Company 30→31 / gated 1163→**1164** EXIT=0; `FiscalPeriodReopenEndpointTest` appended to the backend-pgsql allowlist (`ci.yml:969`, C-3, B-3 precedent — S-14 leg applies). CI-UNVERIFIED (S-17). Residuals → LEDGER C-24. |
| **[Session A]** N-2 day-one SO/DN confirm 404 (P0, campaign) | `fix/campaign-n2-stocklevel` | `e1d21958d` (2026-08-24) | inventory-costing gate r1 **ACCEPT-w/-conditions** (`docs/superpowers/reviews/2026-08-24-n2-stocklevel-gate-r1-inventory.md`: WAC chain/lock order/movement sign byte-identical; exhaustive caller enumeration = no new 500; red-proof 5/5) → fix round `7c5b92765` (batch-tracked coverage red-proven 404/500, i18n refusal EN+FR at unit precision via `QuantityScale::formatForUnit`, handback claims corrected, structural no-phantom-row pin) → parent-verified. Ships: absent `stock_levels` row ⇒ typed 422 `INSUFFICIENT_STOCK` (matches pre-existing zero-row policy, no phantom row) on SalesOrder/DeliveryNote confirm + `create-delivery-and-post` / `confirm-deliveries-and-post`; existing-row-at-zero 500 fixed. NO migration. Post-merge on merged dev: lane 8/49 green; manifest Document 78 / gated 1153 EXIT=0. Residuals → LEDGER **C-17**. |
| **[Session C]** lifecycle-dimensions program — C-P0 purchases research + C-R0 returns research (READ-ONLY sweeps, no branch) | none | in flight (2026-08-24) | announced per HANDOVER §3; impl lanes C-2/C-3/C-P/C-R branch only after N-6 (Session A) merges and, for C-P, after Session B Q-11 merges; SoT `docs/sessions/session-C-lifecycle-2026-08-24/SESSION-LOG.md` |
| **[Session C]** lifecycle-dimensions program — SPEC+PLAN r11 | per-lane branches `feat/sc-*` | **WHOLE PROGRAM ACCEPT-WITH-CONDITIONS + critical-path slice ACCEPT** 2026-08-25 (Codex spec+plan r11 `de2fa0741`; 11 rounds, 167 findings, 5 conditions applied) | first lane C-0a0 dispatches the moment N-6 (Session A) merges; briefs in `docs/sessions/session-C-lifecycle-2026-08-24/BRIEF-*.md`; owner sheet `OWNER-DECISIONS-lifecycle-dimensions-2026-08-24.md` OQ-11..73 — rulings OQ-15/38/46/47/48 wanted before C-QR0b |
| **[Session A]** N-1 silent wrong VAT (P0, campaign) | `fix/campaign-n1-vat-resolution` | `77610a8bb` (2026-08-24) [MIGRATION-BEARING `2026_08_24_100000_backfill_products_tax_rate_from_tax_configuration_n1`] | fiscal-pos r1 CHANGES (autosave persisted 0 %) → fix → r2 CHANGES (type-blind unposted-line repair would rewrite draft credit notes/supplier invoices; deptrac Domain→Application; echoed rate still won) + treasury r1 CHANGES (same repair: stale recoverable_tax_amount → unpostable AP, stale balance_due, stale document_tax_details) → fix round r2 WITHDREW the document-line write → r3 **ACCEPT-w/-conditions** (byte-identical mixed PG fixture for documents/lines/tax_details/allocations; guards non-vacuous 8/22; deptrac PASS; precedence tamper-proven both paths) → conditions closed `365012401`. Ships: `TaxResolutionService::resolveRateFromTaxConfiguration()` single derivation site wired into product store+update (foreign/non-percentage config 422); `DocumentForm` sends `tax_configuration_id`; `AutoSaveDraftRequest` accepts it and `DraftController::resolveLineTaxRates()` resolves via `DocumentLineTaxResolver` — configuration outranks a client-echoed `tax_rate` on autosave AND create; POS seals the configured rate. Migration: products-only backfill (campaign tenant: 2 rows SERU-PHY20 19→7, LAIT-INF400 19→13) + report-only worklist of unposted docs whose lines carry the stale rate (campaign: QT-2026-0002/3/4 + SO-2026-0001, 7 lines — operator re-picks the product; confirmed invoices need `vat:backfill-tax-details --apply`). Post-merge on merged dev: 42/208 by path; deptrac PASS; manifest Document 79 / POS 153 / Product 57 / gated 1159 EXIT=0. Residuals → LEDGER **C-21**. **DEPLOY CHECKLIST**: after `tenants:migrate`, grep the migration log for `status=FAILED` and for the `worklist` lines per tenant; hand the worklist to the operator. |
| **[Session A]** N-3/N-4/N-7 FE↔API contract crashes (P1, campaign) | `fix/campaign-fe-contract` | `9bc55cef2` (2026-08-24) | frontend-conventions gate r1 **ACCEPT-w/-conditions** (`docs/superpowers/reviews/2026-08-24-fe-contract-gate-r1.md`: both crashes fixed + live-verified on the campaign tenant; preview contract test pins drift both directions) → fix round `2d187ba95` (F-1 OPEN-branch contract now seeds a sealed invoice + tax details and pins both branches key-for-key, tamper 2F/4; F-2 loading/error/retry from both queries; F-5 currency formatting) → parent-verified. Ships: `PostPreview` discriminated union + additive `batch_type`; `BatchPreview` reads `entry.entry_date`; ACCOUNTING footer + Post-step fixes; `useVatPeriod` (tenantScopedKey) header; API fix: CLOSED/FILED VAT snapshot emits `tax_configuration_id`; N-4b: TN stamp-duty/retenue panel gated on `period.country_code` (was rendering for NO tenant); 71 opening-balances i18n keys en/fr/ar. NO migration. Post-merge on merged dev: contract tests 8/54; `pnpm lint` 6448 ≤ 6449; manifest Taxation 32 / gated 1160 EXIT=0. Rule-7 deviation (no DTO family, contract tests instead) accepted for P1 — DTO lane = LEDGER **C-19**. |
| **[Session A]** W2-3 import drops categories (P1, wave 2) | `fix/campaign-w23-import-category` | merged 2026-08-24 (see `git log`) | imports gate r1 **CHANGES** (`docs/superpowers/reviews/2026-08-24-w23-import-gate-r1.md`: ruling concurred — create-on-miss, company-scoped, brand precedent; CRITICAL recovery dead on PG inside the row tx; max:255; silent slug merges) → fix round `860f86ce8` (onlyTrashed pre-check + SAVEPOINT, max:255 + slug clamp, `category_matched_by_slug` warning) → parent-verified PG 7/103 + sqlite 7/103. Ships: `CategoryResolutionService` + `ProductServiceInterface::resolveCategoryByName` (rule-6 path), row warnings `category_created/restored/matched_by_slug` reaching the result workbook. NO migration. Post-merge: 7/103; manifest EXIT=0 (Import 17 unchanged); deptrac PASS. Residuals → LEDGER **C-23**. |

## D. Session log pointers
- Wave-1 lane branches: `fix/ar-locale-coverage`, `fix/fiscal-bytea-param-lob`,
  `fix/g3-training-receipt-containment`, `fix/g4-vat-pos-refund-netting`,
  `codex/z-sale-branch-decomposition-2026-08-18` (worktrees under `.worktrees/`).
- Queued next slots: POS mocked Shift/Reports screens (onboarding-audit G3 — fake money on live
  manager routes, confirmed at `apps/pos/src/pages/ShiftClosurePage.tsx:9-21` and
  `ReportsPage.tsx:7-18`); onboarding-hygiene lane (G1 guard, G11 trial-plan warning, G9 dead import
  links); setup-checklist expansion (G5); R-8/R-10 catch-site lanes.
