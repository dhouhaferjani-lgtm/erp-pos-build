# Codex plan gate r3 prompt — RBAC wave 1 plan rev 3 (2026-09-10)

You are the adversarial PLAN gate (round 3) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox; artisan cannot boot here — use the shared checkout /Users/houssamr/Projects/syneriva/apps/erp read-only). Declare `git rev-parse --short HEAD`. Review ONLY docs/superpowers/plans/2026-09-10-rbac-wave-1.md (REV 3, 10408 lines). It implements the ACCEPTED spec docs/superpowers/plans/../specs/2026-09-10-roles-permissions-catalogue-design.md (rev 9.1: rev 9 gate r9 ACCEPT + editorial amendment A-1 in its Amendments section; r9 register docs/superpowers/reviews/2026-09-10-rbac-spec-codex-gate-r9.md "Preserve" list binding). It consumes wave 0b REV 2 (docs/superpowers/plans/2026-09-10-rbac-wave-0b.md, its "Signature changes for wave 1" section; 0b is itself in a fix round for its gate r2 findings — see docs/superpowers/reviews/ once persisted / scratchpad rbac-plan-0b-gate-r2.out — do not re-report 0b defects here, but flag wave-1 assumptions that a 0b fix could invalidate).

FIRST read your round-1 register docs/superpowers/reviews/2026-09-10-rbac-plan-0b1-codex-gate-r1.md ("## Wave 1" + cross-plan + wave-1 citation rows) and produce a "Rev-1 closure table" (finding → CLOSED at rev-2 anchor / NOT CLOSED / REJECTED-correctly); verify the "Change log rev 1 → rev 2" against code. Rulings applied in rev 2 and SETTLED: catalogue 325 (304 dev + 2 W-LOT + 19 0b); complete 325-row manifest table + 38 manifest bodies + 38 enums; green-per-commit via shrink-only `permission-catalogue-unassigned.json` over nine commits; conditional template_version write; single commit Phase 1.10 landing sync + commands + SyncDryRunRollback + deletion of all eight 0b stopgap items; YAML-generated deploy table with exact-order test; recordTenantWide actor param; full implementations for migration (PG/SQLite split), nine-step sync, commands, entrypoint/health/provisioning, scaffold, PHPStan rules + AST evaluator, exporter, seeder shim, reapply action + route (`can:roles.manage`); `TemplateDeltaApplier::applyTo` in LOCK_INHERITED_FROM; `AuditEvent.companyId ?? ''` guard; six keys via `legacy()`; `LEGACY_ACTION_CEILING` 59; deprecation replacements are a provable no-op in wave 1 (all 22 `replacedBy: null`); four orphaned 0b tests handled. Known residuals the author declared (judge whether each blocks dispatch): ~45 test bodies named-not-pasted (three pasted); templateDefaults machine-derived from the post-0b grant map (spot-checked, totals admin 325 / gm 257 / manager 255 / accountant 87 / operator 58 / cashier 49 / viewer 34 / technician 14); PHPStan literal-baseline and label-gap counts deliberately left to be measured in the lane; class bodies precede test bodies in several tasks; SoD payment group includes payments.reverse.

Verify against CODE (dev tip and lanes via `git show`), never prose:
1. Every path:line — confirm; list wrong/stale.
2. The 325-row table: re-derive the key set (dev seeder + lane wrapper + the 19 0b keys as named in the 0b plan) and diff it against the table; spot-check ≥15 templateDefaults rows against the seeder grant map incl. accountant/cashier/viewer/technician; verify the 22 deprecations and 11 renames match the spec's authoritative lists; verify the six `legacy()` keys fail `RESOURCE_PATTERN` and no others do; verify the SoD baseline rows (18) against the seeder combinations for the 8 groups.
3. Manifest + enum code: compile-plausible PHP 8.4 (readonly, enums, strict types), PSR-4 paths, enum ⊆ manifest per module; `PermissionRegistry` aggregation via provider tagging — does each module's ServiceProvider exist at dev tip (cite) and is the tag registration shown?
4. Sync service: the nine steps in the settled order; adopt-before-write; canonical(S) normalisation; marker write only on general_manager creation with the lane's CHECK satisfied (cite lane migration lines); exact template diff for uncustomised roles; deprecation replacements; admin = activeKeys; after-commit cache reset while team id set; PermissionWriteLock first with the lane's key/order; dry-run write-free with failure exit; mutations vs gauges; three tenant types tests; zero-write idempotency by query-log count.
5. Fleet: `permissions:sync-fleet` via `TenantFleetRunner` (0b name), selector contract, readiness FAILED, status file, health reader (`HealthCheckService.permissions_sync`), entrypoint block replacement — safe on staging push?
6. Migration: columns/CHECK/unique(tenant_id, template_key) + partial unique NULL team; SQLite lane equivalents; down migration; no ALTER of enum-typed columns.
7. Static guards: PHPStan rule with injected `permission-registry-keys.json` (registration in phpstan.neon shown?), AST route-literal test with constant-expression evaluation, shrink-only baseline; label coverage test against apps/web/src/locales/{en,fr,ar}/common.json; generated FE map + TS union; `PermissionDeploySequenceTest` exact-order from YAML.
8. Commit hygiene: every commit green in isolation (esp. Phase 1.4a–1.4i and 1.10), `Phase 1.<task>:` subjects, exact `git add`/`git rm`, explicit PHPUnit (PG env, `-c phpunit-pgsql.xml`), PHPStan, Pint per task.
9. Residuals: which of the author's declared residuals are acceptable for DISPATCH-READY vs must be supplied?

Output a Markdown register: "# Codex plan gate r2 — RBAC wave 1 plan rev 2 (gpt-5.6-sol, high, read-only, 2026-09-10)", sections "## Rev-1 closure table / ## BLOCKER / ## MAJOR / ## MINOR / ## Citation audit / ## Rejected false positives / ## Preserve / ## Owner decisions required / ## Dispatch assessment", LAST LINE exactly `VERDICT: DISPATCH-READY` or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Print in full.

Round-3 additions: FIRST read your round-2 register docs/superpowers/reviews/2026-09-10-rbac-plan-w1-codex-gate-r2.md and produce a "Rev-2 closure table" (finding → CLOSED at rev-3 anchor / NOT CLOSED / REJECTED-correctly); verify the "Change log rev 2 → rev 3" against code. Rulings applied in rev 3 and SETTLED: `PermissionSyncService` split into plan() over a virtual state / execute(); `SyncRollback` thrown on every non-green outcome and every dry run; `verifyAdmin()` after execute with `admin_not_current`; `permissions:sync --tenant=` resolves by id or slug and initialises/ends Stancl tenancy; provisioning throws `TenantPermissionSyncFailed`; keys fixture generated in Phase 1.16; catalogue 327 (T2's two transfer keys granted to manager+general_manager); `LOCK_INHERITED_FROM` list shape with both callers; entrypoint runs dry-run first and nests apply in its success branch with a `blocked` status token; nine written-out `git add` blocks; 109 PHP blocks `php -l` clean. Wave 0b rev 3 (`692cede7a`) is the consumed producer — its gate r3 found a scanner false-positive defect (row-order visitor flags 23 non-roles chains) that 0b fix round 3 is repairing; flag wave-1 assumptions that depend on that repair but do not re-report it. Declared residuals to judge (acceptable for DISPATCH-READY or must-supply): ~30 test cases named with exact assertions + fixture setup in prose (ten bodies supplied incl. the four oracles); `SyncPlan` dual representation without a static guard; `ForbidRoleNameAuthorization` duplicating eight role names guarded by `SystemRoleTemplateAlignmentTest`; entrypoint dry-run doubling boot work under the soak flag; class-before-test ordering. Scrutinise: (a) plan()/execute() equivalence — can the executed state differ from the planned state under the shared lock (cite the divergence assertion); (b) `SyncRollback` semantics vs Laravel `DB::transaction` retry/afterCommit (cache reset must not fire on rollback); (c) tenancy init/end correctness incl. the case where tenancy was already initialised by the fleet runner; (d) re-derive the 327 set and the 1.4a–1.4i commit arithmetic; (e) `php -l` a sample of ≥10 blocks yourself; (f) the entrypoint nesting under `set -e`/`|| true` conventions of the existing script; (g) residual internal inconsistencies. If only MINOR findings remain, the verdict MUST be DISPATCH-READY (noting entry conditions) — then list the minors as an editorial list. Output heading: "# Codex plan gate r3 — RBAC wave 1 plan rev 3 (gpt-5.6-sol, high, read-only, 2026-09-10)".
