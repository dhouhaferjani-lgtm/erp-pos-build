# Codex Desktop dispatch — lane W-LOT-A-1a, fix round 3 (paste the absolute path of THIS file into a NEW thread named `W-LOT-A-1a fix round 3` and say "read this file and execute it")

> Self-contained: the previous threads are gone. Everything you need is in this file, the files it names, and the repo. Supersedes `CODEX-PROMPT-WLOTA-1a-fix-round-3-2026-09-10.md` (same content, same-thread form).

## Where you resume
- Repo `/Users/houssamr/Projects/syneriva/apps/erp`. **Existing worktree `.worktrees/w-lot-a-1a`, branch `lane/w-lot-a-1a`, HEAD `a7010fe4d`** (code tip `52f5ad796`, docs `a7010fe4d`). Do NOT create a new worktree; do NOT touch the main checkout, other worktrees, or `dev`. `git status` must be clean before you start.
- PostgreSQL lane DB **`autoerp_test_w`** on **port 5433** (Docker `autoerp_postgres`); set BOTH `DB_DATABASE` and `DB_CENTRAL_DATABASE`; env recipe in `docs/sessions/wlota1a/test-env.sh` if present, else the handback's "Resume and exact local commands". ONE PHPUnit file per invocation, never the full suite, never in parallel. Node 20.19.4 for web (`export PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH"`).
- Standing rules: never edit `docs/superpowers/plans/**` or `docs/superpowers/reviews/**` (orchestrator-owned; record plan-level decisions in the handback); no merge, no push, no Dokploy; end with the handback at `status: review`.

## What this lane is (one paragraph)
Slice W-LOT-A-1a adds lot (batch) action permissions behind `LOT_ACTION_PERMISSIONS_ENFORCE` (default false), a provisioned `general_manager` role delta, and web route/action gating for `/inventory/batches/*` and the Roles page. Three fix rounds are done and gated. Plan authority: **rev 12** `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-12.md` (read §000, §00, §0 in that order — they rule every item below; §6.1 lists the four declared flag-off exceptions; §10 is the five-push ledger with the web string-tolerance slice in Push 3). Handback (yours to extend): `docs/handoff/HANDBACK-WLOTA-1a-2026-09-09.md` (read "## Fix round 2", "## Fix round 2b", "## Commit groups and source-push ledger"). Gate register that produced this round: `docs/superpowers/reviews/2026-09-10-w-lot-a-1a-impl-gate-r3-tenancy.md` (read in full: every r2 blocker/major is VERIFIED closed — do not touch them).
- Owner rulings (not reopenable): `technician` is NOT granted `batches.view`; legacy NULL-team roles unchanged; Option B (string totals at Push 3). Orchestrator rulings in force: role-assignment membership requirement gated behind `activation->enforced()`; the pre-existing per-lot transfer 500 is OUT OF SCOPE (known-red: skipped unless `WLOTA1A_RUN_KNOWN_REDS=1` + rollback pin, ticket `docs/superpowers/tickets/2026-09-10-batch-transfer-missing-movement-reference.md`).

## Items (cite `path:line` at your final HEAD in a new "## Fix round 3" handback table)
**MAJOR M-1(r3) — the Push-3 Vitest depends on a Push-5 page.** `apps/web/src/features/batches/pages/__tests__/BatchPermissions.test.tsx` is in the Push-3 ledger row (web string-tolerance slice with `types.ts` and `BatchListPage.tsx`), but its four `BatchDetailPage` gating cases (`:54-71`) need this lane's `BatchDetailPage.tsx`, which ships in Push 5; against the pre-lane detail page three of them fail. Split: the four list cases (`:35-53`) stay in a Push-3 file `BatchListPermissions.test.tsx`; the four detail cases move to a Push-5 file `BatchDetailPermissions.test.tsx`. Prove in the handback (`grep import`) that the Push-3 file imports nothing outside `types.ts`, `BatchListPage.tsx` and shared libs. Re-reconcile the ledger (every non-doc lane file exactly once; now 76 rows) and name the Push-3 file in `docs/handoff/RELEASE-NOTES-WLOTA-1a-2026-09-10.md:16`.

**Minors.** N-2(r3): flag-off `BatchController::expiring()` passes the raw `location_id` query value to a UUID column (`?location_id=0` → PG 22P02 → 500; baseline ignored it) — guard with `Str::isUuid()` (ignore non-UUID like baseline); one test in `BatchExpiringLocationScopeTest` (flag off, `location_id=0` → 200). N-3(r3): the 2b rollback pin test (`BatchReadLocationScopeTest` ~`:235-268`) asserts PG SQLSTATE `23502` and fails on SQLite — add the `pgsql` driver skip (`markTestSkipped` when `DB::getDriverName() !== 'pgsql'`; precedent `apps/api/tests/Feature/Identity/LotActionPermissionDeltaTest.php:32-34`). N-4(r3): fix the six drifted cites in the handback round-2 table (`:56`, `:59`) to the method declaration lines (`:156`, `:177`, `:200`, `:213`, `:235`, `:270`). N-1(r3) is already applied in plan rev 12 — no action.

## Commits
One commit for the split + code minors, one for docs. No new test CLASS is expected; if you add one, raise `apps/api/tests/feature-lane-manifest.json` with the standard four-statement note and add it to the `.github/workflows/ci.yml` `backend-test-pgsql` alternation (comment block ABOVE the `php artisan test` line).

## Verification (record every command + result in the handback)
PG (one file at a time, `autoerp_test_w`): `BatchReadLocationScopeTest` (default → 16 tests / 1 skipped, exit 0), `BatchExpiringLocationScopeTest`; SQLite `-c phpunit.xml`: `BatchReadLocationScopeTest` (the pin test now SKIPPED, not failing); `./vendor/bin/pint --test` + scoped `./vendor/bin/phpstan analyse` on `BatchController.php`; web: Vitest one file at a time (`BatchListPermissions.test.tsx`, `BatchDetailPermissions.test.tsx`, `ExpiryWriteOffPage.test.tsx`), `pnpm typecheck`, `pnpm audit:keys`, `pnpm audit:design-system`, `node scripts/factory/gen-route-manifest.mjs --check`; `php tools/feature-lane-manifest-check.php` exit 0. Stop with `status: review`.
