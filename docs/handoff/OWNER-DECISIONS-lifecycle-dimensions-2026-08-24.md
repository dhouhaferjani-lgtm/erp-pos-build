# OWNER DECISIONS — document lifecycle dimensions program (Session C) — 2026-08-24

Each row: the question, the FAIL-SAFE default the spec/plan proceeds on until ruled, and what changes if you rule otherwise.
Spec/plan: `docs/sessions/session-C-lifecycle-2026-08-24/{SPEC-document-lifecycle-dimensions.md,PLAN-phase2-3-P-R.md}` (r1).
Rule by editing the "Ruling" cell; the session log records the date.

| # | Question | Fail-safe default in force | If ruled otherwise | Ruling |
|---|---|---|---|---|
| OQ-11 | `in_payment` (payment registered, pending bank reconciliation) — `payments.is_reconciled` EXISTS. Surface it in `payment_status` per the repo spec, or not? | Not a `payment_status` value; `has_unreconciled_payments` boolean on the read DTO | Add `in_payment` value + CHECK + FE colour; B2B default `require_bank_reconciliation=true` | |
| OQ-12 | (reserved — identifier kept free to avoid collision; no question) | — | — | n/a |
| OQ-13 | Supplier goods return: does it reopen the PO for replacement receipt, reduce NET fulfilment, or leave gross receipt unchanged? | Reduces net fulfilment (PO may show `partially_fulfilled` again; receivable again) | "gross unchanged" ⇒ returns never touch PO verdict; "reopen" ⇒ explicit replacement-expected flag | |
| OQ-14 | Confirmed-unposted invoice printout: N-6 ruling says VAT shown normally + "not posted" line; adopted expert ruling (Art. 18) says no VAT mention before delivery (proforma). Which stands for TN? | PROFORMA / non-definitive label, NO VAT block, until ruled | Keep N-6 behaviour (VAT shown) — accepted Art. 18 exposure recorded | |
| OQ-15 | TN: "posted = sealed + Ministry QR". While the QR is pending: refuse `confirmed→posted` (synchronous QR) or post with `fiscal_authority_status=pending` and block print/dispatch until accepted? | Refuse (fail-closed) — posting waits for acceptance | Post-with-pending: printing/dispatch gated; a rejected QR path needs a ruling too | |
| OQ-16 | Fully applied/refunded credit note: keep emitting the existing `DocumentFullyPaid` event (semantics "fully settled") or version a settlement event? | Reuse existing event, schema unchanged | `CreditNoteSettledV1` event + listeners | |
| OQ-17 | FR purchase-posting + supplier-advance defaults: copy TN (`require_receipt_first`, `refuse`) or hold provisional pending FR expertise? | Seed FR rows PROVISIONAL — posting refused with a typed message on FR tenants until approved | Approve = flip `provisional=false` | |
| OQ-18 | `ReturnNoteMetadata` / `RefundMethod` (dead backend, live web fields): delete, or revive as first-class refund disposition? | Untouched (no deletion, no revival) until the CN cash-refund lane R-3 defines disposition | Delete ⇒ FE fields + table removal lane; revive ⇒ R-3 writes them | |
| R-OQ-9 | CN VAT declaration: today status-blind (a confirmed-never-posted or cancelled credit note already deducts output VAT). Fix changes declared figures on existing tenants. | Fix ships behind a per-tenant per-period delta report (`vat:declaration-delta --dry-run`) — merge only after you acknowledge the deltas | Keep status-blind (non-compliant, recorded) | |
| OQ-19 | Dashboard revenue is gross of credit notes (`DashboardController:50-58`). Net it in this program? | Unchanged | Net-of-CN lane (changes a daily-watched number) | |
| OQ-20 | `closed` meaning per type — confirm SPEC §1.3/§1.4 (Quote/RFQ converted/expired/cancelled; orders fully fulfilled ∧ every line invoiced; DN billing mark or fully returned per tuple; RN at confirm — per OQ-28, which governs). | As tabled (OQ-28 governs the RN cell) | Edit the table row | |
| OQ-21 | Fully returned invoice: closed once the credit is fully settled, or does `returned` satisfy "fulfilled"? | `returned` counts as fulfilled for closure; the invoice closes when posted ∧ settled | Require linked CN settled | |
| OQ-22 | Is cancellation itself a closure condition (Quote/RFQ and all types), with `closed_at` = cancellation instant? | Yes — `closed_reason=cancelled`, never reopened | Cancelled ≠ closed (clears `closed_at`) | |
| OQ-23 | Stamped TN credit note: fully settled when its EX-STAMP credit value is applied/refunded? | Yes — base = `total − stamp_duty_amount` (matches the production application writer) | Base = total (stamp refundable) | |
| OQ-24 | Existing `procurement_policies` rows: genuine company overrides or provisioned defaults migrable to inherited country policy? | Rows equal to the vertical defaults are migrated to `inherited`; others stay `override` | All rows override | |
| OQ-25 | Expense/Income settlement: keep `expense_metadata.is_paid` / `income_metadata.is_received` as the payment basis, or migrate them to payment allocations? | Keep metadata basis (no allocations, no `DocumentFullyPaid`) | Migration lane to allocations | |
| OQ-26 | Deferred-revenue account purpose: TN 472 only; what is the approved FR equivalent before FR can ever seed `allow`? | TN mapped; FR absent ⇒ `allow` cannot seed for FR | Provide FR mapping | |
| OQ-27 | Supplier-invoice input VAT: `document_tax_details` never written for SIs today. Which historical periods get backfilled and acknowledged when introduced (lane P-4)? | Not in this program's VAT predicate change; P-4 ships a per-period delta for your ack | Name the periods | |
| OQ-28 | Return-note closure while refund disposition stays deferred to R-3? | RN closes at confirm (terminal) | Wait for R-3 | |
| OQ-29 | Which country, if any, is authorized to activate `allow` (pre-delivery invoicing with 472 deferral) once the engine lands? TN = `require_delivery_first`; FR has no approved deferred-revenue mapping. | None — activation lane stays owner-gated | Name country + account mapping | |
| OQ-30 | Historical AP openings are minted as Invoice/CreditNote today (`ArApOpeningService`). Migrate to native SupplierInvoice/SupplierCreditNote, or keep the type and add an immutable `opening_side` discriminator? | `opening_side` discriminator (lane C-H0), no type migration | Type migration lane | |
| OQ-31 | Returned DeliveryNote closure: what durable fact proves "returned" — RN confirmation, fully returned quantity, or linked CN disposition? | Fully returned quantity (Σ confirmed RN lines ≥ DN lines) | RN confirmation alone / CN disposition | |
| OQ-32 | Quote/RFQ expiry: persisted by a scheduled `documents:expire` command, or clock-derived at read time (no `closed_at`)? | Scheduled idempotent command stamps `expired_at` | Read-time only | |
| OQ-33 | POS account-charge invoices (lines without `product_id`): is the sealed POS receipt authoritative proof of physical fulfilment? | Yes — fulfilled by provenance; lines carry `line_kind` (physical/service) from the receipt | Require product identity on imported lines | |
| OQ-34 | Unapplied SupplierCreditNote residue: future supplier offset, supplier cash refund, or owner-approved manual closure? | Retained supplier credit (future offset); manual closure refused | Cash refund lane / manual closure | |
| OQ-35 | Cancellation of a posted document whose accounting period is closed while the VAT period is open (or vice versa): refuse, or allow a current-period reversal policy? | Refuse unless BOTH periods are open (dual guard, lane C-X0) | Current-period reversal policy (seeded per country) | |
| OQ-36 | Already-`paid` sales credit notes whose settlement is a backwards customer receipt (cash IN / Cr 411): reverse/reclassify automatically, quarantine for manual correction, or rule tenant-by-tenant? | Quarantine + manual correction via the N-6 repair command (`--type=credit_note`); `paid` CHECK not tightened while any remain | Automatic reclassification lane | |
| OQ-37 | Historical AR/AP CreditNote openings: applicable to future invoices only, cash-refundable, or non-settleable in this program? | Non-settleable (allocation refused, shown as open credit) | Application via R-2 / refund via R-3 | |
| OQ-38 | TN QR: the program REFUSES TN `confirmed→posted` (`FISCAL_AUTHORITY_UNAVAILABLE`, lane C-QR0) until the QR acquisition lane F-2 lands — that is the fail-safe per OQ-15. Do you sign an OVERRIDE authorizing seal-only TN posting for a bounded period instead (tenant #1 impact)? | Refusal stands (no override); F-2 is the first fiscal lane after C-QR0 prerequisites | Signed bounded override with end date | |
| OQ-39 | Legacy rows with unrecoverable `sealed_at`: does quarantine block the tenant's whole fiscal-chain verification rollout or only the affected chain? | Only the affected chain's rows are reported; other chains verify | Block whole tenant | |
| OQ-40 | After a TN authority REJECTION: retry as a new immutable authority attempt against the same unchained payload (document stays `confirmed`, unsealed), or must a replacement/corrective document be minted? | New attempt against the unchained payload; the document is sealed only once, on acceptance | Replacement document per rejection | |
| OQ-41 | SalesOrder payment base and its state after partial transfer to several invoices? | Base = uninvoiced remainder; settled = prepayments still attached; `not_applicable` once every line is invoiced | Base stays total | |
| OQ-42 | "Returned" invoice: must every ordered physical tuple have first been delivered, or may a linked fully-settled credit note close undelivered quantities? | Every tuple must have been delivered then returned; a CN never closes undelivered quantities | Linked-CN rule | |
| OQ-43 | Cancelling a supplier invoice that has a downstream supplier credit note / goods return: unconditional refusal, or cascade reversing children first? | Unconditional refusal (reverse children explicitly first) | Cascade lane | |
| OQ-44 | `supplier_advance_mode`: company-overridable, or country-mandated only? | Company-overridable (nullable override column, precedence override > country row > typed refusal) | Country-mandated (no override column) | |
| OQ-45 | Fulfilment quarantine (ambiguous provenance / unresolved POS line kind): blocks posting under `require_delivery_first` AND blocks closure — confirm? | Yes, both blocked until resolved | Allow closure with quarantine listed | |
| OQ-46 | POS account-charge invoice: is it the document representation of the ALREADY-posted POS fiscal event + JE (no second seal, no second GL), or are you authorizing a second fiscal invoice? | Representation only — posting adopts the POS event/JE; native invoice GL skipped; payments clear the existing 411 | Second fiscal invoice (double GL — not recommended) | |
| OQ-47 | Supplier invoice: make `confirmed` a durable user-visible state/action (needed so supplier advances have a target), or should supplier advances target purchase orders instead? | Durable confirm endpoint + UI action (lane C-P0); posting accepts only persisted confirmed | Advances target POs | |
| OQ-48 | Order prepayments across several partial invoices: FIFO capped to each invoice total, proportional, or user-selected? | FIFO capped; residue stays on the order; 419 cleared only when each invoice posts (clear-at-conversion removed) | Proportional / user-selected | |
| OQ-49 | Legacy TN Posted documents with no provable Ministry acceptance when authority mode becomes `required`: quarantine, grandfather under signed override, or tenant-by-tenant correction? | Quarantine (listed; lifecycle untouched) | Signed grandfather override | |
| OQ-50 | Unlocated ordered lines fulfilled from multiple locations: ordered capacity product-wide, or allocated by source-line provenance? | Product-wide unless durable source-line provenance allocates a location | Provenance-only | |
| OQ-51 | Replacement supplier invoice after cancellation: explicit `replaces_document_id` + copied supplier reference audit? | Yes | No linkage | |
| OQ-52 | Country `provisional=true`: absolute jurisdiction gate that no company override can bypass? | Yes (evaluated before override resolution) | Overridable | |
| OQ-53 | Ministry authority (TN QR): applies to Invoice + CreditNote only, or also to DeliveryNote / ReturnNote chains (which seal at `draft→confirmed`)? | Invoice + CreditNote only (`fiscal_authority_types` seeded per country); DN/RN `not_required` | Extend to DN/RN (needs a draft-preserving attempt protocol) | |
| OQ-54 | Which supported countries seed `fiscal_authority_mode=not_required` (FR, UK, IT, generic), and must a missing country row always refuse posting? | All non-TN seeded `not_required` (provisional where expertise owed); missing row = typed refusal | Different per-country values | |
| OQ-55 | What durable evidence on a POS receipt proves Ministry acceptance for a TN account-charge invoice? (The current optional tenant-signed QR is not that.) | None today ⇒ in a `required` country, POS account-charge invoices are BLOCKED from posting until F-2-compatible evidence exists on the receipt | Accept tenant-signed QR as evidence (non-compliant) | |
| OQ-56 | After the final partial invoice, must untransferred SalesOrder prepayment residue become a partner-level advance before the order closes? | Yes — detached to partner advance; order `not_applicable` only with nothing attached | Residue stays on the closed order | |
| OQ-57 | Authority accepted a payload but the local digest no longer matches (raw write): permanent quarantine, or corrective document? | Quarantine + corrective document (never re-seal) | Permanent quarantine only | |
| OQ-58 | May ordinary Expense / Income be cancelled? If so their GL + repository cash movements must be reversed by new documents. | No `posted→cancelled` edge; correction = posted reversal document | Add a cancellation lane with full reversal | |
| OQ-59 | Service-only / mixed supplier invoices: do service lines bypass receipt-first and post directly to expense/payable, with only physical lines using GR-IR? | Yes (`line_kind` per line; service-only SI ⇒ fulfilment n/a) | All lines need receipts (service invoices unpostable) | |
| OQ-60 | Authoritative set of "supported countries" for fiscal-authority policy parity? | Every provisionable ISO-3166 country in `CountriesSeeder` (GB, not UK; no generic key); missing row refuses | Named subset | |
| OQ-61 | Are supplier credit notes stamped in TN, and is that stamp application-eligible? | Unstamped (seeded explicitly); missing policy refuses | Stamped + rule | |
| OQ-62 | Goods-receipt reversal after any quantity was invoiced or supplier-returned: refuse until downstream reversed, or allowed? | Refuse until all downstream documents are reversed | Allow with cascade | |
| OQ-63 | DN/RN reversal: distinct corrective document type in its own fiscal chain, or same chain/type? | Distinct reversal type, own chain; original stays sealed | Same chain | |
| OQ-64 | For every catalog country beyond TN/FR: explicit pre-delivery invoicing policy value, and does a `provisional` jurisdiction row block sales posting before company overrides? | Non-TN/FR rows seeded `require_delivery_first` + `provisional` (posting refused until approved); provisional blocks before overrides | Per-country values / no block | |
| OQ-65 | Service supplier-invoice lines: expense leg always the country-seeded `PurchaseExpenses` purpose, or may PO/category/account provenance pick a more specific account? | `PurchaseExpenses` purpose always (this program) | Category-specific mapping lane | |
| OQ-66 | After a DN/RN reversal: original `fiscal_status` → VOIDED (hash/sequence kept, chain member), or a separate reversal marker? | VOIDED, member kept | Separate marker | |
| OQ-67 | Supplier-invoice input VAT (P-4): entitlement date = `posted_at`; cancellation leaves the period by reversal-JE date; replacement enters on its own `posted_at`? | Yes | Invoice date / other | |
| OQ-68 | Exit of a `legacy_authority_unproven` quarantine: permanent reporting, owner-certified grandfathering, or corrective document? | Corrective document; owner-certified grandfathering only by signed ruling | Permanent reporting | |
| OQ-69 | POS-derived invoice fiscal representation: adopted-event FK outside document chains (row stays DRAFT/NON_FISCAL, no hash copy), a separate authority kind, or another structure? | Adopted-event FK outside chain membership | Separate authority kind | |
| OQ-70 | May a company override ever select `allow` before a country capability is approved? | No — `pre_delivery_allow_authorized` capability checked before override; persisted `allow` overrides rejected | Allow override | |
| OQ-71 | Procurement policy values + approval state for EVERY field × EVERY catalogue country? | TN approved (existing vertical values); all other countries `provisional` (posting refused) until you rule per country | Provide the matrix | |
| OQ-72 | Does a supplier goods return reopen fulfilment on the linked supplier invoice as well as the PO? | Yes — SI verdict nets linked returns; both recomputed | PO only | |
| OQ-73 | Cancelled supplier invoice input VAT: immutable negative reversal tax rows (keyed to originals, reversal date) remove entitlement? | Yes | Other representation | |
