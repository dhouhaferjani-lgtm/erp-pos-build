# Codex prompt — W-LOT-A-1a fix round 3 (paste into the SAME thread)

Fix round 3 on `lane/w-lot-a-1a` (worktree `.worktrees/w-lot-a-1a`, HEAD `a7010fe4d`). Authority: **plan rev 12** `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-12.md` — read **§000** first; then the gate-r3 tenancy register `docs/superpowers/reviews/2026-09-10-w-lot-a-1a-impl-gate-r3-tenancy.md` in full (every r2 blocker/major is VERIFIED closed on PG — do not touch them). Small round: one commit for the split + code minors, one for docs. Cite `path:line` at current HEAD in a "## Fix round 3" handback table.

**MAJOR M-1(r3) — the Push-3 Vitest depends on a Push-5 page.** `apps/web/src/features/batches/pages/__tests__/BatchPermissions.test.tsx` is in the Push-3 row but its four `BatchDetailPage` gating cases (`:54-71`) need the lane's `BatchDetailPage.tsx` (Push 5); with the pre-lane detail page three of them fail. Split it: the four list cases (`:35-53`, need only `BatchListPage.tsx` + `types.ts`) stay in a Push-3 file (`BatchListPermissions.test.tsx`), the four detail cases move to a Push-5 file (`BatchDetailPermissions.test.tsx`). Re-reconcile the handback ledger (every non-doc file exactly once; now 76 rows), name the Push-3 file in `RELEASE-NOTES-WLOTA-1a-2026-09-10.md:16`. Prove the Push-3 set is self-consistent: `git stash`-free check — run the Push-3 Vitest against a scratch checkout of the pre-lane `BatchDetailPage.tsx` is NOT required; instead assert in the handback that the Push-3 Vitest imports nothing outside `types.ts`, `BatchListPage.tsx` and shared libs (`grep import`).

**Minors.** N-2(r3): flag-off `BatchController::expiring()` — guard the raw `location_id` with `Str::isUuid()` (ignore non-UUID like baseline did) so `?location_id=0` is 200, not a PG 22P02 500; one test in `BatchExpiringLocationScopeTest` (flag off, `location_id=0` → 200). N-3(r3): the 2b pin test (`BatchReadLocationScopeTest:235-268`) gets the `pgsql` driver skip (`markTestSkipped` when `DB::getDriverName() !== 'pgsql'`, precedent `LotActionPermissionDeltaTest:32-34`). N-4(r3): fix the six drifted cites in the handback round-2 table (`:56`, `:59` → method declaration lines `:156`, `:177`, `:200`, `:213`, `:235`, `:270`). N-1(r3) is orchestrator-owned (plan §6.1, done in rev 12) — no action.

Verification (one process at a time; PG on `autoerp_test_w`, both DB env vars): `BatchReadLocationScopeTest` (PG, default → 16/…/1 skipped; SQLite → the pin test now skipped, not failing), `BatchExpiringLocationScopeTest` (PG), `pint --test` + scoped PHPStan on `BatchController.php`; Vitest one file at a time: the two new files + `ExpiryWriteOffPage.test.tsx`; `pnpm typecheck`; `pnpm audit:keys`; `node scripts/factory/gen-route-manifest.mjs --check`; `php tools/feature-lane-manifest-check.php` exit 0. Update the handback ("## Fix round 3"; ledger; verification). Stop with `status: review`. Do not merge, do not push.
