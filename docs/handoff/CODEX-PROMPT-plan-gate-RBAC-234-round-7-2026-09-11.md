# Codex plan gate r7 prompt — RBAC waves 2, 3, 4 plans rev 7 (2026-09-11)

You are the adversarial PLAN gate (round 7) for AutoERP. Repository root is the current directory (a git worktree on branch docs/rbac-audit-2026-09-09; read-only sandbox; artisan cannot boot here — for route arithmetic use the shared checkout /Users/houssamr/Projects/syneriva/apps/erp read-only or docs/superpowers/audits/2026-09-09-roles-permissions/02-route-enforcement-sweep.csv). Declare `git rev-parse --short HEAD`. Review: docs/superpowers/plans/2026-09-10-rbac-wave-2.md, docs/superpowers/plans/2026-09-10-rbac-wave-3.md, docs/superpowers/plans/2026-09-10-rbac-wave-4.md (all rev 7). They implement the ACCEPTED spec docs/superpowers/specs/2026-09-10-roles-permissions-catalogue-design.md (rev 9; gate r9 register docs/superpowers/reviews/2026-09-10-rbac-spec-codex-gate-r9.md — "Preserve" list binding). Context: wave 0b rev 6.3 `5257eb0f1` and wave 1 rev 6.4 `70384d7e0` (editorial re-spelling only) are BOTH DISPATCH-READY (gate r8 each, 0 B / 0 M); NO producer change is owed — do not claim otherwise — do NOT re-report those, but DO check that waves 2/3/4 consume the wave-1 names the wave-1 plan currently declares and flag any that will need re-pinning. Also read docs/superpowers/plans/2026-09-10-rbac-programme-execution-plan.md and, for wave 4, docs/qa/ONBOARDING-CAMPAIGN.md + scripts/campaign-onboarding.sh + docs/conventions/09-SECOND-OF-EVERYTHING.md.

Verify against CODE (dev tip via `git show dev:<path>`; lanes via `lane/…:`), never against prose:
1. Every path:line — confirm; list wrong/stale.
2. Wave 2 (two lanes 2a/2b): principal_kind migration + CHECK + down; kind-check before Hash::check in login AND PIN paths (cite AuthController/PosAuthController lines); service-account routes + CreateServiceAccountRequest + AssignableRole; typed 422 on generic user AND RoleController assign/remove; pinHolders exclusion; notification/invitation/reset selectors skip service kind (census complete vs spec §4.1 + the r2 gate's writer list); seat exclusion in every counter (PlanLimitsService, PlanEnforcementService, SuperAdminController) + max_service_accounts in every tier incl. trial; Sanctum abilities `permission:<key>` narrowing, TTLs, `tenant:` claim; EnforceTokenScope in global api group + priority + coverage test + non-tenant no-op; role-name authz ban (PHPStan rule + exact shrink-only baseline) and conversion BEFORE token issuance (entry-condition test ScopedTokenIssuanceEntryConditionTest — is it falsifiable?); EffectivePermissionResolver forSubject/forTarget + token_id selector; getAllPermissions narrowing only for the authenticated subject; effective-permissions endpoint authz (self or roles.view) + UI; MembershipRevocationService central best-effort + retry; roles.is_system + LastAdminFloor wired into every writer the spec enumerates with deterministic locking (uses PermissionWriteLock from 0b — same key/order?); RoleCreated/Updated/Deleted extending DomainEvent + stable names + subscriber + attribution at AuditService; denial audit events with atomic Redis dedup (Lua/MULTI) + Redis test; roles.view names-only shaping; FE server-authoritative usePermissions (fallback map + uiAliasPermissions deleted), generated TS union; Playwright e2e listed for every UI change; rollback restores prior users schema.
3. Wave 3: enforcement-style sweep — the 127/44 numbers re-derived from the CSV (unescaped commas) vs a fresh route:list classification; module order; POS last; FormRequest authorize rule; POS legacy ladder removal AFTER fetchDiscountPermissions wiring; MembershipRole reconciliation (only authz read at UserController.php:929 — verify); glossary rows for every §2 noun; per-role regression matrix generated from registry templates; deprecated-key retirement conditions; Sidebar field rename. Reviewer gates per module.
4. Wave 4: does the campaign cover every §7 edge-case row (58 by prose / 57 enumerated — verify the count and the EC-22 withdrawal), correct lane per row, Playwright scenarios per the repo harness with 5xx + console capture and toast assertions, evidence-doc template, second campaign vs the onboarding one, staging replay checklist.
5. Task-level: red tests compilable at dev tip, first failing assertion truthful, implementations complete (no prose stubs), exact commands with PG env, `Phase 0.<n>.<task>:` subjects consistent with the ordinals the plans chose (0.4/0.5/0.6/0.7 — check AGENTS.md:15-16 and that they don't collide with 0a=0.1, 0b=0.2, wave1=1.x), exact `git add` lists.
6. The 17 Q-w points: consistent with spec/code, or must any be escalated (only OQ-1/OQ-2 are legitimately owner items for wave 2b)?

Output a Markdown register: "# Codex plan gate r1 — RBAC waves 2, 3, 4 plans rev 1 (gpt-5.6-sol, high, read-only, 2026-09-10)", per-wave sections "## Wave 2" / "## Wave 3" / "## Wave 4" each with "### BLOCKER / ### MAJOR / ### MINOR", then "## Cross-plan consistency", "## Citation audit", "## Rejected false positives", "## Preserve", "## Owner decisions required", "## Dispatch assessment", LAST LINE exactly `VERDICT: DISPATCH-READY` (all three) or `VERDICT: CHANGES-REQUIRED`. Cite path:line for every finding. Print in full.






Round-7 additions: FIRST read your round-6 register docs/superpowers/reviews/2026-09-11-rbac-plan-234-codex-gate-r6.md and produce a per-wave "Rev-6 closure table"; verify each plan's "Change log → rev 7" against code. Producers FINAL: wave 0b rev 6.3 `5257eb0f1`, wave 1 rev 6.4 `70384d7e0` (editorial only; its `:3` label now reads rev 6.4), spec rev 9.2 (A-1, A-2). SETTLED in rev 7: wave 2 majors + scaffold resource + `Phase 0.4.10`; wave 3 matrix actors get active `UserCompanyMembership` (mirroring `RBACTest.php:61-74`) and every request sends `X-Company-Id`, deny oracle asserts `error.code === 'FORBIDDEN'` and not the company-access codes; wave 4 `staging_env:` overlay + mechanical env composer for SSH/docker (`env_compose`/`env_docker_flags`), EC-16a inside the pause asserting a tenant-correlated pending job, replay selection guard, `generate-evidence-rows.php --check`, numeric `0.7.6…0.7.31`; runner record separator changed to US (0x1f) and `local IFS` pinned in helpers (two real bugs found by exercising). Scrutinise: (a) re-extract and run the generator (176 / 25), re-parse manifest + fixture JSON, exercise ≥5 runner contracts on a fake-ssh harness incl. one env-composition case (assert the generated remote line carries DB_CONNECTION/host/port/Redis/CACHE_STORE); (b) the matrix test fixture vs `CompanyContextMiddleware` and the membership migration defaults; (c) any residual placeholder or overclaim. Per-wave verdict; if only MINOR findings remain for all three, the verdict MUST be DISPATCH-READY (noting entry conditions per wave) with editorial lists. Output heading: "# Codex plan gate r7 — RBAC waves 2, 3, 4 plans rev 7 (gpt-5.6-sol, high, read-only, 2026-09-11)".
