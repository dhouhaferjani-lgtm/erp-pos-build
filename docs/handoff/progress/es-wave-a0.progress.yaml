wave: es-wave-a0
brief: docs/handoff/CODEX-DISPATCH-es-wave-a0-2026-08-11.md
harness: docs/handoff/SELF-REVIEW-HARNESS.md
# No pre-dispatch adversarial register exists for this brief; per-milestone registers land here.
review_register: docs/handoff/reviews/es-wave-a0/
reviewer_model: opus
max_fix_rounds: 5

# PIN AT DISPATCH. base_sha = REVIEWED CONTENT BASELINE (entry gate CLOSED at round 4), NOT a
# strict HEAD-equality pin — the pin commit itself advances dev, so HEAD == base_sha can never hold.
# M0 base check (ancestor + digest + admin-only delta) verifies ALL THREE at HEAD:
#   1. base_sha is an ancestor of HEAD: git merge-base --is-ancestor <base_sha> HEAD
#   2. contract digest verifies at HEAD:
#        git show HEAD:docs/handoff/ES-CONSOLIDATED-REGISTER-2026-08-11-SNAPSHOT.md | tail -n +63 | shasum -a 256
#        expected 04760455ac3f80b96502884e9efc2a8f00c35785d2126a9f9786d96d97e20540
#   3. git diff --stat base_sha..HEAD touches ONLY administrative paths:
#        docs/handoff/progress/*.progress.yaml, docs/handoff/CODEX-DISPATCH-es-wave-a0-2026-08-11.md,
#        docs/handoff/CODEX-DISPATCH-sv-stage1-2026-08-11.md, docs/superpowers/reviews/*
#   If any OTHER path appears in that diff, or check 1/2 fails: the base is contaminated — STOP, set
#   status: blocked_precondition, request an orchestrator re-pin. Do NOT edit base_sha.
base_sha: df85d43f404a9e55fd29b0e3c7533852966652df
# Branch codex/es-wave-a0 in worktree .worktrees/es-wave-a0. On 2026-08-12 its existing unpushed
# commits were replayed onto CURRENT local dev tip a5520f23ca39209f5b517723037e9516808f2bca;
# base_sha remains the reviewed CONTENT baseline verified by the three-part M0 check.
# Handback only: NOT merged, NOT pushed — the orchestrator merges after the wave completes.
branch: codex/es-wave-a0

owner_gates:
  - id: base-sha-pin
    question: "Confirm base_sha is an ancestor content baseline, digest verifies at HEAD, and base_sha..HEAD is administrative-only — M0 verifies all three."
    status: satisfiable_by_M0
  - id: D-8-es06-second-approver
    question: "ES-06: does resolving a parse failure on a SEALED fiscal event require a second approver, and is the correction recorded as a NEW correcting fiscal event rather than an in-place payload rewrite? (Handover Q1 / owner sheet D-8.) A0 ships DETECTION regardless; the workflow half is excluded until ruled."
    blocks_milestone: none
    status: open
  - id: D-11-es43-signature
    question: "ES-43: unkeyed SHA-256 vs the NF525 RSA-2048/ECDSA-256 requirement — acceptable for the TN first tenant, deferred to the FR lane, or launch-blocking? (Handover Q5 / owner sheet D-11.) ES-43 is EXCLUDED from A0's scope until answered; if it defers to FR it leaves A0's exit scope entirely. Forward-only when it lands: signature_* populated at INSERT for NEW rows only, never on a sealed row."
    blocks_milestone: none
    status: open
  # informational — conditional logic lives in the milestone text (M4).
  # The harness treats a milestone `owner_gate:` field as an UNCONDITIONAL STOP
  # (SELF-REVIEW-HARNESS.md step 4 / condition B), so this gate must NOT be wired
  # onto M4 as a field: the permission is already CHOSEN (pos.operate_terminal).
  - id: ES-42-device-permission-grant
    question: "ES-42: adding a can: gate to POST /pos/sync/fiscal-events is a LIVE DEVICE route. The permission is LOCKED to the EXISTING pos.operate_terminal (seeded; already gates comparable POS write surfaces). This gate fires ONLY if M4's first check finds that the device principal does NOT hold pos.operate_terminal in the fixture — then a grant (new permission + role seeder + permission:cache-reset) becomes a policy/deploy choice: STOP blocked_owner rather than inventing it."
    blocks_milestone: none
    status: conditional

milestones:
  - id: M0
    title: "Preflight — contract digest verify (abort on mismatch), base ancestor + digest + administrative-only dispatch delta, seeded two-context v3 tenant fixture, TAMPERED-FIXTURE toolkit (T-a payload-vs-canonical_bytes, T-b previous_hash from wrong chain_context, T-c fiscal_hash mirror) built on VerifyEventChainCommandTest/ParseFailureResumeTest, narrowed ES-07 claim + do-NOT-rewrite-the-Z-arm record, citation sweep (unresolved=0)"
    review_lenses: [fiscal-pos, treasury]
    status: passed
    fix_rounds: 2
    commit: a7b4ad43da7d295176622395dae17c2b93e25d1c
    verdict: docs/handoff/reviews/es-wave-a0/M0-round3.md
    last_verdict: ACCEPT
    updated: 56d83003b2f50c7aed8379ce564c0b051fbb0d91
  - id: M1
    title: "ES-08 — extend fiscal:verify-event-chain: payload<->canonical_bytes, integrity_status (quarantined-but-intact must stop reporting verified), sealed coordinates, sequence_number contiguity, + the MANIFEST-DRIVEN fleet driver (operator-supplied tenant->actor-id manifest; per listed tenant run the existing single-tenant verification with the actor gate and tenant binding preserved EXACTLY; missing/unauthorised tenants REPORTED LOUDLY with a non-zero aggregate exit, never silently skipped; NO service account or new identity model; terminal/context enumeration from the distinct (terminal_id, chain_context) pairs in fiscal_events within the bound tenant; unworkable manifest shape -> STOP condition C). Verifier-class contract: RED on T-a and T-b, green on clean, EACH new check individually demonstrated red."
    review_lenses: [fiscal-pos, treasury]
    status: passed
    fix_rounds: 4
    commit: 065cab23008e060ed7620ca02d9742fe2c43fe37
    verdict: docs/handoff/reviews/es-wave-a0/M1-round5.md
    last_verdict: ACCEPT
    updated: d8122d10676ed8e30a5f5a1b2c3916f153902b36
  - id: M2
    title: "ES-07 (NARROWED) — pos:verify-chains COVERAGE/REPORTING + the MIRROR. Defect (a): fiscal-era receipts are not counted, so a terminal whose receipts are all projected returns is_valid:true over a count of 0 and the fiscal arm's result is never surfaced per-arm. Defect (b): NO pos_receipts.fiscal_hash <-> fiscal_events.current_hash cross-check exists anywhere — add it as a new arm. ReceiptHashService::verifyTerminalChain() ALREADY self-partitions (fiscal arm canonical-hashes; legacy arm re-applies whereNull and pipe-hashes only fiscal_event_id IS NULL rows), so a canonical-bytes row can never reach a pipe recomputation — the implementer CHOOSES the mechanism (safe filter removal + per-arm reporting, or an explicit fiscal-era pass). The Z arm is NOT blind — out of scope beyond what the mirror requires. RED on T-c + tampered projected chain; per-arm counts must prove nonzero fiscal-era coverage; negative control on an all-legacy tenant."
    review_lenses: [fiscal-pos, treasury]
    # Fix round 1 delivered 2026-08-19 under the STOP C ruling
    # (docs/handoff/reviews/es-wave-a0/ORCHESTRATOR-RULING-2026-08-19-m2-stop-c.md,
    # commit bd4139ec3). Round-1 findings 1, 2, 3, 4 closed; 5 recorded; 6 is an
    # inherited residual with an open owning-lane question for the parent.
    # M2 round 2 (2026-08-19) = ACCEPT over the fix-round slice 4fea617e0..b8b3c52f6.
    # Four P3 residuals (round-2 findings 7, 8, 9, 10) were SWEPT INTO M3's diff,
    # exactly as the round-2 disposition asked.
    status: passed
    fix_rounds: 1
    commit: b8b3c52f6
    verdict: docs/handoff/reviews/es-wave-a0/M2-round2.md
    last_verdict: ACCEPT
    updated: 325499fe3
  - id: M3
    title: "ES-06 DETECTION half only (divergence on sealed rows detectable + demonstrated red; second-approver/correcting-event workflow is owner-gated D-8 -> blocked_owner if reached) + the ES-16/ES-17 straggler contract PROPOSALS as a COMMITTED artifact at docs/handoff/reviews/es-wave-a0/M3-straggler-contracts.md (the harness reviews a committed range, so the proposal must be IN the diff the reviewer inspects). This milestone's bridge review approves/rejects THAT artifact. NO straggler implementation here."
    review_lenses: [fiscal-pos, treasury]
    # M3 also SWEEPS the four P3 residuals the M2 round-2 register left open
    # (M2-round2.md findings 7, 8, 9, 10) — its disposition assigned them to
    # "M3's diff or the wave's close-out". They are reporting-honesty items;
    # none of them changes a verdict.
    #
    # Delivered 2026-08-19 in ONE commit, 1edbf2633. Evidence:
    # docs/handoff/reviews/es-wave-a0/M3-implementation-evidence.md.
    # Proposal artifact (what this milestone's review must approve/amend/reject):
    # docs/handoff/reviews/es-wave-a0/M3-straggler-contracts.md — COMMITTED in
    # this range, per R-7.
    #
    # D-8 was NOT reached. ParseFailureResolutionService.php diff = 0 lines; no
    # second approver, no correcting event, no change to what the resolver may
    # do. Detection only, per R-9.
    # R-1 HOLDS: ZReportHashService.php diff = 0 lines (finding 7 was closed via
    # the comment/rendering path exactly so the Z service stays untouched).
    # NO straggler implementation landed — DeadLetteredProjectionsController,
    # FiscalEventQuarantine and OutboxIngestor are all untouched in this range.
    #
    # ROUND 1 = CHANGES-REQUIRED (docs/handoff/reviews/es-wave-a0/M3-round1.md,
    # register commit 4a93f8631). Fix round 1 delivered 2026-08-19 in ONE commit,
    # 7c836cee3, over the slice 4a93f8631..7c836cee3:
    #   F-1 (Important) — the recovery round-trip omitted JSON_UNESCAPED_UNICODE
    #     while production canonical bytes are RFC 8785 raw UTF-8
    #     (CanonicalJsonEncoder.php:106-108), so the ES-06 discriminating branch
    #     was unreachable for any envelope carrying non-ASCII free text — i.e.
    #     most French/Tunisian receipts. Fixed with the one flag
    #     (VerifyEventChainCommand.php:697-700) plus THREE red-first tests: the
    #     faithful and the divergent non-ASCII corrections (recovery must fire)
    #     and a \uXXXX-escaped non-canonical envelope (recovery must still
    #     refuse — the widening is bounded). The register's "harness cannot
    #     express a non-ASCII fixture on PG (22P02)" sub-note resolved: it is a
    #     BACKSLASH problem, not a non-ASCII one — Connection::bindValues() binds
    #     a plain string as PDO::PARAM_STR (bytea escape rules); the fixture now
    #     seals the canonical raw-UTF-8 form and binds via a stream
    #     (PDO::PARAM_LOB). Test harness only.
    #   F-2 (Important) — ES-16 clause 16-C DROPPED (option (i)); ES-16 is now a
    #     pure visibility contract, F16-7 added, defect item 5 corrected in place.
    #     M3-straggler-contracts.md is at revision 2. NO command change:
    #     EnqueueResolvedEventProjectionsCommand.php diff = 0 lines.
    #   F-3 (Minor) — clause 16-F citation :104 -> :106.
    # The evidence artifact now DISCLOSES the F-1 limit (new §1.6 + §2.1 + a
    # fix-round verification block) — the register faulted the non-disclosure
    # explicitly.
    # D-8 still NOT reached; R-1 still holds; still no straggler implementation.
    #
    # ROUND 2 = ACCEPT (docs/handoff/reviews/es-wave-a0/M3-round2.md, register
    # commit 32671a9f6), reviewed over the fix-round delta 4a93f8631..b1d86e641.
    # All three round-1 findings CLOSED and verified by execution, not by
    # assertion: the reviewer reverted the F-1 flag itself and reproduced the
    # red-first trio's exact counts (3 failed, 49 assertions), re-derived F-2's
    # factual premise from EnqueueResolvedEventProjectionsCommand.php +
    # OutboxIngestor.php:922, and confirmed F-3's :106 exact. Headline counts
    # reproduce (ParseFailureResumeTest 27/209, VerifyEventChainCommandTest
    # 34/138) on BOTH drivers (61/347 combined).
    #
    # Round 2 left THREE P3s, all accuracy rather than behaviour, with the
    # explicit disposition "swept into M3b rather than spending another round":
    #   N-1 — ParseFailureResumeTest.php:299 cited VerifyEventChainCommand.php:687
    #     for the byte-identity test; the fix round's own 18-line comment
    #     insertion moved it to :705.
    #   N-2 — the :663-665 docblock's boundedness claim was over-broad in BOTH
    #     directions. Probed on PHP 8.4 in this worktree: U+2028/U+2029 stay
    #     ESCAPED under JSON_UNESCAPED_UNICODE, so an envelope carrying one is
    #     a form the canonical encoder DOES emit and the guard RECOVERS it;
    #     and `{}` IS canonical
    #     (CanonicalJsonEncoder::encode([]) emits it) but the guard REFUSES it
    #     because json_decode('{}',true) -> [] re-encodes as []. The comment and
    #     its echo at ParseFailureResumeTest.php:340-343 now say what is true.
    #   N-3 — M3-implementation-evidence.md:245 "6 falsifiers" -> 7.
    # All three are swept in M3b's opening commit. Also swept: N-3's cosmetic
    # rider (F16-7 reordered after F16-6 in M3-straggler-contracts.md,
    # presentation only, recorded in the artifact as revision 2a).
    #
    # ESCALATED by round 2, NOT a finding against M3 and NOT M3b's work:
    # OutboxIngestor.php:815 binds canonical_bytes (PG bytea) as PDO::PARAM_STR,
    # so a canonical envelope carrying a backslash escape fails to INSERT with
    # 22P02. Separate ingestion-lane ticket, already escalated to the
    # orchestrator. M3b must NOT touch that binding.
    status: passed
    fix_rounds: 1
    commit: 7c836cee3
    verdict: docs/handoff/reviews/es-wave-a0/M3-round2.md
    last_verdict: ACCEPT
    updated: 32671a9f6
  - id: M3b
    title: "ES-16/ES-17 straggler IMPLEMENTATION, strictly to the contracts approved at M3. Implementing beyond or against the approved contract is a milestone failure, not a style problem. Own bridge review."
    review_lenses: [fiscal-pos, treasury]
    # Contract of record: docs/handoff/reviews/es-wave-a0/M3-straggler-contracts.md
    # at REVISION 2 (as amended in M3 fix round 1 and ACCEPTed at M3 round 2).
    # ES-16 = 6 clauses (16-A..16-F) + 7 falsifiers (F16-1..F16-7); 16-C is a
    # PURE VISIBILITY ABSENCE requirement — the surfaced row names no recovery
    # command at all. ES-17 = 7 clauses (17-A..17-G) + 7 falsifiers (F17-1..F17-7).
    # Binding constraints carried in: R-1 (ZReportHashService zero-diff), D-8 (no
    # second-approver / correcting-event workflow — STOP blocked_owner if the
    # contract path requires one), zero-line diff on
    # EnqueueResolvedEventProjectionsCommand.php (F16-7 / contract :147-153), and
    # no OutboxIngestor.php:815 PARAM_STR change (separate ingestion-lane ticket).
    #
    # DELIVERED 2026-08-19 over two commits, f28306baf (opening ledger + the
    # three swept M3 round-2 P3s) and the implementation commit recorded in
    # `commit:` below. Evidence:
    # docs/handoff/reviews/es-wave-a0/M3b-implementation-evidence.md — §3 and §4
    # are the clause-by-clause mapping brief line 594 requires, each clause
    # quoted verbatim.
    #
    # ES-16: a THIRD read partition on DeadLetteredProjectionsController::index()
    #   (source `z_session_lifecycle_quarantine`), matching on the SAME string
    #   OutboxIngestor's own suppression uses, so the rows surfaced are by
    #   construction the rows it hid. Carries the `lifecycle_violation`
    #   discriminator read out of integrity_exception_reason, and DELIBERATELY
    #   no action affordance at all (16-C as amended, F16-7) — not even the
    #   write_off_action_url the other two partitions carry.
    # ES-17: the missing resolved_at/resolved_by writer —
    #   QuarantineIncidentResolutionService + QuarantineIncidentResolutionController
    #   + POST /api/v1/fiscal/quarantine/{id}/resolve-incident, gated by the
    #   EXISTING seeded fiscal.events.resolve_quarantine. Single-actor
    #   adjudication record; refuses a re-stamp rather than overwriting the
    #   original resolved_by. VerifyEventChainCommand.php:856 is UNTOUCHED —
    #   only the writer was missing (F17-4).
    #
    # GATES, measured mechanically over 32671a9f6..HEAD, all 0-line diffs:
    #   ZReportHashService.php (R-1), OutboxIngestor.php (16-D, and so :815's
    #   PARAM_STR binding stays untouched), EnqueueResolvedEventProjectionsCommand.php
    #   (F16-7), ParseFailureResolutionService.php (F16-6 / D-8).
    # NO migration, NO onQueue (so no horizon.php entry owed), NO config, NO
    # front-end, NO new permission / role seeder / permission:cache-reset.
    # D-8 NOT reached. NO STOP triggered: 17-E's conditional STOP was checked
    # FIRST and does not fire (the permission is seeded and the principal holds
    # it, asserted as the first test in the ES-17 file).
    #
    # VERIFICATION: red-first on both rows (ES-16 5 of 7 red, each because the
    # row is ABSENT; ES-17 8 of 10 red, all HTTP 404 = no path exists today —
    # two ES-17 tests were vacuously green on the first red run and were
    # STRENGTHENED before implementing, disclosed in the evidence §5). Green on
    # PG 7/91 + 10/69; default driver agrees at 17/160. M3 headline counts
    # reproduce unchanged (ParseFailureResumeTest 27/209,
    # VerifyEventChainCommandTest 34/138). Regression 50/240; the pre-existing
    # DeadLetteredProjectionsControllerTest reproduces 12/33 unchanged (16-E).
    # pint --test pass on all 9 changed files; phpstan level 8 [OK] on all 7
    # production files.
    #
    # INHERITED REDS, proven by removing every line of M3b production code and
    # re-running (identical baselines, evidence §6): OutboxIngestorTest clock
    # test (local TZ artifact), QuarantineBestEffortParseControllerTest x3 (the
    # tests' OWN fixtures trip the fiscal_events immutability trigger), and
    # tests/Architecture x4 (46/231 identical across the swap — which also
    # proves the new controller IS tenant-classified).
    #
    # DISCLOSED RESIDUAL for M3b's reviewer to rule on: DeadLetteredProjectionsController::show()
    # was NOT extended. Clause 16-A names index() and only index(), so a
    # lifecycle row still returns 409 NOT_DEAD_LETTERED on the detail endpoint —
    # the behaviour it has today, unchanged. Not taken silently either way: if
    # the reviewer reads "the existing dead-letter read surface" in the
    # contract's headline as including show(), that is an amendment to make at
    # review and implement in a fix round.
    status: review
    fix_rounds: 0
    # Milestone slice for the reviewer: 32671a9f6..dfa58a418
    #   f28306baf — opening ledger + the three swept M3 round-2 P3s
    #   dfa58a418 — ES-16 + ES-17 implementation
    commit: dfa58a418
    verdict: null
    last_verdict: null
  - id: M4
    # SCOPE AMENDED 2026-08-19 by docs/handoff/reviews/es-wave-a0/ORCHESTRATOR-RULING-2026-08-19-m2-stop-c.md:
    # ES-09 covers TerminalRegistrySnapshotService and VirtualAdminFiscalEventService
    # ONLY. ReceiptHashService's (company_id, chain_context) partition was pulled
    # forward into M2 (commit bc692820a) — M4 must NOT re-fix it. M4's two-context
    # append test still asserts the full end-to-end shape INCLUDING the
    # receipt-verification arm.
    title: "Guard/constraint class — THREE DIFFERENT CONTRACTS, do not conflate. ES-09 = CORRECTNESS: chain-head resolution scoped by (company_id, chain_context) at BOTH TerminalRegistrySnapshotService and VirtualAdminFiscalEventService, matching OutboxIngestor's shape; before-fix RED demonstrates context-blind head resolution on a two-context terminal, after-fix GREEN = the two-context append SUCCEEDS with heads resolved per (company_id, chain_context) and no unconditional Verified/Parsed stamp. ES-41 = TRIGGER-PRESENCE regression scoped to its snapshot row (PG trigger presence asserted; the non-PG driver scope is DOCUMENTED, not 'fixed'; pending_seal->fiscalized branch SUSPECTED — verify before asserting). ES-42 = the two-sided REFUSAL contract, gated with the EXISTING pos.operate_terminal (LOCKED — no new permission, no reseed, no permission migration): FIRST verify the fixture's device principal holds pos.operate_terminal; if it does NOT, set status blocked_owner (gate ES-42-device-permission-grant) and STOP; otherwise proceed — unauthorized principal 403s and persists NOTHING, and the fixture device sync path still SUCCEEDS end-to-end."
    review_lenses: [fiscal-pos, treasury]
    status: pending
    fix_rounds: 0
    commit: null
    verdict: null
    last_verdict: null
  - id: M5
    title: "Program-wide ratchets + WHOLE-LANE GATE — orphaned-event CI ratchet (baseline the existing 14 dead events BY NAME, fail on the 15th) + projector-emission architecture test as a RATCHET with an enumerated skip-list (A1 has not run; a hard assertion would be a red wall), both demonstrated to bite. Whole-lane review over the integrated branch with full accumulated evidence + both lenses. Report the A0 EXIT STATEMENT."
    review_lenses: [fiscal-pos, treasury]
    status: pending
    fix_rounds: 0
    commit: null
    verdict: null
    last_verdict: null

status: in_progress      # pending | in_progress | blocked_owner | blocked_review | blocked_architecture | blocked_precondition | complete

# TAKEOVER, 2026-08-19. The Codex executor's quota was exhausted mid-STOP-C.
# The parent orchestrator's implementation agent took over in this same
# worktree/branch, delivered M2 fix round 1 (ACCEPTed at round 2,
# M2-round2.md, register commit 325499fe3) and continues with M3. Same rules:
# no push, no stash, no full PHPUnit suite, tests by path, PG recipe
# (apps/api/phpunit-pgsql.xml). Commits continue the Phase 0.1.<seq> series.

blockers:
  # RESOLVED 2026-08-19 — STOP C RULED by the parent orchestrator.
  # Ruling of record: docs/handoff/reviews/es-wave-a0/ORCHESTRATOR-RULING-2026-08-19-m2-stop-c.md
  # (commit bd4139ec3, committed before any code).
  #   - The (company_id, chain_context) partition of the ReceiptHashService
  #     fiscal arm is PULLED FORWARD into M2 as a prerequisite of M2's own
  #     mandated contract (green on the clean two-context v3 fixture).
  #     Delivered in bc692820a; VerifyEventChainCommandTest.php:637-647 flipped
  #     from the M0 characterization assertFalse to assertTrue.
  #   - M4's ES-09 scope is AMENDED by the same ruling: TerminalRegistrySnapshotService
  #     and VirtualAdminFiscalEventService ONLY. It must NOT re-fix
  #     ReceiptHashService, but its two-context append test still asserts the
  #     full end-to-end shape including the receipt-verification arm.
  #   - M2-round1 findings 1, 3 and 4 were fixed in the same round; finding 5
  #     is recorded.
  - "OPEN QUESTION FOR THE PARENT (not a wave blocker) — M2-round1 finding 6: ReceiptReturnRefactorV3Test is 2/9 red on this branch and on its base, failing identically at tests/Feature/POS/ReceiptReturnRefactorV3Test.php:771 (bccomp($independentNet,'10.000') -> -1). It is an INHERITED residual: this wave touches no Z-aggregate code and the ruling excludes it from A0's scope. Which lane owns it? Consequence worth registering: the failure aborts before the post-Z-close pos:verify-chains assertion at :941, which is how the context-flattening defect stayed invisible to the suite."
