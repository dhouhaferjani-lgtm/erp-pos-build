wave: z-sale-branch-decomposition
brief: docs/handoff/CODEX-DISPATCH-z-sale-branch-decomposition-2026-08-18.md
harness: docs/handoff/SELF-REVIEW-HARNESS.md
review_register: docs/handoff/reviews/z-sale-branch-decomposition/
reviewer_model: opus
max_fix_rounds: 5

# Pinned at dispatch 2026-08-21 by the parent orchestrator (session 0578e8d8).
# Brief gate: Codex Sol CHANGES-REQUIRED round 1 (6 findings) -> all applied in the dispatched
# revision -> dispatch authorized. Round-0 precheck: all three defect sites live at base.
# Pinned by the executor at M0 = `git rev-parse dev` at worktree creation, per the brief's
# HARD PREREQUISITES §1. NOTE: the brief's "OPEN AT DISPATCH" item 1 says c6d6308ae on the
# assumption origin/dev == local dev tip; that is stale — the dispatch commit 9d14cb8e1 itself
# landed on local dev after c6d6308ae. §1's normative rule governs. c6d6308ae is VERIFIED in
# 9d14cb8e1's first-parent ancestry (git merge-base --is-ancestor). Drift item D-1 in
# docs/handoff/reviews/z-sale-branch-decomposition/M0-preflight.md.
base_sha: 9d14cb8e1afae9b9cd3e5afa9d8bfac8dd2aabd3
branch: codex/z-sale-branch-decomposition-2026-08-18
worktree: .worktrees/z-sale-decomposition
commit_series: "z-decomp M<m>.<s>:"
ledger_row: C-2   # parent closes at merge with the lane's evidence line

status: complete   # M0-M4 all passed; branch NOT merged, NOT pushed — awaiting the parent

# Obligations this lane CANNOT discharge itself (parent-owned per the brief's §DELIVERABLE),
# tracked here so they cannot be lost between the M1 ruling and the merge.
owes_parent:
  - id: ledger-c2-strike-headline-sentence
    source: M1-ruling.md §"Sub-item rulings" F-2; M2 gate-r1 finding 3
    what: >-
      LEDGER row C-2 still contains the sentence "Headline totals correct". The ruling requires
      it STRUCK — it is false (F-2: signed net_sales == gross_sales on every taxed shift). The
      executor annotated the BRIEF (done, this commit) but LEDGER.md is parent-owned and must not
      be edited from this lane.
    owner: parent orchestrator, at merge
  - id: ledger-c2-name-session-close
    source: M1-ruling.md §"Sub-item rulings" F-1
    what: >-
      LEDGER C-2 and the ticket name only Z_REPORT and X_REPORT. SESSION_CLOSE is a THIRD
      affected signed event type and the ruling applies to it in lockstep; the parent amends both.
    owner: parent orchestrator, at merge
  - id: ticket-residue-column-label
    source: M3 net-to-zero reproduction
    what: >-
      The ticket (:13-14) described the residue as "+2.00 net / +2.00 VAT". Reproduced exactly, it
      is +2.00 net / +2.00 GROSS with VAT cancelling to 0.00. Magnitude and net figure right,
      second column label wrong. The TICKET is not this lane's file to edit and is deferred here.
      M4 gate-r1 finding 6 correction: the BRIEF carried the same error at :111-112, and since the
      lane had already edited the brief under the ruling's annotation authority twelve lines above
      it, leaving that one wrong while fixing the other read as an oversight rather than scope
      discipline. The brief's copy is now annotated in-place (same commit), so this row covers the
      TICKET ONLY — the brief is NOT pristine and the parent should not assume it is.
    owner: parent orchestrator (ticket only; the brief's copy is already annotated)
# M1's gate RULED (Option B) and the parent recorded it at 9e43a4f5a — the M1 blocker is
# DISCHARGED and kept below as `resolved_blockers` for the audit trail, not as an open item.
blockers: []
resolved_blockers:
  - id: z-signed-bytes-versioning
    milestone: M1
    kind: owner/reviewer gate — the wave STOPS here BY DESIGN (brief §M1, §R-1)
    question: >-
      The corrected Z/X per-rate VAT decomposition changes the bytes of SIGNED Z_REPORT and
      X_REPORT events for ordinary sale-only shifts. Under rule 8 (events immutable forever):
      Option A — bump the payload event_version, so old events keep the old (wrong) decomposition
      semantics and new events carry the corrected one, with the version as the discriminator a
      verifier reads; or Option B — correct the computation in place at the current version,
      treating it as a bug fix to a derived field with no version discriminator?
    ruling_channel: >-
      PARENT dispatches the fiscal-pos-reviewer specialist gate over the committed memo. That gate
      rules A vs B. The parent records chosen_option / ruled_by / record in owner_gates above and
      files it as an owner-review item under the standing delegation (P3-M1 R-4 precedent).
    must_also_answer:
      - >-
        F-1 — does the ruling apply to SESSION_CLOSE (a THIRD affected signed event type)?
      - >-
        F-2 — the net_sales headline defect in the same signed bytes. Ticket it, or fold it in
        (a scope expansion the brief currently forbids)? See identity_inversion — answering
        "ticket it" means M2 ships a signed Z whose headline and per-rate rows disagree by the
        full VAT of the shift.
      - >-
        If Option A — is the key-set enforcement work (A4-A7) required, or is a
        stamped-but-unenforced v2 sufficient?
      - >-
        If Option A — who owns the SERVER-BEFORE-DEVICE deploy ordering (A14), and how is it
        reconciled with LEDGER D-3's opposite device-before-server obligation on the same fleet?
      - >-
        Q2 (has any demo/pilot device closed a shift?) is closable by NO server query and is
        expected to remain open. If the ruling proceeds on an undischarged Q2, say so on the record.
    blocks: [M2, M3, M4]
    resume_condition: >-
      owner_gates.z-signed-bytes-versioning.chosen_option recorded in this file. M2 implements
      EXACTLY the ruled option. Resuming without the recorded ruling is a harness violation.
    resolved: >-
      SATISFIED at 9e43a4f5a — chosen_option: B (in-place semantic correction at the current
      event_version), ruled by the parent-dispatched fiscal-pos-reviewer specialist gate, record
      at docs/handoff/reviews/z-sale-branch-decomposition/M1-ruling.md. Q2 accepted open on the
      record; Q1/Q3 are NOT preconditions. M2 resumed under conditions 1-5 of that ruling.

owner_gates:
  z-signed-bytes-versioning:
    question: >-
      New event_version vs in-place semantic correction for the corrected Z/X per-rate
      decomposition (signed-bytes change for ordinary sale-only shifts).
    ruled_by: fiscal-pos-reviewer specialist gate (parent-dispatched 2026-08-21, standing owner delegation)
    chosen_option: B      # in-place semantic correction at the current event_version — NO version bump; UNCONDITIONAL on Q1/Q2/Q3
    record: docs/handoff/reviews/z-sale-branch-decomposition/M1-ruling.md
    owner_review: pending # filed as owner-review item under the standing delegation (P3-M1 R-4 precedent)
  z-device-build-number:
    question: which device build carries this fix (v60-67 stack, D-1/D-3 sequencing)
    disposition: record-never-assign

milestones:
  - id: M0
    title: preflight — base pin, defect present in all 3 sites, citation inventory
    review_lenses: []
    status: passed          # setup-only milestone; review_lenses is empty by design
    commit: 795966c1e       # z-decomp M0.1
    verdict: null           # no bridge review at M0 (review_lenses: [])
    fix_rounds: 0
    artifact: docs/handoff/reviews/z-sale-branch-decomposition/M0-preflight.md
    findings:
      base_check: 3/3 PASS (ancestor, empty diff at creation, all three defect sites live)
      citation_inventory: 23 citations re-derived at base_sha; unresolved = 0
      citation_drift:
        D-1: brief "OPEN AT DISPATCH" item 1 pins c6d6308ae; actual local dev tip is 9d14cb8e1 (no consequence — §1 governs, ancestry verified)
        D-2: endOfDayPreview.ts citations are +4 stale (comment 263-278 -> 267-282; refund 284-295 -> 288-299; SALE DEFECT 297-304 -> 301-308). LEDGER C-2 inherits the same +4.
      third_site: >-
        CONFIRMED INDEPENDENT — aggregateReportData is module-private (zReportService.ts:803,
        no `export`) with exactly one call site (:232, same file); reportApi.ts imports only
        generateZReport (:12) and carries its own loop (:452-504, own vatByRate at :449);
        endOfDayPreview.ts carries a third (:284-309, own vatByRate at :212). Scope stays at 3.
      fourth_vatByRate_site: >-
        buildReceiptData.ts:270-283 also builds a vatByRate — OUT OF SCOPE and ALREADY CORRECT:
        it consumes CartItem[] (:255) not offline_receipts.lines[], shape {taxable,tax}, feeds
        the PRINTED receipt (:318-324), and :274 already derives taxable = bcsub(line_total,
        tax_amount, decimals). Corroborates that line_total is GROSS on a sale line.
      regression_set: >-
        Tier 1 (6 suites): zReportService.test.ts, zReportService.cashRounding.test.ts,
        endOfDayPreview.test.ts, api/__tests__/reportApi.test.ts,
        api/__tests__/reportApi.localTimestamps.test.ts, refundReportingEndToEnd.test.ts.
        Tier 2 (8 downstream .tsx/.ts suites) + Tier 3 (6 writer-side fixture suites) —
        full list in the M0 artifact. Tests BY PATH only.
      env: worktree had no node_modules; `pnpm install --frozen-lockfile` run at M0 (exit 0). No tests executed at M0.
  - id: M1
    title: signed-bytes decision memo (memo-quality bridge review, then STOP blocked_review)
    review_lenses: [fiscal-pos]
    owner_gate: z-signed-bytes-versioning
    status: passed          # memo ACCEPT (round 5) + the gate RULED; blocker discharged at 9e43a4f5a
    commit: 0edbf402b       # z-decomp M1.1 (memo); amended by the round-1 fix round, see fix_commit
    fix_commits: [63b756784, b13536a76, faa0a3ec1, 464eea67a]  # M1.2 r1, M1.3 r2, M1.4 r3, M1.5 r4 fixes. The round-5 P3 closure is THIS commit (self-reference impossible at write time).
    verdict: docs/handoff/reviews/z-sale-branch-decomposition/M1-round5.md   # ACCEPT. Exists at this commit — this field never forward-references.
    verdict_history:
      - round: 1
        register: docs/handoff/reviews/z-sale-branch-decomposition/M1-round1.md
        result: ACCEPT (exit 0) — 0 P1, 1 P2, 4 P3; all five closed anyway (one-way-door consumer)
      - round: 2
        register: docs/handoff/reviews/z-sale-branch-decomposition/M1-round2.md
        result: CHANGES-REQUIRED (exit 2) — 1 P1 (NF525 claim wrong), 1 P2 (identity inversion), 3 P3
      - round: 3
        register: docs/handoff/reviews/z-sale-branch-decomposition/M1-round3.md
        result: >-
          CHANGES-REQUIRED (exit 2) — 1 P1 (the round-2 REPLACEMENT NF525 claim was also false),
          2 P2 (F-2's net_sales IS archive-visible as <VentesNettes>; the A10 prose had broken the
          Option-A cost table so A11-A14 rendered as a run-on paragraph), 2 P3 (transaction-discount
          caveat; YAML verdict forward-reference)
      - round: 4
        register: docs/handoff/reviews/z-sale-branch-decomposition/M1-round4.md
        result: >-
          CHANGES-REQUIRED (exit 2) — 0 P1, 2 P2 (the Option-A cost table was STILL broken — a
          blank line orphaned A11-A14 as a delimiter-less block; two surviving cost claims still
          charged Option A for zero archive work), 2 P3 (the absolute "reaches NO archive surface"
          over-corrected — <HashFiscal> IS a hash over the changed bytes; YAML forward-reference)
      - round: 5
        register: docs/handoff/reviews/z-sale-branch-decomposition/M1-round5.md
        result: >-
          ACCEPT (exit 0) — 0 P1, 0 P2, 3 P3. MEMO-QUALITY GATE SATISFIED. The three P3s
          (Option-A cost figure double-counted A7 and mis-stated A12's count; three surviving
          absolute NF525 framings; a new downstream defect) were closed anyway in the M1.6 commit
          rather than shipped with a ticket — no memo CONCLUSION changed.
    last_verdict: ACCEPT (round 5, exit 0) — 0 P1, 0 P2, 3 P3; all three closed in the M1.6 commit
    fix_rounds: 5
    memo: docs/handoff/reviews/z-sale-branch-decomposition/M1-signed-bytes-decision.md
    no_production_code: true
    findings:
      F-1: >-
        SESSION_CLOSE is a THIRD affected signed event type (zSessionAuthoring.ts:440 carries the
        same vat_breakdown). The brief, the ticket and LEDGER C-2 name only Z_REPORT and X_REPORT.
        The ruling must say whether it applies to SESSION_CLOSE.
      F-2: >-
        🚨 The brief's "headline totals are correct" premise is FALSE. cartTotals.ts:36 defines
        subtotal = Sum(line_total) = GROSS (docblock :7), and with no transaction discount
        total === subtotal (:40,:52,:66); receiptService.ts:145-158 sums the same into the row's
        subtotal (:547); zReportService.ts:900 sums receipt.subtotal as netSales. Therefore signed
        net_sales == gross_sales on any taxed shift, and subtotal + tax_amount != total on every
        sale row (the identity refundReportingEndToEnd.test.ts:292 asserts for refunds).
        Independently recorded as the "unnamed 5th defect" of audit R-01
        (docs/superpowers/audits/2026-08-05-production-v1-readiness.md:185, status open).
        NOT FIXED HERE (rule 4 — the brief declares the headline out of scope). Needs a
        parent/owner sequencing decision; it also changes Option A's arithmetic (a v2 that fixes
        only vat_breakdown would need a v3 when net_sales is fixed).
      F-3: >-
        A FOURTH gross-as-net site exists in zReportService.ts itself — buildReceiptSnapshots()
        taxByRate, defect at :1015-1017. NOT in the signed bytes and NOT in the Z hash
        (receipt_snapshots is a sibling of report_data, :504 vs :507; computeZReportHash hashes
        report_data only). Recorded, not fixed.
      F-4: >-
        vat_breakdown has NO structural validation on either side (FiscalEventEngine.ts:3748-3760,
        FiscalPayloadConstraintValidator.php:764-771 never iterate it), and the Z/X key sets are
        not covered by the PHP<->TS drift gate. Downstream finding, separate lane.
      legacy_chain: >-
        The fix ALSO changes the legacy z_reports hash chain — computeZReportHash hashes the whole
        report_data, which contains vat_breakdown (zReportService.ts:995, hashed :445-451). That
        chain has NO event_version, so Option A cannot make it self-describing. Grand totals are
        UNAFFECTED (:459-465 uses headline scalars only).
      precedent: >-
        77283d2a5 (2026-08-01) corrected the identical expression class on the REFUND branch of the
        same three files, changing the same signed vat_breakdown field, IN PLACE at v1 with no
        version bump and no registry/payload file touched. Real precedent for Option B; narrower
        blast radius (v4 refunds were brand-new), so it is evidence, not a ruling.
      F-6: >-
        M4 gate-r1 finding 3 — the HEADLINE accumulators sitting inside the SAME loop iteration as
        the corrected sale branch still call bcadd with NO scale argument, taking decimal.ts:22's
        default of 3, and they feed the SIGNED gross_sales / net_sales / tax_amount:
        zReportService.ts:899-901 and reportApi.ts:484-486 (endOfDayPreview.ts:235-237 likewise,
        unsigned). This is the IDENTICAL R-3 defect class as the one this lane fixed, on signed
        fields, and it is REACHABLE not theoretical — receipt.subtotal carries genuine sub-cent
        precision on cash-rounded EUR receipts (receiptService.cashRounding.test.ts:199-211
        persists line_total '9.997'), so two such receipts diverge by one cent between scale-2 and
        scale-3 accumulation, exactly the arithmetic the new R-3 guards demonstrate.
        CORRECTLY NOT FIXED HERE — brief NOT-IN-SCOPE + ruling conditions 1 and 4 put the headline
        out of this lane. RECORDED AGAINST F-2 SO THE SIBLING LANE INHERITS IT: those three lines
        are F-2's entire subject, and a lane that rewrites them without knowing the scale defect
        rides along would ship a corrected headline still accumulating at the wrong scale.
      option_A_nf525_not_visible: >-
        This memo was wrong about NF525 TWICE, in opposite directions; the verified position was
        reached at gate-r3. (a) gate-r2 finding 1 (P1): the original claim that a v2 Z is
        regulator-visible was FALSE — <VersionEvenement> is emitted at exactly one site
        (Nf525XmlBuilder.php:598), only inside addQuarantineSection() (:548), fed solely by the
        quarantine table and by fiscal_events rows with integrity_status <> 'verified' OR
        payload_parse_status = 'failed'. (b) gate-r3 finding 1 (P1): the REPLACEMENT claim that the
        vat_breakdown VALUES reach the archive GRANDTOTAL was ALSO FALSE —
        mapCanonicalZReportGrandTotal() puts vat_breakdown into periodTotals
        (Nf525DataProvider.php:1260), but Nf525XmlBuilder::addGrandTotals() (:319-350) emits ONLY
        <VentesBrutes> (gross_sales, :335) and <Taxe> (tax_amount, :336); addZReports() (:296-302)
        emits five scalars and no breakdown; the ticket-level <VentilationTVA> (:160-169) is fed
        per-receipt from the SALE_RECEIPT view, not from the Z. NET POSITION: this lane's per-rate
        correction has no INTERPRETABLE NF525 export consequence under EITHER option, and A9 is not
        an Option-A cost. (gate-r4 finding 3: ONE exported value does change — <HashFiscal>,
        Nf525XmlBuilder.php:291, carrying ZReportProjection.php:114's fiscal_hash = the SHA-256 over
        canonical_bytes. A hash differentiates nothing, so this does not restore archive
        self-description as an Option-A argument; the claim is qualified rather than absolute
        because this memo's NF525 reasoning was overstated in both directions before.) Archive self-description is STRUCK as a flip condition. What the archive DOES carry
        wrong is F-2's net_sales, exported as <VentesNettes> (Nf525XmlBuilder.php:299) for every
        taxed shift — see identity_inversion.
      identity_inversion: >-
        M1 gate-r2 finding 2 (P2, closed in round 3 as memo §1.3a): the fix MOVES the payload's
        internal contradiction rather than removing it, under BOTH options. Today Sum(net_amount)
        == net_sales (both gross) and Sum(gross_amount) != gross_sales. After this fix with F-2
        unfixed, Sum(gross_amount) == gross_sales but Sum(net_amount) != net_sales, off by the
        shift's FULL VAT. Operator-visible (ZReportModal.tsx:158-159 renders the headline directly
        above the per-rate table at :176-193), server-visible (ZReportProjection.php:149 vs :154)
        and — gate-r3 finding 2 — REGULATOR-visible: the headline reaches the NF525 JET export as
        <VentesNettes> (Nf525XmlBuilder.php:299) from the projection's verbatim passthrough. So the
        asymmetry is: the per-rate values this lane FIXES reach no interpretable archive surface, while the
        net_sales it does NOT fix is archived wrong for every taxed shift. gate-r3 finding 4: on a
        DISCOUNTED shift the gross column stays contradictory too (lines[] carry the
        pre-transaction-discount line_total, receiptService.ts:537, while the discount is
        header-only, :549/:555-557), so post-fix Sum(gross_amount) - gross_sales = discount. The
        honest summary is "one contradiction moves to the net column and, on discounted shifts, a
        second is retained in the gross column". The analogous identity
        Sum(vat_breakdown[].net_amount) == subtotal is a HARD
        server-enforced invariant for SALE_RECEIPT (FiscalPayloadConstraintValidator.php:1097-1098,
        enforced :1139-1166); the Z family escapes it only via F-4. Ruling question 3 must be
        answered knowing this.
      F-5: >-
        gate-r5 finding 3 (P3), verified independently: NF525 GrandsTotaux period totals are always
        0.00 for canonical Z reports. mapCanonicalZReportGrandTotal() (Nf525DataProvider.php:1255-1262)
        builds periodTotals with exactly five keys (receipt_totals, refunds_totals, voids_totals,
        vat_breakdown, payment_method_totals) and NO top-level gross_sales/tax_amount — the real
        figures are nested at receipt_totals.* — while addGrandTotals() (Nf525XmlBuilder.php:335-336)
        reads periodTotals['gross_sales'] and ['tax_amount'], so both fall through to the '0.00'
        default. The sibling mapGrandTotal() (:1286-1298) passes period_totals verbatim, so the two
        mappers produce DIFFERENT SHAPES for one DTO. Does not change this memo's conclusions
        (the builder still never renders vat_breakdown) but falsifies "at least gross_sales reaches
        the archive through the grand totals". NOT FIXED HERE (rule 4) — belongs to whichever lane
        picks up F-2/F-4.
      option_A_real_cost: >-
        gate-r4 finding 2: Option A's real cost is NINE code sites across device + server
        (A1-A3, A5-A8, with A4/A7 conditional on whether the version is ENFORCED or merely
        stamped), plus 9 hardcoded-1 assertions across 5 files (A12) and a golden fixture that must
        be authored from scratch (A13, because no Z/X byte-stability vector exists). Corrected at
        gate-r5: the count is EIGHT code sites (6 unconditional: A1, A2, A3, A5, A6, A8; plus
        A4/A7 conditional) — an earlier "9" double-counted A7. A9/A10 are NOT costs — the archive
        layer requires zero work under either option — and A14 is a rollout constraint, not code.
        Earlier revisions said "~13 sites ... plus an archive format", which inflated the
        disfavoured option in the recommendation's own direction.
      option_A_deploy_ordering: >-
        M1 gate-r1 finding 1 (P2, closed in round 2 as memo item A14): Option A creates a
        SERVER-BEFORE-DEVICE deploy obligation that does not exist today. A device build authoring
        v2 that reaches a terminal before the API carrying the SUPPORTED_VERSIONS change quarantines
        100% of that terminal's Z/X at StrictCanonicalParser.php:628-634, on an append-only device
        chain that cannot be re-authored. It points OPPOSITE to LEDGER D-3's device-before-server
        obligation on the same fleet. Option B has no ordering constraint at all.
      option_A_highest_risk: >-
        FiscalEventPayloadRegistry.php:119-127 — SUPPORTED_VERSIONS holds only SALE_RECEIPT.
        Omitting a Z/X entry quarantines 100% of Z and X events at StrictCanonicalParser.php:628-634.
      history_evidence: >-
        Repo supports "no signed Z/X history is demonstrable from the codebase, and none is seeded,
        fixtured or migrated"; it does NOT support "none exists". Four open operator queries
        (Q1 staging fiscal_events counts, Q2 device-local unsynced chains — unclosable by any
        server query, Q3 the abandoned §Z campaign device, Q4 the tenant-#1 premise itself).
      recommendation: >-
        Option B (in-place), CONDITIONAL on Q1 and Q3 returning empty, with F-1 folded in so
        SESSION_CLOSE moves with Z and X. Q2 (device-local unsynced chains) is closable by NO
        server query and is EXPECTED to remain open: the recommendation still stands at Option B
        on an open Q2, and only a POSITIVE Q2 answer (a device actually found with closed shifts)
        is a flip condition — but accepting an open Q2 is the ruling gate's call, on the record.
        Advisory only — stated because brief M1 item 5 asks for a recommendation. Flip conditions
        are in memo §6; one earlier flip condition (NF525 archive self-description) was WITHDRAWN
        at gate-r2 as factually wrong.
  - id: M2
    title: three sale-branch fixes + scale args, real-writer red-first per consumer
    review_lenses: [fiscal-pos, general]
    status: passed
    commit: d1afcba9d
    verdict: docs/handoff/reviews/z-sale-branch-decomposition/M2-round1.md
    last_verdict: ACCEPT (round 1, exit 0) — 0 P1, 0 P2, 4 P3; all four closed in the M2.2 commit
    fix_rounds: 1
    p3_closures: >-
      gate-r1 finding 1: endOfDayPreview.ts:280-282 still claimed "The SALE branch is left
      byte-identical ... not in this wave's scope" twenty lines above the corrected sale branch.
      Struck (comment-only; R-2's "do not tidy the refund branch" is not engaged, and a stale
      "untouched" note is exactly what gets a fix re-reverted later).
      finding 2: the handback report was stale at M2 (title, ruled option, empty R-4 register, no
      M2 section) and the red output existed nowhere verbatim. Report brought current with the
      red pasted verbatim, the final R-4 register, and the M2/M3 sections; the old STOP paragraph
      is RETAINED as superseded text so the handback shows the stop was real, not back-filled.
      finding 3: the brief's "which the writer already stores correctly" justification is FALSE
      and the ruling ordered it annotated — done in-place at §SCOPE and in the NOT-IN-SCOPE table
      (the instruction stands, but as a SCOPE BOUNDARY, not a correctness claim). The LEDGER half
      is parent-owned and is now tracked under `owes_parent`.
      finding 4: process — the tree was dirty with in-flight M3 work while M2 was under review.
      M3 is committed separately BEFORE its own gate; this commit deliberately excludes it.
    ruled_option_implemented: >-
      B — in-place semantic correction at the current event_version. ZERO registry / payload /
      parser / validator / projection / archive changes (ruling condition 1); verified by the
      whole-branch file list at M4.
    red_first_evidence: >-
      NEW real-writer suite apps/pos/src/lib/offline/__tests__/saleReportingEndToEnd.test.ts,
      run BEFORE the three fixes: 4 failed | 1 passed. The one PASS is the writer-premise test
      (the real createOfflineReceipt persists line_total = GROSS 12.00 with tax_amount 2.00
      extracted) — the premise had to be green or the reds would be measuring the wrong thing.
      The four REDS, one per consumer plus the ruling's lockstep assertion, all showed the same
      defect on a 12.00-gross / 2.00-VAT line -- net_amount '12.00' (gross-as-net) and
      gross_amount '14.00' (gross+vat) against the hand-computed 10.00 / 12.00.
    revert_replay: >-
      PER SITE, not merely per commit (stash-free, scratchpad backups). Reverting ONLY
      zReportService's sale branch -> 2 failed (consumer-1 Z + the SESSION_CLOSE byte-identity
      assertion, which reads the same aggregateReportData output — exactly the lockstep the
      ruling's condition 2 asserts). Reverting ONLY endOfDayPreview -> 1 failed (consumer-2).
      Reverting ONLY reportApi -> 1 failed (consumer-3). Each restored; full suite green after.
    session_close_lockstep: >-
      Ruling condition 2 SATISFIED by a real assertion, not an assumption: the test captures the
      ACTUAL AuthorZSessionCloseInput passed to appendZSessionCloseAndZReport, feeds it to the
      REAL buildZReportPayload and buildSessionClosePayload, and asserts
      JSON.stringify(vat_breakdown) is byte-identical between them AND equals the corrected
      decomposition (a byte-identity check alone would pass on two wrong payloads).
    fixture_corrections: >-
      TWO, both in endOfDayPreview.test.ts, both named with old -> new and writer justification.
      :37 line_total '8.40' -> '10.00' and :50 line_total '16.81' -> '20.00'. Justification:
      cartStore.recalcLineTotal() writes GROSS and EXTRACTS tax; on a 10.00 total at 19 pct the
      writer's values are line_total 10.00 / tax_amount 1.60 (10.00 - 10.00/1.19). DECISIVE
      EVIDENCE that the fixture (not the assertion) was wrong: the expected values at :83-85
      (net 25.21 / vat 4.79 / gross 30.00) are UNCHANGED — the old code only reached them
      because the NET-valued input was wrong in exactly the compensating way.
      Scope check: these are the ONLY two sale fixtures any decomposition assertion relies on
      (endOfDayPreview.test.ts:80-84 is the only such assertion in the file; :127 asserts empty;
      zReportService.test.ts:1045 is a REFUND row at tax_rate 0; reportApi tests assert []).
      REGISTERED-NOT-CORRECTED, complete across ALL files (M3 gate-r1 finding 2 — the earlier
      enumeration listed only endOfDayPreview rows and omitted the ones the brief and M0 named):
        endOfDayPreview.test.ts :144,:193,:247,:323,:390,:450,:488,:523,:555,:622
        zReportService.test.ts :123 (line_total '42.00' = NET on a 50.00/8.00 receipt — the
          brief's own §M2 masking citation) and :452 ('84.03' on 100.00/15.97)
        reportApi.localTimestamps.test.ts :136, :143
        zReportService.cashRounding.test.ts :166 (line_total := input.total)
      All feed expected_cash / payment / timestamp assertions, never a per-rate decomposition,
      so none masks anything this lane asserts (verified: zReportService.test.ts touches
      vat_breakdown only at :1045, a refund row at tax_rate 0; the reportApi tests assert []).
      Left uncorrected under rule 4 — correcting them would be a large diff with no assertion
      benefit. STANDING RISK, recorded deliberately: if a later change adds a per-rate assertion
      in one of those files against the surviving NET-valued fixture, it will re-derive a wrong
      expectation and re-establish exactly the masking pattern this lane exists to kill. The
      antidote is the real-writer suite — any new per-rate assertion should be written there,
      not against a hand-authored lines JSON.
      DISCLOSED RESIDUAL: the corrected rows still carry a NET `subtotal` column ('8.40'/'16.81')
      where the real writer would store GROSS — that is F-2's defect, deliberately NOT corrected
      here (ruling condition 4 keeps F-2 out of this lane).
    gates: >-
      pnpm typecheck clean. pnpm lint:ratchet — @autoerp/pos HELD at 84 (zero warning growth,
      the gate this brief names). @autoerp/web shows 6449 -> 6452 (+3) and is INHERITED, proven
      structurally rather than asserted: `git diff --stat base..HEAD -- apps/web` is EMPTY and
      `git status --porcelain -- apps/web` is EMPTY, so apps/web is byte-identical to base and
      cannot have been affected by this lane.
    regression: >-
      Tier 1 (6 suites + the new one) 83 passed. Tier 2 (8 downstream suites) 80 passed.
      Tests BY PATH only; no full suite run.
  - id: M3
    title: real-writer E2E matrix (mixed-rate, sale+refund net-to-zero) + fixture-correction register
    review_lenses: [fiscal-pos, general]
    status: passed
    commit: 5b805657c
    verdict: docs/handoff/reviews/z-sale-branch-decomposition/M3-round1.md
    last_verdict: ACCEPT (round 1, exit 0) — 0 P1, 1 P2, 7 P3; all eight closed in the M3.2 commit
    fix_rounds: 1
    matrix: >-
      Both required cases added to the real-writer suite, and BOTH PROVEN MEANINGFUL by
      reverting the sale branch and observing the predicted wrong values (not merely by
      passing after the fix).
    mixed_rate: >-
      Two rates on ONE receipt through the real writer: 12.00 @ 20 pct (vat 2.00, net 10.00) +
      10.50 @ 5 pct (vat 0.50, net 10.00), receipt gross 22.50, asserted across all THREE
      consumers. The two rates deliberately share a net of 10.00 while differing in VAT, so a
      bucket leaking across rates lands on a plausible-looking number rather than an obviously
      wrong one. Pre-fix it reported rate-5 net 10.50 / gross 11.00 and rate-20 net 12.00 /
      gross 14.00 — BOTH rates independently wrong.
    net_to_zero: >-
      A fully-refunded taxed sale — real createOfflineReceipt followed by real
      createRefundReceipt reversing THAT row (real refund_intents seed, real v4 payload) —
      nets every per-rate component to exactly 0.00 across all three consumers. Pre-fix it
      leaves net +2.00 / gross +2.00 / vat 0.00, which is the ticket's live symptom reproduced
      to the cent.
    ticket_wording_correction: >-
      The reproduction exposes a small error in the TICKET's own description
      (docs/superpowers/tickets/2026-08-01-device-z-sale-branch-gross-as-net.md:13-14) and in
      the brief's SCOPE echo of it: both say the residue is "+2.00 net / +2.00 VAT". The actual
      residue is +2.00 NET and +2.00 GROSS with VAT cancelling to 0.00 — the buggy sale adds
      (net 12.00, vat 2.00, gross 14.00) while the corrected refund subtracts (net 10.00,
      vat 2.00, gross 12.00), so the VAT column is the one component that DOES cancel. The
      magnitude and the net figure are right; the second column label is wrong. Recorded, not
      edited (the ticket is not this lane's file to change).
    fixture_register: see M2.fixture_corrections — no further fixtures were corrected at M3.
    gates: >-
      M3 gate-r1 finding 3 — this entry was missing (M2 had one) and the M3 commit message
      recorded only typecheck + Tier-1, though the house rules make lint:ratchet binding EVERY
      milestone. Run and recorded: pnpm typecheck CLEAN; pnpm lint:ratchet @autoerp/pos HELD at
      84 (zero growth). @autoerp/web 6449 -> 6452 (+3) INHERITED — every changed path in this
      branch is apps/pos/** or docs/**, with no eslint config, no baseline file, no package.json
      and no shared package touched, so apps/web lints identically to base. Tier-1 85 passed,
      Tier-2 80 passed, BY PATH only; no full suite.
    record_ordering_disclosure: >-
      M3 gate-r1 finding 4 — the M3 milestone record (including the pre-fix red values it cites)
      was committed in 0cf1c7835 (M2.2), BEFORE the M3 implementation commit 5b805657c, and the
      M3 commit touched no YAML. So commit order does NOT corroborate the "proven meaningful by
      reverting" claim; the reviewer re-derived the numbers independently and confirmed them.
      Disclosed rather than quietly re-committed. Root cause: the YAML was updated while M3 was
      still in the working tree, then swept into the M2 fix-round commit. Corrected here — the
      M3 commit SHA is now filled and this closure commit carries the evidence that follows the
      artifact rather than preceding it.
    i18n: >-
      No user-facing string changed in this lane (brief M3): the diff touches aggregation
      expressions and tests only. apps/pos has no `ar` tree and none was created.
  - id: M4
    title: whole-lane gate over the integrated branch
    review_lenses: [fiscal-pos, general]
    status: passed
    commit: null            # self-reference impossible at write time — this IS the closure commit
    verdict: docs/handoff/reviews/z-sale-branch-decomposition/M4-round2.md   # ACCEPT; exists at this commit
    verdict_history:
      - round: 1
        register: docs/handoff/reviews/z-sale-branch-decomposition/M4-round1.md
        result: >-
          CHANGES-REQUIRED (exit 2) — 0 P1, 1 P2 (the whole-branch census was numerically wrong in
          the very artifact M4's evidence contract names as the anti-faking antidote), 5 P3
          (R-3 guard covered only the UNSIGNED consumer; the in-loop headline accumulators carry
          the same unfixed scale defect on SIGNED fields; "truncation" vs Big.RM=1 rounding;
          stale `updated:`; owes_parent implied the brief was pristine when the lane had edited it)
      - round: 2
        register: docs/handoff/reviews/z-sale-branch-decomposition/M4-round2.md
        result: >-
          ACCEPT (exit 0) — 0 P1, 1 P2, 2 P3. WHOLE-LANE GATE SATISFIED. The gate re-ran all three
          REPRODUCE commands rather than reading the numbers (18/13/5 and 15 lines, exact) and
          traced the two new signed guards for vacuity (they cannot pass for the wrong reason —
          the precision row is the only receipt in a per-test SqliteTestAdapter). All three
          findings closed in THIS commit; the gate explicitly directed NOT to re-gate for them,
          because another round re-breaks the file census by one and buys nothing.
    fix_rounds: 1
    gates: >-
      pnpm typecheck CLEAN. pnpm lint:ratchet @autoerp/pos HELD at 84 (zero growth) with all
      three R-3 guards added. @autoerp/web 6449 -> 6452 (+3) INHERITED — every changed path is
      apps/pos/** or docs/**, no eslint config, baseline, package.json or shared package touched,
      so apps/web lints identically to base. Tier-1 88 passed BY PATH; no full suite.
    r3_coverage_completed: >-
      M4 gate-r1 finding 2 — the R-3 guard existed for ONE consumer, and it was the only one whose
      output is NOT SIGNED. Dropping the scale args on the signed paths left all 86 Tier-1 tests
      green. Two further guards added, both in saleReportingEndToEnd.test.ts (no new file in the
      census): one over the SIGNED Z (which also feeds SESSION_CLOSE) and one over the SIGNED X.
      Both VERIFIED TO BITE — reverting the scale arguments in zReportService.ts and reportApi.ts
      fails them with "expected '20.01' to be '20.00'".
      COMPLETED AT M4 gate-r2 finding 2: the guards initially asserted net_amount ONLY, leaving the
      GROSS accumulator's scale argument unguarded on every consumer — dropping it alone yields
      10.004 + 10.004 = 20.008 -> '20.01' in the SIGNED vat_breakdown[].gross_amount with all 88
      Tier-1 tests still green. gross_amount is now asserted in all three guards, and the
      gross-only revert is VERIFIED TO FAIL them. Coverage is now complete PER-ACCUMULATOR, not
      merely per-consumer. Residual, stated rather than glossed: the `vat` accumulator is
      structurally identical but does not drift on this fixture (0.001 + 0.001 = 0.002 -> '0.00'
      either way), so it is guarded by inspection and by the structural-identity requirement, not
      by an assertion that can distinguish the two.
      The precision row is inserted by direct SQL and is the ONE non-writer-produced fixture in
      that file, labelled as such: the drift is invisible to any well-formed scale-2 row because
      every consumer re-formats through bcformat at emission, and sub-cent line_totals are exactly
      what the real cash-rounding writer produces (receiptService.cashRounding.test.ts:202).
      Decomposition semantics remain proven by the real-writer tests; this fixture proves SCALE.
    whole_branch_file_list: >-
      Stated as an INVARIANT so it cannot go stale again (M4 gate-r2 finding 1 — the round-1
      correction anchored to "the closure commit", a commit that had not happened yet, and was
      therefore wrong by exactly the mechanism it was fixing):
        apps/pos = **5, FIXED** — 3 production (zReportService.ts, endOfDayPreview.ts,
          reportApi.ts) + 2 test (saleReportingEndToEnd.test.ts new, endOfDayPreview.test.ts).
          This count does not change again; the lane touches no other apps/pos file.
        docs/ = the brief + the progress YAML + the M0 preflight artifact + the M1 memo +
          the M1 ruling + **one register per gate round actually run**.
      Rounds run: M1 x5, M2 x1, M3 x1, M4 x2 = 9 registers. So docs = 5 + 9 = **14**, and the
      whole branch = **19 files** at THIS closure commit. Any further gate round adds exactly one
      docs file and one to the total — the formula, not the number, is the durable statement.
      REPRODUCE (the durable part — run these, do not trust the prose):
        `git diff --name-only 9d14cb8e1..HEAD | wc -l`          -> 19
        `git diff --name-only 9d14cb8e1..HEAD | grep -c '^docs/'`    -> 14
        `git diff --name-only 9d14cb8e1..HEAD | grep -c '^apps/pos/'` -> 5
    production_diff_size: >-
      CORRECTED at M4 gate-r1 finding 1 — the earlier "18 lines / three six-line blocks" was
      wrong. Re-derived: **15 added non-comment lines = three IDENTICAL FIVE-line blocks**
      (one gross local, one bcsub-derived net, three scaled accumulators), differing only in each
      file's own scale identifier (`decimals` vs `scale`). Everything else in the production diff
      is comment. REPRODUCE:
      `git diff 9d14cb8e1..HEAD -- <the 3 production files> | grep -E '^\+' | grep -vE '^\+\+\+' | grep -vE '^\+[[:space:]]*(//|$)' | wc -l`
      -> 15.
    negatives_proven:
      - >-
        NO apps/api/** change at all — `git diff --name-only base..HEAD | grep ^apps/api/` is
        EMPTY. Option B required none, and ruling condition 1 forbade one.
      - >-
        NO registry / payload / parser / validator / projection / archive change — the same grep
        over Registry|Payload|Parser|Validator|Projection|Nf525|zSessionAuthoring is EMPTY.
      - >-
        HEADLINE AGGREGATION UNTOUCHED — no `grossSales|netSales|taxAmount = bcadd` line appears
        in the diff at all.
      - >-
        REFUND BRANCHES UNTOUCHED AS CODE — no added/removed non-comment line contains a
        `bcabs(` call; the two refund derivations still read
        `const lineGross = bcabs(line.line_total ?? '0', decimals)` at zReportService.ts:868 and
        reportApi.ts:463. R-2's "do not tidy them" honoured. (The only refund-side edit in the
        branch is the endOfDayPreview COMMENT struck per M2 gate-r1 finding 1 — a now-false
        sentence claiming the sale branch was out of scope.)
      - >-
        WRITER SEMANTICS UNTOUCHED — receiptService.ts, cartStore.ts and cartTotals.ts do not
        appear in the diff. F-2 stays out of scope (ruling condition 4).
      - >-
        NO SIGNED-EVENT BACKFILL — no migration, no seeder, nothing that rewrites history.
        Rule 8 honoured: no event renamed, deleted or rewritten; nothing added a version either
        (that was Option A, not ruled).
    ledger_c2_evidence_line: >-
      DUPLICATED HERE DELIBERATELY (M4 gate-r2 finding 3): the session report that normally
      carries this is gitignored under house rule 15, so it is NOT part of the branch and does not
      survive pruning of the worktree. The parent closes C-2 from THIS field.
      ---
      C-2 CLOSED. Defect confirmed live at 9d14cb8e1 in all three declared sites
      (zReportService.ts:907-914, endOfDayPreview.ts:301-308 — the row's own :297-304 was +4 stale
      — reportApi.ts:491-496). Corrected per the z-signed-bytes-versioning ruling (OPTION B,
      in-place at the current event_version; M1-ruling.md): one pattern applied three times,
      net = gross - vat at the explicit currency scale, 15 added non-comment production lines =
      three identical five-line blocks, no bcabs on the positive-signed sale rows. Proven by a
      real-writer end-to-end suite (saleReportingEndToEnd.test.ts) that drives the actual writer
      into real SQLite and reads the row back through ALL THREE consumers, red-first per consumer
      before the fix (net 12.00 / gross 14.00 -> 10.00 / 12.00), with per-SITE revert-replay.
      SESSION_CLOSE covered in lockstep (ruling condition 2), asserted byte-identical to Z_REPORT
      on the same close. The ticket's live symptom is closed: a fully-refunded taxed sale now nets
      the per-rate buckets to exactly zero, where pre-fix it left net +2.00 / gross +2.00.
      Two masking fixtures corrected (R-4) with expected values UNCHANGED. R-3 scale arguments
      added and guarded on all three consumers and all drifting accumulators.
      Gates: typecheck clean; lint:ratchet @autoerp/pos held at 84.
      RECORDED, NOT FIXED (rule 4 / ruling condition 4): F-1 SESSION_CLOSE was a third affected
      signed type the row never named; F-2 the row's "headline totals correct" sentence is FALSE
      (signed net_sales == gross_sales on every taxed shift) and MUST BE STRUCK; F-3 a fourth,
      unsigned gross-as-net site at zReportService.ts:1015-1017; F-4 vat_breakdown has no
      structural validation on either side; F-5 NF525 GrandsTotaux exports 0.00 period totals for
      every canonical Z; F-6 the in-loop HEADLINE accumulators carry the same unfixed R-3 scale
      defect on SIGNED fields — F-2's sibling lane must inherit it.
      SHIPS VIA A DEVICE BUILD, NOT THIS MERGE (R-6 / D-1 / D-3), and the ruling's REVERSAL WINDOW
      CLOSES AT BUILD ROLLOUT, NOT AT MERGE.
    r6_device_build_sequencing: >-
      RECORDED, NEVER ASSIGNED (the build number is the device/deploy operator's call and O-22
      is OPEN). This fix is DEVICE-SIDE ONLY and therefore reaches a terminal ONLY via a device
      build; it joins the cumulative stack LEDGER D-1: v60/61 -> v62 -> v63 -> v66 -> v67
      (v67 subject to owner ruling O-22, LEDGER.md:34). LEDGER D-3 remains binding: the SV-11
      device build must deploy fleet-wide BEFORE the SV-9 migration reaches any origin/dev
      promotion — this lane joins the same fleet rollout and must not be sequenced in a way
      that breaks that. Option B introduces NO server-before-device obligation (the A14 hazard
      the memo costed applies only to Option A, which was not ruled), so this lane adds no new
      ordering constraint of its own.
      RULING CONDITION 5 — THE REVERSAL WINDOW CLOSES AT DEVICE BUILD ROLLOUT, NOT AT MERGE: an
      owner override back to Option A before any terminal receives the fixed build costs
      essentially nothing; after rollout a v2 would separate "correct" from "ambiguous" rather
      than "wrong" from "correct" — strictly worse, permanently. The parent should surface this
      to the owner BEFORE the build ships, not before the merge.
      SEQUENCING PREFERENCE from the ruling (prefer, do not block): bind this fix and the F-2
      sibling lane to ONE device build. C-2 may ship alone if F-2 slips, but a tenant onboarding
      on a C-2-without-F-2 build is an owner-visible residual — internally inconsistent Zs
      (per-rate rows correct, headline net_sales wrong by the full VAT) plus a wrong NF525
      <VentesNettes> from event one.

updated: 23dda4501   # short HEAD at write time; this closure commit is its child (self-reference is impossible — see whole_branch_file_list for the invariant form)
