wave: ui-wave0
brief: docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md
harness: docs/handoff/SELF-REVIEW-HARNESS.md
# Pre-dispatch brief gates (content already PASSED at r3); per-milestone registers land here.
review_register: docs/handoff/reviews/ui-wave0/
brief_gates:
  - docs/superpowers/reviews/2026-08-11-wave0-brief-gate-r1.md
  - docs/superpowers/reviews/2026-08-11-wave0-brief-gate-r2.md
reviewer_model: opus
max_fix_rounds: 5

# OWNER RULING 2026-08-17 supersedes the original fresh-origin rule: use this exact curated dev tip.
# Do NOT fetch and repin.
base_sha: d682b38ec9761a917b9716428091a482745795f6
# Branch codex/ui-wave0-2026-08-11 in worktree .worktrees/ui-wave0, created from base_sha.
# NOT merged, NOT pushed: the parent Claude session performs the terminal audit and owns the merge.
branch: codex/ui-wave0-2026-08-11

# F-6: the commit-message series for this wave is a PARENT decision (AGENTS.md:16 mandates
# `Phase <major.minor.patch>: <imperative summary>`; live dev history is mixed). The parent writes the
# verbatim answer here BEFORE dispatch. If this is still null at M0, that is a blocked_precondition
# STOP — do not invent a phase number and do not fall back to fix(web): on your own authority.
commit_series: "Phase 0.<task#>.<seq>"

# Milestone grouping is an EXECUTION-MODE construct for the review gates ONLY. It changes no task, no
# acceptance criterion and no ordering: it is exactly the §2 binding dependency chain
# (T2 -> {T10,T11,T12,T13} -> T3(a) -> T3(b) -> T9, and T1 -> T9), batched into reviewable units.
milestones:
  - id: M0
    title: "Dispatch preconditions (SETUP-ONLY, no implementation, NO bridge review — see the banner's M0 exception): fetch origin, pin base_sha = fresh origin/dev tip, create the dedicated worktree + branch, confirm `commit_series` is filled in (null = blocked_precondition STOP, F-6), and read the §0 documents in order (00-EXECUTIVE-REPORT rev 5, OWNER-DECISIONS, 10-verification-orphans-duplicates, 11-research-finance-hub). Do NOT read section files 01-04 standalone. Completion = set status: passed with the base_sha recorded; no adversarial register is produced."
    review_lenses: []
    status: passed
    updated: d682b38e
    fix_rounds: 0
    commit: null
    verdict: null
    last_verdict: null
  - id: M0b
    title: "Owner-authorized tests-only baseline remediation before M1: re-derive the full failing set at pinned base d682b38ec9761a917b9716428091a482745795f6, then fix every stale test to assert current ruled behavior with a one-line comment citing the promoted lane that changed it (including UoM unit-precision quantity display and L3 locationScopedKey where applicable). Diff is limited to test files; zero production-code changes. Owner amendment 2026-08-18: individually confirmed real-behavior defects remain failing as an enumerated exception list with owning lanes, and execution continues unless a defect invalidates this wave's own tasks; the M0b bridge must verify every classification. Initial exception: src/__tests__/i18n/arLocaleCoverage.test.ts, owned by CODEX-DISPATCH-arabic-i18n-backfill-2026-08-10.md. Exit requires the remaining apps/web `pnpm test -- --maxWorkers=1` set green plus pnpm typecheck and pnpm lint, followed by the normal bridge review. Later milestones inherit the same reviewed exceptions and deterministic test command. Commit series: Phase 0.0b.<seq>."
    review_lenses: [frontend-conventions, general]
    authority: docs/handoff/reviews/ui-wave0/OWNER-RULING-2026-08-18-M0b.md
    status: passed
    updated: 87cbda211
    fix_rounds: 2
    commit: 87cbda21112e7da89a2cd8b2c872c03830381c0c
    verdict: docs/handoff/reviews/ui-wave0/M0b-round2.md
    last_verdict: ACCEPT
  - id: M1
    title: "T1 (CX-4) - component-graph-aware listing re-census: apps/web/tools/audit-listing-census.mjs (pure exported functions + main), its node --test suite, and the report at docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/17-listing-census-recensus.md. Discovery rule stated and not hardcoded to 46; import-following into organisms; orphan reachability incl. parameterized routes and breadcrumb parents; always exit 0 and NOT chained into `lint` or CI. Supersedes UI-28/UI-29/UI-34 with method-per-number."
    review_lenses: [frontend-conventions, general]
    status: passed
    updated: 4c847a414
    fix_rounds: 3
    commit: 72c0207b41afdda1fa18cbec109a33ef7456bebb
    verdict: docs/handoff/reviews/ui-wave0/M1-round3.md
    last_verdict: ACCEPT
  - id: M2
    title: "T2 (UI-11) - gen-route-manifest.mjs resolves through the local KeyedByRouteId wrapper so /sales/invoices/:id records InvoiceDetailPage. NO manifest file is committed here (T3 owns scripts/factory/manifests/)."
    review_lenses: [frontend-conventions, general]
    status: passed
    fix_rounds: 1
    updated: daf0a8f50
    commit: daf0a8f5065184cae0bf8c60eb4422dd6ae387f9
    verdict: docs/handoff/reviews/ui-wave0/M2-round1.md
    last_verdict: ACCEPT
  - id: M3
    title: "T10, T11, T12, T13 - the four owner-ruled deletions (/pos/shifts + POSShiftsDashboard; /marketing + MarketingHubPage; /finance + FinanceHubPage OUTRIGHT with no redirect, preserving finance:hub.cards.treasuryOverview.title and applying the three-row disposition table; the duplicate /settings/chart-of-accounts mount), each against the six-point deletion checklist in §3, one commit per task, source only. EXPECTED AND PERMITTED: these commits leave scripts/factory/manifests/ drifted and check-manifest-drift.sh exiting 1 in the intermediate state (§2 exception 2) - the reviewer must not treat that as a defect at this milestone. T10 hand-removes exactly the two baseline lines (baseline.json:202, :717); --write-baseline is forbidden. Each commit still green on typecheck/lint/test."
    review_lenses: [frontend-conventions, general]
    status: passed
    fix_rounds: 5
    updated: 4446b7f62
    commit: eb4ea4fe0c6e6f3668e8343fa5401c1ee9f11c11
    verdict: docs/handoff/reviews/ui-wave0/M3-round5.md
    last_verdict: ACCEPT
  - id: M4
    title: "T3 (UI-10) - two authorised commits: T3(a) regenerate scripts/factory/manifests/ (manifests and nothing else) so the manifest reflects T2 plus the four deletions, then T3(b) the standalone manifest drift CI job. check-manifest-drift.sh must exit 0 at this point."
    review_lenses: [frontend-conventions, general]
    status: passed
    fix_rounds: 1
    updated: 3fc0c52bc
    commit: 6747d9042c252883426f1ebc0f44c2d5c5e54d13
    verdict: docs/handoff/reviews/ui-wave0/M4-round1.md
    last_verdict: ACCEPT
  - id: M5
    title: "T4 (UI-01/UI-02) + T15 (UI-07) - the gating pair. T4: canAccessModule fails CLOSED, the ModuleKey union typed, the mandatory pre-step widening hasAnyPermission AND hasAllPermissions to readonly Permission[], the four invalid keys across five literal occurrences fixed, the six call surfaces (incl. CashPositionWidget.tsx:124-127) covered, and EXACTLY the five enumerated diagnostics - a sixth is a STOP-and-report. T15: the one-line /expenses nav parity fix at Sidebar.tsx:280 with role-level tests, the treasury siblings untouched, and the bankingAndPayments module-gate constraint recorded report-only. Both are USER-VISIBLE navigation changes in opposite directions (F-3, F-5) - state them plainly."
    review_lenses: [tenancy-authz, frontend-conventions, general]
    # UNBLOCKED by the parent M5 ruling of 2026-08-19 (recorded verbatim under `blockers:`).
    # Implemented commits, in order: 77257bf88 (T4), 3608a8b1d (T15), a6dc879b8 (authorised
    # manifests-only M5 sync). The M5 bridge ran as TWO lens-scoped registers (both round 1,
    # both ACCEPT, zero fix rounds): tenancy-authz 901982d89 and frontend-conventions 9b46e8d52.
    # `commit:` carries the last register commit; `verdict:` lists both register paths.
    status: passed
    updated: 9b46e8d52
    fix_rounds: 0
    commit: 9b46e8d52
    verdict:
      - docs/handoff/reviews/ui-wave0/M5-round1-tenancy-authz.md
      - docs/handoff/reviews/ui-wave0/M5-round1-frontend-conventions.md
    last_verdict: ACCEPT   # both lenses: tenancy-authz ACCEPT, frontend-conventions ACCEPT
  - id: M6
    title: "T5 (UI-12 voucher filter chips - both ternary branches empty), T7 (UI-40 partial: the C6 detector regex fix + the colorClasses migration/baseline PLAN only - do not remove the carve-outs, do not migrate the 52 importers; T7's C1-C5 rule is commit-local), T8's T8-c claim only (dead-code sweep; T8-b stays a non-executable BLOCKED record, touchOptimized stays unassigned per F-2b), T14 (OQ-1 app name as a config value, incl. the seeded-product render test on TenantSupportAccessPage and the renderWithProviders/ProductConfigProvider wrapper fix)."
    review_lenses: [frontend-conventions, general]
    # Implemented by the parent orchestrator's implementation agent (Claude) in the same worktree
    # and on the same branch, continuing the M5 execution handover. Four commits, one per task, in
    # brief order: 83b46e752 (T5), 4fcf447bf (T7), ee87f153e (T8-c), 4275794ee (T14). `commit:`
    # carries the last IMPLEMENTATION commit; the ledger commit that records this row sits on top
    # of it. The M6 bridge ran as a single register (round 1, ACCEPT, zero fix rounds):
    # docs/handoff/reviews/ui-wave0/M6-round1.md at 2621abbff, range 9b46e8d52..5caeb61a2.
    # Its P2-1 and P3-1 documentation directives are applied at the M7 opening commit.
    # Nothing was pushed.
    status: passed
    updated: 2621abbff
    fix_rounds: 0
    commit: 4275794ee
    verdict: docs/handoff/reviews/ui-wave0/M6-round1.md
    last_verdict: ACCEPT
  - id: M7
    title: "T9 (UI-44) - documentation debt, consuming T1's report (M1) and T3's regenerated manifest (M4). Where a runtime assertion is impossible the acceptance criterion is a reproducible command whose output goes in the handback."
    review_lenses: [general]
    # Implemented by the parent orchestrator's implementation agent (Claude) in the same worktree
    # and on the same branch, continuing the M5/M6 execution handover. ONE implementation commit
    # (T9 is a single task): 85d2f8a26. The M7 opening ledger commit 6e51c4e48 sits BELOW it and
    # also carries the M6 register's two doc directives (P2-1, P3-1). The M7 bridge review has NOT
    # been run - it is the parent's to run (--range 2621abbff..HEAD covers exactly the M7 work).
    # Nothing was pushed.
    # M7 BRIDGE RESULT: run by the parent as ONE register (round 1, ACCEPT, zero fix rounds) -
    # docs/handoff/reviews/ui-wave0/M7-round1.md at e7cf6b0ca, range 2621abbff..1dff4a00e, lens general.
    # No P1. Its P2-1 and P3-1/P3-2/P3-3/P3-4/P3-5 corrections are applied at the M8 opening commit.
    status: passed
    updated: e7cf6b0ca
    fix_rounds: 0
    commit: 85d2f8a26
    verdict: docs/handoff/reviews/ui-wave0/M7-round1.md
    last_verdict: ACCEPT
  - id: M8
    title: "WHOLE-BRANCH GATE (no new implementation): the SIX §4 whole-branch contract items at final state - (1) ./scripts/preflight.sh with the PHPUnit skip reported AS A SKIP, never as green, and every non-PHPUnit stage genuinely passing; (2) check-manifest-drift.sh exit 0 with /sales/invoices/:id -> InvoiceDetailPage present and the four deleted routes absent; (3) audit-design-system.mjs 0 new/0 stale with the categorised per-category delta - ONLY C6 grows, C2 -1 and C3 -1 traceable to the two deleted files, C1/C4/C5 frozen, anything else is a fail; (4) the per-deleted-route greps with quoted -E patterns, the production-only /finance grep and the enumeration of the seven intentional HubCard.test.tsx literals (lines 12, 22, 35, 54, 63, 87, 103); (5) pnpm typecheck/lint and `pnpm test -- --maxWorkers=1` green in apps/web, subject only to the bridge-confirmed M0b real-defect exceptions; (6) the UI E2E run with any skipped spec documented by screenshot, or the named blocker. Then write the handback per §5 - including the hard prohibition on the words 'Wave 0 complete' / 'UI-43 closed'."
    review_lenses: [frontend-conventions, tenancy-authz, general]
    # Executed by the parent orchestrator's implementation agent (Claude), continuing the M5/M6/M7
    # handover, in .worktrees/ui-wave0 on branch codex/ui-wave0-2026-08-11. Branch tip and name were
    # re-verified immediately before every commit; no foreign commit was observed; nothing was pushed;
    # `git stash` was never used. TWO commits: 4706d437d (Phase 0.9.4 - M7 close-out + the M7
    # register's six documentation corrections) and the handback/ledger commit that records this row.
    # NO new implementation: the only source-tree change in M8 is markdown plus this YAML.
    # RESULT: four of the six §4 items PASS (2, 3, 4, 5); item 1 (preflight) and item 6 (UI E2E) are
    # ENVIRONMENT-BLOCKED and reported as blocked, never as green - see findings below.
    # M8 is the FINAL milestone: the parent runs the wave's terminal gate and the visual E2E pass.
    status: review
    updated: b6012cd34
    fix_rounds: 0
    commit: 4706d437d
    verdict: null
    last_verdict: null

owner_gates:
  # informational record — the conditional/standing logic lives in the banner and the milestone text.
  # No milestone carries an `owner_gate:` field: the harness reads that field as an UNCONDITIONAL
  # stop (harness step 4 / condition B), and these are standing constraints, not per-milestone stops.
  - id: F-1-rafiq-parked-directory
    question: "T6 (UI-14, ProductEditHero.tsx:137) and T8-b (BarcodeHero.tsx) both target apps/web/src/features/products/editor/, which owner ruling OQ-4 (PARKED) puts off-limits pending the unmerged Rafiq round-2 worktree. BOTH REMAIN NON-EXECUTABLE BLOCKED RECORDS: no commit, excluded from the task count. The gate-r1 reviewer's merge-risk assessment LOWERS the estimated risk and is NOT authorisation - the executor must not act on it. Only an explicit parent/owner ruling changes the disposition, and it must also name who reconciles audit-design-system-baseline.json against the Rafiq branch. The three baseline lines (:224-225, :665) must still be PRESENT at final state."
    blocks_milestone: none
    status: standing_constraint
  - id: F-2b-touchOptimized-ownership
    question: "UI-43's touchOptimized claim (15 TSX files) has NO recorded owner: rev 5 assigns all four split UI-43 claims to Wave 0 and the size argument is a scheduling recommendation, not an ownership transfer. Do not implement it. Until the parent dispatches it, reassigns it on the record, or accepts it as a standing gap, NEITHER Wave 0 NOR UI-43 may be reported complete (§5 item 7) - report per-finding status and mark UI-43 PARTIAL with its four claims itemised."
    blocks_milestone: none
    status: standing_constraint
  - id: F-6-commit-series
    question: "The <major.minor.patch> phase series for this wave is a parent decision (AGENTS.md:16). It is recorded in `commit_series:` above BEFORE dispatch; if it is still null at M0 the run stops as blocked_precondition. Blocking for commit hygiene only - no implementation work depends on it once it is filled in."
    blocks_milestone: M0
    status: pre_dispatch
  - id: do-not-touch-boundaries
    question: "Standing DO-NOT-TOUCH set (§1): features/products/editor/**, LaneSeparationReportPage + the /finance/lane-separation route (owned by the DN-consolidation lane), the events/shift-variance dossiers (fixes session), any Wave-3 listing-pattern migration, removing the colorClasses ESLint carve-outs or migrating the 52 importers, and apps/api/** (no backend change in this wave - if a task appears to need one, STOP and report)."
    blocks_milestone: none
    status: standing_constraint
  - id: F-3-F-5-visible-nav-changes
    question: "T4 removes nav entries from roles that never held the permission; T15 adds /expenses for cashier/operator/viewer who already hold expenses.view. Both are USER-VISIBLE and must be stated plainly at gate time - acknowledged, not softened."
    blocks_milestone: none
    status: standing_constraint

status: review    # pending | in_progress | blocked_owner | blocked_review | blocked_architecture | blocked_precondition | complete
# EXECUTION HANDOVER 2026-08-19: the Codex executor exhausted its quota after M4. M5 was
# implemented by the parent orchestrator's implementation agent (Claude) in the same worktree
# and on the same branch, under the parent ruling recorded below. No push was made. The M5
# bridge review has not been run; it remains the parent's to run.
blockers:
  # RESOLVED 2026-08-19 by the parent ruling quoted verbatim immediately below.
  - "Owner ruling required before M5: T4 must replace /crm/companies moduleKey=partners with permission=partners.view, which necessarily changes the generated manifest row from module_gate: partners / permission: null to module_gate: null / permission: partners.view. M8 requires final drift exit 0, but the brief declares T3(a) the only manifest commit. Recommended amendment: authorize one additional manifests-only sync immediately after T4 and before M5 review; do not fold the manifest into T4's source commit."
  - "PARENT RULING 2026-08-19 (verbatim), resolving the blocker above: \"M5 manifest contradiction RULED: one additional manifests-only synchronization commit is authorized, placed after the last M5 source commit (T4/T15, whichever lands second) and before M5 review. It supersedes the one-manifest-commit restriction to exactly this extent: the branch now carries exactly TWO manifest commits - T3(a) and this M5 sync. The sync commit contains manifests only, nothing else. The regenerated diff must show ONLY the /crm/companies gate change (moduleKey partners -> permission partners.view); T15 is sidebar-only and must produce zero manifest delta. Any other entry changing in the regen is a STOP, not something to absorb. Paste check-manifest-drift.sh exit 0 after the sync. M8's zero-drift gate is evaluated against this amended two-commit model.\" Executed as commit a6dc879b8; the regen matched the ruling exactly and check-manifest-drift.sh exits 0."
findings:
  - "Pinned-base inventory run 1: 20 failed files / 45 failed tests / 4,179 passed. Structured repeat: 19 failed files / 44 failed tests / 4,180 passed; the extra first-run failure was a CreateStockTransferPage.availability timeout absent from the repeat."
  - "Deterministic isolated evidence: pnpm exec vitest run src/__tests__/i18n/arLocaleCoverage.test.ts => 3 failed / 7 passed; current runtime falls back to English for the missing Arabic keys despite src/lib/i18n.ts claiming the enforced namespaces are fully translated."
  - "Owner ruling 2026-08-18 CONFIRMS src/__tests__/i18n/arLocaleCoverage.test.ts as an enumerated real-defect exception owned by CODEX-DISPATCH-arabic-i18n-backfill-2026-08-10.md; Arabic parity is not launch scope."
  - "Candidate exception for M0b bridge confirmation: tools/__tests__/offset-pagination-meta-consolidation.test.mjs correctly detects the inline pagination meta reintroduced at src/features/treasury/statements/api.ts:112. Historical lane: CODEX-replenishment-followups-2026-07-12.md Wave B / pagination-meta consolidation. Forward owner: the parent terminal audit must route this finding to a live production-fix lane before merge; it is not accepted as a standing gap. The test remains unchanged."
  - "Candidate exception for M0b bridge confirmation: src/components/__tests__/SharedSingletons.tenantScope.test.tsx correctly detects AddQuickProductModal invalidating the foreign-tenant products cache through the bare ['products'] prefix at AddQuickProductModal.tsx:209. Historical lane: promoted tenant-scoped invalidation sweep cc1332fd, which missed this tenantScope-pinned site. Forward owner: the parent terminal audit must route this launch-program tenant-isolation finding to a live production-fix lane before merge; it is not accepted as a standing gap. The test remains unchanged."
  - "Tests-only remediation commit 3e87358c5197cbee1fbff10c4c63fef119da9f99 plus review-fix commit 62c75bf7093f6127efbef5f4397be29006e80643 update 17 stale or racy test files and change zero production files. The repaired set passed 168/168 tests."
  - "Deterministic full apps/web pnpm test (--maxWorkers=1) after remediation: 4,224 passed, 5 failed, 1 skipped, 3 todo; the only failing files are the three enumerated exception candidates above. Four-worker evidence is superseded because round-1 review reproduced load-induced flakes. pnpm typecheck and pnpm lint pass; React Doctor changed-scope scan reports 100/100 with no issues."
  - "M1 round-1 review found that the 46-page filename population was inherited rather than independent, a material route-mounted non-detail DataTable surface was outside that cohort, seven action/form routes had resolvable prefix-dynamic links, and the empty-state predicate could span unrelated comparisons. Commit 5383a2467 discloses the scope boundaries, resolves the bounded link patterns, tightens the predicate, and adds regression tests."
  - "M1 round-2 review confirmed the round-1 repairs, then identified five more raw action/form false negatives beyond the bounded expression model, a broken audit-source pointer, an undisclosed prop-union approximation, and an unreproducible secondary-surface count. Commit 72c0207b4 separates the raw 27 from the 22 remaining after manual call-flow review, attributes all five live links, fixes the pointer/method, and adopts a conservative reproducible 33-page secondary-surface rule."
  - "M1 round-3 bridge ACCEPTED the milestone and independently reproduced 46 listings, 271 route records, 27 raw candidates, all distributions, 12/12 tests in both runners, and zero production changes. Its close-before-merge P2 remains open for the parent terminal audit: applying the report's stated conservative secondary-surface rule literally yields 35 rather than 33; the report path ordinal collision also remains the parent's merge-time decision."
  - "M3 source commits d7f3ea21d, cdce8ab38, 1184a83ac, and 85bf6b02d implement the four owner-ruled deletions one task per commit. Each used a red-first route assertion, then passed typecheck, lint/audits, React Doctor, and the deterministic single-worker full suite subject only to the three M0b-reviewed exception files."
  - "T12 deletes obsolete E2E test MTP-GL-28 under OWNER-DECISIONS:58 and removes audit-shot entry 24-finance; the seven HubCard.test.tsx /finance fixture literals remain byte-identical. This is an authorized deviation and must remain explicit in the final handback."
  - "M3 intentionally leaves route manifests drifted for M4: check-manifest-drift.sh exits 1 and shows /pos/shifts, /marketing, /finance index, and /settings/chart-of-accounts as removals; git diff d3b0550ed..HEAD -- scripts/factory/manifests is empty."
  - "M3 bridge round 1 produced a blank review-tool error and no verdict/findings; the harness treats exit 3 as CHANGES-REQUIRED. No implementation fix was possible, so the failed register is preserved and round 2 retries the read-only gate."
  - "M3 bridge round 2 found a vacuous /marketing route-literal assertion, a T10-orphaned POS invalidation helper plus stale references, missing pasted red/per-key evidence, and four newly zero-consumer POS API exports. Commit 9ac83f0eb fixes the source/test findings; the evidence and out-of-scope API exports are recorded in the handback."
  - "M3 round-2 owner escalation: keeping the finance parent while deleting its index and adding no redirect makes /finance match the element-less parent and render a blank outlet; it does not fall through to the catch-all /dashboard redirect stated in OWNER-DECISIONS:58. The implementation follows the brief exactly, so no unruled behavior change was made; parent/owner must rule before merge."
  - "M3 round-2 post-fix verification: focused route/POS tests 26/26; deterministic full suite 4,202 total / 4,193 passed / 5 failed / 1 skipped / 3 todo with only the three reviewed M0b exception files; typecheck and lint pass; design audit 736 acknowledged / 0 new / 0 stale; React Doctor 100/100."
  - "M3 bridge round 3 produced a blank review-tool error and no verdict/findings; the harness treats it as CHANGES-REQUIRED. The round-2 repairs remain verified, no source change is indicated, and round 4 retries the read-only gate."
  - "M3 bridge round 4 found 15 en/fr POS locale leaves whose sole consumers were the deleted shift console. Commit eb4ea4fe0 removes those leaves, preserves the seven live Z-report detail leaves, and the handback now pastes zero-consumer/null-locale proof for every removed key plus the captured T12/T13 red output."
  - "M3 round-4 post-fix verification: focused route/i18n/sidebar/POS tests 69/69; deterministic full suite 4,202 total / 4,193 passed / 5 failed / 1 skipped / 3 todo with only the three reviewed M0b exception files; typecheck and lint pass; design audit 736 acknowledged / 0 new / 0 stale; React Doctor skipped because the locale-only fix changed no React source files."
  - "M3 bridge round 5 ACCEPTED: the reviewer independently re-derived all 57 deleted-component translation keys, confirmed every removed key has zero consumers and every retained key has a live consumer, reproduced the focused gates, and found no remaining P1. The /finance blank-pane contradiction remains a correctly disclosed parent/owner pre-merge gate; the remaining P3 notes are ticketable."
  - "M4 T3(a) commit 00366d151 regenerates routes-web.yaml and nothing else after T2 and T10-T13; routes-pos.yaml is unchanged. Drift exits 0, /sales/invoices/:id resolves to InvoiceDetailPage, and /pos/shifts, /marketing, the /finance index, and /settings/chart-of-accounts are absent."
  - "M4 T3(b) commit 6747d9042 adds an always-run Route Manifest Drift Guard with only checkout, pnpm/Node setup, workspace install, and the drift command; all-checks-pass depends on it with a comment explaining it cannot be skipped. A throwaway route made the check exit 1 and print the fix command; after reversal it exits 0."
  - "M4 pre-review verification: generator tests 10/10; deterministic full suite 4,202 total / 4,193 passed / 5 failed / 1 skipped / 3 todo with only the three reviewed M0b exception files; typecheck and lint pass; design audit 736 acknowledged / 0 new / 0 stale; CI YAML parses and confirms the job has no if guard and is in aggregate needs."
  - "M4 bridge round 1 ACCEPTED all M4 criteria and independently reproduced drift exit 0, the one-manifest-commit invariant, correct InvoiceDetailPage resolution, lean always-run CI job structure, aggregate dependency, and 10/10 generator tests."
  - "M4 review forward blocker: T4's mandated /crm/companies moduleKey=partners -> permission=partners.view repair will necessarily drift routes-web.yaml, while M8 requires drift 0 and the brief forbids a second manifest commit. Owner/parent must amend manifest ownership before M5 closes; execution stops before touching M5."
  - "M4 review P3 carryovers: the CI workflow itself does not trigger on direct pushes to dev; the workspace install may be broader than the generator needs; and the scalar YAML commit field records T3(b) while T3(a)'s SHA is retained in the handback/findings."
  - "M5 execution handover: the Codex executor ran out of quota on 2026-08-19 after M4 was accepted. The parent orchestrator's implementation agent took over T4 and T15 in the same worktree (.worktrees/ui-wave0) on branch codex/ui-wave0-2026-08-11, under the parent M5 ruling. Branch tip and branch name were re-verified immediately before each commit; no foreign commit was observed and nothing was pushed. Commit series follows the YAML's own 'Phase 0.<task#>.<seq>' rule: T4 -> Phase 0.4.1, T15 -> Phase 0.15.1, and the authorised manifest sync -> Phase 0.3.5 because T3 owns scripts/factory/manifests/."
  - "M5 T4 commit 77257bf88: MODULE_PERMISSIONS is now `as const satisfies Record<string, readonly Permission[]>` with an exported ModuleKey union; hasAnyPermission and hasAllPermissions were widened to readonly Permission[] first; canAccessModule(moduleKey: ModuleKey) returns false for an unrecognised key. All six call surfaces re-typed: RequirePermission.tsx:11, Sidebar.tsx:111 (NavChild), :121 (NavModule) and :425 (isNavItemVisible), useCommandPalette.ts:42, PosHubPage.tsx:25 and InventoryHubPage.tsx:28 (permissionModule), plus the two direct callers PosRefundPoliciesPage.tsx:130 and CashPositionWidget.tsx:128, which compile clean on valid keys. MarketingHubPage/FinanceHubPage no longer exist (deleted at M3), so only two hub-card types remained."
  - "M5 T4 diagnostic proof: with the union in place and the two new map rows temporarily removed (i.e. the exact pre-fix state the brief specifies), pnpm typecheck failed on EXACTLY the five enumerated production occurrences and no sixth - Sidebar.tsx:182 (goods-receipt.create-standalone), Sidebar.tsx:215 (inventory.view), routes/index.tsx:1428 and :1440 (parts_catalog), routes/index.tsx:2747 (partners). Line numbers differ from the brief's 1431/1443/2777 only because the M3 deletions shifted routes/index.tsx; the keys, sites and count match. Four further diagnostics appeared in the NEW red-first test file src/hooks/__tests__/usePermissions.moduleAccess.test.tsx (lines 58, 64, 69, 73), which asserts the not-yet-added map rows; those are TDD artifacts of the test author's own file, not a sixth production site. After the four keys were resolved, pnpm typecheck is clean."
  - "M5 T4 resolutions as tabulated: rows 1-2 added as self-mapped MODULE_PERMISSIONS entries ('goods-receipt.create-standalone', 'inventory.view'); row 3 deleted both RequirePermission moduleKey=\"parts_catalog\" wrappers at routes/index.tsx while KEEPING <ModuleGuard module=\"PlatformIntegration\"> on both routes; row 4 replaced moduleKey=\"partners\" with permission=\"partners.view\" at /crm/companies and left the route in place (dedup is UI-35, Wave 4)."
  - "M5 T4 red-first evidence: the three new/extended test files failed 6 tests / 46 passed before the implementation - usePermissions.moduleAccess.test.tsx (unknown key returned true; the two new map rows undefined; goods-receipt.create-standalone open to a purchases role), RequirePermission.moduleKey.test.tsx (children rendered under an unknown moduleKey), and Sidebar.test.tsx (newGoodsReceipt visible to a purchases role). All green after."
  - "M5 T4 test-harness note: Sidebar.test.tsx stubs usePermissions wholesale, which cannot express role-level gating. The mock now delegates to the REAL hook via importOriginal behind a `useRealModuleAccess` flag that only the new T4/T15 describe block sets; the 41 pre-existing Sidebar tests keep the stub and are otherwise unmodified. (Corrected from 43 per M5 frontend-conventions review P3: 41 is the pre-existing `it(` count at acf5dc6bd; 43 is the post-T4 total.)"
  - "M5 T15 commit 3608a8b1d: Sidebar.tsx:280 permission 'treasury' -> 'expenses'. One line of production code plus tests; the payments/repositories/instruments treasury siblings, the /expenses route guard and MODULE_PERMISSIONS are untouched. Red-first: the cashier case failed before the change while the negative case (a role holding neither permission) passed both before and after, as it must. T4 landed first on this branch; both orders leave the assertions true because 'treasury' and 'expenses' are both long-standing valid keys, so neither reading depended on the fail-open behaviour. REPORT-ONLY constraint carried forward: the bankingAndPayments group carries module: 'Treasury' (Sidebar.tsx:272), so the item stays hidden for tenants without the Treasury backend module regardless of role."
  - "M5 USER-VISIBLE navigation changes, stated plainly (F-3 and F-5): T4 REMOVES the 'newGoodsReceipt' entry from every role except admin/manager and makes 'stockByLocation' a real inventory.view check; T15 ADDS the /expenses entry for cashier, operator and viewer. Both are intended repairs, not refactors."
  - "M5 manifest sync commit a6dc879b8 (authorised by the parent ruling): `node scripts/factory/gen-route-manifest.mjs` changed exactly one entry - /crm/companies module_gate partners -> null, permission null -> partners.view - and nothing else. The parts-catalog routes produce no delta because module_gate records the ModuleGuard, which wins over moduleKey (gen-route-manifest.mjs:12-13); T15 produced zero manifest delta; routes-pos.yaml unchanged. `bash scripts/factory/check-manifest-drift.sh` exits 0 after the commit. The branch now carries exactly two manifest commits: 00366d151 (T3(a)) and a6dc879b8 (M5 sync)."
  - "M5 verification: pnpm typecheck clean; pnpm lint exit 0 (0 errors, 6513 pre-existing warnings; Gate C 0/0/0; design-system 736 acknowledged / 0 new / 0 stale; audit-quantity 0/0/0; all three ESLint RuleTester suites pass). Deterministic `pnpm test -- --maxWorkers=1` at the T15 tree: 4,215 total / 4,205 passed / 6 failed / 1 skipped / 3 todo. Five of the six failures are the three reviewed M0b exception files (arLocaleCoverage x3, offset-pagination-meta-consolidation, SharedSingletons.tenantScope). The sixth, TaxSettingsPage 'renders exactly one h1 via PageHeader', is a 5000ms-timeout load flake of the class M0b already documented: re-run in isolation it passes 5/5. An earlier whole-suite run made while focused runs were executing concurrently produced three such timeouts (RepositoryDetailPage, RepositoryListPage, partnerDelete); all three pass 20/20 in isolation."
  - "M5 React Doctor: changed-scope scans report 'No issues found' for both source commits (--scope changed --base acf5dc6bd for T4, --base 77257bf88 for T15). The repo pre-commit hook prints 'React Doctor found staged regressions' on both commits; the hook is advisory (it does not exit non-zero) and the staged whole-file scan attributes exactly one pre-existing warning, react-doctor/js-combine-iterations at Sidebar.tsx:444 (a filter().map() chain in filteredNavigation that neither task touched)."
  - "M5 open for the parent (SUPERSEDED by the two-lens bridge result recorded below): at the time of the M5 handover the bridge had not been run. UI E2E was not run for M5 (it is the M8 whole-branch item). No push was made."
  - "M5 bridge RESULT: run as two lens-scoped registers, both round 1, both ACCEPT, zero fix rounds - docs/handoff/reviews/ui-wave0/M5-round1-tenancy-authz.md (commit 901982d89) and docs/handoff/reviews/ui-wave0/M5-round1-frontend-conventions.md (commit 9b46e8d52). M5 status set to `passed`. The frontend-conventions register's only actionable item was a P3 YAML wording correction (the pre-existing Sidebar test count is 41, not 43), applied at the M6 opening commit."
  - "M6 execution: continued under the M5 handover - the parent orchestrator's implementation agent (Claude) executed T5, T7, T8-c and T14 in .worktrees/ui-wave0 on branch codex/ui-wave0-2026-08-11. Branch tip and name were re-verified immediately before every commit; no foreign commit was ever observed and nothing was pushed. Commit series follows the YAML's 'Phase 0.<task#>.<seq>' rule: ledger 0.6.0, T5 0.5.1, T7 0.7.1, T8-c 0.8.1, T14 0.14.1."
  - "M6 NO-MANIFEST-DRIFT constraint HELD: `git diff --name-only 9b46e8d52..HEAD -- scripts/factory/` is EMPTY across all four M6 commits, and `bash scripts/factory/check-manifest-drift.sh` exits 0 at the M6 tip. The branch's two-manifest-commit model (00366d151 T3(a) + a6dc879b8 M5 sync) is closed and untouched; no M6 change required regeneration, so no STOP was triggered."
  - "M6 T5 commit 83b46e752 (UI-12): VoucherListPage.tsx:104-112's ternary had BOTH branches as the empty template literal, so the active chip was byte-identical to the inactive ones with no accessible selection state. Matched to the house filter-chip pattern at features/import/pages/ImportHistoryPage.tsx:90-104 (selected = intent.primary.bgStrong + text.inverse; unselected = neutral surface). DEVIATION DISCLOSED: implemented through the canonical Button variants (selected variant='primary', unselected stays variant='secondary') rather than className overrides. The brief permits changing the variant when matching the house pattern requires it, and it does here - Button already composes those exact semantic tokens in variantStyles, and a className override would have to win a Tailwind class-order fight against them to render at all. aria-pressed now carries selection so it is not colour-only; geometry classes and the data-testid contract are unchanged. Red-first 3/3 failed, then 78/78 green across src/features/vouchers."
  - "M6 T7(a) commit 4fcf447bf (UI-40 detector): per-category whole-repo scan before -> after was C1 12->12, C2 250->250, C3 458->458, C4 14->14, C5 2->2, C6 0->82. ONLY C6 moved, satisfying the commit-local C1-C5 rule; the sole non-C6 line in the baseline diff is a trailing-comma reflow on the last pre-existing C5 entry. Baseline regenerated in the same commit, 736 -> 818 entries, and the audit reports 818 acknowledged / 0 new / 0 stale."
  - "M6 T7(a) SCOPE JUDGEMENT, flagged for the reviewer: the brief's acceptance text says the regex family must match Tone/Tones SUFFIXES, but a suffix-only fix would have left most of F-8's population undetected - the live maps are identified by their VALUE TYPE, and directionTone / matchTone / invoiceTone / matchPreviewTone carry no status|state token in the name. Implemented BOTH: `Tones?` added to alternations 1 and 3, plus a fourth alternation keyed on `Record<…, StatusTone>` directly. Rationale recorded in the source docblock, the commit message and §6 of the plan doc."
  - "M6 T7(a) HONEST NUMBER: 82 is an ENTRY count, not a defect count. STATUS_RE is an alternation family matched independently, so one declaration can carry several entries (InvoicesPage.tsx:14 matches three). Distinct source sites = 46 across 41 files, which is the real burn-down target. The over-counting predates this fix and de-duplicating would change baseline-key semantics shared with C1-C5, so it is documented rather than changed."
  - "M6 T7(b) plan at docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/18-colorclasses-migration-plan.md: script-derived inventory of 52 importers / 1,901 occurrences / 40 documents + 12 admin / 0 outside the carve-out - confirming 00 §2:128 over 02's '43 of 52'. Also corrects a measurement trap: a bare `grep -rl colorClasses src` returns 54, the two extras being the definition at designTokens.ts:568 and a comment at stock-adjustments/api/queries.ts:23; burn-down measurement must filter on the import statement. eslint.config.js is byte-identical - carve-outs NOT removed, no importer migrated. The doc is force-added because docs/sessions/ is gitignored (.gitignore:58), the same precedent M1 set for 17-listing-census-recensus.md."
  - "M6 T7 OWNERSHIP BOUNDARY with enforcement package P2 (parallel lane .worktrees/enforcement-p2), recorded so P2's executor can reconcile: Wave 0 T7 owns ONLY the STATUS_RE family in apps/web/tools/audit-design-system.mjs, the regenerated apps/web/tools/audit-design-system-baseline.json, the C6 fixtures in apps/web/tools/__tests__/audit-design-system.test.mjs, and the plan document. It has NOT authored or edited any tamper test, any file in the enforcement worktree, or apps/web/eslint.config.js. The shipped detector contract P2 should reconcile against: value-type matching on `Record<…, StatusTone>` PLUS Tone/Tones name suffixes, feature/page-scoped .tsx only (isFeatureOrPageFile + isProductionTsx), so the StatusBadge atom that defines the tone vocabulary is not self-reported. Boundary is stated in the commit message and §6 of the plan doc as well as here."
  - "M6 T8-c commit ee87f153e (UI-43 partial): removed 13 key instances across five bundles - advancedPayments.discountComingSoon (en/fr pos), products.messages.stockComingSoon (en/fr inventory), counting.detailsComingSoon and counting.create.categorySelectionComingSoon(+Hint) (en/fr/ar inventory). ar/inventory.json needed care because line 74 packed four keys inline, so the two categorySelection entries were removed by verbatim string match rather than line deletion; a first attempt left a double comma, which was caught by JSON re-validation and repaired before commit. All five touched bundles re-verified as valid JSON. Red-first 14/26 failed, 26/26 green after; the test is two-sided (it also pins the three LIVE coming-soon keys and asserts EN/FR alignment) because the risk here is over-deletion."
  - "M6 T8-c SURPRISE - DISCOVERED FINDING for the parent to ticket, reported not fixed: the brief's 'zero code usages' claim holds for apps/web/src, but the repo also carries a TRACKED, STRAY, NESTED DUPLICATE TREE at apps/web/apps/web/src/ (5 files, introduced by 9a159d09c 'chore: sync working directory before schema hardening phase'), and apps/web/apps/web/src/features/inventory-counting/pages/CountingDashboardPage.tsx:83 references inventory:counting.detailsComingSoon. Proven unreachable before deleting the key: tsconfig.json:54 includes only 'src', vite never enters it, no import in src resolves into it, and the REAL src/features/inventory-counting/pages/CountingDashboardPage.tsx does not use the key. Deleting the key therefore breaks no live consumer. Removing the stray tree is outside T8-c's scope (rule 4)."
  - "M6 T14 commit 4275794ee (OQ-1): copy now interpolates {{appName}} supplied from useProductConfig().productName at PrivacyPolicyPage.tsx:11/40/78 and TenantSupportAccessPage.tsx:18/33. All 10 verified AutoERP locale hits handled; `grep -rn \"AutoERP\" apps/web/src/locales/` now returns ZERO. The dead appName key was DELETED from en/fr/ar common.json (grep proved zero code consumers). ar/common.json has no legal.privacy subtree so Arabic privacy needed no change; the Arabic support-access subtitle IS updated. No new config mechanism and no new env var - PRODUCT_INFO stays the single source."
  - "M6 T14 harness repair, as the brief warned: TenantSupportAccessPage.test.tsx rendered through a bare QueryClientProvider + MemoryRouter with no ProductConfigProvider, and useProductConfig THROWS without one (ProductConfigContext.tsx:128-135), so the suite would have thrown the moment the page called the hook. CHOSE the explicit `<ProductConfigProvider initialProduct={…}>` wrap over switching to renderWithProviders, because it adds exactly the missing provider without also pulling in CompanyConfigProvider and a seeded company-config cache entry that none of these tests asked for. renderPage() defaults to 'izipos' so the two pre-existing cases keep their behaviour and still pass."
  - "M6 T14 red-first evidence, both component proofs plus the data assertion: PrivacyPolicyPage.test.tsx 4/4 failed then 4/4 passed; TenantSupportAccessPage.test.tsx 3 new cases failed then passed; appNamePlaceholder.test.ts 13/14 failed then 14/14 passed. Both render tests seed the NON-default product ('otospex') so the assertion cannot pass by coincidence, and each also pins the 'izipos' branch to prove the copy follows config rather than a new baked-in name. The locale test is explicitly labelled a DATA assertion that does not substitute for either render proof."
  - "M6 BRIEF ARITHMETIC CORRECTIONS, disclosed rather than silently absorbed. (1) T14's site table is introduced as 'exactly the eight sites' but its rows expand to TEN verified locale hits (3 appName + 2 en privacy + 2 fr privacy + 3 support-access); all ten are handled. (2) T14 says TermsOfServicePage hardcodes the brand 'six times'; the actual count at :37-38, :49-50, :107-108, :131-132 is TEN occurrences across eight lines (four EN/FR branch pairs). The TermsOfServicePage leak remains OUT OF SCOPE and unfixed, reported for the parent to ticket together with its rule-11 violation (inline non-t() strings doing their own FR/EN branching in JSX)."
  - "M6 gate outputs: pnpm typecheck clean at every commit. pnpm lint EXIT=0 at T5, T7, T8-c and T14 - 0 errors throughout (warnings 6513 -> 6520, all from the new test files; the two trivially fixable Array<T> warnings were cleaned up, the remaining three are unsafe-type-assertion warnings inherent to walking untyped locale JSON). Gate C 0 acknowledged / 0 new / 0 stale; audit-quantity clean; design-system 736 -> 818 acknowledged with 0 new / 0 stale after T7. Focused vitest by path with --maxWorkers=1 throughout: vouchers 78/78, tools/audit-design-system 19/19, comingSoonKeyPruning 26/26, i18n + legal + support-access 74 passed. The ONLY failing file anywhere in M6's focused runs is the inherited M0b exception src/__tests__/i18n/arLocaleCoverage.test.ts (3 failed / 7 passed), whose missing-key counts are UNCHANGED at 122 / 1 / 19 on common, workshop-technicians and vehicles - namespaces M6 never touched. CORRECTED per M6 review P2-1 (the earlier wording here claimed 'ar/inventory PASSES in arLocaleCoverage', which was false): arLocaleCoverage.test.ts:67-75 enumerates only nine namespaces (common, validation, workshop-bundles, workshop-technicians, workshop-work-orders, vehicles, vehicle-ownership, scheduling, pickers) and NEVER evaluates ar/inventory, so it can supply no evidence either way about that bundle. The real evidence that the ar deletions kept ar/inventory aligned is (a) the deletions are symmetric across en/fr/ar for all three ar-touched keys - counting.detailsComingSoon and counting.create.categorySelectionComingSoon(+Hint) were removed from en, fr AND ar together - and (b) comingSoonKeyPruning.test.ts:54,58,62 assert the absence of exactly those three paths in ar/inventory, with all five touched bundles re-validated as parseable JSON. COVERAGE GAP, stated honestly: no EN<->AR alignment test covers the inventory namespace anywhere in apps/web/src - comingSoonKeyPruning.test.ts's alignment assertion (:113-131) is EN<->FR only - so ar/inventory alignment rests on the symmetry of the diff, not on an automated check."
  - "M6 open for the parent: the bridge review for M6 (lenses frontend-conventions, general) has NOT been run - milestone status is `review`, verdict null. Full-suite `pnpm test` was not run for M6 (M8 owns the whole-branch gate); evidence is focused-by-path per the brief's iterate-by-path rule. UI E2E was not run (M8 item). No push was made."
  - "M6 CLOSE-OUT: bridge ran as ONE register (round 1, ACCEPT, zero fix rounds) - docs/handoff/reviews/ui-wave0/M6-round1.md at 2621abbff, range 9b46e8d52..5caeb61a2, lenses frontend-conventions + general. No P1. Its two actionable directives were both DOCUMENTATION-only and are applied at the M7 opening commit 6e51c4e48: P2-1 (the false 'ar/inventory PASSES in arLocaleCoverage' evidence clause at :221, rewritten above) and P3-1 (the 'Tailwind class-order fight' mechanism in the VoucherListPage.tsx:106-110 comment, reworded to stylesheet-order precedence; comment block kept the same length, geometry classes untouched). The three remaining P3s are NOT actioned by M7 and are carried to the parent: P3-2 (selected chip's primary accent competes with the page CTA - route to the UI audit as a design-system question about the canonical chip treatment, explicitly do-not-change-in-this-wave), P3-3 (the 818-entry C6 baseline was regenerated against this branch; at MERGE time re-run node apps/web/tools/audit-design-system.mjs on the MERGED tree and require 0 new / 0 stale - do NOT --write-baseline to absorb anything), P3-4 (the stray tracked tree apps/web/apps/web/src/ now holds a dangling reference to a key T8-c deleted; parent ticket to delete those five files)."
  - "M7 execution: T9 is a single task, so ONE implementation commit, 85d2f8a26 (Phase 0.9.1). Branch tip and name were re-verified immediately before both commits (2621abbff before 6e51c4e48, 6e51c4e48 before 85d2f8a26); no foreign commit was ever observed and nothing was pushed."
  - "M7 T9 (UI-44) deliverable 1 - docs/architecture/design-system.md gains a `Listing-Page Composition` section. ACCEPTANCE EVIDENCE, RESTATED WITH THE EXACT COMMAND AND SCOPE per M7 review P3-1 (the earlier wording gave two bare numbers that reproduce only under commands and scopes it did not name): (a) `grep -wnE \"list page|ListPageLayout|FilterPanel|canonical\" docs/architecture/design-system.md | wc -l` - ONE file, word-boundary matching - returned 0 before T9 and 8 at the M7 tip 1dff4a00e, which is the number 02 F-10 is measured against; without -w the same pattern and file return 9 because \"list pages\" in prose then matches. (b) `grep -rnE \"listing|ListPageLayout|FilterPanel\" docs/architecture/ | wc -l` - RECURSIVE over the whole docs/architecture/ directory, not the single file - returns 13 at the M7 tip; scoped to design-system.md alone the same pattern returns 12 (14 with -i), never 13, because frontend-navigation.md supplies the extra line. AT THE M8 TIP both counts move by exactly +1 (9 and 14) because the M8 opening commit adds the `ActiveFilters` row mandated by M7 review P3-2, which contains the token FilterPanel. Content is bounded by 00 §D:189 and adds nothing beyond it: the eight real components with verified paths, the composition (ListPageLayout shell -> filters slot -> DataTable with the empty state passed in -> OffsetPagination in the pagination slot), four rules, and a note on DataTable's legacy children-markup passthrough. The whole section carries the mandated banner `Status: canon PROPOSED, Wave 3 gated on the CX-4 re-census`."
  - "M7 T9 PROPOSED-vs-BUILT verified in source, not assumed: `FilterBar` does not exist anywhere in apps/web/src (find returns nothing) and `DataTableColumn` has NO `sortable` field - its real fields are key/header/align/numeric/render/accessor/headerClassName/cellClassName/width, and grep for `sortable` across components/molecules/DataTable/ returns zero. Both are documented as PROPOSED with an explicit do-not-import instruction, plus a statement that a third new component would be a change to the canon and belongs in the audit."
  - "M7 T9 NUMBERS DISCIPLINE, per the brief's link-don't-restate criterion: the section states exactly ONE figure - ListPageLayout 12 of 46, the single number 00 §4:217 marks decision-grade - pinned to base d682b38ec9761a917b9716428091a482745795f6 and confirmed by T1's re-census ('ListPageLayout remains 12/46'). Pagination, empty-state and filter-pattern distributions are DELIBERATELY NOT restated; they are delegated to 17-listing-census-recensus.md with its reproduction command (`cd apps/web && node tools/audit-listing-census.mjs`). The report's own method boundary is carried across: the 46-page cohort comes from an inherited filename rule and the report flags ~33 further route-mounted DataTable pages, so the distributions are decision-grade for that cohort only."
  - "M7 T9 deliverable 2 - docs/architecture/frontend-navigation.md refreshed 1.0 (2026-01-02, 'Milestone 8') -> 2.0 (2026-08-19). This was not a header touch-up: the doc's CENTRAL MECHANISM no longer exists. 1.0 described filtering through a `MODULE_NAME_MAP` lookup (lowercase sidebar key -> PascalCase backend module; absent from the map = 'core module, always visible'). `grep -rn MODULE_NAME_MAP apps/web/src` returns ZERO. Real mechanism: per-item `module` / `permission` declarations evaluated by `isNavItemVisible`, FAIL-CLOSED on both axes, with BackendModule (lib/modules.ts:44) and ModuleKey (usePermissions.ts:94) as typed unions and canAccessModule denying unknown keys. Every 1.0 section resting on the map - the module-mapping how-to step, both troubleshooting recipes, the four worked scenarios - is rewritten against Sidebar.tsx."
  - "M7 T9 CLAIMS DELETED rather than carried forward (the brief's rule for unverifiable content): (1) the 'Performance Metrics' microbenchmarks (~2-5ms initial render / 16 navigation items / <1KB memory) - unreproducible and the item count is wrong; (2) the 'Quality Metrics' scorecard ('Opus 4.5 Audit Results 2026-01-02, 9/10') - an unverifiable point-in-time score; (3) the pasted 16-test expected-output block naming individual test titles - the suite now has 45 tests across 15 describe blocks, and a hardcoded test-name list in prose is a maintenance trap; (4) the three per-vertical 'Visible Sidebar Items' checklists - they listed a top-level `Finance` group and top-level `Vehicles`/`Services` entries that do not match the current tree. Also CORRECTED: 1.0's 'future vertical-specific modules not yet in sidebar (menu, tables, batch_expiry)' - all three are in the sidebar today (batches/expiryWriteOff under BatchExpiry, tables under Tables, kitchen under Menu, compositeItems/menus/modifierGroups under ['Menu','CompositeItems'])."
  - "M7 T9 CONTENT ADDED to frontend-navigation.md, all source-derived: a table of ALL 15 top-level nav groups with their real module/permission gates (extracted programmatically from Sidebar.tsx, not transcribed); the vertical-adaptive mechanism as it actually works (buildNavigation + AUTOMOTIVE_VERTICALS decides WHERE the Services children mount; VERTICAL_NAV_KEYS + getNavLabel decides the LABEL via catalog:vertical.<vertical>.<VERTICAL_NAV_KEYS[key]> - the map's VALUE, not the nav key, so modifierGroups resolves to ...modifierGroup, singular, corrected per M7 review P3-5; the top-level catalog group deliberately does NOT adapt); the three-step label resolution order; the fail-closed loading/error behaviour with an explicit 'do not fix this by defaulting hasModule to true'; and hasModule's defensive object-shape normalisation and why it exists."
  - "M7 T9 ROUTE RECONCILIATION section, built from T3's regenerated manifest (M4): routes-web.yaml carries 265 route records, routes-pos.yaml 9. The four Wave 0 deletions are CONFIRMED ABSENT from the manifest by grep - /pos/shifts, /marketing, the bare /finance index (the finance parent and all /finance/* children remain), and the /settings/chart-of-accounts duplicate mount (only /finance/chart-of-accounts remains) - and are tabulated with their dispositions so nobody re-adds them. T1's remaining orphan candidates are CONFIRMED STILL PRESENT in the manifest by grep (/growth, /growth/modules, /scheduling/capacity, /treasury/payment-methods, /treasury/sales-withholding-tracking, /inventory/delivery-notes/consolidate, /finance/lane-separation, four /settings/compliance/*) and are labelled CANDIDATES, NOT RULINGS, with the report's own unresolved-expression caveat carried across. UI-39 (no automated route<->nav coverage check exists) is stated so the manual nature of this reconciliation is explicit."
  - "M7 T9 CROSS-LINKS: both docs link 00-EXECUTIVE-REPORT.md as the audit of record, as the brief requires. DISCLOSED HONESTLY IN BOTH DOCS: docs/sessions/ is gitignored (.gitignore:58), so the executive report is an untracked session artefact that may be ABSENT from a fresh clone; only 17-listing-census-recensus.md and 18-colorclasses-migration-plan.md are tracked (force-added at M1/M6). The brief mandates the cross-link, so it is present, but the dangling-link risk is stated rather than hidden. Force-adding the executive report was NOT done - it is outside T9's scope (rule 4) and is flagged for the parent instead."
  - "M7 T9 DISCOVERED FINDING, reported not fixed (rule 4): Sidebar.tsx's own docblock at :143 still tells the reader that the Automotive group 'is hidden downstream by the `isModuleEnabledForVertical('automotive')` filter'. That function does not exist either - the real gate is isNavItemVisible on module: ['Vehicle','Workshop','PlatformIntegration']. The BEHAVIOUR the comment describes is correct; only the named function is stale. T9 is a docs-only task and touching Sidebar.tsx source is outside its scope, so the doc now states the real mechanism and this stale code comment is left for the parent to ticket."
  - "M7 T9 NO-MANIFEST-DRIFT constraint HELD: `git diff --name-only 6e51c4e48..85d2f8a26 -- scripts/factory/` is EMPTY and `bash scripts/factory/check-manifest-drift.sh` exits 0 at the M7 tip. The branch's two-manifest-commit model (00366d151 T3(a) + a6dc879b8 M5 sync) is untouched; T9 changed no route, so no regeneration was triggered and no STOP was needed."
  - "M7 gate outputs: T9 changes ONLY markdown (docs/architecture/design-system.md, docs/architecture/frontend-navigation.md). The repo has NO markdown lint and no docs build - there is no .markdownlint*/.markdownlintrc, and no markdownlint/remark/mdlint/docs:lint target in the root package.json, apps/web/package.json or scripts/preflight.sh - so there is no docs gate to run, stated explicitly rather than skipped silently. The preceding commit 6e51c4e48 (which DID touch one .tsx comment) ran the code gates: pnpm typecheck clean; pnpm lint EXIT=0 with 0 errors / 6520 warnings (unchanged from M6), Gate C 0/0/0, design-system 818 acknowledged / 0 new / 0 stale, audit-quantity 0/0/0, all three RuleTester suites pass. Manifest drift exit 0."
  - "M7 CLOSE-OUT: bridge ran as ONE register (round 1, ACCEPT, zero fix rounds) - docs/handoff/reviews/ui-wave0/M7-round1.md at e7cf6b0ca, range 2621abbff..1dff4a00e, lens general. NO P1. Six corrections applied at the M8 opening commit (Phase 0.9.4), all documentation-only: P2-1 (frontend-navigation.md's orphan list was under-inclusive by four routes - the section now states the report's own 15/4/3 split, marks four of the 15 views as the Wave 0 deletions, and enumerates all 18 remaining candidates under three headed sub-lists, so 22-4=18 closes on the page); P3-1 (the two acceptance greps above restated with the exact command and scope that reproduces each); P3-2 (an ActiveFilters row added to the components table with its five real consumers, the PROPOSED FilterBar bullet now tells readers not to hand-roll a chip row, and FilterPanel's 'the one purpose-built filter container' line now discloses its single feature consumer); P3-4 (the two-layer enforcement claim in the Overview is now explicitly the RULE, qualified with the manifest's own numbers - 34 of 265 web routes carry neither module_gate nor permission, 138 of 265 carry no module_gate; both re-derived from scripts/factory/manifests/routes-web.yaml at the M8 tip); P3-5 (the VERTICAL_NAV_KEYS label path corrected to catalog:vertical.<vertical>.<VERTICAL_NAV_KEYS[key]> in both the doc and this ledger, per Sidebar.tsx:133-136,392-397). P3-3 was handled as the parent directed and NOT by force-adding: every markdown LINK to 00-EXECUTIVE-REPORT.md in both docs is replaced by a plain-text reference naming the file, naming its session location docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/, and stating it is untracked (.gitignore:58) - three sites in design-system.md, one in frontend-navigation.md; no file was force-added and docs/sessions/ remains gitignored."
  - "M7 open for the parent (SUPERSEDED in its first clause by the M7 CLOSE-OUT entry immediately above; the carried items in its last sentence remain live): at the time of the M7 handover the bridge review for M7 (lens general) had NOT been run - milestone status was `review`, verdict null. `--range 2621abbff..HEAD` covers exactly the M7 work (6e51c4e48 ledger+M6 doc fixes, 85d2f8a26 T9). Full-suite `pnpm test` was not run for M7 (M8 owns the whole-branch gate) and would be uninformative for a markdown-only change. UI E2E not run (M8 item). No push was made. Carried to M8/merge: the M6 P3-3 merge-time C6 baseline re-check, the P3-4 stray-tree deletion, the P3-2 chip-accent question for the UI audit, and the Sidebar.tsx:143 stale-comment ticket."
  - "M8 execution: NO new implementation. Two commits - 4706d437d (Phase 0.9.4: M7 close-out plus the M7 register's six documentation corrections) and the handback/ledger commit that records this row. Branch tip and name were re-verified immediately before each commit (e7cf6b0ca before 4706d437d); no foreign commit was ever observed; nothing was pushed; `git stash` was never used. Vitest was run at --maxWorkers=1 only, and zero orphan worker pools survived the run."
  - "M8 §4 item 1 - ./scripts/preflight.sh is ENVIRONMENT-BLOCKED and is reported as BLOCKED, never as green and never as 'PHPUnit skipped'. It halts on its FIRST stage (Pint, preflight.sh:58) because apps/api/vendor/ does not exist in this worktree, so the run never reaches the PHPUnit stage and the expected red INCOMPLETE banner was never printed. Blocked stages, named: Pint, PHPStan, PHPUnit, the typescript:transform drift guard and the permissions:export-frontend-map drift guard. Bounding evidence, not a substitute: `git diff --name-only d682b38ec..HEAD -- apps/api` = 0 files and the same diff over packages/shared/types/generated.d.ts + apps/web/src/hooks/permissionsMap.generated.ts = 0 files, so the branch gives those stages nothing to judge and neither drift guard can fire. EVERY non-backend preflight stage was run individually and PASSES: pnpm typecheck exit 0; pnpm lint:eslint exit 0 (0 errors); audit:keys Gate C 0/0/0; audit:design-system 818 acknowledged / 0 new / 0 stale; audit:quantity 0 total / 0 new / 0 stale; apps/pos pnpm test:eslint-rules exit 0 (no-hardcoded-step 6 valid/3 invalid, no-raw-quantity-input 6 valid/1 invalid); check-manifest-drift.sh exit 0; apps/pos/scripts/check-fiscal-fixture-parity.sh exit 0 (2 files / 29 tests). The parent's terminal audit should run preflight from a checkout that has apps/api/vendor/."
  - "M8 §4 item 2 PASS - `bash scripts/factory/check-manifest-drift.sh` exits 0 at the M8 tip. routes-web.yaml:762-764 records /sales/invoices/:id -> InvoiceDetailPage (proving T2 landed before T3(a)), and `grep -nE '^  - path: (/pos/shifts|/marketing|/finance|/settings/chart-of-accounts)$' scripts/factory/manifests/routes-web.yaml` exits 1 with zero hits. The branch carries exactly two manifest commits, 00366d151 (T3(a)) and a6dc879b8 (the parent-authorised M5 sync)."
  - "M8 §4 item 3 PASS with the categorised delta accounted line by line. `node apps/web/tools/audit-design-system.mjs` exits 0 at 818 acknowledged / 0 new / 0 stale. Baseline census base d682b38ec -> tip 4706d437d: C1 12->12, C2 251->250, C3 459->458, C4 14->14, C5 2->2, C6 0->82, total 738->818. C2 -1 = T10's hand-removed ShiftDashboardPage.tsx line; C3 -1 = T10's hand-removed POSShiftsDashboard.tsx line; C6 +82 = T7's detector fix. C5 shows one removed and one re-added BYTE-IDENTICAL line (RepositoryMovementsTab.tsx) - a trailing-comma reflow caused by C6 entries being appended after it - so net movement is zero and C1/C4/C5 are frozen exactly as the contract requires. Those three are the ONLY non-C6 lines in the whole-branch baseline diff. --write-baseline was not used at T10. F-1 CONSTRAINT VERIFIED AT FINAL STATE: the three BarcodeHero baseline entries are PRESENT (C2 :223, :224 and C3 :664), BarcodeHero.tsx and ProductEditHero.tsx both still exist, and the unconditional successDot is still at ProductEditHero.tsx:136."
  - "M8 §4 item 4 PASS. /pos/shifts: the route-shaped grep `grep -rnE \"pos/shifts\\\"|'/pos/shifts'|POSShiftsDashboard|ShiftDashboardPage\" src --include='*.ts' --include='*.tsx'` exits 1 (zero); the broad `grep -rnE \"/pos/shifts\" src --include='*.ts' --include='*.tsx'` returns exactly SIX hits, all the permitted API path strings in features/pos/api/shiftApi.ts (:77,:88,:95,:156) and shiftHistoryApi.ts (:52 plus the :39 doc comment naming the backend endpoint) - called out explicitly, not filtered away. T10 docs grep is reported under the brief's 'retain as historical' disposition: every ShiftDashboardPage hit in .md lives inside apps/web/src/features/pos/README.md and IMPLEMENTATION_SUMMARY.md, both of which carry the dated superseded note at :3. Baseline grep zero. /marketing zero. FinanceHubPage zero. settings/chart-of-accounts zero. The production-only /finance inbound grep returns zero, and the seven intentional HubCard.test.tsx fixture literals it excludes are enumerated (lines 12, 22, 35, 54, 63, 87, 103) with `git diff --stat d682b38ec..HEAD -- HubCard.test.tsx` EMPTY, i.e. byte-identical to base. `grep -rn Navigate src/routes/index.tsx | grep -i finance` returns zero, proving the superseded redirect was not introduced. MTP-GL-28 is gone from apps/web/e2e (grep exit 1) and the ['24-finance','/finance'] entry is gone from tools/ui-audit-shots.mjs. finance:hub.cards.treasuryOverview.title survives in en/fr/ar with exactly Treasury / Tresorerie / al-khazina, and it is the only surviving finance hub.* key."
  - "M8 §4 item 5 PASS subject only to the reviewed M0b exceptions. pnpm typecheck exit 0. pnpm lint exit 0: 6520 problems, 0 ERRORS (unchanged since M6), Gate C 0/0/0, design-system 818/0/0, audit-quantity 0/0/0, all three apps/web RuleTester suites pass. Deterministic full suite `pnpm test -- --maxWorkers=1`: Test Files 3 failed / 665 passed (668); Tests 5 failed / 4259 passed / 1 skipped / 3 todo (4268); duration 707s. THE FULL RED LIST, ENUMERATED AGAINST THE EXCEPTION LIST, with no red outside it: (1) src/__tests__/i18n/arLocaleCoverage.test.ts - 3 tests, ar/common.json missing 122 keys, ar/vehicles.json 19, ar/workshop-technicians.json 1, all three counts UNCHANGED from M6 and on namespaces this wave never touched, owner-confirmed exception owned by CODEX-DISPATCH-arabic-i18n-backfill-2026-08-10.md; (2) tools/__tests__/offset-pagination-meta-consolidation.test.mjs - 1 test, the M0b bridge-confirmed real defect at src/features/treasury/statements/api.ts:112; (3) src/components/__tests__/SharedSingletons.tenantScope.test.tsx - 1 test, the M0b bridge-confirmed real defect at AddQuickProductModal.tsx:209. Exceptions (2) and (3) are PRODUCTION defects, not test debt, and per the M0b findings they are NOT accepted as standing gaps - the parent terminal audit must route both to a live production-fix lane before merge. None of the load-induced timeout flakes seen in earlier concurrent runs recurred at one worker."
  - "M8 §4 item 6 - the UI E2E run is ENVIRONMENT-BLOCKED and is reported as blocked with the blocker named, not omitted. The money-campaign specs require a live local stack (finance-permissions.spec.ts:1-19: web :5173 -> api :8010, tenant demo-pharmacy-tn, real login, real backend, no mocks). Nothing listens on :8010 (curl returns 000) and apps/api/vendor/ is absent here, so the API cannot be started from this worktree. The run was ATTEMPTED, not assumed: `pnpm exec playwright test --max-failures=1 --reporter=line --workers=1` started Vite successfully on :5173 and every /api/v1/* proxy call failed with AggregateError [ECONNREFUSED]; the first spec alphabetically timed out waiting for backend-fed UI and the run stopped with 1 failed / 459 did not run. That is an environment failure, not a wave regression, and --max-failures=1 bounded it deliberately because with no backend all 460 specs fail identically. CONSEQUENCE, stated plainly: the brief's requirement that finance-permissions.spec.ts PASSES with MTP-GL-28 removed COULD NOT BE EXECUTED here; what is verified statically is that MTP-GL-28 is absent and that the file's only change is that removal (2 insertions / 61 deletions). NO spec was intentionally skipped, so the screenshot-per-skip rule does not apply. This item is handed to the parent, which owns the visual/E2E pass against a live stack."
  - "M8 handback: docs/handoff/HANDBACK-ui-wave0-2026-08-11.md now covers M0 through M8 per §5 - header with the verbatim commit_series answer and the full commit list (53 commits at 4706d437d, 54 with this report's own commit), per-task sections through T15, the six-item whole-branch verification section, the blocked-items table, eleven discovered findings, nine deviations, and the completion-language section. UI-43 is reported PARTIAL with its four claims itemised (T8-a DONE in T4/M5 77257bf88, T8-c DONE in M6 ee87f153e, T8-b BLOCKED under F-1 with no commit, touchOptimized NOT IMPLEMENTED and UNASSIGNED under F-2b). The F-2b ownership gap and the standing constraint records F-1/F-2b/F-3/F-4/F-5/F-6 plus the DO-NOT-TOUCH set are all restated. `grep -nE \"Wave 0 complete|UI-43 closed\"` over the handback returns ZERO - the §5 item 7 prohibition holds."
  - "M8 open for the parent: M8 status is `review` and the wave status is `review`. M8 is the FINAL milestone - the parent runs the wave's terminal gate (full-branch adversarial review plus the specialized reviewer agents) and the visual E2E pass itself, and owns the merge into dev. Carried into that gate: the two environment-blocked §4 items (preflight, E2E); the two M0b production defects needing a live fix lane; M1's 33-vs-35 secondary-surface P2 and the report-path ordinal collision; M3's /finance blank-pane owner question; the M6 P3-3 merge-time design-system baseline re-check (re-run the audit on the MERGED tree, require 0 new / 0 stale, do NOT --write-baseline); the M6 P3-2 chip-accent design question; the M6 P3-4 stray apps/web/apps/web/src/ tree deletion; the Sidebar.tsx:143 stale-comment ticket; the TermsOfServicePage brand/rule-11 leak; and the F-1 and F-2b owner gates. Nothing was pushed."
