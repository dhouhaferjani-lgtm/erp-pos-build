# Codex prompt — W-CASH-1 fix round 1 (paste into the SAME thread that wrote the W-CASH-1 plan)

---

Fix round 1 for your slice plan docs/superpowers/plans/2026-09-06-WCASH-1-custody-config-transfer-doc.md. Read the gate review docs/superpowers/reviews/2026-09-06-w-cash-1-slice-codex-gate-r1.md in full and revise the plan IN PLACE (same path) to rev 2. Read-only on everything else: no code, tests, migrations or git commands. Do not ask questions; when done reply with the path only.

Non-negotiables for rev 2:
1. Owner authority: Q11–Q13 are now RULED (docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md, section "Q10–Q13 RULED"). Replace the "OPEN" register with the ruled text verbatim. This slice still ships no reason-code enum, no drawer-session model and no alignment; state that these are DEFERRED to later slices under the rulings, not withheld pending a decision.
2. Deployment: delete the slice's own deployment prose and reference the canonical manifest docs/superpowers/plans/2026-09-06-parapharmacy-staging-push-manifest.md exactly as its §5 prescribes, supplying every per-slice variable it requires (migration list, flags with config key + env var, censuses, web changes yes/no with the web deploy block, device build no, rollback point per push, host-side backup step). Note the manifest's UNVERIFIED items U-1/U-2 as promotion preconditions.
3. Replay authorization and semantic identity: define the idempotency contract precisely — operation UUID scoped by (tenant, company, actor authorization at first submission); a replay by a different actor or with different semantic content (source, destination, amount, currency, reason) is a CONFLICT (409 with the existing document id), never a silent success; state the unique index and the comparison fields; add the red tests.
4. Close every MAJOR with a plan change (T1 tenant/company/actor consistency incl. compatibility topology; T2 command path/namespace matching the Treasury module conventions and its operational contract; T2 repository-metadata protection with a named writer and lock order compatible with TreasuryMovementService; per-task red-first contracts and convention-09 named tests with explicit rerun outcome; T5 complete HTTP contract with W1 location-scope as a hard prerequisite named by task; convention-11 migration of ALL shadow types including POS client types) and every MINOR, or reject with a path:line citation in a change log at the top.
5. Keep ≤6 tasks; every claim about current code cites path:line at current HEAD (re-verify the ones the gate flagged as drifted).
