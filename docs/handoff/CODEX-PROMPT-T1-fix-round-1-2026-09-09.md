# Codex prompt — lane T-1 fix round 1 (paste into the SAME `T-1 transfers/requests edge campaign` thread)

Fix round 1 for lane T-1 on branch `lane/t1-transfers-edge` (worktree `.worktrees/t1-transfers`). Read the gate report in full: `docs/superpowers/reviews/2026-09-09-t1-transfers-gate-r1-inventory.md` (verdict MERGE-WITH-FIXES: M1–M4, m1–m5). Close every item; one commit per major, one for the minors; cite `path:line` at current HEAD in the handback.

- **M1** (cases 4 and 14 were asked to PIN CURRENT behaviour): keep the skipped desired-behaviour tests as they are (ticket-linked), and ADD a green test per case that pins today's outcome verbatim (recalled/expired lot still lands at destination on complete; requester cancel refused once `in_progress`), named `..._pins_current_behaviour_until_ticket_<id>` with the ticket id in the docblock, so the day the ticket lands both tests flip together.
- **M2** (evidence claims reader agreement its own transcript contradicts): correct `docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md` §case 5 — `/products/{id}/stock-levels.incoming` is PO remainder (`ProductController.php:1073-1093`), not transfer in-transit; state which three readers were compared, show their transcripts side by side, and add the stock-levels endpoint as an explicitly different concept (not a fourth reader).
- **M3** (same denial, two contracts): the new guard in `CreateTransferFromRequestsRequest.php` must produce the SAME envelope as `/stock-transfers` (`403` with `LOCATION_ACCESS_DENIED`, see `StoreStockTransferRequest.php:20-25` and the rule's typed exception); the case-15 test asserts status AND error code, not `assertUnprocessable()` only. Stay ≤ 20 lines in the seam.
- **M4** (convention 09): second-company test on the newly guarded endpoint — company B's location as source for a company-A request → refused, nothing created; plus the positive control.
- **Minors m1–m5**: T1-11 ticket severity + statement that the listener is synchronous (`ReplenishmentServiceProvider.php:19`) and that the real gap is the quantity-blind settlement; rename the case-6 test to what it proves; remove skip-rot (skips carry the ticket path); strip demo-tenant residue from fixtures; add `RefreshDatabase` (or the PG-lane equivalent) to the PG class now in the live allowlist.

Verification (by file only, never the full suite): the touched SQLite classes; the PG classes on `autoerp_test_t`; `php tools/feature-lane-manifest-check.php`; Pint + PHPStan on the touched PHP. Update the handback with a "Fix round 1" section. Stop with `status: review`. Do not merge, do not push.
