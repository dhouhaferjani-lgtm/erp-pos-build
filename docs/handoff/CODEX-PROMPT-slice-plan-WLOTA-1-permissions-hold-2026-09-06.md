# Codex prompt — slice plan W-LOT-A-1: lot permissions, general-manager role, policy-neutral hold

Paste the block below into a NEW Codex thread (Desktop) or let Fable run it via CLI (`codex exec --sandbox read-only -m gpt-5.6-sol -c model_reasoning_effort=high`). One slice per thread; never reuse the thread that produced the 130 KB package plans.

---

NON-INTERACTIVE SLICE-PLAN TASK — do not ask questions or stop; your final message is the COMPLETE slice plan in Markdown (it is saved verbatim to docs/superpowers/plans/). Read-only sandbox: read any source; no file edits, no tests, no git writes. Repository: /Users/houssamr/Projects/syneriva/apps/erp (HEAD = local dev; declare the SHA you read; every code claim cites path:line at that SHA and must be true).

Scope of THIS slice only (≤6 tasks, each fully dispatchable):
1. Enforce action permissions on the body-less BatchExpiry routes: recall, destroy/deactivate, traceability and read routes (apps/api/app/Modules/BatchExpiry/Presentation/routes.php:12-48), reusing the seeded permissions batches.view / batches.traceability / batches.delete / batches.recall (apps/api/database/seeders/RolesAndPermissionsSeeder.php:403-409). Keep module:BatchExpiry + company checks. Validate location_id as UUID in BatchController::expiring() and scope it like expired() (:215-236 vs :252).
2. Web layer (rule 12): gate batch routes/actions with RequirePermission / usePermissions; Sidebar.tsx:222-223 currently gates on module only. Regenerate the frontend permission map.
3. New seeded role general_manager = manager permission set + batches.recall + treasury.manage_all_locations, assigned only with an unrestricted user_company_memberships.allowed_location_ids = NULL (cite LocationContext.php:194, UserController.php:241); seeded manager LOSES batches.recall. Delta migration/command that preserves custom roles (no blind syncPermissions). Reseed + permission:cache-reset per tenant in the deploy steps.
4. Policy-neutral branch hold: table batch_recall_requests with ONLY the states that are true under every Q10 branch: requested, recalled. A branch manager with batches.recall.request (new permission, granted to manager) creates a request for a lot present at one of their allowed locations; creation immediately blocks that lot at that location for POS sale (FEFO eligibility: FEFOInventoryService.php:258-272 and :930-934), direct batch transfer (TransferBatchStockRequest / BatchStockService) and StockTransferService allocation; the general manager executes company-wide recall (existing is_recalled path, now permissioned) which transitions requested → recalled. Stable operation UUID on the request for idempotent retry; append-only; reason mandatory. NO release/reject state, route, permission or test — Q10 is OPEN; add a "withheld behind Q10" section naming what the ruling will add.
5. Tests (red first): convention-09 second company via real registration, second location, re-run idempotency; cashier/viewer denied 403 with unchanged stock/history snapshot; manager request at allowed location succeeds and blocks sale+transfer at that location only; manager request at a non-allowed location 404 (scoped-not-found); general_manager recall spans branches; manager cannot recall company-wide; PG lane for the hold-eligibility queries.

Mandatory format:
- Convention-10 matrix in the exact format: ID | Guarantee | Odoo | ERPNext | Dolibarr (or NV + reason) | AutoERP today path:line | Gap | Decision (MATCH/DEFER/DIVERGE/ALREADY), with rows for create, duplicate, edit, cancel, rerun, second company, second location, permission, audit. Benchmark facts you may cite: Odoo lot hold via quality_hold / OCA stock_lock_lot, no approval chain; ERPNext Batch disabled + User Permission on Warehouse; GMP hold/release norm (only the quality authority releases). Link sources.
- Convention-11 Vocabulary line reconciled with docs/glossary.md (Recall request, Branch hold, General manager). Propose the glossary rows.
- Owner rulings: copy Q10 VERBATIM as OPEN from docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md §Q10–Q13; do not decide it.
- Per task: exact production files verified at HEAD (no placeholders), full signatures, COMPLETE migration schema (every column: type, nullability, default, FK + on-delete, checks, indexes, company-scoped uniques, JSONB DTO class), PHP enums for every status/type column (rule 9), red-first tests with exact file + class::method + first failing assertion + exact command + lane (phpunit PG / phpunit sqlite / vitest), implementation steps, reviewer gate (tenancy-authz-reviewer + inventory-costing-reviewer), rollback.
- Deployment: this slice's pushes reference the shared staging push manifest docs/superpowers/plans/2026-09-06-parapharmacy-staging-push-manifest.md (Fable writes it; if absent, state the exact steps you need from it: additive migration push, reseed + cache reset, permission-map regen, web deploy + fingerprint, rollback point).
- End with dispatch order and a verification checklist. Title: "Slice plan W-LOT-A-1 — lot permissions, general-manager role, policy-neutral branch hold (rev 1)".
