# Treasury Phase ④ — Expense Depth: spec+plan cycle kickoff (2026-07-13)

> **STATUS ADDENDUM (2026-07-13 PM — readiness audit + E2 verification done; two Opus lanes, evidence-cited):**
>
> 1. **✅ E2 IS FIXED on dev — do NOT re-verify, do NOT scope a fix.** `GeneralLedgerService::createFromExpense` branches on `is_paid` (unpaid → Cr **SupplierPayable** with `partner_id`, `GeneralLedgerService.php:3370-3384` — the comment names the old bug; paid → Cr Cash/Bank). Unpaid posts write **no** treasury movement (`ExpenseService.php:240`); `settle()` books mirror Dr AP / Cr Cash via `createExpenseSettlementJournalEntry` (GL `:789`) + one `Out` movement on idempotency leg `settlement` — no double-credit; rejects `LinkedCost`/already-settled/already-paid. Test-asserted: `tests/Feature/Expense/ExpensePostSpineTest.php:109` (unpaid credit IS SupplierPayable, NOT Cash, zero balance change) + `:53` (paid path) + atomicity `:160`; settle coverage in `ExpenseSettlementTest.php`. The spec §5.3 "Cr SupplierPayable (unpaid) | Cash/Bank (paid)" line correctly describes SHIPPED behavior that W1's VAT split layers onto.
> 2. **Cycle state: spec Rev 2 + 4-lane adversarial review COMMITTED (`f2dc43f55`); plan WRITTEN (`docs/superpowers/plans/2026-07-13-treasury-phase4-expense-depth.md`).** Plan↔spec consistency spot-checked ✓ (widening migration = spec §5.3 note; `bcround` naming; gate structure matches mandate).
> 3. **REMAINING before dispatch (in order):** (a) **independent plan review** — Fable lane on W1 money-path wave, Opus on W2–W4, saved to `docs/superpowers/specs/reviews/` (plan's own "self-review notes" section does NOT satisfy this); (b) fold fixes → **author `docs/handoff/CODEX-treasury-phase4-expense-depth-2026-07-13.md`** with the Phase-③ §3 autonomous-gate protocol — **owner re-confirmed 2026-07-13: FULLY AUTONOMOUS Codex implementation with proper gates, same rigor as Phases ②/③, non-negotiable** (autonomous `claude -p` gates per wave: GATE 1 Fable-tier treasury on W1 money path; GATE 2 Opus + tenancy-authz w/ Fable escalation on money-path BLOCKER/HIGH only; GATE 3 Opus + frontend-conventions; GATE 4 final full-branch multi-lane — ALL must APPROVE before the merge request goes to the owner; no gate may be skipped or self-attested by the executor); (c) push clean-ff; (d) **STOP at owner dispatch gate**.
> 4. **Interlocks re-checked 2026-07-13 PM:** banks-tug merged (`bb44387f1`, PayExpenseDialog post-polish on dev) ✓; phase3-followups branch (head `8f174fe85`) has ZERO expense-file overlap — merge-gated + merged into local dev same day (Phase ④ worktree must be cut off the post-merge dev tip: followups own `TreasuryAlertRecipients`, the W2 exemplar); permission-map generator: 0 `usePermissions.ts` changes yet (plan Task 9 sequencing note stands); design-sweep expense commits already in dev — FE waves re-verify line anchors (plan Tasks 5/15 already say so).
> 5. Working-tree hygiene note: stage Phase-④ artifacts SELECTIVELY — `docs/design/pos-clean-concept.*` (unrelated POS mockups) and the stray `apps/web/vite.config.replenishment8020.mts` deletion (confirmed unreferenced) must not ride along.

**Mandate (owner, 2026-07-13):** run the full spec/plan cycle for treasury Phase ④ — expense depth — same process as Phases ②/③: brainstorm → owner scope decisions (AskUserQuestion, batched) → spec → adversarial review (saved to file) → Rev 2 → writing-plans → plan review → Codex brief with AUTONOMOUS `claude -p` gates (Opus standard, Fable 5 escalation only on money-path BLOCKER/HIGH) → **STOP at the owner dispatch gate**. Execution dispatch and the final orchestrated whole-branch review follow the Phase-③ pattern exactly (gate protocol to copy: `docs/handoff/CODEX-treasury-phase3-cash-visibility-2026-07-12.md` §3; final-review pattern: `docs/superpowers/audits/2026-07-13-treasury-phase3-final-review.md`).

## Ground truth to load first

1. **Phases ①–③ are ALL SHIPPED on origin/dev** — Phase ③ merged at `05674a586` (2026-07-13; final review all-APPROVE, same-day). Also merged: `feat/treasury-ui-gaps` (adjust-balance + **expense pay dialog**), `feat/bank-reference-verification` (banks + BankPicker). Memory: `project_treasury_phase3_cash_visibility`, `project_treasury_phase2_instruments`, `project_treasury_spine_rebuild`.
2. **What Phase ④ is, per the owner's 5-phase decomposition:** ④ expense depth → ⑤ bank statement import. Gap register P3 block: `docs/superpowers/audits/2026-07-07-treasury-industry-gap-audit/README.md` §4 (E-series) + §6 (G16–G20). **Verify against code, don't trust the register** — several inputs have moved since it was written:
   - **G16 — VAT/input-tax split (E1):** no tax field anywhere; recoverable TVA déductible booked 100% gross into expense — overstated costs, unclaimed VAT, real money. The headline item.
   - **G17 — supplier link (E4):** free-text `vendor_name`; GL hardcoded `partner_id => null` (was `GeneralLedgerService.php:2311` — re-anchor). Blocks per-supplier reporting and any expense→AP bridge.
   - **G18 — expense analytics (E5/E10):** category×period endpoint + summary tiles; list page still lacks `date_to` UI + export. NOTE: report export is a known repo-wide hole (`/report/export` endpoint doesn't exist; counting report hid its buttons behind `REPORT_EXPORT_ENABLED=false`) — decide deliberately whether Phase ④ builds real export or inherits the flag.
   - **G19 — recurring expenses (E3):** templates (rent/salaries/subscriptions) + generation + forecast feed. The notification center NOW EXISTS (Phase ③) — due-reminder delivery is one `Notification::send` away; recipient resolution exemplar: `TreasuryAlertRecipients` (tenant team id + company-membership filter + registrar flush — copy it, do not reinvent).
   - **G20 — expense payable-by-instrument (E7):** expense paid by check must clear the till at CLEARING, not immediately. ⚠️ **Phase ② deferred SUPPLIER-direction (outbound) instruments entirely** (cutover was customer-direction only). G20 likely requires outbound instrument registration first — the spec must scope this explicitly or defer G20 with a reasoned note.
3. **🔑 E2 — unpaid-expense accounting bug — VERIFY STATUS FIRST (was P0/G2):** the register says `createFromExpense` credits Cash/Bank UNCONDITIONALLY even when `is_paid=false` (no AP liability → GL cash ≠ treasury balance on every unpaid expense). The ui-gaps track shipped the PAY dialog over the existing `/expenses/{id}/pay` endpoint (`ExpenseService::settle`), which implies unpaid expenses are now a first-class state — but whether the POSTING side was fixed is unverified. Read `GeneralLedgerService::createFromExpense` + `ExpenseService::settle` before scoping: if E2 is still live, it is an active books-corruption bug and Phase ④ MUST lead with it (it also reshapes G16/G20 posting design).
4. **What already exists (don't rebuild):** expense cutoff E2E (log → categorize → attach → post → JE + treasury decrement + idempotency); landed-cost Phase 1 (`expense_kind=linked_cost` settles at capitalization — the pay flow excludes it; any Phase-④ posting change must respect this split); category→GL mapping UI; `treasury.adjust`/`expenses.pay` FE+BE permission wiring; cash-movements report (G12) as the unified outflow view.
5. **Interlocks (check branch/worktree state at session start):**
   - `chore/treasury-phase3-followups` (Codex brief `docs/handoff/CODEX-treasury-phase3-followups-2026-07-13.md` — dispatch pending as of writing): owns `RepositoryDetailPage.tsx`, `TransferCashModal.tsx`, `PaymentRepositoryController::update`, `TreasuryAlertRecipients`, `CashPositionController` comment. No expense-file overlap expected.
   - `chore/banks-tug-polish` (merge pending/done as of writing — verify): touched `PayExpenseDialog.tsx` + `AdjustBalanceDialog.tsx` + `PartnerData` DTO. **`PayExpenseDialog` is Phase-④-adjacent — base on whatever dev tip includes the polish merge.**
   - `feat/design-system-unification` (gate 4 in progress): may touch expense pages in its PageHeader pass — re-check `git log origin/dev -- apps/web/src/features/expenses` before FE waves.
   - `docs/handoff/CODEX-replenishment-followups-2026-07-12.md` (dispatch pending): permission-map generator — could collide on `usePermissions.ts` if both add permissions; sequence or note.
6. **Standing invariants (non-negotiable):** every cash movement through the Phase-① port with a JE (`postEntryNow`, never afterCommit); global lock order instrument→documents→GL advisory→repository; precision rule 19 (incl. FormRequest regex ceilings — VAT percent is NOT currency-scaled: `/^\d+(\.\d{1,2})?$/`); rule 20 in console/queue contexts; reconcile checks #1–#4 stay green; fiscal perimeter untouched; `journal_entries(source_type,source_id)` has NO global uniqueness — source-type-scoped partial indexes only (posted-scoped if the flow re-inserts drafts, see the Phase-③ `treasury_transfer` index lesson).
7. **Deploy reality:** no production tenants; staging auto-deploys from dev (entrypoint-automated migrate/reseed/cache-reset). **Stacked deploy owes at writing** (apply to any tenant provisioned before their merges): Phase ③ (`treasury-phase3-deploy-checklist.md` — notifications table + JE index + `treasury.transfer` perm), banks backfill (`bank-directory-deploy-checklist.md` — bare `tenants:run db:seed` silently no-ops), location-hierarchy `tenants:migrate`.

## Research scope

- TVA déductible mechanics FR (44566) + TN (4366x — verify exact TN sub-accounts; the Phase-③ research found procomptable PDFs unparseable, use OECT sources): partial deductibility/prorata, when VAT is recoverable on expenses vs blocked (véhicules de tourisme, carburant % rules), invoice-required threshold. Flag account codes as expert-comptable decision points.
- Recurring expense patterns: Odoo (recurring vendor bills), QBO (scheduled transactions) — template fields, generation timing (in-advance draft vs on-date auto-post), skip/pause semantics, forecast integration.
- Expense analytics baselines: category×month matrix, top-vendors, MoM deltas — what Odoo/QBO/Pennylane show on their expense dashboards.
- Supplier-direction (outbound) instruments GL if G20 stays in scope: 403 "Fournisseurs — effets à payer" (FR) / TN equivalent; interaction with the Phase-② instrument state machine (which is inbound-only today).

## Process contract (repo standing rules — same as Phase ③)

- superpowers:brainstorming FIRST; batch genuinely owner-level scope forks via AskUserQuestion (Phase-③ precedent: 4 questions, one round); spec to `docs/superpowers/specs/2026-07-13-treasury-phase4-expense-depth-design.md` (adjust date to actual); adversarial review saved to `docs/superpowers/specs/reviews/` — **treasury-reviewer Fable-tier for the posting/VAT/GL surfaces** (owner tiering: crucial financial-spine), Opus for analytics/recurring/FE lanes, tenancy-authz-reviewer if any new permission/notification surfaces; reconcile Rev 2; writing-plans; plan review (Fable on money-path wave, Opus rest); Codex brief with autonomous gates; STOP at owner dispatch gate.
- Owner decision points flagged in a spec section (Phase-② §14 / Phase-③ §12 style) with defaults chosen — GL account codes, VAT prorata policy, recurring auto-post vs draft, G20 in/out.
- All artifacts committed to dev + pushed (clean ff) so the Codex worktree sees them.

## Session-start prompt

"Read docs/handoff/HANDOFF-treasury-phase4-spec-kickoff-2026-07-13.md and run the Phase ④ spec/plan cycle it mandates."
