# Lane F1 — products import can target a location; in-tenant companies get a coded default location

Branch `fix/f-bug-1-import-location` (worktree `.worktrees/f-bug-1`, base `dev` = `a4ceeb0f5`). Triage + evidence: `F-BUG-1-TRIAGE.md` (read it first). DESIGN PRE-APPROVED — do not stop to ask; implement exactly this.

Rules that apply (from `CLAUDE.md`): TDD (red → green, tests by path), no placeholders, strict types, constructor injection, enums, `t()` for every FE string (en + fr + ar), design tokens in touched TSX, `tenantScopedKey` on tenant queries, migrations self-guarding (push to `origin/dev` auto-migrates every tenant). Do NOT touch anything outside the four tasks. Do not run the full PHPUnit suite — run the touched tests by path.

## Task 1 — backend: `CompanyController::store` provisions the default location with `code = 'MAIN'`
- `apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php` ~line 123: add `'code' => 'MAIN'` (parity with `TenantProvisioningService::provisionForRegistration` and `AuthController` registration).
- Test first: a Feature test on `POST /api/v1/companies` asserting the created company's `is_default` location has `code === 'MAIN'` (put it next to existing Company feature tests; if it is a NEW test class, raise the manifest ceiling per `apps/api/tools/feature-lane-manifest-check.php` precedent notes).

## Task 2 — backend: tenant migration backfilling code-less default locations
- New migration in `apps/api/database/migrations/tenant/` named `2026_08_29_100000_backfill_default_location_code_f1.php`.
- For every location with `is_default = true` and (`code IS NULL` or `code = ''`): set `code = 'MAIN'` **unless** the same `company_id` already has a location whose code is `MAIN` (partial unique index `idx_locations_company_code (company_id, code) WHERE code IS NOT NULL`) — in that case leave it and print a census line `default-location-code-collision company_id=… location_id=…` to the migration output (Log + echo), never throw. Also normalise `code = ''` → keep the same rule (empty string is what `CompanyController` stored).
- Idempotent; `down()` is a no-op with a comment (data backfill).
- Test first: a migration test (follow the style of `2026_08_26_100000_backfill_payment_repository_location_n12` tests if present, else a Feature test that seeds a company + code-less default location, runs the migration class `up()`, asserts `MAIN`, and asserts the collision branch leaves the row untouched).

## Task 3 — backend: job resource exposes a warning summary
- `ImportController` job payload (the array that already carries `warning_rows`, ~line 671): add `warning_summary` = `{ <code>: <count> }` aggregated over `import_rows.warnings[*].code` for the job (one grouped query, Postgres `jsonb_array_elements`; keep it working on SQLite in tests if the suite runs there — if not portable, compute in PHP over the rows' `warnings` column). Add the field to the TS type via the DTO/`typescript:transform` path if a DTO exists, else extend `apps/web/src/features/import/types.ts` `ImportJob`.
- Test first: Feature test — execute a products import whose rows carry no `location_code` and a positive `quantity`; assert the job payload has `warning_rows = N` and `warning_summary.location_unresolved = N`.

## Task 4 — web wizard: choose the stock location; surface warnings on completion
File: `apps/web/src/features/import/pages/ImportWizardPage.tsx` (+ `types.ts`, `importApi.ts` only if needed), locales `apps/web/src/locales/{en,fr,ar}/import.json`.
1. **Options step** — show it for `products` whenever the column mapping includes `quantity` (extend `optionVisibility` with `stock: boolean`; `shouldShowOptionsStep` includes it). Add a "Stock location" section: a native `<select>` (match the existing placement-mode select styling/tokens) listing the company's locations from `useLocations()` (`@/features/locations/hooks/useLocations`), label `name (code)`; default value = the `isDefault` location (else first). Locations with no `code` are listed **disabled** with hint `t('options.stockLocation.noCode')`. Helper text explains: per-row `location_code` cells override this choice; this is where opening stock is posted.
2. On continuing from the options step, include `location_code: <selected code>` in the existing `updateOptions` PATCH payload (line ~496) whenever the stock section is visible. Never send an empty string.
3. **Complete step** — under the Imported/Failed tiles, when `jobData.warning_rows > 0` render a warning panel (`colorTokens.intent.warning.*`): headline `t('wizard.complete.warnings', { count })` and one line per `warning_summary` code using `t('warnings.<code>', { count })` with keys for `location_unresolved`, `qty_without_cost`, `price_conflict`, `category_created`, `expiry_conflict_existing_lot`, `quantity_ignored_service`, `opening_failed`, plus a generic fallback `t('warnings.other', { code, count })`. The `location_unresolved` line must say plainly that **no stock was created** for those rows and to fix `location_code` (or pick a stock location) and re-import quantities.
4. Column mapper label clarification (1c): in the three `import.json` locales, make the `location_code` column label/hint read "Location (site/branch) code — e.g. MAIN" and `placement_path` "Shelf position (zone/aisle/rack/bin)" — FR must not use bare « emplacement » for both. Find the existing label keys via `grep -n "location_code\|placement_path" apps/web/src/locales/*/import.json apps/web/src/locales/*/products.json`; if labels come from the backend column list only, add the hint text in the wizard's mapping UI next to those two targets.
5. Tests first (Vitest, `ImportWizardPage` has existing tests — extend them): (a) mapping with `quantity` → options step shows the stock location select defaulting to the default location and the PATCH carries its code; (b) completion renders the warning panel from `warning_rows`/`warning_summary`; (c) codeless location rendered disabled.

## Verification (must pass before you stop)
- Backend: `cd apps/api && ./vendor/bin/phpunit <each new/changed test by path>`; `./vendor/bin/phpstan analyse <touched files>`; `./vendor/bin/pint --test <touched files>`; `php tools/feature-lane-manifest-check.php`.
- Web: `cd apps/web && pnpm vitest run src/features/import` ; `pnpm typecheck` ; `pnpm lint` (kill stray vitest workers after: `pkill -f 'node (vitest'`).
- Leave the tree DIRTY (you cannot commit in this sandbox); write a 15-line summary of what changed + test commands/results to `/private/tmp/claude-501/-Users-houssamr-Projects-syneriva-apps-erp/5479251d-ea18-4852-93e8-a96337b138f5/scratchpad/lane-f1-summary.md`.
