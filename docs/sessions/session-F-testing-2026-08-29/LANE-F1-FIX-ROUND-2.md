# Lane F1 — fix round 2 (gate r2: imports CHANGES 1×P1+1×P2, tenancy CHANGES 1×P1+1×P2)

Worktree `.worktrees/f-bug-1`, HEAD `6f36f777f`. Gate records: `docs/superpowers/reviews/2026-08-29-f1-import-location-gate-r2-imports.md` and `…-gate-r2-tenancy.md` (untracked copies here). DESIGN PRE-APPROVED — do not ask. TDD, minimal diff, tests by path only, no full `pnpm lint`.

## Fix
1. **P1 (both gates) — PHPStan red from the loosened `ImportRow` docblock.** Restore the strict `@property list<array{code: string, detail: string}> $warnings` docblock in `apps/api/app/Modules/Import/Domain/ImportRow.php` (revert your change) and instead guard per element inside `ImportController::warningSummary()`: skip entries that are not arrays or whose `code` is not a non-empty string. Then run `./vendor/bin/phpstan analyse --no-progress app/Modules/Import` — must be **No errors** (this includes `ResultWorkbookService.php:119-120`). Test: a row whose `warnings` JSON carries a malformed entry (`{"detail":"x"}`) does not appear in `warning_summary` and does not throw.
2. **P2 (both gates) — `show` hydrates warning rows on every 2-second poll.** In `formatJob`, compute `warning_summary` only when `$withWarningSummary && $job->status->isTerminal()` (see `ImportStatus::isTerminal`, `ImportStatus.php:~22`); otherwise `null`. Test: a `processing` job's show payload has `warning_summary === null`; a `completed` job's has the map.
3. **P2 (imports) — wizard stalls when the completion refetch fails and `apiJobData.status` is already `completed`** (`ImportWizardPage.tsx:~483-497`). Make the failed-refetch path fall through: on refetch rejection, log once and transition to `complete` with the data already in hand (the panel simply shows whatever `warning_rows`/`warning_summary` it has, or nothing). Also refetch on the `failed` status branch the same way (imports P3). Tests: (a) refetch rejects → wizard still reaches `complete`; (b) `failed` status also triggers a refetch before transitioning.
4. **P3 (both) — the new migration test runs nowhere in CI.** Its lane in `apps/api/tests/feature-lane-manifest.json` is the PARKED `Company` group. Do exactly what its two sibling migration tests do (the manifest note in the file names them / see `.github/workflows/ci.yml` `backend-pgsql --filter` allowlist): add `BackfillDefaultLocationCodeF1MigrationTest` to that allowlist and keep `php tools/feature-lane-manifest-check.php` green.

## Do NOT do
- No per-row `location_code` validation (Session G). No change to `LocationService::findIdByCode`. No other wizard behaviour changes.

## Verification
`cd apps/api && ./vendor/bin/phpunit tests/Feature/Import/ProductsImportPipelineTest.php tests/Feature/Company/Migrations/BackfillDefaultLocationCodeF1MigrationTest.php` (+ any file you touched, by path); `./vendor/bin/phpstan analyse --no-progress app/Modules/Import app/Modules/Company/Presentation/Controllers`; `./vendor/bin/pint --test <touched>`; `php tools/feature-lane-manifest-check.php`. `cd apps/web && pnpm vitest run src/features/import`; `pnpm typecheck`; `npx eslint <touched web files>`; `pkill -f 'node (vitest'`. Leave the tree dirty; write an 8-line summary to `/private/tmp/claude-501/-Users-houssamr-Projects-syneriva-apps-erp/5479251d-ea18-4852-93e8-a96337b138f5/scratchpad/lane-f1-fix2-summary.md`.
