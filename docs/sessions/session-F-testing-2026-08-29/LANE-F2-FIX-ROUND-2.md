# Lane F2 — fix round 2 (gate r2: tenancy MERGEABLE; frontend CHANGES 1×P2)

Worktree `.worktrees/f-bug-2`, HEAD `7ab135511`. Gate records: `docs/superpowers/reviews/2026-08-29-f2-company-switch-gate-r2-frontend.md` (P2-A + P3 list) and `…-gate-r2-tenancy.md` (N-2/N-4). DESIGN PRE-APPROVED — do not ask. FE only, TDD, minimal diff. Do not touch the boot-reset guard, `CompanySelector`'s effect mechanism, or `AdminSupportAccessPage`.

## Fix
1. **P2-A — `invalidateCompanies()` after `adoptCreatedCompany` is a guaranteed production no-op** (the provider is still keyed to the OLD company at call time; the list refresh actually happens because the provider's query re-keys to the new id). Make the truth explicit: delete the `invalidateCompanies()` calls in `CompanyOnboardingPage.tsx` and `AddCompanyModal.tsx` and the now-unused `useInvalidateCompanies` hook (grep for other callers first — if any remain, keep the hook and only remove the two calls). Replace the synthetic test that pre-seeds a cache entry (`CompanyOnboardingPage.test.tsx:~163-164`) with an honest one: render the create flow WITH `CompanyProvider` in the tree and assert that after a successful create the provider issues a real `/user/companies` fetch under the NEW company key (spy on the query function / mocked `api.get`) and the store ends on the created id. Remove the corresponding synthetic assertion in `CompanyProvider.tenantScope.test.tsx` (~264-283) if it only existed to justify the no-op call; keep `.107`.
2. **N-2 (tenancy) — cover the mounted switch path**: add a test rendering `CompanySelector` under `CompanyProvider` (populated store) asserting that switching company re-keys and refetches the list under the new key, so reverting `CompanyProvider`'s `companies.length === 0` loader clause goes red.
3. **N-4 — remove the unreachable `invalidationScheduledRef`** in `CompanySelector.tsx` (keep `previousCompanyIdRef` + `queueMicrotask`).

## Verification
`cd apps/web && pnpm vitest run src/features/company src/stores src/components/organisms/AddCompanyModal src/components/organisms/CompanySelector src/lib` ; `pnpm typecheck` ; `pnpm eslint` on the touched files only (do NOT run full `pnpm lint` — the machine is saturated); `pkill -f 'node (vitest'`. Leave the tree dirty; write an 8-line summary to `/private/tmp/claude-501/-Users-houssamr-Projects-syneriva-apps-erp/5479251d-ea18-4852-93e8-a96337b138f5/scratchpad/lane-f2-fix2-summary.md`.
