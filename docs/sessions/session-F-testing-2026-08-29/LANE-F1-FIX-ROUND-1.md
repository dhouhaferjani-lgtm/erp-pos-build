# Lane F1 — fix round 1 (gate r1: imports CHANGES 3×P1, tenancy CHANGES 1×P1)

Worktree `.worktrees/f-bug-1`, branch `fix/f-bug-1-import-location`, HEAD `afd2aaf71`. Gate records (read both first, they cite file:line): `docs/superpowers/reviews/2026-08-29-f1-import-location-gate-r1-imports.md` and `…-gate-r1-tenancy.md` (untracked copies in this worktree). DESIGN PRE-APPROVED — do not ask. TDD: each item below gets its failing test FIRST. Run tests by path only.

## Must fix (blocking)
1. **Nullable `code` crashes the wizard (both gates P1).** `locations.code` is nullable; the FE type lies (`features/locations/types.ts:12` `code: string`). Fix at the boundary: in `features/locations/api/locations.ts` declare the raw shape `code: string | null` and map `code: raw.code ?? ''` (same for the scoped-locations mapper if it has one), so every consumer keeps `string`. Remove the `?? ''` the ESLint `no-unnecessary-condition` flagged at `ImportWizardPage.tsx:~288`. Tests: a wizard test feeding a location with `code: null` from the API mock must render the options step (option disabled, no throw); a mapper unit test for `null → ''`.
2. **Async imports never show warnings (imports P1-2).** On the WebSocket `completed` event (`ImportWizardPage.tsx:~449-456`), `await` a refetch/invalidate of the job query (`tenantScopedKey`-keyed job query in `features/import/api/queries.ts`) BEFORE switching to the `complete` step, so `warning_rows`/`warning_summary` come from the finalized job. Test: simulate WS completion with a stale job in cache → the panel shows the post-finalize `warning_summary`.
3. **`warningStats` on the list endpoint (imports P1-3, tenancy P2).** `formatJob` must not hydrate warning rows for the paginated list. Restore the previous COUNT for `warning_rows` on `index` (keep both drivers working) and compute `warning_summary` ONLY for single-job payloads (`show`, `execute`, progress) — e.g. `formatJob(ImportJob $job, bool $withWarningSummary)`; the list emits `warning_summary: null` (type `Record<string,number> | null` in `types.ts`). Count **rows per code** (a row with two `price_conflict` entries counts once) so the "rows" wording is true (imports P2-3). Guard `$warning['code'] ?? ''`. Test: list payload has no per-row hydration (assert the query count with `DB::enableQueryLog` or assert `warning_summary === null`), show payload has rows-per-code counts.
4. **Wizard location source is `module:Inventory`-gated (tenancy P2).** Replace `useLocations()` in `ImportWizardPage` with the ungated company endpoint (`GET company/locations` → `features/locations/hooks/useScopedLocations` / `api/scopedLocations.ts`); confirm the returned shape carries `code`, `isDefault`, `isActive` (extend the mapper if not). Exclude inactive locations from the select (tenancy P3). Test: wizard test asserts the request goes to the company-locations endpoint and inactive locations are not offered.
5. **Prove the green path (imports P2-1).** Feature test in `tests/Feature/Import/ProductsImportPipelineTest.php`: products import with `quantity` + `purchase_price`, NO per-row `location_code`, job `options.location_code = 'MAIN'` → an opening stock movement/level exists at the MAIN location; and a per-row `location_code` still overrides the option.
6. **Backfill sets `updated_at`** (tenancy P2): `->update(['code' => 'MAIN', 'updated_at' => now()])`; add a docblock to the migration explaining the census line. Extend the migration test.

## Do NOT do
- No validation-time resolvability check for per-row `location_code` (imports P2-2) — noted for Session G. No changes outside the files the gates cite.

## Verification (must pass before you stop)
`cd apps/api && ./vendor/bin/phpunit tests/Feature/Company/CreateCompanyTest.php tests/Feature/Company/Migrations/BackfillDefaultLocationCodeF1MigrationTest.php tests/Feature/Import/ProductsImportPipelineTest.php` (and any test file you touched, by path); `./vendor/bin/phpstan analyse <touched php files>`; `./vendor/bin/pint --test <touched php files>`; `php tools/feature-lane-manifest-check.php`. `cd apps/web && pnpm vitest run src/features/import src/features/locations`; `pnpm typecheck`; `pnpm lint` (0 errors AND no new warnings in touched files); `pkill -f 'node (vitest'`. Leave the tree dirty; write a 12-line summary to `/private/tmp/claude-501/-Users-houssamr-Projects-syneriva-apps-erp/5479251d-ea18-4852-93e8-a96337b138f5/scratchpad/lane-f1-fix1-summary.md`.
