# Lane F2 — fix round 1 (gate r1: tenancy CHANGES 3×P2, frontend CHANGES 3×P2; no P1)

Worktree `.worktrees/f-bug-2`, branch `fix/f-bug-2-company-switch`, HEAD `4da24fddb` (your previous round). Gate records (read first, file:line cited): `docs/superpowers/reviews/2026-08-29-f2-company-switch-gate-r1-tenancy.md` and `…-gate-r1-frontend.md` (untracked copies in this worktree). DESIGN PRE-APPROVED — do not ask. TDD: failing test first for every item. FE only. Task 1 of the original brief (boot reset guard) is confirmed correct — do not touch it.

## Fix list
1. **Adopting a created company must not blank the app / remount `LocationProvider`** (frontend P2-3, tenancy P3-3). Today `adoptCreatedCompany` re-keys the companies query to the new id → cache-less query → `CompanyProvider` full-screen loader unmounts the subtree, resetting `LocationProvider`'s `previousCompanyIdRef` guard. Fix both halves: (a) `CompanyProvider.tsx` shows the full-screen loader only while it has NO companies yet (`isLoading && companies.length === 0`) — a re-key with a populated store keeps children mounted; (b) in `CompanyOnboardingPage` and `AddCompanyModal` call `adoptCreatedCompany(company)` FIRST, then invalidate the companies list for the NEW selection (make `useInvalidateCompanies` return a function that reads the current company id at call time, or accept the id) so the invalidation is not a no-op against the abandoned key. Tests: CompanyProvider test — with companies already in the store, a re-keyed pending query does not render the loader and children stay mounted; onboarding test asserts the invalidation targets the new key.
2. **Pin the LIVE post-create path** (tenancy P2-2, frontend P2-1). `AddCompanyModal` is mounted nowhere; the reachable flow is `CompanySelector → /company-onboarding → CompanyOnboardingPage`. Add to `CompanyOnboardingPage`'s test: after a successful create, `useCompanyStore.getState().currentCompanyId` and `localStorage['autoerp-company-selection']` equal the created id (keep the modal test too).
3. **Cover the `createCompany` mapping** (tenancy P2-3, frontend P2-2): a direct test of `features/company/api.ts` `createCompany` where `apiPost` is mocked to return the exact backend shape from `CompanyController::formatCompany` (snake_case, nullable `legal_name`) and the result is asserted field-by-field. Fix P3-4: `default_target_margin`/`default_minimum_margin` are `string | null` (backend `bcformatOrNull`). Set `isPrimary: false` explicitly on the adopted company (P3-5) — the server list refetch corrects it.
4. **Task 3 actually covers the observed 403** (tenancy P2-1, frontend P3-1). The stale requests are refetches fired by `CompanySelector.handleCompanyChange`'s synchronous blanket `queryClient.invalidateQueries()` — observers still hold the OLD location-scoped keys at that instant, so the refetch carries the NEW `X-Company-Id` with the OLD `location_ids[]` and is not suppressed. Fix: move the invalidation out of the click handler into an effect keyed on `currentCompanyId` (previous→current change only) in `CompanySelector`, so it runs after React has re-keyed every observer; queries under old keys are then inactive and `invalidateQueries()` (default `refetchType: 'active'`) only marks them stale. Keep the `isStaleCompanyResponse` suppression in `api.ts` for genuinely in-flight requests, and add the `currentCompanyId !== null` bail-out so a 403 during a scope re-bootstrap is not swallowed. Tests: CompanySelector test — switching company triggers no refetch of a query keyed under the old company (spy on the queryFn), and `invalidateQueries` runs once after the re-render; api test — 403 with a stale header while `currentCompanyId === null` is NOT suppressed.
5. `api.ts:~319`: `error.config?.headers?.['X-Company-Id']` (frontend P3-2 — unguarded `headers` now on every errored status).

## Do NOT do
- No change to authStore persistence, no change to the Task-1 guard, no `AdminSupportAccessPage` work (P3-2 noted for a later lane), no cross-tab listener changes (P3-6 pre-existing).

## Verification (must pass before you stop)
`cd apps/web && pnpm vitest run src/features/company src/stores src/components/organisms/AddCompanyModal src/components/organisms/CompanySelector src/lib` ; `pnpm typecheck` ; `pnpm lint` (0 errors, no new warnings in touched files) ; `pkill -f 'node (vitest'`. Leave the tree dirty; write a 12-line summary to `/private/tmp/claude-501/-Users-houssamr-Projects-syneriva-apps-erp/5479251d-ea18-4852-93e8-a96337b138f5/scratchpad/lane-f2-fix1-summary.md`.
