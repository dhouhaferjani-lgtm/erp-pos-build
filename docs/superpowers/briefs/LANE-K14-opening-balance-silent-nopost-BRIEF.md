# Lane K-14 — opening-balance import reports success while posting nothing (pre-existing dev defect, P1) — adjudicated by the k89 code gate
Evidence: `docs/superpowers/reviews/2026-09-02-k89-branch-code-gate-r1.md` (adjudication of `tests/Feature/Import/ImportTypesTest.php:438`): `stageOpeningBalance` never checks the referenced account exists; `finalizeImport:988-998` demotes rows only on `opening_locked`; `ImportCountersData:23` counts `outcome = imported` — so an opening-balance row referencing a missing account completes as **1 imported / job Completed** while no balance is posted anywhere. Pre-existing on `dev` (not introduced by wave 1). Scope: validation-time account existence check (coded error), execution demotion for any opening-balance row whose posting did not happen, counters honest, PG+SQLite tests incl. the currently-red `ImportTypesTest:438` turning green for the RIGHT reason. Queue: wave-1 backlog — dispatch after the k89 merge lands.
