# POS receipts/reporting spec adversarial gate — round 4

## 1. Gate verdict — FAIL

r4 genuinely resolves the original R3-1 contradiction and fully closes R3-2 and R3-3 against source. OI-17 is a conservative, technically honest owner-visible ruling. The gate still fails because the binding training table and BT-1 are described as exhaustive but omit three legal non-TRAINING code subsets, and §9.3 repeats that false completeness claim while also miscounting six changed test rows as five. No round-1 defect or round-3 PASS item was re-litigated.

## 2. Per-fix verification

| Fix | Verdict | Spec evidence | Source verification / conclusion |
|---|---|---|---|
| **R3-1 (N-1)** | **PARTIAL** | The contradiction is resolved: `include_training` is the only training switch; any explicit `TRAINING` without `include_training=true` is a 422; empty arrays are rejected by `min:1`; defaults and screen payloads are pinned (`SPEC:157-193,590-594,664-665,693,705`). | The premise is correct. The migration documents `training_flag` as denormalized from `invoice_type_code == 'TRAINING'` and gives legacy rows `SALE`/`false` defaults (`2026_05_20_120000_add_invoice_type_code_and_training_flag_to_pos_receipts.php:15-45`); the fiscal validator enforces the equivalence at `FiscalPayloadConstraintValidator.php:901-914`; the projector writes both signed values at `PosCoreReceiptProjection.php:395,405-406`. However, the claimed every-subset table omits `['SALE','REFUND']`, `['SALE','VOID']`, and `['SALE','REFUND','VOID']`, for both toggle states (`SPEC:174-191`). BT-1 likewise does not assert those six legal 200 cases (`SPEC:664`). The rules imply their outcome, but the promised exhaustive matrix is not present. |
| **R3-2 (N-2)** | **PASS** | The live mechanic now enumerates the full per-call construction surface, wraps the entire reader call in `catch (\InvalidArgumentException\|\TypeError)`, forbids `\Throwable`, and gives BT-17 one malformed fixture for each exception class (`SPEC:568-578,681`). | Source matches. The reader docblock includes sub-object construction failures (`CanonicalPayloadReader.php:55-62`); the early guards are at `:65-77`; payload and sub-DTO construction spans `:79-117`. `FiscalPayloadArrayGuards` throws `InvalidArgumentException` for key/type failures (`:38-51,58-69,78-90,96-110,118-130,141-168`), with additional same-class shape guards in `SellerDTO.php:39-42` and `BuyerDTO.php:40-48`. A scalar list element reaches an `array`-typed `fromArray()` and raises `TypeError`; the unchecked list-element assumption is explicitly admitted at `CanonicalPayloadReader.php:114`. The narrow two-class catch and both BT-17 fixtures are source-correct. |
| **R3-3 (N-5)** | **PASS** | The line allowlist now exposes `product_code`, drops `product_sku` and `unit_of_measure_code`, keeps current-product enrichment only for `quantity_decimals`, widens S-4 to `lines.product.unitOfMeasure`, and aligns BT-6/FT-6 (`SPEC:302-332,427,670,697`). | `product_code` and `product_name` are receipt-line snapshots (`create_pos_receipt_lines_table.php:38-45,87-89`; `ReceiptLine.php:17-40,69-86`), and the projector writes canonical `sku` to `product_code` (`PosCoreReceiptProjection.php:1181-1197`). `product_id` is intentionally nullable (`:1157-1176`). `LineItemDTO.php:31-48` has no unit field, while the projector writes literal `'pc'` to `unit` (`PosCoreReceiptProjection.php:1193`); the legacy writer alone snapshots the actual sellable unit (`ReceiptCreationService.php:212-223,331-340`). The current show action loads only `lines.product` (`ReceiptController.php:475-486`), while the cited enrichment precedent loads `lines.product.unitOfMeasure`, applies fallback 4, and removes the relation before serialization (`ShiftController.php:301-337`). The revised mapping is source-correct. **OI-17 holds:** omitting the symbol avoids falsifying fiscal-era history; a reliable future symbol requires a canonical payload field plus projector change, and already-sealed receipts need an explicit no-rewrite/backfill answer. |
| **Revision-history honesty** | **PARTIAL** | §9.2 now records the exact gate-r3 split—N-3/N-4/N-6/N-7 PASS and N-1/N-2/N-5 PARTIAL—and withdraws the second extra N-5 correction as wrong (`SPEC:833-858`). §9.3 accurately records the r3 verdict, the three responses, OI-17, and the nothing-re-gated disclaimer (`SPEC:860-871`). | The requested §9.2 correction is honest. Two r4 statements are not: §9.3 calls the incomplete table “complete” (`SPEC:866`), and its scope paragraph says “five affected test rows” while enumerating six—BT-1, BT-2, BT-6, FT-2, FT-6, FT-16 (`SPEC:871`). |

## 3. New defects

| ID | Severity | Defect | Required correction |
|---|---|---|---|
| **R4-1** | **MAJOR** | The binding “Every combination, exhaustively” table and the §4.6/§9.3 completeness claims omit the valid non-TRAINING mixed subsets `SALE+REFUND`, `SALE+VOID`, and `SALE+REFUND+VOID`, under both absent/false and true `include_training`. BT-1's purported full matrix omits them too (`SPEC:174-191,594,664,866`). | Add the three subsets for absent/false (**200, exact union with the vacuous training predicate**) and true (**200, byte-identical no-op on this axis**), and add all six cases to BT-1's data provider. Only then retain the “every subset/full matrix/complete” language. |
| **R4-2** | **MINOR** | The r4 scope statement says “five affected test rows” but lists six (`SPEC:871`). | Change “five” to “six”. |
