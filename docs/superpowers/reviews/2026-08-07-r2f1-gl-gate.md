# GATE RECORD — R2-F1 `fix/r2f1-cancel-period-refusal` — GL/accounting axis

**Round 1. Verdict: spec ✅ + quality APPROVE-WITH-FIXES** (0 Critical, 4 Important, 4 minor).
**Branch:** @ `d9d4f0f62`, 6 commits on `264e6c483`, 13 files +917/−0.

## Axis verification (all probed/read in-worktree)
1. **Guard placement VERIFIED; bypass enumeration CLEAN.** `DocumentPostingService.php:193` inside the tx (`:151`), after `lockForUpdate()` (`:162`) + in-tx `hasBlockingAllocations` (`:167-169`), before `reverseDocumentGl()` (`:216`) and the fiscal split (`:205`) — non-fiscal else (`:223`) covered. Bypass hunt: `reverseDocumentGl` has exactly ONE call site; `RefundService.php:40/:66` direct Cancelled writes are on the non-Posted arms; `StandaloneReceiptService.php:412` compensation cancel has no GL; `cancelSalesOrder()` (`:259`) reached before the guard but `:234-236` throws on posted SOs — safe-by-construction, name it in the ticket; `CloseInvoiceWithTolerance` writes Paid; `CreditNoteService` writes Draft only; the only HTTP cancel endpoints are invoices/credit-notes (`routes.php:166/:211`). **No unguarded GL-reversing cancel path.**
2. **Fiscal-period-guard interplay claim FALSE — see I-1** (probed).
3. **Typed 422 renderer VERIFIED.** `renderViaCallbacks` first-non-null in registration order; `DocumentPeriodLockedException` #18 (`bootstrap/app.php:433`), generic `DomainException` #19 (`:448`); no other subtype changes envelope. `DocumentCancelConsolidationTest` still passes on the old flat envelope; `RefundController.php:64/:103` re-throw carve-out preserves it.
4. **Reversible seam mechanics accurate; instruction unsafe — see I-4.** `PURCHASE_DOCUMENT_TYPES` complete vs the 12-case enum (ReturnNote = customer return; Income = sales-side).
5. **Semantics:** absent-period precedent verbatim (`FiscalPeriodResolverService.php:278-292` + consumer `GeneralLedgerService.php:3265`); `AccountingService.php:441` stamps document_date ✅; `EloquentVatDataRepository:41` filters document_date ✅; `createSupplierInvoiceEntry()` does not exist → m-1. FILED-first + company scoping pinned; identical spans blocked by the unique index (partial overlaps only).
6. **Non-vacuity VERIFIED EXACTLY:** guard call commented → 6/11 fail, the 5 green are precisely the pinned happy paths; restored byte-for-byte.
7. **Deptrac:** exactly 2 branch-attributable violations at `DocumentPeriodLockInterface.php:40`, byte-identical in shape to the gate-approved `DocumentGlPreflightInterface`/`DocumentGlReversalInterface` precedents. **Disposition: named-edge acceptable**; feeds the open dev-baseline item.
8. **Scope discipline CLEAN** — no F2/F3/F4 leakage. Precision rule 19 clean.

Runs: CancelRefusedOnNonOpenVatPeriodTest OK 11/32 · GlReversal+Consolidation OK 16/54 (1 skip) · pint pass · phpstan 7 files clean. By path only.

## Findings
**I-1** — `VatPeriodCancellationGuard.php:39-42` orthogonality claim FALSE: `reverseDocumentGl()` writes the reversal via direct `JournalEntry::create` (`AccountingService.php:933-947`), never `postEntryNow()` where `ClosedFiscalPeriodException` lives (`GeneralLedgerService.php:3265`). Probe: OPEN vat period + CLOSED fiscal period covering today → cancel SUCCEEDS, reversal sealed into the closed fiscal period. Pre-existing hole (on dev, untouched by this diff) — but this branch writes the false safety claim into the record F2 inherits. Fix the claim + F2 ticket "cancellation reversal bypasses the closed-fiscal-period refusal".
**I-2** — `EloquentVatPeriodRepository.php:22-23` raw string compare: correct on PG, WRONG on SQLite (`'2026-01-01 00:00:00' <= '2026-01-01'` false — period_start day escapes the lock in the environment where 100% of tests run; end edge passes by lexicographic accident). Rule-20 class. Fix + two boundary tests.
**I-3** — `RefundService::canCancelInvoice()` (`:279-293`, surfaced via `RefundController.php:190`) not period-aware: `GET /invoices/{id}/can-cancel` returns true for a FILED-period invoice whose cancel permanently 422s. Wire the same `DocumentPeriodLockInterface` or explicitly ticket the split.
**I-4** — c2 seam instruction (`VatPeriodCancellationGuard.php:88-105` + `:113`), followed literally TODAY, produces "no reversal at all" (not reverse-in-current-period): `reverseDocumentGl()` returns null for non-Invoice/CreditNote (`:833`); SupplierInvoice takes the non-fiscal else. GR-IR legs (`GeneralLedgerService.php:1996`) would stand forever. Make the instruction conditional on F2's AP mirror.
**m-1** — `:22` cites nonexistent `createSupplierInvoiceEntry()`; real site `createSupplierInvoiceGrIrClearingEntry` (`:1921`, stamps at `:1996`); decoy `createSupplierInvoiceJournalEntry` (`:684`) is dead code with a caller-supplied date.
**m-2** — blanket `true` covers VAT-less types; latent (no cancel endpoint for them) but a future PO-cancel endpoint gets refused by a VAT lock on a VAT-less document. `// latent:` note wanted.
**m-3** — `DocumentPeriodLockInterface.php:9` unused Shared→ModuleApplication import serving a `@see`. Drop; inline FQCN.
**m-4** — untracked `ZzGateProbeVatPeriodTest.php` appeared mid-review (parallel gate session; rule-21 hazard). Useful probe (surfaced I-2) but must not be committed; promote its boundary case only after the I-2 fix.

**Fix before merge:** I-1 claim + ticket · I-2 (+2 boundary tests) · I-3 wire-or-ticket · I-4 conditional instruction · m-1 citation; drop the probe file.
