# Treasury Phase ⑤ Plans — treasury-reviewer (Opus) — 2026-07-18

Verdicts: **⑤a APPROVE-WITH-FIXES (BLOCKER-1 merge-gating; Waves 1-2 may proceed; NOT Wave 3 T6 / Wave 4 T9 until amended)** · **⑤b APPROVE-WITH-FIXES (Waves 0-2 clean; HIGH-5 before Wave 3; MED-7 before Wave 4)**.

Load-bearing question resolved: `InstrumentLifecycleService::receive()` posts NO GL (`:67-131`; only instrument row + audit event) — reuse for outbound issue is safe; the hazard is the surrounding controller path (BLOCKER-1).

## Findings
1. **[BLOCKER, ⑤a T6]** Deferred supplier TODAY posts `Dr 401 / Cr bank` via `createSupplierPaymentJournalEntry` (`PaymentController:975-996` → `GeneralLedgerService:707-775`) + an Out movement (`:1096`). Plan adds the issue JE and suppresses the movement but never gates off the `:979` supplier-JE branch → DOUBLE 401 debit + wrong bank credit. Fix: for `$isDeferredSupplier`, `:979` must NOT fire; issue JE replaces it (same partner-tagged 401 resolution, credit → payable-purpose). Test: exactly ONE JE, zero bank-account lines, repository balance unchanged.
2. **[HIGH, ⑤a T3-6/T9]** No outbound GL builders exist (`GeneralLedgerService` has only inbound instrument builders `:2481/:2587/:2811`; `postEntryNow:2889` only posts pre-built entries) and the file isn't in the ⑤a plan. Fix: add `createOutboundInstrumentIssue/Clearing/Dishonor/CancellationEntry` builders as owned tasks/files, each with **partner-tagged 401 legs** (partner-balance listener recomputes payable_balance from partner-tagged 401 lines — untagged = silent divergence).
3. **[HIGH, ⑤a T9]** Instrument-mode settle: `$alreadySettled` guard keys on a settlement MOVEMENT (`ExpenseService:480-497`) which instrument mode never writes, and `is_paid` stays false → retry re-posts AP JE + second instrument. Fix: durable anchor (reject when metadata already links an outbound instrument in Received/Cleared, or persist settlement idempotency row) + retry test.
4. **[HIGH, ⑤a T5/T9]** Treasury directly writing `ExpenseMetadata` (is_paid flip in clear(), reset in cancel()) violates rule 6 (Treasury imports zero Expense symbols today). Fix: `InstrumentCleared`/`InstrumentCancelled` domain events consumed by an Expense listener, or Shared/Contracts service.
5. **[HIGH, ⑤b T5/T7]** Signed-allocation convention unspecified — tier-4 net line (in) allocates gross card movements (in) + fee movement (out); with only positive `matched_amount` the completion identity is undefined. Fix: `contribution = movement.direction == line.direction ? +matched_amount : −matched_amount` in T5's invariant + T7 test asserting gross − fee == net closes the line.
6. **[MED, ⑤a T3-5]** Cancel writes no movement and JE source index is non-unique ⇒ "replay returns original" unmeetable for cancel (sequential retry hits transition guard and throws; concurrency-safe though). Fix: persist a cancel key or restate contract as throw-safe.
7. **[MED, ⑤b T8]** Checkpoint guard mutates the shared port: (a) regression tests needed for existing value-dated callers (inbound clear/bounce pass occurredAt = value date `:279/:497`) against reconciled periods; (b) decide checkpoint-bypass flag vs conflating with `allowWhileFrozen` (`:79-85`).
8. **[LOW]** Resolver match has no default arm (exhaustiveness genuinely enforced — good); 403/4035 plausible, expert-comptable gate correct; Dr/Cr net-consistent across all transitions (verified both cancel variants); `receive()` gets no instrument-level idempotency key today — T6 retry test must state it relies on the client Idempotency-Key header.

## Spec-to-plan coverage
Complete at task level (⑤a §4.1→T1 … ⑤b §6.7→T9); gaps are INSIDE tasks (findings 1-5), not missing tasks. Port contract (idempotency-before-GL, postEntryNow-only, no out-of-port movements) otherwise honored.
