# Gate r2 — lane T-1 (inventory-costing-reviewer, 2026-09-09)

**Audited HEAD:** `a2fd11174` (`lane/t1-transfers-edge`, worktree `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t1-transfers`).
**Delta reviewed:** `691eacba2..a2fd11174` (5 commits), re-verified against the whole-lane diff from `git merge-base dev HEAD` = `63e0e5e1697584475ddc15c8720549dfccb72f03` (14 files, +1247/−9; `apps/api/app` = **one** file, +26/−2).
Every line number below was re-derived at HEAD; the handback's numbers were not trusted. Commands I ran are quoted verbatim.

---

## Round-1 closure table

| # | r1 finding | Verdict | Evidence at HEAD |
|---|---|---|---|
| **M1** | no green pin of CURRENT behaviour for cases 4 / 14 | **CLOSED** | `apps/api/tests/Feature/Inventory/StockTransferEdgeCasesTest.php:113-124` `test_recalled_lot_receipt_pins_current_behaviour_until_ticket_t1_4` (200 + `TransferStatus::Completed` + `is_recalled` true + destination `BatchStock` `'4.0000'`), `:126-138` expiry twin, `apps/api/tests/Feature/Replenishment/ReplenishmentEdgeCasesTest.php:193-205` `test_requester_cancel_in_progress_pins_current_behaviour_until_ticket_t1_14` (422 + status still `InProgress` + `sourcing_document_id` not null). Each carries the ticket path in the docblock (`:113`, `:126`, `:193`) and points at the **same** ticket as its skipped desired twin (`StockTransferEdgeCasesTest.php:99-111` → `2026-09-09-t1-recalled-in-transit.md`; `ReplenishmentEdgeCasesTest.php:178-191` → `2026-09-09-t1-cancel-in-progress.md`), so they flip together. Both classes green on SQLite: 17/79/2 skips and 9/57/3 skips (I ran them). |
| **M2** | evidence claimed reader agreement its own transcript contradicts | **CLOSED** | `docs/superpowers/reviews/2026-09-09-t1-transfers-requests-edge-evidence.md:54-66` names the three readers (`LocationStockQueryService::read`, `StockMatrixQueryService::matrix`, `WeightedAverageCostService` via `recordCostAdjustment`) with a 4-row side-by-side transcript table; `:67` states the product-detail endpoint is PO remainder and an *explicitly different concept*; the three curl sections are relabelled `Case 5 DIFFERENT CONCEPT…` (`:97`, `:147`, `:207`). New ticket `docs/superpowers/tickets/2026-09-09-t1-product-detail-transit.md` is **accurate**: I verified `apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:1073-1087` sums confirmed-PO remainder only and `:1091` is `$incomingByLocation->get(...)->incoming ?? '0.00'` (a scale-2 literal on a quantity — real rule-19 debt, correctly ticketed, not touched). |
| **M3** | same denial, two contracts | **CLOSED** | `apps/api/app/Modules/Replenishment/Presentation/Requests/CreateTransferFromRequestsRequest.php:28-38` now returns the byte-identical envelope of `apps/api/app/Modules/Inventory/Presentation/Requests/StoreStockTransferRequest.php:38-60` — 403, `code=LOCATION_ACCESS_DENIED`, same `message`, same `details.location_id`/`details.user_id`. Test pins status **and** code **and** details: `ReplenishmentEdgeCasesTest.php:211-214`. (Correction to the gate prompt's premise: `App\Rules\ValidLocationAccess:86` has **no** typed exception — it calls `$fail('You do not have permission to access this location.')`, so message-identity matching is the only mechanism available, and it is the established sibling pattern.) Footprint caveat → MINOR-1. |
| **M4** | convention 09 gap on the guarded endpoint | **CLOSED** | `ReplenishmentEdgeCasesTest.php:220-251` `test_foreign_company_source_is_refused_then_own_source_can_fulfill_request`: real second-company fixtures (`Company::factory()->for($this->tenant)` `:223`, its own `Location` `:224`, its own `Product` `:225`, its own **stock** `:226`) — not company A relabelled. Refusal asserted as data meaning: 422 `VALIDATION_ERROR` + `error.errors.source_location_id.0 = 'Location does not belong to the active company.'` (`:231-233`), `StockTransfer::count()===0`, `StockMovement::count()` unchanged, request still Pending with null `fulfillment_id`, company-A source stock still `'10.0000'` (`:234-238`). Positive control on the same request/actor/endpoint changing only the source (`:240-250`) asserts the transfer's company/source/destination, the request now `Fulfilled` with `fulfillment_id`, source `'8.0000'` and company-B stock untouched `'10.0000'`. I confirmed the refusal really comes from the downstream ordering guard `apps/api/app/Modules/Replenishment/Presentation/Controllers/ReplenishmentActionController.php:142`, which is exactly the ordering M4 asked to pin. |
| **m1** | T1-11 severity / reachability | **CLOSED** | `docs/superpowers/tickets/2026-09-09-t1-settlement-replay.md:3` severity downgraded to medium; `:5` states the listener is synchronous — verified at `apps/api/app/Modules/Replenishment/Providers/ReplenishmentServiceProvider.php:19` (`Event::listen(StockTransferInitiated::class, SettleRequestsOnTransferInitiated::class)`, no `ShouldQueue`); `:7` states the reachable quantity-blind gap — verified at `apps/api/app/Modules/Replenishment/Application/Listeners/SettleRequestsOnTransferInitiated.php:44-71` (selects every open request at the grain, no quantity predicate) — and it is now **live-pinned** through the ordinary endpoint at `ReplenishmentEdgeCasesTest.php:90-105` (5.0000 request fulfilled by a 2.0000 transfer). T1-10 also now states "no second stock decrement" (`2026-09-09-t1-idempotency-payload.md:5`). |
| **m2** | case-6 method name overstated | **CLOSED** | `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:143` `test_freight_capitalization_uses_ten_owned_units_not_fourteen_inside_transaction`. |
| **m3** | skip-rot | **CLOSED, with a placement caveat** | All six `markTestSkipped` at HEAD carry either an existing ticket path (`StockTransferEdgeCasesTest.php:102,255`; `ReplenishmentEdgeCasesTest.php:110,181`) or a driver reason (`ReplenishmentEdgeCasesTest.php:129`, `StockTransferCompleteConcurrencyPostgresTest.php:82`); all four ticket files exist. The retirement checklist landed in the **superseded** spec rev 1 → MINOR-3. |
| **m4** | demo-tenant residue | **CANNOT VERIFY** | `docs/handoff/HANDBACK-T1-2026-09-09.md:55` asserts the `T1-EDGE-092254` fixture and transfers `TR-2026-00005..7` were deleted with post-checks. This is a claim about a live demo database outside the repo; nothing in the diff can confirm or refute it. No committed fixture residue remains in code — the SKU/UUID survive only inside the evidence transcripts, which are now explicitly labelled historical (`evidence:88`). |
| **m5** | PG class without `RefreshDatabase` in the live allowlist | **NOT CLOSED — new risk introduced** | See **MAJOR-1**. |

---

## BLOCKER

None. Nothing in the lane changes WAC arithmetic, stock levels, reservations, batch/FEFO consumption, movement signs or GL. `git diff --numstat 63e0e5e16..a2fd11174 -- apps/api/app` returns exactly one file (`CreateTransferFromRequestsRequest.php`, +26/−2); `StockTransferService.php`, every counting file, every migration, every DTO and all frontend source are untouched.

## MAJOR

**[MAJOR-1] `apps/api/tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php:34,63-77` — the m5 harness rework makes the lane's only PostgreSQL proof non-deterministic, and its failure mode destroys the shared schema mid-process.**

The class now `use DatabaseMigrations` (`:34`) with an overridden `runDatabaseMigrations()` (`:63-77`) that registers a whole-database `Artisan::call('db:wipe', ['--drop-views' => true, '--force' => true])` in `beforeApplicationDestroyed` (`:71`) and resets `RefreshDatabaseState::$migrated = false` (`:73`), then calls `refreshTestDatabase()` — i.e. a full `migrate:fresh` **per test** plus a full schema wipe **per test** against the shared lane/CI database.

What I actually observed on the documented lane (`sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php`, `autoerp_test_t` @ 127.0.0.1:5433), 11 invocations at this HEAD:

- run 1 (first thing I did, pristine shell): `ERRORS! Tests: 2, Assertions: 0, Errors: 2` — 5 min 21 s.
- run 2: error on test 1.
- run 3 (after killing every stray process): `Tests: 2, Assertions: 8, Errors: 1`, exit 2 — `Illuminate\Database\QueryException: SQLSTATE[42P01] … relation "migrations" does not exist … (SQL: select max("batch") as aggregate from "migrations")`, raised from `DatabaseMigrations.php:37` ← `StockTransferCompleteConcurrencyPostgresTest.php:76`.
- runs 4–11 (each test alone; class alone ×4; the 3-class CI-shaped run `--filter '/(ReplenishmentEdgeCasesTest|StockTransferCompleteConcurrencyPostgresTest|StockTransferEdgeCasesTest)::/' tests/Feature` → `OK, 28 tests, 171 assertions, 4 skipped`; and the class immediately after a 277-table pre-migrated DB): **all green, 2 tests / 15 assertions**.

So the handback's "real completion concurrency/WAC class **2/15/no skips**" (`HANDBACK-T1-2026-09-09.md:83`) is true *sometimes*: 3 of my 11 invocations were red at the same commit. The reds correlate with any other client holding locks on the lane database (I observed an `idle in transaction` backend on `autoerp_test_t` during that window) — `db:wipe`/`migrate:fresh` then blocks for minutes, half-drops the schema, and the run dies. Why it matters: (a) the class is now wired into the **live, required** `backend-test-pgsql` merge gate (`.github/workflows/ci.yml:1129-1139` + the filter at `:1141`, and `ci.yml:2769` lists it in `needs`), where it runs in ONE phpunit process with ~200 other classes; a half-dropped schema plus `RefreshDatabaseState::$migrated=false` poisons every class scheduled after it, converting one flake into a whole-gate red; (b) the handback presents "0 public base tables" after the run (`:81`) as proof of cleanliness, but that is also the exact signature of the failure mode, so the evidence cannot distinguish success from partial destruction; (c) the r1 harness (a bounded, tenant-scoped nine-table delete) was deterministic and I verified it green at `691eacba2` in round 1 (2 tests / 13 assertions).
**Minimum correction (pick one, then re-verify):** revert `:34,63-97` to the r1 hand-clean and answer m5 by *scoping* rather than wiping; or keep `DatabaseMigrations` but (i) drop the per-test `db:wipe` (`migrate:fresh` at the next test already wipes) and (ii) set a short `lock_timeout`/`statement_timeout` on the wipe so a blocked drop fails fast instead of leaving a partial schema. Either way the orchestrator must see **3 consecutive green** runs of the class on the PG lane, plus one green multi-class run, before promotion.

## MINOR

**[MINOR-1] `apps/api/app/Modules/Replenishment/Presentation/Requests/CreateTransferFromRequestsRequest.php` — the lane's production footprint is now +26/−2, over the brief's ≤ 20-line seam budget.** `docs/handoff/BRIEF-lane-T1-transfers-requests-edge-campaign-2026-09-08.md:3`: "Any defect found is fixed only if the fix is **≤ 20 lines** and inside the pinned seam". `git diff --numstat 63e0e5e16..a2fd11174 -- apps/api/app` → `26 2`. The evidence re-frames this as a per-round budget ("fix round 1 adds 14 lines … within the round's ≤20-line allowance", `evidence:5`), which is not what the brief says. The excess was directly caused by my own r1 M3, and the added code is a verbatim mirror of the sibling contract, so this is a budget question, not a design defect. **Minimum correction:** the orchestrator ratifies the 26/2 total explicitly in the merge note (or the guard moves to T-2). No code change needed.

**[MINOR-2] `CreateTransferFromRequestsRequest.php:30` — the denial contract is now three copies of one string literal.** The message is authored at `apps/api/app/Rules/ValidLocationAccess.php:86` and compared by identity at `StoreStockTransferRequest.php:40` and now `CreateTransferFromRequestsRequest.php:30`. If the rule's message is ever reworded or translated, **both** endpoints silently degrade from 403 `LOCATION_ACCESS_DENIED` to 422 with no code change visible in review. Tripwires do exist (`ReplenishmentEdgeCasesTest.php:211`, `StockTransferLocationScopeTest.php:271,282,307,327`), so this is not a silent-forever hazard. Note also the guard fires only when the location error is the *sole* error (`:30` array-identity, the sibling's `count($errors) === 1` at `:43`), so a caller who sends a bad `lines` payload as well still gets 422 — pre-existing sibling behaviour, unchanged. **Minimum correction (T-2):** hoist the literal to a constant on `ValidLocationAccess` and reference it from both FormRequests.

**[MINOR-3] `docs/superpowers/specs/2026-09-08-transfer-destination-receipt-and-blind-receiving-design.md:285-294` — the skip-retirement checklist landed in a superseded spec revision.** The lane appended "T-1 regression retirement acceptance" (+12 lines) to the **rev 1** file. `git ls-tree -r --name-only dev -- docs/superpowers/specs` shows dev already carries `…-rev-2.md` through `…-rev-9.md` of the same design; T-2 will be implemented from rev 9 and will never see this table. The lane cannot be faulted for not knowing the rev chain (rev 2+ do not exist on this branch: `git ls-tree HEAD` lists only the rev-1 file), and the addendum merges without conflict — it is simply inert where it sits. Skip-rot protection survives anyway because each of the four tickets carries its own reproduction/acceptance line (e.g. `2026-09-09-t1-settlement-replay.md:19`). **Minimum correction:** the orchestrator copies the table into `…-rev-9.md` (or the four tickets) at merge and deletes it from rev 1.

**[MINOR-4] `apps/web/src/features/replenishment/components/CreateTransferDialog.tsx:99-100` — the new 403 reaches the operator as a raw English server string.** The dialog calls `getErrorMessage(error)` (`apps/web/src/lib/api.ts:90-105`), which returns `data.error.message`; it does not use the existing refusal→i18n mapping that the stock-adjustments feature already has for this exact code (`apps/web/src/features/stock-adjustments/api/refusals.ts:27,60`, keys in `apps/web/src/locales/{en,fr}/stock-adjustments.json:131`). This is **not a regression** (the pre-fix 422 also surfaced the server string) and the lane is correctly forbidden from touching frontend source — record it as a T-2/T-3 follow-up.

## Verified fine

- **Costing / rule 19 across the whole delta.** `grep -n '(float)\|floatval\|number_format\|parseFloat'` over the four touched PHP files → no matches. Every quantity is a scale-4 decimal string (`'4.0000'`, `'10.0000'`, `'8.0000'`, `'0.0000'`), every money/cost value a decimal string. The fix round **tightened** the WAC assertions from `bccomp('6.0000', …, 4)` to exact `assertSame('6.000000', …)` at `StockTransferEdgeCasesTest.php:196,199` and `StockTransferCompleteConcurrencyPostgresTest.php:151,155,157` — correct, because `Product::$casts['cost_price'] => 'decimal:6'` (`apps/api/app/Modules/Product/Domain/Product.php:164`, column `decimal(19,6)` per `:316`) and `StockMovement::$casts['avg_cost_after'] => 'decimal:6'` (`apps/api/app/Modules/Inventory/Domain/StockMovement.php:104`); the assertion now also pins the emitted scale and is driver-independent. No rounding to currency scale mid-stream, no float rebase, no new `bcmath` scale literal in `app/`.
- **The WAC invariant the lane exists to protect still holds and is exercised.** `StockTransferEdgeCasesTest.php:188-201` (both terminal actions): +10.000 over **10 owned** units moves the running average 5→6 while 4 units are in transit (14 units would give 5.7142, 6 units 6.6667), then 6→7 after the terminal action. The PG twin pins the same denominator inside one transaction (`StockTransferCompleteConcurrencyPostgresTest.php:143-158`, `quantity_before`/`quantity_after` = `'10.0000'`).
- **Case 8 regression (r1 said already fine) still holds.** `tests/Feature/Inventory/StockTransferLocationScopeTest.php` is absent from the whole-lane diff — unmodified. The M3 envelope change only affects `/api/v1/replenishment-requests/actions/create-transfer`; the location-scope class asserts the 403 `LOCATION_ACCESS_DENIED` contract on `/stock-transfers` (`:271,282,307,327`), which the lane did not touch.
- **Production-change regressions.** `ReplenishmentActionsTest` → `OK (16 tests, 67 assertions)`; `ReplenishmentCriticalPathTest` (the only other test hitting `actions/create-transfer`) → `OK (1 test, 10 assertions)`. Both on SQLite from the worktree.
- **Lane wiring.** `php tools/feature-lane-manifest-check.php` from `apps/api` → **exit 0**, "every `--filter` entry is anchored and uniquely matched against 1928 test classes". The fix round changed no class counts (the manifest is not in `691eacba2..a2fd11174`), so the r1-verified ceilings (Inventory 127→129, Replenishment 7→8, aggregate 1250→1253) remain consistent; the three class names are still present exactly once in the `backend-test-pgsql` filter (`.github/workflows/ci.yml:1141`).
- **Style/static analysis.** `./vendor/bin/pint --test` on all four touched PHP files → `{"result":"pass"}`. `./vendor/bin/phpstan analyse` (level 8, repo config) on the same four files → **[OK] No errors**.
- **Test quality.** No `assertTrue(true)`, no mocked subject, no fabricated API payloads; `RefreshDatabase` + `RolesAndPermissionsSeeder` + real models on both SQLite classes (`StockTransferEdgeCasesTest.php:38,59`, `ReplenishmentEdgeCasesTest.php:40,61`). Every new pin asserts a balance, a row count or a status — not a bare status code. `assertTerminalStock` (`StockTransferEdgeCasesTest.php:285-290`) changed `?->quantity` to `->quantity ?? '0.0000'`; this is safe (PHP evaluates the property fetch under `??` in BP_VAR_IS mode, no warning) and both data-provider legs pass.
- **Inventory invariants I specifically hunted for and did not find:** no second stock-movement write path, no device-side decrement, no hand-rolled movement sign, no opening-balance path, no `if (batchTracked) continue;`, no FEFO/expiry-ordering change, no new `stock_level` lock ordering, no new `unique(['tenant_id', …])`, no queue/console-reachable costing call resolving currency scale with a bare `getScale()`. The lane only *reads* WAC.

## Merge instructions

1. **Gate on MAJOR-1 before promotion, not before merge into local `dev`.** The lane is tests + docs + a 26-line FormRequest guard, so merging is safe; but do **not** let it ride into a PR/`main` run until the PG concurrency class is deterministic. Required evidence: three consecutive `sh docs/sessions/t1/pg-test.sh tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php` green (2 tests / 15 assertions) **plus** one green multi-class run, on a database no other client is holding open. If any red appears, revert `StockTransferCompleteConcurrencyPostgresTest.php:34,63-97` to the `691eacba2` hand-clean harness and re-run — m5 is worth less than a deterministic gate.
2. **Ratify the production budget in the merge note:** the lane's `apps/api/app` footprint is **+26/−2** in one FormRequest, over the brief's ≤ 20-line seam (the overrun was caused by gate r1 M3 asking for the sibling 403 envelope).
3. **Relocate the retirement table** from `docs/superpowers/specs/2026-09-08-transfer-destination-receipt-and-blind-receiving-design.md:285-294` (superseded rev 1) into `docs/superpowers/specs/2026-09-09-…-rev-9.md` on dev, or fold it into the four tickets. No merge conflict is expected either way — dev's copy of the rev-1 file is unchanged.
4. **Carry into T-2/T-3 as tickets, not as lane work:** T1-5 product-detail transit + `ProductController.php:1091` scale-2 quantity literal; the reachable quantity-blind settlement in `SettleRequestsOnTransferInitiated.php:44-71` (prioritise over the replay hardening, per the corrected T1-11); the shared denial-message constant (MINOR-2); the replenishment-dialog refusal i18n mapping (MINOR-4).
5. **No conflicts expected.** Only `.github/workflows/ci.yml` (append-only inside an existing filter and comment block) and `apps/api/tests/feature-lane-manifest.json` (ceilings) touch shared files; both were r1-verified and unchanged in this round. Re-run `php tools/feature-lane-manifest-check.php` after the merge if any other lane lands ceilings in the same batch.
6. **Unverifiable claim to accept on trust or re-check manually:** the demo-tenant fixture cleanup (`HANDBACK-T1-2026-09-09.md:55`) — it is a live-database assertion with no repo-side artefact.

**What to fix before merge:** make the PostgreSQL concurrency class deterministic again (MAJOR-1) and ratify the 26-line production footprint; everything else is a documented follow-up.

VERDICT: MERGE-WITH-FIXES
