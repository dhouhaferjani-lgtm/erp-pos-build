1. **P1 — M1 cannot obtain the ruling required to resume.** The brief prohibits the executor from choosing (`docs/handoff/DRAFT-CODEX-DISPATCH-z-sale-branch-decomposition-2026-08-18.md:129-131,256-262`) and says the fiscal-pos review must rule (`:337,352`). However, `scripts/adversarial-review.sh:62-64,86-91` tells the reviewer not to relitigate design and permits only findings plus ACCEPT/CHANGES-REQUIRED—there is no A/B decision field. An ACCEPT cannot identify the chosen option or satisfy the resume condition.

2. **P1 — The mandatory progress/resume control file is absent.** The executor must read `docs/handoff/progress/z-sale-branch-decomposition.progress.yaml` first and source milestone lenses/state from it (`DRAFT-CODEX-DISPATCH…md:15-16`; `docs/handoff/SELF-REVIEW-HARNESS.md:14-17,23-37`), but that file does not exist at `c6d6308ae`, nor does the brief embed a template. Dispatch therefore starts with undefined `owner_gates`, `review_lenses`, milestone records, and resume state.

3. **P1 — The milestone review bridge is fail-open against malformed verdict output.** `scripts/adversarial-review.sh:109-113` takes the last line beginning `VERDICT:` and matches wildcards such as `*ACCEPT*`; it neither requires one verdict nor requires it to be the final non-empty line. Thus `VERDICT: NOT-ACCEPT`, multiple verdicts, or trailing output can exit 0 despite the contract at `:88-91`. The repository’s hardened final bridge already demonstrates the required checks at `scripts/adversarial-review-final.sh:245-253`.

4. **P2 — The milestone split defeats the stated red-first acceptance proof.** M2 implements all three fixes using unspecified per-site red assertions (`DRAFT-CODEX-DISPATCH…md:264-275`), while the required real-writer acceptance tests are deferred until M3 (`:277-284,306-307`). Consequently those acceptance tests can be authored only after the fix and never shown red, contradicting the binding TDD rule at `:314-315`. Existing masking fixtures remain live, including `apps/pos/src/lib/offline/__tests__/zReportService.test.ts:104-124` and `endOfDayPreview.test.ts:24-51`; the brief also fails to enumerate which existing fixtures must be corrected.

5. **P2 — Dispatch metadata still contradicts the resolved repository state.** The brief says no LEDGER row exists and asks the executor to propose one (`DRAFT-CODEX-DISPATCH…md:369,377-380`), but row C-2 already records the defect at `docs/handoff/LEDGER.md:111`. It also leaves question 4 open (`DRAFT-CODEX-DISPATCH…md:381-384`) while its binding gate table already declares autonomous fiscal-pos authority (`:337,352`). As written, this invites duplicate ledger work and conflicting gate handling.

6. **P2 — The verification contract names the obsolete lint command instead of the live ratchet.** The brief requires only `pnpm lint` and typecheck (`DRAFT-CODEX-DISPATCH…md:327`), while CI explicitly says the warning ratchet supersedes raw lint and runs `pnpm lint:ratchet` (`.github/workflows/ci.yml:1189-1195`). The ratchet fails warning growth even when ESLint itself exits successfully (`scripts/lint-ratchet.mjs:108-118,136-141`), so an executor can produce brief-compliant “green” evidence that fails the current enforcement gate.

VERDICT: CHANGES-REQUIRED