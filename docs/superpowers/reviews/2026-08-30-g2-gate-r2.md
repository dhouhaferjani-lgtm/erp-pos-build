# Lane G-2 adversarial gate r2 — blank-SKU generation on import + product create

**Worktree:** `.worktrees/g2-sku` (`feat/g2-sku-generation`, changes UNCOMMITTED)
**Review base:** `70968fbf29fe9abaf554227f678abc8c762b71cf`
**Method:** static reading + `git diff` only. Per the orchestrator's memory constraint I did NOT re-run PHPUnit, Vitest, PHPStan, deptrac or ESLint. Browser evidence is the orchestrator's 2026-08-30 18:41 run (`apps/web/e2e-local/sku-generation.spec.ts`, 1/1 PASS).
**Verdict:** **CHANGES**

All six r1 findings are genuinely fixed in code, with real data-meaning assertions behind each one — no BLOCKER survives. Two MAJORs remain, both introduced or exposed by the fix round and both small/localised: the census widening made an operator-facing preview sentence factually wrong, and the half-normalisation of blank SKUs leaves a *padded* SKU on a non-idempotent path that now fails with a message that actively lies.

## Findings

| ID | severity | file:line | finding | change |
|---|---|---|---|---|
| G2-R2-01 | MAJOR | `apps/api/app/Modules/Import/Services/DuplicateCensusService.php:83-85`; `apps/api/app/Modules/Import/Services/DuplicateCensusService.php:350-360`; `apps/web/src/locales/en/import.json:256`; `apps/web/src/locales/fr/import.json:256`; `apps/web/src/locales/ar/import.json:77` | The G2-R1-03 fix widened the **census** `matched_by_name` row list to include `InFile` rows whose identity is the name (`:84`), and that list is persisted into `job.options.duplicate_census` (`:114`) and surfaced to the preview screen (`apps/web/src/features/import/types.ts:194`). The three locale strings still read *"{{count}} rows matched an **existing record** by normalized name."* For the exact case this lane exists to serve — two blank-SKU rows sharing a name in one file — the loser matched a **later row in the same file**, not an existing record. The preview now makes a false factual claim in the operator's pre-execution feedback. The row-level warning trail is correct (`ImportService.php:542-547` → `'Matched the in-file winner by normalized name.'`); only the aggregate copy drifted. | Either narrow the census list back to `ExistingName` and keep the in-file signal purely at row-warning level, or re-word `import.json` `matched_by_name` in en/fr/ar to cover both ("matched an existing record **or an earlier row in this file** by normalized name") and assert the rendered banner in `ImportWizardPage.duplicates.test.tsx`. |
| G2-R2-02 | MAJOR | `apps/api/app/Modules/Product/Application/Services/ProductService.php:486-493`; `apps/api/app/Modules/Product/Application/Services/ProductResolver.php:215-224`; `apps/api/app/Modules/Import/Services/SpreadsheetParserService.php:80-85`; `apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:462-464`; `apps/api/app/Modules/Product/Application/Services/ProductService.php:409-428` | Blank detection is now trim-aware, but **non-blank** supplied SKUs are deliberately left untrimmed (`emptyToNull()` returns `$value` verbatim at `:492`), while the resolver's `blankToNull()` **does** trim (`ProductResolver.php:221`). The CSV parser does not trim cell values (`SpreadsheetParserService.php:82-84` only `rtrim("\r")`), so a padded Excel cell `" AB "` reaches `upsertWithResult` intact. Run 1: the resolver looks up `'AB'`, finds nothing, no barcode, and the name ladder is skipped because `$sku !== null` (`ProductResolver.php:109-111`) → a product is created with `sku = ' AB '`. Run 2 of the same file repeats the miss and hits the lifetime `unique(company_id, sku)`, which `createWithSuppliedSku()` translates into `SkuTakenConcurrentlyException` (`:426`) — the operator is told *"was taken concurrently; choose a different SKU and retry"* for a row that was never concurrent and whose SKU they already own. The API create path disagrees with the import path here: `ProductController::store()` **does** `trim()` the supplied SKU (`:462-464`). Two write paths, two normalisations, one non-idempotent import. | Trim the supplied SKU once at the `ProductService` boundary so import and API agree (`$fileSku = is_string($raw) ? trim($raw) : $raw` before the resolver call), and add a `BlankSkuGenerationImportTest` case: import `" AB "` twice, assert the product's SKU is `AB`, exactly one product, and zero `sku_taken_concurrently` rows on the second run. |
| G2-R2-03 | MINOR | `apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:484-488` | The API create path hardens only the **generated** SKU: `if (! $skuWasGenerated || ! DuplicateBarcodeException::isViolationOf(...)) { throw $exception; }` rethrows the raw `QueryException` for a supplied SKU that loses the race between `CreateProductRequest`'s `Rule::unique` (`CreateProductRequest.php:178-186`) and the insert. `bootstrap/app.php:211-340` has no catch-all renderer, so with `APP_DEBUG=false` this is a bare 500 "Server Error" — STOP-3's letter holds in production, but the operator gets an unexplained 500 instead of the coded refusal the import path just gained, and a debug-enabled environment would print `SQLSTATE[23505]` and the constraint name. | Catch `ProductIndexNames::COMPANY_SKU_UNIQUE` on the supplied-SKU branch too and render a 422 on `sku` (never re-allocate a supplied value). Assert the status and the absence of `23505`/`SQLSTATE` in `CreateProductTest`. |
| G2-R2-04 | MINOR | `apps/api/resources/private/tmp/g2-generated.d.ts` (3188 lines, untracked); `git check-ignore` returns non-zero for it | A `typescript:transform` scratch artifact was left in the tree under a **new, untracked, non-ignored** directory `apps/api/resources/private/tmp/`. `git status --short` lists `?? apps/api/resources/private/` and the path is not matched by any `.gitignore` rule, so a `git add -A` at merge time commits 3188 lines of generated declarations into `apps/api`. CLAUDE.md rule 10 ("check for build artifacts before committing"). r1 checked only for `apps/api/autoerp_test_*`. | Delete `apps/api/resources/private/` before the merge, or ignore it. Do not commit it. |
| G2-R2-05 | MINOR | `apps/api/app/Modules/Import/Services/ResultWorkbookService.php:129-134`; `apps/api/app/Modules/Product/Application/Services/ProductService.php:477-483` | The new `sprintf('import [%s]: %s', $code, $row->import_error)` prefix is applied to every coded row error, but two existing codes already embed their token in the stored message. A soft-deleted-holder row now reads `import [sku_held_by_deleted_product]: sku_held_by_deleted_product: SKU X is held by …` in the Rejected sheet — the token is printed twice. Same for `vat_held_by_deleted_partner` (`ImportService.php:677-679`). This also silently changes the reason format for **all** import types (parties, opening balances) beyond this lane's scope, and the change is not recorded in `docs/modules/imports.md`. | Strip a leading `"<code>: "` from `$row->import_error` before prefixing, or give the Rejected sheet a dedicated `code` column instead of encoding it in the reason string. |
| G2-R2-06 | MINOR | `apps/api/app/Modules/Product/Domain/Exceptions/SkuAllocationExhaustedException.php:11-20`; `apps/api/app/Modules/Import/Services/ImportService.php:667-681` | `SkuAllocationExhaustedException` carries `CODE = 'sku_allocation_exhausted'` but there is no matching `ImportErrorCode` case and `codedFailure()` has no branch for it, so an exhausted allocation lands in the catch-all `ImportErrorCode::InternalError` (`:681`). The row's workbook reason becomes `import [internal_error]: sku_allocation_exhausted: …` — the honest code is in the message but the machine-readable column says `internal_error`, which is what G-1's error-line export and any operator filter will key on. No driver text leaks, so STOP-3 is satisfied. | Add `ImportErrorCode::SkuAllocationExhausted` (row-level in `isJobLevel()`), branch on the exception in `codedFailure()`, and regenerate types. |
| G2-R2-07 | MINOR | `apps/api/app/Modules/Product/Application/Services/ProductService.php:430-438` | The `resolveIdentity()` docblock still says the trashed-holder check runs "against `$createSku` in `upsert()`". `$createSku` was deleted in this diff; the guard now keys on `$fileSku` and only for supplied SKUs (`:135-137`). The docblock is the only place the G3A-R2-1 rationale is recorded, so a stale variable name here is how the next lane re-introduces the removed fallback. | Update the docblock to name `$fileSku` and state that blank SKUs bypass the guard because the allocator's `withTrashed()` pre-check (`SkuAllocator.php:80-88`) covers them. |
| G2-R2-08 | MINOR | `apps/api/app/Shared/Contracts/Exceptions/SkuTakenConcurrentlyException.php:5`; `apps/api/app/Shared/Domain/Exceptions/DuplicateBarcodeException.php` (existing home, imported at `ProductService.php:18`) | The lane creates a **second** namespace for shared exceptions — `App\Shared\Contracts\Exceptions` — beside the established `App\Shared\Domain\Exceptions`, which is where the sibling cross-module SKU/barcode exception already lives and which this same file imports two lines above. One-surface-per-concept: two homes for the same kind of object, discovered rather than declared. | Move `SkuTakenConcurrentlyException` to `App\Shared\Domain\Exceptions` and delete the new directory, or add the split to `docs/glossary.md` with the rule that decides which namespace a shared exception belongs to. |
| G2-R2-09 | MINOR | `packages/shared/types/generated.d.ts:2613-2615` | Beyond the intended `ImportErrorCode` line (`:1056`), the regenerated declarations now also add `RepositoryCensusCode`, `RepositoryLocationAttributionVerdict` and `RepositoryNormalisationAction` — enums belonging to the already-merged SG-3c-FU lane, whose author evidently did not re-run `typescript:transform`. They are correct, but they are unrelated changes riding in this lane's diff (r1 recorded "no generated-type diff"). | Keep them (the base is stale and this is the honest regeneration) but call it out explicitly in the merge note so the diff reviewer is not surprised, or land them as a separate one-line commit. |

## Per-r1-finding disposition

| r1 ID | disposition | evidence |
|---|---|---|
| G2-R1-01 (whitespace-only SKU persisted verbatim) | **FIXED** | `ProductService.php:485-493` — `emptyToNull()` is now `$value === null \|\| (is_string($value) && trim($value) === '')`, so `'   '` and `"\t"` take the allocator branch at `:239-242`. Proven by data, not status: `tests/Feature/Import/BlankSkuGenerationImportTest.php:81-94` imports a space-only and a tab-only cell and asserts `['SKU-000001','SKU-000002']`, two `sku_generated` warnings, zero `import_error` rows, and `whereIn('sku', ['   ', "\t"])->exists() === false`; `tests/Feature/Product/CreateProductTest.php:105-127` covers the API leg. **Residual:** non-blank padded values are still untrimmed → G2-R2-02. |
| G2-R1-02 (STOP-6 derived-SKU refusal still executable) | **FIXED** | `ProductResolver.php` — `createSku()` is deleted (was `:217`), `resolveMany()` now reads `self::blankToNull($input->sku)` (`:32`), and the deleted-holder arm is gated `$sku === null ? null : array_find($skuRows[$sku] …)` (`:121-133`), so a blank SKU can never be refused for a barcode/name/`PRODUCT` twin. `ProductService.php:135-137` refuses only when `$fileSku !== null`. Both stale pinned tests were amended deliberately, not deleted: `ProductUpsertKeyPrecedenceTest.php:186-234` (`test_blank_sku_with_deleted_barcode_fallback_holder_gets_a_generated_sku`, `…deleted_name_fallback_holder…`) and `DuplicateCensusTest.php:279-283` flips ladder row 5 from `Refused/SkuHeldByDeletedProduct` to `New/Imported` with the Refused count 3 → 2. The upload→execute regression the r1 remediation asked for exists at `BlankSkuGenerationImportTest.php:151-180` (deleted `BARCODE-HOLDER`, `NAME-HOLDER` and `PRODUCT` holders; blank+barcode, blank+name and CJK rows all get `SKU-00000{1,2,3}`, zero row errors, the three trashed rows still trashed). |
| G2-R1-03 (`matched_by_name` missing on the in-file merge) | **FIXED** (with a new side-effect, G2-R2-01) | `DuplicateRowDecisionData.php:15` adds `matchedByName`; `DuplicateCensusService.php:181-186` sets it only when `$winnerRowNumber !== null`; `usesNameIdentity()` at `:350-360`; `ImportService.php:542-547` emits the warning ahead of `duplicate_in_file`. Exact-array assertions at `BlankSkuGenerationImportTest.php:108-126` (loser = `[matched_by_name, duplicate_in_file]`, winner = `[sku_generated => 'SKU-000001']`, one of each, one `in_file` row) and the orchestrator's Playwright trail `1\|matched_by_name\|…`, `1\|duplicate_in_file\|…`, `2\|sku_generated\|SKU-000003` (`sku-generation.spec.ts:135-144`). |
| G2-R1-04 (concurrent **supplied** SKU leaks raw PostgreSQL text) | **FIXED** on the import path | `ProductService.php:409-428` — `createWithSuppliedSku()` inserts inside a nested `DB::transaction` (SAVEPOINT) and catches **only** `DuplicateBarcodeException::isViolationOf($e, ProductIndexNames::COMPANY_SKU_UNIQUE)` (matched by the constant; `DuplicateBarcodeException.php:13-16` requires `errorInfo[0] === '23505'` **and** the constraint name), never re-allocating the supplied value, throwing `SkuTakenConcurrentlyException` whose message contains no driver text. `ImportService.php:661-666` maps it to `ImportErrorCode::SkuTakenConcurrently` (row-level per `ImportErrorCode.php:44`) with `detail => ['sku' => …]`. `BlankSkuGenerationImportTest.php:182-247` is a real PG two-writer test (a `Product::creating` hook inserts the winner) that asserts the row code, the exact safe message, and that neither `import_error` nor the **Rejected workbook sheet** contains `23505`, `SQLSTATE` or `products_company_id_sku_unique`. **Residual:** the API create path still rethrows raw → G2-R2-03. |
| G2-R1-05 (second-of-everything proof absent) | **FIXED** | `BlankSkuGenerationImportTest.php:249-312` creates company B through the **real** `POST /api/v1/companies` (`:254-267`), a second `pos_enabled: true` location through `POST /api/v1/locations` (`:269-282`), imports a blank-SKU opening row into company B twice, and asserts company-**filtered** reads: `SKU-000001` in both companies (`:299-300`), one product each (`:301-302`), `next_value = 2` in both sequences (`:303-304`), one `sku_generated` on the first run and zero on the re-run (`:305-306`), and exactly one `stock_levels` row at the **selected** branch with `bccomp('5.0000', …, 4) === 0` (`:308-311`). The Playwright leg mirrors it end-to-end through the UI company switcher (`sku-generation.spec.ts:164-233`), including `pos_enabled` on the joined location row and an explicit assertion that company A's product count and `next_value` are unchanged (`:231-232`). |
| G2-R1-06 (hand-rolled `Product` type + `parseFloat` on money) | **FIXED** | `AddQuickProductModal.tsx:39-42` replaces the hand-rolled interface with `Pick<App.Modules.Product.Application.DTOs.ProductData, …>`; `generated.d.ts:2219-2223` confirms `sale_price`/`cost_price`/`tax_rate` are `string \| null`. The payload at `:184-195` carries `sale_price: data.sale_price` and `tax_rate: data.tax_rate` as strings with **no** `parseFloat`/`Number` anywhere in the file, and the numeric `cost_price: 0` is gone (harmless: `CreateProductRequest` never validated `cost_price`, so it was never persisted). The input is now `<MoneyInput>` under a `Controller` (`:255-281`), whose contract is a canonical decimal string (`MoneyInput.tsx:12-23`) and which forwards a ref (`:62,105,76`). `slugifySkuPrefix`/`generateQuickProductSku` are deleted; blank posts `null` (`:188`). The tests assert the **whole** payload object, not a subset: `AddQuickProductModal.test.tsx:207-214` (`sku: null`, `sale_price: '10.125'`, `tax_rate: '19.00'`) and `:234-242`. Downstream consumers still typecheck: `DocumentLineEditor.tsx:167-180` declares `sale_price?: string \| number \| null`. |

## Acceptance-criterion audit (deltas vs r1)

| contract area | evidence | r1 | r2 |
|---|---|---|---|
| Preconditions G-3a/G-4 | `ProductIndexNames::COMPANY_SKU_UNIQUE`; resolver seam `ProductResolver.php:18-28` | PASS | PASS |
| M5 exact schema, guard, forward-only | `database/migrations/tenant/2026_08_30_100500_create_sku_sequences_table.php:12-40` — `hasTable` guard `:14`, `unique(['company_id','prefix'])` `:27` (carries `company_id`, so rule 22's ratchet is satisfied), `down()` is a logged no-op `:35-39` | PASS | PASS |
| Per-company allocator, first-use insert, row lock, growing padding, deleted-holder skip, five-attempt bound | `SkuAllocator.php:37-46` `insertOrIgnore`, `:49-53` `lockForUpdate`, `:64-69` `max($padding, strlen($digits))`, `:80-88` `withTrashed()` skip, `:62/91` bound; lock-ordering docblock `:21-24`; only production caller is inside `processPendingRow`'s transaction (`ImportService.php:513`, `:931`) or `store()`'s (`ProductController.php:450`), so the row lock is always real | PASS for canonical input | **PASS** |
| Blank import rows, sequence values, warning details | `ImportService.php:934-936`; `BlankSkuGenerationImportTest.php:66-94` | FAIL for whitespace/deleted-fallback | **PASS** |
| Same-name one-product/one-allocation merge **with `matched_by_name`** | `BlankSkuGenerationImportTest.php:96-127` | PARTIAL | **PASS** at row level; preview copy now wrong (G2-R2-01) |
| Re-import allocates nothing | `BlankSkuGenerationImportTest.php:129-140`, `:288-306`; `ProductUpsertKeyPrecedenceTest.php:95-113`; `sku-generation.spec.ts:146-154`, `:220-232` | PASS | PASS |
| CJK / supplied-SKU preservation | `BlankSkuGenerationImportTest.php:142-149`; `sku-generation.spec.ts:156-162` | PASS | PASS for **unpadded** values; padded values are G2-R2-02 |
| Supplied-SKU race is coded, no driver text | `ProductService.php:409-428`; `BlankSkuGenerationImportTest.php:182-247` | FAIL (STOP-3) | **PASS** on the import path; API path is G2-R2-03 |
| API create nullable SKU, generated response, supplied lifetime uniqueness | `CreateProductRequest.php:178-186`; `ProductController.php:462-499`; `CreateProductTest.php:89-127` | PASS | PASS |
| FE optional SKU, server allocation, precision contract | `ProductGeneralSection.tsx:100-113` (no `*`, no `required` rule, placeholder + helper); `ProductForm.tsx:733` (`satisfied: true`); `AddQuickProductModal.tsx:39-42,184-195,255-281` | SKU PASS / precision FAIL | **PASS** |
| Amended pinned tests recorded deliberately | `ProductUpsertKeyPrecedenceTest.php:83-113`, `:186-234`; `ProductFormInvalidSubmit.test.tsx:179-190`; `ProductGeneralSection.test.tsx:63-130`; `DuplicateCensusTest.php:279-283` | n/a | PASS |
| Second-of-everything (2nd company, 2nd location, re-run) | `BlankSkuGenerationImportTest.php:249-312`; `sku-generation.spec.ts:164-233` | FAIL | **PASS** |
| i18n en/fr/ar | `locales/ar/inventory.json:418-420` adds `sku`/`skuPlaceholder`/`skuHelper` | PASS | PASS for the product form; **FAIL** for `import.json` `matched_by_name` (G2-R2-01) |
| Docs drift / warning code | `docs/modules/imports.md:167`, `:192-197`, `:270` — `sku` optional, `sku_generated` in the warning table, `sku_taken_concurrently` documented | PASS | PASS; the reason-format change (G2-R2-05) is undocumented |
| One surface per concept | one allocator behind `SkuAllocatorInterface`; modal hand-roll removed | PARTIAL | **PASS** for the lane's nouns; a second shared-exception namespace is G2-R2-08 |
| Rule 19 (no float on money/qty) | `emptyToNull()` returns money strings unchanged (`ProductService.php:492`); allocator touches no money; modal posts strings | PASS/FAIL split | **PASS** |
| Manifest / CI | `feature-lane-manifest.json` `gated_ceiling` 1236→1237, `Import` 38→39, `Product` 58→59 (one pre-existing headroom slot); `.github/workflows/ci.yml:1119` appends `\|SkuGenerationTest\|BlankSkuGenerationImportTest` to the single anchored filter, comment added at `:1116-1117` | PASS | PASS |
| Repo hygiene | `git status --short` | PASS | **FAIL** — untracked non-ignored artifact (G2-R2-04) |

## STOP adjudication

- **STOP-1 / STOP-2 / STOP-5:** unchanged from r1 (not hit / adjudicated as designed / no new deptrac edge inspected this round — not re-run).
- **STOP-3:** **closed on the import path** (`BlankSkuGenerationImportTest.php:224-246` proves no `23505`/`SQLSTATE`/constraint text in `import_rows.import_error` or the Rejected sheet). Open in a narrow form on the API create path (G2-R2-03); contained to a bare 500 under `APP_DEBUG=false`.
- **STOP-4:** not hit. No float touches money on either the import or the quick-create path.
- **STOP-6:** **closed.** `ProductResolver::createSku()` is gone and no blank-SKU row can be refused for a derived value; the two positively-pinning tests were amended, not deleted.

## What I could not verify this round

PHPUnit (SQLite + PG), Vitest, PHPStan, Pint, deptrac, ESLint and the manifest checker were **not** re-run (memory-constrained machine, per the dispatch). Every claim above is from reading the files; every green/red statement about a test is about what it *asserts*, not about it having passed under me. The Playwright evidence is the orchestrator's.

## VERDICT: spec ✅ + quality CHANGES-REQUESTED

All six r1 findings are really fixed; do not merge until G2-R2-01 (preview copy) and G2-R2-02 (padded supplied SKU) are addressed and G2-R2-04 (stray `apps/api/resources/private/`) is deleted from the tree.
