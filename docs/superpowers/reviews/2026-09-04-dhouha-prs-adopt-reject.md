# Dhouha's open PRs — ADOPT / DROP decision sheet
Date : 2026-09-04 · Local `dev` = `4cb609f22` (146 ahead of `origin/dev` `4d5b8812e`) · read-only, nothing modified, no worktree created.
Extends `docs/superpowers/reviews/2026-09-04-teammate-pr-audit-dhouha.md` (inventory, 7 P1s, CI attribution) — not repeated here.
Context : client #1 = **pharmacy / parapharmacy (IziPOS, TN)**. Nothing merges to `dev`/`main` now.

## 1. Decision table

| PR | Feature | Still a real gap on `dev` `4cb609f22`? | Pharmacy-launch relevance | Verdict | Re-port | Blockers |
|---|---|---|---|---|---|---|
| **#200** `fix/topbar-profile-menu` | Profile/language dropdown above page content + intermediate-width clipping | **Half fixed.** The z-index defect is **gone**: `TopBar.tsx:132,172` already carry `z-50` (commit `520ffb533`, ticket `docs/superpowers/tickets/2026-08-02-topbar-dropdown-unclickable-zindex.md`). **Still absent on dev**: `min-w-0`/`shrink-0` flex guards, `top-full`, `max-w-[calc(100vw-2rem)]`, and search `sm:flex`→`xl:flex` — the w-96 search box (`TopBar.tsx:87`) still squeezes the right cluster at 640–1280 px | Cosmetic, all verticals | **DROP the PR, ADOPT the 15-line delta later** | 0.25 d | 2 conflicts (`TopBar.tsx`, `TopBar.test.tsx`) vs a file rewritten twice since July → rebase pointless. Re-type by hand + one visual check at 768/1024/1280 |
| **#201** `feat/floor-plan-view` | Visual floor-plan **editor** (drag-drop canvas, table nodes, add/delete) | **Yes, net-new.** dev has floors+tables backend (`POS/Domain/Floor.php`, `tenant/2026_03_13_100000_create_pos_floors_table.php`) and a **list-based** CRUD page (`TableManagementPage.tsx:20` "Admin page for managing floors and tables (CRUD)") — no canvas. dev's POS cache already stores `position_x/y/width/height/shape` (`apps/pos/src/lib/db/repositories/tableRepository.ts:14-28`), so only the UI is missing | **None.** `Tables` is not in `pharmacy` nor `parapharmacy` `compatible_extras` (`apps/api/config/verticals.php`, pharmacy §42-46 / parapharmacy §339-343). F&B only, paid extra | **ADOPT-LATER** (first F&B tenant) | 3–4 d | 3 conflicts (`Sidebar.tsx`, `designTokens.ts`, `routes.test.tsx`). **Her regressions**: `useLiveFloor.test.ts:102` TS2322; `Button.tsx` → `cn()`/`twMerge` across **703** `<Button` callers with no visual proof; scope creep (`AGENTS.md`, `vite.config.ts`, 2 Playwright configs, 23 `.superpowers/sdd/*`) |
| **#202** `feat/decor-items` | Décor objects on the floor plan | Net-new | None | **DROP** | — | Comfort feature; revisit only after #201 ships |
| **#203** `feat/barista-workstation` | Barista station **on the web app** | Net-new | None — and the owner rules the surface belongs to Tauri POS / mobile, not the back-office | **DROP the web surface**; salvage 2 slices ↓ | — | — |
| ↳ #203-a | API: table status **derivation** (`Occupied` order-driven only) + `repair_occupied_tables_without_current_order` migration + 690 lines of tests | **Yes, real bug on dev**: `SetTableStatusRequest.php:19` still accepts `Occupied` manually → table occupied with `current_order_id = null` | F&B only | **ADOPT-LATER** | 0.5–1 d | Clean, backend-only, no conflict. Ship with #201 |
| ↳ #203-b | `getOrders` pagination envelope (`apiGet` drops `meta`) | **Yes**: `apps/web/src/features/pos/api/orderApi.ts:145` still `return apiGet<OrderListResponse>(url)` → `meta` lost; no `page` param | F&B path only, but a 2-line generic-convention bug | **ADOPT NOW** (fold into any pass) | 0.25 d | None. Her fix predates dev's `OffsetPaginationMeta` type — retype by hand, don't cherry-pick |
| **#204** `feat/pending-orders` | Pending order slips barista → register | Net-new | None | **DROP entirely** | — | **There is no reusable server-side slice**: the diff touches zero `apps/api` files — it is POS SQLite + web only. Also carries the v61/62/63 collision and the superseded `apps/pos/eslint.config.js` rework |
| **#205** `feat/register-floor-plan` | Floor plan **in the register** + occupancy bridge (`seated_at`, `tableSeating.ts`, `FloorPlanSheet`, `AssignedTableChip`) | **Yes.** `seated_at` exists **nowhere** on dev (0 grep hits across `apps/pos`, `apps/api`, `apps/web`); dev's register table picker is a flat list (`apps/pos/src/components/atoms/TableSelector.tsx`) | None | **ADOPT-LATER** (core only) | 3–4 d | **17 conflicts.** **Her regressions**: (a) IBAN/RIB exposed to any `pos.operate_terminal` holder — dev is clean, `Treasury/Presentation/routes.php:66-67` still has `can:repositories.view`, so **carve the whole Treasury diff out**; (b) POS migrations v61/62/63 vs **dev at v67** (`apps/pos/src/lib/db/migrations.ts:2163`) → renumber v68+; add/add on `migrations.v61.test.ts`; (c) `consumption_mode` forced to `SUR_PLACE` into a signed `SALE_RECEIPT`; (d) `acceptPendingDraft` not idempotent when `markAccepted()` fails after server accept |
| **#206** `feat/table-occupancy-timer` | Occupancy chrono + per-table note | Net-new (depends on #205) | None | **ADOPT-LATER** (right after #205) | 1–1.5 d | Inherits #205's 17 conflicts; smallest and cleanest of the chain (23 files / +853). Includes her own good P1-4 fix (gate on `Tables`, not `Menu`) |
| **#207** `feat/nacef-regime-seam` | Certification-regime seam (dark launch), TN-NACEF | Net-new, inert | **Zero, provably.** `TnNacefDefinition.php:19-21` → eligible only when `countryCode === 'TN'` **and** vertical ∈ {Restaurant, CoffeeShop}. A TN pharmacy/parapharmacy can never be eligible; the terminal payload stays `certifications: []` | **ADOPT-LATER** — best-quality work of the lot; the same seam is what FR/NF525 will need | 1.5–2 d | Only **3 conflicts** (`TerminalController.php`, `bootstrap/app.php`, `generated.d.ts`). Drop 2 dead `Data`-typed interfaces (`CertificationRegimeContract`, `SecurityElementClient`); decide the duplicated FE type (`apps/pos/tsconfig.json` doesn't expose `packages/shared`); populate `certifications` on **all** terminal endpoints, not just `show()` |

**Her regressions vs our pre-existing debt.** Hers: TS2322, `Button`/`twMerge` sweep, POS migration renumbering, IBAN exposure, `consumption_mode` forcing, `acceptPendingDraft` gap, scope creep. Ours: `apps/pos` not wired to `packages/shared/types`; Pint / §14.3 chokepoint / Deptrac / generated-types-drift reds on #207 are all `dev`-side files; most POS Vitest reds on #201 are dev specs broken by a 3369-commit-stale base.

## 2. Branch plan (10 lines)

1. A plain merge of the chain is **not viable**: chain tip `0aa7e42ec` → current `dev` = **17 conflicting files**, including `apps/pos/src/lib/db/migrations.ts` (silent schema corruption on any device already at v63) and `PaymentRepositoryController.php` (the IBAN leak). Resolving that by hand imports two defects.
2. `#207` **is** viable as a merge — only 3 conflicts, none dangerous.
3. So: **one holding branch, but as a parking lot for re-ported slices, not as a merge of her branches.**
4. `hold/dhouha-fb-2026-09` — cut from current `dev` **when re-porting actually starts** (after the pharmacy launch). One commit per slice: #201 editor, #203-a status derivation, #205 occupancy core (Treasury diff excluded), #206 chrono. Never merge her originals into it.
5. `hold/nacef-regime-2026-09` — separate, off current `dev`, for #207's rebase. Independent of F&B; can move first and alone.
6. Do **not** delete `feat/floor-plan-view`, `feat/decor-items`, `feat/barista-workstation`, `feat/pending-orders`, `feat/register-floor-plan`, `feat/table-occupancy-timer`, `feat/nacef-regime-seam` — they are the source material for the re-port. Freeze, don't prune.
7. Convert #201–#206 to **draft** and label them `parked/f&b` so nobody re-opens the rebase question.
8. Close **#200**; delete `fix/topbar-profile-menu` and `fix/topbar-dropdown-stacking` (the latter is a 4-commit orphan, **not** an ancestor of `fix/topbar-profile-menu` — two divergent attempts at the same bug, both superseded by `520ffb533`).
9. Safe to delete now — all three verified ancestors of `origin/dev`: `fix/import-cp1252-encoding` (`bca8dfbe6`), `fix/api-nginx-client-body-perms` (`8a60a863f`), `docs/client-bugs-2026-08-05` (`fe0df479e`).
10. Only work that touches the pharmacy path before launch: **#203-b** (2-line `getOrders` envelope) and optionally the **#200 delta** (15 lines). Everything else waits.

## 3. What to tell her (5 lines, owner relays)

1. Three of your branches are already in `dev` (CP1252 import, nginx client-body, client-bugs doc) — thanks, they shipped; we're deleting the remote branches.
2. The topbar z-index bug you found was real and is fixed on `dev` since `520ffb533` — your PR #200 is closed as superseded, but the *intermediate-width* squeeze you also caught is still there and we want that 15-line part.
3. The F&B chain (#201–#206) is good work landing at the wrong moment: our first client is a pharmacy, `Tables` isn't in its module set, and the chain is now 3369 commits behind with 17 conflicts. We're parking it, not dropping it — we'll re-port slice by slice when an F&B tenant signs.
4. Two things to fix before any of it can land, independent of timing: `payment-repositories` must keep `can:repositories.view` (your version exposes IBAN/BIC to any cashier), and the POS migrations must be renumbered — `dev` is at v67, so v61/62/63 would never replay on an already-migrated device.
5. #207 is the best work of the series (real TDD, exact inertia proofs) and needs only a rebase plus two dead files removed — one rule from now on: **a PR always targets `dev`**, never one of your own branches, otherwise CI never runs.
