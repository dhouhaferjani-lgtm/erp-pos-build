# Codex round-4 adversarial review — DISPATCH-PLAN-v4-first-tenant-2026-07-31

**Review target:** `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md` in the supplied main checkout, `dev` @ `65d5b8537e62157d9e24f0abbe1b09975a5452cb`.

V4 is materially safer than v3. The D2-a → E transfer is a real sequential fence for the files it names, Phase E can persist evidence for each of its current E-1…E-8 rows, D1 now reuses the real cash-rounding command, and the concurrent write sets do not collide. It is still not dispatchable. Two round-3 fixes are weakened rather than fully folded: the smoke repair omits a mandatory verifier argument and does not reconcile the complete P0 protocol, while the Lane-E criteria still diverge from the secret, migration, smoke, and legal sources. More importantly, the plan authors a staging runbook but assigns nobody to execute/close it, promotes only to `origin/dev` even though the handover says that deploys staging, and reduces the target-device cash-rounding rollout to command/API evidence while the repository checklist makes the build, SQLite, operator-briefing, real transaction, Z, projection, GL, and audit checks mandatory (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:119-144`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:160-169`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:198-203`, `docs/handoff/HANDOVER-first-tenant-orchestrator-2026-07-31.md:8-11`, `docs/handoff/cash-rounding-phase2-deploy-checklist.md:238-263`).

## 1. Fold-completeness audit for round 3 R1–R6

| Round-3 required fix | Verdict | Evidence |
|---|---|---|
| **R1 — make correction-chain implementation non-waivable** | **incorporated** | V4 separates the refund-rounding `D/2` acceptance from correction-chain integrity and makes E-7 non-waivable (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:20-23`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:155-168`). That is necessary against current code: the device force-syncs approval evidence and then submits the server return as separate operations (`apps/pos/src/stores/refundCheckoutStore.ts:331-390`); the server return advances the terminal chain through `ReceiptFinalizationService` (`apps/api/app/Modules/POS/Application/Services/ReceiptReturnService.php:469-472`); the principal return fixture still pins schema 2 (`apps/api/tests/Feature/POS/ReceiptReturnRefactorTest.php:490-496`); and VOID still mutates the original receipt rather than authoring a correction event (`apps/api/app/Modules/POS/Application/Services/ReceiptVoidService.php:71-88`). E-7 requires the reviewed code phase, schema-3 sale → return → next sale → Z evidence, and the selected VOID implementation/compensating control, so the rounding exception cannot close it (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:168`). |
| **R2 — repair Lane E owners, locations, and exact pass/no-go criteria** | **weakened** | V4 names Houssam and gives each current row a persisted evidence location (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:146-169`), but several criteria still do not match the cited sources. E-1 requires every row to be `revoked` and forbids a High-risk acceptance (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:162`), while the source's final readiness gate permits High rows to be `accepted-non-prod-with-owner-signoff` and Medium rows to be `accepted-with-owner-signoff` (`docs/security/secret-rotation-2026-05-12.md:235-243`). E-3 omits required schema-diff, actual staging migration, migrate-status, chain-verifier, staging smoke, and timing evidence (`docs/qa/2026-05-12-migration-audit-and-rollback.md:20-37`, `docs/qa/2026-05-12-migration-audit-and-rollback.md:67-86`). E-4 omits the five legal subjects and the required reviewer/date/items/approval record (`docs/qa/2026-05-13-first-tenant-handoff.md:88-108`). These are not exact folds. |
| **R3 — make the P0 smoke protocol executable and owned** | **weakened** | V4 adds the smoke file to D2-a's manifest, replaces the nonexistent command family, raises offline duration to at least ten minutes/five receipts, names an owner, and requires evidence/sign-off (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:128-142`). But its explicit authoritative-event invocation lists only `--tenant/--terminal` (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:129-134`); the real command also requires `--actor-id`, failing without it (`apps/api/app/Modules/Fiscal/Infrastructure/Commands/VerifyEventChainCommand.php:70-75`, `apps/api/app/Modules/Fiscal/Infrastructure/Commands/VerifyEventChainCommand.php:88-103`), and the repository manual includes that argument (`docs/runbooks/fiscal-verify-all-chains.md:28-40`). The repair brief also does not add the handoff's release checksum and clock/timezone checks (`docs/qa/2026-05-13-first-tenant-handoff.md:69-80`) or repair the protocol's structural mismatch between its promise to record an actual outcome and its tables, which have only Status and Evidence columns (`docs/qa/2026-05-12-first-tenant-smoke.md:23-34`). |
| **R4 — reuse the existing enable command, pin exact filenames, and reconcile the trigger** | **incorporated** | D1 marks `ConfigureCashRoundingCommand.php` and its existing test read-only unless the launch-contract test proves a defect, forbids a duplicate command, names the new migration and tests exactly, and exercises the resolver plus HTTP endpoint after target-scoped enablement (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:74-111`). The real command is `pos:configure-cash-rounding` with denomination, enable/disable, dry-run, and verify flags (`apps/api/app/Console/Commands/ConfigureCashRoundingCommand.php:65-79`), and its existing test already pins enablement (`apps/api/tests/Feature/POS/ConfigureCashRoundingCommandTest.php:63-90`). The repository's outer tenancy invocation requires explicit `--tenants='<tenant-uuid>'` (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:224-236`). |
| **R5 — add target-tenant activation evidence** | **incorporated** | E-8 contains all four items demanded by round 3: a recorded location disposition, the real terminal at schema 3, an explicitly target-scoped command, and device/API policy evidence before the first transaction (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:169`). `TerminalResource` exposes the schema version (`apps/api/app/Modules/POS/Presentation/Resources/TerminalResource.php:119-127`), and the policy endpoint serializes the resolver DTO (`apps/api/app/Modules/POS/Presentation/Controllers/PosPaymentPolicyController.php:28-33`). This fold is complete as to R5's four requested facts; section 2(d) identifies additional repository-required rollout evidence that R5 did not enumerate. |
| **R6 — resolve D2/E ownership and name exact paths** | **incorporated** | V4 makes D2-a and E sequential phases of one docs/ops lane, transfers ownership only after D2-a merges, bans later agent writes to the transferred files, and identifies the E evidence set (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:115-153`). It also names both D2-a handoff files and all D1 new files (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:98-110`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:136-142`). The fence is real for the declared program; a new staging/release execution gate will require expanding the transferred set, as described below. |

No R1–R6 item is wholly missed. R2 and R3 remain weakened.

## 2. Explicit answers to “What round 4 must answer”

### (a) D2-a → Phase-E fence and E write-set completeness

**The declared ownership transfer is a real fence.** D2-a owns the gate sheet, smoke protocol, and all `docs/pos-operations/*.md` files through its review and merge, then stops (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:119-144`). Only on that merge does Phase E receive the gate sheet, smoke protocol, migration audit, secret-rotation file, and `docs/pos-operations/*.md`; the plan explicitly prohibits later agent writes (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:146-153`). None of A, B, C-spec, D1, or later F writes any of those paths (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:180-190`). There is therefore no concurrent writer on the declared shared files.

**The E write set is complete for E-1…E-8 exactly as those rows are currently written.** E-1 writes the secret file and gate sheet; E-2 the smoke file and gate sheet; E-3 the migration-audit file and gate sheet; E-4/E-6 the walkthrough file under `docs/pos-operations` and the gate sheet; E-5 the `docs/pos-operations` placeholders and gate sheet; E-7/E-8 the gate sheet (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:148-169`). That is a narrow yes about persistence, not a finding that the rows are sufficient or executable.

The set is not complete for the actual launch program. D2-a creates `docs/handoff/STAGING-RUNBOOK-first-tenant-2026-07-31.md` but that file does not transfer to E (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:121-124`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:148-153`), and no E row requires its remaining steps to be executed. If the new execution gate records evidence in the staging runbook, that exact path must transfer too; alternatively, the gate sheet must be the declared evidence sink.

### (b) Exactness of E-row pass/NO-GO criteria

**No. The rows are mixed: E-5, E-6, and E-7 are source-faithful; E-1, E-2, E-3, E-4, and E-8 need correction or expansion.**

| Row | Exactness finding |
|---|---|
| **E-1 secret rotation** | The inventory structure is now correctly count-independent: the file has nine credential rows representing twelve named variables (`docs/security/secret-rotation-2026-05-12.md:53-67`), a connection-target decision (`docs/security/secret-rotation-2026-05-12.md:69-80`), an evidence ledger (`docs/security/secret-rotation-2026-05-12.md:245-258`), and final sign-off (`docs/security/secret-rotation-2026-05-12.md:260-266`). The pass status is not exact because the same source says both “Every row ... `revoked`” (`docs/security/secret-rotation-2026-05-12.md:53-55`) and, in its formal gate, that High/Medium rows may use owner-signed acceptance statuses (`docs/security/secret-rotation-2026-05-12.md:235-243`). V4 silently selects the stricter interpretation (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:162`). A stricter rule may be reasonable, but it must be declared as an intentional override and the source normalized; it cannot be described as an exact match while the source contradicts it. |
| **E-2 P0 smoke** | The real commands are `fiscal:verify-chains --company`, `pos:verify-chains --company/--terminal/--type`, and `fiscal:verify-event-chain --tenant/--terminal/--actor-id` (`apps/api/app/Modules/Compliance/Commands/VerifyFiscalChainsCommand.php:23-26`, `apps/api/app/Modules/POS/Commands/VerifyPosChainCommand.php:35-38`, `apps/api/app/Modules/Fiscal/Infrastructure/Commands/VerifyEventChainCommand.php:70-75`). V4's repair omits the mandatory `--actor-id` from the third command (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:128-135`). It also does not add the release checksum and clock/timezone items required by the P0 handoff (`docs/qa/2026-05-13-first-tenant-handoff.md:69-80`), and it leaves unresolved how the protocol records the “actual outcome” promised by its prose when each table only has Status/Evidence columns (`docs/qa/2026-05-12-first-tenant-smoke.md:23-34`). Finally, V4 says any failed step is an unconditional NO-GO (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:163`), whereas the smoke protocol and handoff currently provide owner risk-acceptance routes (`docs/qa/2026-05-12-first-tenant-smoke.md:105-114`, `docs/qa/2026-05-13-first-tenant-handoff.md:82-84`). The plan must either preserve those source rules or explicitly supersede and edit them. |
| **E-3 migration rehearsal** | Not exact and not executable from the current source. The source requires a live backup plus checksum and five fiscal-table counts, a pretend run, irreversible review, and schema diff (`docs/qa/2026-05-12-migration-audit-and-rollback.md:20-30`); an actual migration, `migrate:status`, chain verification, and happy-path smoke (`docs/qa/2026-05-12-migration-audit-and-rollback.md:31-37`); and, on the staging clone, the actual migration, timing capture, chain verifier, and smoke before production is unblocked (`docs/qa/2026-05-12-migration-audit-and-rollback.md:67-86`). V4 names only a dry-run/review/checksum/count subset (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:164`). The source also invokes nonexistent singular `fiscal:verify-chain` (`docs/qa/2026-05-12-migration-audit-and-rollback.md:35-37`, `docs/qa/2026-05-12-migration-audit-and-rollback.md:83-86`), while D2-a has no write ownership over the migration document (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:140-142`). |
| **E-4 TN legal** | The destination and risk-acceptance concept are correct, but the evidence contract is incomplete. The handoff requires VAT rates, receipt legal fields, certification scope, Z format, and retention to be reviewed, then requires reviewer name, date, reviewed-item list, and an explicit approved/approved-with-caveats flag (`docs/qa/2026-05-13-first-tenant-handoff.md:88-108`). V4 requires only “sign-off” or a dated risk acceptance (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:165`). |
| **E-5 preflight** | Exact and slightly stronger. The script exits 0 only with zero unresolved marker hits (`scripts/preflight-runbooks.sh:55-66`), and V4 additionally persists the full output in the gate sheet (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:166`). |
| **E-6 walkthrough** | Source-faithful. The rehearsal requires another teammate to explain the runbooks, has one explain-back result per document, and records “Approved for go-live docs? YES/NO” plus required edits (`docs/pos-operations/walkthrough-rehearsal.md:1-22`, `docs/pos-operations/walkthrough-rehearsal.md:35-45`). V4 correctly rejects “rehearsal held” as sufficient (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:167`). |
| **E-7 correction chain** | Correct for the launch defect. The current online-only refund flow, separate approval sync/server settlement, schema-2 server test, and mutating VOID path remain in code (`apps/pos/src/lib/refundFlow/refundSettlementService.ts:1-32`, `apps/pos/src/stores/refundCheckoutStore.ts:331-390`, `apps/api/tests/Feature/POS/ReceiptReturnRefactorTest.php:490-496`, `apps/api/app/Modules/POS/Application/Services/ReceiptVoidService.php:71-88`). V4 makes reviewed code, schema-3 integration evidence, and the VOID disposition non-waivable (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:168`). |
| **E-8 activation** | It exactly folds round 3's four requested facts, but it is not an exact closure of the cited deployment source. The source requires a dry-run and real target-scoped enable (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:224-236`), then a mandatory manager briefing, the v64/v3 build, device migration 64, device policy-cache and CASH-method checks (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:238-251`), followed by a real rounded sale, tender edge cases, EOD/Z verification, server projection, GL, and audit-event verification (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:253-263`). V4 records only the location, terminal version, enable command, and policy response (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:169`). |

### (c) D1 corrected-manifest dispatch safety

**Yes. D1 is dispatch-safe as written.**

The existing command is the right dependency: it is tenant-context scoped, exposes the required switches and dry-run/verify options, and refuses invalid work (`apps/api/app/Console/Commands/ConfigureCashRoundingCommand.php:65-79`, `apps/api/app/Console/Commands/ConfigureCashRoundingCommand.php:98-167`). Its tests already cover dry-run and enablement (`apps/api/tests/Feature/POS/ConfigureCashRoundingCommandTest.php:63-90`). V4 makes both files read-only unless the new launch-contract test demonstrates a real defect and forbids a duplicate command (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:88-90`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:109-111`).

The default-flip migration closes omitted-version writers without rewriting existing rows. The current column default is 2 (`apps/api/database/migrations/tenant/2026_05_01_000002_add_fiscal_schema_version_to_pos_terminals.php:11-15`). All three public controller creation arrays currently omit the field (`apps/api/app/Modules/POS/Presentation/Controllers/TerminalController.php:98-124`, `apps/api/app/Modules/POS/Presentation/Controllers/TerminalController.php:375-399`, `apps/api/app/Modules/POS/Presentation/Controllers/TerminalController.php:441-465`), so explicit v3 assignments there make the API contract visible. The other production creation sites are compatible with the default flip: the virtual-admin resolver and DemoPharmacy already write v3 (`apps/api/app/Modules/POS/Application/Services/VirtualAdminTerminalResolver.php:38-55`, `apps/api/database/seeders/DemoPharmacySeeder.php:752-770`), while CoffeeShop omits the version and will receive the new database default (`apps/api/database/seeders/CoffeeShopSeeder.php:1205-1233`).

The proposed files are exact in v4 (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:98-110`). Read-only existence checks at this checkout returned each of these as absent/new, so there is no filename collision:

```text
apps/api/database/migrations/tenant/2026_07_31_000001_default_pos_terminals_fiscal_schema_version_3.php
apps/api/tests/Feature/POS/TerminalCreationFiscalSchemaVersionTest.php
apps/api/tests/Feature/Identity/TenantLaunchContractTest.php
```

The plan also correctly keeps registration from importing POS models: registration creates the company and a POS-disabled location, then calls the tenant initializer, but creates no terminal (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:137-189`). The launch test can therefore prove the real controller creation paths, fresh disabled resolver state, target-scoped command, resolver result, and HTTP response entirely within D1's declared conditional write set (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:91-111`).

### (d) Remaining uncovered tenant-#1 blockers

**Yes—three program-level blockers remain uncovered.**

1. **The consolidated staging runbook has no execution/closure owner or gate.** D2-a is instructed to author it, “EXECUTE NOTHING,” merge, and stop (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:119-144`). The source checklists contain load-bearing migrations, permission reseeds/cache resets, long-running-process restarts, backfills, shared storage/cleanup, and staging/runtime checks (`docs/handoff/treasury-phase3-deploy-checklist.md:13-37`, `docs/handoff/treasury-phase5a-deploy-checklist.md:18-67`, `docs/handoff/treasury-phase5b-deploy-checklist.md:14-53`, `docs/handoff/treasury-phase5b-deploy-checklist.md:55-94`, `docs/handoff/multiloc-deploy-checklist.md:5-24`). None of E-1…E-8 requires the resulting runbook to be executed and evidenced (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:160-169`).
2. **There is no explicit production release/cutover gate.** The handover says the orchestrator owns merges into local `dev` and batched promotion to `origin/dev`, and that a push to `origin/dev` deploys staging (`docs/handoff/HANDOVER-first-tenant-orchestrator-2026-07-31.md:8-11`). V4's execution order ends with local-dev merges and a batched promotion of A+B+D1+D2-a+F, with the C code phase in a later batch (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:198-203`); it never names the production revision/artifact promotion, actual production migration, permission/backfill operations, process restart, or release evidence. The migration source separately defines an actual production run and verification (`docs/qa/2026-05-12-migration-audit-and-rollback.md:31-37`), but E-3 is only a rehearsal row (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:164`). Whether tenant #1 is intended to run on staging cannot be verified from the plan; the plan calls it the “real tenant #1” (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:169`).
3. **The real device is not gated through the repository's complete enable-and-rollout procedure.** E-8 stops at policy activation (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:169`), while the deploy checklist calls the manager briefing non-optional, requires the v64/v3 build and device DB checks, and then requires real sale/Z/projection/GL/audit verification (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:238-263`). The current generic smoke does include sales, refunds, Z, offline, backup/restore, and environment checks (`docs/qa/2026-05-12-first-tenant-smoke.md:44-99`), but it does not assert the cash-rounding-specific screen/ticket amounts, device schema 64, policy-cache value, `is_cash_tender`, GL sources, or absence of the two policy/change audit events required by the deploy checklist (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:245-263`).

## 3. Concurrent-lane pairwise-disjointness and manifest sufficiency

### Pairwise-disjointness on the declared paths

The actual first wave is A, B, C-spec, D1, and D2-a; F starts only after A and D1 merge (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:171-176`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:198-203`). On the declared paths, every pair is disjoint:

| Lane | Normalized declared write set | Collision result |
|---|---|---|
| A | `apps/api/app/Modules/Accounting/Application/Services/Reports/SalesReportService.php`; one of the two named Accounting tests (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:27-40`) | No other lane writes the Accounting service or Accounting tests. |
| B | `apps/pos/eslint.config.js`; test/helper files only under the three named POS test directories (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:42-56`) | No other first-wave lane writes `apps/pos`; C is docs-only in this phase. |
| C-spec | `docs/superpowers/specs/2026-07-31-v3-refund-chain-integration.md` only (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:58-68`) | D2-a writes `docs/handoff`, `docs/pos-operations`, and one `docs/qa` file, not `docs/superpowers/specs`. |
| D1 | The four named seeders, TerminalController, exact new migration/tests, conditional TenantProvisioningService, and the two conditional command/test paths (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:98-111`) | Shares `apps/api` with A but no file or test directory. It shares no file with B, C-spec, or D2-a. |
| D2-a | The two exact handoff files, `docs/pos-operations/*.md`, and the exact smoke file (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:136-142`) | No overlap with C-spec or any code lane. |
| F, later | `apps/api/deptrac.baseline.json` and `docs/superpowers/tickets/2026-07-31-deptrac-36-edges.md` (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:171-176`) | No overlap with A/D1 after it starts, and no overlap with B/C-spec/D2-a. |

A's test alternative is not one frozen filename before dispatch, but both alternatives are named and both are confined to the disjoint Accounting directory (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:37-39`). That choice does not create a race. B's existing `cartMutatorGuard.eslint.test.ts` sits outside B's writable test directories, under `apps/pos/src/lib/stock/__tests__`, but B4 only needs to run that existing test after changing the config; the plan does not instruct B to edit it (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:48-55`).

### Manifest sufficiency by lane

- **Lane A — sufficient.** The defect is confined to the query's sum and `COUNT(*)` (`apps/api/app/Modules/Accounting/Application/Services/Reports/SalesReportService.php:163-186`); the DTO already carries string amount and integer transaction count (`apps/api/app/Modules/Accounting/Application/DTOs/Reports/PaymentMethodBreakdownData.php:11-19`), and the controller passes through the service result (`apps/api/app/Modules/Accounting/Presentation/Controllers/ReportsController.php:151-163`). The service plus one discriminating Accounting test can satisfy the contract.
- **Lane B — sufficient.** The PHP cap is a private integer-keyed constant (`apps/api/app/Shared/Domain/CashRoundingCaps.php:31-44`), while the existing helper recognizes only `public const` (`apps/pos/src/lib/fiscal/__tests__/FiscalPayloadKeyDrift.test.ts:104-125`), so B1 fits the allowed fiscal test/helper scope. The 32-value SQL bind is in `insertOfflineReceipt` (`apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:90-121`) and can be exercised by a new real-SQLite test in the allowed DB test directory. B3's existing Z test is in the allowed offline test directory, and B4 needs only `apps/pos/eslint.config.js`, whose third flat-config block currently omits the cart selectors (`apps/pos/eslint.config.js:315-340`).
- **Lane C-spec — sufficient for the spec phase.** Its only deliverable is the one specification, and the code phase is forbidden until B merges and must freeze a new manifest against then-current `dev` (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:58-68`). The current code demonstrates why the later code manifest cannot be inferred now: `SALE_VOID` and `REFUND_RECEIPT` are reserved but absent from the implemented registry list (`apps/pos/src/lib/fiscal/FiscalEventPayloadRegistry.ts:55-92`), and canonical corrections require the original fiscal event UUID (`apps/pos/src/lib/fiscal/FiscalEventEngine.ts:367-375`).
- **Lane D1 — sufficient and dispatch-safe.** Its conditional manifest reaches the database default, every public controller writer, demo writers that rely on the default, fresh provisioning/seeder state, command, resolver, and HTTP boundary (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:82-113`). Section 2(c) verifies the topology.
- **Lane D2-a — file-set sufficient for its stated authoring deliverables, but the brief is not acceptance-safe.** Every file it is told to author or repair lies inside its manifest (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:119-144`). The problem is not a missing write path for its current tasks; it is that the smoke command contract omits `--actor-id`, the migration procedure also needs pre-transfer authoring but is outside D2-a, and no later row executes the staging runbook (`apps/api/app/Modules/Fiscal/Infrastructure/Commands/VerifyEventChainCommand.php:70-75`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:140-169`).
- **Lane F — sufficient.** Its contract is exactly baseline regeneration plus a ticket, and its two declared files cover those outputs (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:171-176`).

## 4. Required fixes

VERDICT: REJECT (round 5 review required)

1. **Repair D2-a's smoke contract and source completely.** Add the mandatory `--actor-id` to `fiscal:verify-event-chain`; make all three invocations explicitly target the real tenant/company/terminal; add release-version/checksum and clock/timezone checks from the P0 handoff; add an Actual Outcome column or explicitly define how Status records `expected = actual`; reconcile the current risk-acceptance language; and require every named signatory before PASS (`apps/api/app/Modules/Fiscal/Infrastructure/Commands/VerifyEventChainCommand.php:70-103`, `docs/runbooks/fiscal-verify-all-chains.md:28-43`, `docs/qa/2026-05-13-first-tenant-handoff.md:69-84`, `docs/qa/2026-05-12-first-tenant-smoke.md:23-34`, `docs/qa/2026-05-12-first-tenant-smoke.md:105-114`).
2. **Move migration-procedure authoring into D2-a before the E transfer.** Add `docs/qa/2026-05-12-migration-audit-and-rollback.md` to D2-a's manifest, replace its nonexistent singular verifier, and make E-3 require the full source procedure: backup/checksum/five row counts, pretend output and irreversible review, schema diff, actual staging migration, timing/downtime estimate, migrate status, all applicable chain verifiers, staging smoke, and the production run/verification record (`docs/qa/2026-05-12-migration-audit-and-rollback.md:20-37`, `docs/qa/2026-05-12-migration-audit-and-rollback.md:67-86`). Transfer the repaired file to E only after doc review.
3. **Normalize E-1 to one authoritative secret-closure rule.** Reconcile the source's “every row revoked” instruction with its formal High/Medium acceptance statuses, then make the plan and gate sheet use that exact rule while preserving all nine rows/twelve variables, the connection-target decision, one evidence entry per rotation, and final sign-off (`docs/security/secret-rotation-2026-05-12.md:53-80`, `docs/security/secret-rotation-2026-05-12.md:235-266`).
4. **Make E-4's evidence contract complete.** Require evidence for VAT rates, receipt legal fields, certification scope, Z format, and retention, plus reviewer name, date, reviewed-item list, and explicit approval/approval-with-caveats—or a dated owner risk acceptance with reason and revisit milestone (`docs/qa/2026-05-13-first-tenant-handoff.md:88-108`).
5. **Add a named staging-runbook execution gate and an explicit production release/cutover gate.** The former must close every still-applicable consolidated deploy owe with persisted output; the latter must identify the exact production revision/artifact, actual production migration and verification, permission reseeds/cache resets, required backfills/configuration, process restarts, and final release evidence. Transfer `docs/handoff/STAGING-RUNBOOK-first-tenant-2026-07-31.md` to E if it will carry execution evidence, or name the gate sheet as the sole evidence sink. Do not treat `origin/dev`'s staging deploy as production clearance (`docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:119-144`, `docs/handoff/DISPATCH-PLAN-v4-first-tenant-2026-07-31.md:198-203`, `docs/handoff/HANDOVER-first-tenant-orchestrator-2026-07-31.md:8-11`).
6. **Expand E-8 (or E-2 with an explicit cross-reference) to close the full real-device rollout.** Require both target-scoped dry-run and real enable commands, manager briefing, the v64/v3 release build, device migration-64 query, policy-cache and CASH-method checks, a real rounded cash sale and receipt, EOD/Z success, populated server projection, expected GL source rows, and absence of the policy-mismatch/change-exceeds-cash audit events before the first live transaction (`docs/handoff/cash-rounding-phase2-deploy-checklist.md:224-263`).
7. **Re-run the exact-path fence and row-source audit in round 5.** Include every new/expanded evidence file in the D2-a → E transfer and verify that the added staging/production/device gates have named owners, pass/no-go rules, and persisted evidence locations.
