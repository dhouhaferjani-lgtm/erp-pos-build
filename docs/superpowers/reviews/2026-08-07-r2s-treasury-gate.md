# GATE RECORD — R2-S (`fix/r2s-partner-refguard`) — treasury axis, round 1

**Verdict: APPROVE-WITH-FIXES** (money-bearing reference semantics axis; module-boundary/connection-timing owned by the parallel authz gate). Reviewed @ `bdfd71eb7`.

## Verified correct (recorded so later gates do not re-litigate)
- `payments` RESTRICT, no soft deletes — flag false correct. `journal_lines` RESTRICT, partner_id genuinely populated on AR/AP legs (GeneralLedgerService:160,319,375,457; AccountingService:472,634) — posted GL is a real backstop behind soft-deletable documents.
- **`payment_instruments` — the lane's headline fix is REAL:** bare nullable uuid, no FK, nullable payment_id; pre-lane a partner holding an uncashed cheque deleted cleanly → silent anonymization (null-safe renderers, `partner: null`), not a crash. Post-lane: 409.
- `vouchers` soft-delete exclusion financially SAFE today: redemption/lookup run the non-trashed scope; NO code path in `app/` soft-deletes a voucher, so exclusion releases no live value. Two-column OR correct + tested.
- withholding tables/flags match migrations; `expense_recurrence_templates` blocking correct (standing instruction, not history). Base-class SQL correct (per-ROW count). No missed FK-DECLARED money table (three-way cross-check: constrained()/on('partners') declarations, all 23 `belongsTo(Partner::class)` hits, per-table column scan of 14 candidate money tables).
- Aggregate consistency sufficient by construction: journal_lines counting means any partner that ever hit the GL is undeletable; PartnerBalance caches on the partners row itself; and the delete is SOFT, so even a hypothetical escape leaves the row `withTrashed()`-resolvable.

## Findings
- **I-1** `pos_deposit_receipts` / `pos_account_charge_receipts` / `pos_account_payment_receipts` are partner-keyed MONEY read models (FK-less string `customer_id` — the class an information_schema FK sweep cannot see; the fiscal validator enforces customer_id == partner_id) neither counted nor excluded-with-reason. Concrete scenario: projectors run in independent transactions; DEPOSIT_RECEIPT's POS-core projection commits while the Treasury bridge row stays `pending` (module gating / dependency deferral) → no payments/journal_lines rows → counter 0 → 204 → **stranded advance-payment liability keyed to a deleted partner, `GET /partners/{id}/deposits` unreachable forever**. Caveat for the fix: `pending_create` rows hold the device uuid — covered transitively by counted `pos_customer_aliases`.
- **I-2** no completeness ratchet: coverage test circular vs schema; I-1 was found by grep, not by any test; the counter docblock's "gap is now closed for every table…" claim unverified. Fix: PG-capable schema-sweep + pinned FK-less column list + asserted EXCLUDED constant.
- **m-1** soft-delete flag asserted as a mechanical biconditional of schema — the day someone adds softDeletes() to `payments`/`withholding_certificates`, the test FORCES `hasSoftDeletes: true` and the guard silently stops blocking archived money rows. Keep forward direction; make the reverse a curated allowlist.
- **m-2** `payments`/`payment_instruments` counts have no usable index (`(tenant_id, partner_id)` composites, leading column absent from the predicate) → 2 of 21 counts seq-scan per DELETE. [Round-2 correction: the honest count is larger — see the T11 correction in the ticket.]
- **m-3** `payments` counted status-blind (cancelled/failed rows permanently veto with no remediation path) — defensible as "payments are history" but must be SAID, since documents filters (via soft-delete) and the asymmetry reads as oversight.
- **Recommendation (no scope creep):** first-class partner archive/deactivation remains the better end state; this lane's block-on-live-references is correct for what it is but accumulates one-off declarations — ticket it.
