VERDICT: SHIP

Prior storeMultiple HIGH: CLOSED. The current `POST /payments` route still dispatches payloads containing `payments` to `PaymentController::storeMultiple()` (`apps/api/app/Modules/Treasury/Presentation/routes.php:107`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:107`-`109`), but the previously vulnerable primary `document_id` load now calls `rejectSupplierInvoiceInMultiline()` before any transaction/write (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:682`-`695`). The previously vulnerable manual excess allocation load now locks the target document and calls the same guard before creating the excess allocation or AR journal (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:931`-`954`).

## BLOCKER

None.

## HIGH

None.

Coverage check: all reviewed HTTP-reachable supplier-invoice pay/allocation paths are guarded. Single-payment `/payments` remains the only supplier-aware path: it detects `DocumentType::SupplierInvoice`, rejects mixed supplier/non-supplier allocations, requires a ledgered repository, and rejects supplier payment excess (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:217`-`225`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:256`-`288`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:291`-`305`). Multi-line `/payments` rejects supplier invoices at the primary document and manual-excess sites (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:619`-`631`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:682`-`695`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:931`-`943`). Split payment and deposit application reject supplier invoices (`apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:54`-`65`, `apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:184`-`194`). Smart manual allocation rejects supplier invoices in both preview and locked apply flow (`apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:170`-`183`, `apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:615`-`623`), while smart FIFO/due-date auto allocation queries only customer invoices and sales orders (`apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:425`-`443`). The open-invoices endpoint likewise exposes only `DocumentType::Invoice` (`apps/api/app/Modules/Treasury/Presentation/Controllers/SmartPaymentController.php:176`-`186`). Deposits/on-account create unallocated payments without a document id, so they do not allocate against supplier invoices until the guarded apply path is used (`apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:103`-`148`, `apps/api/app/Modules/Treasury/Domain/Services/MultiPaymentService.php:106`-`156`, `apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:270`-`300`, `apps/api/app/Modules/Treasury/Domain/Services/MultiPaymentService.php:255`-`304`).

GL correctness check: supplier payments post the required Stage C4 direction. The design requires `Dr SupplierPayable / Cr Treasury` for payment (`docs/superpowers/specs/2026-06-24-domestic-procurement-to-pay-gr-ir-design.md:50`-`52`). The supplier branch decrements repository balance with `bcsub()` (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:478`-`488`), calls `createSupplierPaymentJournalEntry()` (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:517`-`531`), and refreshes the derived partner balance after commit (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:533`-`540`). The GL service creates `source_type = supplier_payment`, debits SupplierPayable partner-tagged, and credits the repository account with no partner (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:548`-`605`). The AR method remains the opposite `customer_payment` path and is only called in the non-supplier branch (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:541`-`552`, `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:615`-`677`).

Concurrency check: I did not find a supplier over-payment race that can pass the locked write. The supplier path still has pre-transaction reads of document balance (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:187`-`225`), but the write loop locks each document with `lockForUpdate()` and re-checks `allocationAmount > currentBalance` before creating the `PaymentAllocation`, repository movement, or GL (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:387`-`425`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:464`-`531`). INFERRED: a concurrent stale request can still block and then fail, but it should not over-allocate the supplier invoice because the locked-row guard runs before mutation.

Precision check: the reviewed payment amounts are scale-capped at ingress and use bcmath for money arithmetic. Single-payment amount/allocation validators cap at 3 decimals (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:133`-`150`), multi-line and excess amounts cap at 3 decimals (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:665`-`679`), split/deposit/apply-deposit/on-account validators cap at 3 decimals (`apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:40`-`51`, `apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:117`-`130`, `apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:174`-`176`, `apps/api/app/Modules/Treasury/Presentation/Controllers/MultiPaymentController.php:280`-`285`), and smart allocation ingress caps at 3 decimals (`apps/api/app/Modules/Treasury/Presentation/Controllers/SmartPaymentController.php:72`-`80`, `apps/api/app/Modules/Treasury/Presentation/Controllers/SmartPaymentController.php:121`-`128`). I found bcmath use on the reviewed balance/repository paths, including supplier overpayment checks and repository decrement (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:164`-`167`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:225`-`246`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:431`-`436`, `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:485`-`487`).

AR regression check: same-customer AR payments still have coverage. The test suite still covers single-payment multiple AR invoice allocation (`apps/api/tests/Feature/Treasury/PaymentTest.php:900`-`941`) and the new multi-line customer payment path asserts cash-in, paid invoice, customer GL present, and no supplier-payment GL (`apps/api/tests/Feature/Treasury/PaymentTest.php:724`-`765`). Customer overpayment cap/excess behavior remains covered for the single-payment AR path (`apps/api/tests/Feature/Treasury/PaymentTest.php:790`-`821`).

## MEDIUM

None.

## LOW

1. Test gap: no direct manual-excess supplier-invoice regression test for the second new guard. The code guard exists at the manual excess allocation site (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php:931`-`943`), but `PaymentTest` only exercises a multi-line payload whose primary `document_id` is a supplier invoice (`apps/api/tests/Feature/Treasury/PaymentTest.php:694`-`721`). Concrete impact: a future edit could remove the line-943 guard while the current new storeMultiple test still passes, reopening the prior manual-excess half of the HIGH. Recommended fix: add a test with a customer primary invoice, excess allocation method `manual`, and `excess_allocations[0].document_id` set to a posted `supplier_invoice`; assert 422, no payments/allocations persist, repository unchanged, and no `customer_payment`/`supplier_payment` JE.

2. Test assertion gap: rejected supplier paths usually assert no payment/JE/repository movement, but they do not always assert no `payment_allocations` row. The new multi-line rejection asserts no payment, no cash movement, and no supplier/customer payment JE (`apps/api/tests/Feature/Treasury/PaymentTest.php:716`-`721`), and the smart allocation rejection asserts no supplier-payment JE (`apps/api/tests/Feature/Treasury/PaymentTest.php:688`-`691`), but neither directly asserts `payment_allocations` absence for the rejected supplier document. Concrete impact: allocation side effects would be caught indirectly in the primary multi-line case because no payment exists, but not as a first-class invariant for every rejected path. Recommended fix: add `assertDatabaseMissing('payment_allocations', ['document_id' => $supplierInvoice->id])` to supplier rejection tests, especially the smart/manual and future manual-excess storeMultiple cases.
