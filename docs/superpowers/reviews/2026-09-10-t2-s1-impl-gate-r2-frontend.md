# T-2 S1 receipt spine — implementation gate r2 — frontend-conventions-reviewer

Reviewed: lane/t2-receipt-spine @ d7123fa30 (web diff of the slice de3007fe9..d7123fa30, fix round 93b106461..d7123fa30), plan rev 9 §0AA option (a)
Date: 2026-09-10
Reviewer: Claude Opus `frontend-conventions-reviewer`, read-only. First frontend gate on this slice. All `path:line` are at `d7123fa30` under `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/`. Never diffed against `dev`. Vitest SKIPPED on the swap rule (see Runs). Tenancy's T-9..T-15(r2), stock-gl's G-9..G-12(r2) and inventory's I-11(r2) are theirs; I answer the two items deferred to me (**T-14(r2)** badge, **I-11(r2)** frontend half) and do not re-report the rest.

## Verdict
VERDICT: CHANGES-REQUIRED
BLOCKER=0 MAJOR=3 MINOR=9

**Tenancy's T-9(r2) BLOCKER stands and gates the lane.** I re-ran it and confirm it independently (exit 1, exactly 118 NEW `ar|stock-transfers|missing` gaps); I do not count it again here to avoid double-counting. I additionally **proved the ruled revert green** by simulating it in the worktree and restoring byte-identical — diff-level instructions in the dedicated section below, plus a trap the auditor's own output sets for the fix round (**F-12**).

The ruled M-2 option (a) is implemented and behaviourally correct: the generated union is in, all seven statuses are nameable and filterable, `canComplete` accepts `partially_received`, and **no receive/close UI shipped**. My three MAJORs are (1) the slice made three backend DTOs `#[TypeScript]` **and put them on the wire** while leaving the hand-rolled FE family that now misdescribes that wire; (2) the badge rewrite exceeded the ruling and lands a status→colour map the C6 detector provably cannot see; (3) the widening turned `StockTransferDetailPage` into a "web receipt surface" in the R5 ruling's own sense while it renders the unmasked sent quantity — which also changes what the ruled I-11(r2) copy should say.

## Required verifications (items 1–10)

| # | Requirement | Status | Evidence |
|---|---|---|---|
| 1 | Convention 11 / 04 — status union aliases the generated enum; residual hand-rolled family assessed | **VERIFIED (alias) / CONTRADICTED (residual family)** | Alias in: `apps/web/src/features/stock-transfers/types/index.ts:2` `export type StockTransferStatus = App.Modules.Inventory.Domain.Enums.TransferStatus` → `packages/shared/types/generated.d.ts:1453` (7 members). The old hand-rolled 4-member union and its "replace this file with the generated output" docblock are gone (`git show de3007fe9:…/types/index.ts:1-12`). **But** `:11-24`, `:26-34`, `:36-62` still hand-roll `StockTransferLine`, `StockTransferLineBatchAllocation`, `StockTransfer` beside `generated.d.ts:1264`, `:1250`, `:1215`, and `:4` / `:6-9` still hand-roll `StockTransferType` / `TransferCostDistribution` beside `generated.d.ts:1454` / `:1448`. Full census + blast radius in the table below → **F-1** |
| 2 | Badge — atom is canonical; `tokens.badge` fitness; variants available; audit result | **CONTRADICTED** | `StockTransferStatusBadge.tsx:2` dropped `Badge`/`BadgeVariant`; `:5-13` holds raw token colours; `:21` renders a bare `<span>`. `tokens.badge.*` DO exist (`src/lib/designTokens.ts:1011-1022`) and are the atoms' own substrate (`components/atoms/StatusBadge/StatusBadge.tsx:36-43`), so the classes are legal — but the *surface* is not. `Badge.tsx:4` already declared `warning`, `danger`, `default`, so all three new rows were available. `pnpm audit:design-system` → **exit 0, 802 acknowledged, 0 new, 0 stale** — and that green is **not** evidence: C6 provably cannot see this map (mechanism test in **F-2**) → **F-2** |
| 3 | i18n — three keys en/fr; no hardcoded user-facing string on a touched line; `lib/i18n.ts` needs no change after the revert | **VERIFIED** | Keys: `locales/en/stock-transfers.json` `status.partially_received` / `.closed_with_writeoff` / `.closed_returned` (en block confirmed 7 members), `locales/fr/stock-transfers.json` same three (`Partiellement reçu` / `Clôturé avec perte` / `Clôturé avec retour`). Namespace **already** registered for en (`lib/i18n.ts:219`), fr (`:276`) and in the `ns` array (`:497`) — so after the revert the file needs **no** change at all; I proved that empirically (`git diff --stat 93b106461 -- src/lib/i18n.ts` was EMPTY post-revert). No hardcoded user-facing string on any touched line: badge label is `t(\`status.${status}\`)` (`:21`), filter labels `t(\`status.${s}\`)` (`StockTransferListPage.tsx:74-76`), the detail change is a predicate (`:38`). The `ar` badge test case must go — see the revert section |
| 4 | Detail page — `canComplete` widening; `detail.confirmComplete.*`; `quantity_remaining` availability; per-line column in scope? | **VERIFIED (widening) / CONTRADICTED (copy + availability)** | Widening at `StockTransferDetailPage.tsx:38`, backend agrees (`TransferStatus.php:45-48`). Copy at `locales/en/stock-transfers.json` `detail.confirmComplete.description` = *"Destination stock will be incremented and the company-wide weighted average cost will be recomputed if the transfer has additional costs."* — quantity-free and identical for `in_transit` and `partially_received`. `quantity_remaining` **is on the wire** (`StockTransferLineData.php:67`ff, `generated.d.ts:1276`) but is **invisible to the FE type** (`types/index.ts:11-24` omits it) — so the ruled I-11(r2)(b) copy is **blocked by F-1**. Recommendation on the column: **do NOT add it in S1** → **F-3**, **F-4** |
| 5 | List filters — seven statuses; options via `t()`; no hand-rolled label map | **VERIFIED, with two nits** | `StockTransferListPage.tsx:15-24` carries all seven + `'all'`; `:74-76` renders `t('filters.all')` / `t(\`status.${s}\`)`; no label map anywhere. Backend-safe: `StockTransferController.php:71` `TransferStatus::tryFrom`. Nits: no compile-time exhaustiveness (**F-5**), lifecycle order diverges from the enum (**F-6**) |
| 6 | TanStack keys — `tenantScopedKey` on every tenant-data query | **VERIFIED** | `features/stock-transfers/api/queries.ts:13` and `:20` both `tenantScopedKey([...])`; the four `invalidateQueries({ queryKey: [namespace] })` calls (`:36,48,62`) are prefix-matches and correct because `tenantScopedKey` appends tenant/company as SUFFIXES. `pnpm audit:keys` → **exit 0**, "0 total, 0 baselined, 0 new, 0 stale" |
| 7 | Both-layer gating (T-4 residual) — checklist line; `routes/index.tsx` untouched | **VERIFIED** | `git diff --name-only de3007fe9..d7123fa30 \| grep -i route` returns **only** `apps/api/app/Modules/Inventory/Presentation/routes.php` — the web router is untouched. Guards unchanged at `apps/web/src/routes/index.tsx:1405`, `:1415`, `:1425` (`inventory.transfers.view` / `.create` / `.view`, no module gate). Disclosure present: `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md:11`. **The backend side is NOT missing** — the whole Inventory group carries `'module:Inventory'` (`apps/api/app/Modules/Inventory/Presentation/routes.php:31`), so the asymmetry is FE-only, exactly as the checklist states; S4 per plan `:811` / §10.4. Mechanism sharpened in **F-11** |
| 8 | Generated artefacts — artisan output, no hand edit; FE use of `TransferReceiptFailureReason` | **VERIFIED** | `permissionsMap.generated.ts` self-hash **recomputed and MATCHES** (`sha256:8c56d87a7a3765ded57192e845e147815d13aacc540132faff35692e46fb48a8`, hashed per `ExportFrontendPermissionsMap.php:84-90` over `"\n".$body`); the only content delta is the two new S1 permissions `inventory.transfers.close` / `.reconcile` → `['admin','manager']`, matching the checklist's seeding step. `generated.d.ts` byte-identical regeneration was proved by tenancy (their §"Tree state"); I did not re-run artisan. **The web consumes NO generated receipt DTO** — `grep -rn "TransferReceiptFailureReason\|StockTransferReceiptData\|TransferReconciliationData\|StockTransferData\|StockTransferLineData" apps/web/src apps/pos/src` → **zero hits**. That is itself the evidence for F-1: the generated family is on the wire and referenced by nothing |
| 9 | Runs | **VERIFIED / one SKIPPED with reason** | Table below. Vitest skipped: `vm.swapusage used = 11 989 MB` at start and `11 769 MB` at the end, i.e. **> 11 GB on both readings** — the brief's skip threshold. Zero vitest worker pools before and after |
| 10 | Scope census of the web diff against §0AA's ruled file set | **VERIFIED for the file set, two files justified outside it** | Table below. The web/shared file set is exactly the permitted list plus the three `inventory.json` movement-source keys (justified by necessity) and the two generated artefacts |

## Convention-11 census of apps/web/src/features/stock-transfers/types/index.ts

Wire authority: `GET /stock-transfers` and `GET /stock-transfers/{id}` now serialise **`StockTransferData`** — `StockTransferController.php:97` and `:131` call `$this->payloadBuilder->build(...)`, and `TransferPayloadBuilder.php:12-20` returns `StockTransferData::fromModel($transfer, $includeLines)`. **This slice created the shadow**: at `de3007fe9` the generated file contained `TransferStatus` / `TransferType` / `TransferCostDistribution` but **no** `StockTransferData` / `StockTransferLineData` / `StockTransferLineBatchAllocationData` (`git show de3007fe9:packages/shared/types/generated.d.ts` → 3 hits only, at `:1239-1241`), and the controller hand-built the payload array (`:330-333`).

| Hand-rolled type | Generated counterpart | Divergent fields | Consumers | Recommendation |
|---|---|---|---|---|
| `StockTransfer` `types/index.ts:36-62` (25 fields) | `StockTransferData` `generated.d.ts:1215-1249` (33 fields) | **8 absent**: `closed_by_user_id`, `closed_by_name`, `closed_at`, `close_disposition`, `close_reason`, `close_note`, `freight_uncapitalized`, `receipts`. `lines?: StockTransferLine[]` (optional) vs `lines: Array<StockTransferLineData> \| null` (required-nullable). `transfer_type`/`status` point at local aliases. No field is EXTRA | `api/stockTransferApi.ts:4,37,38,41,42,45,46,49,50` (type-only); `__tests__/StockTransferListPage.test.tsx:6,38`; `__tests__/StockTransferDetailPage.batchAllocations.test.tsx:6,8,16` | **REPLACE NOW** — `export type StockTransfer = App.Modules.Inventory.Application.DTOs.StockTransferData`. Costs 8 fixture lines in each of the two typed fixtures |
| `StockTransferLine` `:11-24` (12) | `StockTransferLineData` `generated.d.ts:1264-1282` (17) | **5 absent**: `quantity_received`, `quantity_damaged`, `quantity_written_off`, `quantity_returned`, `quantity_remaining`. `quantity_decimals: number \| null` vs `number` | `__tests__/StockTransferDetailPage.batchAllocations.test.tsx:6,16,46,81`; read structurally by `pages/StockTransferDetailPage.tsx:207-268` | **REPLACE NOW** — this is the type that blocks the ruled I-11(r2)(b) copy. `getQuantityDecimals` already accepts `number \| null \| undefined` (`src/lib/quantityScale.ts:1-16`), so `:225`/`:258` need no change |
| `StockTransferLineBatchAllocation` `:26-34` (7) | `StockTransferLineBatchAllocationData` `generated.d.ts:1250-1263` (12) | **5 absent**: the same five counters. `batch_number: string \| null` vs `string`; `expiry_status: string \| null` vs `string`; `can_be_sold: boolean \| null` vs `boolean` — the DTO casts `(string)`/`bool` (`StockTransferLineBatchAllocationData.php:51-55`) and `product_batches.batch_number` is NOT NULL (`2026_01_05_150000_create_product_batches_table.php:25`), so the generated non-nullability is the honest one | `__tests__/StockTransferDetailPage.batchAllocations.test.tsx` (batch fixture); read at `pages/StockTransferDetailPage.tsx:242-259` | **REPLACE NOW**, and delete the now-dead `?? '—'` at `:248` in the same edit (**F-10** — otherwise the ESLint warning ratchet grows) |
| `StockTransferType` `:4` | `TransferType` `generated.d.ts:1454` | **None** — identical members, pure duplication | none outside the file (`status` field only) | **S4** — pre-existing (generated since before `de3007fe9`), zero blast radius, no urgency |
| `TransferCostDistribution` `:6-9` | `TransferCostDistribution` `generated.d.ts:1448` | **None** — identical members, same name, pure duplication | `pages/CreateStockTransferPage.tsx:32,64,70,525` | **S4** — pre-existing; the alias would touch a page outside §0AA's set for zero behavioural gain |
| `CreateStockTransferLineInput`, `CreateStockTransferLineBatchAllocationInput`, `CreateStockTransferInput`, `StockTransferListFilters`, `StockTransferListResponse` `:64-98` | **none** | n/a — request/UI shapes with no `#[TypeScript]` DTO | api + create page + tests | **KEEP** — legitimately hand-rolled; no shadow |

**Scoped recommendation to the orchestrator: replace-now for the three DTO-shadowing interfaces, S4 for the two redundant unions.** Reasons: (a) `types/index.ts` is already in §0AA's ruled set and already touched, and the fix round already used exactly this alias pattern at `:2`; (b) the shadow is **slice-created** — before `de3007fe9..` there was no generated counterpart, so this is new debt, not inherited; (c) the ruled I-11(r2)(b) copy is unimplementable without `quantity_remaining` on the FE line type. Blast radius = **3 alias lines in, ~31 fixture lines and 1 dead-fallback deletion out**, in three files: `__tests__/StockTransferListPage.test.tsx` (+8 header fields), `__tests__/StockTransferDetailPage.batchAllocations.test.tsx` (+8 header, +5×2 line, +5 allocation), `pages/StockTransferDetailPage.tsx:248` (−`?? '—'`). Two of those three are **outside §0AA's file set**, so the orchestrator must widen the ruled set by those two test files or this stays open. Nothing else changes: `transfer.lines ?? []` (`:207`) already handles `null`, and `stockTransferApi.ts` needs no edit because the exported names are unchanged.

## Findings

### MAJOR

**F-1 — The hand-rolled `StockTransfer` / `StockTransferLine` / `StockTransferLineBatchAllocation` family is a stale, lossy declaration of a wire shape THIS SLICE changed, and it hides 18 fields the API now emits.** `types/index.ts:11-24, 26-34, 36-62` against `generated.d.ts:1215, 1250, 1264`, live via `TransferPayloadBuilder.php:19` ← `StockTransferController.php:97,131,290,315`. Conventions 04 ("generated types are the source of truth"; "never manually edit TypeScript interfaces for domain entities") and 11 ("no hand-rolled FE type beside a generated DTO"). Typecheck stays green precisely because the hand-rolled type is a strict subset — which is why nothing else in the pipeline can see this. *Fix directive:* replace the three interfaces with `App.Modules.Inventory.Application.DTOs.*` aliases in `types/index.ts`, patch the three consumer files per the census, and widen §0AA's ruled file set by the two test files.

**F-2 — The `Badge` atom was replaced by a hand-rolled `<span>` carrying a per-feature status→colour map, exceeding the ruling, and that map is provably invisible to the C6 detector (resolves T-14(r2)).** `StockTransferStatusBadge.tsx:2` (import dropped), `:5-13` (raw token colours), `:21` (`<span>`). §0AA ruled "**three `statusVariant` rows**" — a 3-line diff; the atom already carried every variant needed (`components/atoms/Badge/Badge.tsx:4,15-17`: `warning`, `danger`, `default`). The canonical surface is stronger still: `components/atoms/StatusBadge/StatusBadge.tsx:8-13` self-declares itself "the ONE sanctioned palette for status pills across the app… to replace the per-feature `Record<status, 'bg-x-100 text-x-800'>` color maps" — which is literally what `:5-13` now is. Precedent in the two closest siblings: `features/stock-adjustments/components/StockAdjustmentStatusBadge.tsx:2-3,22` (whose docblock names "exactly what the design-system audit's C6 rule exists to catch") and `features/replenishment/components/ReplenishmentStatusBadge.tsx:2,20`.
**Mechanism audit (protocol item 3).** I ran `tools/audit-design-system.mjs`'s four `STATUS_RE` alternations (`:75-80`) against the real strings: shipped `const statusVariant: Record<StockTransferStatus, string>` → **C6 MISS**; `const statusTone: Record<…, StatusTone>` → **C6 HIT on P1, P3 and P4**; `statusTone(status, {…})` inline → **C6 MISS**. Confirmed against the baseline, which carries three C6 entries for `ReplenishmentStatusBadge.tsx` (`tools/audit-design-system-baseline.json:778-780`) and **zero** for `StockTransferStatusBadge.tsx`. So `audit:design-system` exit 0 / 0 new is a naming accident, not compliance.
**Conservation (protocol item 4).** Expanding to literal classes, the rewrite is a **zero-pixel change** and restoring the atom is also zero-pixel: `Badge` `default/info/success/danger` = `surface.muted+text.strong` / `intent.primary.bgSoft+textStronger` / `success` / `danger` = `bg-gray-100 text-gray-800` / `bg-blue-100 text-blue-800` / `bg-green-100 text-green-800` / `bg-red-100 text-red-800` (`designTokens.ts:136`ff), identical to `tokens.badge.gray/blue/green/red` (`:1015-1019`); `warning` = `bg-yellow-100 text-yellow-800` = `tokens.badge.yellow`; and `tokens.badge.base` (`:1013`) is the same class SET as `Badge.tsx:24`.
*Fix directive (decision):* **restore the atom and add exactly the three ruled rows** — `partially_received: 'warning'`, `closed_with_writeoff: 'danger'`, `closed_returned: 'default'` — reverting `:2` and `:21` to `93b106461`. This is pixel-identical to what shipped, satisfies §0AA literally, adds zero baseline violations, and stays inside the ruled file. **Do NOT switch to `StatusBadge`/`Record<…, StatusTone>` in this fix round**: that shape would add **3 new C6 violations** (measured above) and would recolour the four pre-existing pills from `*-100/*-800` to `tokens.alert.*` = `bg-blue-50 text-blue-700` etc. (`designTokens.ts:990-996`) — a visual change outside the ruling. Record migration to `StatusBadge` + inline `statusTone(status, {…})` (the C6-clean `StockAdjustmentStatusBadge` shape) as an S4 / UI-debt item.

**F-3 — The M-2 widening turned `StockTransferDetailPage` into a "web receipt surface" in the R5 ruling's own sense, while the page renders the unmasked SENT quantity and reads five members the S2 receiver shape will not send.** `StockTransferDetailPage.tsx:38` + `:98-108` put the terminal receipt affordance on a page that renders `line.quantity` at `:225`. Plan rev 9 §3 records owner ruling **R5 option (b)**: "hide the expected/sent/remaining quantities of the SPECIFIC DOCUMENT being received only, **on mobile and the web receipt surfaces**", and baseline row **B4** repeats it; the glossary defines the receiver view as "never sent/expected/remaining, never sender-authored text or cost fields" (`plan-rev-9.md:589`). From S2, `GET /stock-transfers/{transfer}` chooses the builder **per transfer** (`plan-rev-9.md:4759`) and returns the receiver shape with `data.blind === true`, "no `transfer_cost`, no `notes`, no `initiated_by_name`" (`:5034`) — yet this page reads `transfer.transfer_cost` (`:136`), `transfer.transfer_cost_distribution` (`:139`), `transfer.initiated_by_name` (`:141`), `transfer.notes` (`:168`) and `line.quantity` (`:225`) unconditionally, against a hand-rolled type that declares all five REQUIRED (F-1), so nothing in the FE will surface the variance: `notes` will be `undefined !== null` → an empty `<dd>`, and `formatQuantity(undefined, …)` will be called. Two consequences the orchestrator needs before ruling on I-11(r2)(b): (i) adding a per-line received/remaining column or a remaining-bearing description here **deepens** the masking debt S2/S3 must undo on this exact page, and (ii) the generated `StockTransferData` does not model the receiver shape either, so S2 must ship it as a discriminated union or S4 inherits the same blind spot in typed form. *Fix directive:* record the masking obligation for `StockTransferDetailPage` in the promotion checklist's S1-disclosures block and in the S4 packet (S4 must render the sent/remaining columns behind `canSeeExpected`/`blind` before any terminal affordance on a partial); do **not** add a remaining column or a remaining total to this page in S1.

### MINOR

**F-4 — `detail.confirmComplete.description` is quantity-free and identical for `in_transit` and `partially_received`; a single cross-unit "remaining total" is not a well-defined quantity (frontend half of I-11(r2)(b)).** `locales/{en,fr}/stock-transfers.json` `detail.confirmComplete.description`, consumed at `StockTransferDetailPage.tsx:277`. Summing `quantity_remaining` across lines is meaningless on this page because each line carries its own unit precision (`getQuantityDecimals(line)` at `:225`, `:258`) and no single line defines the total's unit — and doing it in JS would need decimal-string arithmetic under rule 19 (`parseFloat`/`Number()` forbidden). *Fix directive:* make the copy **line-count** bearing, not quantity bearing — e.g. en `"{{shortLines}} of {{totalLines}} lines have not fully arrived. Confirming books the entire outstanding quantity as received at the destination; use Close to write off or return a short shipment."`, fr `"{{shortLines}} lignes sur {{totalLines}} ne sont pas entièrement arrivées. Confirmer comptabilise la totalité du reliquat comme reçu à destination ; utilisez Clôturer pour perte ou retour."`, rendered only when `status === 'partially_received'`, with `shortLines` counted from `quantity_remaining !== '0.0000'` (string compare, no arithmetic) — which requires F-1. `ar` stays aliased per the T-9 ruling.

**F-5 — `STATUS_OPTIONS` has no compile-time exhaustiveness against the generated union, so a future backend status silently drops out of the filter.** `StockTransferListPage.tsx:15-24` is a plain `readonly (StockTransferStatus | 'all')[]`; the badge's `Record<StockTransferStatus, …>` (`StockTransferStatusBadge.tsx:5`) does have it, which is exactly why M-2 caught the badge and not the filter. *Fix directive:* derive the array from an exhaustiveness-checked record — `const STATUS_FILTERS = { draft: true, in_transit: true, … } as const satisfies Record<StockTransferStatus, true>` and spread its keys — so adding a backend status breaks the build.

**F-6 — Filter order diverges from the enum lifecycle: the two `closed_*` states are listed before `completed`.** `StockTransferListPage.tsx:18-22` vs `generated.d.ts:1453` (`draft, in_transit, partially_received, completed, closed_with_writeoff, closed_returned, cancelled`). Cosmetic; a dropdown that reads draft → in transit → partially received → closed → closed → completed → cancelled is confusing. *Fix directive:* reorder to the enum order.

**F-7 — The new badge test is co-located while every other test in the feature lives in `__tests__/`, and its `ar` case must be deleted with the T-9 revert.** `components/StockTransferStatusBadge.test.tsx` vs the nine files under `features/stock-transfers/__tests__/`. Its `:19-27` case asserts `getByText('مستلم جزئيًا')`; once `lib/i18n.ts:478` is back to the whole-namespace `enStockTransfers` alias, `t('status.partially_received')` under `ar` returns `"Partially received"` and that assertion cannot pass. *Fix directive:* delete `:19-27` (and the now-unused `act`/`i18n` imports at `:2`/`:4`); optionally move the file to `__tests__/`.

**F-8 — The detail-page fixture is an untyped module-level object mutated across tests.** `__tests__/StockTransferDetailPage.test.tsx:6-47` is a bare literal (no `: StockTransfer`), and `:64` mutates `transfer.status = 'partially_received'` with a `finally` restore at `:69`, making the suite order-dependent and unable to catch the F-1 shape drift. *Fix directive:* type the fixture and build a per-test copy (`{ ...transfer, status: 'partially_received' }`) instead of mutating shared state.

**F-9 — The same page renders two raw enum values as user-facing text with no `t()` — the identical "unnameable enum" defect M-2 exists to fix, one field over.** `StockTransferDetailPage.tsx:130` `value={transfer.transfer_type}` renders `intracompany`; `:139` `value={transfer.transfer_cost_distribution}` renders `pro_rata_value`. The `en` bundle has label keys for the ROWS (`detail.summary.type`, `detail.summary.transferCostDistribution`) but none for the VALUES. Untouched lines, so pre-existing under rule 18's touch-only scope — recorded for S4, not for this fix round. *Fix directive:* S4 adds `type.*` / `distribution.*` value keys in en/fr and renders `t(\`type.${transfer.transfer_type}\`)`.

**F-10 — `allocation.batch_number ?? '—'` is a dead branch and will become an ESLint warning the moment F-1 lands, growing a ratcheted count.** `StockTransferDetailPage.tsx:248`. `product_batches.batch_number` is `string(100)` NOT NULL (`2026_01_05_150000_create_product_batches_table.php:25`) and the DTO casts `(string)` (`StockTransferLineBatchAllocationData.php:51`), so `'—'` is unreachable. `@typescript-eslint/no-unnecessary-condition` is `'warn'` (`apps/web/eslint.config.js:144`) and CI ratchets the app-wide warning COUNT against `scripts/lint-warning-baseline.json` (`@autoerp/web: 6448`, "may shrink/hold, never grow", `.github/workflows/ci.yml:2494-2499`), so +1 fails `pnpm lint:ratchet`. *Fix directive:* delete `?? '—'` in the same commit as the F-1 alias, and run `pnpm lint:ratchet` from the repo root (I did not — see Runs).

**F-11 — The FE has no `hasModule('Inventory')` counterpart to the backend group's `module:Inventory`, and `Inventory` is an upgrade extra for two verticals.** `apps/web/src/routes/index.tsx:1405,1415,1425` gate on permissions only; the backend group carries `'module:Inventory'` (`apps/api/app/Modules/Inventory/Presentation/routes.php:31`) and `Inventory` is a `compatible_extras` member for `restaurant` and `coffee_shop` (`apps/api/config/verticals.php:75,107`). So a permission-holding user in a tenant without the extra can deep-link `/inventory/stock-transfers`, render the chrome and take a 403 from the API. Note the correct mechanism: `RequirePermission moduleKey` maps to a PERMISSION bundle (`src/hooks/usePermissions.ts:69` `inventory: ['inventory.view']`, `:172`), **not** to the vertical module — the real counterpart is `hasModule('Inventory')` from `CompanyConfigContext` (`src/contexts/CompanyConfigContext.tsx:33,66,102`). S4 per plan `:811` / §10.4; disclosed at `PROMOTION-CHECKLIST-2026-09-09-t2t3.md:11`. *Fix directive:* one sharpening word in the checklist line ("no `hasModule('Inventory')` FE counterpart to the backend `module:Inventory` group gate"), and S4 wraps the three routes.

**F-12 — Baseline-honesty trap: the i18n auditor reports a FALSE burn-down and advises the exact bypass that would absorb this slice's 118 new gaps into the protected baseline.** The run prints `i18n completeness — 1 baseline entry now translated (burn-down; regenerate the baseline and re-pin to lock the gain in).` (`tools/audit-i18n-completeness.mjs:788-789`). The stale entry is the wildcard **`"ar|stock-transfers|aliased|*"`** — the only `ar|stock-transfers` row in the baseline. **Nothing was translated**: the entry went stale because `lib/i18n.ts:478` reclassified the namespace from `aliased` to wired. Anyone who follows the tool's own advice (`node scripts/lint-ratchet.mjs`-style regenerate + re-pin) would write 118 fresh `missing` rows into the protected baseline — the `--write-baseline`-to-absorb-your-own-diff pattern that is an automatic REJECT. **The implementer did NOT take it**, and I verified that independently: `git diff --stat de3007fe9..d7123fa30 -- apps/web/tools/` is **empty**, and `git hash-object apps/web/tools/i18n-completeness-baseline.json` = **`fd6dbe3952cc3daaec15dc432e6b99e007f50dd6`** = the owner-pinned protected blob mirrored at `docs/handoff/progress/enforcement-p2.progress.yaml:84`. *Fix directive:* fix the wiring (the revert below), never the pin; and consider a note in the auditor that an `aliased|*` row going stale is a reclassification, not progress.

## Exact T-9 revert (diff-level) and badge decision

**I simulated the revert in the worktree and it makes the gate GREEN**, then restored byte-identical (sha256 `f73284c9…` for `i18n.ts`, `00aa735c…` for `ar/stock-transfers.json`, `git status --short` empty). The reverted state is **byte-identical to `93b106461`** — `git diff --stat 93b106461 -- src/lib/i18n.ts src/locales/ar/stock-transfers.json` was EMPTY after my edit, so the fix round cannot get it wrong:

```
cd apps/web
git checkout 93b106461 -- src/lib/i18n.ts
git rm src/locales/ar/stock-transfers.json
```

Equivalently, by hand, exactly three edits and one deletion:

1. `src/lib/i18n.ts:53` — **delete** `import arStockTransfers from '../locales/ar/stock-transfers.json'`.
2. `src/lib/i18n.ts:475-476` — restore the comment to `// namespace, the same treatment stock-transfers gets.` (drop `// namespace. Stock transfers overrides the new receipt status labels below.`).
3. `src/lib/i18n.ts:478` — replace `'stock-transfers': { ...enStockTransfers, status: { ...enStockTransfers.status, ...arStockTransfers.status } },` with `'stock-transfers': enStockTransfers,`.
4. **Delete** `src/locales/ar/stock-transfers.json`.

**No other change to `lib/i18n.ts` is needed or permitted** — the namespace is already registered for `en` (`:219`), `fr` (`:276`) and in the `ns` array (`:497`), and `ar` keeps the whole-namespace English alias its 121 keys already had. Measured result after the revert: `I18N_BASELINE_PROTECTED_BLOB=fd6dbe39… node tools/audit-i18n-completeness.mjs` → **exit 0**, "55 namespaces … 2816 known gap(s) held at the baseline; English-aliased namespaces — ar: 21 ns / 1928 keys". No baseline, mirror or pin change.

**Also required with the revert:** delete `components/StockTransferStatusBadge.test.tsx:19-27` (the `ar` case) plus the now-unused `act` and `i18n` imports at `:2`/`:4`. The seven `it.each` English cases at `:8-16,29-32` are unaffected. Arabic coverage of the `stock-transfers` namespace (121 keys) is not S1 scope — record it as a ticket/checklist follow-up.

**Badge decision (T-14(r2)): RESTORE THE ATOM.** Revert `StockTransferStatusBadge.tsx:2` and `:21` to `93b106461` (`import { Badge, type BadgeVariant } from '@/components/atoms/Badge'`; `return <Badge variant={statusVariant[status]}>{t(\`status.${status}\`)}</Badge>`), keep the map as `Record<StockTransferStatus, BadgeVariant>` and add exactly the three ruled rows:

```
  partially_received: 'warning',
  closed_with_writeoff: 'danger',
  closed_returned: 'default',
```

Pixel-identical to what shipped (F-2 conservation), exactly the "three `statusVariant` rows" §0AA ruled, zero new design-system violations, zero new ESLint warnings. Migration to the canonical `StatusBadge` + inline `statusTone(status, {…})` is recorded for S4 because it recolours the four pre-existing pills.

## Runs

One process at a time, Node 20.19.4, from `apps/web` in the lane worktree.

| Command | Result |
|---|---|
| `node -v` | `v20.19.4` |
| `pnpm typecheck` (`tsc --noEmit`) | **exit 0**, no output |
| `pnpm audit:keys` | **exit 0** — "Gate C … without an approved tenant scope: **0**"; "0 acknowledged, 0 new, 0 stale" |
| `pnpm audit:design-system` | **exit 0** — "C1-C6 violations: **802**"; "802 acknowledged, **0 new**, 0 stale". See F-2: this green does not cover the new status→colour map |
| `pnpm audit:quantity` | **exit 0** — "raw quantity display sites: **0** total (0 baselined, 0 new, 0 stale)" |
| `I18N_BASELINE_PROTECTED_BLOB=fd6dbe39… node tools/audit-i18n-completeness.mjs` | **exit 1 — exactly 118 NEW gaps**, all `ar\|stock-transfers\|missing` (counted: `grep -c` → 118), plus the false burn-down of F-12. **Confirms tenancy T-9(r2)** |
| same, after the simulated T-9 revert | **exit 0** — "2816 known gap(s) held at the baseline". Tree restored byte-identical afterwards |
| `git hash-object apps/web/tools/i18n-completeness-baseline.json` | `fd6dbe3952cc3daaec15dc432e6b99e007f50dd6` = the owner-pinned protected blob (`enforcement-p2.progress.yaml:84`). `git diff de3007fe9..d7123fa30 -- apps/web/tools/` **empty** — no baseline bypass |
| Recomputed `permissionsMap.generated.ts` self-hash (`ExportFrontendPermissionsMap.php:84-90` algorithm) | **MATCH** `8c56d87a7a3765ded57192e845e147815d13aacc540132faff35692e46fb48a8` — verified artisan output, no hand edit |
| `npx eslint` on the 8 touched web files | **exit 0 — 0 errors, 1 warning**: `StockTransferListPage.tsx:121` `local/no-hardcoded-entity-route`. Line 121 is **untouched** by this slice (the diff touches only `:19-21`), so it is pre-existing |
| C6 detector probe (`STATUS_RE` from `audit-design-system.mjs:75-80`, run in node) | shipped `statusVariant: Record<…, string>` → **MISS**; pre-fix `Record<…, BadgeVariant>` → **MISS**; `Record<…, StatusTone>` → **HIT (P1,P3,P4)**; `statusTone(status, {…})` → **MISS**. Corroborated by `audit-design-system-baseline.json:778-780` |
| `pnpm vitest run src/features/stock-transfers/components/StockTransferStatusBadge.test.tsx` | **NOT RUN — skipped on the brief's swap rule.** `vm.swapusage used = 11 989 MB` before and `11 769 MB` after the review (threshold 11 GB). Zero `node (vitest` worker pools before and after |
| `pnpm vitest run src/features/stock-transfers/__tests__/StockTransferDetailPage.test.tsx` | **NOT RUN** — same reason. Note: the inventory reviewer re-ran this file (3 passed, incl. `partially_received → Confirm receipt` at `:63-72`) and I did not re-derive it |
| `src/features/stock-transfers/__tests__/StockTransferListPage.test.tsx` | Exists but was **NOT touched** by the slice and asserts **nothing** about the status filter (`grep -n "status\|option\|toHaveLength"` → one hit, the fixture's `status: 'in_transit'` at `:42`) → the three new filter options have zero test coverage (F-5) |
| `pnpm lint:ratchet` (repo root) | **NOT RUN** — full-repo ESLint under 11.7 GB swap. The lane's `frontend-lint` job is red at the i18n step regardless (T-9). Must be run in the fix round because of F-10 (baseline `@autoerp/web: 6448` warnings, never-grow) |

## Scope census

Fifteen web/shared files. §0AA's ruled web set: `stock-transfers/{types/index.ts, components/StockTransferStatusBadge.tsx, pages/StockTransferDetailPage.tsx, pages/StockTransferListPage.tsx}`, `lib/i18n.ts`, three `stock-transfers` i18n keys en/fr/ar.

| File | §0AA classification | Verdict |
|---|---|---|
| `features/stock-transfers/types/index.ts` | RULED (status union → generated) | **in scope**; residual family → **F-1** |
| `features/stock-transfers/components/StockTransferStatusBadge.tsx` | RULED ("three `statusVariant` rows") | **EXCEEDS** the ruling — atom→span rewrite → **F-2** |
| `features/stock-transfers/components/StockTransferStatusBadge.test.tsx` | ADDITION, not named | **accepted** (a test for a ruled change); placement + `ar` case → **F-7** |
| `features/stock-transfers/pages/StockTransferDetailPage.tsx` | RULED (`canComplete`) | **in scope**, exactly one line (`:38`); consequences → **F-3**, **F-4** |
| `features/stock-transfers/pages/StockTransferListPage.tsx` | not named in §0AA; named in the r1 M-2 wording ("all filters") | **accepted in scope** — a status the badge can name but the list cannot filter is half a fix; nits **F-5**, **F-6** |
| `features/stock-transfers/__tests__/StockTransferDetailPage.test.tsx` | ADDITION, not named | **accepted** (covers the ruled `canComplete`); hygiene → **F-8** |
| `lib/i18n.ts` | RULED registration only | **EXCEEDS** — the `ar` spread reclassified the namespace → tenancy **T-9(r2) BLOCKER**; revert above |
| `locales/en/stock-transfers.json` (+3) | RULED | **in scope** |
| `locales/fr/stock-transfers.json` (+3) | RULED | **in scope** |
| `locales/ar/stock-transfers.json` (new, 3 keys) | RULED **by the letter** ("en/fr/ar") | the ruling itself did not model the aliased namespace; **must be deleted** per the T-9 ruling |
| `locales/en/inventory.json` (+1 `movementSource.stock_transfer_receipt`) | outside the web set | **justified by necessity** — S1 adds a movement source; without the key the movement feed shows a raw enum |
| `locales/fr/inventory.json` (+1) | outside | **justified**; correct French (`Réception de transfert`) |
| `locales/ar/inventory.json` (+1) | outside | **justified**; `ar/inventory.json` is a real wired bundle, so this adds no i18n gap |
| `hooks/permissionsMap.generated.ts` | generated artefact | **verified artisan output** (self-hash recomputed, MATCH); content delta = the two S1 permissions only |
| `packages/shared/types/generated.d.ts` | generated artefact | **verified** (byte-identical regeneration proved by tenancy); +`TransferStatus` 3 members, +3 DTO families — the cause of **F-1** |

No web route file, no permission map hand edit, no `tools/` or baseline change, no new picker, no raw `<table>`, no raw form control, no `parseFloat`/`Number()` on a quantity, no query key without `tenantScopedKey`, no `StickyFormFooter`/`PageHeader` regression, no `disabled`-but-dead control, no hardcoded brand string.

## Tree state after review

No file was edited, committed, merged or pushed. The T-9 revert simulation was applied in place and restored byte-identical: `apps/web/src/lib/i18n.ts` sha256 `f73284c96a7487824dc86fadbcd64a45aa4dfef1c8dd5c087cdc97efaf0f9953`, `apps/web/src/locales/ar/stock-transfers.json` sha256 `00aa735c00dc0c6bc30b5a4837ed2c85bb606a92a617a03d31aeeadb14475be3`; `git status --short` **empty**; `git rev-parse HEAD` = `d7123fa3021299ccc911ea06482fc2a97c9fe88e`. No `playwright-report` or `test-results` directory was created (no Playwright run). Zero `node (vitest` worker pools left behind. Scratch copies and logs live only in the session scratchpad.

## Summary for the orchestrator

**What must change before merge, in one fix round:** (1) the T-9 revert exactly as specified, plus deletion of the badge test's `ar` case — makes `frontend-lint`'s i18n step green with no pin, baseline or mirror change; (2) restore the `Badge` atom with the three ruled variant rows (pixel-identical, zero new violations); (3) alias the three DTO-shadowing FE interfaces to the generated types and patch the three consumers, deleting the dead `?? '—'` at `StockTransferDetailPage.tsx:248` in the same commit — this also unblocks the ruled I-11(r2)(b) copy, and needs §0AA's file set widened by two test files; (4) make `detail.confirmComplete.description` line-count bearing rather than quantity bearing (F-4), and record the `StockTransferDetailPage` masking obligation for S4 rather than adding a remaining column now (F-3); (5) run `pnpm lint:ratchet` and the two Vitest files to exit 0 and record them. The remaining MINORs (F-5, F-6, F-7 placement, F-8, F-9, F-11, F-12) are cheap or S4-assigned and none of them gate.

**What is genuinely right in this fix round:** the generated union replaced the hand-rolled one exactly as convention 11 asks, all seven statuses are nameable and filterable through `t()` templates, `canComplete` matches `TransferStatus::canBeCompleted()`, no receive/close/reconciliation UI leaked in, the web router and permission map were not hand-touched, no float or `parseFloat` touched a quantity, every query key is tenant-scoped, and **no baseline, pin or detector was weakened to make anything green** — the implementer disclosed the red i18n gate honestly instead of absorbing it, which is the single most important thing they could have got right here.

Orchestrator: session_01AcU81as26G2apodoimmyq7 (reviewer = Claude Opus `frontend-conventions-reviewer` agent).

## Orchestrator rulings (session_01AcU81as26G2apodoimmyq7, 2026-09-10)
- **F-1: REPLACE NOW** — the three DTO-shadowing interfaces alias the generated `StockTransferData` / `StockTransferLineData` / `StockTransferLineBatchAllocationData`; the ruled file set is widened by `__tests__/StockTransferListPage.test.tsx` and `__tests__/StockTransferDetailPage.batchAllocations.test.tsx` (fixtures) and the one-line dead-fallback deletion at `StockTransferDetailPage.tsx:248` (F-10). The two redundant unions (`StockTransferType`, `TransferCostDistribution`) → S4.
- **F-2 / T-14(r2): RESTORE THE `Badge` ATOM** with exactly the three ruled rows (`partially_received: 'warning'`, `closed_with_writeoff: 'danger'`, `closed_returned: 'default'`). `StatusBadge` migration → S4 UI-debt item.
- **F-3: RECORD** the `StockTransferDetailPage` masking obligation (R5) in the promotion checklist's S1 disclosures and in the S4 packet; NO remaining column and NO remaining total in S1.
- **F-4 supersedes the inventory I-11(r2)(b) wording**: the confirm copy becomes LINE-COUNT bearing (short lines / total lines) rendered only for `partially_received`, computed by string comparison against `'0.0000'` (no arithmetic); en/fr only. I-11(r2)(a) disclosure and (c) backend test stand.
- **T-9(r2) revert** exactly as specified (byte-identical to `93b106461` for `lib/i18n.ts`; delete `ar/stock-transfers.json`; delete the badge test's `ar` case). Arabic coverage of the namespace → ticket.
- **F-5, F-6, F-7, F-8, F-10, F-11, F-12: FIX** in the round (F-11 = one sharpening word; F-12 = the auditor note is optional, the pin is never touched). **F-9: S4.**
- `pnpm lint:ratchet` and the two Vitest files must be run to exit 0 in the fix round (laptop permitting) and recorded.
