# Focused adversarial re-review - POS Phase 1 Foundation Spec v7
**Reviewer:** Codex
**Review date:** 2026-05-14
**Spec reviewed:** `/Users/houssamr/Projects/syneriva/apps/erp/docs/superpowers/specs/2026-05-14-pos-phase1-foundation-spec-v7.md`
**Verdict:** APPROVE
**Total findings:** 0 BLOCKER, 0 P1, 0 P2, 0 P3

## Executive summary
- The v6 BLOCKER is resolved. v7 explicitly dispositions the POS order-close server-authored `SALE_RECEIPT` path in §14, §14.2, §14.3, §17.5, and §18, and the code confirms the path is `apps/web` plus backend only.
- The new §14.3 chokepoint rule is buildable against the current code. Grep finds the production callers of `ReceiptCreationService::createReceipt()` and `ReceiptFinalizationService::finalize()` that v7 lists; no additional production caller was found.
- The direct `Receipt` construction scan found no undispositioned production server-side `SALE_RECEIPT` path. `ReceiptSyncService` directly creates a receipt row before finalization, but its production non-training path reaches `finalize()` and `/pos/receipts/sync` is retired by §14.1; that does not create a new finding.
- Earlier resolved v3 Codex, v4 Opus, and v5 Codex issues remain resolved. I did not find a regression in the bounded-modules seam, the Tauri online checkout fix, the receipt-sync retirement surface, or the void/return carve-out.

## v6-finding resolution
| v6 finding | Verdict | Evidence |
|---|---|---|
| BLOCKER - POS order close still server-authors `SALE_RECEIPT` through `ReceiptCreationService`, but v6 does not disposition it | RESOLVED | v7 adds the missing disposition row for `POST /pos/orders/{id}/close` and says the order-close-to-receipt step is disabled for Phase 1 (`spec v7:603-605,627-653`). §14.3 enumerates the backend path as `OrderToReceiptService::convertToReceipt()` -> `ReceiptCreationService::createReceipt()` and assigns disposition (b), disabled/deferred with web-POS order-management parity (`spec v7:655-678`). §17.5 adds a proving test that order-close no longer reaches `ReceiptCreationService::createReceipt()` after the rebuild (`spec v7:758-765`). Code confirms the old path: `routes_orders.php:27-28`, `OrderController.php:348-358`, `OrderManagementService.php:419-438`, and `OrderToReceiptService.php:11-16,68-74`. This satisfies SoT D1 and D8, which require device authority and one fiscal pattern (`source-of-truth-v3.md:254,261`). |

## §14.3 chokepoint-enumeration exhaustiveness check
| Check | Verdict | Grep evidence |
|---|---|---|
| Production callers of `ReceiptCreationService::createReceipt()` | Exhaustive. | `rg '->createReceipt\\(|createReceipt\\(' apps/api/app apps/api/routes` finds the method definition plus three production callers: `ReceiptController::store()` (`ReceiptController.php:284-315`), `OrderToReceiptService::convertToReceipt()` (`OrderToReceiptService.php:68-74`), and `ExchangeService` sale-half creation (`ExchangeService.php:219-226`). v7 lists and dispositions all three (`spec v7:671-673`). Test helper methods named `createReceipt()` are not production callers. |
| Production callers of `ReceiptFinalizationService::finalize()` | Exhaustive for POS receipt sealing. | `rg '->finalize\\(|finalize\\(' apps/api/app apps/api/routes` finds `ExchangeService.php:286,289`, `ReceiptPaymentService.php:402`, `ReceiptReturnService.php:351`, and `ReceiptSyncService.php:623` for POS receipts. It also finds unrelated inventory finalization (`InventoryCountingService.php:539`), which is not a receipt seal. v7 lists and dispositions all POS receipt callers (`spec v7:673-676`). |
| Backend order-close caller enumeration | Exhaustive for production code. | The production route and caller chain are `routes_orders.php:28` -> `OrderController::close()` (`OrderController.php:348-358`) -> `OrderManagementService::closeOrder()` (`OrderManagementService.php:419-438`) -> `OrderToReceiptService::convertToReceipt()` (`OrderToReceiptService.php:68-74`). v7's table and checklist name this chain (`spec v7:605,640,646,672,780`). |
| `apps/web`-only order-close claim | Confirmed. | The browser caller is `apps/web/src/features/pos/api/orderApi.ts:201-202`, reached through `useOrders.ts:196-202`, `OrderPanel.tsx:211-215`, and `CloseOrderButton.tsx:10-24`. A repo grep for `/pos/orders/{id}/close` / `closeOrder(` found no production `apps/pos` caller; `apps/pos` has no order-close endpoint call. Backend tests still exercise the route (`KitchenDisplayTest.php:265`, `PosStabilizationTenantIsolationTest.php:1859`), which are not production callers and are covered by the Phase 1 proving-test requirement. |
| Direct `Receipt` construction bypass scan | No undispositioned production bypass found. | Direct production construction is limited to `ReceiptCreationService.php:557`, `ReceiptSyncService.php:478`, and `ReceiptReturnService.php:574`. `ReceiptCreationService` is the draft chokepoint named by §14.3. `ReceiptSyncService` directly constructs a sale receipt but production non-training payloads then call `ReceiptFinalizationService::finalize()` (`ReceiptSyncService.php:618-623`) and the entire `/pos/receipts/sync` path is retired by §14.1. `ReceiptReturnService` is the knowingly-retained void/return carve-out (`ReceiptReturnService.php:344-351`; spec v7:649,676). No production `Receipt::create()` or `DB::table('pos_receipts')->insert` authoring path was found under `apps/api/app`. |

## Regression check
| Earlier resolved issue / requirement | Status | Evidence |
|---|---|---|
| Codex v5 BLOCKER - Tauri online checkout server-authored `SALE_RECEIPT` | Still resolved. | v7 keeps the connectivity-independent Tauri checkout disposition: `executeCheckout()` always authors locally post-rebuild and the `onlineCheckout()` branch is discarded (`spec v7:586-588,642,761`). Current code confirms the old branch still exists and is correctly targeted: `onlineCheckout()` calls `createReceipt()` and `processReceiptPayments()` (`offlineCheckoutService.ts:83-115`), and `executeCheckout()` chooses it when online (`offlineCheckoutService.ts:178-199`). |
| Codex v5 P3 - receipt-sync feature-suite cleanup list | Still resolved. | v7 still names `OfflineV3CutoverSyncTest.php` and classifies `PosStabilizationTenantIsolationTest.php` as a broader suite with receipt-sync assertions only to migrate (`spec v7:621-623,760`). Grep confirms the endpoint suites include `SyncReceiptsTest.php`, `SyncReceiptsRequestTest.php`, `ReceiptSyncServiceV3Test.php`, `ReceiptSyncServiceTrainingModeTest.php`, `OfflineV3CutoverSyncTest.php`, and receipt-sync assertions in `PosStabilizationTenantIsolationTest.php:1366,1406`. |
| v3 Codex BLOCKER - bounded-modules seam / Treasury bridge | Still resolved. | SoT §13.6/D16 permits inbound mirrored reference data but forbids outbound operational dependency on Treasury/accounting/B2B from the fiscal engine (`source-of-truth-v3.md:242-246,269`). v7 keeps POS-core projection always-on and Treasury bridge gated (`spec v7:591-592,742-749,778-779`). Code confirms the module token mechanics remain strict and PascalCase: `CompanyConfig::hasModule()` uses `in_array(..., true)` (`CompanyConfig.php:50-52`) and all `Vertical::defaultModules()` lists include `'Treasury'` with PascalCase (`Vertical.php:124-136`). |
| Opus v4 P1 - canonical `'Treasury'` module token casing | Still resolved. | v7 keeps the test-lock for `isActive('Treasury', ...)` and `TreasuryReceiptBridge::requiresModule() === 'Treasury'` (`spec v7:742-745`). Code confirms strict compare and PascalCase defaults (`CompanyConfig.php:50-52`; `Vertical.php:124-136`). |
| Opus v4 P2 - void/return carve-out | Still resolved. | v7 explicitly scopes the no-coexistence claim to new-sale `SALE_RECEIPT` and retains `void` / `processReturn` for Phase 1 (`spec v7:635-649`). Backend routes are distinct from new-sale authoring (`routes.php:100-101`), and Tauri online void/return calls remain in `VoidReturnModal.tsx:143-166`. |
| Opus v4 P3 - `ReceiptPaymentService` split | Still resolved. | v7 still splits POS-core `ReceiptPayment` creation from Treasury `Payment` + GL (`spec v7:591-592`). Code confirms Treasury `Payment::create()` and GL posting at `ReceiptPaymentService.php:251-269`, while POS `ReceiptPayment::create()` is at `ReceiptPaymentService.php:297-303`. |

## Codebase-claim verification table
| v7 codebase claim | Verdict | Evidence |
|---|---|---|
| `offlineCheckoutService.ts` currently has an online server-authoring branch. | Confirmed. | `onlineCheckout()` calls `createReceipt()` (`offlineCheckoutService.ts:83-88`) and `processReceiptPayments()` (`offlineCheckoutService.ts:113-115`); `executeCheckout()` chooses `onlineCheckout()` when online (`offlineCheckoutService.ts:178-199`). |
| `apps/pos/src/api/receiptApi.ts` write methods post to the server-authoring new-sale routes. | Confirmed. | `createReceipt()` posts to `/pos/receipts` (`receiptApi.ts:14-17`); `processReceiptPayments()` posts to `/pos/receipts/{id}/payments` (`receiptApi.ts:20-27`). |
| Browser web POS new-sale write paths are still live today and are targeted by §14.2. | Confirmed. | `POSTransactions.tsx` calls `createReceipt` / `processReceiptPayments` (`POSTransactions.tsx:315-318,368-396`); web `receiptApi.ts` posts to `/pos/receipts` and `/pos/receipts/{id}/payments` (`apps/web/src/features/pos/api/receiptApi.ts:73-77,121-128`). |
| Backend new-sale routes exist and point to old server authoring. | Confirmed. | Routes are `routes.php:98,102`; controller paths are `ReceiptController::store()` -> `ReceiptCreationService::createReceipt()` (`ReceiptController.php:284-315`) and `ReceiptController::storePayments()` -> `ReceiptPaymentService::processReceiptPayments()` (`ReceiptController.php:499-515`). |
| POS order-close route exists and server-authors through `ReceiptCreationService`. | Confirmed and now dispositioned. | Route/controller/service chain is `routes_orders.php:28`, `OrderController.php:348-358`, `OrderManagementService.php:419-438`, and `OrderToReceiptService.php:11-16,68-74`. v7 dispositions it in §14.2 and §14.3 (`spec v7:640,646,672`). |
| No Tauri caller of `POST /pos/orders/{id}/close` exists. | Confirmed. | The production caller grep found the browser web chain (`orderApi.ts:201-202`, `useOrders.ts:196-202`, `OrderPanel.tsx:211-215`, `CloseOrderButton.tsx:10-24`) and backend tests, but no `apps/pos` production caller. |
| `/pos/receipts/sync` retirement surface remains correctly named. | Confirmed. | Client old sync call is `syncService.ts:334`; backend route/controller are `routes.php:85` and `SyncController.php:49-68`; exception import/throw are `ReceiptSyncService.php:23,629`; endpoint test suites and broader suite references are present as listed in §14.1 (`spec v7:609-625`). |
| `ExchangeService` has no live route/caller found, but remains on the checklist. | Confirmed with caveat. | `ExchangeService` creates and seals a sale half (`ExchangeService.php:219-226,251-257,285-289`). Grep found no controller/route invoking `ExchangeService::processExchange()` in production code. v7 correctly requires re-grep and either disposition or removal/inert recording during implementation (`spec v7:673`). |

## New findings
No new findings.

## Out-of-scope note
- The old receipt code has training-mode branches that can set sentinel `fiscal_hash` values without entering the production chain (`ReceiptCreationService.php:605-621`; `ReceiptSyncService.php:618-623`). I did not count this as a v7 issue because Phase 1 retires the server-side new-sale authoring path and `/pos/receipts/sync`, and the locked review scope is production `SALE_RECEIPT` server authoring. The writing plan should still ensure the §17.5 grep or equivalent test does not accidentally leave a training-mode server-authoring exception for rebuilt `SALE_RECEIPT`.
