# Task 01 Opus Review

Verdict: APPROVE

Scope: amended commit `525c9f92` (`feat(fiscal): preflight verification gate command + minimal FiscalServiceProvider`) reviewed against parent `62f25087`. Diff is 5 files, +387/-0: `PreflightFiscalGateCommand.php`, `FiscalServiceProvider.php`, `bootstrap/providers.php`, `docs/sessions/2026-05-14-fiscal-preflight-signoff.md`, `tests/Feature/Fiscal/PreflightFiscalGateCommandTest.php`.

This is a re-review of the same Task 1 after Codex amended the commit to address the three findings in the prior Opus review (P2 test isolation, P3 TDD evidence, P3 Web-POS clarification). All three are closed. The production code faithfully implements Plan Task 1 and spec §2; the command is correctly wired and discoverable; the test suite is now real, per-source, and meaningful; the sign-off artifact remains honest and does not fabricate owner sign-off. The two findings below are residual evidence/isolation notes, not logic defects — they do not block.

## Findings

### P3 — The four per-source negative tests have no separately recorded red run
`apps/api/docs/sessions/2026-05-14-fiscal-preflight-signoff.md:25-47`. The recorded red run (`Tests: 2 ... CommandNotFoundException`) and first green run (`OK (2 tests)`) cover only the two original plan tests. The four tests added to satisfy the prior P2 finding — which assert the new per-source output lines (`pos_z_reports: 1`, `pos_receipt_prints: 1`, `pos_terminals_with_chain_state: 1`) produced by the new `reportServerFindings()` method (`PreflightFiscalGateCommand.php:65-75`) — would have failed against the pre-amendment command, but the doc only records them as a post-edit green run (`OK (6 tests, 19 assertions)`), not red-then-green. Because this is a single amended (squashed) commit the discrete red evidence for those four is unrecoverable; the structural reality is sound (each new test asserts a specific output line that only the new code emits, so a regression deleting any sub-check is caught), so this is a residual-evidence note, not a defect. Fix (optional): on the next TDD cycle, capture red runs per added test rather than only a final green.

### P3 — `test_gate_fails_when_receipt_prints_present` is not single-surface isolated
`apps/api/tests/Feature/Fiscal/PreflightFiscalGateCommandTest.php:84-120`. The test creates a `Receipt` (a hard FK dependency of `pos_receipt_prints.receipt_id`) before inserting the print row, so both `pos_receipts` and `pos_receipt_prints` are non-empty when the gate runs. Unlike the `pos_z_reports` and terminal-chain-state tests (which are cleanly single-surface), this one cannot fully isolate the print surface. It is still meaningful: the assertion `expectsOutputToContain('pos_receipt_prints: 1')` only passes if the `pos_receipt_prints` sub-check runs and emits its line, so a regression in that specific sub-check is still caught. Acceptable as-is given the FK constraint; noted only so the partial overlap is on record. No change required.

## Verification Notes

Commands/files inspected:
- `git show 525c9f92` (full diff) + `git log --oneline` around parent `62f25087`. Diff is purely additive (+387/-0), no modifications to existing logic — no regression surface.
- **Prior review** — `docs/superpowers/reviews/2026-05-14-phase1-task-01-opus-review.md` (verdict APPROVE-WITH-MINOR-EDITS on the pre-amendment commit `9204d7ae`); its three findings (P2 test isolation, P3 TDD evidence, P3 Web-POS scope) are the closure target of this re-review.
- **P2 closed** — the command now emits per-source findings via `reportServerFindings()` (`PreflightFiscalGateCommand.php:65-75`), and the test file expanded from 2 to 6 tests. `test_gate_fails_when_pos_receipts_present` now pins the fixture terminal to `current_sequence => 0` (no `last_hash`), so the receipt row is the sole non-clear surface, and asserts `SERVER SURFACE: NON-EMPTY` + `pos_receipts: 1`. New isolated tests cover `pos_z_reports` (raw `DB::table` insert into `pos_shifts` + `pos_z_reports`, terminal at `current_sequence => 0` — cleanly single-surface), terminal `current_sequence > 0`, and terminal `last_hash` non-null — each asserting exit `1` + the specific `NON-EMPTY`/source line via `assertGateFailsWithNonEmptyServerSurface()`. The prior "would stay green if the receipt check were deleted" defect is gone.
- **P3 TDD evidence closed** — `2026-05-14-fiscal-preflight-signoff.md:25-47` now records the PHPUnit red run (`Tests: 2, Errors: 2`, `CommandNotFoundException: The command "fiscal:preflight-gate" does not exist.`) and green run (`OK (2 tests)`), plus the post-P2-edit green run (`OK (6 tests, 19 assertions)`). The red run genuinely exercises the registration path (command-not-found ⇒ provider/command absent). See P3 finding above for the residual gap on the four added tests.
- **P3 Web-POS clarification closed** — `2026-05-14-fiscal-preflight-signoff.md:73` adds: "route detection is conservative. It proves the web-POS receipt-creation route is registered; it does not by itself prove active web-terminal usage." This matches spec §2's framing (the §14.2 disposition is owner-decided) and prevents the owner from reading the command's "live receipt-creation path detected" line as a usage finding.
- **Plan/spec fidelity** — Task 1 of `plans/2026-05-14-pos-phase1-fiscal-event-engine.md` (lines 86-170) specifies exactly these five files, a minimal `FiscalServiceProvider` owning only `fiscal:preflight-gate`, the four server sub-checks (`pos_receipts`, `pos_z_reports`, `pos_terminals` chain state via `last_hash IS NOT NULL OR current_sequence > 0`, receipt prints), device surface as "requires manual inventory", and web-POS via route registration. Implementation matches all of it. Spec §2 (lines 78-88) three-surface gate + written owner sign-off is honored; the §14.2 cross-reference in the command output and sign-off doc is correct. `~/Downloads/fiscal_chain_architecture_strategy.md` and `~/Downloads/pos_printable_documents_architecture.md` describe the device-authored chain and printable-document model this gate exists to protect; nothing in Task 1 touches them — consistent.
- **Server-surface table/column names** — confirmed against migrations in `apps/api/database/migrations/`: `pos_receipts` (`2026_01_08_190637`), `pos_z_reports` (`2026_01_08_190644`), `pos_receipt_prints` (`2026_03_14_100000`), `pos_terminals` with `current_sequence integer default 0` and `last_hash char(64) nullable` (`2026_01_08_190429:42,44`). All four command strings match exactly. `terminalChainStateCount()`'s `whereNotNull('last_hash')->orWhere('current_sequence','>',0)` ⇒ `WHERE last_hash IS NOT NULL OR current_sequence > 0` — no SQL-precedence bug (two OR'd predicates, no preceding `where`).
- **Test fixture inserts validated against schema** — `pos_z_reports` raw insert columns (`id, terminal_id, shift_id, z_number, fiscal_hash, previous_z_hash, report_data, generated_by, generated_at`) match the migration exactly (table has no `created_at/updated_at`, none inserted). `pos_shifts` insert columns match (table has `timestamps()`, both supplied). `pos_receipt_prints` insert columns match the migration exactly. Factories used by the tests (`Receipt`, `Terminal`, `Company`, `Location`, `Tenant`, `User`) all exist in `apps/api/database/factories/`. `ReceiptFactory` defaults `terminal_id => Terminal::factory()` only when not supplied — every test supplies it explicitly, so the pinned `current_sequence => 0` terminal is the one used.
- **Provider wiring** — `FiscalServiceProvider::boot()` registers `PreflightFiscalGateCommand` inside `runningInConsole()` via `$this->commands([...])`, mirroring `POSServiceProvider::boot()` (`app/Modules/POS/Providers/POSServiceProvider.php:39-43`). `bootstrap/providers.php` adds the `use` import (line 15, alphabetically placed) and `FiscalServiceProvider::class` to the returned array (line 74, before `POSServiceProvider`). Command is discoverable; this satisfies the plan's incremental-provider-wiring convention (Task 1 owns the `fiscal:preflight-gate` registration, minimal provider grows in later tasks).
- **Module boundary** — the command imports only Laravel facades (`DB`, `Route`, `Schema`) + `Throwable`; it references POS tables as raw query-builder strings, not via POS domain models. Clean (CLAUDE.md rule 6). The test cross-imports POS/Company/Identity/Tenant domain models — conventional and acceptable for feature tests.
- **Tenant-context classification** — the `@cross-tenant-by-design Audits all POS fiscal surfaces ...` class annotation has a non-empty justification, satisfying `tests/Architecture/ConsoleCommandTenantContextTest.php`'s grammar check (regex `@cross-tenant-by-design\b[ \t]*([^\r\n]*)`, justification must be non-empty). Correctly applied: the command intentionally counts rows across all tenants and queries the global route table.
- **Fail-closed behavior** — `handle()`'s `catch (Throwable)` prints all three surface lines, the error, and returns `self::FAILURE`; `webPosPathLive()` is DB-independent so it still runs in the catch path. Consistent with the sign-off doc's recorded local run (failed closed on missing local `autoerp` PostgreSQL role).
- **Sign-off artifact** — `docs/sessions/2026-05-14-fiscal-preflight-signoff.md` does **not** fabricate owner sign-off: "Owner sign-off is not provided by this implementation task", all Server/Device/Web-POS result fields and `Owner:` / `Signed at:` left blank, local run recorded honestly as "failed closed", and a closing Gate statement that schema-destructive work "remains blocked until written owner sign-off covers server, device, and web-POS surfaces." Matches spec §2 and Plan Task 1 Step 5. File correctly lives under `docs/sessions/` per CLAUDE.md rule 15.
- **Strict typing / style** — all three new PHP files declare `strict_types=1`, classes `final`, all methods return-typed, array shapes documented, no `mixed`. Consistent with project rules and PHPStan level 8.

Residual risk: low. (1) The four per-source negative tests lack discrete recorded red runs (P3 above) — mitigated by their structural reality and the recorded command-existence red→green. (2) The receipt-prints test is not single-surface isolated due to an unavoidable FK dependency (P3 above) — mitigated by the per-source line assertion. Neither weakens the gate's ability to authorize the schema-destructive tasks (Plan Tasks 7–13, 28–30) it guards. The real-environment execution and written owner sign-off remain correctly deferred to the owner, as Task 1 Step 5 and spec §2 require.
