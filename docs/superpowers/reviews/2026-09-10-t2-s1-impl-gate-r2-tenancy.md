# T-2 S1 receipt spine — implementation gate r2 — tenancy-authz-reviewer

Reviewed: `lane/t2-receipt-spine` @ `d7123fa30` (fix-round diff `93b106461..d7123fa30`; slice `de3007fe9..d7123fa30`), plan rev 9, handback Fix round 1
Date: 2026-09-10
Reviewer: Claude Opus `tenancy-authz-reviewer`, read-only. PostgreSQL runner owner for this lane (one file per invocation, serially, never the full suite). All `path:line` refs are at `d7123fa30` under `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/`. Never diffed against `dev`.

## Verdict
VERDICT: CHANGES-REQUIRED
BLOCKER=1 MAJOR=2 MINOR=5

**All eight gate-r1 tenancy items (T-1..T-8) are closed and independently re-verified.** The blocker is new: the M-2 option-(a) web widening turned the i18n completeness ratchet — a discrete `frontend-lint` CI step inside `all-checks-pass` — RED at this HEAD, and the fix round left it red pending an owner scope ruling.

## Gate r1 closure table (T-1..T-8)

| r1 item | Claimed resolution | Status | Evidence |
|---|---|---|---|
| **T-1** BLOCKER — comment block between `php artisan test` and its `\` continuation dropped `--filter` | Fixed; comment block precedes the continued command; bash -n + PATH-stub argv replay exit 0 | **VERIFIED (closed)** | `.github/workflows/ci.yml:1141-1143` are the three T-2/T-3 comment lines; `:1144` is `php artisan test -c phpunit-pgsql.xml \`; `:1145` is `--filter='/\\(…)::/'` — immediately following, nothing between. Reviewer's own proof (not the handback's): extracted the whole `run:` body (`ci.yml:1048`→dedent, 131 lines) → `bash -n` exit 0 → replayed under `bash -eo pipefail` with a stub `php` (the only `php` on PATH) → **exit 0**, three `php` invocations, and the first echoes argv `<artisan> <test> <-c> <phpunit-pgsql.xml> <--filter=…>`. `--filter=` **is** in argv (was absent at `c30dcdf9c`). All fourteen S1 classes plus `TenantOnlyUniqueOnCatalogueTablesRatchetTest`, `StockTransferEdgeCasesTest`, `StockTransferCompleteConcurrencyPostgresTest`, `StockTransferIdempotencyCollisionPostgresTest` present in the alternation (18/18 checked). `php tools/feature-lane-manifest-check.php` → exit 0 |
| **T-2** BLOCKER — three receipt tables unclassified → ratchet RED on PG | Fixed; three parent-scoped exclusions, no waiver, no ceiling change; PG 3/137 | **VERIFIED (closed)** | `apps/api/tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:168-170` — the three entries, in the sibling style, with the parent-scoped reasons I specified; the whole file diff is `+3` lines and nothing else. `LEGACY_ENTRY_CEILING = 11` (`:191`) and `WAIVED_ENTRY_CEILING = 9` (`:197`) untouched; no `waiver` key added. Reviewer PG re-run: **OK (3 tests, 137 assertions), exit 0** — was `Failures: 1` at `c30dcdf9c` |
| **T-3** MAJOR — closed direction of the widened read gate + reconciliation gate unpinned | Fixed | **VERIFIED (closed)** | `TransferReceiptAuthorityGateTest.php:24` `test_inventory_view_alone_cannot_read_transfers` → `inventory.view` alone gets **403 on `GET /stock-transfers` AND `GET /{id}`**; `:31` `test_complete_alone_cannot_read_reconciliation` → a `complete`-only actor gets **403 on `/{id}/reconciliation`**. Exactly the discrimination §7.6 relies on. PG: **OK (8 tests, 19 assertions)** |
| **T-4** MINOR — FE read guard not widened; no `moduleKey` | Disclosed | **VERIFIED (closed)** | `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md:11` — "the web route guard still requires `inventory.transfers.view` and has no Inventory module gate; complete-only and reconcile-only actors cannot reach those pages." `apps/web/src/routes/index.tsx` **unchanged** in the fix round (correct: S4 owns it) |
| **T-5** MINOR — no tenant/company predicate in `transact()`/`lockTransfer()` | Fixed | **VERIFIED for the asked sites; PARTIAL for the compatibility callers** | `StockTransferReceiptService.php:104` `Str::isUuid($transferId)` → `ModelNotFoundException`; `:107` `StockTransfer::query()->where('tenant_id',$tenantId)->where('company_id',$companyId)->findOrFail($transferId)`; signatures `:75/:81/:86` now take `string $tenantId, string $companyId`. `StockTransferMovementSupport.php:41` `lockTransfer(StockTransfer $identity)` with `:43` uuid guard and `:47-48` both predicates under `lockForUpdate()`. Direct foreign-company probe committed: `TransferReceiptAuthorityGateTest.php:37` throws `ModelNotFoundException` and asserts `denialSnapshot()` byte-identical. Controller supplies scope from `CompanyContext`, never input (`StockTransferController.php:308`, `:334`). **Residual → T-5(r2) MINOR below** |
| **T-6** MINOR — `LOCATION_ACCESS_DENIED` echoes `location_id` | Record as intentional | **VERIFIED (closed, with a placement note)** | Shape unchanged: `StockTransferController.php:395-401`. Recorded in the handback fix-round table (`HANDBACK…:345`). **→ T-15(r2) MINOR** (not in the promotion checklist, unlike the other four disclosures) |
| **T-7** MINOR — stray 7 MB SQLite `apps/api/autoerp_test_u` | Removed; trap recorded | **VERIFIED (closed)** | `ls apps/api/autoerp_test_u` → *No such file or directory*. Ignore rule still `.gitignore:81` `apps/api/autoerp_test_*`. Trap written up in the handback T-7 row |
| **T-8** MINOR — handback command omits the port | Fixed | **VERIFIED (closed)** | `HANDBACK-T2-receipt-spine-2026-09-09.md:19` and `:372` both carry `DB_PORT=5432`. **→ T-13(r2)**: the server on 5432 is PostgreSQL **15.15**, not 16 as the handback (and my own r1 register) states |

## Required verifications (items 1–10) and citation regression (r1 citations 1–13)

| # | Requirement | Status | Evidence |
|---|---|---|---|
| 1 | T-1 closure; own argv proof; fourteen classes; manifest exit 0 | **VERIFIED** | See T-1 row. `--filter=` in argv, `bash -n` 0, replay exit 0, 18/18 class names matched, `feature-lane-manifest-check.php` exit 0 ("1532 Feature classes / 74 groups … uniquely matched against 1944 test classes"; 70 parked groups / 1267 classes; 1 debt class; no ceiling/raise-note/CI-activation change in this round — `feature-lane-manifest.json` is **unchanged** in the fix-round diff) |
| 2 | T-2 closure; three exclusions with parent-scoped reasons; no waiver; ceilings unchanged; PG re-run | **VERIFIED** | See T-2 row. `:168-170`; ceilings `:191`/`:197` unchanged; PG OK 3/137 |
| 3 | T-3 closure; both closed directions pinned; PG re-run | **VERIFIED** | `TransferReceiptAuthorityGateTest.php:24`, `:31`; PG OK 8/19 |
| 4 | T-5 closure; both predicates; `Str::isUuid`; foreign-company call throws with zero writes; compatibility callers pass the scoped identity; completion-concurrency still green on an exclusive DB | **VERIFIED except the last clause of the caller claim — see T-5(r2)** | Predicates and guards: `StockTransferReceiptService.php:104,107`; `StockTransferMovementSupport.php:43,47-48`. Probe: `TransferReceiptAuthorityGateTest.php:37`. **Compatibility callers self-derive scope from an UNSCOPED lookup**: `StockTransferService.php:312` `$identity = StockTransfer::query()->findOrFail($transferId);`, `:328` and `:397` `lockTransfer(StockTransfer::query()->findOrFail($transferId))` — no `tenant_id`/`company_id` predicate, so the new predicate is tautological on `complete`/`cancel`/`initiate`. No live cross-company path (all three entries pre-scope: `StockTransferController.php:229-232` for complete, `:264-267` for cancel; `ReplenishmentFulfillmentService.php:102` calls `initiate($transferData)` with a DTO). `pg_stat_activity` on `autoerp_test_u` = **0 sessions** before the exclusive run; `StockTransferCompleteConcurrencyPostgresTest` **OK (2 tests, 20 assertions)** — and the race is *not* weakened: the contender is now started from a `StockTransferReceipt::created` hook that fires **inside** the receipt root transaction (`:94-118`), and `assertTrue($blocked, …)` on `pg_stat_activity.wait_event_type === 'Lock'` survives (`:117`) |
| 5 | T-4/T-6/T-7/T-8 | **VERIFIED** | Checklist `:11` (T-4), `:12` (G-2), `:13` (G-4), `:14` (G-7); T-6 in the handback; stray file gone; `DB_PORT=5432` at handback `:19`/`:372` |
| 6 | M-2 option (a) — file set; no receive/close UI; no route change; no permission-map change; what `ar/stock-transfers.json` contains; no baseline edit | **VERIFIED (file set, no UI, no route, no map) — but the i18n consequence is a BLOCKER** | Web file set is **exactly** the permitted list: `stock-transfers/{types/index.ts, components/StockTransferStatusBadge.tsx, components/StockTransferStatusBadge.test.tsx (A), pages/StockTransferDetailPage.tsx, pages/StockTransferListPage.tsx, __tests__/StockTransferDetailPage.test.tsx}`, `lib/i18n.ts`, `locales/{en,fr}/stock-transfers.json`, `locales/ar/stock-transfers.json` (A). **No receive/close/reconciliation UI**: `grep` over the feature's `.tsx`/`.ts` shows the only new affordance is `StockTransferDetailPage.tsx:38` `canComplete = … \|\| transfer.status === 'partially_received'`, which reuses the existing `POST /complete` button — and it matches the backend, `TransferStatus.php:47` `canBeCompleted() … \|\| $this === self::PartiallyReceived`. **No route change** (`apps/web/src/routes/index.tsx` unchanged). **No permission-map change** (`permissionsMap.generated.ts` unchanged in the fix round; whole-slice it is only the source-hash line + the two S1 keys `['admin','manager']`). The three new list filters are safe on the backend: `StockTransferController.php:71` uses `TransferStatus::tryFrom(...)`, enum-driven. **`ar/stock-transfers.json` contains EXACTLY three keys** — `status.partially_received` / `status.closed_with_writeoff` / `status.closed_returned`. **`lib/i18n.ts` changed by more than a namespace registration**: `:53` adds the import and `:478` replaces the whole-namespace alias `'stock-transfers': enStockTransfers` with the spread `{ ...enStockTransfers, status: { ...enStockTransfers.status, ...arStockTransfers.status } }` — and that spread is precisely what reclassifies the namespace from `aliased` to *wired* in the completeness auditor, exposing 118 English-supplied keys as `missing`. **No baseline touched**: `git diff de3007fe9..d7123fa30 -- apps/web/tools/` is **empty**; `i18n-completeness-baseline.json`, `audit-design-system-baseline.json`, `quantity-display-baseline.json` and the mirror `docs/handoff/progress/enforcement-p2.progress.yaml:84` (`fd6dbe3952cc3daaec15dc432e6b99e007f50dd6`) are all unchanged — the implementer did **not** take the bypass. **→ T-9(r2) BLOCKER** |
| 7 | Regression of r1 citations 1–13 at the new HEAD | **VERIFIED (all 13 hold)** | **Unchanged in the fix round** (per-file `git diff --quiet`): `RolesAndPermissionsSeeder.php`, `Inventory/Presentation/routes.php`, `Http/Middleware/RequireAnyPermission.php`, `2026_09_09_100100_create_stock_transfer_receipt_tables.php`, `permissionsMap.generated.ts`, `bootstrap/app.php`, `config/verticals.php`, `PROMOTION-CHECKLIST-2026-08-26.md`, `feature-lane-manifest.json`, `apps/web/src/routes/index.tsx`. Grants still exactly `RolesAndPermissionsSeeder.php:201,202` (definitions) + `:604` (the single `manager` map line); `grep -rn "transfers.reconcile\|transfers.close" apps/api/database/seeders/` returns **only** those three lines — no grant beyond the plan. `PROMOTION-CHECKLIST-2026-08-26.md` whole-slice diff = **`1 insertion(+), 0 deletions`**. Generated DTOs: reviewer ran `php artisan typescript:transform` ("Transformed 576 PHP types") and `diff` against the committed file → **byte-identical**; the fix round's only `generated.d.ts` change is the artisan-produced `GL_ACCOUNTS_UNMAPPED` member on `TransferReceiptFailureReason` (`:1450`) — no hand edit. Permissions map vs seeder re-run on PG: `ExportFrontendPermissionsMapCommandTest` **OK (3 tests, 19 assertions)** |
| 8 | G-2 / G-3 from the authz side: no change to who can call what; new reason is an enum case; typed 422; no account names/ids in the body | **VERIFIED** | `TransferReceiptFailureReason.php:23` `case GlAccountsUnmapped = 'GL_ACCOUNTS_UNMAPPED'` (rule 9 satisfied) with the static sentence `:44` "This action requires mapped inventory and shrinkage accounts." — no account name, code or id. Surfaced through the pre-existing envelope `StockTransferController.php:372` `['error' => ['code','message','details']], 422`, and the throw at `StockTransferReceiptService.php:141` passes **no** details (`TransferReceiptFailureException::$details` is typed `array{transfer_line_id?: string, batch_id?: int}`, default `[]`). Test asserts only `error.code` + a seven-table byte-identical snapshot (`StockTransferReceiveDamageTest.php:82-98`). The preflight reuses the existing purpose resolver `GeneralLedgerService::hasInventoryWriteOffAccounts()` (`GeneralLedgerService.php:5151`), already consumed by `InventoryGlPostingService.php:99` — no new `SystemAccountPurpose`, no rule-6 breach (Inventory already injects this Accounting service in two other places). **No authorization surface moved**: routes, aliases, middleware, seeder all unchanged; the G-3 nest guard (`StockTransferReceiptService.php:97`, `\LogicException`) is a programmer-error guard, and no production caller is inside a transaction (the only three are `StockTransferService.php:315` and `StockTransferController.php:308,334`) |
| 9 | Scope census of the fix-round diff; orchestrator-owned files | **CONTRADICTED for one file** | 35 files; every one maps to a register ask (table in "Scope census" below) **except** `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-9.md`, which the implementer **committed with an EDIT**, not merely cited. **→ T-11(r2) MAJOR** |
| 10 | Anything new in my area (routes / middleware / seeder lines / migrations) | **VERIFIED — none** | The fix-round `--name-status` contains no `routes.php`, no `bootstrap/app.php`, no `config/`, no `database/migrations/`, no `database/seeders/`, no `Providers/`. Two new files only: `StockTransferStatusBadge.test.tsx` and `ar/stock-transfers.json`. No `onQueue`/`ShouldQueue`/`Event::listen` added (rule 20 still vacuous). New non-authz surfaces assessed: `QuantityScale::decimalPlacesForUnit()` (`QuantityScale.php:65`), `StockTransferMovementSupport::MONEY_SCALE = 4` (`:29`), and the null-safe `$line->product->unitOfMeasure?->decimal_places` (`TransferReconciliationService.php:45`) which removes a 500 on a UoM-less product |

## Findings

### BLOCKER

**T-9(r2) — The i18n completeness ratchet is RED at this HEAD (exit 1, 118 NEW gaps), caused solely by the M-2 `i18n.ts` spread, and the fix round left it red. `frontend-lint` is in `all-checks-pass`'s `needs`, so the lane cannot merge in this state.**

What: `apps/web/src/lib/i18n.ts:478` replaced the whole-namespace English alias
```
-    'stock-transfers': enStockTransfers,
+    'stock-transfers': { ...enStockTransfers, status: { ...enStockTransfers.status, ...arStockTransfers.status } },
```
`tools/audit-i18n-completeness.mjs` classifies a namespace as `aliased` (ONE baselined entry, never per-key) while it is wired to the English bundle, but as *wired* the moment it becomes a `{...en, ...locale}` spread — at which point every English-authored key with no counterpart in `locales/ar/<ns>.json` is a `missing` finding (script header, "WHAT IT CHECKS" 1 vs 2). `ar/stock-transfers.json` authors 3 of the 121 English keys, so 118 become new findings.

Reviewer's own run (not the handback's), with the owner-pinned authority from the mirror `docs/handoff/progress/enforcement-p2.progress.yaml:84`:
```
$ I18N_BASELINE_PROTECTED_BLOB=fd6dbe3952cc3daaec15dc432e6b99e007f50dd6 node tools/audit-i18n-completeness.mjs
i18n completeness — 118 NEW gap(s) not in the baseline:
  ✗ ar|stock-transfers|missing|create.availability.error
  … (118 lines, incl. ✗ ar|stock-transfers|missing|status.draft / status.completed / status.cancelled / title / subtitle)
i18n completeness — 1 baseline entry now translated (burn-down; regenerate the baseline and re-pin to lock the gain in).
EXIT=1
```
Why it matters: the gate is an explicit discrete step in `frontend-lint` (`.github/workflows/ci.yml:2458`, env `I18N_BASELINE_PROTECTED_BLOB` at `:2472`), and `frontend-lint` is enumerated in `all-checks-pass`'s `needs` (`ci.yml:2772`). This is a red ratchet on a merge-gating job, introduced by this fix round, on a lane whose entire M-2 mandate was three status labels. It also blocks preflight at `scripts/preflight.sh:203`, which is why five downstream preflight gate families were never reached this round (**T-10(r2)**). The handback discloses it honestly ("Without that scope ruling, this is not a fully green handback") and no baseline, mirror, protected blob or detector was touched — but disclosure does not make a red gate green.

Minimum fix — two options, and the cheaper one needs **no** owner ruling:
* **(i) Revert the i18n wiring** to `'stock-transfers': enStockTransfers` and delete `apps/web/src/locales/ar/stock-transfers.json`. The three new status labels then fall back to English in `ar` exactly as the other 118 keys already do — zero behavioural inconsistency, zero gate breakage, zero scope decision. Drop the `changeLanguage('ar')` case from `StockTransferStatusBadge.test.tsx:20-27`.
* **(ii) Widen the scope**: apply the prepared 121-key draft (`docs/sessions/t2-receipt-spine/fix-round-1/stock-transfers.ar.proposed.json`, gitignored — I did **not** review its content), then regenerate `i18n-completeness-baseline.json` to bank the burn-down and have the **owner** re-pin `I18N_BASELINE_PROTECTED_BLOB` + the mirror at `enforcement-p2.progress.yaml:84` (an owner write at promotion, per the script's two-phase pin protocol). Note the "1 baseline entry now translated" burn-down means a re-pin is owed under (ii) either way.

Either way the remaining preflight gates must then run (**T-10(r2)**).

### MAJOR

**T-10(r2) — No exit-0 preflight run exists for this fix round, and five preflight gate families after the i18n step were never executed.** The handback states it plainly: "It then exited 1 at i18n with 118 missing Arabic keys. Gates after i18n were not reached in this run. No successful full preflight is claimed." Rule 10 requires a green `./scripts/preflight.sh` before a task is complete. The unreached families are `scripts/preflight.sh:206-214` web detector-liveness (`pnpm test:eslint-rules && pnpm test:tools` — the detector-liveness convention's own gate), `:216-218` POS ESLint rule tests, `:220-222` route-manifest drift, `:224-231` Vitest, `:238-246` fiscal v3 fixture parity, `:256-261` §14.3 chokepoint completeness. I closed three of them myself to reduce the residual: `bash scripts/factory/check-manifest-drift.sh` → **exit 0**; `pnpm audit:design-system` → **exit 0**, "802 acknowledged, 0 new, 0 stale" (so the `Badge`→`<span>` rewrite of **T-14(r2)** introduces no design-system violation); `pnpm audit:keys` → **exit 0**. I did **not** run Vitest: swap was 10 943 / 12 288 MB at review time and hung vitest worker pools are a known laptop-killer — the two named Vitest files are the frontend-conventions reviewer's leg. Fix: after **T-9(r2)**, run preflight through to exit 0 (or, at minimum, the six named families) and record it.

**T-11(r2) — The implementer committed an EDITED copy of the orchestrator-owned plan rev 9 onto the lane, and it is an add/add divergence from the `dev` copy: merging `d7123fa30` will conflict on the lane's own authority document.** `d7123fa30` **adds** `docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-9.md` (6 633 lines) to a branch that did not have it. The orchestrator's copy lives at `5dbb7e1ee` on `dev`, which is **NOT an ancestor** of `d7123fa30` (`git merge-base --is-ancestor 5dbb7e1ee d7123fa30` → false; `git merge-base` = `de3007fe9`). The two blobs differ (`97acfdf5c…` vs `5e7893aca…`). The delta is exactly **one hunk, two lines** — a blank line plus `plan-rev-9.md:2742` `**S1 fix round 1, I-2:** Receipt/close line DTO quantities remain scale-4 strings. S4 joins each receipt line on transfer_line_id …` inserted into §7.12. So: **no content defect**, the note is exactly the I-2 record the fix-round prompt asked for, and the handback discloses it ("Rev 9 is copied from the orchestrator's shared-root authority into this worktree with only the requested §7.12 I-2 note added"). The defect is ownership + merge topology: an implementer wrote into a plan document the orchestrator owns, producing a second copy of the lane's SoT, and git will present this as an add/add conflict where a careless resolution silently drops either the I-2 note or the orchestrator's rev-9 rulings (§0AA, the §7.15 citation-3 correction at `:54`/`:3978`, §14's added frontend-conventions reviewer). I verified the lane copy otherwise carries the rev-9 corrections verbatim. Fix: at merge, the **orchestrator** resolves in favour of the shared-root copy and re-applies the two-line I-2 note itself (or drops the lane copy of the file entirely and records I-2 on `dev`); future fix rounds record plan-level decisions in the handback and let the orchestrator transcribe.

### MINOR

**T-5(r2) — The T-5 predicate is tautological on the three compatibility paths, because they self-derive the scope from an unscoped lookup.** `StockTransferService.php:312` `$identity = StockTransfer::query()->findOrFail($transferId);` then passes `$identity->tenant_id, $identity->company_id` into `receiveAllRemaining`, and `:328` / `:397` pass `lockTransfer(StockTransfer::query()->findOrFail($transferId))`. Whatever row is found supplies its own predicate, so `complete`/`cancel`/`initiate` are exactly as caller-trusting as before the fix round — no regression, and no live exposure, since `StockTransferController.php:229-232` and `:264-267` pre-scope on `tenant_id`+`company_id` behind a `Str::isUuid` guard and `ReplenishmentFulfillmentService.php:102` passes a DTO. The handback's T-5 row says the callers "supply their resolved identity", which is literally true but reads as *scoped*; it is not. Fix (or record): have `complete`/`cancel`/`moveSourceToInTransit` take the already-scoped `StockTransfer` from the controller, or add `->where('tenant_id', …)->where('company_id', …)` at `:312`, `:328`, `:397`. S2/S4 add a notification writer and a receiver projection — the first caller that resolves a transfer id from a payload, job argument or deep link lands on one of these three, not on `receive`/`close`.

**T-12(r2) — Two CI-allowlisted test classes silently switched isolation mechanism: `RefreshDatabase`'s wrapping transaction is disabled and replaced by a per-test `migrate:fresh`, at a measured 2m18s for one class.** `StockTransferEdgeCasesTest.php:57-60` and `InventoryTransferServiceTest.php:73-76` add `protected function connectionsToTransact(): array { return []; }`, and `:65` / `:81` add `RefreshDatabaseState::$migrated = false;` to `setUp()`. Consequences the handback does not state (its ledger says only "adapt to the explicit root-transaction contract … their existing stock, WAC, replay and lock-wait assertions remain", which I confirmed — no assertion was weakened): (a) every test in these two classes now re-migrates the whole schema, and their writes **COMMIT**; (b) reviewer-measured PG runtime for `StockTransferEdgeCasesTest` is **2m17.996s** for 27 tests (vs seconds under transaction rollback), and both classes are named in the `ci.yml:1145` alternation, i.e. this cost lands on the only live PG gate for ~200 parked classes (no `timeout-minutes` on `backend-test-pgsql` — `ci.yml:578-588` — so it is cost, not failure); (c) a forward hazard worth writing down: a per-test `migrate:fresh` is **destructive to any concurrent class on the same database**, so if anyone ever adds `--parallel` to that step it will corrupt neighbours. I checked the db-per-tenant angle specifically and it is clean: no `tenant_*` database was created during my runs (the 15 on the server date from 2026-05-27 … 2026-08-11 by `pg_stat_file` mtime), so committed tenants do not leak per-tenant databases; the only committed residue is one `tenants` row in `autoerp_test_u`, which the next `RefreshDatabase` class drops. Fix: disclose the mechanism change and its per-class cost in the handback, and prefer a targeted `$this->beginDatabaseTransaction()`-free single test over disabling it for a 27-case class.

**T-13(r2) — The lane's PostgreSQL is 15.15, not 16; the handback claims 16 twice, and my own r1 register repeated the error.** `psql … -Atc "show server_version"` → **`15.15 (Homebrew)`**; `lsof -nP -iTCP:5432 -sTCP:LISTEN` shows a single `postgres` pid 1361 (user `houssamr`) on `[::1]:5432` and `127.0.0.1:5432`, so there is no second server to be confused with. Handback `:16` "resolved to native PostgreSQL 16 on 5432" and `:346` "Native PostgreSQL 16" are wrong, as is my r1 register `:78`/`:93` — **I correct my own record here.** Production is PostgreSQL 16+ (root `CLAUDE.md`), so no PG leg in this lane has been exercised on the production major. Nothing in this slice appears to need a 16-only feature (partial unique indexes, `FOR UPDATE`, `numeric`, `pg_stat_activity.wait_event_type` are all ≤ PG 11), and CI's `backend-test-pgsql` uses its own service container — but the handback's version claim should be corrected so no one reads these logs as PG-16 proof.

**T-14(r2) — The shared `Badge` atom was replaced with a hand-rolled `<span>` in this feature, although the atom already had the variant the new status needed.** `StockTransferStatusBadge.tsx:2` dropped `import { Badge, type BadgeVariant } from '@/components/atoms/Badge'` in favour of `tokens` and `:22` now renders `<span className={`${tokens.badge.base} ${statusVariant[status]}`}>`. The atom at `apps/web/src/components/atoms/Badge/Badge.tsx:4` declares `BadgeVariant = 'default' | 'success' | 'warning' | 'danger' | 'info'` with `:15` `warning: …intent.warning.bgSoft…`, so `partially_received: 'warning'` was available and the atom rewrite was avoidable. The design-system audit cannot see this (it looks for raw Tailwind classes, and `tokens.badge.*` at `designTokens.ts:1011-1022` all exist) — `pnpm audit:design-system` is **exit 0, 0 new**. Recorded from my seat as unnecessary scope and a second badge surface inside one feature; **the frontend-conventions reviewer owns the call** (this is the same shape as that reviewer's B-5 finding on the W-LOT lane).

**T-15(r2) — T-6 is recorded only in the handback, while the lane's four other S1 disclosures went to the promotion checklist.** `PROMOTION-CHECKLIST-2026-09-09-t2t3.md:11-14` carries T-4, G-2, G-4 and G-7; the `LOCATION_ACCESS_DENIED` 403-details-vs-404-hide asymmetry (`StockTransferController.php:395-401`, now reached by `receive` and `close` as well as `complete`/`cancel`) lives only in the handback fix-round table. The r1 ruling said "RECORD as intentional" without naming a file, so this is a placement nit, not a miss. Fix: one line in the checklist's "S1 review disclosures" block so S4 inherits the decision where it will be read.

## PostgreSQL runs

Database `autoerp_test_u` on the native server at `127.0.0.1:5432` (**PostgreSQL 15.15** — see T-13(r2)), one file per invocation, serially, never the full suite. Command exactly as the handback specifies, from the worktree root:
`DB_DATABASE=autoerp_test_u DB_CENTRAL_DATABASE=autoerp_test_u DB_USERNAME=houssamr DB_HOST=127.0.0.1 DB_PORT=5432 DB_PASSWORD='' apps/api/vendor/bin/phpunit -c apps/api/phpunit-pgsql.xml FILE`
`pg_stat_activity` on `autoerp_test_u` showed **0 sessions** before the batch and again before the exclusive class.

| File | Result | Matches handback? |
|---|---|---|
| `tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php` | OK (3 tests, 137 assertions), exit 0 | **YES** — handback `:381`. **Was RED (Failures: 1) at `c30dcdf9c`** → T-2 closed |
| `tests/Feature/Inventory/TransferReceiptAuthorityGateTest.php` | OK (8 tests, 19 assertions), exit 0 | YES — `:383` (was 5/15 at r1; +3 for T-3/T-5) |
| `tests/Feature/Inventory/TransferReceiptSecondOfEverythingTest.php` | OK (2 tests, 30 assertions), exit 0 | YES — unchanged from r1 |
| `tests/Feature/Inventory/StockTransferReceiveConcurrencyPostgresTest.php` | OK (3 tests, 43 assertions), exit 0 | YES — `:378` |
| `tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php` *(exclusive DB confirmed)* | OK (2 tests, 20 assertions), exit 0 | YES — `:375`; race intact (see item 4) |
| `tests/Feature/Inventory/StockTransferIdempotencyCollisionPostgresTest.php` | OK (2 tests, 13 assertions), exit 0 | YES — unchanged from r1 |
| `tests/Feature/Migrations/TransferReceiptSchemaRerunPostgresTest.php` | OK (2 tests, 11 assertions), exit 0 | YES — unchanged from r1 |
| `tests/Feature/Inventory/TransferReceiptEventStreamTest.php` | OK (3 tests, 38 assertions), exit 0 | YES — unchanged from r1 |
| `tests/Feature/Inventory/TransferLegacyCompletionBackfillTest.php` | Tests: 7, Assertions: 39, Skipped: 1, exit 0 (the skip is the capture-only baseline case; the PG-only interruption case ran) | YES — unchanged from r1 |
| `tests/Feature/Console/ExportFrontendPermissionsMapCommandTest.php` | OK (3 tests, 19 assertions), exit 0 | n/a (reviewer addition, citation 10) |
| `tests/Feature/Inventory/TransferReceiptGlBoundaryTest.php` | OK (2 tests, 16 assertions), exit 0 | YES — `:384` (was 1/8; +1 for the G-3 nest guard) |
| `tests/Feature/Inventory/StockTransferReceiveDamageTest.php` | OK (6 tests, 22 assertions), exit 0 | YES — `:379` (was 4/11; +2 for G-2 and I-9) |
| `tests/Feature/Inventory/StockTransferCloseTest.php` | OK (7 tests, 31 assertions), exit 0 | YES — `:373` (was 6/29; +1 for M-1) |
| `tests/Feature/Inventory/StockTransferEdgeCasesTest.php` | Tests: 27, Assertions: 115, Skipped: 2, exit 0 — **02:17.996** | YES — `:376`; runtime → T-12(r2) |
| `tests/Feature/Inventory/StockTransferReceiveLotsTest.php` | OK (3 tests, 32 assertions), exit 0 | YES — `:380` (was 3/27; +5 for G-1) |
| `tests/Architecture/TransferInTransitReadersUseRemainderTest.php` | OK (3 tests, 9 assertions), exit 0 | YES — `:382` |
| `tests/Unit/Shared/QuantityScaleFormatForUnitTest.php` | OK (5 tests, 12 assertions), exit 0 | YES — `:372` |

**Every count in the handback's fix-round table that I re-ran matched exactly.** No environmental failure; nothing skipped for driver reasons beyond the two documented ticket skips and the capture-only baseline case.

## Static gates

| Command | Outcome |
|---|---|
| Extract `ci.yml` step body → `bash -n` | exit 0 |
| Replay the extracted step under `bash -eo pipefail` with a stub `php` (only `php` on PATH) | **exit 0**; 3 invocations; first argv = `<artisan> <test> <-c> <phpunit-pgsql.xml> <--filter=…>` → **T-1 closed** |
| 18 class names checked inside the `--filter` argv | 18/18 present (fourteen S1 + ratchet + edge + complete-concurrency + collision) |
| `php tools/feature-lane-manifest-check.php` | **exit 0** — 1532 Feature classes / 74 groups, uniquely anchored against 1944 test classes; 70 parked groups / 1267 classes; 1 debt class; no ceiling or raise-note change this round |
| Scoped PHPStan (level 8, `phpstan.neon`) over all 7 changed files under `apps/api/app` | **`[OK] No errors`** |
| `php artisan typescript:transform` then `diff` vs committed `generated.d.ts` | **byte-identical**; `git status --short` empty afterwards |
| `node tools/audit-i18n-completeness.mjs` with the owner-pinned blob | **exit 1 — 118 NEW gaps** → **T-9(r2) BLOCKER** |
| `bash scripts/factory/check-manifest-drift.sh` | exit 0 (a preflight gate the fix round never reached) |
| `pnpm audit:design-system` | exit 0 — 802 acknowledged, **0 new**, 0 stale |
| `pnpm audit:keys` | exit 0 |
| `git diff de3007fe9..d7123fa30 -- apps/web/tools/` | **empty** — no baseline/detector edit anywhere in the slice |
| `git diff … -- docs/handoff/PROMOTION-CHECKLIST-2026-08-26.md` (whole slice) | `1 insertion(+), 0 deletions` |
| Not run (justified) | Vitest (`StockTransferStatusBadge.test.tsx`, `StockTransferDetailPage.test.tsx`, `StockTransferListPage.test.tsx`) — swap at 10 943/12 288 MB; hung vitest worker pools are a known laptop-killer. Frontend-conventions reviewer's leg |

## Scope census

35 files in `93b106461..d7123fa30`. Every file maps to a register ask except one.

| File(s) | Register item | In scope? |
|---|---|---|
| `.github/workflows/ci.yml` | T-1 | yes |
| `StockTransferMovementSupport.php` | T-5 (`lockTransfer`) + I-1 (`MONEY_SCALE`) | yes |
| `StockTransferReceiptService.php` | T-5, G-2, G-3, I-1, I-7 | yes |
| `StockTransferService.php` | T-5 callers, M-1 (freight on cancel), G-5 (orphan docblock removed) | yes |
| `TransferReconciliationService.php`, `QuantityScale.php`, `QuantityScaleFormatForUnitTest.php` | I-4 | yes |
| `TransferReceiptFailureReason.php`, `packages/shared/types/generated.d.ts` | G-2 (+ artisan regeneration, proven byte-reproducible) | yes |
| `StockTransferController.php` | T-5 (passes `CompanyContext` scope) | yes |
| `TenantOnlyUniqueOnCatalogueTablesRatchetTest.php` | T-2 | yes |
| `TransferInTransitReadersUseRemainderTest.php` | I-3 | yes |
| `TransferReceiptAuthorityGateTest.php` | T-3, T-5 | yes |
| `TransferReceiptGlBoundaryTest.php` | G-3 | yes |
| `StockTransferReceiveDamageTest.php`, `StockTransferReceiveLotsTest.php` | G-1, G-2, I-7, I-9 | yes |
| `StockTransferCloseTest.php` | M-1 | yes |
| `InventoryTransferServiceTest.php`, `StockTransferEdgeCasesTest.php`, `StockTransferCompleteConcurrencyPostgresTest.php`, `StockTransferReceiveConcurrencyPostgresTest.php` | G-3 / T-5 signature consequence; disclosed in the handback ledger | yes — but see **T-12(r2)** for the undisclosed isolation-mechanism change |
| `stock-transfers/{types,components,pages,__tests__}`, `lib/i18n.ts`, `locales/{en,fr,ar}/stock-transfers.json` | M-2 option (a) | yes in file set — but see **T-9(r2)** |
| `docs/glossary.md` | I-8 — three rows marked `**Planned (S2/S3).**` at `:89`, `:90`, `:92`; no row added, renamed or re-pointed, no synonym change (convention 11 clean) | yes |
| `docs/handoff/HANDBACK-…md`, `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` | evidence + T-4/G-2/G-4/G-7 disclosures | yes |
| **`docs/superpowers/plans/2026-09-09-…plan-rev-9.md`** | **I-2 — but the implementer EDITED and committed an orchestrator-owned plan** | **NO → T-11(r2)** |

Authorization surface in the fix round: **none**. No new middleware alias, route file, route group, `module:` guard, permission, role grant, migration, seeder line, queued job or event subscription. No `blind_receiving`, `visibility_version`, receiver-view route, notification listener or masking helper — nothing from S2/S3/S4 leaked in. Convention 09: the three receipt tables are now explicitly classified as parent-scoped (`TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:168-170`), the two document-identity uniques still carry `company_id`, and the second-company/second-location/re-run proof (`TransferReceiptSecondOfEverythingTest`, 2/30) is green on PG. Convention 11: the fix round **removed** a duplicate surface (`types/index.ts:2` now aliases the generated `App.Modules.Inventory.Domain.Enums.TransferStatus` instead of hand-rolling the union) — a real improvement — and **added** one micro-surface (**T-14(r2)**).

## Tree state after review

```
$ git status --short
(empty)
$ git rev-parse HEAD
d7123fa3021299ccc911ea06482fc2a97c9fe88e
$ ls apps/api/autoerp_test_u
ls: ...: No such file or directory
```
No file was edited, committed, merged or pushed by this reviewer. The regenerated `packages/shared/types/generated.d.ts` came back byte-identical, so nothing needed restoring. Reviewer-created artifacts (extracted CI step, `php` stub, logs) live only in the session scratchpad. Residue left in the test database by the non-transactional classes of **T-12(r2)**: one `tenants` row in `autoerp_test_u`; no `tenant_*` database was created by any run of mine.

---

**What to fix before merge:** make the i18n completeness ratchet green again — the cheapest path needs no owner ruling at all: revert `apps/web/src/lib/i18n.ts:478` to the whole-namespace `enStockTransfers` alias and drop `locales/ar/stock-transfers.json`, so the three new status labels fall back to English exactly as the namespace's other 118 keys already do (**T-9(r2)**); then run preflight through to exit 0 (**T-10(r2)**); and have the orchestrator — not the implementer — own the plan rev-9 file at merge so the add/add conflict cannot drop either the I-2 note or the rev-9 rulings (**T-11(r2)**). All eight gate-r1 tenancy items are closed and re-verified on PostgreSQL; nothing in the fix round touched a permission, a role grant, a route, a middleware alias or a migration.

Orchestrator: session_01AcU81as26G2apodoimmyq7 (reviewer = Claude Opus `tenancy-authz-reviewer` agent, PG runner owner for this lane).

Files referenced (absolute):
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/web/src/lib/i18n.ts`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/web/src/locales/ar/stock-transfers.json`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/web/tools/audit-i18n-completeness.mjs`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/api/app/Modules/Inventory/Application/Services/StockTransferService.php`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/api/app/Modules/Inventory/Application/Services/StockTransferReceiptService.php`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/apps/api/tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/docs/superpowers/plans/2026-09-09-T2T3-transfer-receipt-blind-receiving-plan-rev-9.md`
- `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/.github/workflows/ci.yml`

## Orchestrator rulings (session_01AcU81as26G2apodoimmyq7, 2026-09-10)
- **T-9(r2): FIX via option (i)** — revert `apps/web/src/lib/i18n.ts` to the whole-namespace `enStockTransfers` alias, delete `locales/ar/stock-transfers.json`, drop the `ar` case from the badge test. Arabic coverage of the `stock-transfers` namespace (121 keys) is NOT an S1 scope; it is recorded as a follow-up in the promotion checklist / a ticket. No baseline or pin change.
- **T-10(r2): FIX** — preflight to exit 0 after T-9, recorded in the handback.
- **T-11(r2): RESOLVED ON DEV** — the orchestrator adopted the lane's two-line §7.12 I-2 note into dev's rev 9 copy (`dev` commit after `5dbb7e1ee`; blobs now identical, `6e445ae8c`), so the merge has no add/add divergence. Standing rule restated for the implementer: never edit orchestrator-owned plans/reviews; record plan-level decisions in the handback.
- **T-5(r2): FIX** — scope predicates at `StockTransferService.php:312,328,397` (or accept the controller's scoped model).
- **T-12(r2): DISCLOSE + REDUCE** — state the isolation-mechanism change and its cost in the handback; migrate once per class rather than per test where the mechanism allows; no assertion change. **T-13(r2): FIX** the handback (PostgreSQL 15.15). **T-14(r2): deferred to frontend-conventions r2** (orchestrator leaning: use the `Badge` atom's `warning` variant; one surface per concept). **T-15(r2): FIX** (one checklist line).
- Remaining S1 gate r2 reviewers (stock-gl, inventory, frontend-conventions) run next, one at a time; the S1 fix-round-2 prompt is written after all four registers.
