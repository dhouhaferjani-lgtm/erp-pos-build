# Codex Round-2 Review: POS T2.2 Crash Safety + Small Wins

Date: 2026-05-09

Baseline: `1de2e33b`

Reviewed commit: `03d5b16b`

Scope: adversarial round-2 review of the round-1 follow-up commit only, with emphasis on closure of the 3 round-1 MINORs, ricochet regressions from those fixes, comment-only scope, deferral sweep completeness, and the requested POS test subset.

## Round-1 Finding Closure Status

| Round-1 finding | Status | Evidence |
|---|---|---|
| MINOR-1: stale endpoint-reconciliation TODO in `paymentApi.ts` | CLOSED | `grep -n 'TODO' apps/pos/src/api/paymentApi.ts` returned no matches. The diff deletes only the stale TODO block at `apps/pos/src/api/paymentApi.ts:4` old-side, while current exports remain present at `apps/pos/src/api/paymentApi.ts:4` and `apps/pos/src/api/paymentApi.ts:8`. Imports are untouched at `apps/pos/src/api/paymentApi.ts:1` and `apps/pos/src/api/paymentApi.ts:2`. |
| MINOR-2: broken April-doc anchors in 4 surviving TODOs | CLOSED | `grep -rn '2026-04-30-pos-offline-first-hardening\|2026-04-30-pos-first-launch-offline-activation' apps/pos/src apps/api/database/seeders` returned no matches. The diff retargets stranded receipts to row 6 at `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:127`, cold-start mode to row 7 at `apps/pos/src/stores/terminalStore.ts:96`, batch push to row 8 at `apps/pos/src/lib/sync/syncService.ts:296`, and image manifest to row 9 at `apps/pos/src/lib/images/imageCache.ts:9`. The kickoff doc exists and its Step 5.3 rows match those descriptions at `docs/superpowers/plans/2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md:209`, `:210`, `:211`, and `:212`. |
| MINOR-3: missed CSV-driven product import TODO | CLOSED | `grep -n 'TODO(go-live-followup): CSV' apps/api/database/seeders/ParapharmacySeeder.php` returns `apps/api/database/seeders/ParapharmacySeeder.php:334`. The new TODO sits beside the two existing T1.0 TODOs near `seedProducts` at `apps/api/database/seeders/ParapharmacySeeder.php:324`, `:330`, and `:334`, and points to the reachable T1.0 kickoff doc at `apps/api/database/seeders/ParapharmacySeeder.php:339`; that doc contains the matching out-of-scope CSV row at `docs/superpowers/plans/2026-05-08-pos-t1.0-large-catalog-fixture-kickoff-prompt.md:110`. |

## Ricochet Findings

None.

The round-1 fixes did not introduce a new BLOCKER, MAJOR, MINOR, or NIT.

## Per-Finding Evidence

### MINOR-1 Closure

- The stale TODO was removed from `apps/pos/src/api/paymentApi.ts`; `grep -n 'TODO' apps/pos/src/api/paymentApi.ts` returned no matches.
- The diff only deletes the old comment block beginning at old-side `apps/pos/src/api/paymentApi.ts:4`; no import or export statement is changed.
- Current file still exports `fetchPaymentMethods` at `apps/pos/src/api/paymentApi.ts:4` and `fetchPaymentRepositories` at `apps/pos/src/api/paymentApi.ts:8`.
- Ricochet check: imports remain `apiGet` and the two payment types at `apps/pos/src/api/paymentApi.ts:1` and `apps/pos/src/api/paymentApi.ts:2`; no import break observed.

### MINOR-2 Closure

- Old April anchors are gone from the searched source paths; the required grep returned no matches.
- `offlineReceiptRepository.ts` now references `2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md` Section 3 Step 5.3 row 6 at `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:127` and `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:128`; kickoff row 6 is "Stranded-receipt operator/admin UI" at `docs/superpowers/plans/2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md:209`.
- `terminalStore.ts` now references row 7 at `apps/pos/src/stores/terminalStore.ts:96` and `apps/pos/src/stores/terminalStore.ts:97`; kickoff row 7 is "Full offline cold-start mode for fresh devices" at `docs/superpowers/plans/2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md:210`.
- `syncService.ts` now references row 8 at `apps/pos/src/lib/sync/syncService.ts:296` and `apps/pos/src/lib/sync/syncService.ts:297`; kickoff row 8 is "Batch receipt push for 1000-receipt offline backlogs" at `docs/superpowers/plans/2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md:211`.
- `imageCache.ts` now references row 9 at `apps/pos/src/lib/images/imageCache.ts:9` and `apps/pos/src/lib/images/imageCache.ts:10`; kickoff row 9 is "Image preloading manifest" at `docs/superpowers/plans/2026-05-09-pos-t2.2-crash-safety-small-wins-kickoff-prompt.md:212`.
- Ricochet check: the row descriptions match the TODO subjects in all four files.

### MINOR-3 Closure

- The CSV TODO now exists at `apps/api/database/seeders/ParapharmacySeeder.php:334`.
- It is colocated with the other two T1.0 `seedProducts` deferrals at `apps/api/database/seeders/ParapharmacySeeder.php:324` and `apps/api/database/seeders/ParapharmacySeeder.php:330`.
- It references the T1.0 kickoff doc at `apps/api/database/seeders/ParapharmacySeeder.php:339`, and that doc contains the explicit CSV out-of-scope item at `docs/superpowers/plans/2026-05-08-pos-t1.0-large-catalog-fixture-kickoff-prompt.md:110`.
- Ricochet check: `grep -rn 'CSV.*TODO\|TODO.*CSV' apps/pos/src apps/api/database/seeders docs/superpowers` found only the new source TODO plus the round-1 review text; no prior source CSV TODO was duplicated.

## Additional Checks

- Commit body mentions all three round-1 MINORs explicitly: MINOR-1, MINOR-2, and MINOR-3 each have a dedicated paragraph in `git show -s --format=full 03d5b16b`.
- Every new `TODO(go-live-followup)` in the diff references a reachable doc: the CSV TODO points to the T1.0 kickoff at `apps/api/database/seeders/ParapharmacySeeder.php:339`, and that file exists with the matching item at `docs/superpowers/plans/2026-05-08-pos-t1.0-large-catalog-fixture-kickoff-prompt.md:110`.
- Deferral sweep coverage is now 10 items: 9 in-source TODOs from `grep -rn 'TODO(go-live-followup)' apps/pos/src apps/api/database/seeders`, plus ESLint v9 carried in the commit body. The 9 in-source anchors are at `apps/pos/src/stores/terminalStore.ts:90`, `apps/pos/src/components/atoms/SyncButton/SyncButton.tsx:31`, `apps/pos/src/lib/images/imageCache.ts:3`, `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:71`, `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:121`, `apps/pos/src/lib/sync/syncService.ts:289`, `apps/api/database/seeders/ParapharmacySeeder.php:324`, `apps/api/database/seeders/ParapharmacySeeder.php:330`, and `apps/api/database/seeders/ParapharmacySeeder.php:334`.
- I did not find an 11th missed deferral in the T2.2/T1.0/T1.3 scoped sweep after the CSV item was added.
- Comment-only check passed for the reviewed diff: `git diff 1de2e33b..03d5b16b -- '*.ts' '*.tsx' '*.php'` shows only comment deletion/addition/retargeting at `apps/pos/src/api/paymentApi.ts:4`, `apps/pos/src/lib/db/repositories/offlineReceiptRepository.ts:127`, `apps/pos/src/stores/terminalStore.ts:96`, `apps/pos/src/lib/sync/syncService.ts:296`, `apps/pos/src/lib/images/imageCache.ts:9`, and `apps/api/database/seeders/ParapharmacySeeder.php:334`.
- Requested tests passed: `pnpm --filter @autoerp/pos test -- src/lib/offline/__tests__/receiptService.test.ts src/lib/db/repositories/__tests__/offlineReceiptRepository.insert.test.ts` completed with 2 files passed and 32 tests passed.

FINAL VERDICT: APPROVE
