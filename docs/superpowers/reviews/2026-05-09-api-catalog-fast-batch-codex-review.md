Verdict: REQUEST-CHANGES
Commit reviewed: 48ff4bb961c5bdf4de2181741206acf8b26cfaa6
Tests gate: 2/2 passing
PHPStan gate: 1 errors
Pint gate: pass
Sweep inventory gate: 0 problems

## Per-callsite verification (027-033)

- api.catalog.027 - `ProductController::show` at HEAD uses `Product::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $product)` at `app/Modules/Product/Presentation/Controllers/ProductController.php:228-233`. Chained query form confirmed: Y. Regression test exists: Y, `test_show_product_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404 plus same-tenant 200. Gaps: none for this callsite; SQL invariant also checks the captured products lookup contains both `"tenant_id"` and `"company_id"`.
- api.catalog.028 - `ProductController::update` at HEAD uses `Product::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $product)` at `ProductController.php:391-395`. Chained query form confirmed: Y. Regression test exists: Y, `test_update_product_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404 plus unchanged foreign product name. Gaps: none for this callsite.
- api.catalog.029 - `ProductController::destroy` at HEAD uses `Product::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $product)` at `ProductController.php:531-535`. Chained query form confirmed: Y. Regression test exists: Y, `test_destroy_product_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404 plus foreign product still present. Gaps: none for this callsite.
- api.catalog.030 - `ProductController::stockLevels` at HEAD scopes the route product read with `Product::query()->where('tenant_id', $tenantId)->where('company_id', $companyId)->where('id', $product)` at `ProductController.php:572-576`, and scopes the `StockLevel` aggregate with `StockLevel::query()->where('tenant_id', $tenantId)->where('company_id', $companyId)->where('product_id', $productModel->id)` at `ProductController.php:592-597`. Chained query form confirmed: Y. Regression test exists: Y, `test_stock_levels_rejects_cross_tenant_product_id`. Test asserts cross-tenant denial: Y, 404. Gaps: no gap in the fixed product/stock-level reads; see cross-cluster observation on the incoming purchase-order aggregate in the same method.
- api.catalog.031 - `EnrichmentReviewController::show` at HEAD uses `EnrichmentResult::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $id)` at `app/Modules/Product/Presentation/Controllers/EnrichmentReviewController.php:122-127`. Chained query form confirmed: Y. Regression test exists: Y, `test_show_enrichment_result_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404. Gaps: none for this callsite; SQL invariant also checks the captured enrichment_results lookup contains both `"tenant_id"` and `"company_id"`.
- api.catalog.032 - `EnrichmentReviewController::accept` at HEAD uses `EnrichmentResult::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $id)` at `EnrichmentReviewController.php:192-197`. Chained query form confirmed: Y. Regression test exists: Y, `test_accept_enrichment_result_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404 plus foreign result remains `PendingReview`. Gaps: none for this callsite.
- api.catalog.033 - `EnrichmentReviewController::reject` at HEAD uses `EnrichmentResult::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->where('id', $id)` at `EnrichmentReviewController.php:266-271`. Chained query form confirmed: Y. Regression test exists: Y, `test_reject_enrichment_result_rejects_cross_tenant_id`. Test asserts cross-tenant denial: Y, 404 plus foreign result remains `PendingReview`. Gaps: none for this callsite.

## Bar-raising probes

- Hostile-grep results: no surviving `::where`, `::find`, or `::findOrFail` static-call chains were found in `ProductController.php` or `EnrichmentReviewController.php`; the required grep exited 1 with no output.
- Migration verification: tenant_id + company_id confirmed on `products` (tenant_id in `2025_11_30_052910_create_products_table.php`, company_id added in `2025_11_30_130000_add_company_id_to_existing_tables.php:49-54`), `enrichment_results` (both columns in `2026_03_28_100001_create_enrichment_results_table.php:13-30`), and `stock_levels` (tenant_id in `2025_11_30_110000_create_inventory_tables.php:16-29`, company_id added in `2025_11_30_131000_add_company_id_to_stock_tables.php:32-36`). Confirmed: Y for all three.
- SQL-log invariant test analysis: would tests fail if `where('tenant_id')` was dropped? Y for `ProductController::show` and `EnrichmentReviewController::show`: `CatalogTenantIsolationTest.php:1020-1060` captures the products lookup and asserts `"tenant_id"` and `"company_id"` separately; `CatalogTenantIsolationTest.php:1109-1151` does the same for enrichment_results. They are not checking a combined substring, so dropping either predicate would fail the corresponding `assertStringContainsString`.
- Route auth middleware verification: `app/Modules/Product/routes.php:28` wraps categories with `['api', 'auth:sanctum', SetPermissionsTeam::class, EnforceTokenTenantClaim::class]`, and `app/Modules/Product/routes.php:42` wraps products/enrichment routes with `['api', 'auth:sanctum', SetPermissionsTeam::class, EnforceTokenTenantClaim::class, 'module:Inventory']`. Product CRUD is at lines 44-66; enrichment review routes are at lines 106-111. Confirmed: Y.

## Adversarial findings

1. Required PHPStan gate did not pass in this environment. Running `php vendor/bin/phpstan analyse app/Modules/Product tests/Feature/Catalog/CatalogTenantIsolationTest.php` from `apps/api` failed before static analysis with `Failed to listen on "tcp://127.0.0.1:0": Operation not permitted (EPERM)`. Because the required gate did not complete cleanly, this review cannot approve the fix commit even though no tenant-isolation defect was found in the seven reviewed callsites.

2. [CROSS-CLUSTER] `ProductController::index` scopes the products listing only by `company_id` at `ProductController.php:80-82`, even though migrations confirm `products` carries both `tenant_id` and `company_id`. This is not one of api.catalog.027-033 and does not create the same route-id exposure shape, but it is a defense-in-depth mismatch with the two-predicate pattern used in the fixed callsites.

3. [CROSS-CLUSTER] `ProductController::stockLevels` still calculates incoming purchase-order quantity through `DocumentLine::query()->join('documents', ...)->where('documents.company_id', $companyId)->where('document_lines.product_id', $productModel->id)` at `ProductController.php:600-613`, without a `documents.tenant_id` predicate. The product id is already obtained from a tenant/company-scoped product lookup, so this is not a direct cross-tenant read in the reviewed route-id callsite; however, `documents` carries `tenant_id` from `2025_11_30_080000_create_documents_table.php` and `company_id` from the later company-id migration path, so the aggregate should follow the same tenant + company defense-in-depth standard.

## Final verdict and rationale

REQUEST-CHANGES because the required PHPStan gate failed before analysis with an EPERM TCP-listener error, and the review contract says failing gates must be reflected in the verdict. The seven fixed callsites themselves are correctly converted to chained `query()->where('tenant_id')->where('company_id')` forms, hostile grep found no remaining static-call chains in the two controllers, migrations and routes support the intended isolation model, the SQL-log invariants independently assert both predicates, and the PHPUnit, Pint, and sweep inventory gates passed (`36/98`, `73/204` with 32 PHPUnit deprecations, Pint pass, and `verified 1848 event(s) across 356 callsite(s); 0 problem(s)`). Cross-cluster hardening remains for `ProductController::index` and the incoming-stock aggregate in `stockLevels`.
