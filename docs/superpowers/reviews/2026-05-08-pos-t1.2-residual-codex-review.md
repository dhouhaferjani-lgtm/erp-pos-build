VERDICT: BLOCK

Summary: Probed preempts (a), (b), (c), (d), (e), (f), (g), (h), (i), and (j). Scope claims are present in the commit message; the loaded i18n files are `apps/pos/src/locales/{en,fr}/pos.json` and both contain translated `payment.configNotLoaded`; `apps/pos/public/locales/...` does not exist in this tree. The scheduler reference path in this tree is `apps/pos/src/lib/sync/syncScheduler.ts`, not `apps/pos/src/stores/syncScheduler.ts`. Focused tests passed: `pnpm --filter @autoerp/pos test src/components/pos/__tests__/PaymentSummary.test.tsx src/stores/__tests__/paymentStore.refreshFromSQLite.test.ts src/stores/__tests__/terminalStore.preWarm.test.ts src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx`. Typecheck passed. Lint remains blocked by the pre-existing ESLint v9 config-discovery failure.

Findings

BLOCKER (unpreempted)

File: `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:220`

Problematic code:
```tsx
<button
  onClick={onPayCash}
  disabled={checkoutDisabled}
  className="w-full rounded-lg bg-green-600 px-3 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-green-700 active:bg-green-800 disabled:cursor-not-allowed disabled:opacity-50"
>
```

Issue: The refund/exchange net footer bypasses the new `PaymentSummary` payment-config gate entirely. In refund mode, `TransactionCart` renders this separate cash-action button whenever `items.length > 0 && isRefundMode && netTotal !== undefined`; it only honors `checkoutDisabled`, not whether `paymentMethods` and `paymentRepositories` are loaded. That leaves the same pre-config click path open for return/exchange carts: `onPayCash` opens the cash modal, and confirmation reaches `processCashCheckout`, where missing config still throws on the no-cash-method/no-cash-register backstop. The normal sale footer was hardened, but this alternate payment footer was not.

Concrete remedy: Derive the same payment-config readiness in `TransactionCart` for the refund/exchange branch, for example `const paymentConfigReady = (paymentMethods?.length ?? 0) > 0 && (paymentRepositories?.length ?? 0) > 0;`, then disable this net-footer button with `disabled={checkoutDisabled || !paymentConfigReady}`, `aria-disabled={checkoutDisabled || !paymentConfigReady}`, and `title={!paymentConfigReady ? t('payment.configNotLoaded') : undefined}`. Add a `TransactionCart` regression test that renders a return item with `netTotal` and empty `paymentRepositories`, asserts the refund/charge button is disabled, and asserts clicking it does not call `onPayCash`.

Round-count note: Findings total is not >= 2 BLOCKERs across the 3 production surfaces, so the kickoff Section 9 PR-split recommendation is not triggered.
