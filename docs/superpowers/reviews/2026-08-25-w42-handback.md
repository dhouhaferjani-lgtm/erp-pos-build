# HANDBACK — W4-2 (P0) + W4-10 (P1), Session A, 2026-08-25

**Lane** `fix/campaign-w42-opening-cash-float` · worktree `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/w42-cash-float`
**Gate requested** treasury · **Migration** NONE · **CI** UNVERIFIED (S-17)

| | |
|---|---|
| Base | `dev` merged twice — before work and again before this handback; merge commit `cfbc3f5a4`, dev tip at merge `8b0b822c7` |
| Implementation | `6a73930e1` (W4-2 backend) · `ecffad872` (W4-10 + FE) · `a9e437d26` (manifest/ci/runbook) · `6626ebc6c` (reconciler-predicate pin) · `3887589c1` (drop stray symlink) |
| Manifest | **EXIT=0** — Expense `22 → 23`, `gated_ceiling 1169 → 1170`, both named in `apps/api/tests/feature-lane-manifest.json` |
| deptrac | **PASS** — 183 violations on this branch, **183 on dev**; zero involve any file this lane touched (grep-verified) |
| PHPStan L8 | **No errors** (3090 files) | 
| Pint | `--dirty --test` → `pass` |
| Web | `tsc --noEmit` EXIT=0 · `eslint` 0 errors (67 pre-existing warnings, none in touched files) · `vitest` BatchPreview 5/5 |

---

## 1. What was broken

**W4-2 (P0).** Three cash rails that never spoke to each other, and no path from any of them to a
day-one float:

- Treasury → "Adjust balance" refuses a never-seeded repository (`REPOSITORY_NOT_SEEDED`) and, if it
  did not, would credit `PaymentToleranceIncome` — booking the float as **revenue**, sealed into the
  fiscal hash chain.
- The ACCOUNTING opening-balance wizard posted `Dr 53 / Cr 119` and wrote **nothing** to Treasury.
- A transfer out of the resulting empty till fails `INSUFFICIENT_REPOSITORY_BALANCE`.

`MovementSourceType::OpeningBalance` had exactly one reference in the whole API and it was a **read**
(`ReconcileTreasuryCommand.php` `isJournalEntryExempt`). The schema anticipated a writer that did not
exist. Campaign evidence: GL cash 1364.200 vs repositories 209.200.

**W4-10 (P1).** `ExpenseService::create()` defaults `is_paid` to `true`. A payload setting neither
`payment_repository_id` nor `payment_date` minted an expense **born paid with no till**: posting
settled it against the default cash GL account (`Dr 65 + Dr 4456 / Cr 53 45.000`) with **no**
`repository_movements` row, and `/pay` — the only endpoint that accepts a repository — then refused
it as already paid. GL cash fell 45.000; the drawer never moved, irreversibly.

---

## 2. The design, and the two places it departs from the research brief

The wizard's ACCOUNTING template gains an optional **`repository_code`** column. A row carrying it
posts the GL line **and** the repository's opening movement inside `postBatch`'s existing
`DB::transaction`, keyed `opening_balance:{batchId}:repository:{repositoryId}`.

The opening-balance **batch is the justifying document** (document-per-action): it carries the
cutover date, the row, a `Draft → Validated → Locked` lifecycle that seals immutably at post, and now
the treasury leg. No new document type was invented.

Research brief `RESEARCH-opening-float-and-vat-doc-count-2026-08-23.md` §1.7 sketched a different
shape. Two deliberate departures, both worth the reviewer's attention:

1. **The movement carries a `journal_entry_id`** (the batch's own historical entry), not the `null`
   the reconciler already exempts. §1.7 proposed null-JE. Linking it is what makes
   `ReconcileTreasuryCommand` **check 2** — which treats the JE's line(s) on the repository's own
   `gl_account_id` as AUTHORITATIVE — *enforce* `till == ledger` for the life of the tenant rather
   than merely tolerate a divergence. The null-JE exemption is untouched and still covers other
   paths.
   **CORRECTED at gate r1 (F-7).** r1 of this handback claimed a MERGED row (one `Dr 53 1200.000`
   line intended to seed two tills) "would fail check 2's per-line/sum match and freeze the drawer in
   production". The reviewer measured it (PROBE B) and that is **wrong**: such a row validates,
   posts, yields drawer 1200.000 / safe 0.000, and `treasury:reconcile` stays **green** — because the
   one journal line equals the one movement, so check 2 is satisfied. Nothing freezes. The real
   hazard is a **silent over-seed**: one till gets everything and the other stays at zero. See §2.1a
   for what is now refused and what deliberately is not.
2. **No new `MovementReasonCode` case.** §1.7 suggested `opening_float`. `MovementReasonCode` is the
   operator-chosen count-variance vocabulary the "Adjust balance" dialog renders
   (`count_variance | correction | theft_loss | other`); adding a case there would surface "opening
   balance" as an adjustment choice and reopen the exact revenue-booking misuse
   `RepositoryNotSeededException` closes. The movement carries `reason_code = null`;
   `source_type = opening_balance` already carries the meaning.

**Module boundaries.** Accounting depends only on
`App\Shared\Contracts\Treasury\RepositoryOpeningBalanceSeederInterface` and three Shared DTOs — never
on a Treasury model. The `@throws` for the Treasury domain exception is named **in prose** in the
interface docblock, because pint's `fully_qualified_strict_types` fixer converts an `@see`/`@throws`
FQCN into a real `use` statement (the same trap `RepositoryNotSeededException` documents).

---

### 2.1a What a merged cash row does now — the rule, and the rule that was deliberately NOT written

Added in the gate r1 fix round (F-7). For every GL account an ACCOUNTING batch **debits** that at
least one ACTIVE payment repository is linked to, the whole debit on that account must be attributed
to repositories by the rows naming them. Otherwise the batch is refused —
`OPENING_CASH_NOT_FULLY_SEEDED`, at validate **and** authoritatively at post, with the gap listed per
account.

That closes the shape that actually re-opens the P0: a **four-column legacy sheet**. `Dr 53 1200.000`
naming nothing posts 1200.000 into the ledger and zero into every till, permanently, because the
batch locks itself at post. An operator who simply has not heard of the new column would have
reproduced W4-2 exactly.

**The obvious second rule — "every repository on that account must be named" — was written and then
removed.** It refuses PROBE B's merged row, but it is indistinguishable from a shop whose safe is
genuinely empty on day one, and refusing that would block a correct opening. There is no affordance
for an operator to say "this till opens at zero" (a zero row is rejected by the amount rule and would
post no line anyway). So the merged row still posts: drawer 1200.000, safe 0.000. **The ledger and
Treasury still agree** — Σ tills on `53` == GL Dr `53` — which is why `treasury:reconcile` stays
green. It is a data-entry error about *which* till holds the money, not a money error.

That behaviour is now a recorded decision, not an accident:
`test_a_merged_cash_row_over_seeds_one_till_and_is_deliberately_not_refused` asserts it, reconciler
run included, and says in its docblock that a future lane adding an "opens at zero" affordance should
make it fail and rewrite it. Residual **R-9** below carries the ask.

## 3. Item → file:line

### W4-2 — the sanctioned path

| Item | Location |
|---|---|
| Shared contract (why this exists, the path, boundary note) | `apps/api/app/Shared/Contracts/Treasury/RepositoryOpeningBalanceSeederInterface.php:37-77` |
| Intent DTO + per-repository idempotency leg | `apps/api/app/Shared/Contracts/Treasury/DTOs/OpeningFloatIntent.php:17-54` |
| Descriptor DTO (`hasMovements`, `glAccountId`) | `apps/api/app/Shared/Contracts/Treasury/DTOs/OpeningFloatRepositoryDescriptor.php:12-33` |
| Result DTO (`balanceAfter` typed `string` — see §6 R-3) | `apps/api/app/Shared/Contracts/Treasury/DTOs/OpeningFloatResult.php:10-32` |
| Treasury writer — owns no transaction, posts no JE | `apps/api/app/Modules/Treasury/Application/Services/RepositoryOpeningBalanceService.php:35-155` |
| ↳ `describeByCode` (active-only, tenant+company scoped) | `…/RepositoryOpeningBalanceService.php:40-64` |
| ↳ `seed` — transaction assertion, authoritative already-seeded guard, port call | `…/RepositoryOpeningBalanceService.php:66-137` |
| ↳ `hasForeignMovement` — excludes OUR OWN replay key | `…/RepositoryOpeningBalanceService.php:146-155` |
| Typed refusal `REPOSITORY_ALREADY_SEEDED` (mirror of NOT_SEEDED) | `apps/api/app/Modules/Treasury/Domain/Exceptions/RepositoryAlreadySeededException.php:26-48` |
| Container binding to the Shared contract | `apps/api/app/Modules/Treasury/Providers/TreasuryServiceProvider.php:93-99` |
| Seeder injected (constructor, `private readonly`) | `apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php:47-51` |
| Row validation — 5 refusals | `…/AccountingOpeningService.php:245`, `:267-344` |
| Per-batch duplicate-repository census | `…/AccountingOpeningService.php:132`, `:346-373` |
| Post: seed AFTER the JE line, same transaction | `…/AccountingOpeningService.php:471-494` |
| Preview surfaces `repository_code` / `repository_name` | `…/AccountingOpeningService.php:539-545` |
| Ingress rule for the new column | `apps/api/app/Modules/Accounting/Presentation/Controllers/OpeningBalanceBatchController.php:402-406` |
| Guard message now names the working path (en/fr) | `apps/api/lang/en/messages.php:73`, `apps/api/lang/fr/messages.php:73` |

### W4-10 — an expense names the till it left

| Item | Location |
|---|---|
| Typed refusal `EXPENSE_PAID_WITHOUT_REPOSITORY` | `apps/api/app/Modules/Expense/Domain/Exceptions/ExpensePaidWithoutRepositoryException.php:31-46` |
| Guard (fail-closed on an unresolvable method) | `apps/api/app/Modules/Expense/Application/Services/ExpenseService.php:295-320` |
| ↳ applied on `create()` before the transaction | `…/ExpenseService.php:105-109` |
| ↳ applied on `update()` against the RESOLVED post-update state | `…/ExpenseService.php:218-222` |
| 422 `{code,message}` on store + update | `apps/api/app/Modules/Expense/Presentation/Controllers/ExpenseController.php:113-116`, `:240-243`, `:380-391` |
| GL credit prefers the repository's OWN `gl_account_id` | `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:4435-4468` |

### Frontend

| Item | Location |
|---|---|
| `repository_code` in the GL template + comment | `apps/web/src/features/opening-balances/components/FileUpload.tsx:52-59` |
| `GL_COLUMNS` + `AccountingPreviewLine` contract | `apps/web/src/features/opening-balances/types/index.ts:104-119`, `:232` |
| Preview column, rendered only when a row names a till | `apps/web/src/features/opening-balances/components/BatchPreview.tsx:37-39`, `:60-68`, `:82-89`, `:100` |
| Typed error code → translated toast (create + update) | `apps/web/src/features/expenses/hooks/useExpenses.ts:21-34` (`errorCode`), `:44-58` (`expenseWriteErrorMessage`), `:160`, `:192` |
| i18n `openingBalances.preview.repository` en/fr/ar | `apps/web/src/locales/{en,fr,ar}/common.json` |
| i18n `expenses:errors.paidWithoutRepository` en/fr/ar | `apps/web/src/locales/{en,fr,ar}/expenses.json` |

---

## 4. Shapes — drawer 200 / safe 1000 / bank 5000, TND at storage scale 3

Input (the ACCOUNTING template; the drawer and the safe **share** GL `53`, which is what
`PaymentRepositorySeeder.php:120` actually produces):

```csv
account_code,debit,credit,reference,repository_code
53,200.000,0.000,Opening drawer float,CASH-01
53,1000.000,0.000,Opening safe float,SAFE-01
512,5000.000,0.000,Opening bank balance,BANK-01
119,0.000,6200.000,Opening balance equity,
```

`validateBatch` → `valid=true valid_rows=4 invalid_rows=0 debit=6200.000 credit=6200.000
balanced=true`.

`postBatch` → **one** historical journal entry `OB-2026-000001`, `is_historical=true`
(fiscal hash chain deliberately skipped — seeding a till is not a transaction):

```
53   Caisse                Dr  200.000   Cr    0.000   Opening drawer float
53   Caisse                Dr 1000.000   Cr    0.000   Opening safe float
512  Banque                Dr 5000.000   Cr    0.000   Opening bank balance
119  Solde d'ouverture     Dr    0.000   Cr 6200.000   Opening balance equity
```

…and three `repository_movements`, all `direction=in`, all pointing at that same entry:

```
CASH-01  opening_balance  in   200.000   ord=1  balance_after=200.000   journal_entry_id=<OB entry>
         key=opening_balance:{batchId}:repository:{CASH-01 id}
SAFE-01  opening_balance  in  1000.000   ord=1  balance_after=1000.000  journal_entry_id=<OB entry>
         key=opening_balance:{batchId}:repository:{SAFE-01 id}
BANK-01  opening_balance  in  5000.000   ord=1  balance_after=5000.000  journal_entry_id=<OB entry>
         key=opening_balance:{batchId}:repository:{BANK-01 id}
```

`occurred_at = 2026-01-01` (the batch cutover), `reason_code = null`, `currency = TND`.

**W4-10 expense (5.000 from the drawer, `payment_date` in an open fiscal period):**

```
6     Charges               Dr 5.000   Cr 0.000
53    Caisse (drawer's own gl_account_id)  Dr 0.000  Cr 5.000   "Expense payment"

repository_movements: expense / out / 5.000 / CASH-01 / journal_entry_id = the expense JE
drawer balance: 200.000 → 195.000
```

A VAT-bearing expense adds `Dr 4456 TVA déductible` for the deductible split — untouched by this
lane, and `writeDeductibleVatSnapshot` still runs on both branches.

---

## 5. Evidence

### 5.1 Red-proof (before any implementation existed)

Both test files were authored first and run BY PATH on sqlite. Captured verbatim:

```
   FAIL  Tests\Feature\Accounting\OpeningCashFloatSeedsRepositoryTest
  ⨯ posting an accounting batch seeds every named repository and matche…
  ⨯ re posting the same opening row is idempotent
  ⨯ a repository that already holds money is refused at validation
  ⨯ an unknown repository code is refused at validation
  ⨯ the row account must be the repositorys own gl account
  ⨯ a credit row cannot name a repository
  ⨯ the same repository cannot be named twice in one batch
  ⨯ post refuses a repository that already holds money

   FAIL  Tests\Feature\Expense\ExpensePaidFromRepositoryTest
  ⨯ a cash expense paid from a repository moves that repository
  ⨯ a born paid expense without a repository is refused
  ⨯ the api returns a typed 422 for a born paid expense without a repos…
  ✓ a non cash payment method may be paid without a repository
  ✓ an unpaid expense needs no repository
  ⨯ updating an expense to paid without a repository is refused
```

Headline failure messages:

- `Failed asserting that two strings are identical. -'200.000' +'0.000'` — the drawer stayed at zero:
  the exact W4-2 defect.
- `Target class [App\Shared\Contracts\Treasury\RepositoryOpeningBalanceSeederInterface] does not exist.`
  — no writer existed at all.
- `Expected response status code [422] but received 201.` — a born-paid, till-less expense was
  accepted.
- `Failed asserting that exception of type "…ExpensePaidWithoutRepositoryException" is thrown.` ×2.

The two that passed red are honest negatives (they assert the carve-outs — non-cash method, unpaid
expense — which the old code also allowed); they are kept so a future over-tightening of the guard
fails here.

### 5.2 Green — sqlite, BY PATH, on the merged tip

```
   PASS  Tests\Feature\Accounting\OpeningCashFloatSeedsRepositoryTest        8 tests
   PASS  Tests\Feature\Expense\ExpensePaidFromRepositoryTest                 6 tests
   PASS  Tests\Feature\Accounting\OpeningBalancePreviewContractTest          4 tests
   PASS  Tests\Feature\Expense\ExpenseIdempotencyTest                        1 test
   PASS  Tests\Feature\Accounting\Reports\UpcomingPaymentsTest               4 tests
  Tests:  23 passed (118 assertions)
```

### 5.3 Green — PostgreSQL 16, throwaway `autoerp_test_w42` (127.0.0.1:5433, `autoerp`/`autoerp_secret`)

```
Tests:  19 passed (86 assertions)   # the four files above minus the preview contract
```

Earlier full PG run of the two lane files alone: `14 passed (53 assertions)`.
The database was created for this lane and **dropped afterwards**.

### 5.4 Repository-vs-GL equality, measured on the throwaway PostgreSQL

Driven through `AccountingOpeningService` end-to-end (not a fixture):

```
validate: valid=true valid_rows=4 invalid_rows=0 debit=6200.000 credit=6200.000 balanced=true
posted JE OB-2026-000001 is_historical=true

CASH-01  balance=200.000    gl_account=53
SAFE-01  balance=1000.000   gl_account=53
BANK-01  balance=5000.000   gl_account=512

Σ treasury on 53   = 1200.000   GL debit on 53  = 1200.000   EQUAL=true
Σ treasury on 512  = 5000.000   GL debit on 512 = 5000.000   EQUAL=true
```

All three movements carried `journal_entry_id` == the posted entry (`je=SAME` in the probe output).

**CORRECTED at gate r1 (§c).** r1 said the reconciler "was NOT run" because it "needs a provisioned
per-tenant DB". That was a wrong conclusion drawn from one failed invocation: the probe passed
`--company`, which the command does not accept, and the follow-up ran without clearing
`CompanyContext`. `--tenant` is the established in-test pattern
(`tests/Feature/Treasury/ReconcileTreasuryTest.php:243` and two siblings), and the reviewer ran it
green. **The real command is now pinned inside the headline test**
(`OpeningCashFloatSeedsRepositoryTest.php`, end of
`test_posting_an_accounting_batch_seeds_every_named_repository_and_matches_gl`):
`CompanyContext::clear()` (rule 20 — the command is a console-shaped caller with no bound company),
then `assertSame(0, Artisan::call('treasury:reconcile', ['--tenant' => …]))` and an assertion on
`froze 0`. `treasury:reconcile` FREEZES a repository on cash drift, so a green run over three
freshly-seeded repositories is the strongest available statement that the movements and their
journal entry agree. The mirrored check-2 predicate stays alongside it as the finer-grained pin.

### 5.5 Inherited red — measured on `dev`, not assumed

| Suite | This branch | `dev` baseline | Verdict |
|---|---|---|---|
| `tests/Feature/Treasury/` (whole dir) | 12 failed / 54 skipped / 1051 passed | `AdvanceReversalGlShapeTest` 11 + `RepositoryMovementsEndpointTest` 1 = **12 failed** on dev | INHERITED |
| `tests/Feature/Accounting/` (whole dir) | 6 failed / 13 skipped / 842 passed | `InvoiceAndCreditNoteGLIntegrationTest` **5 failed** on dev | 5 INHERITED, **1 was mine** (see below) |
| `tests/Feature/Expense/` (whole dir) | 1 failed → fixed | — | **1 was mine** |

Two real breaks this lane caused, both fixed, both fixture-shape and not subject-shape:

- `ExpenseIdempotencyTest` posted `{'total','is_paid'=>true,'idempotency_key'}` with no repository —
  precisely the shape now refused. Fixed by giving it a repository
  (`tests/Feature/Expense/ExpenseIdempotencyTest.php:31-52`); the test's subject (the idempotency
  key) is unchanged. The guard sits **after** `create()`'s idempotency short-circuit, so a replay of
  an already-created expense is still returned without re-evaluating it.
- `UpcomingPaymentsTest` built its "paid expense must not appear" fixture the same way. Fixed by
  naming a till **and giving that till its float through the new W4-2 path** — because a till only
  pays out what it holds, the naive fix hit `INSUFFICIENT_REPOSITORY_BALANCE`. That second failure is
  the feature working: `tests/Feature/Accounting/Reports/UpcomingPaymentsTest.php:116-146`.

Seeders were checked: `DemoPharmacySeeder.php:1485` already names a repository on every paid
expense, so no seeder breaks. `grep` over `app/` + `database/` found no other `is_paid` writer.

---

## 6. Residuals — seen, deliberately NOT touched

- **R-1 — the drawer and the safe share GL account `53`.** `PaymentRepositorySeeder.php:120` links
  both to the `Cash` purpose account, so a drawer→safe remittance posts no journal entry and cash
  location is invisible in the ledger (the campaign report flags this too). W4-2 works correctly
  either way — the per-row match handles two lines on one account — but the seeded provisioning
  choice is the owner's. The TN chart ships `531 Caisse siège`.
- **R-2 — `reference` vs `description`.** The ACCOUNTING template offers a `reference` column that
  the backend never reads (it validates `description`), so the operator's audit reference is silently
  dropped. One-word fix, adjacent to files this lane touched, deliberately left alone (rule 4). The
  new `repository_code` column does NOT share this defect — it is validated end to end.
- **R-3 — `MovementResult::$balanceAfter` is typed `string`, documented "numeric-string".**
  `OpeningFloatResult` mirrors it rather than narrowing to `numeric-string`, which would have pushed
  a PHPStan error onto six `new MovementResult(...)` sites in `TreasuryMovementService` — out of lane.
  Tightening both together is a clean follow-up.
- **R-4 — the W4-10 carve-out is method-shaped, not rail-shaped.** A non-cash method born paid with
  no repository is allowed and still credits the **Cash** purpose account via `createFromExpense`'s
  `RepositoryType::CashRegister` default. That is the brief's scope boundary ("unless a non-cash
  payment method is chosen"), but a card/transfer expense arguably belongs on a bank account. Worth
  an owner ruling before a second tenant.
- **R-5 — a repository with `gl_account_id = NULL` is accepted** on an opening row and seeded against
  whatever account the row debits. The account-match refusal can only fire when the link exists.
  `PaymentRepositorySeeder` warns and leaves the link null when the chart is missing, so this is
  reachable on a mis-provisioned tenant.
- **R-6 — no backfill for tenants already seeded GL-only. REWRITTEN at gate r1: r1's remedy was
  money-wrong.** r1 told such an operator to "move the cash in with a Treasury transfer from a till
  that does hold it, or post a new opening batch naming it". Both arms are wrong for the tenant
  actually in R-6: a GL-only-seeded tenant has **no** till holding cash, so the transfer arm is
  unusable; and a second opening batch debits `53` **again**, doubling GL cash against unchanged
  physical cash. The honest statement is that **there is no in-product remedy** for an already-Locked
  GL-only batch — the only writer of an `opening_balance` movement is `postBatch`, and it always
  posts a GL leg alongside. The owner has confirmed there are **no existing tenants**, so the
  disposition is **greenfield: re-provision, no backfill**, and the runbook now says exactly that
  (`docs/handoff/RUNBOOK-opening-cash-float-2026-08-23.md` §4). If a GL-only tenant ever does exist,
  it needs a repair command, not a runbook paragraph.
- **R-9 (NEW, gate r1 F-7) — a till cannot declare that it opens at zero.** The coverage refusal
  stops short of "every repository on the account must be named" only because that case is
  indistinguishable from an empty safe. An explicit affordance — a zero-amount repository row, or a
  checkbox in the wizard — would let the batch refuse the merged row too. Needs an owner call on the
  operator flow; not guessed at here.
- **R-10 (NEW) — a cash account modelled outside Treasury now blocks its own opening.** If a tenant
  genuinely holds ledger cash on an account that also carries repositories but is not itself a till
  (petty cash, say), the coverage rule refuses the batch and the only way through is to model that
  cash as a repository. That is arguably the correct pressure — cash in the ledger and in no till is
  what W4-2 exists to stop — but it is a new constraint and the owner should know it exists.
- **R-11 (NEW, gate r1 F-3) — the non-cash carve-out still writes no treasury movement.** It now
  credits `payment_methods.default_account_id` (else the BANK purpose account) instead of `53`, so it
  no longer breaks the Σ-tills == GL-cash equality. But a card or transfer expense arguably ought to
  decrement a bank REPOSITORY, which would mean adopting the method's `default_repository_id`. That
  was NOT done: silently adopting a repository the operator never named would make the W4-10 guard
  unreachable through that door. Owner ruling.
- **R-7 — `TREASURY_SHIFT_VARIANCE_GL_ENABLED` stays `false`.** W4-2 closes the *float* half of SV-3.
  The deposit/payout half (SV-3's other half) and SV-4 (`recordOpening` fires no event) are untouched,
  so the pre-enable gate is **not** cleared. `config/treasury.php:28` unchanged.
- **R-8 — `docs/modules/treasury.md` not updated.** It documents payment methods and instruments and
  has no opening-balance section; the runbook is the right home and carries the change.

---

## 7. Migration / fleet risk

**None.** No schema change. `repository_code` rides in the existing JSONB `raw_data` / `mapped_data`
(`OpeningBalanceBatchService::canonicalizeRow` passes unknown fields through untouched, so no
canonicalization change was needed either). No backfill, no census query, no fleet-abort risk.

The two preview keys (`repository_code`, `repository_name`) ARE an API contract change; the
key-set pin was updated in the same commit
(`tests/Feature/Accounting/OpeningBalancePreviewContractTest.php:129`) and the FE type follows it.

## 8. CI

`.github/workflows/ci.yml` gained two names in the `backend-test-pgsql` `--filter` allowlist —
`ExpensePaidFromRepositoryTest` and `OpeningCashFloatSeedsRepositoryTest` — with the justification
comment above the `env:` block, following the CorrectingEntryEndpointTest / SupplierGoodsReturnNoteTest
/ ResolveLineEntryCodeCostRedactionTest precedent. `Feature/Expense` is PARKED behind
`vars.SELF_HOSTED_RUNNER_READY`, so unnamed the W4-10 guard would execute on no event.
`Feature/Accounting` IS live, and its class is named anyway so the two halves of one invariant
(`treasury == GL`) fail together rather than one silently.

ci.yml is a workflow-touching surface, so the **S-14 dispatch-verification leg applies** to the
promotion carrying this. **S-17 caveat:** CI has not been observed green on this branch.

---

# FIX ROUND r1 — treasury gate r1 (2026-08-25)

Gate: `docs/superpowers/reviews/2026-08-25-w42-cash-float-gate-r1-treasury.md` —
**spec ✅ (core) / quality CHANGES-REQUESTED**, merge-blocking on F-1 and F-6.

**Commits** `4250c54a0` (F-1..F-12) · `40989770c` (PHPStan L8 + pint) ·
`8d67e0f6f` (ExpensePostTest inverted, lint parity). Base re-merged from `dev` at `cb0a2d632`.

## Findings → disposition

| # | Disposition | Where |
|---|---|---|
| **F-1** blocking | **FIXED.** `GL_COLUMNS` was the REQUIRED-header set `FileUpload` gates on, so adding `repository_code` to it refused every pre-existing four-column ACCOUNTING sheet ("missing columns: repository_code") and bricked the day-one wizard. Split out `GL_REQUIRED_COLUMNS`; the chip list still shows the optional column, marked `(optional)` in en/fr/ar. | `types/index.ts:231-241`, `FileUpload.tsx:38-70`, `:81-82`, `:98-100`, `:186-201` |
| **F-6** blocking | **RESOLVED to 1174**, not 1170. dev had moved to 1173 (Document 83→84, Inventory 111→114 with the W2-7 squash); the lane adds one Expense class. The merge conflicted on exactly that line; Document/Inventory taken from dev verbatim, and the note prose re-taken so nobody resolves it to 1170 later. | `tests/feature-lane-manifest.json:9`, Expense note |
| **F-2** | **FIXED.** The guard now also runs at `ExpenseService::post()`. The controller's `catch` on `post()` was dead code and is now live. | `ExpenseService.php:391-403` |
| **F-3** | **FIXED, the fuller way.** With no repository the credit resolves to `payment_methods.default_account_id`, else the **BANK** purpose account — never Cash. A card payment did not come out of a drawer. | `GeneralLedgerService.php` `paymentMethodAccountForExpense()` + the `else` arm of the paid branch |
| **F-4** | **FIXED.** New case with a till on a distinct `5311`: asserts the credit lands there **and** that the Cash purpose account carries nothing. Red-proven by reverting the branch. | `ExpensePaidFromRepositoryTest.php` `test_the_credit_lands_on_the_tills_own_account_…` |
| **F-5** | **FIXED.** `RepositoryAlreadySeededException` is mapped in `OpeningBalanceBatchController::post()` to its `ERROR_CODE` with `messages.treasury.repository_already_seeded` (en/fr); the new coverage refusal likewise, carrying `error.gaps`. Two API tests pin both. | `OpeningBalanceBatchController.php` post() catches; `lang/{en,fr}/messages.php` |
| **F-7** | **FIXED + handback CORRECTED.** New refusal `OPENING_CASH_NOT_FULLY_SEEDED` for a cash debit that reaches no till. The merged row stays legal, deliberately and now testably — see §2.1a and R-9. The "would freeze the drawer" claim in §2.1 is corrected: measured, it does not freeze; it silently over-seeds. | `AccountingOpeningService::openingCashCoverageGaps()`, `OpeningCashNotFullySeededException.php` |
| **F-8** | **FIXED.** The `PaymentMethod` lookup in the guard is `tenant_id` + `company_id` scoped. | `ExpenseService.php:304-312` |
| **F-9** | **FIXED.** `update()` called with two arguments. | `ExpensePaidFromRepositoryTest.php` |
| **F-10** | **FIXED.** `openingBalances.preview.repositoryHint` is rendered under the accounting preview table when a line seeds a till. | `BatchPreview.tsx:110-118` |
| **F-11** | **FIXED, narrowed.** Post refuses when a row **naming a repository** failed validation — that float would vanish from ledger and Treasury while `119` absorbed it, on a write-once batch. Ordinary invalid GL rows keep their pre-existing behaviour; widening that is a separate lane. | `AccountingOpeningService::postBatch()` |
| **F-12** | **FIXED.** `seed()` asserts `batchId` names a real batch of this tenant+company (read through the table, so Treasury imports no Accounting model). Both fixtures now post a real batch. | `RepositoryOpeningBalanceService.php:100-118` |
| **§(c)** reconciler | **FIXED + CORRECTED.** `treasury:reconcile` is pinned inside the headline test and inside the merged-row test. The r1 claim that it could not run was wrong — see §5.4. | `OpeningCashFloatSeedsRepositoryTest.php` |
| **R-6** | **REWRITTEN.** r1's remedy would have doubled GL cash. Now: no in-product remedy; greenfield re-provision (owner: no existing tenants). | §6 R-6, RUNBOOK §4 |

## Tests after the fix round

| Leg | Result |
|---|---|
| `OpeningCashFloatSeedsRepositoryTest` (8 → **15** cases) | green |
| `ExpensePaidFromRepositoryTest` (6 → **9** cases) | green |
| sqlite, lane + adjacent by path | **33 passed / 150 assertions** |
| **PostgreSQL 16**, throwaway `autoerp_test_w42` @ 5433 | **34 passed / 135 assertions** (includes the PG-only advisory-lock case, skipped on sqlite) |
| `tests/Feature/Expense/` (whole dir) | **140 passed**, 1 skipped, 0 failed |
| `tests/Feature/Accounting/` (whole dir) | 850 passed, 13 skipped, **5 failed — INHERITED**: `InvoiceAndCreditNoteGLIntegrationTest`, `DeliveryRequiredBeforeInvoiceException`, **5 failed on `dev` too** (measured, main checkout) |
| web `vitest` (opening-balances + expenses) | **17 files / 120 passed** |

**Red-proof for the fix round** — the three new expense cases, run against the reverted production
code (`ExpenseService::post()` guard removed, `GeneralLedgerService`'s repository/non-cash branch
restored):

```
⨯ the credit lands on the tills own account not the cash purpose acco…   ModelNotFoundException
⨯ posting a draft that is paid with no repository is refused             Failed asserting that exception … is thrown
⨯ a non cash expense credits the methods own account and never cash      ModelNotFoundException
```

F-1's red, against `GL_COLUMNS` as the required set:

```
× accepts a legacy four-column CSV that carries no repository_code
  → Unable to find an element with the text: openingBalances.upload.rowsFound
✓ parses a CSV that does carry repository_code and passes it through
✓ still refuses a CSV missing a genuinely required column
```

## Gates after the fix round

| Gate | Result |
|---|---|
| PHPStan L8 | `[OK] No errors` (3097 files). 9 errors were introduced and fixed properly — `is_numeric` guards rather than casts (rule 19), and `list<>` shapes extracted into named helpers rather than widened |
| Pint | `--dirty` pass; the new Accounting Domain exception names Treasury's sibling **in prose** so `fully_qualified_strict_types` cannot mint a cross-module `use` |
| deptrac | **183 on this branch == 183 on dev `3bbe28480`**; zero violations name any file this lane touched |
| manifest | **EXIT=0**, `gated_ceiling 1174`, Expense 23 |
| web `tsc` | clean |
| web eslint | **6451 warnings, 0 errors — identical to dev's 6451.** The lane adds none. NOTE: `scripts/lint-warning-baseline.json` says 6449, so **dev is already +2 over its own baseline** — inherited drift, deliberately NOT re-baselined here |
| `scripts/lint-ratchet.mjs` | **could not execute in this worktree** — `ERR_MODULE_NOT_FOUND: globals` through the symlinked `node_modules`. Measured by hand instead (the row above), which is what the script computes |

## Two housekeeping notes

- `.gitignore` gained a bare `node_modules` line: the existing `node_modules/` has a trailing slash,
  which matches a directory but **not a symlink** of that name, so the symlink a worktree needs to run
  the web toolchain was staged twice by `git add -A` during this lane. One line, commented.
- **Not done, and named rather than buried:** the coordinator's suggested check "Σ repository
  openings on account 53 ≠ Σ GL Dr 53 rows **naming repositories**" is vacuous as written — those two
  sums are equal by construction, because each naming row seeds exactly its own amount. What shipped
  compares the attributed sum against the **total** debit on the account, which is the version that
  actually catches anything.
