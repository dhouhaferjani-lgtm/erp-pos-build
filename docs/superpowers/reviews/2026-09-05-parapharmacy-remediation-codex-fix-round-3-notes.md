# Parapharmacy remediation — Codex spec fix round 3 notes

Date: 2026-09-05. **Spec v4 is ready for Fable gate r4 (treasury + authz).** This is a narrow documentation revision, not implementation or gate approval. Fable remains the sole orchestrator, reviewer dispatcher, committer, merger and promoter.

Read the [round-3 handover](../../handoff/HANDOVER-parapharmacy-remediation-codex-spec-fix-round-3-2026-09-05.md) in full first, followed by [r3 synthesis](2026-09-05-parapharmacy-remediation-gate-r3-synthesis.md) and all four linked lenses: [fiscal](2026-09-05-parapharmacy-remediation-gate-r3-fiscal.md), [inventory](2026-09-05-parapharmacy-remediation-gate-r3-inventory.md), [treasury](2026-09-05-parapharmacy-remediation-gate-r3-treasury.md), [authz](2026-09-05-parapharmacy-remediation-gate-r3-authz.md). Findings retain their reviewed source baseline `fa000edc3`; this round does not claim a deployed behavior or reopen the six CLOSED r2 blockers and seventeen CLOSED majors.

## Deliverables

- [Spec v4 in place](../specs/2026-09-05-parapharmacy-readiness-remediation-design.md), with v3→v4 change log and narrow dispositions below.
- [Glossary touch-ups](../../glossary.md): existing classification enum/TS source, DIRECT_DEBIT exception, journal-preserving non-money semantics, terminal authority, scoped evidence/permission rollout and D-J0-8 correction of record.
- This note, including the unchanged twelve-row OPEN decisions register.

## Blockers — specified corrections

| Finding | v4 disposition and citation |
|---|---|
| R3-B1 / treasury NEW-1 / fiscal NEW-1 | W2 retains the shared POS payment entry for every leg: debit-only account substitution and repository-movement suppression, using TreasuryReceiptBridge.php:1457–1458,1515–1521,1556–1558. GeneralLedgerService.php:3944–3966 remains the sole proportional revenue/per-rate VAT writer. The same public entry contract must accept a validated non-money debit destination; the existing repository/GL guard at :3915–3921 remains for money legs, so no null/fake repository or early return is prescribed. Store vouchers retain Dr PosTenderClearing and the existing redemption offset (ReceiptPaymentService.php:372–385; PosCoreReceiptProjection.php:1616–1632). The bypass TODO at GeneralLedgerService.php:2960–2962 is superseded in design, never applied literally. Existing configured voucher accounts and posted history remain; ambiguous legacy legs block. Added full/mixed voucher and mixed cash/LOYALTY acceptance: revenue + sealed per-rate VAT + correct debit, clearing nets, zero non-money repository movements, repeat delivery, W4-9 identity. Named purpose seeding is M10 below |
| R3-B2 / authz B-r3-1 | W2 owns the new token/session-to-terminal proof and projection, with allowed persisted-location custody at enrollment and verification on configuration/binding requests. It reads pos_terminals.location_id server-side and cannot use a caller terminal/location label as authority. ClaimTerminalRequest.php:33–40 and PosPaymentPolicyController.php:13–20,29–33 are explicitly insufficient precedents; terminal location is already stored (TerminalController.php:134,401,433), while claim proof/session binding is new work. Preserve current pos.operate_terminal claim gate (:372), add location/proof enforcement and reset/reclaim generation invalidation. Forged terminal_id, hardware/header-only proof, unbound token, foreign tenant/company and A→B read/bind cases now have explicit acceptance rows. Already sealed offline facts remain accepted with historical attribution or blocked Treasury. These are W2 prerequisites before projection activation; W1 human scoping waits for the completed transition |

## Majors — all accepted and addressed in the spec

Identifiers M01–M11 below follow the handover/synthesis topics; the original lens identifiers remain attached. No listed major is rejected or left as an unexplained omission.

| ID / lens finding | v4 disposition and evidence |
|---|---|
| M01 / fiscal NEW-2 (drawer residual) | W6 names ProductDetailDrawer.tsx:20,114–116,297–302 and its organism re-export. Iteration 1 gates-and-defers stock_lots data; module-off/unknown hides the tab, stale selection falls back without fetching, and later data must reuse the W6 cache/freshness/provenance. other_branches remains deferred aggregate stock with no lot leakage. R2, seams and acceptance include the drawer. This takes ownership of the caisse spec's :157–158,206 deferral without building a second online pipeline |
| M02 / fiscal NEW-3 | Replaced the unobservable offline-drain condition with seal-time authored classification in the immutable policy revision under either D8 transport. Local atomic persistence and old-revision readers govern first projection, change netting, replay and W7. An offline terminal with unsynced receipts is an explicit acceptance case; ambiguous legacy history requires bounded cutover evidence/per-leg resolution. TreasuryReceiptBridge.php:961–967 describes the mutable-class netting conflict; no server outbox-empty assertion remains. D8/R3 and the single version roadmap are unchanged |
| M03 / authz N-1 | W2 states treasury.manage grant reality: admin via RolesAndPermissionsSeeder.php:547, accountant :818, absent from manager :590. D5 determines designated manager grants; no blanket manager grant is assumed. Per-tenant approved delta/reseed, custom-role preservation, permission-cache invalidation and readiness gate precede API/web rebind activation, with designated/undesignated access acceptance |
| M04 / authz N-2 | R2/L6 distinguish entitled, positively not_entitled (non-alerting), and entitlement_unresolved (alerting). W4 ingest obligation orchestration writes blocked fiscal_event_projections for expected gated projectors; registry exclusion at FiscalEventProjectionRegistry.php:248–266 is insufficient. Embedded POS lot arms gain proposed fiscal_projection_lot_obligations child evidence. POS projection owns effect/status writes: pending obligation persists with aggregate effect before contained lot work; unresolved/failed lot work remains blocked while normal receipt work commits. W4 re-drive calls that same lot-only owner with stable movement/source identity, never replays aggregate or bypasses receipt idempotency. Existing projection detail and census show unresolved coverage. PosCoreReceiptProjection.php:2169–2183 currently logs the contained failure without this mechanism; ApplyFiscalEventProjectionJob.php:124–130 assumes ingest gating passed |
| M05 / authz N-3 | Company ownership check executes in an initialized-tenant public adapter/orchestration, not the central-only never-throws DefaultModuleActivationResolver.php:39–43,57–70. companies is tenant-local (2025_11_30_104000_create_companies_table.php). HTTP/QueueTenancyBootstrapper callers assert bound tenancy (config/tenancy.php:42); central console/scheduled callers initialize/tear down each persisted tenant (EnqueueResolvedEventProjectionsCommand.php:132–138). Preserve typed lookup failures before any boolean adapter so false cannot silently mean disabled. Failure leaves alerting unresolved work; no central companies query |
| M06 / authz N-4 | L1 requires expiring location_id UUID validation before scope resolution/service call, matching expired validation at BatchController.php:239–243 and preserving its scope at :252. The raw PG predicate at FEFOInventoryService.php:852–856 must never receive malformed UUID input; acceptance specifies 422 for malformed and established scoped 404/403 for foreign/out-of-scope, never 500 |
| M07 / inventory N-1 | W5 explicitly suppresses both postCountCorrection heuristic arms on new tracked lot-grain counts: positive DEFAULT auto-credit at StockAdjustmentService.php:1446–1447 and negative FEFO draw at :1448–1449. Apply observed lot legs and aggregate once in one transaction; L9 precedes finalization. Added +5 identified-lot-after-L9 acceptance with zero DEFAULT re-inflation. Today's legacy positive top-up is already remainder-bounded (:1472–1475; BatchStockService.php:255–300); preserve, do not claim that bound as new |
| M08 / inventory N-2 | L6 requires a dedicated LotLedgerDriftCensusTest plus command/scheduler coverage for nonempty drift/clean/entitled/not_entitled/unresolved/missed-run cohorts on a confirmed-live PostgreSQL lane before scheduling activation. LotLedgerDriftCensus.php:72,75–91 is PG-specific; PosCoreReceiptProjectionBatchLotTest.php:405–408,883–886 documents the parked/filter/PG-skip hazard. The later L6 brief must identify the live job and revision; SQLite, empty cohorts or incidental projection coverage cannot satisfy it. No test file or CI lane was changed in this round |
| M09 / treasury NEW-2 | W2 and glossary name PaymentInstrumentKind.php:26–35,37–42,63–80 and mandatory paymentMethodKind.ts:22–26 mirror as existing sources. Classification uses canonical normalized method code + declared instrument/operation + flags, not an independent list. Census includes payment-method validation and both writers; store_voucher/restaurant_voucher/gift_card are declared separately from the seed codes. MEAL_VOUCHER is not restaurant_voucher. Unsupported restaurant/gift redemption remains unavailable under D2 |
| M10 / treasury NEW-3 (also R3-B1) | W2 names proposed SystemAccountPurpose::LoyaltyLiability / loyalty_liability and MealVouchersReceivable / meal_vouchers_receivable, country-seeded and CONDITIONAL in ProvisioningRequiredPurposesV1, required exactly once before tender enablement. Existing company tags/mappings get reviewed backfill, second-company and rerun proof; account numbers remain configurable. No purpose exists at baseline (SystemAccountPurpose.php:108–112; InstrumentAccountPurpose.php:9–17); RedemptionProcessingService.php:28,105 only moves points. Loyalty opening/accrual evidence is required, not inferred from points. Missing purpose/economic coverage blocks tender readiness. Use the existing PosTenderClearing manifest pattern at ProvisioningRequiredPurposesV1.php:57 |
| M11 / treasury NEW-4 | W2 is the sole terminal authority/projection transport/content owner. §5 removes W2's W1-projection prerequisite; W1's human-scoping activation waits for W2 and device migration, while preparatory W1 work need not block W2. This resolves the v3 §5 W1↔W2 circular rows without altering the device-consumer-before-scoping gate |

## Minors — disposition

| Finding | Correction and citation |
|---|---|
| Fiscal NEW-4 | W7 receipt_count pointer now zReportService.ts:753; :749–752 hash bounds retained. Refresh the stale ZReportProjection.php:289 docblock pointer in the first authorized W7 lane, not this document-only round |
| Inventory N-3 | L3 anchors BatchDetailPage.tsx:271–299 float formatting/summed totals and features/batches/types.ts:38–39,281; explicitly requires no-parsefloat-on-money coverage alongside PHPStan and end-to-end exact quantity conversion |
| Inventory N-4 + authz n-1 | lot_identifications and tender_binding_resolutions each specify unique (tenant_id, company_id, operation_uuid), persisted company/location and prohibition of tenant-only operation uniqueness. Source/predecessor locking remains separate; second-company UUID behavior is explicit. Matches the count-observation scope discipline and avoids the catalogue-only ratchet blind spot (TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:30–54) |
| Inventory N-5 | W5/L9 retain StockMovementRecorded/V2 audit/event symmetry; the unchanged newStockLevel is a debounced channel no-op, not a new delta. Cite StockAdjustmentService.php:1756–1805 and Channel/Providers/ChannelServiceProvider.php:29; acceptance includes unchanged published quantity on replay |
| Inventory N-6 | WAC canonical lock-order citation corrected to WeightedAverageCostService.php:92–96; no new lock order chosen |
| Authz n-2 | L9 explicitly reseeds/grants existing tenants, preserves custom-role deltas, invalidates permission caches and blocks activation until ready; admin only receives the new permission after the real seeder rerun (RolesAndPermissionsSeeder.php:547) |
| Authz n-3 | Existing Identify/split action explicitly uses web hasPermission('batches.identify') plus API gate, BatchExpiry and location custody; acceptance covers both layers, not inferred visibility |
| Treasury NEW-5 | Glossary records the old LEDGER D-J0-8 cross-company repository-index claim as incorrect at reviewed source, not silently dropped: PaymentRepositoryController.php:39–41,58–60 already scopes company. W1's real staff-location gap and tenant-wide HTTP code-validation gap remain unchanged; historical ledger/review files were not edited |
| Treasury NEW-6 | Glossary explicitly classifies DIRECT_DEBIT (has_maturity, InstrumentKind::Other) as electronic settlement, outside cheque/effet handling (PaymentMethodSeeder.php:288; HandlesMaturityTenderLeg.php:33–37) |
| Treasury NEW-7 | Source pointers tightened: account-charge authoring paymentStore.ts:898–923; LOYALTY PaymentMethodSeeder.php:194–208 |

## Preserved scope and decisions

The r2 Preserve paragraph and §3.2 invariants remain verbatim. The D7/D8 single roadmap and all twelve decision rows remain unchanged. The r1/v2 voucher-bypass retraction, twelve-code seed census, L9 ordering, flat movement/no-GL behavior and true no-op guard, inherited tenant entitlement (no independent company licensing), seven-role delta, W4 shared recovery authorities, W7 bounded independence and RD4 gates remain intact. No sealed bytes, source artifacts, prior reviews or closed findings were changed.

## Full owner-decisions register — unchanged, all OPEN

| ID | Status | Decision and recommended default | Alternative and consequence / gate |
|---|---|---|---|
| D1 | OPEN | Confirm branches are locations of one legal company | Separate legal entities require intercompany design; never model cross-company custody as an internal branch transfer |
| D2 | OPEN | Confirm POS/B2B usage and exact enabled tenders; release only the selected surfaces/methods | Either/both surfaces allowed after their acceptance; unused paths remain excluded, not marked tested |
| D3 | OPEN | Keep R3 iteration 1 as suggestion-only shelf guidance; recommend captured-lot capability as iteration 2 when physical traceability is required | Staying on shelf control requires a written expiry/recall procedure, estimate/unknown labels, no software-captured traceability claim and a dated revisit; if capture is a launch requirement, defer launch of that use case until iteration 2 |
| D4 | OPEN | Recommend refreshing branch eligibility before opening a session; permit cached guidance during that session with visible snapshot age/staleness; require refresh before next session | Owner may choose a stricter age cutoff or longer offline tolerance, each with explicit unavailable/stale behavior; applies to iteration 1 cache and later capture. No silent hard cutoff or invented expiry; no guarantee of knowing recalls issued while offline |
| D5 | OPEN | Branch managers operate allowed custody; central treasury has explicit all-location authority | Company-wide manager custody is possible only as an explicit policy; approve source/destination reach before W1 execution |
| D6 | OPEN | Exclude cross-branch returns from initial release | Enabling requires receiving-branch stock/lot disposition, original provenance, payout custody and two-branch acceptance |
| D7 | OPEN | Iteration 2 only: recommend optional line-level lot fields in a new `SALE_RECEIPT` vN+1, required by captured-mode capability | Inline evidence uses the receipt's SQLite transaction/outbox and ordering; sealed errors need append-only corrections. A separate versioned evidence stream preserves fiscal shape but adds linkage/hash checks, local atomicity, reordered arrivals and conflicts. It still needs append-only corrections. Neither transport is selected here; no iteration 1 lot version/outbox work depends on D7 |
| D8 | OPEN | Tender-binding transport: recommend a required validated sealed repository/binding reference for money-destination legs in the single future SALE_RECEIPT version, subject to the owner confirming timing under R3 | Alternative: no sealed change, with an unsealed authored policy-revision snapshot plus durable server binding. This avoids sealing Treasury policy but needs authenticated linkage, immutable server revisions and conflict detection; an unsealed field alone is not fiscal evidence. Sealed bindings need append-only repair if unresolvable. Both options use one D7/D8 roadmap; no iteration-1 payload change is authorized while this remains open |
| D9 | OPEN | Recommend retaining BatchExpiry as always-on for parapharmacy, matching current vertical defaults | Alternative: make it deactivatable with an explicit effective-module exclusion model, server/device invalidation and historical-lot cohort policy; additive extras alone cannot disable it. Worker entitlement fixes and retail module-off acceptance are required either way; no deactivation rollout before this ruling |
| RD2 (review 2) | OPEN | Recommend restricting company-wide `batches.recall` to designated central inventory authority | Keeping seeded manager recall makes it intentionally company-wide despite branch membership; either option still needs action checks. Blocks final W-LOT role defaults |
| RD3 (review 3) | OPEN | Recommend reversing H-3's electronic-settlement → drawer fallback: electronic use unavailable until valid settlement configuration; does not govern voucher/loyalty/account destinations | Retaining fallback leaves that electronic day-one behavior in place as an explicit limitation; this client’s electronic tender launch remains excluded pending safe configuration. Do not change the pin/seed behavior without this ruling |
| RD4 (review 4) | OPEN | Recommend delivering fiscal-derived W7 first and deferring repository-balance comparison until float and drops are booked | Funding Treasury float/drawer-operation coverage first enables the separate repository check after source/idempotency/opening-baseline acceptance. Neither option permits comparing ledgers with different coverage or enabling the disabled variance writer prematurely |

## Verification and stop

Document-only checks cover preservation, the twelve unchanged OPEN rows, per-finding mapping, contiguous acceptance tables, cited file paths, local links and whitespace. No application test, preflight, runtime command, code/config/migration edit, reviewer call, commit, merge, rebase or push was performed. Unrelated working-tree changes were left alone. **Codex stops; Fable runs gate r4 with treasury and authz lenses.**
