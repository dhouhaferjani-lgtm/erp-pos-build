# GATE RECORD — R2-S (`fix/r2s-partner-refguard`) — tenancy/authz + module-boundary axis, round 1

**Verdict: spec ✅ / quality APPROVE-WITH-FIXES** (0 Critical; I-1..I-5 + m-1..m-6). Reviewed @ `bdfd71eb7` (3 commits on `264e6c483`). Tree returned pristine after two probes.

## Ratified
- **Ticket-reading SUPPORTED verbatim** (`2026-08-06-l6-partners-followups.md:29-35`: "do NOT just add more db->table() reads … Shared/Contracts reader per owning module"). Tagged-provider design = direct execution; precedent (FiscalEventProjector tagged set) verified real and mechanism-identical; Shared/Contracts placement better for rule 6.
- Deptrac: branch 102 == base 102 byte-identical category table (the +4 `SharedContracts on ModuleDomain` is PRE-EXISTING on dev) — **zero new edges**. Container probe on a real booted app: 15 sources, 21 tables all register. Zero foreign-module imports in the 15 source files. Zero `ConnectionInterface` in the diff; `DatabaseManager` + `->connection()` inside the loop.
- **Axis 3 soft-delete-vs-FK 500 REFUTED:** partner delete is a SOFT delete (`Partner.php:88`, `PartnerController.php:380` `->delete()`); no hard-delete/purge path for partners anywhere in `app/`. No FK can fire. Exclusion design correct.
- Tenant axis clean (all 21 tables tenant-DB; physical isolation; per-ROW multi-column OR proven); rule-12 middleware unchanged; no new `can:` → no seeder sync owed.
- Exclusion adjudication: bank-accounts/party-contacts/price-lists (deviation noted)/history-searches/cart-items RATIFIED; fiscal_events decision ratified but RATIONALE REFUTED (I-3); 3 pos_*_receipts UNADJUDICATED (I-2).

## Findings
- **I-1** timing test does NOT discriminate for sources — mutation-proven (implanted construction-time capture; all 3 tests stayed GREEN; `Container::tagged()` = RewindableGenerator, make()s at iteration time, post-swap). Verified fix: eager `iterator_to_array` before the swap (RED under mutation). Three docblocks overstate the net.
- **I-2** coverage one-directional: the coverage test compares the test's list to the sources' list (circular vs schema). Three real partner-keyed money tables absent from BOTH covered set and exclusions: `pos_account_charge_receipts`/`pos_account_payment_receipts`/`pos_deposit_receipts` (`customer_id` string, no FK — invisible to information_schema FK sweeps). Fix: schema-sweep test + asserted EXCLUDED constant.
- **I-3** fiscal_events "can never be the sole reference" REFUTED for ACCOUNT_CHARGE/ACCOUNT_PAYMENT/DEPOSIT_RECEIPT (no pos_receipts/documents row by default; Treasury coverage contingent on module + bridge having run). Decision stands; rationale must be rewritten.
- **I-4** loyalty polymorphic anchor uncounted: partner ownership has two equivalent channels (`customer_id` OR `loyaltyable_type='partner'` + `loyaltyable_id`, per Loyalty's own resolvers); anchor-only members (creatable via LoyaltyMemberController::store) earn/redeem invisibly to the guard. VO needs a where-discriminator (bare append would match contact ids).
- **I-5** 409 copy misleading: en/fr/ar say "invoices, payments or receipts" while 21 tables can block and `error.details` is discarded by the FE — the most common new block (pos_customer_aliases, created for EVERY POS customer) is one the message never mentions.
- **m-1** owning-side testability only half-delivered · **m-2** deptrac ratchet red on branch AND base (pre-existing; preflight will fail either way — re-baseline ticket) · **m-3** no module gating on the tagged set (deliberately NOT recommended to add — leftover rows from disabled modules SHOULD block) · **m-4** 47-case fixture matrix SQLite-only by construction (PG FKs would reject raw fixtures) · **m-5** partner_price_lists deviation must be acknowledged in the lane record · **m-6** Workshop submodule outside deptrac's collector glob (pre-existing config gap).
