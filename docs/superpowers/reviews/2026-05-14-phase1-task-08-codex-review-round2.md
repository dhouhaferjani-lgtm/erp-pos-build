REJECT — direct `canonical_parse_failure` `quarantined -> verified` guard is fixed, but the trigger still allows a two-step reclassification bypass that can verify the same failed/null-payload row without the required atomic payload+status write.

**Input Caveat**
- Cannot verify exact prior review markdown text from available evidence: requested paths `/Users/houssamr/Projects/syneriva/apps/erp/docs/superpowers/reviews/2026-05-14-phase1-task-08-codex-review.md` and `/Users/houssamr/Projects/syneriva/apps/erp/docs/superpowers/reviews/2026-05-14-phase1-task-08-opus-review.md` are absent from the working tree and `HEAD`. Review maps the prompt-described prior findings to the current diff/file evidence.

**Prior Finding #1: BLOCKER Closure — PARTIALLY-CLOSED**
- Direct branch closure: CLOSED for the one-update `OLD.integrity_status = 'quarantined'` / `NEW.integrity_status = 'verified'` path. The new guard is inside that branch at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:183-204`, fires only when `OLD.integrity_exception_class = 'canonical_parse_failure'` at `:196`, and rejects unless all four required facts hold: `OLD.payload IS NULL` (`:197` rejects the inverse), `NEW.payload IS NOT NULL` (`:198` rejects null), `OLD.payload_parse_status = 'failed'` (`:199` rejects any other old status), and `NEW.payload_parse_status = 'parsed'` (`:200` rejects any other new status). This is the fix-only diff hunk `git diff 2e036485..9577dc62 -- apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php` at `@@ -108,155 +108,172 @@`.
- `canonical_hash_mismatch` and other non-`canonical_parse_failure` classes can still resolve via stamps-only: the branch requires only `NEW.integrity_resolved_at` and `NEW.integrity_resolved_by` at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:183-187`, and the payload/status atomic guard is class-scoped to `OLD.integrity_exception_class = 'canonical_parse_failure'` at `:196`.
- Alternate transition bypass: NOT CLOSED. The trigger permits `integrity_exception_class` to change in an update with no integrity-status transition because the frozen-column check excludes `integrity_exception_class` from the forbidden list and documents only outside-whitelist columns as rejected at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:64-108`. A `canonical_parse_failure` row can therefore be updated first to another class while still `quarantined`/`failed`/`payload=NULL`; Step 2 does not run unless `payload_parse_status` changes (`:118-151`), Step 4 does not run unless `payload` changes (`:157-172`), and Step 5 does not run unless `integrity_status` changes (`:179-209`). A second update then flips `quarantined -> verified` with stamps only; the new guard is skipped because it checks `OLD.integrity_exception_class = 'canonical_parse_failure'` at `:196`, and the row can become `verified` with `payload=NULL` and `payload_parse_status='failed'`. This is the same stranded projector state the blocker was meant to prevent.

**Prior Finding #2: P1 CI Gate — CLOSED**
- `backend-test-pgsql` now documents both PG-only fiscal-events tests at `.github/workflows/ci.yml:322-326`.
- Exact filter line includes both `FiscalEventsTableTest` and `FiscalEventsImmutabilityTest`: `.github/workflows/ci.yml:334-336` has `--filter="VoucherLedgerTest|VoucherLedgerAppendOnlyTest|VoucherSchemaTest|FiscalHardeningE2ETest|FiscalEventsTableTest|FiscalEventsImmutabilityTest"`.

**Prior Finding #3: P2 Runbook `session_replication_role` — CLOSED**
- Explicit warning is present at `apps/api/docs/runbooks/fiscal-events-break-glass.md:98-106`: `DO NOT use SET session_replication_role = 'replica' on fiscal_events`; it states the flag suppresses default user triggers, creates a wider trigger-disabled window, and instructs use of named `ALTER TABLE fiscal_events DISABLE TRIGGER fiscal_events_immutability_update;` with re-enable before `COMMIT`.
- No remaining recommending reference found: `rg -n "session_replication_role|replica" apps/api/docs/runbooks/fiscal-events-break-glass.md` returns only `apps/api/docs/runbooks/fiscal-events-break-glass.md:98`, the warning line.

**Opus P2-1 DataProvider Negative Test — PARTIALLY-CLOSED**
- Conditions 2, 3, 5, 6, and 7 have explicit negative datasets at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:209-277`; condition 4 has the existing dedicated negative method at `:119-137`; the happy path covers all seven positive conditions at `:94-117`.
- Condition 1 is not correctly covered against the gated `failed -> parsed` condition. The dataset labeled `OLD.payload IS NOT NULL` inserts `payload_parse_status='parsed'` and rewrites an existing payload at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:188-207`, so the Step 3 `failed -> parsed` gate at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:138-151` is never evaluated; the failure comes from the write-once payload check at `:157-160`. It also does not cover the blocker-specific partial resolver case `OLD.payload IS NULL`, `NEW.payload IS NOT NULL`, `OLD.payload_parse_status='failed'`, `NEW.payload_parse_status='failed'`, `OLD.integrity_status='quarantined'`, `NEW.integrity_status='verified'`.

**Opus P2-2 Verified -> Quarantined Positive Test — CLOSED**
- Added test exercises `verified -> quarantined` with exception metadata at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:280-310`.

**Adversarial Bypass Checks**
- Payload written but parse status left `failed`: trigger blocks. A payload write requires `pending -> parsed` or `failed -> parsed` at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:157-170`; the canonical-parse-failure resolve branch also rejects `NEW.payload_parse_status IS DISTINCT FROM 'parsed'` at `:196-203`.
- Parse status flipped to `parsed` without payload: trigger blocks. The `failed -> parsed` gate requires `NEW.payload IS NOT NULL` at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:138-149`; the resolve branch also rejects `NEW.payload IS NULL` at `:196-203`.
- Other exception classes: cannot verify any requirement that `canonical_hash_mismatch`, `time_anomaly`, `sequence_gap`, or `sequence_conflict` need atomic payload+status writes from the touched diff. The enum values are `apps/api/app/Modules/Fiscal/Domain/Enums/IntegrityExceptionClass.php:9-13`; the runbook distinguishes generic resolution from `canonical_parse_failure` atomic payload-write resolution at `apps/api/docs/runbooks/fiscal-events-break-glass.md:41-46`.

**PHPUnit Coverage Checks**
- Negative path for parse-status flipped without payload: covered by the `NEW.payload null` dataset at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:209-220`.
- Negative path for payload written while parse status remains `failed`: not independently covered in PHPUnit. The provider list at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:186-278` has no dataset with `OLD.payload=NULL`, `NEW.payload NOT NULL`, `OLD.payload_parse_status='failed'`, and `NEW.payload_parse_status='failed'`.
- Attribute DataProvider discovery: no issue observed. Import is at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:11`, attribute at `:160`, test method is public at `:161-165`, provider is `public static` returning `iterable` at `:183-187`. Verification command `php artisan test --filter=FiscalEventsImmutabilityTest` in `apps/api` exited 0 and discovered 13 skipped tests/datasets under the local non-pgsql connection.

**Tasks 1-7 Invariant Spot-Check**
- Cumulative Task 8 diff is scoped to four files only: `.github/workflows/ci.yml`, `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php`, `apps/api/docs/runbooks/fiscal-events-break-glass.md`, and `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php` (`git diff --name-status 82b32292..9577dc62`). No unintended source changes outside Task 8 scope were found from that diff.

**New Findings**
- BLOCKER: `canonical_parse_failure` can be reclassified before verification to bypass the new atomic payload+status guard. Evidence: `integrity_exception_class` is mutable without a named transition at `apps/api/database/migrations/2026_05_14_100002_create_fiscal_events_immutability.php:64-108`; no validation runs when only the class changes because Step 2/4/5 are gated by status/payload/integrity changes at `:118`, `:157`, and `:179`; the new guard only keys on the later update's `OLD.integrity_exception_class` at `:196`.
- P3: PHPUnit does not independently cover the partial resolver that writes payload while leaving `payload_parse_status='failed'`; existing provider coverage at `apps/api/tests/Feature/Fiscal/FiscalEventsImmutabilityTest.php:186-278` lacks that tuple.
