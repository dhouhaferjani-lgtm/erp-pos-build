Date: 2026-05-08
Reviewer: Codex (adversarial round-2)
Branch: feat/pos-t1.2-payment-config-residual
Commit reviewed: b6903e8b
Baseline: 9a5fd80b

VERDICT: APPROVE WITH MINOR FOLLOW-UPS

Round-1 BLOCKER status: CLOSED

Evidence:
- `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:218` derives refund-branch `paymentConfigReady` from both `paymentMethods` and `paymentRepositories`; `:221` folds it into `netButtonDisabled`.
- `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:236` applies the HTML `disabled` attribute, `:237` applies `aria-disabled`, `:238-242` applies the `payment.configNotLoaded` title when config is not ready, and `:243` includes `disabled:cursor-not-allowed disabled:opacity-50`.
- `apps/pos/src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx:266-284` covers repositories-empty and asserts disabled, `aria-disabled="true"`, title, and no click-through. `:286-300` covers methods-empty. Focused run passed: 4 files, 33 tests.

Findings

1. MINOR - The i18n key exists, but there is still no locale smoke assertion for both `en` and `fr`.

`apps/pos/src/locales/en/pos.json:55` and `apps/pos/src/locales/fr/pos.json:55` both contain `payment.configNotLoaded`, so this is not load-bearing. The only tests that mention the key use mocked `t()` output: `apps/pos/src/components/pos/__tests__/PaymentSummary.test.tsx:87-101` asserts the key string, and `apps/pos/src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx:280` asserts the mocked key string. A small smoke test importing the locale JSON would close the preempt completely.

2. MINOR - One of the three new tests is not a revert detector.

The two negative tests at `apps/pos/src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx:266-300` would fail if the `TransactionCart.tsx` fix were reverted because the old button was enabled when `checkoutDisabled` was false and would call `onPayCash`. The positive test at `:303-317` would still pass after a revert because the pre-fix button was also enabled and clickable when both arrays were populated. This does not weaken the blocker closure, but the claim that all three tests would fail on revert is not accurate.

3. NIT - The fix-up commit includes an unexpected review document.

`git diff --name-only b6903e8b^..b6903e8b` lists the expected `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx` and `apps/pos/src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx`, plus `docs/superpowers/reviews/2026-05-08-pos-t1.2-residual-codex-review.md`. That extra file is documentation-only and not a runtime risk, but it is outside the expected fix-up scope.

Ricochet Probes

- IIFE pattern: not a correctness issue. The IIFE at `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:217-249` is render-local and deterministic; it is style debt only.
- DRY duplication: not load-bearing for this PR. `TransactionCart.tsx:218-220` and `apps/pos/src/components/pos/PaymentSummary.tsx:44-45` use the same readiness derivation. A shared helper would reduce future drift, but the current fix is correct.
- i18n namespace consistency: correct. `TransactionCart.tsx:72` binds `useTranslation('pos')` and `:240` calls `t('payment.configNotLoaded')`; `PaymentSummary.tsx:33` binds `['common', 'pos']` and `:104` calls `t('pos:payment.configNotLoaded')`. `apps/pos/src/lib/i18n.ts:13-21` registers the `pos` namespace for both languages and `:27` sets default namespace to `pos`; both calls resolve to the same locale key.

Round-1 Preempt Re-Probe

- (b) i18n audit: key present in both locale files at `apps/pos/src/locales/en/pos.json:55` and `apps/pos/src/locales/fr/pos.json:55`; smoke assertion missing, tracked as Finding 1.
- (c) aria-disabled completeness: complete in refund branch at `TransactionCart.tsx:236-243`.
- (g) test honesty: blocker is honestly guarded by the two negative tests at `TransactionCart.test.tsx:266-300`; the positive test nuance is Finding 2.

Original Production Surfaces

No additional BLOCKER/MAJOR findings after re-probing the T1.2 surfaces:
- Sale footer gate remains in `apps/pos/src/components/pos/PaymentSummary.tsx:44-54`, with disabled/aria/title on `:99-106`.
- `HomePage` passes payment config and hash-chain/processing disabled state into `TransactionCart` at `apps/pos/src/pages/HomePage.tsx:984-987`.
- SQLite refresh remains atomic and skip-set guarded at `apps/pos/src/stores/paymentStore.ts:753-787`.
- Terminal activation pre-warm remains fire-and-forget after scheduler start at `apps/pos/src/stores/terminalStore.ts:110-132`.

Scope-Creep Statement

Files touched by b6903e8b:
- `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx` - expected.
- `apps/pos/src/components/organisms/TransactionCart/__tests__/TransactionCart.test.tsx` - expected.
- `docs/superpowers/reviews/2026-05-08-pos-t1.2-residual-codex-review.md` - unexpected but documentation-only.

BLOCK split recommendation: N/A; verdict is not BLOCK.
