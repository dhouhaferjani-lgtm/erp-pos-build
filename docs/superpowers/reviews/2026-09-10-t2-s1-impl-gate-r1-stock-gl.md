# T-2 S1 receipt spine — implementation gate r1 — stock-gl-interaction-reviewer

Reviewed: lane/t2-receipt-spine @ c30dcdf9c (diff de3007fe9..c30dcdf9c), plan rev 8 (§0A amendment cited), spec rev 11, handback HANDBACK-T2-receipt-spine-2026-09-09.md
Date: 2026-09-10
Reviewer: Claude Opus `stock-gl-interaction-reviewer`, read-only, SQLite legs only (one PHP process at a time), no PostgreSQL leg. All `path:line` refs are at c30dcdf9c under `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/`. Overlaps with `docs/superpowers/reviews/2026-09-10-t2-s1-impl-gate-r1-inventory.md` are cited, not restated.

## Verdict
VERDICT: CHANGES-REQUIRED
BLOCKER=0 MAJOR=1 MINOR=7

## Required citations (plan §7.15 stock-gl list + §0A)

| # | Requirement | Status | Evidence `path:line` |
|---|---|---|---|
| 1 | First GL posting on this document type (seam 1); accounts from seeded settings, not hardcoded | VERIFIED | Pre-S1 zero GL: `git show de3007fe9:…/StockTransferService.php \| grep -E 'GlPosting\|GeneralLedger\|JournalEntry\|glBuffer\|MovementGlContext'` → **empty**; the only listeners on the three transfer events at `de3007fe9` are `Replenishment/Providers/ReplenishmentServiceProvider.php:19-20` (`SettleRequestsOnTransferInitiated`, `ReopenRequestsOnTransferCancelled`), neither of which posts GL; no GL subscriber on `StockMovementRecorded` (sole listener `Channel/Providers/ChannelServiceProvider.php:29`). What S1 posts: `StockTransferReceiptService.php:443-464` — one context per destructive movement. Accounts resolved by purpose only: `InventoryGlPostingService.php:185-187` (`CostOfGoodsSold` / `InventoryShrinkageExpense` via `MovementGlCounterFamily`), `GeneralLedgerService.php:5232-5233` and `:5455-5456` (`getAccountByPurpose($companyId, SystemAccountPurpose::Inventory / ::InventoryShrinkageExpense)`). **No account code literal anywhere in the diff** (`git diff … apps/api/app \| grep -E "'6[0-9]{2,3}'\|'3[0-9]{2}'"` → empty; the only codes are the test fixture `tests/Feature/Inventory/StockTransferReceiveTest.php:89,99,109`). Reviewer probe empirically resolved the purposes on both paths — see "Verification" |
| 2 | Every posting via `InventoryGlPostingBuffer`, receipt service OUTERMOST, no nested tx committing GL before stock, flushed exactly once | VERIFIED | Root: `StockTransferReceiptService.php:101` `DB::transaction(function () …, attempts: 3)`; marker `:102`; `rollbackTo(min($marker, mark()))` on ANY throwable `:137-140`; the only `enqueue` is `:456-463`; the only flush is `:408`, once per `post()`, and `post()` is unreachable on replay (`:105-108` returns before it). `ProductCostLock::acquire` opens **no** transaction (`Domain/Services/ProductCostLock.php:40-52` — advisory `pg_advisory_xact_lock` only), so `DB::transactionLevel()` is exactly 1 at `:408` and `flushIfOutermost` takes the `$level === 1` branch (`InventoryGlPostingBuffer.php:58-68`) and posts inline (`:92`, `$contained` false). The two nested transactions in the blast radius are savepoints that cannot commit early: `StockAdjustmentService::receive/issue` (`:143`, `:294` — stock only) and `GeneralLedgerService::createInventoryMovementEntry` draft creation (`:5249`) followed by `postEntryNow` at level 1 (`:5291-5293`). No direct `postFor*` call exists outside the buffer (`grep -rn 'postForExit\|postForBatchWriteOff\|postForEntry\|postForCountCorrection' apps/api/app` → only doc comments), enforced by `app/PHPStan/Rules/InventoryGlPostingViaBufferOnly.php:17,31,35`; scoped PHPStan over all 49 changed `app/` files → `[OK] No errors`. Pin: `tests/Feature/Inventory/TransferReceiptGlBoundaryTest.php:14-27` (buffer empty after receive AND close, `Log::shouldNotHaveReceived('critical')`, `DB::transactionLevel() === 0`), with `connectionsToTransact(): []` at `StockTransferReceiveTest.php:55-58` so the service really is the root. Re-run green |
| 3 | `return_to_source` posts NOTHING to the GL; restocks at source via `restockAtSource` | VERIFIED | `StockTransferReceiptService.php:429-434` — `returned > 0` calls `$this->movementSupport->restockAtSource(...)` and **returns before** the scrap/enqueue block; `StockTransferMovementSupport.php:191-237` contains no `glBuffer`, no `JournalEntry`, no `postFor*`. Pin: `StockTransferCloseTest.php:57-69` asserts source `StockLevel` +12.0000, source `BatchStock` 20.0000, exactly one `transfer_in` movement at source, **`journalCount() === 0`**, and the replay leaves both unchanged. Re-run green (6 tests / 29 assertions) |
| 4 | `requirePerpetual` preflight covers BOTH the lot-less and the batch-tracked path, before any stock or GL write | VERIFIED | The preflight is **line-level and lot-agnostic**: `StockTransferReceiptService.php:124-133` loops the postings and fires on `damaged + written_off > 0` from `$posting['quantities']` — the same array for a lot-less line (`validateReceive` `:202`) and a lot-tracked one (lot sums pinned equal to the line at `:245-247`), so one code path covers both; it runs at `:127`, before `post()` at `:135`, hence before the first `receive()`/`issue()`. Typed refusal: `TransferReceiptFailureException(Failure::ValuationModeUnsupported)` `:129`. Pins for BOTH paths: lot-less `StockTransferReceiveDamageTest.php:36-44` (422 `VALUATION_MODE_UNSUPPORTED`, `stock_movements` count unchanged); batch-tracked `StockTransferReceiveLotsTest.php:63-75` (same refusal on a lot payload, then perpetual → 201, destination `0.0000`, 1 journal). Both re-run green |
| 5 | Movement identity line-level vs lot-level, parent columns NULL, final type + `StockTransfer::class` linkage at INSERT, no post-create UPDATE | VERIFIED | Identity: lot-tracked → ids on the lot rows and the parent explicitly nulled at `StockTransferReceiptService.php:346-347`; lot-less → ids on the receipt line `:364`. CHECK `stock_transfer_receipt_lines_lot_lines_have_no_parent_movement` at `database/migrations/tenant/2026_09_09_100100_create_stock_transfer_receipt_tables.php:228-232` (PG-only, guarded `:83`). Pin: `StockTransferReceiveLotsTest.php:27-31` (`in_movement_id` NULL on the parent, NOT NULL on the lot, movement `reference_id` = transfer id). §0A at INSERT: `StockAdjustmentService.php:130-139` / `:278-287` (backward-compatible `?MovementType $movementType`, `?string $transferId`, direction + exclusivity + `Str::isUuid` validation), final type `:165` / `:320`, linkage written by `recordMovement` `:2089-2090` (`reference_type = StockTransfer::class`, `reference_id = $transferId`). `grep -rn markMovementAsTransfer app/ tests/ database/` → exit 1. Pins: `StockTransferEdgeCasesTest.php:73-94` (`$updated === []` after initiate, complete AND cancel; created tuple `['transfer_out', StockTransfer::class, $id]`), `:112-138` (eight invalid-override cases refused with zero writes). **Reviewer probe additionally proved `$updated === []` across a lot-tracked damage receipt AND a close write-off** — the two paths the committed suite does not cover for append-only |
| 6 | No compensating stock or GL action for the T-1 settlement or freight tickets | VERIFIED | Settlement: no Replenishment production file in the diff (`git diff --name-only … \| grep -i provider` → empty; no `app/Modules/Replenishment/` entry), so `SettleRequestsOnTransferInitiated` / `ReopenRequestsOnTransferCancelled` are untouched and the two new close statuses fire neither event (`StockTransferReceiptService.php:409-417` dispatches only `StockTransferReceiptPosted` and, on completion, `StockTransferCompleted`). Pin: `tests/Feature/Replenishment/TransferCloseReplenishmentSettlementTest.php:16-30` — after BOTH dispositions the settled request stays `Fulfilled` with its original `fulfillment_id` and `processed_by_user_id`. Freight: `StockTransferReceiptService.php:478-495` and `StockTransferMovementSupport.php:88-172` contain no GL of any kind; `StockTransferCloseTest.php:22-32` pins `journalCount() === 1` across a freight-bearing receive+close (the write-off leg only). Tickets present and unamended: `docs/superpowers/tickets/2026-09-09-t1-settlement-replay.md`, `…-t1-freight-capitalization-no-gl.md`. Recall/expiry: `StockTransferEdgeCasesTest.php:173-186` remains skipped behind `T1_RUN_KNOWN_REDS` and `:188-218` still pins current behaviour to ticket `…-t1-recalled-in-transit.md` — no compensating write-off added |
| 7 | §0A consumer census verified against the tree | VERIFIED | `grep -rn 'StockMovementRecorded' apps/api/app/Providers/ apps/api/app/Modules/*/Providers/` → exactly one row: `Channel/Providers/ChannelServiceProvider.php:29` `Event::listen(StockMovementRecorded::class, DispatchStockChangeToChannels::class)`. The listener body `Channel/Application/Listeners/DispatchStockChangeToChannels.php:15-54` reads only `tenantId/companyId/productId/locationId/newStockLevel` (`:17`, `:26-51`) — **no `movementType` reference anywhere in `apps/api/app/Modules/Channel/`**. **Zero** `StockMovementRecordedV2` listener registrations; every other hit is an emitter (`WeightedAverageCostService`, `OpeningBalancePostingService`, `ResetOpeningBalanceService`, `StockAdjustmentService`). No Compliance/Accounting subscriber. Label change sites: `StockAdjustmentService.php:205,224` (receive) and `:359,377` (issue) now pass `$movementSnapshot->movement_type->value`, so non-transfer rows keep `receipt`/`issue` byte-identically. Pin: `StockTransferEdgeCasesTest.php:96-108` asserts BOTH V1 and V2 carry `transfer_out`/`transfer_in` + `StockTransfer::class` + the transfer id. Handback `:87` census matches the tree |
| 8 | Stored events: new classes only, explicit aggregate uuid + version, no existing event class modified, replay test | VERIFIED | `git diff --name-only de3007fe9..c30dcdf9c \| grep -i event` returns exactly four production files, all `A` (added): `StockTransferClosedV1.php`, `StockTransferReceiptLineRecordedV1.php`, `StockTransferReceiptPosted.php`, `StockTransferReceivedV1.php` (`git diff --stat … Domain/Events/` = `4 files changed, 189 insertions(+)`, **zero deletions**) — rule 8 intact; `StockMovementRecorded.php` / `V2.php` / `StockTransferCompleted` / `StockTransferInitiated` / `StockTransferCancelled` are byte-unchanged. Uuid + version are explicit at the persist site, not in the ctor (`DomainEvent.php:27-31` ctor uuid never reaches the row): `StockTransferReceiptService.php:527-528` (`setAggregateRootVersion(1)`, `persist($header, $receipt->id)->handle()`) and `:551-552` (`$index + 2`). Never routed through `event()`. Replay: `TransferReceiptEventStreamTest.php:19-34` (versions `[1,2]`, ordered classes, zero NULL `aggregate_uuid`, replay adds no row), `:36-140` (every receipt/line/lot row, both counter tables and six transfer header columns reconstructed from `event_properties` and asserted `assertSame` — including `freight_uncapitalized`; legacy backfilled receipts stay event-free `:138-139`), `:142-163` (forced failure at the stored-event INSERT leaves 0 events, destination `0.0000`, status `in_transit`) |
| 9 | Damage land-then-scrap GL at the received cost, mirroring the quantity netting, inside the same outermost transaction; freight capitalisation through the buffer once after the terminal status write | VERIFIED, with one restatement | Netting mirror: `StockTransferReceiptService.php:435-436` (`scrap = damaged + written_off`, `land = received + scrap`) → `receive(land)` `:438-441` then `issue(scrap)` `:449-452`, one context per scrap movement `:456-463`, all inside the root opened at `:101`. Cost basis: `unitCost: $line->product->resolveMovementUnitCost()` `:445` = `Product.php:322-325` `cost_price` (current company-wide WAC), i.e. the value at which perpetual inventory is carried — **identical to the only other write-off surface**, `BatchExpiry/Domain/Services/BatchWriteOffService.php:164,189`, so the two surfaces cannot diverge; the GL amount is `unitCost × absoluteDeltaForRow()` at the entity currency scale (`InventoryGlPostingService.php:211-220`, `:214` explicit currency). Kind split honoured: `Exit` for lot-less, `BatchWriteOff` with real `batchNumber`/`productId` for lot rows `:457,:462`. **Restatement:** the plan's phrase "the freight pool capitalisation posts through the buffer" is not literally true and must not be — freight posts **no GL at all** (seam 2 / ticket). What is verified is the ORDERING: terminal status persisted at `:402`, `capitalizeFreight` at `:403-405`, then the buffer flush at `:408`; `capitalizeFreight` `:478-495` → `capitalizeTransferCost` `StockTransferMovementSupport.php:88-172` reaches only `WeightedAverageCostService::recordCostAdjustment` `:159-170` (`grep -n 'glBuffer\|GlPosting\|JournalEntry\|journal' WeightedAverageCostService.php` → empty). Reviewer probe pinned the actual GL content on both paths (see MAJOR G-1) |
| 10 | Rule 20 — nothing in the receipt/close/GL path relies on `CompanyContext` | VERIFIED | Every scale resolution passes the entity currency: `StockTransferReceiptService.php:480` `getScaleSafe($transfer->company->currency, 3)`, `StockTransferMovementSupport.php:59` and `:90` the same; the GL context carries `currencyCode: $transfer->company->currency` `:458`, and `InventoryGlPostingService.php:109,172,214` all call `getScale($ctx->currencyCode)` with an explicit argument — no bare no-arg `getScale()` in the path. Numbering and locks take `tenantId`/`companyId` explicitly (`:322`, `:117`, `:154`). No `app()` helper in any new file. `$transfer->company` is eagerly loaded at `:118` before any use. Caveat: not exercised from a worker, because S1 adds no queued job (no `onQueue`, no listener) — the property is verified structurally, not empirically |
| 11 | Stock↔GL consequence census | VERIFIED — see the table below | Seven stock-changing sites, four GL-relevant, one GL posting kind; no stock change with an unstated consequence and no GL posting without a stock cause |

## Stock↔GL consequence census

| Stock change (`path:line`) | GL consequence (`path:line`) or the reason there is none |
|---|---|
| Destination land, `receive(land, MovementType::TransferIn, transferId)` — `StockTransferReceiptService.php:438-441` | **None, by design.** An intra-company location move leaves the inventory asset unchanged in total and does not touch WAC (`StockAdjustmentService::receive` `:143-238` contains no `wacService->` call — the only one in the file is the opening-balance path `:1779`). Seam 1 / T-1 fact: transfers post zero GL. Pinned indirectly by `StockTransferCloseTest.php:57-69` (`journalCount() === 0` on a pure-landing close) and `StockTransferReceiveTest.php:168-180` (12 units landed, no journal assertion needed because `TransferReceiptGlBoundaryTest.php:20` shows the count only moves on damage) |
| Destination scrap, `issue(scrap, reason from the ACTION)` — `StockTransferReceiptService.php:443-452` | **Exactly one journal per scrap movement.** `glBuffer->enqueue(MovementGlContext)` `:456-463` → `InventoryGlPostingBuffer.php:76-81` → `postForExit` (lot-less) / `postForBatchWriteOff` (lot) → `GeneralLedgerService::createInventoryMovementEntry` `:5187` / `createInventoryWriteOffEntry` `:5389` → `postEntryNow` `:3669-3683` (advisory-locked seal `:3699`, closed-period guard `:3774-3775`, `JournalEntryPosted` dispatched `afterCommit` `:3680-3682`). **Reviewer-probed content: Dr `InventoryShrinkageExpense` 10.000 / Cr `Inventory` 10.000, status `Posted`, `isChained()` true, `JournalEntryPosted` dispatched — on BOTH the `inventory_exit` and the `batch_write_off` path.** No COGS is booked at a transfer receipt (dimension 3 clean) |
| Source restock on close `return_to_source`, `restockAtSource` → `receive(TransferIn)` — `StockTransferMovementSupport.php:203-232` | **None (OQ-1, ruled).** The goods returned to the location they left; no value left the company. Pinned `StockTransferCloseTest.php:66` (`journalCount() === 0`) |
| Source restock on cancel — `StockTransferService.php:349-357` (now through the same `restockAtSource`) | **None** — behaviour unchanged from pre-S1; one writer, two callers, no second writer (`§6.7b` census re-verified in the inventory register, citation 10) |
| Source issue at initiate, `issue(TransferOut, transferId)` — `StockTransferService.php:449-473` | **None** — unchanged; only the label/linkage moved to INSERT time (§0A) |
| Line + allocation counters, `addCounters` — `StockTransferReceiptService.php:470-476`, saved `:360`, `:372` | **None owed.** These are not on-hand quantity; they shrink the DERIVED in-transit remainder (`StockTransferLine::REMAINDER_SQL`) in the same transaction and under the same product advisory lock as the destination `stock_levels` increase, so company-owned quantity is conserved (inventory register citation 3; `StockTransferEdgeCasesTest.php:271`). No `stock_levels`/`inventory_batch_stock` row is ever authored for units in transit (seam 4), so no phantom WAC denominator |
| `products.cost_price` raised by the freight pool, `recordCostAdjustment` — `StockTransferMovementSupport.php:159-170` | **None, deliberately deferred.** Ticket `docs/superpowers/tickets/2026-09-09-t1-freight-capitalization-no-gl.md` owns the missing Dr Inventory / Cr clearing leg; plan §2.3 and §15 forbid S1 from adding it. Consequence disclosed as MINOR G-7 |
| Reconciliation read model — `TransferReconciliationService.php` | No stock and no GL write (`grep -n 'receive(\|issue(\|glBuffer\|save()\|update('` → empty) |
| Legacy completion backfill — `database/migrations/tenant/2026_09_09_100300_backfill_legacy_transfer_completions.php` | **No stock and no GL write.** The only `stock_movements` reference is a READ to resolve existing movement ids (`:140`, `:150`). It authors documents for stock that already moved — correct for a backfill; `TransferReceiptEventStreamTest.php:138-139` pins that backfilled receipts also carry no stored events |

| GL posting (`path:line`) | Stock cause |
|---|---|
| `MovementGlKind::Exit` → `postForExit` → `inventory_exit` entry (`InventoryGlPostingService.php:26-28`, `:130-207`) | The lot-less scrap `issue()` at `StockTransferReceiptService.php:449-452` (`MovementReason::Damage` on a receipt, `::WriteOff` on a close — derived from the ACTION at `:444`, never from the operator's declared reason; pinned `StockTransferReceiveDamageTest.php:27-34`) |
| `MovementGlKind::BatchWriteOff` → `postForBatchWriteOff` → `batch_write_off` entry (`InventoryGlPostingService.php:89-128`) | The per-lot scrap `issue($scrap, batchId: $allocation->batch_id)` at the same lines, one context per lot movement |

No other GL entry is written anywhere in the diff (`git diff … apps/api/app \| grep -E "^\+.*(journal_entries\|JournalEntry::\|postEntry)"` → empty).

## Findings

### BLOCKER
None. No GL posting outside the buffer; no nested transaction commits GL before stock; no hardcoded account code; no existing event class modified; no post-insert `UPDATE` of `stock_movements` (probed on the receipt and close paths, not just the initiate/complete/cancel paths the committed suite covers); `return_to_source` posts nothing; no float on the seam (rule 19 — `git diff … apps/api/app apps/api/database | grep -E "^\+.*((float)|floatval|number_format|parseFloat)"` returns only `(int)` casts on `batch_id`/`sequence`; scoped PHPStan clean over all 49 changed `app/` files).

### MAJOR

**G-1 — The GL side of the new seam is pinned only by a row COUNT: no committed test asserts the accounts, the direction, the amount, the posted status or the `JournalEntryPosted` dispatch of the first journal entry this document type has ever produced.**
What: every GL assertion in the slice is `JournalEntry::count()` — `StockTransferReceiveTest.php:160-163` (the helper), used at `TransferReceiptGlBoundaryTest.php:20,23`, `StockTransferReceiveDamageTest.php:21`, `StockTransferReceiveLotsTest.php:74`, `StockTransferCloseTest.php:28,31,66`, `TransferReceiptSecondOfEverythingTest.php:75`. `grep -rn "JournalLine\|journal_lines\|->debit\|->credit\|fiscal_hash\|JournalEntryPosted\|system_purpose" apps/api/tests/Feature/Inventory/StockTransfer*.php apps/api/tests/Feature/Inventory/TransferReceipt*.php apps/api/tests/Feature/Replenishment/TransferClose*.php` returns **only the three account-seeding rows** at `StockTransferReceiveTest.php:92,102,112` — zero assertions on any journal content.
Why it matters: this is the seam this gate exists to protect, and "one row appeared" is not "value was booked correctly". Every one of these would keep the whole suite green: flipping `debitInventory` at `InventoryGlPostingService.php:196` (Dr Inventory / Cr Shrinkage — capitalising a loss); `MovementReason::Damage` acquiring `MovementGlCounterFamily::Cogs` instead of shrinkage so the loss lands in `603` and inflates gross margin (`:185-187`); a wrong scale or a `bcmul` argument swap in `amount()` (`:216-219`) halving or squaring the amount; the entry staying `Draft` because a future refactor drops `postSynchronously: true` (`:126`, `:206`) — which would also silently stop the fiscal chain and the `JournalEntryPosted` dispatch that every downstream consumer keys on. My mandate is explicit that a seam test must assert BOTH sides; the movement side here is asserted properly (reason `StockTransferReceiveDamageTest.php:32-33`, netting `:22`, lot netting `StockTransferReceiveLotsTest.php:73`, append-only `StockTransferEdgeCasesTest.php:74-93`) while the journal side is only counted.
Failing scenario: not a current defect — I falsified it by probe. A throw-away `ZzStockGlReviewerProbeTest` (deleted; tree byte-identical) asserted, and PASSED, for both the lot-less (`source_type = inventory_exit`) and the lot-tracked (`source_type = batch_write_off`) damage receipt: `system_purpose` `inventory_shrinkage_expense` → `debit 10.000 / credit 0.000`, `inventory` → `debit 0.000 / credit 10.000`, `status === JournalEntryStatus::Posted`, `isChained() === true`, `JournalEntryPosted` dispatched. So the code is right and the pin is missing.
Minimum fix: add those six assertions to `StockTransferReceiveDamageTest::test_damaged_units_land_then_scrap_with_one_shrinkage_journal` (lot-less) and `StockTransferReceiveLotsTest::test_lot_damage_nets_to_zero_and_requires_perpetual_valuation` (lot) — load `$entry->lines`, map `account_id → system_purpose`, `assertSame` the debit/credit pair and the amount `qty × cost_price` at the company scale, plus `assertSame(JournalEntryStatus::Posted, $entry->status)`. No production change; both files are already in S1's scope.

### MINOR

**G-2 — A company whose chart lacks the `Inventory` or `InventoryShrinkageExpense` purpose destroys stock at a transfer receipt with ZERO GL, gets a 201 and a terminal transfer, and only a `Log::warning`.** The preflight at `StockTransferReceiptService.php:124-133` checks the valuation mode and nothing else; the account check happens far downstream and fail-softs: `InventoryGlPostingService.php:99-106` (`hasInventoryWriteOffAccounts` → `Log::warning` + `return null`) and `:161-169` (`hasInventoryMovementAccounts`, same). The plan's own rule-4 rationale — "a typed refusal before any stock or GL write" — is applied to the mode but not to account mappability, even though `GeneralLedgerService::hasInventoryWriteOffAccounts()` could be called at the identical point. Reachability is reduced but not zero: `InventoryVarianceAccountProvisioner::provisionTemplateCompany` treats shrinkage as REQUIRED and grafts a fallback parent rather than aborting (`:49-70`), yet `app/Console/Commands/BackfillInventoryShrinkagePurposesCommand.php` exists precisely because legacy companies lack it, and the provisioner's own comment at `:46-47` names this exact hazard ("a missing shrinkage account silently drops a write-off journal entry"). Nothing in the slice pins the fail-soft as intentional and `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` has no step verifying the two purposes on the launch tenant. Plan §2.3 does direct reuse of the shrinkage bridge, so this is inherited, not drift. Fix: either extend the `:124-133` preflight with the account check and a new `TransferReceiptFailureReason`, or add a test pinning "unmapped accounts ⇒ stock destroyed, zero GL, warning logged" as deliberate **and** a promotion-checklist line asserting both purposes resolve for the launch company.

**G-3 — `receive()`/`close()` are nest-sensitive with no structural guard: a caller inside a transaction commits the receipt document and the destructive stock movements with ZERO GL, alarmed only by a log line.** `InventoryGlPostingBuffer.php:58-64` returns `[]` and registers a leak alarm at `transactionLevel() > 1`; `StockTransferReceiptService.php:101` opens a savepoint rather than refusing. Verified empirically by probe (deleted): wrapping `StockTransferReceiptService::receive()` in `DB::transaction(...)` produced `quantity_damaged = 2.0000`, one `stock_transfer_receipts` row and **`journalCount() === 0`**. Verified unreachable today: `StockTransferController.php:308,334` are the only callers of `receive`/`close` and the controller opens no transaction (`grep -n 'DB::transaction' StockTransferController.php` → empty); `StockTransferService::complete()` `:310-315` forwards without a transaction and its `completionPayload` `:167-184` never carries damage, so it cannot enqueue GL; `ReplenishmentFulfillmentService` calls only `initiate()` inside its transaction (`:52`, `:102`). Mitigation: in the test suite the request/job boundary guard would fail a nested HTTP caller (`InventoryServiceProvider.php:119-133`, `InventoryGlPostingBoundaryGuard`); in production `preserveLeaksForBoundaryGuard` is false so the buffer silently resets (`InventoryGlPostingBuffer.php:114-119`). Note that S1 changes the risk profile: pre-S1 nesting `complete()` was harmless because transfers posted no GL. Fix: three lines at the top of `transact()` — `if (DB::transactionLevel() !== 0) { throw new \LogicException(...); }`, precedent `GeneralLedgerService.php:115,131` — plus one test.

**G-4 — The shrinkage movement for goods that never arrived is attributed to the DESTINATION location.** `StockTransferReceiptService.php:438-441` lands the close-write-off remainder at `$transfer->destination_location_id` and `:449-452` scraps it there, so a `lost_in_transit` write-off appears in the receiving location's movement feed and per-location shrinkage, never the sender's. GL is unaffected (the shrinkage account is company-level), so this is not a value defect — but T-3's whole purpose is sender-vs-receiver accountability, and S3 builds the location-scoped movement feeds (plan §9.5). Plan §7.8 ("land-then-scrap rationale") directs it, so this is plan-directed, not drift. Fix: record the attribution decision in the glossary row for the receipt document (or in §9.5) so S3 does not read it as a bug, and state it in the promotion checklist.

**G-5 — Orphan docblock left by the `lineProductIds` deletion.** `StockTransferService.php:799-804` is the deleted method's docblock ("Unique product ids across a transfer's lines… `@return list<string>`"), now floating immediately above `collectProductIds`'s own docblock at `:806-809`. Harmless to PHPStan (clean) but it misdescribes the next symbol for the next reader of a file whose lock ordering is load-bearing. Fix: delete `:799-805`.

**G-6 — The handback asserts reviewer verdicts that did not exist.** `docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md:209` states "All required reviewers returned ACCEPT with BLOCKER=0, MAJOR=0" and `:212` summarises a "stock-gl-interaction-reviewer" verdict covering exactly this list. No stock-gl register existed before this one, and the inventory-costing register at the same pin returned CHANGES-REQUIRED with MAJOR=2 (`docs/superpowers/reviews/2026-09-10-t2-s1-impl-gate-r1-inventory.md:9-10`), so `:209` is false as of this gate. Fix: replace `:209-213` with "reviewer gates pending" (the handback's `status: review` is otherwise correct); no reviewer summary should be authored inside the artefact under review.

**G-7 — After S1 the GL/inventory-subledger divergence is realised, not just latent, and is undisclosed.** S1 makes the scrap path credit the `Inventory` account at WAC (`InventoryGlPostingService.php:189-207`, `:117-127`), while the freight pool raises `products.cost_price` for every owned unit with no offsetting debit (`StockTransferMovementSupport.php:159-170`). Before S1 transfers touched the GL not at all, so the freight-only divergence was invisible in the GL; from S1 onward the same transfer both moves the GL inventory balance and moves WAC by an unbooked amount, so `GL Inventory` and `Σ qty × cost_price` drift by the capitalised freight on every transfer. Correctly NOT fixed here — ticket `docs/superpowers/tickets/2026-09-09-t1-freight-capitalization-no-gl.md` owns it and plan §2.3/§15 forbid the leg. Fix: one disclosure line in `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` naming the divergence and the ticket, so whoever first reconciles the inventory account on the launch tenant is not surprised.

**G-8 — Every GL leg I could exercise ran on SQLite, where the receipt CHECK constraints do not exist.** `2026_09_09_100100_create_stock_transfer_receipt_tables.php:83` returns early for non-`pgsql`, so `addChecks()` `:185-232` — including `stock_transfer_receipt_lines_lot_lines_have_no_parent_movement`, the constraint that enforces citation 5's identity rule — and the FKs `:112` are absent from every leg run in this review. The database-level enforcement of the lot/parent split, and the `_positive` / `_close_excludes_receipt` shapes, are covered only by the implementer's unre-run `TransferReceiptSchemaRerunPostgresTest` / `StockTransferReceiveConcurrencyPostgresTest`. This compounds the inventory register's I-5. Not a production defect (production is PostgreSQL) — recorded so no one reads my green SQLite legs as constraint proof.

## Verification run by the reviewer

| Command | Outcome |
|---|---|
| `git show de3007fe9:…/StockTransferService.php \| grep -E 'GlPosting\|GeneralLedger\|JournalEntry\|glBuffer\|MovementGlContext'` | empty — zero GL in the pre-S1 transfer graph (citation 1) |
| `git grep -n 'StockTransfer{Completed,Initiated,Cancelled}::class' de3007fe9 -- '**/Providers/*.php'` | only `ReplenishmentServiceProvider.php:19-20`; no GL listener |
| `grep -rn 'StockMovementRecorded' apps/api/app/Providers/ apps/api/app/Modules/*/Providers/` | one row: `ChannelServiceProvider.php:29` (V1 only); zero V2 listeners |
| `grep -rn 'movementType' apps/api/app/Modules/Channel/` | empty — sole listener does not branch on the label (§0A census) |
| `git diff --stat de3007fe9..c30dcdf9c -- '…/Domain/Events/'` | `4 files changed, 189 insertions(+)`, 0 deletions — additive only (rule 8) |
| `grep -rn 'postForExit\|postForBatchWriteOff\|postForEntry\|postForCountCorrection' apps/api/app` | only doc comments outside the buffer/posting service — buffer-only holds |
| `git diff … apps/api/app apps/api/database \| grep -E "^\+.*((float)\|floatval\|number_format\|parseFloat\|journal_entries\|JournalEntry::)"` | only `(int)` on `batch_id`/`sequence`; zero direct journal writes |
| `grep -rn markMovementAsTransfer app/ tests/ database/` | exit 1 — absent from the tree (§0A item 1) |
| `xargs ./vendor/bin/phpstan analyse` over all 49 changed `app/` files | `[OK] No errors` |
| `phpunit tests/Feature/Inventory/TransferReceiptGlBoundaryTest.php` | OK (1 test, 8 assertions) |
| `phpunit tests/Feature/Inventory/StockTransferReceiveDamageTest.php` | OK (4 tests, 11 assertions) |
| `phpunit tests/Feature/Inventory/StockTransferCloseTest.php` | OK (6 tests, 29 assertions) |
| `phpunit tests/Feature/Inventory/StockTransferReceiveLotsTest.php` | OK (3 tests, 27 assertions) |
| `phpunit tests/Feature/Inventory/StockTransferEdgeCasesTest.php` | OK (27 tests, 115 assertions, 2 skipped — both the `T1_RUN_KNOWN_REDS` recall reds) |
| **Reviewer probe** `tests/Feature/Inventory/ZzStockGlReviewerProbeTest.php` (5 cases, deleted afterwards) | **all PASS.** (1) lot-less damage → `inventory_exit`, `Posted`, `isChained()`, Dr `inventory_shrinkage_expense` `10.000` / Cr `inventory` `10.000`, `JournalEntryPosted` dispatched; (2) lot damage → `batch_write_off`, same accounts and amounts; (3) receipt-with-lot-damage **and** close-write-off produce **zero** `StockMovement::updated` events with 2 journals (append-only proof for the two paths the committed suite misses); (4) destination `stock_levels.quantity` = `SUM(inventory_batch_stock)` = `5.0000` both before and after a close write-off (batch invariant at the RECEIVING endpoint — asserted by neither register); (5) a nested caller commits the receipt + damaged counters with `journalCount() === 0` → G-3 |

Claimed by the implementer, **not re-run** here (the PostgreSQL runner is owned by the tenancy reviewer): every `phpunit-pgsql.xml` row of handback `:22-37`, in particular `StockTransferReceiveConcurrencyPostgresTest` (freight capitalised exactly once under a real two-process race), `TransferReceiptEventStreamTest`, `TransferReceiptSchemaRerunPostgresTest` and the seven PG CHECK constraints (see G-8). Also not verified: handback `:209-213` — see G-6.

## Tree state after review

```
$ git status --short
(empty)
$ git rev-parse --short HEAD
93b106461
```
The reviewer probe was deleted; the tree is byte-identical to `93b106461` (= `c30dcdf9c` plus the evidence-only handback commit). No edit, commit, merge or push was made by this reviewer. Note (matching the inventory register's I-10): no production file differs between `c30dcdf9c` and `93b106461`.

---

**What to fix before merge:** pin the GL side of the seam — assert accounts, direction, amount and `Posted` status on the shrinkage entry in both the lot-less and lot-tracked damage tests (G-1, code already verified correct by probe); then take the orchestrator's call on the account-mappability preflight (G-2) and the three-line nest guard (G-3), and add the two disclosure lines to the promotion checklist (G-4, G-7).

## Orchestrator rulings (session_01AcU81as26G2apodoimmyq7, 2026-09-10)
- **G-1: FIX** — six assertions (purpose→debit/credit pair, amount = qty × cost_price at company scale, `Posted`, `isChained()`, `JournalEntryPosted`) in both damage tests. Test-only.
- **G-2: ADOPT the preflight extension** — extend `StockTransferReceiptService` lines 124–133 with the account-mappability check (`hasInventoryWriteOffAccounts` / movement accounts) and a new `TransferReceiptFailureReason` (typed 422 before any stock or GL write). Plus one promotion-checklist line asserting both purposes resolve on the launch company.
- **G-3: ADOPT the nest guard** — `LogicException` when `DB::transactionLevel() !== 0` at the top of the receipt transaction (precedent `GeneralLedgerService.php:115,131`), one test.
- **G-4 / G-7: DISCLOSE** — one line each in `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` (destination-attributed shrinkage; GL-vs-subledger freight divergence with the ticket).
- **G-5: FIX** (delete the orphan docblock). **G-6: FIX** (handback `:209-213` → "reviewer gates pending"; never author reviewer verdicts inside the artefact under review). **G-8: RECORD** in the handback's verification section (SQLite legs prove no CHECK constraint).
- The S1 fix-round prompt is written after the tenancy-authz register lands.
