# Wave 0 dispatch brief — adversarial gate round 1

## 1. Gate verdict: REVISE

Do not dispatch this brief yet. The implementation intent is mostly well grounded, but the execution contract is not internally satisfiable. Most importantly, T10 necessarily removes acknowledged C2/C3 design-system violations (`apps/web/tools/audit-design-system-baseline.json:202,717`), while the final gate says any C1–C5 movement is a failure (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:483-492`). The brief also gives mutually incompatible manifest/commit instructions (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:57,142-152,336-340,446`), omits a second rev-5 Wave-0-owned claim while saying F-2 is the sole omission (`docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/00-EXECUTIVE-REPORT.md:240-248`; `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:48-51,271-290,527`), and contains deletion greps that cannot produce the promised evidence without unrelated or currently unlisted collateral changes (`apps/web/src/components/molecules/HubCard/HubCard.test.tsx:8-35`; `apps/web/e2e/money-campaign/finance-permissions.spec.ts:296-353`). These are dispatch-brief defects, not implementation discoveries that should be delegated to the executor.

## 2. Claim-verification table

| Claim | Verdict | Evidence and adversarial result |
|---|---|---|
| `canAccessModule` fails open and the map has 42 keys | **CONFIRMED** | The map is still `Partial<Record<string, Permission[]>>`, and an absent key still returns `true` (`apps/web/src/hooks/usePermissions.ts:20-72,127-134`). A mechanical literal comparison opened this run found 42 map keys. |
| The complete invalid-key set is four distinct keys: `goods-receipt.create-standalone`, `inventory.view`, `parts_catalog`, `partners` | **CONFIRMED** | The two sidebar literals are live (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:173-183,207-220`); `parts_catalog` occurs twice (`apps/web/src/routes/index.tsx:1426-1448`); `partners` occurs once (`apps/web/src/routes/index.tsx:2771-2782`). Comparing every literal in the named surfaces against the map produced no fifth distinct key. |
| The two sidebar keys should be self-mapped | **CONFIRMED** | Both are real generated permissions with the stated role grants (`apps/web/src/hooks/permissionsMap.generated.ts:89,107-118`), and the module map already uses the self-map pattern (`apps/web/src/hooks/usePermissions.ts:26-29,69-71`). |
| Both `parts_catalog` wrappers should be removed while retaining `ModuleGuard module="PlatformIntegration"` | **CONFIRMED** | Both routes have the valid module guard immediately outside the invalid permission wrapper (`apps/web/src/routes/index.tsx:1426-1448`), and no `parts_catalog` permission exists in the generated map while generated permissions around this domain use other names (`apps/web/src/hooks/permissionsMap.generated.ts:118-149`). |
| `partners` should become `permission="partners.view"`, not a route deletion | **CONFIRMED** | `partners.view` is generated (`apps/web/src/hooks/permissionsMap.generated.ts:146-149`); the current route uses the invalid module key (`apps/web/src/routes/index.tsx:2771-2782`); its page is a redirect to `/sales/customers` (`apps/web/src/features/crm/pages/CompanyListPage.tsx:1-10`). |
| The brief enumerates all `canAccessModule` call surfaces | **REFUTED** | It omits `CashPositionWidget`, which directly calls `canAccessModule('treasury')` (`apps/web/src/features/treasury/components/CashPositionWidget.tsx:124-132`). The omission does not add an invalid key, but it refutes the claimed complete call-surface inventory in the brief (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:162-174`). |
| Tightening the union should make typecheck fail on “exactly four sites” | **REFUTED** | There are five invalid literal occurrences: two sidebar sites, two `parts_catalog` sites, and one `partners` site (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:182,215`; `apps/web/src/routes/index.tsx:1431,1443,2777`). Also, the suggested readonly map shape is not accepted by the current mutable `hasAnyPermission(permissions: Permission[])` signature (`apps/web/src/hooks/usePermissions.ts:113-118`), so an additional local type error is expected unless that signature is changed first. |
| The live manifest drift contains the exact `InvoiceDetailPage` to `KeyedByRouteId` regression | **CONFIRMED** | The generator's wrapper set omits `KeyedByRouteId` (`scripts/factory/gen-route-manifest.mjs:30-34`), the wrapper is live around the invoice detail page (`apps/web/src/routes/index.tsx:311-329,697-708`), and the committed manifest still names `InvoiceDetailPage` (`scripts/factory/manifests/routes-web.yaml:750-757`). Running the drift checker, which generates only into a temp directory (`scripts/factory/check-manifest-drift.sh:8-20`), produced the claimed hunk. |
| The rest of the stated live drift is six new routes, eight finance permission corrections, one module-gate correction, and POS clean | **CONFIRMED** | The run produced those exact categories. CI still contains no manifest/preflight invocation; the only grep hits are comments (`.github/workflows/ci.yml:60-75,180-185`), while local preflight invokes the checker (`scripts/preflight.sh:193-195`). |
| Both named tests pin the preserved treasury-overview locale key | **OVERSTATED** | `i18nRawKeyCoverage.test.tsx` genuinely catalogs the exact raw key and asserts exact en/fr/ar values (`apps/web/src/lib/i18nRawKeyCoverage.test.tsx:62-82,98-110`). `Sidebar.test.tsx` pins that the sidebar uses the raw label key and the correct href (`apps/web/src/components/organisms/Sidebar/__tests__/Sidebar.test.tsx:134-165`), but it mocks `t(key)` to return the key (`apps/web/src/components/organisms/Sidebar/__tests__/Sidebar.test.tsx:25-30`), so it cannot detect deletion or corruption of the locale value. |
| T10's listed removals can yield its promised zero-reference proof without further scope | **OVERSTATED** | Besides the listed runtime imports/barrels/comments, current-source documentation still presents `ShiftDashboardPage` as a live public component and route (`apps/web/src/features/pos/README.md:94-155,268-278`; `apps/web/src/features/pos/IMPLEMENTATION_SUMMARY.md:299-311,354-367`). The design baseline also retains entries for both deleted files (`apps/web/tools/audit-design-system-baseline.json:202,717`). Those files are absent from T10's target inventory. |
| T11's `/marketing` deletion has no hidden production inbound link and the six destinations duplicate the sidebar group | **CONFIRMED** | The hub's six hrefs are `/pos/promotions`, `/pos/coupons`, both loyalty pages, companies, and contacts (`apps/web/src/features/marketing/pages/MarketingHubPage.tsx:24-67`); the sidebar group contains that same set (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:258-267`). The only live app registration is the lazy import/route and namespace wiring (`apps/web/src/routes/index.tsx:94-100,1980-1988`; `apps/web/src/lib/i18n.ts:33,89,197,254,402,442`). |
| T12 can prove zero `/finance` inbound references with its stated grep and listed deletions | **REFUTED** | Production navigation has no exact `/finance` link, but the proposed grep hits unrelated `HubCard` unit-test fixtures (`apps/web/src/components/molecules/HubCard/HubCard.test.tsx:8-35,51-64,83-104`). There are also unlisted route consumers: a dedicated E2E test asserts the finance hub twice (`apps/web/e2e/money-campaign/finance-permissions.spec.ts:296-353`) and the audit-shot tool visits `/finance` (`apps/web/tools/ui-audit-shots.mjs:30-45`). The feature deletion therefore requires explicit E2E/tool disposition and a production-only grep, not a claimed global zero. |
| The preserved treasury-overview key is genuinely borrowed by the sidebar and exists in all locales | **CONFIRMED** | The sidebar uses `finance:hub.cards.treasuryOverview.title` (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:292-300`), and the locale values are present at `apps/web/src/locales/en/finance.json:256-258`, `apps/web/src/locales/fr/finance.json:256-258`, and `apps/web/src/locales/ar/finance.json:39-44`. |
| T13 has zero inbound references to `/settings/chart-of-accounts`, and the finance mount is canonical | **CONFIRMED** | The duplicate settings mount is the only settings-path occurrence and carries the same `accounts.view` guard (`apps/web/src/routes/index.tsx:2326-2334`). Surviving links target `/finance/chart-of-accounts` in sidebar and command palette (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:297-301`; `apps/web/src/components/organisms/CommandPalette/useCommandPalette.ts:78-85`), and the finance route retains the same guard (`apps/web/src/routes/index.tsx:2029-2037`). |
| `useProductConfig().productName` exists and is the correct T14 mechanism | **CONFIRMED** | The context exposes `productName` (`apps/web/src/contexts/ProductConfigContext.tsx:19-25`), derives it from the single `PRODUCT_INFO` map selected by `VITE_APP_PRODUCT` (`apps/web/src/contexts/ProductConfigContext.tsx:29-41,53-75,87-102`), and the app already wraps all routes with its provider (`apps/web/src/App.tsx:25-40`). `renderWithProviders` supports deterministic product seeding (`apps/web/src/test/renderWithProviders.tsx:83-116`). |
| T14's test plan covers every rendering component changed | **REFUTED** | The required tests cover privacy rendering plus locale content only (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:423-434`). The tenant support page renders the affected subtitle (`apps/web/src/features/support-access/pages/TenantSupportAccessPage.tsx:14-30`), but its existing test wrapper has no `ProductConfigProvider` (`apps/web/src/features/support-access/__tests__/TenantSupportAccessPage.test.tsx:73-81`). No required failing-first render assertion proves that component passes `{ appName: productName }`. |
| F-2 is the only rev-5 Wave-0 omission | **REFUTED** | Rev 5 assigns all four split `UI-43` claims to Wave 0 (`docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/00-EXECUTIVE-REPORT.md:240-248`), but the brief explicitly excludes the 15-file `touchOptimized` claim (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:48-51,271-290`). `UI-07` is therefore not the only unassigned Wave-0-owned work. |
| T10–T14 are unauthorized Wave-1+/fixes-session scope creep | **REFUTED** | T10–T13 implement explicit post-audit owner rulings (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:35-46,54-60`); T14 implements the explicit config-placeholder ruling (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:7-18`). The owner also directed immediate action on trivially decidable work (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:73-76`). The dispatch's dossier exclusions match the separately owned remediation packaging (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:69-71`; `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:33-42`). |
| The working and handback rules match the repository | **OVERSTATED** | Worktree/fetch/no-force-push discipline matches repository rule 21 (`CLAUDE.md:89-96`), and TDD/scope/preflight match rules 2, 4, and 10 (`CLAUDE.md:18-28,42-43`). However, the suggested `fix(web): ...` messages (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:440-446`) violate the required `Phase <major.minor.patch>: <imperative summary>` format, and the whole-branch contract omits the required UI E2E run (`AGENTS.md:12-16`; `docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:481-492`). The handback fields themselves are otherwise complete (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:499-515`). |

## 3. Defects and required fixes

1. **The design-system final gate is impossible after T10.** T10 deletes one C2 source and one C3 source that are explicitly baselined (`apps/web/tools/audit-design-system-baseline.json:202,717`); the audit fails on stale entries (`apps/web/tools/audit-design-system.mjs:421-449`), while the brief forbids any C1–C5 movement (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:257-267,481-492`). **Required fix:** assign the baseline shrink to T10 and change the final delta contract to allow the exact deletion-driven C2/C3 decreases while allowing only detector-driven growth in C6. If T8-b is authorized, also account for its two C2 and one C3 entries (`apps/web/tools/audit-design-system-baseline.json:224-225,665`). Require a final categorized diff, not “only C6 moved.”

2. **Manifest ownership, ordering, and commit rules conflict.** The brief says everything except T2→T3 is order-free, T3's manifest commit changes nothing else, every deletion regenerates the manifest, and there is one commit per task (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:57,131-152,336-340,355-359,379-383,399-404,440-446`). Those statements cannot all hold. T9 also consumes T1/T3 outputs (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:310-316`). **Required fix:** publish one explicit dependency chain and one manifest-owner model. A coherent option is T2 → route deletions → one final T3 manifest housekeeping commit → T3 CI commit → T9, with an explicit exception to “one commit per task” and an explicit statement that deletion commits may temporarily drift. If every deletion must be independently green instead, require a manifest update in each deletion commit and remove the “one regeneration” language.

3. **T12's zero-reference command is guaranteed to fail on unrelated fixtures and misses meaningful non-production consumers.** `HubCard.test.tsx` intentionally uses `/finance` as generic test data (`apps/web/src/components/molecules/HubCard/HubCard.test.tsx:8-35,51-64,83-104`), while a finance-hub E2E and screenshot target are genuinely stale after deletion (`apps/web/e2e/money-campaign/finance-permissions.spec.ts:296-353`; `apps/web/tools/ui-audit-shots.mjs:30-45`). **Required fix:** add explicit disposition for the E2E case and screenshot target; retain or deliberately change the unrelated HubCard fixtures; replace the acceptance grep with a production-only search that excludes tests/fixtures and separately enumerate intentional test literals.

4. **T10's deletion inventory is incomplete and conflicts with the promised grep.** Current `src/` documentation still imports, documents, and routes `ShiftDashboardPage` (`apps/web/src/features/pos/README.md:94-155,268-278`; `apps/web/src/features/pos/IMPLEMENTATION_SUMMARY.md:299-311,354-367`), and the deleted runtime files have baseline entries (`apps/web/tools/audit-design-system-baseline.json:202,717`). **Required fix:** add both documentation files and the baseline shrink to T10's target list, or explicitly retain the documents as historical and narrow the grep accordingly. Current-tense import/route documentation should not survive a dead-code deletion.

5. **T4's diagnostic-count acceptance is false and its suggested readonly typing is incomplete.** The invalid set has four keys but five literal sites (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:182,215`; `apps/web/src/routes/index.tsx:1431,1443,2777`), and readonly map values cannot flow into the mutable `hasAnyPermission` parameter (`apps/web/src/hooks/usePermissions.ts:113-118`). The call-surface census also misses `CashPositionWidget` (`apps/web/src/features/treasury/components/CashPositionWidget.tsx:124-132`). **Required fix:** say “four invalid keys across five literal occurrences”; require `hasAnyPermission` (and preferably `hasAllPermission(s)`) to accept readonly arrays; include every direct caller in the compile check; then state the exact expected diagnostic set after the local readonly compatibility change.

6. **The T12 guard-test description assigns protection to a mock that cannot provide it.** The Sidebar suite returns every translation key unchanged (`apps/web/src/components/organisms/Sidebar/__tests__/Sidebar.test.tsx:25-30`); it protects label-key wiring/href, while `i18nRawKeyCoverage` alone protects the three locale values (`apps/web/src/lib/i18nRawKeyCoverage.test.tsx:98-110`). **Required fix:** describe those two distinct contracts accurately. Keeping both files unmodified is useful, but passing Sidebar alone must not be accepted as proof that the locale key survived.

7. **F-2 is not the only ownership omission.** Rev 5 assigns the split `UI-43` to Wave 0 (`docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/00-EXECUTIVE-REPORT.md:240-248`), while this brief defers `touchOptimized` (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:271-290`). **Required fix:** either assign that claim in this dispatch, obtain/record an explicit owner/orchestrator reassignment, or add it beside UI-07 as F-2b and prohibit reporting Wave 0/UI-43 complete. The size argument is a scheduling recommendation, not a source-backed ownership transfer.

8. **T14 lacks failing-first coverage for the support-access rendering path and will break an existing test harness.** The affected tenant page currently renders `t('subtitle')` without interpolation (`apps/web/src/features/support-access/pages/TenantSupportAccessPage.tsx:14-30`), and its test renders without the provider the new hook requires (`apps/web/src/features/support-access/__tests__/TenantSupportAccessPage.test.tsx:73-81`; `apps/web/src/contexts/ProductConfigContext.tsx:128-135`). **Required fix:** require a seeded-product render test for `TenantSupportAccessPage` that fails on the current literal, asserts the chosen product name, and asserts absence of `AutoERP`; update its wrapper via `renderWithProviders` or an explicit `ProductConfigProvider`. Keep the privacy render test as the second component-level proof.

9. **The repository workflow is not reproduced faithfully.** The brief recommends conventional commits and omits E2E from its final five checks (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:440-446,481-492`), while the repository requires `Phase <major.minor.patch>: <imperative summary>` commits and a UI E2E run before merging (`AGENTS.md:12-16`). T12 already has a finance-hub E2E that must change (`apps/web/e2e/money-campaign/finance-permissions.spec.ts:296-353`). **Required fix:** replace the commit examples with the repository format (or record an owner exception), add `pnpm --filter @autoerp/web test:e2e` to whole-branch evidence, and require screenshots for any intentionally skipped spec as the repository says.

10. **“Nothing is blocked” and “fourteen independently committable tasks” contradict the task set.** The mission says no item is blocked on an owner ruling (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:22-31`), but T6 and T8-b are explicitly blocked pending a parent decision (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:44-51,223-235,271-290,519-525`). **Required fix:** define T6/T8-b as non-executable escalation records outside the implementable task count, or change the entry-criteria statement to acknowledge those two blocked statuses. Do not call a no-commit BLOCKED record “independently committable.”

11. **T12 makes orphan-key cleanup optional while the global deletion checklist makes it mandatory.** T12 says other `hub.*` keys “may be pruned” (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:371-385`), but the deletion checklist requires every newly orphaned i18n key to be removed except the one preserved key (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:462-468`). **Required fix:** make the cleanup mandatory with per-key usage evidence, or add a named exception and acceptance reason. Remove “may.”

12. **The preflight completion wording is ambiguous for a frontend-only lane.** Default laptop-safe preflight skips PHPUnit and calls itself incomplete when no backend paths are supplied (`scripts/preflight.sh:13-39,71-102`), while the brief forbids PHPUnit for this wave yet requires preflight “green” (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:448-452,472,481-492`). **Required fix:** state the exact expected invocation and outcome: default/path-scoped preflight with the explicit PHPUnit skip is acceptable for this no-backend lane but must be reported as skipped, not green; all non-PHPUnit stages and the separately required frontend commands must be green.

## 4. F-1 assessment — BarcodeHero/successDot versus the Rafiq worktree

**Escalation scope: CONFIRMED.** The ruling of record parks the Rafiq skin (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:13-15`), and rev 5 explicitly says work under `features/products/editor/` waits on that decision (`docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/00-EXECUTIVE-REPORT.md:193-203,261-269`). Both targets are inside that directory: the live unconditional dot is at `apps/web/src/features/products/editor/components/ProductEditHero.tsx:115-136`, and `BarcodeHero` is a standalone component at `apps/web/src/features/products/editor/components/BarcodeHero.tsx:44-71`. The executor is therefore right not to overrule F-1.

**My merge-risk assessment: the source deletions are safely separable; the generated baseline is the real shared-file risk.** The registered Rafiq worktree points to `feat/rafiq-skin-experiment` (`.git/worktrees/erp.rafiq-skin/HEAD:1`), and the current production hero path imports/renders `ProductEditHero`, not `BarcodeHero` (`apps/web/src/features/products/sections/ProductHeroSection.tsx:1-2,20-39`). Read-only Git comparison of the Rafiq branch against its merge base found no branch-unique change under `features/products/editor/`, and its worktree was clean. That makes deleting unused `BarcodeHero` and removing the one `successDot` span low-risk, mechanically separable source changes rather than substantive conflicts with the present Rafiq work. However, deleting `BarcodeHero` also requires removing its two C2 and one C3 baseline entries (`apps/web/tools/audit-design-system-baseline.json:224-225,665`), and the Rafiq branch itself changes that shared generated baseline, so a baseline-file merge/re-generation conflict remains plausible. Recommendation to the owner: it is reasonable to authorize these two narrow deletions as merge-conflict-tolerable, provided the authorization is explicit and the baseline reconciliation is assigned; until then, preserve the BLOCKED escalation exactly as written (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:519-525`).

## 5. Completeness verdict, including F-2

**REVISE — the dispatch is not complete against rev 5.** The canonical Wave-0 closes-list is `CX-4`, `UI-11`, `UI-10`, `UI-01`, `UI-02`, `UI-07`, `UI-12`, `UI-14`, partial `UI-40`, split `UI-43`, and `UI-44` (`docs/sessions/UI-PRESENTATION-AUDIT-2026-08-10/00-EXECUTIVE-REPORT.md:240-248`). T1–T9 account for all of these except two pieces: `UI-07` is unassigned as F-2, and `UI-43`'s `touchOptimized` claim is explicitly unassigned/deferred (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:48-51,271-290,527`). In addition, T6 and T8-b are present only as BLOCKED records, so this dispatch cannot close `UI-14` or all of `UI-43` without the F-1 decision (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:223-235,271-298,519-525`). F-2 is therefore **not** the only omission, and the handback must be forbidden from claiming either Wave 0 or UI-43 complete.

The added T10–T14 work is **authorized expansion, not unowned scope creep**. `/pos/shifts`, `/marketing`, the duplicate chart-of-accounts mount, and `/finance` have explicit deletion rulings (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:35-46,54-60`); the app-name config placeholder has an explicit ruling (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:7-18`); and the owner directed immediate action on trivially decidable work (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:73-76`). No implementation task enters the event/shift-variance/other-problems dossiers, whose ownership is separately assigned (`docs/handoff/OWNER-DECISIONS-ui-audit-2026-08-10.md:69-71`), no listing migration or carve-out removal is dispatched (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:33-42,239-267`), and the two parked-editor targets remain blocked rather than implemented (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:44-51`). Subject to the corrections above, no other Wave-0 closes-list item is missing and no Wave-1/fixes-session/editor implementation has leaked into scope.

## 6. Residual risk

Even after correction, the largest residual risk is that T1's newly chosen graph/discovery rules produce a reproducible but semantically incomplete census; the report must therefore retain its per-page provenance and method disclosure (`docs/handoff/CODEX-DISPATCH-ui-wave0-2026-08-11.md:75-96`).
