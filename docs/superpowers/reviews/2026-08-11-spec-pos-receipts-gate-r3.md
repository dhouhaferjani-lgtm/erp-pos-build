# POS receipts/reporting spec adversarial gate — round 3

## 1. Gate verdict — FAIL

r3 visibly applies the requested edits for all seven round-2 defects, and N-3, N-4, N-6, and N-7 are now source-correct. It is not build-ready: N-1 still leaves one admitted `invoice_type_codes[]` combination with contradictory behavior, N-2 overstates the exception surface of `CanonicalPayloadReader`, and N-5's extra line-field “correction” points two historical receipt fields at mutable/current product relations instead of the receipt-line snapshots. Section 9.1 now honestly records the round-2 verdict (`SPEC:733-763` versus gate r2 `:9-25`); §9.2 accurately lists the edits but its claims that all seven are closed and both extra N-5 corrections are source-verified are not honest completion claims while the defects below remain (`SPEC:767-779`).

## 2. Per-defect verification

| Defect | Verdict | Observed spec evidence | Source verification / conclusion |
|---|---|---|---|
| **N-1** | **PARTIAL** | The REFUND/VOID contradiction is removed: disjointness is explicitly a screen contract, the register table freezes each screen's emitted codes, and enforcement is assigned to FT-16, BT-2, and BT-2b (`SPEC:139-154,598-599,638`). However, the API also promises to “honour exactly what validation admits” (`SPEC:151`) while validation admits `TRAINING` independently (`SPEC:521-527`) and the server simultaneously default-excludes training unless `include_training=true` (`SPEC:137,157,369,527`). | The intended two screens are deterministic, but `invoice_type_codes[]=TRAINING` with absent/false `include_training` has two incompatible outcomes. N-1 is therefore not fully closed. See R3-1. |
| **N-2** | **PARTIAL** | `refund_reason_source` and `refund_policy_alerts` are now on the refund/void list-row allowlist (`SPEC:256-262`); `refund_reason_source` is on lineage rows (`SPEC:278-282`) and in the §4.5 wire table (`SPEC:500-505`). The mechanic now bulk-loads fiscal events and calls the reader per hydrated row in memory (`SPEC:507-513`). | `CanonicalPayloadReader::forSaleReceipt()` performs no query: it reads the supplied model and constructs DTOs (`apps/api/app/Modules/Fiscal/Application/Services/CanonicalPayloadReader.php:63-129`). The core fix is valid. But r3's claim that it throws `InvalidArgumentException` in “exactly two cases” (`SPEC:512,772,796`) contradicts the reader's own docblock and DTO calls (`CanonicalPayloadReader.php:58-61,79-115`), which can also throw on malformed sub-objects; `FiscalPayloadArrayGuards.php:39-49` is one direct example. See R3-2. |
| **N-3** | **PASS** | The frozen map re-keys Tables to `pos.manage_tables`, retains `ModuleGuard module="Tables"`, keeps Kitchen on operate+Menu, keeps Vouchers on `moduleKey="pos"`, adds the manage-tables identity key, and aligns FT-14/FT-15 (`SPEC:452-468,636-637`). | The live routes match: Tables uses `pos.manage_tables` plus `ModuleGuard Tables` (`apps/web/src/routes/index.tsx:2940-2951`), Kitchen uses `pos.operate_terminal` plus `ModuleGuard Menu` (`:3193-3205`), and Vouchers use `moduleKey="pos"` (`:3113-3132`). The generated map gives cashier operate/manage-shifts/view-receipts but not manage-tables/view-reports (`apps/web/src/hooks/permissionsMap.generated.ts:170-189`). |
| **N-4** | **PASS** | A-1 freezes one composite `compliance` key mapping the three permissions, a top-level bottom-section sibling immediately above Settings, and per-panel gates; OI-16 records the owner-overturnable IA decision (`SPEC:432-448,726`). | `NavItem`/`NavChild` accept one permission key (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:105-122`); `canAccessModule` applies any-of to its mapped list (`apps/web/src/hooks/usePermissions.ts:127-134`). Settings is a bottom leaf with no children (`Sidebar.tsx:358-364`), its key maps to `settings.view` (`usePermissions.ts:54`), accountant lacks that permission (`permissionsMap.generated.ts:249`), and the parent is filtered before children (`Sidebar.tsx:443-455`). The top-level composite resolution is implementable without a component change. |
| **N-5** | **PARTIAL** | BT-5 now asserts the six-key meta shape and ISO window values; all three VAT-sum statements use `net_amount`/`vat_amount`; and the VAT allowlist uses `tax_rate` (`SPEC:245,272-276,602,611,617`). | The meta block is currently hand-built with four paginator keys (`apps/api/app/Modules/POS/Presentation/Controllers/ReceiptController.php:135-143`), so adding ISO `from`/`to` is coherent. `tax_rate`, `net_amount`, `vat_amount`, and `gross_amount` are the real VAT-detail fields (`apps/api/database/migrations/tenant/2026_01_08_190639_create_pos_receipt_vat_details_table.php:33-38`; `apps/api/app/Modules/POS/Domain/ReceiptVatDetail.php:41-47`). Thus the requested N-5 fixes and the `tax_rate` extra correction are correct. The second extra correction—the line-field source map—is not; see R3-3. |
| **N-6** | **PASS** | BT-13 now asserts `pos_receipt_prints` and `PrintMethod::Pdf` on both PDF endpoints and explicitly rejects an invented download/stream audit distinction (`SPEC:610,776`). | The model table is `pos_receipt_prints` (`apps/api/app/Modules/POS/Domain/ReceiptPrint.php:34-46`); the enum contains Pdf/Thermal/EscPos only (`apps/api/app/Modules/POS/Domain/Enums/PrintMethod.php:7-11`); both controller actions pass `PrintMethod::Pdf` (`apps/api/app/Modules/POS/Presentation/Controllers/ReceiptController.php:604-609,637-642`). |
| **N-7** | **PASS** | The preflight command now sets both scope variables, four Playwright paths are frozen, the exact Chromium invocation is supplied, and the live-stack helper versus mocked-fixture rule is binding (`SPEC:647-691`). | Unset `PREFLIGHT_VITEST_PATHS` really falls back to full `pnpm test`, while a set value runs scoped Vitest (`scripts/preflight.sh:197-203`; the script has already changed into `apps/web` at `:163-167`). Playwright's `testDir`, base URL, Chromium project, and web server match the spec (`apps/web/playwright.config.ts:3-26`). `loginAsRole` uses real seeded credentials against the live API (`apps/web/e2e/money-campaign/helpers.ts:3-8,10-28,48-57`), whereas `e2e/fixtures.ts` mocks `/auth/me` as admin (`apps/web/e2e/fixtures.ts:1-38`). |

## 3. New defects

| ID | Severity | Defect | Required correction |
|---|---|---|---|
| **R3-1** | **MAJOR** | The new N-1 sentence “honours exactly what validation admits” includes `TRAINING`, but the independent default training exclusion says the same admitted request returns no training row. The unresolved input is `invoice_type_codes[]=TRAINING` with `include_training` absent/false (`SPEC:151,157,521-527`). | Freeze precedence or reject the inconsistent combination. For example: require `include_training=true` whenever `TRAINING` is explicitly requested, and add a 422 assertion; alternatively state that training exclusion overrides the code array and remove “honours exactly.” |
| **R3-2** | **MINOR** | r3 says the reader throws `InvalidArgumentException` in exactly two cases (`SPEC:512,772,796`). Observed source says it can also throw when payload/sub-object DTO construction fails (`CanonicalPayloadReader.php:58-61,79-115`), with direct guard throws such as `FiscalPayloadArrayGuards.php:39-49`. | State that the two early guards are not exhaustive. Catch `InvalidArgumentException` around the entire per-row reader call, log/degrade that row, and test at least one malformed typed-subobject case if such degradation is intended. |
| **R3-3** | **MAJOR** | The new N-5 line-field map says `product_sku` and `unit_of_measure_code` come from the current eager-loaded product/UOM (`SPEC:276,775`). That discards the immutable receipt snapshots and fails when `product_id` is null/unresolvable. `ReceiptLine` identifies `product_code` as the immutable sale-time code and stores `unit` (`apps/api/app/Modules/POS/Domain/ReceiptLine.php:21-40,69-90`); the fiscal projector explicitly allows a null product FK because the sealed snapshot remains authoritative, then writes canonical SKU to `product_code` (`PosCoreReceiptProjection.php:1157-1176,1181-1197`). Legacy creation likewise snapshots sellable code and unit (`ReceiptCreationService.php:205-223,331-340`). | Map the wire's SKU/code from `ReceiptLine.product_code`. Use the stored `ReceiptLine.unit` for the historical unit (prefer a wire name such as `unit` unless its semantics are genuinely a code), or explicitly separate historical snapshot from current-UOM enrichment. Keep current `product.unitOfMeasure` enrichment only for `quantity_decimals` with its documented fallback. |

## 4. Holistic artifact and revision-history audit

**Stall/resume artifacts:** none observed. The spec has one continuous numbered section sequence (§1 through §10), one §9.1 and one §9.2, one N-1…N-7 history row each, no duplicated headings, no repeated/incomplete r3 amendment block, and no interruption markers. Historical descriptions of superseded text (for example “whatever the client sends,” “Settings → Compliance,” and `receipt_print_audits`) occur only where r3 explains what r2 got wrong (`SPEC:143,435-448,610,733-779`); they are not duplicate live contracts. The only live contradictions found by the holistic read are the three new defects R3-1 through R3-3 above.

**§9.1 honesty:** **PASS.** It explicitly downgrades r2's former completeness claim, reports the same seven PASS / four FAIL / four PARTIAL split recorded by gate r2, and explains why the old “where r2 fixes it” rows remain as an edit history rather than a completeness claim (`SPEC:733-763`; `docs/superpowers/reviews/2026-08-11-spec-pos-receipts-gate-r2.md:9-25`).

**§9.2 honesty:** **FAIL as a completion claim.** Its row descriptions accurately identify the r3 edits, but “All seven are applied,” “Nothing from gate r2 was skipped,” and the claim that both extra N-5 corrections were source-verified overstate the result (`SPEC:767-779`). N-1 remains ambiguous for explicit TRAINING requests (R3-1), N-2's “exactly two” exception statement is source-false (R3-2), and the second extra N-5 correction maps historical line values to mutable current-product data (R3-3).

## 5. Build-readiness

The spec is **not build-ready**. Resolve R3-1 through R3-3 and correct §9.2's completion claims before the owner read-through.
