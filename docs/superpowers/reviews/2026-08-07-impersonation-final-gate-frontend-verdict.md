# Final-Gate Adversarial Review — Frontend Conventions — `codex/tenant-impersonation`

**Date:** 2026-08-07 · **Reviewer:** frontend-conventions-reviewer (Opus) · **Diff:** `46fd7decd..f3466dadc`
**VERDICT: REJECT.** 1 BLOCKER / 6 MAJOR / 12 MINOR.

## Gates re-run (all green — findings are what tooling can't see)
web lint exit 0 (0 new keys, design-system 743 ack/0 new, quantity 0); typecheck exit 0; no baseline touched; no eslint-disable/ts-expect-error/as any/.skip/.only added; support-access + inventory tests 5 files/17 pass; POS 3 files/14 pass; zombies 0.

**React Doctor mystery SOLVED:** clean at both bases on both apps (web 92/100, pos 100/100). The commit-time "staged regressions" was **base selection, not code** — `46fd7decd` is NOT an ancestor of origin/dev (merge-base is `6f14f8232`), so a hook defaulting to origin/dev scanned a large unrelated file set. The lane's 92/100 claim is honest and reproducible.

## BLOCKER 1 — Persistent banner absent from POS terminal + entire admin console
`DashboardLayout.tsx:45` is the ONLY mount. No banner on `/pos/transactions` (full-screen cash terminal), `/pos/shifts`, `/pos/kitchen`, `/company-onboarding`, or any `/admin/*` — all render outside that tree while a live subject token is on every request. Requirement 6 fails both halves (not across layouts; unmountable by nav). Fix: mount `<ImpersonationBanner/>` once in `App.tsx:31` above `<AppRoutes/>` (inside BrowserRouter; returns null without context), delete the DashboardLayout mount.

## MAJOR
- **M2 — impersonation bearer token written to localStorage** (authStore.ts:106-113, key autoerp-auth) while the less-sensitive super-admin token is deliberately memory-only (adminAuthStore.ts:14-17). Subject token survives tab close, is visible cross-vertical on shared localhost, and starting/exiting a session destroys/logs-out any real tenant user's persisted session. Fix: memory-only store for the impersonation token, or exclude `token` from partialize when `user.impersonation !== null`.
- **M3 — every feature form is validation-free** (RequestAccessDialog, SupportWindowForm, ElevationDialog): no zodResolver, raw `<label>` not FormField, errors never read; SupportWindowForm lacks starts_at<expires_at. Violates conventions/06-FORMS.md. Fix: zod schemas + zodResolver + FormField translated errors.
- **M4 — every mutation failure is silent, incl. the exit kill-switch.** All handlers fire-and-forget (GrantQueue/IncomingRequests/ElevationQueue/ActiveSessionPanel/AdminSupportAccessPage/ElevationDialog). ImpersonationBanner.tsx:33-42 exit: if server exit fails, client discards token + reports success while server session lives to TTL. Fix: `.catch → toast.error` (sonner is house toast); exit clears local state only on confirmed 2xx.
- **M5 — new feature dir never enrolled in the token/i18n gates → 6 hardcoded colors already.** eslint.config.js:240-247 + 358-382 enumerate dirs; support-access in neither; global rule misses `white`+alpha. Live: ImpersonationBanner.tsx:46/49/50/53 (text-white, text-white/90, bg-white/15, border-white/40 bg-white/10 hover:bg-white/20). Fix: enroll dir in both blocks; use tokens.text.inverse; EXTEND designTokens.ts with white-alpha tokens (not a neighbour shade). **Land this fix first — it's the gate that would have caught the rest.**
- **M6 — admin consent queue silently truncated to newest 20.** Backend paginates (per_page 20, real meta); client never sends page/per_page, discards meta (adminSupportAccessApi.ts:15-17, useAdminSupportAccess.ts:14). A pending request past row 20 is unreachable. Fix: thread page/per_page + render pager from meta (or status-filter server-side + surface total).
- **M7 — two hand-rolled `<dialog>` modals duplicate canonical Modal organism** (RequestAccessDialog.tsx:37, ElevationDialog.tsx:22). Fix: render both through organisms/Modal.

## MINOR (8-19, land in same pass)
8 status chips hand-rolled vs StatusBadge atom. 9 `tenantSupportAccessKeys` dead code — test asserts scoping on an unused artifact (test-integrity gap). 10 invalidates unscoped `['support-access']` prefix. 11 `meta` mistyped as OffsetPaginationMeta (unchecked cast; envelope handling itself correct). 12 admin overview shows all operators' sessions + a write-access button that backend refuses (combines with M4 → silent no-op button). 13 AdminLayout sentinel-string translation smuggle. 14 stale Sidebar comment. 15 `active.description` uses {{count}} with no plural forms in en/fr/ar → "3 active support session" (**otherwise i18n complete — all keys present in all 3 locales incl. ar; i18n.ts all 3 places wired**). 16 banner shows subject UUID until /auth/me resolves. 17 no auto-exit at expiry countdown 00:00 → next 401 routes to tenant /login not /admin. 18 the 3 POS + 1 inventory test edits are LEGITIMATE pre-existing-red repairs (each verified against byte-identical source), but are scope creep — split into a separate `fix(tests)` commit. 19 tip commit self-declares WIP/pending-final-gate → not mergeable as-is.

## Verified clean (no finding)
Route/permission gating (RequireAdminAuth + RequirePermission + SERVER_AUTHORITATIVE_PERMISSIONS, backend middleware matches, no module leak); tenant query keys scoped; API unwrapping correct against all 4 controllers (no double-unwrap); AuthProvider (/auth/me returns impersonation, fresh login can't inherit stale state, queryClient.clear before setAuth/logout, types regenerated); exit POST allowlisted in readonly_post_routes; no dead Tailwind interpolation, no parseFloat on money/qty, no raw table.
