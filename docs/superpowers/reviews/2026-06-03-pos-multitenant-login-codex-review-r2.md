## Round-1 Resolution Audit

- MAJOR 1: RESOLVED. The revised spec now distinguishes unknown/stale tenant and bad password from inactive users and suspended/archived organizations, matching `apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:235`, `apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:255`, `apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:261`, and `apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:269`.
- MAJOR 2: RESOLVED. The revised signature preserves `opts.signal` and explicitly threads it through the first login POST, auto-select re-POST, manual-pick POST, and `/user/companies`, addressing the existing cancel path at `apps/pos/src/pages/LoginPage.tsx:53`, `apps/pos/src/pages/LoginPage.tsx:57`, `apps/pos/src/pages/LoginPage.tsx:80`, `apps/pos/src/stores/authStore.ts:167`, and `apps/pos/src/stores/authStore.ts:204`.
- MAJOR 3: RESOLVED. The revised spec explicitly requires disabled organization buttons, same-tenant repeat-click ignoring, different-tenant blocking while pending, and store-level single-flight behavior for picker submits.
- MAJOR 4: RESOLVED. The revised spec corrects the T1.1 invariant to a network-ordering guard, acknowledges sequential storage writes, and makes `LOGIN_TENANT_ID` last, best-effort, and non-authoritative.
- MAJOR 5: RESOLVED. The revised spec explicitly scopes the durable reset path to Sub-Spec B and documents the owner-approved Sub-Spec A interim reset as clear app data / reinstall.
- MINOR 1: RESOLVED. The revised spec now names the unauthenticated organization-list disclosure and states the owner-approved choice to display business name plus slug.
- MINOR 2: RESOLVED. The revised spec adds a one-shot auto-select guard and typed `UnexpectedLoginResponseError` for explicit-tenant picker-shaped responses.
- MINOR 3: RESOLVED. The revised testing section now covers wrong password after auto-select, `/user/companies` failure after auto-select, `LOGIN_TENANT_ID` write failure, abort during the chained flow, and explicit-tenant picker-shaped contract violations.

## New Findings

Focus-area audit:

1. Single-flight guard vs manual-pick re-invocation of `login()`: No issue found. The legitimate manual pick happens only after the email-first call returns `{ status: 'requires_org_selection' }` (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:117`), while the guard is intended to block overlapping attempts (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:147`); current POS submit awaits `login()` before continuing (`apps/pos/src/pages/LoginPage.tsx:57`), so the first call should no longer be in flight when the picker is rendered.
2. One-shot auto-select guard logic: No issue found. The guard is tied to whether `opts.tenantId` was already supplied (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:113`), which matches the backend's explicit-tenant branch forcing a single tenant id (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:192`) and the picker branch only running without an explicit tenant (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:205`).
3. Best-effort `LOGIN_TENANT_ID` write ordering vs T1.1 snapshot/restore: No issue found. The hint is specified as non-authoritative (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:158`) and written after successful authentication (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:159`), while the existing T1.1 guarded point is the token-backed `/user/companies` fetch and restore window (`apps/pos/src/stores/authStore.ts:193`, `apps/pos/src/stores/authStore.ts:200`, `apps/pos/src/stores/authStore.ts:204`, `apps/pos/src/stores/authStore.ts:211`).
4. AbortSignal propagation across the auto-select re-POST: No issue found for the auto-select chain. The spec requires reusing `opts.signal` for the email-first POST, auto-select re-POST, manual-pick POST, and `/user/companies` (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:129`), matching the current low-level request option forwarding (`apps/pos/src/lib/api.ts:94`, `apps/pos/src/lib/api.ts:124`).
5. Backend failure-contract table accuracy: No issue found. The table matches the real check order: zero tenants throw `auth.no_organizations` (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:197`), multi-tenant email-first returns picker before credential validation (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:205`), unknown explicit tenant and bad credentials throw `auth.invalid_credentials` (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:235`, `apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:255`), inactive users throw `auth.account_not_active` (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:261`), and suspended/archived tenants return `403 ORGANIZATION_UNAVAILABLE` (`apps/api/app/Modules/Identity/Presentation/Controllers/AuthController.php:269`).
6. Scope boundary to B/C: No issue found. Sub-Spec A explicitly excludes logout/device-unbind hardening and audit-event emission (`docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:75`), while the currently visible logout paths still call `authStore.logout()` (`apps/pos/src/components/Header.tsx:382`, `apps/pos/src/components/Header.tsx:390`, `apps/pos/src/pages/PinEntryPage.tsx:27`, `apps/pos/src/pages/PinEntryPage.tsx:36`), so the dependency on B is visible rather than hidden.
7. TDD/acceptance criteria: One minor gap found below.

- **[MINOR] F-1: Manual-pick cancellation is required but not explicitly tested**
  - Problem: The spec requires the same `AbortSignal` to reach the manual-pick POST, but the TDD list only names abort during the email-first POST, auto-select re-POST, and `/user/companies`. Because the current POS page creates an `AbortController` only in the form submit path and clears the ref when that attempt settles, the manual picker implementation needs its own fresh controller path; without an explicit test, this is easy to miss while still satisfying the listed authStore abort cases.
  - Evidence: `docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:129`, `docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:146`, `docs/superpowers/specs/2026-06-03-pos-multitenant-login-design.md:252`, `apps/pos/src/pages/LoginPage.tsx:53`, `apps/pos/src/pages/LoginPage.tsx:57`, `apps/pos/src/pages/LoginPage.tsx:74`, `apps/pos/src/pages/LoginPage.tsx:80`
  - Fix: Add explicit `LoginPage` and/or `authStore` tests for aborting a manual organization selection after the picker is displayed, asserting that the tenant-bound POST receives a fresh signal and no token/user/company/tenant state is committed after cancel.

## Verdict

APPROVE-WITH-MINOR-EDITS
Confidence: HIGH
Summary: The round-1 issues are genuinely addressed for the revised Sub-Spec A scope: the backend contract is now accurate, abort propagation is specified for the chained login flow, picker concurrency is called out, the tenant hint is correctly non-authoritative, the reset trap is explicitly deferred to B, and the anti-loop guard matches the backend. The remaining gap is narrow: manual organization selection is part of the required abort surface, but the tests do not explicitly force a fresh picker-selection `AbortController` path.
