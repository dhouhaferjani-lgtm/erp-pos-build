# Codex spec gate r11 — T-2/T-3 (gpt-5.6-sol, high, read-only, 2026-09-09)

## Rev-10 closure table

| Rev-10 finding | Rev-11 disposition |
|---|---|
| r10-M1.1 — product show `stock_quantity` absent from R5 and T9 | **CLOSED at rev-11 anchor.** The product-show aggregate is catalogued and its exact `7391.4517` value is asserted in 6t-c (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:261,291,318,634,672`). The code does compute and return that scale-4 company aggregate (`apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:304-319,356-374`; `apps/api/app/Modules/Product/Application/DTOs/ProductData.php:131,166-183`). |
| r10-M1.2 — batch list and POS batch-suggestion response shapes absent | **CLOSED at rev-11 anchor.** Both shapes are now enumerated separately (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:259,322-323`) and exercised in 6u-d/e (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:635,673`). The list’s eager-loaded `batch_stock[]` and the distinct FEFO suggestion envelope match code (`apps/api/app/Modules/BatchExpiry/Infrastructure/Persistence/BatchRepository.php:89-119`; `apps/api/app/Modules/BatchExpiry/Application/DTOs/BatchSuggestionResultDTO.php:23-35`). |
| r10-M1.3 — counting reconciliation claimed but not exercised | **CLOSED at rev-11 anchor.** Step 5d creates a real location counting after receipt, 6s-c proves the admin reconciliation exposes the three snapshots, and 6s-d proves both counter projections omit them (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:646,670-671`). The service copies positive `stock_levels.quantity` into `theoretical_qty` and the counter transformers omit it (`apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:277-302,505-559,1053-1073`; `apps/api/app/Modules/Inventory/Presentation/Controllers/InventoryCountingController.php:355-377`). |
| r10-M1.4 — rebalance presence control was conditional | **CLOSED at rev-11 anchor.** L3 and deterministic thresholds now force exactly one P row, with `7391.4517 − 1000.0000 = 6391.4517` (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:634,639,646,672`). This follows the actual deficit-plus-surplus condition (`apps/api/app/Modules/Inventory/Application/Services/StockRebalanceQueryService.php:61-115`). |
| r10-M1.5 — T9 omitted two named batch routes | **CLOSED at rev-11 anchor.** 6u now makes all five requests and asserts the per-lot position on each (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:635,673`). |
| r10-m1 — stale baseline pin | **CLOSED at rev-11 anchor.** The structural rule is stated at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:5`. Audit HEAD is `76b6e412ec1906747306d4b91fc1955237b188cc`; the diff from pinned `324b8a2c7b7486fad374a07b22bcddf7a9e45e92` contains only the rev-11 spec and prompt under `docs/`, with no production file. |
| r10 rejected findings concerning R5 visibility, readers, GL, events, locks, permissions, one-writer and migrations | **REJECTED-correctly.** The underlying code remains unchanged since r10, and the corresponding rev-11 contracts remain coherent (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:135-225,327-486,683-690`). |

## BLOCKER

None.

## MAJOR

None.

## MINOR

### m1. The R5 implementation is correct, but several catalogue cross-references remain internally stale

This does not weaken the executable oracle, but the fix round should make the prose agree with the final table:

- The claim that the “count of R5 rows below matches” items 1–10 and table rows 19–25 is false (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:295`). There are ten numbered R5 families, seven dedicated surface rows 19–25, and twelve `R5` response-shape rows at lines 313–324 because the batch family is decomposed into three wire shapes and the POS types are a separate row.
- The POS device types do not mirror rows 19–23 and 25 as claimed (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:260,290`). The cited types represent POS stock-level and stock-distribution positions—surface rows 8–9—not product show, counting, rebalance, batch, or the other rows (`apps/pos/src/lib/db/repositories/locationStockRepository.ts:45-61`; `apps/pos/src/types/stockDistribution.ts:6-29`).
- Four references still say rows 19–24 and therefore omit new row 25: cache hygiene, the `R` definition and the applied-R5 ruling (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:507,604,698`). The change-log assertion that the prior cross-references were corrected is consequently overstated (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:18`).
- T9’s overview calls matrix and both POS responses part of “rows 19–25,” although those mixed R5 surfaces are rows 7–9 (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:577,597`; actual rows at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:273-275`).

These are editorial anchor errors only. The concrete steps 6p–6u and their values remain executable and correct.

## Citation audit

All 964 line-qualified citation occurrences, representing 597 unique path/range triples, resolve to existing files and in-range lines. Production citations previously verified in r10 are unchanged; rev-11’s newly added product, batch, counting, threshold and rebalance citations were re-derived from code.

| Claim | Verified/wrong with the real line |
|---|---|
| Baseline remains current despite documentation-only child commits | **VERIFIED.** The structural rule is at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:5`; the pinned-to-audit diff has no non-`docs/` path. |
| Product show returns company-wide scale-4 `stock_quantity` | **VERIFIED.** Query at `apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:304-319`, formatting at `apps/api/app/Modules/Product/Application/DTOs/ProductData.php:166-183`, response at `apps/api/app/Modules/Product/Presentation/Controllers/ProductController.php:356-374`. |
| Batch list exposes roll-ups and per-location `batch_stock[]` | **VERIFIED.** Eager load at `apps/api/app/Modules/BatchExpiry/Infrastructure/Persistence/BatchRepository.php:116-119`; emitted members at `apps/api/app/Modules/BatchExpiry/Presentation/Resources/BatchResource.php:39-40,55-60`. |
| POS batch suggestions expose lot availability through a distinct envelope | **VERIFIED.** DTO fields are at `apps/api/app/Modules/BatchExpiry/Application/DTOs/BatchSuggestionDTO.php:27-41` and `apps/api/app/Modules/BatchExpiry/Application/DTOs/BatchSuggestionResultDTO.php:23-35`; FEFO caps by availability at `apps/api/app/Modules/BatchExpiry/Domain/Services/FEFOInventoryService.php:143-185`. |
| Expiring/expired batch routes introduce another response shape missing from the table | **VERIFIED false positive.** Both return the same `BatchResource::collection` shape already represented by the batch-list row (`apps/api/app/Modules/BatchExpiry/Presentation/routes.php:14-15`; `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:215-258`; `apps/api/app/Modules/BatchExpiry/Presentation/Resources/BatchResource.php:20-64`). |
| Counting C snapshots exactly P, PB and PC after step 5b | **VERIFIED.** Location scope filters at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:537-542`, positive-only predicate at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:558`, and snapshot copy at `apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:291-298`. |
| Counting creation affects movements or earlier movement-feed assertions | **VERIFIED false.** Creation only builds document/items/assignments (`apps/api/app/Modules/Inventory/Application/Services/InventoryCountingService.php:73-120`); the spec correctly states movement occurs only on finalize (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:646`). |
| Rebalance fixture emits exactly one P row | **VERIFIED.** Thresholds are read from stock rows at `apps/api/app/Modules/Inventory/Application/Services/StockRebalanceQueryService.php:36-49`; a row requires both deficit and surplus at `apps/api/app/Modules/Inventory/Application/Services/StockRebalanceQueryService.php:92-94`. P has both; PB and PC do not. |
| Table-P arithmetic after step 5b | **VERIFIED.** P: `2391.4517 + 5000.0000 = 7391.4517`; PB remainder: `2617.2500 − 1017.2500 = 1600.0000`; sent total: `7391.4517 + 2617.2500 = 10008.7017`; rebalance excess: `7391.4517 − 1000.0000 = 6391.4517`; FEFO shortfall: `2000.0000 − 1017.2500 = 982.7500` (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:602-604,634-635`). |
| POS types mirror surface rows 19–23 and 25 | **WRONG association.** Claim at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:290`; the cited members mirror POS rows 8–9 (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:274-275`). |
| R5 response-row counts match items 1–10/table rows 19–25 | **WRONG count.** Claim at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:295`; actual R5 shape rows occupy `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:313-324`. |
| R5 references to rows 19–24 remain current | **WRONG/stale.** Occurrences at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:507,604,698`; product-show row 25 is at line 291. |
| T9’s matrix/POS R5 controls belong to rows 19–25 | **WRONG cross-reference.** Claims at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:577,597`; matrix and POS are rows 7–9 at lines 273–275. |
| Exactly three in-transit aggregate readers require remainder conversion | **VERIFIED.** Two queries in `apps/api/app/Modules/Inventory/Application/Services/LocationStockQueryService.php:137-165,217-245`, matrix at `apps/api/app/Modules/Inventory/Application/Services/StockMatrixQueryService.php:390-408`, and WAC at `apps/api/app/Modules/Inventory/Application/Services/WeightedAverageCostService.php:117-127`. No other application reader aggregates transfer incoming by `status = in_transit`. |
| Status values fit the existing column | **VERIFIED.** Column is `string(20)` at `apps/api/database/migrations/tenant/2026_05_28_120000_create_stock_transfers_table.php:41-42`; `closed_with_writeoff` is exactly 20 characters. |
| Current status exhaustiveness sites are identified | **VERIFIED.** PHP match/helpers are centralized at `apps/api/app/Modules/Inventory/Domain/Enums/TransferStatus.php:17-49`; web action predicates are at `apps/web/src/features/stock-transfers/pages/StockTransferDetailPage.tsx:38-39`, and the exhaustive badge record is at `apps/web/src/features/stock-transfers/components/StockTransferStatusBadge.tsx:5-10`. |
| Damage and write-off use the shrinkage GL family | **VERIFIED.** Reasons exist at `apps/api/app/Modules/Inventory/Domain/Enums/MovementReason.php:26-28`, map to Shrinkage at lines 73–93, and require GL at lines 96–112. The bridge posts the shrinkage counter at `apps/api/app/Modules/Inventory/Application/Services/InventoryGlPostingService.php:185-187`. |
| Stored events have an aggregate/idempotency anchor and sufficient replay fields | **VERIFIED.** Explicit receipt-id streams and versions are specified at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:181-198`; header and line fields include identities, counters, lots, movements, statuses, actor and hashes at lines 189–197. |
| New routes inherit rule-12 middleware and module gating | **VERIFIED.** Existing Inventory group is at `apps/api/app/Modules/Inventory/Presentation/routes.php:31`, with transfer routes at lines 98–117; the final route/permission matrix is specified at `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:400-418`. |
| D1 is necessary rather than duplicating `inventory.view` | **VERIFIED.** `inventory.view` is seeded to broad non-supervisory roles at `apps/api/database/seeders/RolesAndPermissionsSeeder.php:702,741,771,806`; rev 11 limits reconcile and close to manager/admin while keeping them independently grantable (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:400-403`). |
| Migration is additive and requires no database-enum alteration | **VERIFIED.** Status is already a string, additions are nullable/defaulted, constraints/indexes are guarded, and the completed-only backfill is predicate-idempotent (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:89-129,683-690`). |

## Rejected false positives

- Blind receiving now follows the owner’s exact document-only ruling. Receiver-view, blind list/show, receipt echo, static 403/422 envelopes, replenishment, carrying movement feeds and notifications exclude or mask transfer-derived quantities (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:244-263,299-312`). Batch allocations remain identity-only in the receiver projection, `partially_received` is the accepted coarse R3 signal, and `NOTHING_TO_RECEIVE` contains only static text and IDs (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:248,263,299,305,360`).

- R5 is applied rather than reopened: on-hand, available, lot stock and accepted multi-receiver delta inference stay visible. T9 asserts absence only on transfer-derived members and presence on the stock-position members, including product show, counting, deterministic rebalance and both newly covered batch shapes (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:313-324,592-676`).

- §5.10 remains necessary and correct. Both movement controllers expose other receivers’ rows today, and rev 11 requires the three quantity/balance fields to be nulled only for carrying-transfer rows while retaining terminal and non-transfer history (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:423-440`; code emitters at `apps/api/app/Modules/Inventory/Presentation/Controllers/StockMovementController.php:208-245` and `apps/api/app/Modules/Inventory/Presentation/Controllers/EntryExitNoteController.php:216-254`).

- The completed-row backfill preserves all historical reader answers: completed transfers were excluded by the old status predicate and remain outside `CARRYING_STATUSES`; setting their counters to sent makes their quantity remainder zero as well. In-transit and cancelled rows are untouched (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:156-162,685-686`).

- TransferIn followed by destination Damage/WriteOff issue is compatible with the existing stock/GL bridge, nets destination stock to zero for the affected units, and does not mutate WAC. Return-to-source remains TransferIn-only with no GL (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:448-469`).

- Event sourcing respects rule 8: existing transfer events remain unchanged, receipt events are stored synchronously in the receipt transaction, and integration/movement events dispatch after commit (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:179-200`). Notification recipients are resolved before queue execution and T10 clears `CompanyContext`, satisfying rule 20 (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:492-496,579`).

- Idempotency and locking are coherent: transfer header first, then sorted product locks, company-wide idempotency key/hash discrimination, external unique-race reread and typed mismatch 422 (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:327-362`). This follows the current `complete()` order (`apps/api/app/Modules/Inventory/Application/Services/StockTransferService.php:317-339`).

- Convention coverage is adequate. The benchmark has fifteen decision rows (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:29-49`), glossary additions are explicit at lines 71–83, `/complete` delegates to the single receipt writer at lines 393–394, generated entity DTOs replace the web shadows at lines 503–506, and the S-matrix covers company, location and rerun for every production writer at lines 548–560.

## Preserve

- Preserve the owner-ruled document-only blind guarantee and accepted R5 visibility/delta inference (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:244-263`).
- Preserve movement masking for every receiver while a transfer carries, including the posting actor, with terminal and non-transfer history visible (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:423-440`).
- Preserve the corrected fixture and all Table-P arithmetic, including the real counting document, movement-free L3 and deterministic rebalance row (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:602-646`).
- Preserve both close dispositions: write-off through Shrinkage GL and `return_to_source` without GL (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:367-373,448-469`).
- Preserve quantity-based remainder in exactly the three aggregate readers and the completed-only backfill (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:156-162,683-686`).
- Preserve stored header and per-line events, receipt-id stream anchoring, immutable existing events and worker independence from request `CompanyContext` (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:177-225,490-496`).
- Preserve manager/admin seeding of both independently grantable reconcile and close permissions, with close requiring both (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:399-418`).
- Preserve one receipt writer, non-blind `/complete` delegation, generated web DTOs and mobile-primary blind-by-construction commitments (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:393-394,503-506,536-542`).
- Preserve scale-4 decimal storage and strings, `QuantityScale`/bcmath arithmetic, and the prohibition on floats (`docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:99-121,486`).

## Owner decisions required

None. OD-1, close authority, OD-4, R5, D1/D2, OQ-1–OQ-4, multi-receiver attribution and stored per-line pattern events are all applied rulings (`docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md:98-110`; `docs/superpowers/specs/2026-09-09-transfer-destination-receipt-and-blind-receiving-design-rev-11.md:694-698`).

VERDICT: ACCEPT