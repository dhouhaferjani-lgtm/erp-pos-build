# G-3c company payment repositories — adversarial gate r1 (treasury + tenancy/authz)

**Lane:** G-3c `feat/g3c-company-repositories`  
**Worktree:** `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/g3c-repos`  
**Base:** `ea4b6b10bdd321c2cdb7be97c251a79168c957e6`  
**Review posture:** source read-only; dirty tracked diff and all five untracked lane files inspected. No source, stash, commit, or branch mutation.  
**VERDICT: CHANGES**

## Findings register

| ID | severity | file:line | finding | change |
|---|---|---|---|---|
| G3C-R1-01 | **MAJOR** | `apps/api/app/Modules/Treasury/Application/Services/PaymentRepositoryProvisioningService.php:34-50,52-87`; `apps/api/tests/Feature/Treasury/CompanyPaymentRepositoryProvisioningTest.php:256-298` | The categorical failure-containment claim is narrower than the implementation. `Company::find`, `Account::findByPurpose`, and fallback-location resolution all execute **before** the nested transaction and its `try`; a `QueryException`/`Throwable` from any of those reads escapes into `CompanyController::store()`'s outer transaction and rolls the new company back. The only failure-path test removes the cash purpose and proves the non-exceptional `null` branch; it never makes provisioning throw. The insert itself is correctly savepoint-contained, but “treasury provisioning can never roll back company creation” is not yet true or regression-pinned. | Put the lookup/resolution work under the same nested savepoint and catch boundary (or add an equally strong outer containment boundary), then add a real `POST /api/v1/companies` failure-injection test. It must force a provisioning query/insert exception, assert HTTP 201 and the company/location/membership survive, assert only the repository work rolls back, and assert `payment_repositories.provisioning_failed` carries the exception. Run this on PostgreSQL so `25P02` recovery is genuinely exercised. |
| G3C-R1-02 | **MAJOR** | `apps/api/tests/Feature/Seeders/PaymentRepositorySeederTest.php:77-90`; `apps/api/tests/Feature/Treasury/CompanyPaymentRepositoryProvisioningTest.php:77-123`; source `apps/api/app/Modules/Treasury/Application/Services/PaymentRepositoryProvisioningService.php:43-74` | The source currently has no movement or journal writer and does not include `balance` in the `forceCreate` payload, and the inherited seeder test asserts zero balance plus zero `repository_movements`. But the gate explicitly requires **no repository movement and no journal entry, cite + test**. No G-3c or touched seeder assertion checks `journal_entries`; the second-company HTTP test checks only repository rows. A future provisioning call to a journal writer would remain green. | Extend the provisioning/HTTP regression to snapshot or assert zero company `repository_movements` **and** zero company `journal_entries` after provisioning, while retaining the balance-zero assertions. Keep the direct source assertion that the service payload omits `balance`; money must enter only through `AccountingOpeningService`. |
| G3C-R1-03 | **MAJOR** | `apps/api/app/Modules/Treasury/Application/Services/PaymentRepositoryProvisioningService.php:50,98-108`; `apps/api/app/Shared/Contracts/Treasury/CompanyPaymentRepositoryProvisionerInterface.php:38-43`; `apps/api/tests/Feature/Treasury/CompanyPaymentRepositoryProvisioningTest.php:77-123,232-253`; `apps/api/tests/Feature/Treasury/PaymentRepositoryLocationTest.php:51-143` | Nullable `$defaultLocationId` and the fallback order are documented and implemented, but not meaningfully tested on the runtime service. The second-company test passes an explicit location ID, while the registration test passes `null` indirectly but asserts only count/codes—not `location_id`. `PaymentRepositoryLocationTest` tests CRUD/index and the PG partial unique, not provisioning fallback. Reordering the resolver or returning `null` despite an available default location would leave every requested test green and recreate N-12 attribution drift. | Add a direct scalar-port test with competing locations that calls `provisionForCompany(..., null)` and proves `is_default → is_active → pos_enabled → oldest`; at minimum assert the default wins over older active/POS distractors. Also pin the legal no-location case to `location_id = null`. Keep the explicit-location path assertion already present. |

## Verified contract and call chains

- **One runtime implementation:** the Shared scalar-only port is declared at `CompanyPaymentRepositoryProvisionerInterface.php:29-49`, bound at `TreasuryServiceProvider.php:115-118`, implemented once by the final readonly service at `PaymentRepositoryProvisioningService.php:19-155`, and consumed by `PaymentRepositorySeeder.php:82-109`. The seeder no longer owns a second day-one definition. The migration's frozen inline definition is deliberate deployment-history isolation (`2026_08_30_100800...php:55-76,185-201`), as required by the brief.
- **Exact day-one set/idempotence:** `PaymentRepositoryProvisioningService.php:60-73,90-95,114-131` checks `(company_id, code)` and creates only `CASH-01`/cash-register and `SAFE-01`/safe, both active. Existing rows are never updated or repointed. Partial and repeated runs are pinned at `CompanyPaymentRepositoryProvisioningTest.php:177-230`.
- **Zero-balance source shape:** the service payload at `PaymentRepositoryProvisioningService.php:65-72` omits `balance`; `PaymentRepository.php:67-87,185-204` owns the zero model default and keeps `balance` non-fillable. The PostgreSQL guard rejects non-zero births at `2026_07_08_160000_forbid_direct_payment_repository_balance_writes.php:43-69`. No movement/journal call appears in the service. G3C-R1-02 is the missing journal assertion, not an observed side effect.
- **In-tenant company creation:** the route is authenticated and tenant-claim guarded without requiring CompanyContext (`app/Modules/Company/routes.php:18-26`). `CompanyController::store()` derives `tenant_id` from the authenticated user, creates the company and `MAIN` location inside the transaction (`CompanyController.php:74-145`), then passes `$company->tenant_id`, `$company->id`, and `$location->id` to the Shared port inside that transaction at `:172-199`. No CompanyContext supplies any repository identity.
- **Registration:** shared-DB registration calls `TenantInitializationService` at `AuthController.php:483-492`; database-per-tenant registration calls it at `TenantProvisioningService.php:195-197`. The initializer has one repository step at `TenantInitializationService.php:107-115`, which constructs the delegating seeder at `:308-319`; the seeder makes the single runtime port call at `PaymentRepositorySeeder.php:102`. Registration asserts exactly two codes at `CompanyPaymentRepositoryProvisioningTest.php:232-253`. Repo-wide call tracing found no second initializer-side provisioning call.
- **Second-of-everything:** second-company ownership and sibling isolation are asserted at `CompanyPaymentRepositoryProvisioningTest.php:77-123`; the second POS location adds exactly one N-12 branch drawer and leaves the two day-one codes unchanged at `:125-175`; partial repair and three total invocations preserve IDs at `:177-230`. This satisfies rule 22's core company/location/re-run matrix (`CLAUDE.md:100-105`; `docs/conventions/09-SECOND-OF-EVERYTHING.md:29-55`), subject to the resolver-specific gap G3C-R1-03.
- **N-12 separation:** `LocationCashRegisterProvisioner.php:20-49,94-170` remains the branch-drawer writer. G-3c does not call it or create a second company pair on location creation. Its partial unique remains byte-untouched at `2026_08_26_100000_backfill_payment_repository_location_n12.php:411-438`.
- **Uniqueness untouched:** `(company_id, code)` is the Session-E predecessor constraint at `2025_12_30_195300_fix_multi_company_unique_constraints.php:31-35`, not the 2026-08-28 payment-method migration. No constraint migration was changed by G-3c. The N-12 `(company_id, location_id, type)` partial unique permits one cash-register and one safe at the same location because their types differ.
- **Brownfield migration:** guards cover all four tables and required columns at `2026_08_30_100800...php:21-37,84-99`; the unconditional census is named `payment_repositories.census` and echoes `payment-repositories-census` at `:39-53`; every company is examined so a partial `CASH-01` holder receives only missing `SAFE-01` (`:101-168`); each company runs in its own nested transaction/catch and failure log at `:107-182`; success emits `payment-repositories-seeded company_id=...` at `:174-176`; `down()` is the documented logged no-op at `:204-212`. It reads explicit company rows and never imports/reads CompanyContext. Migration tests pin zero-company, empty, already-correct, rerun, and partial shapes at `BackfillCompanyPaymentRepositoriesMigrationTest.php:27-109`.
- **Module/authz boundary:** Company and Tenant callers depend only on `App\Shared\Contracts\Treasury`; the scalar contract has no module-domain types (`CompanyPaymentRepositoryProvisionerInterface.php:20-27,49`). The service resolves Company/Location within Treasury Application (`PaymentRepositoryProvisioningService.php:26-34,98-108`). Deptrac names no G-3c class.

## Fresh command outputs

| command | output |
|---|---|
| SQLite, by path: the two new test classes, touched seeder test, `CreateCompanyTest`, `CompanyCreationEventsTest`, and `PaymentRepositoryLocationTest` | **PASS — 42 tests, 204 assertions, 2 expected PostgreSQL-only N-12 skips**, 0 failures, 26.69s. |
| PostgreSQL private DB `autoerp_test_g6`, corrected CI-form class selector, two new classes only | **PASS — 10 tests, 63 assertions**, 0 skips/failures, 21.85s. The first escaped-selector attempt matched no tests and is not counted as evidence; both PG invocations carried `DB_DATABASE=autoerp_test_g6 DB_CENTRAL_DATABASE=autoerp_test_g6`. |
| PHPStan level 8 on all 11 dirty PHP paths | **PASS — `[OK] No errors`.** |
| Pint `--test` on all 11 dirty PHP paths | **PASS — `{"result":"pass"}`.** |
| `php vendor/bin/deptrac analyse --no-progress` | **BASELINE — 183 violations**, skipped 0, warnings 0, errors 0. Focused output grep returned only `Violations 183`; neither `PaymentRepositoryProvisioning` nor `CompanyPaymentRepositoryProvisioner` appears. The command's exit 1 is the repository's existing raw-violation posture, not growth. |
| `php tools/feature-lane-manifest-check.php` | **PASS** — 1,473 Feature classes / 74 groups; every group disposed, every declared lane present, every filter anchored and uniquely matched across 1,874 test classes. Standing warnings: 1,213 parked classes and 1 coverage-debt class. |
| Manifest union against current local `dev` `f2760c37e92f` | **Correct:** current dev `gated_ceiling 1212`, Treasury 122, Migrations 10; G-3c union is **1214 / 123 / 11**. The inherited Migrations 7→10 raise is sibling G-3a; G-3c adds one Treasury and one Migrations class (+2 ceiling), not stale fork arithmetic. |
| CI YAML + PHPUnit filter parse | **PASS** — Ruby/Psych parses `.github/workflows/ci.yml`; both `--filter` PCREs parse; manifest checker proves anchoring/unique class matches. G-3c comment is at `ci.yml:1108-1109`, and both new class names are in the anchored PG filter at `:1111`. |
| `git diff --check ea4b6b10b`; dirty census; frontend check | **PASS** — no whitespace errors; exactly 8 tracked modifications + 5 untracked lane files; no `apps/web/` path. |

## Verdict

**CHANGES.** The implementation and every requested mechanical gate are otherwise sound: exact two-row provisioning, company ownership, branch-drawer separation, idempotence/partial repair, registration delegation, migration guards/census/forward-only behavior, untouched uniques, scalar Shared contract, manifest union, and both database legs all verify. Promotion remains blocked until G3C-R1-01 through G3C-R1-03 are closed and re-run on SQLite and the prefixed private PostgreSQL database.
