# W2-1 handback — fresh signup 403-deadlock on a browser holding another company's selection

**Lane:** W2-1 (P1), Session A, 2026-08-24 campaign wave 2 §F1 / §5.
**Worktree:** `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/w21-signup-state`
**Branch:** `fix/campaign-w21-signup-stale-company` (base local `dev` `7cd024946`; `git merge dev`
done at `9b458badc` — docs-only, no code conflicts).
**Also closes:** LEDGER **C-13(iii)** — malformed `X-Company-Id` 500s.
**Migration-bearing:** NO.

| Commit | Scope |
|---|---|
| `25694fe40` | backend — typed middleware error contract + `Str::isUuid()` guard |
| `81feb5a94` | frontend — scope clear on login/register, scope-free bootstrap calls, 403 self-heal |
| `5df907473` | CI — feature-lane manifest named raise + pgsql `--filter` append |
| (this commit) | handback |

---

## 1. What was actually wrong

Three separate things, and only all three together explain "no in-product recovery":

1. **Nothing cleared the scope on a NEW session.** `clearAllAppState()` drops the origin-wide
   `autoerp-company-selection` key, and it was wired to logout and to 401 expiry only.
   `RegisterPage.tsx` and `LoginPage.tsx` called `setAuth(...)` and navigated. A fresh signup
   therefore inherited the previous account's company id.
2. **The bootstrap call carried the stale header too.** `apps/web/src/lib/api.ts` attached
   `X-Company-Id` to *every* request, `/user/companies` included. That call is the one that would
   have taught the client the truth; because it 403'd like everything else, the membership list
   never loaded, the switcher opened empty, and the deadlock could not self-heal.
3. **The client had nothing to key a recovery on.** The middleware already returned a typed
   `COMPANY_ACCESS_DENIED`, but no client code read the code, so a 403 just logged and died.

And while in that middleware, **C-13(iii)**: `X-Company-Id` is client-supplied and flowed straight
into the `user_company_memberships.company_id` **uuid** predicate. A non-uuid raises SQLSTATE 22P02
on PostgreSQL — a 500, not a denial.

## 2. Changes (file:line, production only)

**Backend**

| File | Change |
|---|---|
| `apps/api/app/Http/Middleware/CompanyContextMiddleware.php:25-36` | error contract documented on the class (400 `INVALID_COMPANY_ID` / 403 `NO_COMPANY_ACCESS` / 403 `COMPANY_ACCESS_DENIED`) |
| `…CompanyContextMiddleware.php:69-87` | malformed header ⇒ typed **400 `INVALID_COMPANY_ID`**, checked BEFORE the membership lookup; then header-or-default resolution inline |
| `…CompanyContextMiddleware.php:129-141` | `resolveCompanyId()` → `headerCompanyId()`: returns the raw explicit selection only (empty header = "no explicit selection", still falls back to the default company — unchanged behaviour) |
| `apps/api/app/Modules/Company/Services/CompanyContext.php:154-164` | `userHasAccessToCompany()` fails **closed** on a non-uuid id, so every caller (`OwnerReportScope` too) denies instead of throwing |

**Frontend**

| File | Change |
|---|---|
| `apps/web/src/lib/clearAppState.ts:8-31` | new `clearScopeForNewSession(queryClient)`: company store reset (drops the persisted key) → location store reset → `queryClient.removeQueries()`. Stores first so any query that restarts is already re-keyed and header-free. Uses `removeQueries`, not `clear()`, because it runs inside the login/register mutation's own `onSuccess` and must not wipe the mutation cache. Deliberately does NOT touch the auth store. |
| `apps/web/src/lib/api.ts:120-150` | `isCompanyContextExempt(url)` + `COMPANY_CONTEXT_EXEMPT_PATHS = ['/user/companies', '/auth/me']`; tolerates absolute URL, `/api/v1` prefix, query string, trailing slash |
| `apps/web/src/lib/api.ts:152-192` | `handleCompanyScopeRejection(code)` — resets the selection on `COMPANY_ACCESS_DENIED` / `INVALID_COMPANY_ID` only |
| `apps/web/src/lib/api.ts:241-247` | request interceptor skips `X-Company-Id` on exempt URLs |
| `apps/web/src/lib/api.ts:314-323` | response interceptor routes 400/403 typed codes into the handler |
| `apps/web/src/features/auth/LoginPage.tsx:91-95` | `clearScopeForNewSession(queryClient)` immediately BEFORE `setAuth(...)` (authenticated branch only — the org-picker branch returns earlier and must not clear) |
| `apps/web/src/features/auth/RegisterPage.tsx:100-105` | same, before `setAuth(...)` |

**CI**

| File | Change |
|---|---|
| `apps/api/tests/feature-lane-manifest.json` | `Company` `classes` 31 → **32**, `gated_ceiling` 1175 → **1176**, with the named justification in the group note |
| `.github/workflows/ci.yml:983` | `CompanyContextMiddlewareTypedErrorsTest` appended to the `backend-test-pgsql` `--filter` allowlist (B-3/Q-6/Q-7/Q-10 precedent). Justified: `feature-lane-tenancy/Company` is PARKED behind `vars.SELF_HOSTED_RUNNER_READY`, and the C-13(iii) defect only reproduces on PostgreSQL. |

## 3. Why no loop

The 403 self-heal resets **only while a selection exists**. After the reset `currentCompanyId` is
null, so (a) the header stops being sent and the server falls back to the user's own default
company, and (b) a second rejection returns `false` without touching anything. The re-bootstrap is
causal rather than manual: `currentCompanyId` is a suffix of every `tenantScopedKey`, so clearing it
re-keys `['user','companies',tenantId,null]` and `CompanyProvider` refetches — now without the
header. `resolveCompanySelection` then picks the primary membership (the stale persisted id is gone).
`NO_COMPANY_ACCESS` is deliberately excluded: it means the user is a member of no company at all,
which no client-side reset can fix.

## 4. Red → green evidence (tests BY PATH; full suite never run)

### Backend — `apps/api/tests/Feature/Company/CompanyContextMiddlewareTypedErrorsTest.php` (9 tests)

RED, sqlite, before the fix:

```
Expected response status code [400] but received 403.   × 5 (the malformed-id data set)
Tests:    5 failed, 4 passed (14 assertions)
```

RED, **PostgreSQL** (`autoerp_test_w21` on 127.0.0.1:5433), before the fix — this is C-13(iii):

```
SQLSTATE[22P02]: Invalid text representation: 7 ERROR:  invalid input syntax for type uuid: "not-a-uuid"
  (SQL: select exists(select * from "user_company_memberships"
        where "user_id" = … and "company_id" = not-a-uuid and "status" = active))
  at app/Modules/Company/Services/CompanyContext.php:151 (pre-fix line)
Tests:    6 failed, 3 passed (11 assertions)
```

GREEN, sqlite: `Tests: 9 passed (19 assertions)`.
GREEN, PostgreSQL: `Tests: 9 passed (19 assertions)`, duration 135 s.
Throwaway DB `autoerp_test_w21` **dropped** after the run.

No regression: `CompanyContextActiveMembershipTest` + `OnboardingStatusNoCompanyContextTest`
→ `Tests: 10 passed`. On the dev-merged tree, the new file + `CompanyContextActiveMembershipTest`
→ `Tests: 17 passed (29 assertions)`.

### Frontend

`apps/web/src/lib/__tests__/api.companyScope.test.ts` (9 tests) — RED before the fix:

```
× never sends X-Company-Id on /user/companies (the membership bootstrap)
    AssertionError: expected '01a034af-94ea-713d-8ce0-462216bf6ab5' to be undefined   <- the W2-1 mechanism
× never sends X-Company-Id on /auth/me                          (same)
× handleCompanyScopeRejection …                 TypeError: not a function  (× 5)
Tests  8 failed | 1 passed (9)
```

GREEN: `Tests 9 passed (9)`. These exercise the REAL axios instance via a captured
`api.defaults.adapter`, so the request interceptor genuinely runs.

`apps/web/src/features/auth/__tests__/newSessionCompanyScope.test.tsx` (7 tests) — RED before:

```
× clearScopeForNewSession …            TypeError: clearScopeForNewSession is not a function (× 4)
× bootstrap after a scope clear …      (same)
× RegisterPage.tsx calls clearScopeForNewSession   expected … to contain 'clearScopeForNewSession'
× LoginPage.tsx   calls clearScopeForNewSession    expected … to contain 'clearScopeForNewSession'
Tests  7 failed (7)
```

GREEN: `Tests 7 passed (7)`. Includes the campaign scenario end-to-end at the provider level:
a stale selection from *another tenant* in `localStorage` → `clearScopeForNewSession` →
`CompanyProvider` mounts → `/user/companies` → **switcher populated (1 company)** and
`currentCompanyId` = the new tenant's primary, persisted key rewritten.

**Honest note on the two source-level guards.** The last two assertions read `RegisterPage.tsx` /
`LoginPage.tsx` and assert both contain `clearScopeForNewSession(queryClient)` *before* the first
`setAuth(`. They are guards, not behavioural tests: driving `RegisterPage`'s 4-step wizard to submit
in jsdom is disproportionate, and a behavioural test of `LoginPage` alone would leave the register
path — the one the campaign actually broke — unguarded. They fail loudly if either call is removed
or moved after `setAuth`.

Regression, by path, one file per process, workers killed after
(`ps aux | grep "[n]ode (vitest"` → 0):

| File | Result |
|---|---|
| `src/lib/__tests__/clearAppState.test.ts` | 5 passed |
| `src/lib/__tests__/api.unauthorized.test.ts` | 4 passed |
| `src/lib/__tests__/api.csrfRetry.test.ts` | 5 passed |
| `src/lib/__tests__/tenantScopedKey.test.ts` | 9 passed |
| `src/features/auth/auth.test.tsx` | 11 passed, 1 skipped |
| `src/features/auth/__tests__/useLogout.test.tsx` | 6 passed |
| `src/features/auth/__tests__/AuthProvider.test.tsx` | 5 passed |
| `src/features/company/__tests__/CompanyProvider.tenantScope.test.tsx` | 4 passed |
| `src/stores/__tests__/companyStore.test.ts` | 32 passed |

## 5. Gates

| Gate | Result |
|---|---|
| PHPStan (level 8, both touched PHP files) | **OK, no errors** |
| Pint | clean (one fixture-order fix applied to the new test file) |
| `pnpm typecheck` | clean |
| `pnpm lint` (eslint + audit:keys + design-system + quantity + i18n:local + rule/tool tests) | **exit 0**, zero new warnings — ratchet held |
| deptrac | **183 violations in the worktree, 183 on `dev`** → zero new; no violation names either touched class |
| `php tools/feature-lane-manifest-check.php` | **EXIT=0** after the named raise |
| react-doctor `--staged` | score 87, 1 warning — `api.ts:389` `authenticatedDownload`, **pre-existing and untouched** (and a false positive: it does call `URL.revokeObjectURL`) |
| i18n en/fr/ar | **no new user-facing strings** — the recovery is silent and automatic, so no `t()` key was added |
| Design tokens | no color classes touched; the two `.tsx` edits are a function call plus comments |
| `any` / `mixed` / `app()` | none introduced |

## 6. POS cross-app check (explicitly asked)

`apps/pos` persists `izipos-device-id` plus zustand keys `izipos-cash-drawer`,
`izipos-customer-display`, `izipos-printer`, `izipos-settings`, `izipos-scanner`. The keys this lane
clears are `autoerp-company-selection` (company store) and the location store's own key — disjoint
prefixes, and `queryClient.removeQueries()` is in-memory and per-app. `grep -rn
"autoerp-company-selection\|autoerp-company" apps/pos/src` → **no hits**. POS is unaffected.

## 7. Residuals — seen, NOT touched (no scope creep)

1. **`user/companies` is still enforced server-side.** Its route group is commented *"Routes that
   don't require company context"*, but `CompanyContextMiddleware` sits in the global `api` group
   (`apps/api/bootstrap/app.php:142-146`), so it applies anyway. `UserController::companies` is
   entirely user-scoped (reads the caller's own memberships; never touches `CompanyContext`), so
   exempting that route name in the middleware would be safe and would make the deadlock impossible
   even for a stale cached client bundle. The brief scoped the backend to the typed contract + the
   uuid guard, so I did not add it. **Recommend as a follow-up.**
2. **`OwnerReportScope.php:30`** passes a request-derived company id to `userHasAccessToCompany`. It
   no longer 500s (fail-closed guard), but its own rejection is not typed the way the middleware's
   is; a client cannot key a scope reset on it.
3. **`AuthProvider` 401 handling and `handleUnauthorized` are untouched** — a 401 still hard-clears
   and redirects, which is correct and orthogonal to this lane.
4. **The campaign report quotes the 403 body as `{"message":"Access denied: …"}`.** No such string
   exists anywhere in `apps/api` (grep: 0 hits outside `EnsureSuperAdmin`). The real envelope is
   `{"error":{"code":"COMPANY_ACCESS_DENIED","message":"User does not have access to the requested
   company."}}`, now pinned by test. The report paraphrased; the code was already typed.
5. **Cross-tab `storage` listener** (`companyStore.ts` tail) adopts a new selection when
   `companies.length === 0`. Correct today (it is validated on the next fetch), but it means a reset
   in tab A momentarily propagates a null to tab B. Not a defect in this flow; noted.

## 8. Reviewer entry points

- Deadlock mechanism and the three layers: §1 and §3.
- Loop-safety argument: §3 — the specific claim to attack is *"after the reset the header stops
  being sent, so the server's default-company fallback cannot produce a second
  `COMPANY_ACCESS_DENIED`."*
- Authz surface: the middleware now rejects a class of request it previously accepted (malformed
  header → 400 instead of 403/500). No request that previously succeeded now fails: a valid uuid
  takes exactly the old path.
- The `X-Company-Id` exemption is the one deliberate loosening — argued in §2 and in the
  `COMPANY_CONTEXT_EXEMPT_PATHS` docblock: both endpoints are user-scoped server-side, so the header
  was never load-bearing there.

---

# Fix round r1 — response to `2026-08-25-w21-signup-gate-r1-tenancy.md`

Gate verdict was **spec ✅ / quality CHANGES-REQUESTED**, merge-blocking on F-1 only.
All of F-1..F-5 are addressed here; F-6 too. Merged `dev` at `925306c97`
(`7ce2b2553`).

| Commit | Scope |
|---|---|
| `7ce2b2553` | merge `dev` — `ci.yml` `--filter` resolved as the UNION, manifest recomputed |
| `df1cb2c6c` | backend — bootstrap-route exemption (item 5 / F-4) + ACTIVE-only membership list (F-2 server half) + F-6 pin |
| `9b92e1210` | frontend — denied-company invariant (F-2) + sent-id matching (F-3) + F-5 label |
| (this commit) | lint zero-new fixes + this addendum |

## F-1 — CI accounting (was merge-blocking) — CLOSED

`git merge dev` conflicted on the `--filter` line exactly as predicted. Resolved as the **union**:
`dev`'s `ExpensePaidFromRepositoryTest` + `OpeningCashFloatSeedsRepositoryTest`, plus ours
`CompanyContextMiddlewareTypedErrorsTest` → **131 entries**, `ci.yml` re-parsed as valid YAML.
`gated_ceiling` **1176 → 1177** (dev had independently reached 1176), `groups.Company.classes`
stays **32**, note extended to record the recomputation against live `dev` `925306c97`.
`php tools/feature-lane-manifest-check.php` → **OK — 1428 Feature classes in 74 groups; every
--filter entry is anchored and uniquely matched**, EXIT=0.

The four new backend tests were added to the **existing** `CompanyContextMiddlewareTypedErrorsTest`
class deliberately, so this round adds no further class and does not move the ceiling again.

## Item 5 / F-4 — server-side bootstrap exemption — DONE

`CompanyContextMiddleware.php` now skips company resolution **entirely** for the bootstrap routes,
matched by route name (`user.companies`, `auth.me`) with a path fallback
(`v1/user/companies`, `v1/auth/me`). Not a header-ignore, per the reviewer: ignoring only the header
would still answer `403 NO_COMPANY_ACCESS` for a user with no ACTIVE membership — the exact state the
POS recovery screen is in.

Safety, re-verified in code: `UserController::companies` reads only `$user->id`, and `getMeta` only
`X-Request-ID`; neither consults `CompanyContext`. `auth.me` is on the `web` group and does not reach
this middleware today — it is listed so the exemption survives a future move, and `AuthController::me`
is company-free as well.

**This closes the POS till deadlock without a POS build** (`apps/pos/src/lib/api.ts` sends
`X-Company-Id` on every request and `authStore.fetchCompanies` recovery depends on
`/user/companies` succeeding).

New tests, all in `CompanyContextMiddlewareTypedErrorsTest`:
foreign company header ⇒ **200** with only the user's own company; malformed header ⇒ **200**;
zero-ACTIVE-membership user ⇒ **200** with an empty list.

## F-2 — loop safety is now an enforced invariant — DONE (both halves)

*Server:* `UserController::companies` filters `status = Active` (`UserController.php:834-847`), so the
list a client may select from is exactly the set `userHasAccessToCompany` accepts. Also removes the
small info-disclosure the reviewer noted.

*Client:* `handleCompanyScopeRejection` records the denied id in a session-scoped module-level `Set`
in `companyStore.ts`, and `resolveCompanySelection` never picks a denied company — it falls to the
next candidate, or returns **null** when every listed company is denied. Denials are forgotten by
`clearScopeForNewSession` and `clearAllAppState` (different account / restored membership) and by an
explicit `setCurrentCompany` (user intent outranks a remembered 403; one more 403 is a user action,
not a loop). The `LOOP SAFETY` comment in `api.ts` was rewritten — it no longer claims impossibility
from the null selection alone.

The reviewer's probe is kept as a permanent test. **Red-proof by reverting only the filter hunk:**

```
× re-bootstraps onto a DIFFERENT company after the denied one is rejected
    AssertionError: expected '01a034af-…' to be '7c9e2f14-…'      <- the denied primary re-picked
× leaves the selection null when the ONLY listed company is denied
    AssertionError: expected '01a034af-…' to be null
```

## F-3 — a stale in-flight 403 no longer wipes a fresh selection — DONE

The interceptor reads the failing request's own header
(`error.config?.headers['X-Company-Id']`) and `handleCompanyScopeRejection(code, sentCompanyId)`
resets only when it still equals `currentCompanyId`. A request that carried no header was resolved
server-side against the user's default company, which no client reset can fix, so it is ignored.
Red-proof (behaviour hunk reverted): 4 failures in `api.companyScope.test.ts`, including the
in-flight-switch case driven through the real axios interceptor.

## F-5 / F-6 — DONE

F-5: the two register/login guards are now explicitly labelled **HEURISTIC GUARDS, not behavioural
tests**, with the failure modes spelled out.
F-6: `test_company_context_middleware_is_registered_in_the_api_middleware_group` asserts
`CompanyContextMiddleware::class` is in the resolved `api` group, so the probe route can no longer
pass while the real registration is gone.

## F-7 / F-8 — acknowledged, no change

F-7 (login always resets a multi-company user to their primary) stands as a deliberate decision:
correctness over convenience, and logout already resets. F-8 (`Str::isUuid` rejects braced/undashed
uuids PG would accept) stands as a deliberate tightening; no client in the repo emits those forms.

## Gates, re-run after the fix round

| Gate | Result |
|---|---|
| Backend, sqlite | `CompanyContextMiddlewareTypedErrorsTest` **14 passed (32 assertions)** |
| Backend, **PG** throwaway `autoerp_test_w21` | **43 passed (153 assertions)** across the new file + `CompanyContextActiveMembershipTest` + `SanctumSpaAuthTest` + `UserOffboardingCascadeTest`; DB dropped |
| Red-proof, backend (`git apply -R` on the two prod files) | 4 failed — 403 / 400 / 403 / "actual size 2 matches expected size 1" |
| Red-proof, frontend (behaviour hunks reverted) | 4 failed + 2 failed, loop reproduced |
| vitest, by path, one file per process | api.companyScope **14**, newSessionCompanyScope **10**, companyStore **32**, clearAppState **5**, api.unauthorized **4**, api.csrfRetry **5**, CompanyProvider.tenantScope **4** — workers killed |
| PHPStan (3 touched files) | **[OK] No errors** |
| Pint | pass |
| `pnpm typecheck` | clean |
| `eslint .` | **6450 warnings, 0 errors — identical to `dev` (6450). Zero new.** The round initially landed at 6454; all four (2 × template-literal `${index}`, `["error"]` dot-notation, unsafe `any` assignment on the header read) were fixed rather than baselined. |
| `pnpm lint` (full) | EXIT=0; Gate C tanstack keys 0 new / 0 stale; design-system 807 acknowledged, 0 new |
| deptrac | **183 = 183 on `dev`**; 0 attributable to the changed classes |
| `feature-lane-manifest-check.php` | **EXIT=0** at 1177 / Company 32 |

## Residual after this round

**POS client header hygiene (F-4 residual).** The till is now safe because the server exempts the
route, but `apps/pos/src/lib/api.ts` still attaches `X-Company-Id` to every request from
`getHeaders()`, and the POS has none of the web client's other two layers — no scope clear on a new
session, no typed-403 self-heal. A POS lane should mirror `isCompanyContextExempt` and the rejection
handler. Not done here: it is a second app, outside this lane's brief, and needs a POS build +
device test.

