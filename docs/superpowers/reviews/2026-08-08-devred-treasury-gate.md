# GATE RECORD — dev-red fixture-repair lane (`fix/expense-vat-fixture-reds`) — treasury axis (sole gate)

**Round 1. Verdict: spec ✅ + quality APPROVED — CLEAR TO MERGE.** Merged to local dev (rebased tip `446ca2e14`); green-proof on merged dev OK 27/84.
**Scope:** 2 commits on `264e6c483`, +139/−6, exactly 2 test files. 10 live origin/dev reds → 0.

## Verified
1. **FIXTURE-ONLY:** zero product code/migrations/seeders; no `allowNegative` anywhere (both `fundRepository()` arg lists end at `allowWhileFrozen: false`; `MovementIntent::$allowNegative` stays default false). Guard byte-identical to base. Funding legs are INFLOWS — never touch the guard at all.
2. **FUNDING SHAPE:** structural match (16 named args) for `PaymentRepositorySeeder::recordOpeningBalance()` + the W-5b precedents (`PaymentGlPostingTest:362`, `VendorPrepaymentRefundTest:~130`) incl. the MED-9 `DB::transaction()` wrapper. Reconcile-clean proven EMPIRICALLY: the branch's own reconcile test runs `treasury:reconcile`, asserts exit 0 + no drift audit event, with the opening leg present. No direct balance write possible (`balance` not fillable; port is sole writer).
3. **GOLDEN RE-PINS — all four SPEC-DERIVED, no laundering:** balance as `bcsub(opening, outflow, 3)` (identity, better than a literal); ordinals 1→2 mechanically forced by the counter (`TreasuryMovementService.php:568/601` — column stores last-used ordinal); `firstOrFail()` scoping NARROWS; **JE-exemption verified against the spec in code** — `ReconcileTreasuryCommand::isJournalEntryExempt()` (:902-906) returns true unconditionally for `OpeningBalance`, §9.2 docblock (:50-53) and seeder docblock state it independently, all pre-dating the lane. The new assert is codified existing behaviour. Net coverage POSITIVE (check-2 exemption branch now exercised).
4. **PaymentOriginWriterInventoryTest:** no golden changed (all 30 assertions untouched — origin/fiscal_event_id/allocations/status only); masking margin verified by enumerating every refund call site (max outflow 150.00 vs 1000.00 funding); EUR scale-2 constant correct, rule-19 clean.
5. **SWEEP PREDICATE verified from source both halves** (factory persists balance via `Model::unguarded` at Factory.php:390; `::create()` drops it — not fillable). All three non-defect spot-checks confirmed (PaymentTest:714 BankAccount guard-exempt; `makeLedgeredRepository` param dead at all 5 call sites; PosRefundReceiptBridgeTest is the deliberate W-5b allowNegative positive test).
6. **BASE REDS PROVEN both directions:** files checked out at base → 10 errors (3+7 split as claimed, all `InsufficientRepositoryBalanceException` from TreasuryMovementService.php:528); branch → OK 27/84. Tree restored clean.
7. **REAL-DEFECT STOP validated:** `RepositoryMovementController.php:102-105` LogicException — pre-existing (blame `2afd37ebef`, 2026-07-20), reproduced (11th live dev red), correct STOP; ticket filed `2026-08-08-repository-movement-allocation-aggregate-logicexception.md` (first action: confirm PDO_PGSQL numeric-as-string behaviour before concluding prod safety).

## Minors (non-blocking, filed in `2026-08-08-devred-lane-followups.md`)
`fundRepository()` now copy-pasted in 5 files → extract trait; `app()` vs `$this->app->make()` cosmetic split; dead `balance` keys in `::create()` fixtures (the exact confusion class that produced the 10 reds); factory scaffolded-balance reconcile-drift note for the proposed model-level guard.
