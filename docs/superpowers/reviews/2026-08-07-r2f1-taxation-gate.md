# GATE RECORD — R2-F1 `fix/r2f1-cancel-period-refusal` — taxation / VAT-period axis

**Round 1. Verdict: APPROVE-WITH-FIXES** — 1 must-fix behavioural (I-1) + 2 must-fix documentation corrections feeding the expert ruling (I-2, I-3) + 1 design narrowing (I-4). Nothing critical: on PostgreSQL the period resolution is correct, the refusal transactional, no reversal sealed on refusal.

Runs: CancelRefusedOnNonOpenVatPeriodTest + VatPeriodControllerTest + VatPeriodManagementServiceTest **OK 26/101** · adjacent files green individually · phpstan 5 files clean · pint pass. (Stacked RefreshDatabase in one invocation exits 1 with no summary — harness/memory artifact, not a branch defect.)

## Axis findings (verified)
- **document_date IS the declaration axis** — `EloquentVatDataRepository.php:41`; GL stamps `AccountingService.php:441` (invoice) / `:593` (CN); reversal `now()` at `:940` with the 6a ruling comment. Guard reads the lockForUpdate-re-read row.
- **Boundary inclusivity SPLIT:** PG inclusive both days (probed live); SQLite EXCLUDES the period_start day (`date` col holds `Y-m-d H:i:s` TEXT; `toDateString()` binding compares lexicographically false). End day survives by accident; all 11 shipped tests are mid-month → green. See I-1.
- **FILED-first ordering CORRECT, trap avoided:** explicit `orderByRaw('CASE status WHEN ? THEN 0 ELSE 1 END', ['FILED'])` (`EloquentVatPeriodRepository.php:24`) — a naive `orderBy('status')` would have picked CLOSED. Probed both insertion orders + PG. Enum bindings safe via `enum_value()`. Gap: no tie-breaker among multiple CLOSED overlaps (m-4); ordering unpinned by the suite (m-3).
- **company_id scoping pinned** (`:20`, test `:368-390`).
- **Status set complete:** Open/Closed/Filed only; `fromPeriodStatus` throws LogicException on OPEN (unreachable by construction); **reopened period permits cancel — verified** (`VatPeriodManagementService.php:149`, test `:247-258` proves reopen→cancel→exactly 1 reversal JE). No raw status writes (grep clean).
- **Absent-period-permitted precedent verbatim** (`FiscalPeriodResolverService.php:258-272`; consumer refuses only exists-and-closed). Truth table recorded: no row → permitted; OPEN → permitted; CLOSED → `DOCUMENT_PERIOD_CLOSED`; FILED → `DOCUMENT_PERIOD_FILED`; overlap ≥1 FILED → FILED code; overlap all-CLOSED → arbitrary label; other company → permitted. **July-gap probe: permitted, and CORRECT** — a `vat_periods` row is materialized at close (`closePeriod()` freezes totals/declaration_data/breakdowns, `VatPeriodManagementService.php:90-115`); no row ⇒ nothing frozen ⇒ nothing disturbed. Residual: refusal strength bounded by period-row coverage (m-5, runbook).
- **Declaration-side honesty TRUE:** declaration aggregates `invoice/credit_note/expense` with NO status filter (`EloquentVatDataRepository.php:40-44`) — cancelled docs still count (F3's scope); the GL reversal NEVER enters the declaration (single-method interface; journal_entries never read in Taxation) — no double-representation. **F1 is neutral-for-open, strictly-better-for-locked.**
- **Error surface:** both locales complete + faithful; envelope structurally identical to the InsufficientRepositoryBalance precedent; renderer order verified (#18 before #19); RefundController re-throw correct PHP semantics; translated at throw-time vs precedent's render-time (m-6, no in-request effect); **no FE consumer of the cancel endpoints exists yet** (grep apps/web + apps/pos) — zero FE regression risk, zero user benefit until an FE lane consumes it.
- **Test quality:** 11 green; distinct codes, company pin, absent/non-covering/OPEN-happy paths, reopen path, HTTP 422 with error.code. Gaps: NO boundary-day test (consequence of I-1 — the demanded test goes red today); overlap ordering unpinned (m-3); the two supplier "zero reversal JEs" asserts are vacuous (supplier_invoice not in FISCAL_DOCUMENT_TYPES; no GL fixture) (m-7); test 11 doesn't assert period_label/period_status/document_number.

## FINDINGS
**I-1** — start-boundary day escapes the refusal under the project's own test DB (`EloquentVatPeriodRepository.php:16-27`, `toDateString()` binding). Fix: bind the Carbon (house precedent `FiscalPeriodResolverService.php:288-289` passes `$date` directly) + boundary tests on period_start and period_end. Probed: Carbon binding is inclusive on both drivers.
**I-2** — the c2 expert-relay rationale is factually wrong: "supplier input VAT sits in the very same filed declaration" (`VatPeriodCancellationGuard.php:69-71`, test `:264-266`) — declared input VAT comes ONLY from `expense` documents (`EloquentVatDataRepository.php:42`; `ExpenseService.php:427`); supplier_invoice appears nowhere in Taxation; `createSupplierInvoiceJournalEntry` (`:684`) has no production caller. The refusal default is still defensible on AP/trial-balance integrity + forward-compatibility — correct the ground before the relay.
**I-3** — nonexistent method cited as evidence (`:20-23`): `createSupplierInvoiceEntry()` has zero hits. Real stamp site `createSupplierInvoiceGrIrClearingEntry` (declared `:1921`, stamps document_date `:1996`, sole caller `SupplierInvoicePostingService.php:277`). In an artifact carrying an expert ruling, a fabricated citation is a credibility defect.
**I-4** — guard refuses EVERY type incl. declaration-less ones (`:110-119` fall-through true for Quote/DeliveryNote/ReturnNote/PO/PQR/Income). Latent today (only invoice/CN cancel routes exist; SO short-circuits pre-guard) — but a future PO-cancel lane yields a permanently uncancellable PO with zero fiscal justification. Recommend narrowing to declaration/GL-bearing types.
**m-1** — purchase arm unreachable in production (no caller/route); c2 relay must say so. **m-2** — can-cancel preflight not period-aware (follow-up with the FE lane). **m-3** — overlap ordering unpinned. **m-4** — no CLOSED-overlap tie-breaker. **m-5** — period-row-coverage bound (runbook). **m-6** — throw-time translation asymmetry. **m-7** — vacuous supplier zero-JE asserts; envelope extras unasserted.

## Recommendation for the R-c c2 relay (recorded verbatim for the rulings record)
**Keep the refusal uniform for CLOSED and FILED; do not adopt CLOSED-permits.** A CLOSED row is a frozen snapshot (`closePeriod()` materializes totals/declaration_data/breakdowns); CLOSED-permits would stale it against the ledger — the exact desync F1 prevents, one layer earlier. The sanctioned path is `reopenPeriod()` (deletes breakdowns, clears totals — un-freezes correctly), and CLOSED already carries its own error code so the UI can say "ask your accountant to reopen". CLOSED-permits would require auto-invalidating the period's breakdowns on cancel — F3 work, not a flag flip. Relay notes: the purchase arm has no live workflow today, and supplier-invoice input VAT is not currently in the declaration at all (expense documents only) — c2 is a forward-looking ruling, not a bleeding workflow.

## Merge conditions
1. I-1 Carbon binding + two boundary tests · 2. I-2 correct the declaration claim before the R-c relay · 3. I-3 fix the citation · 4. I-4 narrow or explicitly document default-open · 5. m-1/m-2 as relay note + ticket; m-3..m-7 discretionary.
