# Gate r1 — Lane G-3b "Import job company pin + CompositeItems entitlement" (imports angle)

- **Reviewer:** imports-reviewer (adversarial, code-grounded), 2026-08-29
- **Worktree:** `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/g3b-company-pin`, branch `feat/g3b-import-company-pin`, DIRTY/uncommitted (nothing modified or committed by this review)
- **Fork base:** `4e7f9a031` — **pre-G-3a**. `dev` has since advanced to `68c698f1a` (G-3a merged).
- **Brief:** `docs/sessions/session-G-imports-hardening-2026-08-29/briefs/LANE-G3B-company-pin-entitlement-BRIEF.md` incl. `## r5 patch (gate r4)`
- **Spec:** `docs/superpowers/specs/2026-08-29-imports-hardening-design.md` §3.3 M4, §5.2, §5.3, §5.4, §13.6 (G-R62)
- **Lane summary:** `…/scratchpad/lane-g3b-summary.md`

## VERDICT

**spec ❌ (one orphaned §5.2 requirement) + quality CHANGES-REQUESTED**

The core of the lane is correct and well tested: the four-way pin/entitlement contract is real,
the migration is genuinely guarded/idempotent/forward-only and attributes only on unanimous
evidence, and every held boundary (`formatJob`, `countWarningRows`, `ProcessImportJob`,
`apps/web`, locales, `ImportServiceProvider`) is byte-verified untouched. Two things block merge:
a **live-CI red** the lane left in G-7's acceptance net (F-1), and a **merge hazard** where the lane
re-implements a function G-3a already landed on `dev` with different, incompatible contents (F-2).

---

## Findings

| ID | severity | file:line | finding | change |
|---|---|---|---|---|
| **F-1** | **Critical** | `apps/api/tests/Feature/Import/RoundTrip/CompositeItemsRoundTripTest.php:288-304` (fails at `:417`) | `test_today_disabled_tenant_can_upload_and_execute_composite_items` still asserts the **pre-G-3b** behaviour ("The ONE pin G-3b flips: with the module DISABLED the import still goes through", `:290`). It has **no** reflection skip-guard (unlike its three siblings at `:227`, `:248`, `:264`), so G-3b's gate makes it fail: `Expected response status code [201] but received 403`. **Reproduced on BOTH drivers** (SQLite and PostgreSQL, see commands). The file is not in the lane's diff at all. This class is named in the **live** `backend-test-pgsql --filter` allowlist (`.github/workflows/ci.yml:1109`) on a job gated `if: … github.base_ref == 'dev' …` (`.github/workflows/ci.yml:588`) — i.e. merging G-3b turns a **live merge gate red**, not a parked one. The lane's task explicitly required "skips removed / companions flipped"; the lane's own summary never reports running this class. | Invert the companion to assert `403` on upload with the module disabled (or delete it as superseded by `test_upload_requires_composite_items_module_entitlement:225`), and re-run `CompositeItemsRoundTripTest` on both drivers. |
| **F-2** | **Important** | lane `apps/api/app/Modules/Import/Presentation/Controllers/ImportController.php:884-914, 924` vs `dev:805-840` | The lane rewrites `handleProductImagesUpload()`'s signature to `(UploadedFile $file, User $user, string $tenantId, string $companyId, string $sourceHash)` and keeps base's `ProcessProductImageImport::dispatch($job->id, $fullPath, $tenantId)` (`:924`). **G-3a already landed the identical signature change on `dev`** with `(…, string $tenantId, string $companyId)`, deleted `$fullPath`, and dispatches `($job->id, $path, $tenantId, $companyId)` (`dev` ImportController `:840`; `ProcessProductImageImport::__construct` now takes a 4th `?string $companyId`, `dev :56-64`). Spec §5.2 assigns that queued gap to **G-3a**, not G-3b. Overlapping edit to the same function ⇒ guaranteed conflict; a careless resolution silently reverts G-3a: an **absolute** path handed to a worker that now expects a **disk-relative key** (breaks every product-images import), and the company anchor dropped from a queued job (rule-20 violation). `ScheduledJobTenantIsolationTest:192-217` on dev already pins the 4-param constructor. | At merge, union to a 5-param signature and keep **dev's** body: `ProcessProductImageImport::dispatch($job->id, $path, $tenantId, $companyId)`; do not restore `$fullPath`. Add this to the merge checklist explicitly. |
| **F-3** | **Important** | spec §5.2 `GET /imports` row vs `ImportController.php:66-69` | Spec §5.2 assigns the **whole** `index()` method to G-3b — "`accepts page, per_page, status, type, q`; scoped to the current company … **G-3b only** (G-R52: … the whole method — company predicate, pagination and filters — is G-3b's, and **G-6a no longer touches it**)". The lane implemented **only** the company predicate + `unattributed`; `index()` is still `->paginate(20)` with zero request filters. The brief's Task 4 asked only for the predicate ("Keep the `meta` block exactly as it is"), and under the r5 principle the brief is the executable contract — so this is **not a lane defect**, but the pagination/filter requirement is now **orphaned**: G-6a was explicitly relieved of it and no other lane owns it. | Orchestrator decision before Session G closes: re-lane `page`/`per_page`/`status`/`type`/`q` on `GET /imports` (new lane or an explicit G-6b addendum), or record the spec row as deliberately deferred. |
| **F-4** | **Important** | `apps/api/tests/feature-lane-manifest.json:9,819-820,864-865` | Lane wrote `gated_ceiling 1209`, `Import 26`, `Migrations 8` — correct arithmetic against its **fork base** `4e7f9a031` (1206 / 24 / 7), stale against `dev` (**1212 / 25 / 10**; base→dev also moved `Partner 21→22` and `Catalog 33→34`). Same for `.github/workflows/ci.yml:1109` — `dev` already carries G-3a's five appended classes, the lane's line does not. | Merger must write **`gated_ceiling: 1215`, `Import: 27`, `Migrations: 11`**, keep BOTH `DELIBERATE RAISE` sentences, and **union** the single `--filter` line so it contains dev's G-3a entries **plus** `ImportJobCompanyBackfillMigrationTest`. Re-run `php tools/feature-lane-manifest-check.php` post-merge (it enforces unique matching per entry). |
| **F-5** | **Minor** | `apps/api/tests/Feature/Import/ProcessImportJobStatusTest.php:69` | The lane adds `app(ChartOfAccountsService::class)->seedForCompany($this->company);` to `setUp()`. **Verified**: with that line removed the class fails at `:221` (`Document::where(is_historical)->count()` = 0), and the **same test is already RED on `dev`** (`:218`, run in the main checkout). So the lane silently repairs an inherited red it does not own, and the summary never mentions the addition. Legitimate fixture fix, but it also papers over a product-level observation worth an owner ticket: with no chart of accounts, the parties AR-opening path still creates an `OpeningBalanceBatch` while creating **no** historical `Document`. | Disclose the inherited-red repair in the merge record and file the "AR opening posts a batch but no Document when the CoA is unseeded" observation as a Session-G ticket. |
| **F-6** | **Minor** | `apps/api/tests/Feature/Import/ImportPreviewTest.php:80`, `apps/api/tests/Feature/Import/ImportTypesTest.php:89` | Adds `app(UnitsProvisioningService::class)->provisionForCompany($this->company);` to two `setUp()`s. Units are on the brief's explicit OUT-of-scope list (G-4 / G-12). Test-only and harmless, but a boundary nibble that can collide with the concurrent G-4 lane. | Keep, but list both files in the merge record as G-4-adjacent fixture touches. |
| **F-7** | **Minor** | `apps/api/database/migrations/tenant/2026_08_30_100200_add_company_to_import_jobs.php:21-29` + `tests/Feature/Migrations/ImportJobCompanyBackfillMigrationTest.php` | `TARGET_TABLES` maps 7 import types onto 4 tables, but **only the `products` branch is exercised** (all three backfill tests use `'products'`). `parties→partners`, `partners→partners`, `product_images→products`, `composite_items→composite_items`, `opening_balances→journal_entries` are untested; a wrong mapping there silently mis-attributes brownfield jobs fleet-wide on an auto-deploying migration. Also untested: `down()`'s logged no-op (the forward-only safety property, `:69-74`), the unsupported-driver skip (`:36-40`), and the missing-target-table / missing-`company_id` guards (`:146-150`). | Add a data provider over `ImportType` (one job per type resolving to one company) plus a `down()`-is-a-no-op assertion. Verified-safe note for the record: for `opening_balances`, `AccountingBalancesPhase.php:67` transiently parks the **ImportRow's own id** in `imported_entity_id`, so a job that died mid-execution can hold row ids — they join to zero `journal_entries` rows, so the backfill abstains rather than mis-attributing. Correct, but only by accident of the join; pin it. |
| **F-8** | **Minor** | `apps/api/app/Modules/Import/Presentation/Controllers/ImportController.php:120-123` | `hash_file('sha256', $file->getRealPath())` — `SplFileInfo::getRealPath()` returns `string\|false`; on `false` PHP 8 raises a deprecation/`TypeError` **before** the `is_string($sourceHash)` guard can return the intended 500. The failure body `['error' => 'Failed to hash uploaded file']` is also a bare string, inconsistent with the coded `['error' => ['code' => …, 'message' => …]]` envelope this same controller uses at `:427` (`IMPORT_ALREADY_STARTED`) and `:545` (`IMPORT_TYPE_RETIRED`). | `$realPath = $file->getRealPath(); if ($realPath === false) { return …coded 500…; }` then hash `$realPath`. |
| **F-9** | **Minor / info** | `apps/api/app/Modules/Import/Presentation/Controllers/MigrationWizardController.php:84` (`dependencies/{type}`), `:106` (`suggestMapping`) | Both are type-bearing and remain ungated, so a tenant without `CompositeItems` can still read composite-items dependency and column-mapping metadata. Spec §5.4's table enumerates exactly ten surfaces and **excludes** these two, and the lane's surface × check table lists them honestly. Brief- and spec-compliant. | Owner decision only — no change required of G-3b. |
| **F-10** | **Minor / info** | `2026_08_30_100200_add_company_to_import_jobs.php` vs `dev`'s `2026_08_30_100200_enforce_company_scoped_partner_vat_numbers.php` | Shared timestamp prefix, as the task flagged. `Migrator::getMigrationFiles()` sorts by basename, so `add_company…` < `enforce_company…` runs **first**, deterministically, on every node. The two touch disjoint tables (`import_jobs` vs `partners`) with no ordering dependency, and the `migrations` table keys on the full filename. **Harmless.** | No action. Recorded per the stress instruction. |
| **F-11** | **Minor** | `apps/api/tests/Feature/Import/ImportCompanyPinTest.php:109-119` | The NULL-company "allowed from any company" arm is proven only through `show`. The other seven job-loading surfaces are proven only for the **409** arm (`:97-107`). A regression that made e.g. `result-workbook` 409 on an unattributed job would not be caught, and unattributed history is exactly the brownfield case §3.3 protects. | Reuse the existing `pinnedEndpoints()` provider for a second test that asserts a NULL-company job is **not** 409 on all eight surfaces. |
| **F-12** | **Minor** | `apps/api/app/Modules/Import/Domain/ImportJob.php:21,27,57,63` | Adds `company_id` / `source_hash` to the `@property` docblock and `$fillable`. G-6a's M6c adds `error_code`, `error_detail`, `claimed_at`, `worker_started_at`, `source_purged_at` to the **same** array and docblock. | Trivial union at merge; both sides must survive. Add to the merge checklist. |

---

## Verified correct (no finding — recorded so the merger does not re-litigate)

- **Held boundaries.** `formatJob()` and `countWarningRows()` compared byte-for-byte against `4e7f9a031` → **IDENTICAL**. `apps/api/app/Modules/Import/Application/Jobs/ProcessImportJob.php` is **not in the diff** (G-6a's claim/reaper surface untouched). No file under `apps/web/`, no `locales/*/import.json`. `ImportServiceProvider.php` untouched → rule 12 chain (`['api','auth:sanctum',SetPermissionsTeam::class,EnforceTokenTenantClaim::class,'can:imports.manage']`, `:66`) intact.
- **Company pin, all eight job-loading routes.** `companyMismatch()` (`ImportController.php:936-949`) returns 409 `IMPORT_COMPANY_MISMATCH` only when `company_id !== null && !== $companyId` — NULL is allowed from any company (§3.3). Called immediately after the 404 arm and before any `formatJob`/row read in `show:267`, `preview:297`, `errors:364`, `updateOptions:428`, `errorSummary:486`, `execute:539`, `downloadFailedRows:668`, `downloadResultWorkbook:710`. All eight covered by a data provider (`ImportCompanyPinTest:83-107`).
- **Queue contract preserved (G-R16, rule 20).** `ProcessImportJob::dispatch($job->id, $companyId, $tenantId)` unchanged (`ImportController.php:640`), pinned by `ImportCompanyPinTest:163-190`, and the no-CompanyContext worker path is pinned by `:192-216` (wrong-tenant company ⇒ job Failed, `error_message = 'Company not found'`). No new named queue, no bare no-arg `getScale()` introduced.
- **Rule 19.** Nothing in the diff touches money or quantity; no `(float)`, no `number_format`, no new scale resolution. `source_hash` is a hex string.
- **Entitlement equivalence.** `ModuleEntitlementCheck::ensure()` reproduces `RequireModule::handle():54-66` exactly — same `getConfigForTenant($user->tenant)` → `hasModule($module)` → `abort(403, "Module '{$module}' is not enabled for this business type")`. Constructor-injected `CompanyConfigService` (rule 13, no `app()`). `ImportModuleEntitlementTest:30,70` asserts the literal message. Module key `'CompositeItems'` agrees across `ImportType::requiredModule()`, `config/verticals.php:92,121`, and `app/Modules/Catalog/Presentation/routes.php:32` — STOP condition 2 not triggered.
- **`requiredModule()` exhaustiveness.** The `match ($this)` covers all seven enum cases (`ImportType.php:9,10,11,39,41,42,43`) with no `default`, so a future type cannot be added without a decision.
- **All ten entitlement surfaces gated** (`store`, `show`, `preview`, `updateOptions`, `errors`, `errorSummary`, `execute`, `downloadFailedRows`, `downloadResultWorkbook`, `MigrationWizardController::template`) and asserted as a set in `ImportModuleEntitlementTest:108-130`; `store`'s `ensure()` at `:119` precedes the file write at `:164` as the brief requires. Products unaffected in both configurations (`:96-105`).
- **`index` (Task 4).** Company predicate added as a grouped `where(company_id = X OR company_id IS NULL)` (`:66`), `unattributed` computed in the map closure via `array_merge($this->formatJob(...), [...])` (`:71-74`) — the held method is **wrapped, not edited**. `meta` block unchanged. `ImportCompanyPinTest:121-145` proves each company sees exactly its own job + the NULL job and never the sibling's.
- **M4.** Guards driver ∉ {pgsql, sqlite} (`:36`), table absent (`:42`), then per-column `hasColumn` (`:48`, `:54`) and per-index `hasIndex` (`:80`, `:86`, `:92` — `Schema::hasIndex` exists in Laravel 12.58, `vendor/laravel/framework/.../Schema/Builder.php:448`). Both required indexes present incl. the G-R62 compound `(company_id, created_at)`. `company_id` is nullable `uuid` with **no FK**. Backfill attributes only when `count($counts) === 1` (`:110`), leaves NULL on ≥2 (`:121`) and on 0 (`:127`) — **no one-company fallback anywhere**. Update is re-guarded `whereNull('company_id')` (`:114`). Three census lines emitted **unconditionally** (`:130-132`, ambiguous at `warning` level) — observed in every run. `down()` is a logged no-op with the forward-only reason in its docblock (`:64-74`). `imported_entity_id` is `uuid` (`2025_11_30_150000_create_import_tables.php:44`) and every target `id` is `uuid`, so the join is type-safe on PostgreSQL.
- **Upload stamping.** `ImportService::createJob()` gains a **required** `string $companyId` and `?string $sourceHash` (`ImportService.php:53,58,63,71`); all 18 call sites repo-wide are updated (grep-verified against `dev`). Hash computed from source bytes before storage, for the ZIP path too — pinned by `ImportCompanyPinTest:147-161` and `ProductImagesZipRoundTripTest:119-120`.
- **Deptrac.** 183 violations / 0 errors in the lane — **identical to `dev`'s 183**. The new `Import → App\Services\CompanyConfigService` edge is reported as *Uncovered*, exactly like five pre-existing consumers (Document, Fiscal, Procurement, Tenant). No STOP, no baseline bump needed.

---

## Command outputs

All run from `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/g3b-company-pin/apps/api` unless noted. PG leg used a purpose-created isolated database `autoerp_g3b_gate` on `127.0.0.1:5433` (the shared local `autoerp` DB is schema-stale and fails **untouched** baseline classes too — `ImportInfrastructureTest` = 26 failed on `dev`-equivalent code — so it is not a usable PG runner).

| # | command | result |
|---|---|---|
| 1 | `php artisan test tests/Feature/Import/RoundTrip/CompositeItemsRoundTripTest.php` | **FAIL** — `1 failed, 1 skipped, 9 passed (67 assertions)`. Failure = `today disabled tenant can upload and execute composite items` at `:417`, `Expected 201 but received 403`. The three entitlement pins (`upload`/`template`/`execute`) now **run and pass** (skips auto-lifted by the reflection guard). |
| 2 | `php artisan test tests/Feature/Import/RoundTrip tests/Feature/Import/ImportCompanyPinTest.php tests/Feature/Import/ImportModuleEntitlementTest.php tests/Feature/Migrations/ImportJobCompanyBackfillMigrationTest.php` | `1 failed, 1 skipped, 37 passed (286 assertions)` — same single failure. ImportCompanyPinTest 13/13, ImportModuleEntitlementTest 3/3, migration test 4/4 (census lines printed: attributed 0 / ambiguous 1 with both company counts / none 1 across the fixtures). |
| 3 | `php artisan test …ImportInfrastructureTest …ProcessImportJobStatusTest …ImportReExecutionGuardTest …StockLevelsImportDeprecatedTest …ScheduledJobTenantIsolationTest` | **PASS** — `48 passed (143 assertions)`. No assertion weakened. |
| 4 | `php artisan test tests/Feature/Import tests/Feature/Modules/Catalog/Media/ProductImageImportServiceMediaTest.php` (whole Import feature dir) | `1 failed, 1 skipped, 192 passed (1155 assertions)`, 151s — the **only** failure in the entire module is F-1. |
| 5 | `php artisan test -c phpunit-pgsql.xml tests/Feature/Migrations/ImportJobCompanyBackfillMigrationTest.php` (fresh PG DB) | **PASS** — `4 passed (21 assertions)`. Reproduces the lane's claimed PG leg. |
| 6 | `php artisan test -c phpunit-pgsql.xml …ImportCompanyPinTest …ImportModuleEntitlementTest …CompositeItemsRoundTripTest` (fresh PG DB) | `1 failed, 1 skipped, 25 passed (133 assertions)` — pin 13/13 and entitlement 3/3 green on PostgreSQL; **F-1 reproduces on PG too**. |
| 7 | `./vendor/bin/phpstan analyse` on 10 touched PHP paths (level 8) | **`[OK] No errors`** |
| 8 | `./vendor/bin/pint --test` on `app/Modules/Import`, the migration, `tests/Feature/Import`, the two new test dirs | **`{"result":"pass"}`** |
| 9 | `php vendor/bin/deptrac analyse` (lane) | `Violations 183, Errors 0` |
| 10 | `php vendor/bin/deptrac analyse` (main checkout, `dev`) | `Violations 183` — **no new violation** |
| 11 | `php tools/feature-lane-manifest-check.php` | **exit 0** — "manifest OK — 1468 Feature classes in 74 groups … every `--filter` entry is anchored and uniquely matched". Parked warning reports 1209 classes = the lane's ceiling (see F-4). |
| 12 | `python3 -c "yaml.safe_load(open('.github/workflows/ci.yml'))"` | **parses**, 26 jobs. `ImportJobCompanyBackfillMigrationTest` appended inside the anchored single `--filter`. |
| 13 | `git diff -U0 … ImportController.php \| grep '^@@'` + byte-compare of `formatJob` / `countWarningRows` vs `4e7f9a031` | **IDENTICAL** for both held methods; no hunk inside either. |
| 14 | overlap analysis `comm -12 <(git diff --name-only 4e7f9a031 dev) <(lane dirty files)` | 6 shared files: `.github/workflows/ci.yml`, `ImportController.php`, `ProductImagesZipRoundTripTest.php`, `ScheduledJobTenantIsolationTest.php`, `ProductImageImportServiceMediaTest.php`, `feature-lane-manifest.json` |
| 15 | manifest arithmetic `4e7f9a031` → `dev` | ceiling `1206 → 1212`; `Migrations 7→10`, `Partner 21→22`, `Import 24→25`, `Catalog 33→34` |
| 16 | ablation: remove `seedForCompany` from `ProcessImportJobStatusTest` and re-run (lane); then run the same class in the main checkout on `dev` | lane-minus-line: `1 failed, 3 passed` at `:221`; **`dev`: `1 failed, 3 passed` at `:218`** ⇒ inherited red, see F-5. Lane file restored, `git diff --stat` back to `4 ++++`. |

---

## Merge checklist the merger must apply against `dev` (ceiling 1212 / Migrations 10 / Import 25)

1. Fix **F-1** before merging — it reds a live `backend-test-pgsql` gate.
2. Resolve `ImportController::handleProductImagesUpload` per **F-2**: 5-param signature, **dev's** `dispatch($job->id, $path, $tenantId, $companyId)`, no `$fullPath`.
3. Manifest union per **F-4**: `gated_ceiling 1215`, `Import 27`, `Migrations 11`; union the `ci.yml --filter` line (dev's G-3a entries + `ImportJobCompanyBackfillMigrationTest`); re-run the checker.
4. Union `ImportJob::$fillable` with G-6a's M6c columns when that lane lands (**F-12**).
5. Record the inherited-red repair (**F-5**) and the G-4-adjacent fixture touches (**F-6**) in the merge note.
6. Take the **F-3** orphan (`GET /imports` filters/pagination) to the orchestrator.
