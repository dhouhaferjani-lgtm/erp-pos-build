# Parapharmacy remediation — Codex spec fix round 2 notes

Date: 2026-09-05. **v3 documentation complete; awaiting Fable gate r3.** Proposed remedies are specified, not implemented or gate-approved. Fable remains the sole orchestrator, reviewer dispatcher, committer and promoter.

Read the [round-2 handover](../../handoff/HANDOVER-parapharmacy-remediation-codex-spec-fix-round-2-2026-09-05.md) in full before changes, then [r2 synthesis](2026-09-05-parapharmacy-remediation-gate-r2-synthesis.md), all four linked lenses ([fiscal](2026-09-05-parapharmacy-remediation-gate-r2-fiscal.md), [inventory](2026-09-05-parapharmacy-remediation-gate-r2-inventory.md), [treasury](2026-09-05-parapharmacy-remediation-gate-r2-treasury.md), [authz](2026-09-05-parapharmacy-remediation-gate-r2-authz.md)) and the round-1 R1–R4 handover. Source findings are anchored at the reviewed `f75aa5023`, with later-plan recensus required; working HEAD was `fa000edc3` when read. This round did not change HEAD.

## Deliverables

- [Spec v3 in place](../specs/2026-09-05-parapharmacy-readiness-remediation-design.md), with v2→v3 change log and verbatim synthesis Preserve paragraph in §3.3.
- [Glossary](../../glossary.md): all three lot-evidence producers; new Lot identification, Tender classification, Tender binding and Tender binding resolution; bounded Session reconciliation definition; corrected repository location/uniqueness gap.
- This note, including the full twelve-row OPEN register below. Earlier handovers/reviews/tickets and round-1 notes remain unchanged.

## Six blockers — specified closure

| Blocker | v3 change and evidence |
|---|---|
| B1 — unrowed sealed tender transport | D8 is OPEN; W2 contains conditional transport contracts without recommending one in its body. §5 gives D7/D8 one future SALE_RECEIPT version/cutover, holding sealed authoring if either choice is unresolved; R3 stays unchanged payload. Sealed money bindings require device UUID/value/scope validation, correcting the optional-string precedent at `apps/pos/src/lib/fiscal/FiscalEventEngine.ts:2844`. W2 adds append-only permissioned resolution for never-projected blocked legs, with POS-core unaffected and posted-leg rebind forbidden |
| B2 — false voucher/account bypass | W2 explicitly retracts r1/v2: `apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:480` processes every payments[] leg. Derived four-class routing adds `non_money_destination`; full PaymentMethodSeeder country lists, MEAL_VOUCHER portfolio, LOYALTY liability and account-charge/collection distinctions have named outcomes. RD3 governs electronic settlement only |
| B3 — scoping breaks terminal destinations | W1 names sync/API/cache/cash/card/refund consumers (`apps/pos/src/lib/sync/syncService.ts:1256`, `paymentStore.ts:950–962,1185–1192`, refund store). Chooses a narrow terminal-authorized projection of the same catalogue, independent of human balance scope, and requires device migration/first-active retirement before human-index scoping. Acceptance covers restricted second-branch cashier, fresh hydration then offline restart, supervisor cache and company/terminal changes |
| B4 — DEFAULT identification dead end | L9 is ordered **before L4**, and must accompany L2 freeze. One existing batch-detail Identify/split action authors a scoped, permissioned identification document and flat stock justification with conserved lot legs; no aggregate/WAC/GL change, invented expiry, used-history merge or repeat effect. L9 has its own acceptance row and glossary noun. Opening source: `apps/api/app/Modules/Import/Services/ProductOpeningStockPhase.php:71–77` |
| B5 — module invariant false today | R2 is explicitly a worker/projection fix; company ownership/effective entitlement must be resolved rather than ignored (`apps/api/app/Modules/Fiscal/Application/Services/DefaultModuleActivationResolver.php:50–56`). Current tenant inheritance is distinguished from independent company licensing. Product UI/write checks, 24h cache invalidation/race handling (`apps/api/app/Services/CompanyConfigService.php:34,89–103`), effective-company census filter (`LotLedgerDriftCensus.php:81`) and retail flagged-product/history fixtures are required. D9 deactivation versus parapharmacy-always-on stays OPEN |
| B6 — backend-only L1 | L1 names Sidebar/RequirePermission/usePermissions and list/detail action gates, plus a seven-role current/proposed grant delta and existing-tenant reseed/cache rollout. Existing seven permissions are reused; only L9 introduces `batches.identify`. RD2 still decides manager recall. API and rendered entry/action denials are both acceptance requirements (`apps/web/src/components/organisms/Sidebar/Sidebar.tsx:222–223`) |

## Every major — disposition

M01–M15 follow the synthesis Majors list in order. M16–M17 additionally track the Treasury lens’s MAJOR-1/5 so no lens major disappears through deduplication. Findings already represented by blockers remain cross-referenced here. **No major finding is rejected.**

| ID / review finding | Change in v3 / controlling citation |
|---|---|
| M01 / fiscal M-3 — second recovery surface | W4 extends the existing manual enqueue command/shared dispatcher and uses one cohort/claim implementation for manual, scheduled and ingest recovery. Explicit manual versus system authority; no actor fabrication or scheduling of the protected command; remove current-activation historical reseeding. `apps/api/app/Modules/Fiscal/Infrastructure/Commands/EnqueueResolvedEventProjectionsCommand.php:245,358,439–440` |
| M02 / fiscal M-4 — NearExpirySlot | W6 fills the shipped NearExpirySlot across ProductCard/ProductListRow/ProductTable; supersedes only the deferred Spec-2 lot plumbing in the caisse presentation spec, reuses cart semantics, and names alternate renderer census/acceptance. `apps/pos/src/components/organisms/ProductGrid/NearExpirySlot.tsx:1–27` |
| M03 / fiscal M-2 — server-only binding validation | W2/D8 require local required UUID and scope/class checks for money bindings; null/garbage refuses before sealing. Server unresolvable binding has blocked-leg repair, not a copied hard-throw dead end. `FiscalEventEngine.ts:2844`; `TreasuryAccountPaymentBridge.php:406–419` |
| M04 / treasury MAJOR-3 — GL concurrency | W3 PostgreSQL acceptance includes B2B versus POS Treasury bridge, repository adjustment and procurement/supplier posting, with lock hold/wait time and bounded progress, not invoice-only. Shared company chain collision at `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:591–609` and `GeneralLedgerService.php:5722–5731` |
| M05 / treasury MAJOR-4 — repository code scope | W1 aligns **HTTP validation** to existing company-scoped DB uniqueness; no redundant migration. Real API second-company create/update, same-company duplicate refusal and rerun are required. `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRepositoryController.php:80,170–172`; migration `2025_12_30_195300_fix_multi_company_unique_constraints.php:31–35` |
| M06 / treasury MAJOR-6 — fourth classification surface | W2/glossary define one derived value object over existing cash/maturity/instrument flags plus typed tender-operation semantics, no fourth writable class column; explicit non-money handler dispatch replaces classification-by-elimination. `apps/api/app/Modules/Treasury/Domain/PaymentMethod.php:26–29,95–99`; `Concerns/HandlesMaturityTenderLeg.php:33–48` |
| M07 / inventory MJ-1 — three provenance producers | L5/glossary/acceptance enumerate POS allocations, document_lines via converters/reservations and transfer allocations; both document_sales and pos_sales traces, detail/export/returns inherit provenance. `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchTraceabilityController.php:60–88`; `StockTransferService.php:789,947–989` |
| M08 / inventory MJ-2; fiscal m-5 — float census | L3 retires/converts BatchStock accessor, hasAvailableStock, reserve/releaseReservation/adjustQuantity, Batch aggregates→BatchResource, dormant FEFO total and all consumers; exact strings at rest/transport, PostgreSQL discrimination and live float guard. `apps/api/app/Modules/BatchExpiry/Domain/Entities/BatchStock.php:44–84`, `Batch.php:143–151`, `FEFOInventoryService.php:940–957` |
| M09 / inventory MJ-3 — reattribution GL | W5 requires one flat zero-quantity stock justification, existing count reason/reference linkage, paired opposite lot legs and zero journals/unchanged WAC. True no-op guard remains; nonzero lot reattribution gets its own branch. L9 uses the same shape with its own evidence source. `apps/api/app/Modules/Inventory/Application/Services/InventoryGlPostingService.php:37–52`; `StockMovement.php:183–192`; `StockAdjustmentService.php:1410–1415` |
| M10 / inventory MJ-4 — lock order/SKIP LOCKED | W5 requires full lot/stock/product/reservation writer census compatible with WAC ordering; serialize count/identification and POS consumption before FEFO, or leave genuinely incomplete count provisional with a visible exception. PG acceptance prohibits silent lot diversion. `apps/api/app/Modules/Inventory/Application/Services/WeightedAverageCostService.php:87–95`; `FEFOInventoryService.php:264–272` |
| M11 / inventory MJ-6/MJ-7 — device exact effective stock | W6 extends stockDistribution/gridStock/locationStockRepository, requires later SQLite TEXT migration and decimal strings, nets server reservations plus local pending sales/cart through the availability/stockGate path, tracks snapshot inclusion independently of ACK, and defines next-add versus cart-line semantics. Unit precision, fractional/reservation/offline-sale/resync acceptance included. `apps/pos/src/types/stockDistribution.ts:1–13`; `apps/pos/src/lib/stock/gridStock.ts:6–9,31–38` |
| M12 / authz M-1 — route ratchet | G1 uses the booted router, `can:` and `require.any.permission:`, explicit public/controller/service classifications, static starting counts 559/205/~77 qualified as measurements, shrink-only baseline and unchecked DELETE liveness in the default lane. `apps/api/bootstrap/app.php:113–121`; authz r2 M-1 measurement |
| M13 / authz M-2 — HTTP resolver in workers | W1 HTTP adapter authorizes; shared service receives scoped user or explicit system intent and validates persisted custody/version without HTTP CompanyContext. Queued/no-user acceptance included, without activating RD4 in production. `apps/api/app/Modules/Company/Services/LocationScopeResolver.php:12–16`; `PostShiftCashVarianceAdjustment.php:199,267` |
| M14 / authz M-5 — bypass grant/seeding | W1 uses real `admin` role; catalogue+grant/reseed/cache invalidation precede scope activation in every tenant, D5 governs any extra grants; not-yet-reseeded tenant blocks activation. `apps/api/database/seeders/RolesAndPermissionsSeeder.php:547` |
| M15 / authz M-6 — route-grain location coverage | G2 names 15 downstream consuming files plus the Company scope-owner consumer, recognized request/rule enforcement, and per-route classification; L1 fixes expiring while preserving expired scope. `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:215–236,252`; `apps/api/app/Rules/ValidLocationAccess.php:69–79` |
| M16 / treasury MAJOR-1 — RD3 silently normative | W2 body explicitly branches RD3-reverse and RD3-retain. Reverse-only refusal cannot ship while the decision is OPEN; retaining electronic drawer fallback keeps a marked limitation and excludes unsafe client tender enablement. Vouchers/loyalty/account never inherit RD3’s electronic-only restriction. `TenderRepositoryResolver.php:119–130,198–207` |
| M17 / treasury MAJOR-5 — nondiscriminating matrix | W2 distinguishes explicitly mapped CARD→drawer even with bank present from unmapped/no-bank fallback, and names selected-policy outcomes. H-3 limitation marker is required in the first authorized W0 lane independent of RD3, not edited in this spec-only round. `TenderRepositoryResolver.php:157–171,198–207`; `CleanRegistrationDownstreamAssumptionsTest.php:133–151` |

Additional major cross-map: fiscal M-1 / treasury MAJOR-2 / authz M-4 → B1 and M03; fiscal M-5 / authz M-3 / inventory MJ-5 → B5 (worker/company/cache and entitlement-filtered census). These are specified fixes, not unresolved census-only placeholders.

## Qualifications and minor corrections

- The synthesis’s compressed repository-uniqueness wording is corrected to the verified mechanism: the API is tenant-wide, **the DB is already company-scoped** (M05 citations). Fix validation, preserve the DB key.
- Authz M-3 recommends tenant-level wording while the owner handover requires company-level enforcement. v3 honors both facts: today companyId is ignored; the fix validates company ownership and effective inherited entitlement, without inventing independent company licensing. D9 owns any new disable model.
- The lens labels its consumer baseline “15 files” but lists Company LocationController as well as 15 downstream files. G2 states that counting convention explicitly (15 downstream, 16 including the scope owner) and requires route-level census; it does not repeat the old single-page baseline.
- Treasury MAJOR-5’s request to mark the test “now” cannot override the owner’s no-test-edits scope. The marker is now an unconditional future W0 requirement; its actual test edit waits for Fable dispatch. This defers execution, not the finding.
- W7 attributes receipt_count’s refund exclusion to the device, bounds independence to the Z summary, names chain verification/quarantine as complementary, and adds reclassified/unknown tender acceptance. W3 states actionable invoice-blocking chart/period failures; W1 corrects unrestricted membership citation and transactions company predicate. W2 adds the no-unsettled-projections precondition before cash-classification changes (`TreasuryReceiptBridge.php:961–967`).
- W5 corrects replay citations, scopes/keys child observations, preserves the true no-op guard and marks T22’s negative-lot leg as already present. W6 names unit precision and alternate renderers. W8 requires catalogue/evidence classifications while preserving existing company-scoped product_batches uniques. G7 registers its new marker in conventions; G1 does not inherit the PG-only skip.

## Full owner-decisions register — all OPEN

Copied from spec §2.3 for this handback; the spec remains the authoritative register for subsequent rulings. No recommendation below is an owner decision.

| ID | Status | Decision and recommended default | Alternative and consequence / gate |
|---|---|---|---|
| D1 | OPEN | Confirm branches are locations of one legal company | Separate legal entities require intercompany design; never model cross-company custody as an internal branch transfer |
| D2 | OPEN | Confirm POS/B2B usage and exact enabled tenders; release only the selected surfaces/methods | Either/both surfaces allowed after their acceptance; unused paths remain excluded, not marked tested |
| D3 | OPEN | Keep R3 iteration 1 as suggestion-only shelf guidance; recommend captured-lot capability as iteration 2 when physical traceability is required | Staying on shelf control requires a written expiry/recall procedure, estimate/unknown labels, no software-captured traceability claim and a dated revisit; if capture is a launch requirement, defer launch of that use case until iteration 2 |
| D4 | OPEN | Recommend refreshing branch eligibility before opening a session; permit cached guidance during that session with visible snapshot age/staleness; require refresh before next session | Owner may choose a stricter age cutoff or longer offline tolerance, each with explicit unavailable/stale behavior; applies to iteration 1 cache and later capture. No silent hard cutoff or invented expiry; no guarantee of knowing recalls issued while offline |
| D5 | OPEN | Branch managers operate allowed custody; central treasury has explicit all-location authority | Company-wide manager custody is possible only as an explicit policy; approve source/destination reach before W1 execution |
| D6 | OPEN | Exclude cross-branch returns from initial release | Enabling requires receiving-branch stock/lot disposition, original provenance, payout custody and two-branch acceptance |
| D7 | OPEN | Iteration 2 only: recommend optional line-level lot fields in a new `SALE_RECEIPT` vN+1, required by captured-mode capability | Inline evidence uses the receipt's SQLite transaction/outbox and ordering; sealed errors need append-only corrections. A separate versioned evidence stream preserves fiscal shape but adds linkage/hash checks, local atomicity, reordered arrivals and conflicts. It still needs append-only corrections. Neither transport is selected here; no iteration 1 lot version/outbox work depends on D7 |
| D8 | OPEN | Tender-binding transport: recommend a required validated sealed repository/binding reference for money-destination legs in the single future SALE_RECEIPT version, subject to the owner confirming timing under R3 | Alternative: no sealed change, with an unsealed authored policy-revision snapshot plus durable server binding. This avoids sealing Treasury policy but needs authenticated linkage, immutable server revisions and conflict detection; an unsealed field alone is not fiscal evidence. Sealed bindings need append-only repair if unresolvable. Both options use one D7/D8 roadmap; no iteration-1 payload change is authorized while this remains open |
| D9 | OPEN | Recommend retaining BatchExpiry as always-on for parapharmacy, matching current vertical defaults | Alternative: make it deactivatable with an explicit effective-module exclusion model, server/device invalidation and historical-lot cohort policy; additive extras alone cannot disable it. Worker entitlement fixes and retail module-off acceptance are required either way; no deactivation rollout before this ruling |
| RD2 (review 2) | OPEN | Recommend restricting company-wide `batches.recall` to designated central inventory authority | Keeping seeded manager recall makes it intentionally company-wide despite branch membership; either option still needs action checks. Blocks final W-LOT role defaults |
| RD3 (review 3) | OPEN | Recommend reversing H-3's electronic-settlement → drawer fallback: electronic use unavailable until valid settlement configuration; does not govern voucher/loyalty/account destinations | Retaining fallback leaves that electronic day-one behavior in place as an explicit limitation; this client’s electronic tender launch remains excluded pending safe configuration. Do not change the pin/seed behavior without this ruling |
| RD4 (review 4) | OPEN | Recommend delivering fiscal-derived W7 first and deferring repository-balance comparison until float and drops are booked | Funding Treasury float/drawer-operation coverage first enables the separate repository check after source/idempotency/opening-baseline acceptance. Neither option permits comparing ledgers with different coverage or enabling the disabled variance writer prematurely |

## Verification and stop

Document-only checks: required sections/decision rows, B1–B6 and every-major mapping, ordered L9 before L4, source/document paths and verbatim preservation. No application tests, code/migration edits or runtime changes; no review call, commit, merge, rebase or push. Pre-existing unrelated working-tree files were left alone. **Codex stops; Fable runs gate r3.**
