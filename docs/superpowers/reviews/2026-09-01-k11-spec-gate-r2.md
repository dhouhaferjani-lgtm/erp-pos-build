# Lane K-11 adversarial SPEC gate r2 — barcode identity, multi-location lines, census honesty

**Target (v2):** `docs/sessions/session-K-otospex-money-2026-08-30/LANE-K11-barcode-collision-census-BRIEF.md`
**r1 register:** `docs/superpowers/reviews/2026-09-01-k11-spec-gate-r1.md` (REJECT; K11-S1-01..15)
**Base read:** main checkout at `dev` `bef257b5a`; in-flight worktree `.worktrees/k89-units` (`fix/k89-units-validation-mapping`, uncommitted K-8/K-9 edits) inspected for the collision seam.
**Method:** static reading only. Nothing modified, no suites run.

## Verdict

**ACCEPT-WITH-CONDITIONS.** The owner rulings resolve OR-1/OR-2/OR-3 cleanly and v2 closes the four r1 BLOCKERs at the design level. Two claims in v2 are **false against the code** and would each produce a shipped defect if an implementer trusted them — the named census-then-refuse precedent actually **throws** (§K11-S2-01), and per-line multi-location opening stock does **not** land today for a merged group under `duplicate_policy = skip` (§K11-S2-02). Both are brief-text edits, not redesigns. Fix C1 and C2 before dispatch; C3–C12 are honourable inside the lane.

---

## §1 — r1 disposition table (each finding vs v2)

| r1 ID | v2 disposition | verified? |
|---|---|---|
| **K11-S1-01** write-guard on the barcode WRITE | **ADDRESSED** — v2 §3 puts the guard in `ProductService` for import + API and names the `CoalescingAttributeMerger.php:56` seam. | ✅ Correct seam: `ProductService.php:145` writes `'barcode' => $barcode` unconditionally on both branches; `CoalescingAttributeMerger.php:56` `default => [$field]` lets it survive the merge. |
| **K11-S1-02** placement key must not absorb barcode | **ADDRESSED** — v2 §1 "gains a barcode dimension WITHOUT replacing the SKU key". | ✅ `DuplicateCensusService.php:321-346` keeps its ladder; adding a parallel hash is compatible. |
| **K11-S1-03** option (a) is an outage | **SUPERSEDED by ruling 2** — consistent groups import as one product, contradictory groups refuse. Option (a) struck. | ✅ Refusal removes the twin state that `ProductResolver.php:87-100` turns into `BarcodeAmbiguous` and that `InventoryCountingController.php:770-772` resolves with an arbitrary `->first()`. |
| **K11-S1-04** resolver untouched | **ADDRESSED** — v2 §3 last sentence, explicitly. | ✅ |
| **K11-S1-05** both policies | **PARTIAL** — v2 §1/§6 cover Override for the *conflict* class only. No `skip` × *multi-location* case, which is exactly where the silent loss is (K11-S2-02). | ❌ see C2 |
| **K11-S1-06** workbook split | **ADDRESSED** — v2 §5. | ✅ `ResultWorkbookService.php:21-31` splits on `is_valid && import_error IS NULL`; the Skipped section is genuinely new work. |
| **K11-S1-07** six-file vocabulary contract | **PARTIAL / WRONG LIST** — v2 §5 names `warningCodes.ts` for an **error** code. | ❌ see K11-S2-05 |
| **K11-S1-08** merge order vs uncommitted worktrees | **ADDRESSED** — v2 §5 names `.worktrees/k67-import` and `.worktrees/k89-units`, drops G-2/G-5, and orders K-11 after K-8-FR2. | ✅ Both worktrees confirmed dirty on K-11's files. |
| **K11-S1-09** `decide()` O(n²) | **PARTIAL** — v2 §1 requires the *new* grouping be O(n) but leaves the existing per-row re-scan (`DuplicateCensusService.php:144-178`, called from `ImportService.php:512-514`) untouched and drops the 859-row timing assertion. | ❌ see C9 |
| **K11-S1-10** hand-rolled FE bucket union + 6-col grid | **NOT ADDRESSED** | ❌ Still `apps/web/src/features/import/types.ts:78` hand-rolled (`:80` correctly aliases the generated `ImportErrorCode`); `ImportWizardPage.tsx:1027-1035` hardcodes six buckets in `sm:grid-cols-6`; `:1020-1025` sums three. See K11-S2-11. |
| **K11-S1-11** blank-SKU in-file barcode path already works | **NOT RESTATED** — low risk now that grouping is additive, but the pinned test is unnamed. | ⚠ C10 |
| **K11-S1-12** `preview_drift` already exists | **MOOT** — conflicting rows now fail rather than drift. | ✅ |
| **K11-S1-13** `barcode` has no import validation rule | **NOT ADDRESSED** | ❌ Verified still absent: `ImportType.php:202-229` has `sku max:100`, `category_name max:255`, no `barcode` key at all, against `products.barcode varchar(100)` (`2025_11_30_052910_create_products_table.php:27`). |
| **K11-S1-14** benchmark table | **REGRESSED** — v2 deleted the benchmark note without replacing it with the baseline table. | ❌ round-0 check 6 of `docs/conventions/10-BENCHMARK-FIRST-SPECS.md`. |
| **K11-S1-15** 179 vs 180 | **NOT ADDRESSED** — §6 asks for a 180-barcode fixture but states no arithmetic. | ❌ C12 |
| r1 **C13** second company | **ADDRESSED** — v2 §6 "same barcode in another company is legal". | ✅ Consistent with `ProductResolver.php:152-153` (`where tenant_id` + `where company_id`) and with a `unique(company_id, barcode)` key. |

---

## §2 — New findings

| ID | severity | file:line | finding | required brief change |
|---|---|---|---|---|
| **K11-S2-01** | **BLOCKER** | brief §4; `apps/api/database/migrations/tenant/2026_08_30_133000_enforce_company_scoped_journal_entry_numbers.php` (`assertNoCrossCompanyNumberCollisions()` → `throw new RuntimeException(...)`, ~L134-148); `2026_08_30_100000_enforce_company_scoped_product_skus.php:71-113` (same shape, `throw new RuntimeException('Cannot enforce unique(company_id, sku) ...')`) | **The named precedent is census-then-THROW, not census-then-skip.** v2 §4 says the migration "records/logs and SKIPS constraint creation … so `tenants:migrate` never throws on legacy data (precedent: `journal_entries` … `d8215e111`)". `d8215e111`'s migration does the opposite: it censuses, `emitError`s, and **throws**, aborting the migration. An implementer told to "follow the precedent" will ship a throwing migration that, per the standing rule, runs fleet-wide via `tenants:migrate` on the next `origin/dev` push and hard-fails every tenant that already holds barcode twins — the exact fleet outage OR-3 was written to avoid. | Strike the precedent citation or restate it honestly: *"the JE/SKU lanes THROW; K-11 deliberately diverges to log-and-skip"*. Then specify the skip contract explicitly: which log/table records violators, what the census command is called, how a later run promotes to the constraint once clean, and how `Schema::hasIndex`-style re-entry stays idempotent. Add a migration test for **both** legs (violators → no constraint, no throw; clean → constraint present). |
| **K11-S2-02** | **BLOCKER (K-2 class — undeclared new work)** | brief §2; `ProductOpeningStockPhase.php:40-45` (`->where('outcome', ImportRowOutcome::Imported)->whereNotNull('imported_entity_id')`); `ImportService.php:539-553` (`DuplicateLoser`: `is_imported=false`, no entity id) and `:556-569` (`DuplicateSkipped`: same); `DuplicateCensusService.php:344` (`$identity."\0".($locationId ?? 'no-location')`) | **"Per-line `location_code` already flows through `ProductOpeningStockPhase.php:247-249`" is true only under `duplicate_policy = override`.** `:247-249` is just `resolveLocationId()`; the gate that decides whether a line reaches it is `:40-45`. Because the placement key already carries the location (`:344`), N lines to N distinct locations are **not** in-file duplicates, so under **Override** each line is stamped `Imported` with the same `imported_entity_id` and stock lands per location — the claim holds. Under **Skip** (an operator-selectable policy, `ImportWizardPage.tsx:1071-1090`) line 2 resolves to `ExistingSku`/`ExistingBarcode`, `isExistingBucket()` (`ImportService.php:601-611`) returns true, the row becomes `DuplicateSkipped` with `is_imported=false` and **no** `imported_entity_id` — so `ProductOpeningStockPhase` never sees it and **the second location's opening stock is silently dropped**. Same for any same-location repeat (`DuplicateLoser`). The ruled behaviour ("import ONE product with stock landed per line's location") therefore requires new code under `skip`, and the brief presents it as free. | Say explicitly: the multi-location group must land stock per line **under every policy**, which means `duplicate_policy = skip` must not suppress the opening-stock leg for a confirmed multi-location group. Name the mechanism (e.g. a distinct outcome for "attributes skipped, placement applied", or extend the phase's row filter) and add the `skip` × 2-location test asserting **stock quantity at both locations**, not a status. |
| **K11-S2-03** | MAJOR | brief §1/§5; `apps/api/app/Modules/Catalog/Application/Services/VariantLabelService.php:94,143,158,165`; `apps/web/src/locales/en/catalog.json:322`, `fr/catalog.json:322` | **`barcode_conflict` is already a live, different concept.** The variant-label lane uses the literal string `barcode_conflict` as a **skip** reason (label not printed) with its own en/fr translations. K-11 would mint a second `barcode_conflict` meaning **row refused** in a different enum and namespace. One-surface-per-concept: two operator-visible strings, same name, opposite outcomes. | Either pick a distinct code (`barcode_row_conflict`, `barcode_group_conflict`) or declare the synonym in `docs/glossary.md` with both surfaces and both meanings. State which. |
| **K11-S2-04** | MAJOR | brief §1 ("reuse the existing policy-confirm surface, not a new dialog system"); `ImportWizardPage.tsx:1069-1090` (radio `fieldset`), `:637-641` (`updateOptions`), `:296`, `:648-651`, `:674-676`, `:1474-1486` (`ConfirmDialog` + `showPartialImportDialog`) | **There is no "policy-confirm surface".** The duplicate-policy control is a **radio selection** persisted by the Next button; the only actual confirm in the wizard is the shared `ConfirmDialog` driven by `showPartialImportDialog`, and it fires only when `jobData.failed_rows > 0` (a *validation* count). "Reuse the existing surface" is therefore under-specified: an implementer can plausibly build three different things. | Name the component and the state: `ConfirmDialog` (`@/components/ui/ConfirmDialog`) + a new `showMultiLocationDialog` state alongside `showPartialImportDialog`, OR a persisted job option (`multi_location_confirmed`) set from the existing fieldset. Say which, and say whether the confirm is blocking. |
| **K11-S2-05** | MAJOR | brief §5 ("6-file vocabulary contract … `warningCodes.ts`"); `apps/web/src/features/import/warningCodes.ts:1-29` (`satisfies Record<ImportWarningCode, true>`); `apps/web/src/locales/en/import.json` `errors.*` keys; `ImportErrorCode.php:32-49` | **The file list is the WARNING contract applied to an ERROR code.** `warningCodes.ts` is typed `Record<ImportWarningCode, true>` — an `ImportErrorCode` case cannot go there. Error codes render from the `errors.*` namespace in `import.json` (which has `errors.barcode_ambiguous` … and, note, is **already missing** `errors.worker_lost` because there is **no locale-parity test for error codes**, unlike `ImportWarningLocales.test.ts` for warnings). And `ImportErrorCode::isJobLevel()` is an exhaustive `match` with **no `default`** — a new case must add its arm. | Split the two vocabularies in the brief. `barcode_conflict` (error) = `ImportErrorCode.php` enum case + `isJobLevel()` arm (row-level ⇒ `false`) + `errors.barcode_conflict` in en/fr/ar + regenerated committed `packages/shared/types/generated.d.ts` (`php artisan typescript:transform`). `multi_location` (warning, if it is one) = `ImportWarningCode.php` + `warningCodes.ts` + `warnings.*` ×3 + generated types. Add the missing `errors.worker_lost` key while in there, or record it as a follow-up. |
| **K11-S2-06** | MAJOR | brief §5 (docs list = `docs/modules/imports.md` only); `docs/glossary.md:36` | **The glossary edit is missing and the ruling contradicts the current entry.** Line 36 defines Product as "keyed by **SKU** … with **optional barcode**" and pins "`product_variants` barcode stays **tenant-wide** by RUL-2". Ruling 1 makes `products.barcode` a **company-scoped key**, so the tenant now has two barcode surfaces at two different uniqueness scopes. Undeclared, that is a one-surface-per-concept violation and a future migration trap. | Add `docs/glossary.md:36` to the deliverable list; the edit must state the products barcode key, its scope, and that the variant-barcode tenant-wide scope is deliberately unchanged (or flag the divergence as an owner question). |
| **K11-S2-07** | MAJOR | brief §3/§4; `ProductResolver.php:87-100`; `InventoryCountingController.php:770-772`, `:1204-1206`; `CountingItemController.php:146` | **Legacy twins are left in a lockout.** With the migration skipping on violators, existing twin pairs survive indefinitely. A write guard phrased as "never write a barcode that another product of the company already holds" then makes **both** twins unsaveable from the product form (each is blocked by the other) even for an edit that does not touch the barcode, while scan lookups keep resolving an arbitrary `->first()`. The brief provides only a census command. | Require the guard to (a) exclude the row itself and (b) tolerate a **pre-existing** duplicate on the row being edited — i.e. refuse only a write that *creates or extends* a collision. Add the "edit an existing twin's price, save succeeds" test. State the remediation surface for censused twins, or record the residual explicitly with an owner item. |
| **K11-S2-08** | MAJOR | brief ruling 3; `ProductResolver.php:145-160` (`rowsBy` scopes `tenant_id` + `company_id`) vs bare `Product::where('barcode', …)` shapes elsewhere | **The 409/422 conflict payload must be company-scoped, and the brief does not say so.** `products` lives in a per-tenant DB, so a bare barcode query is tenant-wide and would put a **sibling company's** `{id, sku, name}` into the response — and the FE would render a "view/edit it" link to a product the user cannot open. | State that the holder lookup is `company_id`-scoped (reuse `ProductResolver`/`resolveIdentity`, do not hand-roll), that the payload carries only same-company fields, and add a second-company test asserting the conflict does **not** fire and no cross-company id is returned. |
| **K11-S2-09** | MAJOR | brief §1 ("Preview counts MUST equal execution outcomes"); k89 worktree `ImportWizardPage.tsx` +`validationValidRows` (`previewData?.summary.valid_rows ?? total − failed`) feeding the Next-button disable and the partial-import confirm text; `ImportService.php:646-661` `outcomeCounts()` | **The honesty rule has no mechanism, and it collides with K-8.** `barcode_conflict` refuses at **execution**, but K-8's new validation-step counter and the partial-import dialog (`:648-651`) both read **validation** counts. A 677-conflict file therefore shows "856 valid / 0 failed", no confirm dialog, Next enabled — then completes with 677 `failed`. That is precisely the F-W1-6 dishonesty, relocated. | Name the mechanism: the census must publish the `barcode_conflict` row count as part of the preview payload AND that count must be subtracted from / displayed beside `summary.valid_rows`, with a test asserting `preview.barcode_conflict_rows === jobData.failed_rows` after execution on the fixture. Coordinate with K-8-FR2 (same file) — say who owns the counter. |
| **K11-S2-10** | MAJOR | brief §4/§6; `2026_06_02_100003_create_product_variants_table.php:37-45` (all partial uniques inside `if (driver === 'pgsql')`); `2026_08_30_100000_enforce_company_scoped_product_skus.php:54-58` (`$table->unique()`, **not** partial) | **Partial unique = raw SQL, and the only precedent is PG-only.** `$table->unique()` cannot express a `WHERE` predicate. If K-11 copies the variants shape the index exists on PG only, so §6's "PG + SQLite BY PATH" leg proves nothing about the constraint — a SQLite-masks-PG hazard the conventions call out by name. Separately, `unique(company_id, sku)` is **not** partial (it covers trashed rows, which is why `ProductIdentityFailure::SkuHeldByDeletedProduct` exists at `ProductResolver.php:72-84`), so a `deleted_at IS NULL` barcode predicate is a deliberate asymmetry: a soft-deleted product's barcode becomes reusable. | Decide and write down: PG-only or PG+SQLite (SQLite does support partial unique indexes). State the soft-delete asymmetry as intentional and consistent with the resolver's `! trashed()` barcode filter (`:88-91`). If PG-only, say the constraint test `markTestSkipped`s on SQLite and the SQLite leg covers only the guard logic. |
| **K11-S2-11** | MAJOR (carries K11-S1-10) | `apps/web/src/features/import/types.ts:78`; `ImportWizardPage.tsx:1020-1025`, `:1027-1035`; `packages/shared/types/generated.d.ts` | **Unaddressed in v2.** If `multi_location` / `barcode_conflict` become census counts, the hand-rolled `DuplicateBucket` union at `types.ts:78`, the hardcoded six-bucket array in a `sm:grid-cols-6` grid, and the three-bucket headline sum all drift silently. Verified all three still in their r1 state on `dev`. | Add to §5: `types.ts:78` aliases the generated `DuplicateBucket`; the bucket list is derived, or all three sites change together; a preview-panel test renders the new counts. |
| **K11-S2-12** | MAJOR | brief §1 ("rows are **excluded from execution** with row outcome `failed`"); `ImportService.php:583-596` (the only writer of `import_error` / `import_error_code`); `ResultWorkbookService.php:21-31` | **"Excluded from execution" and "outcome `failed`" are mutually exclusive as written.** A row never processed stays `Pending`; the only path that stamps `import_error` (and therefore the only path that puts the row on the workbook's **Rejected** sheet) is the `catch` in `processPendingRow`. | Specify the mechanism: the conflict is raised as a `CodedImportRowException(ImportErrorCode::BarcodeConflict, …)` **inside** `processPendingRow`, before the policy branch, so the existing catch stamps `outcome=failed` + `import_error_code` + `import_error_detail` and the workbook files it under Rejected with its reason. |
| **K11-S2-13** | MINOR (carries K11-S1-13) | `ImportType.php:202-229`; `2025_11_30_052910_create_products_table.php:27` | `barcode` still has **no** import validation rule — not even `max:100` against `varchar(100)`. Note the API side already has it (`CreateProductRequest.php:216`, `UpdateProductRequest.php:200`), so the import is the only unguarded door. | Add `'barcode' => ['nullable','string','max:100']`, plus the cheap Excel-float-corruption shape warning (cell containing `.` or `E`/`e`) — the observed root cause of this very file. |
| **K11-S2-14** | MINOR (carries K11-S1-14) | brief (benchmark section absent); `docs/conventions/10-BENCHMARK-FIRST-SPECS.md` | v2 **removed** the benchmark note rather than replacing it with the required baseline table. | Add the three-column table (guarantee · AutoERP today `path:line` · decision) with rows for barcode uniqueness scope, duplicate-barcode-at-import behaviour, multi-line/multi-location import, per-row feedback, re-import idempotency. Mark the competitor column unverified-from-code. |
| **K11-S2-15** | MINOR (carries K11-S1-09/15) | brief §1/§6 | No timing assertion on the 859-row fixture (`decide()`'s existing per-row re-scan at `DuplicateCensusService.php:144-178` is untouched and F-W1-5 already fataled a worker on this file), and the 179-vs-180 arithmetic is still unexplained. | Restore both: an 859-row timing/query-count assertion, and an explicit expected-count arithmetic line in the fixture test. |

---

## Conditions for merge (ordered)

- **C1 (K11-S2-01, pre-dispatch)** Correct the census-then-refuse precedent claim; specify the log-and-skip contract, the promotion path, and both migration legs as tests.
- **C2 (K11-S2-02, pre-dispatch)** State that stock-per-line for a confirmed multi-location group is **new work under `duplicate_policy = skip`**; name the mechanism; add the `skip` × 2-location test asserting quantities at both locations.
- **C3 (K11-S2-12)** `barcode_conflict` is raised as a `CodedImportRowException` inside `processPendingRow`, before the policy branch.
- **C4 (K11-S2-05)** Split the warning vs error vocabularies; list the correct files per vocabulary; require `typescript:transform`.
- **C5 (K11-S2-07)** Write guard excludes self and tolerates pre-existing duplicates; "edit an existing twin, save succeeds" test; residual for censused twins recorded.
- **C6 (K11-S2-08)** Holder lookup and conflict payload are company-scoped; second-company test asserts no cross-company id.
- **C7 (K11-S2-09)** Named preview↔execution counter mechanism, with the `preview conflict count === failed rows` test; K-8-FR2 ownership stated.
- **C8 (K11-S2-10)** PG-only vs PG+SQLite decided in writing; soft-delete asymmetry declared.
- **C9 (K11-S2-03 + K11-S2-06)** `barcode_conflict` name collision resolved; `docs/glossary.md:36` added to the deliverables.
- **C10 (K11-S2-11 + K11-S1-11)** `types.ts:78` aliases the generated union; bucket sites updated together with a panel test; the blank-SKU in-file barcode path (`DuplicateCensusService.php:340-342`) named as must-stay-green.
- **C11 (K11-S2-04)** Confirm surface named concretely (`ConfirmDialog` + new state, or persisted job option).
- **C12 (K11-S2-13/14/15)** `barcode` validation rule + corrupted-cell warning; benchmark baseline table; 859-row timing assertion; 179/180 arithmetic.

## Owner items

- **OR-4 (new)** `product_variants.barcode` is tenant-wide unique by RUL-2 (`docs/glossary.md:36`, `2026_06_02_100003_create_product_variants_table.php:40-41`) while ruling 1 makes `products.barcode` company-unique. Confirm the asymmetry is intended, or lane a follow-up to converge the two scopes.
- **OR-5 (new)** Remediation for tenants whose census reports existing barcode twins: is a repair tool in scope for K-11, a follow-up lane, or accepted as a manual staging fix? (Their products are otherwise unsaveable/unimportable until repaired.)

**VERDICT: spec ❌ as written + quality ACCEPT-WITH-CONDITIONS** — dispatch permitted once C1 and C2 land as brief text; C3–C12 are honourable inside the lane.

One-line fix before merge: correct the two false code claims (the precedent throws; multi-location stock-per-line is not free under `skip`) and pin the conflict as a `CodedImportRowException` with company-scoped holder data.
