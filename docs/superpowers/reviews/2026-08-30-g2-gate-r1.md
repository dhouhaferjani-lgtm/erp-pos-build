# Lane G-2 adversarial gate r1 — imports + inventory costing

**Worktree:** `.worktrees/g2-sku` (`feat/g2-sku-generation`)  
**Review base:** `70968fbf29fe9abaf554227f678abc8c762b71cf` (the worktree `HEAD`; local `dev` advanced after the lane was cut)  
**Verdict:** **CHANGES**

The happy path, allocator locking, migration, UI optionality, docs, translations, manifest and focused suites are green. The lane is not releasable because “blank” is not normalized consistently, G-4's obsolete derived-SKU refusal remains executable, a concurrent supplied-SKU import can expose raw PostgreSQL text, and the required second-of-everything proof is absent.

## Findings

| ID | severity | file:line | finding | change |
|---|---|---|---|---|
| G2-R1-01 | BLOCKER | `apps/api/app/Modules/Import/Services/SpreadsheetParserService.php:82`; `apps/api/app/Modules/Product/Application/Services/ProductService.php:109`; `apps/api/app/Modules/Product/Application/Services/ProductService.php:238`; `apps/api/app/Modules/Product/Application/Services/ProductService.php:467` | A whitespace-only CSV SKU is a blank cell to the resolver (`blankToNull()` trims), but not to `ProductService::emptyToNull()`, which recognizes only `''`/`null`. CSV values retain spaces. On a create miss, `'   '` therefore takes the supplied-SKU branch and is persisted verbatim instead of allocating `SKU-000001`. This directly breaks R2 and answers STOP-6's final question: **yes, an import path can still write a non-generated SKU for a semantically blank cell**. Existing tests use only `,,`, so all green runs miss it. | Make blank detection trim-aware at the Product service boundary while preserving non-blank supplied values. Add CSV/XLSX HTTP tests with whitespace-only SKU cells asserting a generated SKU, `sku_generated` detail, no row error, and no whitespace SKU at rest. |
| G2-R1-02 | BLOCKER | `apps/api/app/Modules/Product/Application/Services/ProductResolver.php:33`; `apps/api/app/Modules/Product/Application/Services/ProductResolver.php:122`; `apps/api/app/Modules/Product/Application/Services/ProductResolver.php:217`; `apps/api/app/Modules/Import/Services/ImportService.php:841`; `apps/api/tests/Feature/Product/ProductUpsertKeyPrecedenceTest.php:196` | STOP-6 remains functionally open. `ProductResolver::createSku()` still derives barcode/name/`PRODUCT` for a blank SKU and uses that fabricated value to find a deleted holder. Import then turns the result into `sku_held_by_deleted_product`, even though G-2 promises a blank SKU is generated regardless of other file content and never becomes a row error. The retained barcode-derived and name-derived refusal tests positively pin the obsolete behavior. No ordinary empty-string create writes the fallback—the failure occurs before allocation—but valid blank rows are still refused. | In the G-4-owned resolver, build the SKU read-set and deleted-holder refusal only from an actually supplied SKU. Let `SkuAllocator` skip real generated candidates with `withTrashed()`. Amend the two stale derived-SKU refusal tests and add blank+barcode, blank+name and CJK/`PRODUCT` deleted-holder regressions through upload→execute. |
| G2-R1-03 | MAJOR | `apps/api/app/Modules/Import/Services/DuplicateCensusService.php:74`; `apps/api/app/Modules/Import/Services/ImportService.php:539`; `apps/api/app/Modules/Import/Services/ImportService.php:924`; `apps/api/tests/Feature/Import/BlankSkuGenerationImportTest.php:76` | Task 4.2 requires the same-name in-file merge to carry `matched_by_name`. It does not. Census records `matched_by_name` only for `ExistingName`; the earlier in-file loser returns before `importProduct()`, while the winner is a create miss, so neither row reaches the only `MatchedByName` emission. The lane test asserts only product count, sequence state and `in_file`; Playwright likewise checks only the bucket and `sku_generated`. | Persist/emit the accepted name-match explanation on the in-file duplicate decision (without allocating for the loser), and assert the exact row warning plus one generated warning in PHPUnit and Playwright. Coordinate this G-4-owned change under STOP-6. |
| G2-R1-04 | BLOCKER | `apps/api/app/Modules/Product/Application/Services/ProductService.php:241`; `apps/api/app/Modules/Import/Services/ImportService.php:586`; `apps/api/app/Modules/Import/Services/ResultWorkbookService.php:129` | STOP-3 is hit on a concurrent **supplied** SKU. Two import jobs can both resolve a miss; the losing supplied-SKU `Product::create()` has no named-constraint catch. Its `QueryException` reaches `processPendingRow()`, which stores `getMessage()` in `import_rows.import_error`; the result workbook prefixes and exposes that raw message. PostgreSQL's `SQLSTATE[23505]` and constraint text can therefore reach the operator. Generated-SKU collisions are contained correctly; supplied-SKU races are not. | Catch only `ProductIndexNames::COMPANY_SKU_UNIQUE` on the supplied insert and translate it to a safe coded duplicate-SKU domain/import error (never reallocate a supplied value). Add a two-connection PG test and assert API/workbook/error-line output excludes `23505`, `SQLSTATE` and raw constraint text. |
| G2-R1-05 | MAJOR | `apps/api/tests/Feature/Product/SkuGenerationTest.php:168`; `apps/api/tests/Feature/Import/BlankSkuGenerationImportTest.php:36`; `apps/api/tests/Feature/Import/ProductSkuCompanyScopeImportTest.php:91`; `apps/web/e2e-local/sku-generation.spec.ts:104` | Rule 22's second-of-everything proof is incomplete. The allocator unit test creates two companies with factories, not the real company-creation path; the new import suite has one company and no location; the existing G-3a second-company/second-location interaction uses supplied SKUs; Playwright uses one company and its product SQL is not company-filtered. Re-run/idempotency is covered, but blank-SKU generation is not proven through a real second company or selected second location. The convention classifies a missing leg as MAJOR. | Add a journey that creates company B through the real path, provisions its units, imports blank SKUs in A and B, and proves each sequence starts at `SKU-000001` with company-filtered reads. Add a second `pos_enabled` location blank-SKU opening import and assert the generated product's stock lands only at the selected location; repeat the file and assert no new product, allocation or doubled stock. Extend Playwright or a by-path HTTP test with company-scoped DB predicates. |
| G2-R1-06 | MAJOR | `apps/web/src/components/organisms/AddQuickProductModal/AddQuickProductModal.tsx:38`; `apps/web/src/components/organisms/AddQuickProductModal/AddQuickProductModal.tsx:191`; `packages/shared/types/generated.d.ts:2213` | The edited quick-create surface still violates rule 19 and one-surface-per-concept. It declares a hand-rolled `Product` with numeric money beside generated `ProductData`, then converts `sale_price` with `parseFloat` and posts a number (also numeric `cost_price: 0`). Touched-file ESLint reports `precision/no-parsefloat-on-money`; the new SKU test inspects only `payload.sku`, so it blesses the float payload by omission. | Replace the hand-rolled entity with the generated DTO (or a `Pick` of it), keep money payload values as canonical decimal strings, and assert exact payload types/values in the blank- and supplied-SKU modal tests. Keep tax/percent values as strings as well. |

## Acceptance-criterion audit

| contract area | evidence | result |
|---|---|---|
| Preconditions G-3a/G-4 | `ProductIndexNames::COMPANY_SKU_UNIQUE` at `app/Modules/Product/Domain/ProductIndexNames.php:11`; resolver seam at `ProductResolver.php:19` | PASS |
| M5 exact schema, guard, forward-only output | `database/migrations/tenant/2026_08_30_100500_create_sku_sequences_table.php:12-39`; migration test at `SkuGenerationTest.php:245` | PASS |
| Per-company allocator, first-use insert, row lock, growing padding, deleted-holder skip, five-attempt bound | `SkuAllocator.php:14-92`; PG race at `SkuGenerationTest.php:110`; company/padding/holder tests at `:168`, `:180`, `:200`, `:217` | PASS for canonical empty input; see G2-R1-01/02 |
| Named-constraint SAVEPOINT retry for generated insert | `ProductService.php:384-406`; controller parity at `ProductController.php:451-497`; PG tests at `SkuGenerationTest.php:31` and `:68` | PASS |
| Blank import rows, sequence values and warning details | `ImportService.php:918-923`; `BlankSkuGenerationImportTest.php:61-107` | PASS for `''`; FAIL for whitespace and deleted-fallback adversaries |
| Same-name one-product/one-allocation merge | `BlankSkuGenerationImportTest.php:76-85` | PARTIAL: data merge passes; required `matched_by_name` visibility fails |
| Re-import allocates nothing | `BlankSkuGenerationImportTest.php:87-98`; `ProductUpsertKeyPrecedenceTest.php:98-116`; Playwright `sku-generation.spec.ts:132-140` | PASS |
| CJK and supplied SKU preservation | `BlankSkuGenerationImportTest.php:100-107`; Playwright `sku-generation.spec.ts:142-148` | PASS for tested values |
| API create nullable SKU, generated response, supplied lifetime uniqueness | `CreateProductRequest.php:178-185`; `ProductController.php:462-481`; `CreateProductTest.php:89-119`, `:318-355` | PASS |
| FE optional SKU and server allocation | `ProductGeneralSection.tsx:100-112`; `ProductForm.tsx:730-735`; `AddQuickProductModal.tsx:183-196` | SKU behavior PASS; precision/type contract FAIL (G2-R1-06) |
| i18n en/fr/ar | `locales/en/inventory.json:134-136`; `locales/fr/inventory.json:134-136`; `locales/ar/inventory.json:417-420` | PASS |
| Docs drift/warning code | `docs/modules/imports.md:159-194`, `:261-268` | PASS |
| One allocator surface / generated types | allocator callers use `SkuAllocatorInterface`; transform produced no generated-type diff | PARTIAL: allocator surface PASS; modal hand-roll FAIL |
| Rule 19 import/upsert strings | `NumericFieldNormalizer.php:7-20`; targeted round-trip suites | PASS on import path; touched quick-create path FAIL |
| Document-per-action / inventory costing | no new direct stock writer; opening remains `OpeningBalancePostingService` at `ProductController.php:562-595` | PASS |
| Manifest/CI | manifest `1236 → 1237`, Product `58 → 59`, Import `38 → 39`; one pre-existing headroom slot; anchored filter at `.github/workflows/ci.yml:1118-1119` | PASS |

## STOP adjudication

- **STOP-1:** not hit; both prerequisites exist on base.
- **STOP-2:** adjudicated as designed. No `companies.sku_prefix`/settings JSON exists; `SkuAllocator::DEFAULT_PREFIX = 'SKU-'` is sufficient before tenant #1. `sku_sequences.prefix` is already per company, so no settings surface is required for this lane.
- **STOP-3:** **hit** by G2-R1-04.
- **STOP-4:** no float enters the import `upsert` path; its DTO/data values remain strings. The FE quick-create path independently violates rule 19 (G2-R1-06).
- **STOP-5:** not hit; deptrac reports the standing 183 violations/0 errors and no touched-path edge.
- **STOP-6:** **hit**. The G-4 deleted-holder fallback must change to satisfy R2. Additionally, G2-R1-01 proves a whitespace-blank import can actually persist a non-generated SKU.

## Verification outputs

- Summary requirement: present and includes the required verbatim failed local Playwright transcript. Per orchestrator ruling, that sandbox launch failure is environmental, not a finding. Independent log: `1 passed (43.8s)`.
- Playwright spec audit: it asserts DB/API meaning for generated sequence values, one-product name merge, unchanged `next_value` on re-import, supplied SKU and CJK. It does **not** prove a second company and does not company-filter product queries. Lane Vite `:5174` was down during this gate (`:8011` API still answered), so no reviewer rerun was possible.
- SQLite scoped paths (lane changed/new + G-4 interaction set): **97 tests, 612 assertions, 3 expected PG-only skips**.
- PostgreSQL scoped paths with `DB_DATABASE=autoerp_test_g2 DB_CENTRAL_DATABASE=autoerp_test_g2`: **97 tests, 633 assertions**.
- G-3a second-company/second-location interaction, SQLite and PG: **2 tests, 25 assertions** on each.
- Pint touched PHP: `pass`. PHPStan touched PHP: `[OK] No errors`.
- Deptrac: **183 standing violations, 0 errors**; no touched Product/Import/allocator edge.
- Manifest checker: **1500 Feature classes / 74 groups**, pass; parked ceiling **1237**. CI filter is anchored and each new class occurs once.
- Web focused Vitest: **24 files / 114 tests passed**. Typecheck exit 0. TanStack key audit: 0 new/stale. i18n audit: pass.
- Touched-file ESLint: **0 errors, 17 warnings**; two precision warnings are at `AddQuickProductModal.tsx:191,193` and support G2-R1-06.
- React Doctor: isolated-cache run produced no output for about one minute and was terminated (environmental diagnostic limitation; deterministic web gates above completed).
- `CACHE_STORE=array php artisan typescript:transform`: regenerated declarations with **no git diff** in `packages/shared/types`.
- `git diff --check`: pass. No `apps/api/autoerp_test_*` artifact. Ignored `apps/web/e2e-local/`, `vite.local.config.ts`, and `test-results-local/` were excluded from source review.

## VERDICT: CHANGES

Do not merge until G2-R1-01 through G2-R1-06 are addressed and both database legs plus the company/location/re-run journey are green.
