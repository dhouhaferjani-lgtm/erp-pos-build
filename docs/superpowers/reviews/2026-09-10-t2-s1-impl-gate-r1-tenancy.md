# T-2 S1 receipt spine — implementation gate r1 — tenancy-authz-reviewer

Reviewed: lane/t2-receipt-spine @ c30dcdf9c (diff de3007fe9..c30dcdf9c), plan rev 8, spec rev 11, handback HANDBACK-T2-receipt-spine-2026-09-09.md
Date: 2026-09-10
Reviewer: Claude Opus `tenancy-authz-reviewer`, read-only. Owns the PostgreSQL runner for this lane (one file per invocation, serially, never the full suite). All `path:line` refs are at c30dcdf9c under `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/t2-receipt-spine/`. Overlaps with the inventory-costing and stock-gl registers are cited, not restated.

## Verdict
VERDICT: CHANGES-REQUIRED
BLOCKER=2 MAJOR=1 MINOR=5

## Required citations (plan §7.15 tenancy list, rev 2 + rev 3 additions)

| # | Requirement | Status | Evidence `path:line` |
|---|---|---|---|
| 1 | Two new permissions with their EXACT seeded roles vs §7.6 | VERIFIED | Definitions: `apps/api/database/seeders/RolesAndPermissionsSeeder.php:201` (`inventory.transfers.reconcile`) and `:202` (`inventory.transfers.close`), inside `permissionNames()` beside the existing transfer block `:197-200`. Grant: exactly ONE role map line changed — `:604`, the `manager` map, which now ends `…, 'inventory.transfers.reconcile', 'inventory.transfers.close',`. `admin` receives them through `rolePermissionGrants()['admin'] = self::permissionNames()` `:584` and `createRoles()`'s `$roleName === 'admin' ? Permission::all() : $permissions` `:570`. **No other role**: `grep -rn "transfers.reconcile\|transfers.close" apps/api/database/seeders/` returns only `:201`, `:202`, `:604`. Generated map agrees: `apps/web/src/hooks/permissionsMap.generated.ts:119` `'inventory.transfers.close': ['admin','manager']`, `:122` `'inventory.transfers.reconcile': ['admin','manager']`. Matches §7.6 ("Both are added to the `manager` map … reach `admin` through `permissionNames()` … No other seeded role receives either") exactly — no role beyond the plan |
| 2 | Route matrix; `receive` NOT under the any-of; `close` chains two `can:`; reconciliation read gate; every new route inherits the rule-12 group; no route escapes | VERIFIED | Group: `apps/api/app/Modules/Inventory/Presentation/routes.php:31` — `Route::prefix('api/v1')->middleware(['api', 'auth:sanctum', SetPermissionsTeam::class, EnforceTokenTenantClaim::class, 'module:Inventory'])->group(…)`. **Exactly one group in the file**: `grep -n "^Route::\|^});"` returns `31:Route::prefix(…)` and `405:});` and nothing else, so no route can escape it. New routes, all inside: `:115-117` `receive` → `can:inventory.transfers.complete` (single `can:`, NOT the any-of — as §7.6 requires); `:119-121` `close` → `->middleware(['can:inventory.transfers.reconcile', 'can:inventory.transfers.close'])` (two chained `can:`); `:123-125` `reconciliation` → `can:inventory.transfers.reconcile`. Widened reads: `:100` and `:108` → `require.any.permission:inventory.transfers.view,inventory.transfers.complete,inventory.transfers.reconcile`. Alias pre-exists and is unchanged by this diff: `apps/api/bootstrap/app.php:120` `'require.any.permission' => RequireAnyPermission::class` (`bootstrap/app.php` is not in the diff). `module:Inventory` matches the SoT: `apps/api/config/verticals.php:29,57,147,177,206,235,265,294` (`default_modules`) and `:75,:107` (`compatible_extras`). No new `module:` guard is introduced (`git diff … \| grep -E "^\+.*module:"` → empty) |
| 3 | Both document-identity uniques carry `company_id`; **no new `CATALOGUE_TABLES` / `EXCLUDED_TABLES` entry needed; ratchet unaffected** | **SPLIT: uniques VERIFIED; ratchet claim CONTRADICTED** | Uniques: `apps/api/database/migrations/tenant/2026_09_09_100100_create_stock_transfer_receipt_tables.php:101` `stock_transfer_receipts_company_number_unique ON (tenant_id, company_id, receipt_number)` and `:102` `stock_transfer_receipts_idempotency_unique ON (tenant_id, company_id, idempotency_key)` — both carry `company_id`; the numbering writer agrees (`StockTransferReceiptService.php:322` `generateForKey($transfer->tenant_id, $transfer->company_id, …)`) and the second-company test proves numbering restarts per company (`TransferReceiptSecondOfEverythingTest.php:52`). **But the ratchet claim is false.** `TenantOnlyUniqueIndexScanner::scanAll()` (`apps/api/tests/Architecture/Support/TenantOnlyUniqueUniqueIndexScanner.php` → actual path `apps/api/tests/Architecture/Support/TenantOnlyUniqueIndexScanner.php:52-62`) qualifies EVERY non-primary unique that is not a single `id`/`uuid` column and does not contain `company_id`; the migration adds four such: `:103` `(transfer_id, sequence)`, `:106` `(receipt_id, transfer_line_id)`, `:108` `(receipt_line_id, batch_allocation_id)` and the six single-column movement partials `:113-118`. `every_table_with_a_qualifying_unique_is_explicitly_classified` (`TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:199`, asserting at `:269`) therefore requires the three new tables in `CATALOGUE_TABLES` `:31-54` or `EXCLUDED_TABLES` `:56-127`; `grep -rn "stock_transfer_receipt" apps/api/tests/Architecture/` returns **nothing**, and the diff touches no file under `tests/Architecture/` except the new remainder ratchet and the DPA baseline. **Re-run red on PostgreSQL — see T-2** |
| 4 | Genericised `RequireAnyPermission` message leaks nothing | VERIFIED | `apps/api/app/Http/Middleware/RequireAnyPermission.php:25` — the 403 body is `{error:{code:'FORBIDDEN', message:'You do not have permission to perform this action.'}, meta:{timestamp, request_id}}`. `git diff de3007fe9..c30dcdf9c -- apps/api/app/Http/Middleware/RequireAnyPermission.php` = exactly one line changed (`'…to view transaction destinations.'` → the static sentence). No permission name, no role name, no id, no transfer field; `request_id` is echoed from the caller's own `X-Request-ID` header (`:29`). Nothing else in the file changed — the permission list arrives as variadic `string ...$permissions` `:15` and is never rendered. No stale assertion anywhere: `grep -rn "permission to view transaction destinations" apps/api apps/web packages` → empty, so the other consumer's tests (`apps/api/app/Modules/Company/routes.php:99`) cannot break on the literal |
| 5 | Convention 09: second-company assertions prove isolation on DATA; second location; re-run/idempotency | VERIFIED | `apps/api/tests/Feature/Inventory/TransferReceiptSecondOfEverythingTest.php:41` second company (own currency `TND`), `:44-45` second source + destination, `:46` second product, `:53/:56/:61/:65` re-runs. Data-meaning assertions, not status codes: `:74` company A destination `StockLevel` still `7.0000`; `:75` company A `stock_transfer_receipts` rows byte-identical JSON; `:76` company A source `StockLevel` unchanged; `:77` `journal_entries where company_id = A` count `=== 0`. Idempotency key space per company proven by construction: company B posts `receipt-one` (already consumed in company A) and gets **201**, `:53`, then the replay at `:56` returns 200 + `meta.replayed` |
| 6 | (rev 2, gate r1 B1) manifest carries the exact §6.7.3 numbers; four §6.7.4 statements, no observed-CI claim; all fourteen names in the `ci.yml:1142` alternation; `feature-lane-manifest-check.php` exits 0 | VERIFIED (numbers/names/checker) — but see T-1 for what the alternation now does | Against §6.7.3's table (`plan-rev-8.md:857-861`), not the handback: `apps/api/tests/feature-lane-manifest.json` `gated_ceiling` `1253 → 1267`, `Inventory` `129 → 141` (+12), `Replenishment` `8 → 9` (+1), `Migrations` `14 → 15` (+1) — all four exact. Four raise notes written, none edited: `groups.Inventory.raise_note_2026_09_09_t2t3_s1`, `groups.Migrations.raise_note_2026_09_09_t2t3_s1`, `groups.Replenishment.raise_note_2026_09_09_t2t3_s1`, top-level `gated_ceiling_raise_note_2026_09_09_t2t3_s1`; each carries (1) `from -> to` + class names, (2) the PARKED-behind-`vars.SELF_HOSTED_RUNNER_READY` consequence, (3) the allowlist statement + the verbatim `NOT on push->dev` caveat, (4) "Remove the allowlist entries and lower this ceiling when the lane's gate is flipped, not before" + the handback path + **"No observed CI run is claimed."** — and no observed-CI claim anywhere. Fourteen names present in the `.github/workflows/ci.yml:1145` alternation (the alternation moved from `:1142` to `:1145` because of the inserted comment block — T-1): `StockTransferReceiveTest`, `StockTransferReceiveDamageTest`, `StockTransferReceiveLotsTest`, `StockTransferReceiveValidationTest`, `StockTransferCloseTest`, `StockTransferReceiveConcurrencyPostgresTest`, `TransferReceiptEventStreamTest`, `TransferReceiptGlBoundaryTest`, `TransferReceiptPatternQueryPostgresTest`, `TransferLegacyCompletionBackfillTest`, `TransferReceiptSecondOfEverythingTest`, `TransferReceiptAuthorityGateTest`, `TransferCloseReplenishmentSettlementTest`, `TransferReceiptSchemaRerunPostgresTest`. Checker run on the slice tip: `php tools/feature-lane-manifest-check.php` → `tests/Feature lane manifest OK — 1532 Feature classes in 74 groups … every --filter entry is anchored and uniquely matched`, **exit 0** |
| 7 | (rev 3, gate r2 m1) the three raise notes name only their own group's PG-only classes | VERIFIED | `Inventory` note names two wholly-PG classes — `StockTransferReceiveConcurrencyPostgresTest`, `TransferReceiptPatternQueryPostgresTest` — plus the cross-lane `TransferLegacyCompletionBackfillTest` with its single PG-only method, exactly as §6.7.4's template (`plan-rev-8.md:889-913`) prescribes, and mentions `TransferReceiptSchemaRerunPostgresTest` **only** in the plan's own mandated disclaimer sentence ("is NOT an Inventory class: it is in the Migrations group and is named in that group's own raise note", template line `plan-rev-8.md:908-909`). `Migrations` note claims exactly its own class (`pg_constraint` / `pg_indexes` / `information_schema`). `Replenishment` note claims none ("It is NOT PostgreSQL-only: it runs on both lanes"). Empirically corroborated: on PG, `TransferLegacyCompletionBackfillTest`'s only skip is `test_capture_the_pre_change_reader_baseline` ("capture-only; set T2T3_CAPTURE_BASELINE=1 on the PRE-CHANGE tree", `:121-124`), i.e. `test_interrupted_backfill_rolls_back_and_the_next_run_completes` (`:230-233`) DID execute on PG — the note's claim is true in both directions |
| 8 | (rev 3, gate r2 M1) `return_to_source` source-location check via `canAccessLocationOrFail()` BEFORE the service; destination check on `receive`; scope from membership, never from request input | VERIFIED | `apps/api/app/Modules/Inventory/Presentation/Controllers/StockTransferController.php:325-330` — destination check first (`:328`), then `if ($request->input('disposition') === 'return_to_source' && ($denied = $this->canAccessLocationOrFail($model->source_location_id, …)) !== null) return $denied;` at `:331`, and only then `$this->receiptService->close(...)` at `:334`. `receive` checks the destination at `:304-306` before `:308`. The single decision point is `canAccessLocationOrFail` `:358-361`, which delegates to `$this->locationContext->canAccessLocation(...)`; visibility uses `canSeeTransfer` `:376-386` → `getAllowedLocationIds($companyId, $user)` (`null` = unrestricted). **Scope never comes from input**: the location ids compared are `$model->destination_location_id` / `$model->source_location_id` read off the DB row; the only request-derived value in the decision is the `disposition` string, which selects *whether* to add the stricter source check. Line ids in the payload are resolved from the loaded aggregate and re-checked: `StockTransferReceiptService.php:196-199` — `$transfer->lines->firstWhere('id', $input['transfer_line_id'])` plus an explicit `$line->company_id !== $transfer->company_id \|\| $line->tenant_id !== $transfer->tenant_id` refusal (`Failure::LineNotOnTransfer`), and lots from `$line->batchAllocations` `:232` (`Failure::UnknownLot`) — so no payload field can reach another company's row. Deny paths pinned on DATA: `TransferReceiptAuthorityGateTest.php:37-46` (destination-only actor refused `return_to_source`, `denialSnapshot()` over six tables byte-identical) and `:49-57` (source-only actor refused `receive` and both closes, same snapshot) |
| 9 | (rev 3, gate r2 B1) the three §7.12b DTOs exist as PHP `#[TypeScript]` classes and as interfaces in `generated.d.ts`; a fresh `typescript:transform` is byte-identical | VERIFIED | `apps/api/app/Modules/Inventory/Application/DTOs/StockTransferData.php:32` `#[TypeScript]` → `packages/shared/types/generated.d.ts:1215`; `StockTransferLineData.php:20` → `:1264`; `StockTransferLineBatchAllocationData.php:21` → `:1250`. Receipt/close/reconciliation families also present (`:1283`, `:1296`, `:1314`, `:1337`, `:1354`, `:1371`, `:1401`). Regenerated by the reviewer: `php artisan typescript:transform` → `Transformed 576 PHP types to TypeScript`, then `diff` against the pre-run copy → **no output (byte-identical)**, and `git status --short` empty afterwards. Also pinned in-suite by `TransferReceiptSecondOfEverythingTest.php:21-28` (the three names must appear, and no `Array<any>` in four receipt shapes) |
| 10 | (rev 3, gate r2 M6) `PROMOTION-CHECKLIST-2026-08-26.md` carries exactly the one-line successor pointer and no other change; the new checklist carries all four spec §11.4 items; `permissionsMap.generated.ts` matches the seeder | VERIFIED | `git diff de3007fe9..c30dcdf9c -- docs/handoff/PROMOTION-CHECKLIST-2026-08-26.md` = `1 insertion(+), 0 deletions`, a single line under `## 0. Preconditions`: "Lane-scoped successor for the 2026-09-09 T-2/T-3 promotion: `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md`." — nothing else. `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` §1 tenant permission seeding (with the per-tenant `SELECT name FROM permissions …` → two rows verification and the "`tenants:seed` exits 0 regardless — gate on the printed per-tenant output" warning), §2 `permission:cache-reset` + a real-request re-verify, §3 `permissions:export-frontend-map` + `typescript:transform` with both drift gates cited, §4 the §3.1 manual post-commit drift recovery incl. the "NEVER `migrate:rollback` — `down()` DROPS POSTED DOCUMENTS" line and the per-tenant backfill census — all four spec §11.4 items present. Map/seeder agreement re-run on PG: `tests/Feature/Console/ExportFrontendPermissionsMapCommandTest.php` → OK (3 tests, 19 assertions); the committed map's source hash line moved to `sha256:8c56d87a…` in the same commit as the seeder edit |
| 11 | Tenant/company isolation of the receipt/close/reconciliation reads and writes; per-company idempotency key space; a `complete`-holder at company A cannot receive company B's transfer | VERIFIED (test, not probe) | Every HTTP entry re-scopes: `StockTransferController.php:302` (`receive`), `:328`-preceding `:326` (`close`), `:349` (`reconciliation`) each do `StockTransfer::query()->where('tenant_id', $company->tenant_id)->where('company_id', $company->id)->findOrFail($transfer)` behind a `Str::isUuid($transfer)` guard (`:300`, `:323`, `:346`) — so an invalid uuid is a 404, not a PostgreSQL 22P02. Idempotency lookup is company-scoped: `StockTransferReceiptService.php:154-155` `where('tenant_id', …)->where('company_id', …)->where('idempotency_key', $key)`, matching the `(tenant_id, company_id, idempotency_key)` index `2026_09_09_100100…:102` (§6.3 key space per company). `TransferReconciliationService::build()` takes an already-scoped model (`TransferReconciliationService.php:37`) and writes nothing (`grep -n 'receive(\|issue(\|glBuffer\|save()\|update('` → empty); its `PATTERN_DETECTION_SQL` `:19-33` filters `WHERE r.company_id = :company_id` `:32`. The cross-company probe is committed, not just structural: `TransferReceiptSecondOfEverythingTest.php:67-71` — a user who is `manager` at company B and holds complete/reconcile/close attacks company A's transfer id on **all five** routes (`/complete`, `GET /{id}`, `/receive`, `/close`, `/reconciliation`) and gets **404 on each**, followed by the four company-A data assertions `:74-77`. Cross-TENANT isolation is physical (db-per-tenant) and additionally predicated on `tenant_id` at every lookup |
| 12 | Rule 20 — no `CompanyContext` / team-id dependence in queued or replay paths | VERIFIED | The slice adds **no** queued job and **no** listener: `git diff … apps/api \| grep -E "^\+.*(onQueue\|ShouldQueue\|Event::listen)"` → empty; no `apps/api/app/Modules/*/Providers/` file is in the diff. Team context is read/written in exactly one production place, unchanged by this diff: `apps/api/app/Modules/Identity/Presentation/Middleware/SetPermissionsTeam.php:28` `setPermissionsTeamId($user->tenant_id)`. Every `setPermissionsTeamId` / `setCompanyId` occurrence in the diff is inside `tests/` or a test race-probe script (10 hits, all under `apps/api/tests/`) — no production line in the diff reads the team id. Scale resolution passes explicit currency (stock-gl register citation 10: `StockTransferReceiptService.php:480`, `StockTransferMovementSupport.php:59,90`), and the backfill migration takes company/tenant from the row, never from context |
| 13 | Hard scope §2.3 from the authz side; the S4-contended web files contain only what the plan allows | VERIFIED | The only web/generated paths in the diff are `apps/web/src/hooks/permissionsMap.generated.ts` (artisan-generated: source-hash line + the two new keys, nothing else), `apps/web/src/locales/{en,fr,ar}/inventory.json` (**one** key each — `stock_transfer_receipt`, the label for the new `StockMovementReferenceType` case) and `packages/shared/types/generated.d.ts` (artisan-generated, proven byte-reproducible above). No hand-written web file, no component, page, hook or route change. No receiver-view route (`routes.php:99-129` adds only `receive`, `close`, `reconciliation`), no `blind_receiving` / `visibility_version` column or DTO member, no notification listener, no masking helper, no new `SystemAccountPurpose`. `is_blind` is written constant `false` (`StockTransferReceiptService.php:325`) |
| 14 | (M-2 authz input) roles holding `complete`; cost of option (b) | PROVIDED | See "Input to the M-2 owner ruling" below — stated as input, not as a finding |

## Findings

### BLOCKER

**T-1 — The `backend-test-pgsql` `--filter` is no longer passed to the command: the slice inserted its comment block BETWEEN `php artisan test` and its line-continuation, so the step runs the ENTIRE PostgreSQL suite unfiltered and then dies on `--filter…: command not found`.**
What: `.github/workflows/ci.yml:1141` is `php artisan test -c phpunit-pgsql.xml \` and `:1145` is the `--filter='…'` continuation. This diff inserted three comment lines at `:1142-1144` between them (`git diff de3007fe9..c30dcdf9c -- .github/workflows/ci.yml` = `+3` comment lines above an otherwise unchanged `--filter` line). Under `bash`, `\`+newline is removed **before** tokenization, so the logical line becomes `php artisan test -c phpunit-pgsql.xml   # T-2/T-3 S1: fourteen named receipt classes…` — the `#` begins a word and therefore a comment, the filter is dropped, and `:1145` is parsed as a **new command**.
Why it matters: this is the ONLY live CI gate for all ~200 allowlisted classes while their feature lanes are parked — including all fourteen S1 classes, `StockTransferEdgeCasesTest`, `StockTransferCompleteConcurrencyPostgresTest`, and `TenantOnlyUniqueOnCatalogueTablesRatchetTest` (see T-2). The step now (a) invokes the full `phpunit-pgsql.xml` suite with no selection — the exact whole-suite run this repo's process forbids, on a CI runner, before any of the intended classes are named — and (b) exits non-zero at `:1145` under GitHub Actions' default `bash -eo pipefail`, so the job fails for a reason unrelated to any test. Plan §6.7.5 is explicit about the mechanics that were violated: "appending that slice's class names to the single alternation … and adding a comment block **above** `:1141`" (`plan-rev-8.md:961`). `php tools/feature-lane-manifest-check.php` cannot catch this — it text-matches class names in `ci.yml` and does not parse shell.
Failing scenario (empirically reproduced by the reviewer, byte-for-byte with the committed lines and a `php` stub):
```
$ bash --noprofile --norc sim2.sh
php CALLED WITH: artisan test -c phpunit-pgsql.xml        <-- NO --filter
sim2.sh: line 7: --filter=/\\(AaaTest|BbbTest)::/: No such file or directory
EXIT=1
```
Minimum fix: move the three comment lines `:1142-1144` to ABOVE `:1141` (i.e. into the existing comment block that ends at `:1140`), leaving `php artisan test -c phpunit-pgsql.xml \` immediately followed by `--filter='…'`. Then re-verify with `bash -n` plus a stubbed `php` that echoes its arguments, and record that check in the handback — a `--filter` that is not in the command's argv is not a gate.

**T-2 — Three new tables carry qualifying tenant-only uniques and are classified nowhere, so `TenantOnlyUniqueOnCatalogueTablesRatchetTest` — a convention-09 gate that IS in the allowlist — is RED on PostgreSQL at this pin.**
What: `TenantOnlyUniqueIndexScanner::scanAll()` (`apps/api/tests/Architecture/Support/TenantOnlyUniqueIndexScanner.php:52-62`) skips only primary keys, single-column `id`/`uuid` uniques, and indexes containing `company_id`. The new migration creates `stock_transfer_receipts_transfer_sequence_unique (transfer_id, sequence)` (`2026_09_09_100100_create_stock_transfer_receipt_tables.php:103`), `stock_transfer_receipt_lines_receipt_line_unique (receipt_id, transfer_line_id)` (`:106`), `stock_transfer_receipt_line_lots_line_allocation_unique (receipt_line_id, batch_allocation_id)` (`:108`) and six single-column movement-id partial uniques (`:113-118`) — all qualifying. `every_table_with_a_qualifying_unique_is_explicitly_classified` (`TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:199`, assertion at `:269`) then demands each table appear in `CATALOGUE_TABLES` (`:31-54`) or `EXCLUDED_TABLES` (`:56-127`). None does — and the diff touches no `tests/Architecture/` file except the new remainder ratchet and the DPA baseline (`git diff --stat … -- apps/api/tests/Architecture/` = 2 files). The sibling tables are already classified, which shows the shape that is owed: `:111` `'stock_transfer_line_batch_allocations' => 'Batch allocation is unique within one transfer line.'`, `:112` `'stock_transfer_lines' => 'Product keys are unique within one stock transfer.'`.
Why it matters: the class is named in the `ci.yml:1145` alternation, so on every `PR->dev` / `PR->main` / `push->main` run of `backend-test-pgsql` it executes and fails; combined with T-1 the whole PG job fails twice over. It is also exactly the convention-09 review question this gate owns — someone must state, in the repo, whether a receipt document's identity keys are company-owned catalogue keys or parent-scoped relationship keys. (They are parent-scoped: `transfer_id`, `receipt_id`, `receipt_line_id` and the movement ids all descend from a company-owned row, so no isolation defect exists — but that decision is currently unrecorded.) Plan §7.15's required citation asserts the opposite ("no new `CATALOGUE_TABLES` entry or `EXCLUDED_TABLES` waiver needed; `TenantOnlyUniqueOnCatalogueTablesRatchetTest` unaffected") — **CONTRADICTED**.
Failing scenario (reviewer re-run, exact output):
```
1) …TenantOnlyUniqueOnCatalogueTablesRatchetTest::every_table_with_a_qualifying_unique_is_explicitly_classified
classify table stock_transfer_receipt_line_lots: catalogue (company-owned) or excluded (tenant-global, say why)
classify table stock_transfer_receipt_lines: catalogue (company-owned) or excluded (tenant-global, say why)
classify table stock_transfer_receipts: catalogue (company-owned) or excluded (tenant-global, say why)
… TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:269
Tests: 3, Assertions: 134, Failures: 1.   (exit 1)
```
Minimum fix: three `EXCLUDED_TABLES` entries with the reviewed reason, in the sibling style — e.g. `'stock_transfer_receipts' => 'Sequence numbers are unique within one company-owned stock transfer; the number and idempotency keys already carry company_id.'`, `'stock_transfer_receipt_lines' => 'Transfer-line and movement links are unique within one receipt.'`, `'stock_transfer_receipt_line_lots' => 'Batch-allocation and movement links are unique within one receipt line.'`. No `waiver` is involved and neither `LEGACY_ENTRY_CEILING` (`:166`) nor `WAIVED_ENTRY_CEILING` (`:172`) needs raising — the baseline test `live_tenant_only_unique_indexes_match_the_reviewed_baseline` (`:157`) passed unchanged, because `scan()` restricts to `CATALOGUE_TABLES`. Then re-run this class on PG (it is now the reviewer-verified red proof).

### MAJOR

**T-3 — The read gate was WIDENED on two live routes and a new read route was added, but no test exercises the closed direction of either: nothing proves an actor holding none of the three permissions is refused `GET /stock-transfers` / `GET /{id}`, and nothing proves an actor lacking `inventory.transfers.reconcile` is refused `GET /{id}/reconciliation`.**
What: `routes.php:100` and `:108` changed from `can:inventory.transfers.view` to `require.any.permission:inventory.transfers.view,inventory.transfers.complete,inventory.transfers.reconcile`, and `:123-125` added `GET /{transfer}/reconciliation` behind `can:inventory.transfers.reconcile`. The allow direction is pinned — `TransferReceiptAuthorityGateTest.php:14-19` (a `complete`-only actor reaches list and show) and `StockTransferReceiveTest.php:202` (`GET …/reconciliation` → 200). The deny direction is not: `grep -rn "getJson('/api/v1/stock-transfers')" apps/api/tests` returns five call sites (`TransferReceiptAuthorityGateTest.php:17`, `StockTransferEdgeCasesTest.php:327`, `StockTransferLocationScopeTest.php:209,223`, `StockTransferRestrictedMembershipTest.php:100`) and **every one asserts 200 or a location-scope outcome**; the four 403s in `StockTransferLocationScopeTest.php:270,281,306,326` are `LOCATION_ACCESS_DENIED` on `/complete`, not permission denials; and the only `/reconciliation` assertions in the tree are the 200 above and the cross-company 404 (`TransferReceiptSecondOfEverythingTest.php:71`). `TransferReceiptAuthorityGateTest` does test the deny path for `receive` (`:31-35`, view-only actor) and for `close` (`:21-29`, each single permission alone) — the read gate is the one it skips.
Why it matters: widening an authorization gate is precisely the change whose closed direction must be pinned, and this one is unpinned in three ways that would all stay green: a typo in one of the three names in the alternation (the middleware silently treats an unknown ability as denied — `vendor/spatie/laravel-permission/src/Traits/HasPermissions.php:262-266` catches `PermissionDoesNotExist` and returns `false`, so a mistyped disjunct is invisible, never a 500); a fourth permission accidentally appended to the alternation; or `reconciliation` being re-pointed at a weaker gate. The reconciliation payload is the expected-vs-received variance surface that S2's blind receiving is built to withhold, so "who is refused" is the load-bearing half of its contract, and S2 inherits this gate.
Failing scenario: not a current defect — the code is right. I confirmed the enforcement by construction (alias `bootstrap/app.php:120`; middleware `RequireAnyPermission.php:15-31`; Spatie's unknown-ability path above) and by the green allow/deny cases that DO exist. What is missing is the pin.
Minimum fix (test-only, both files already in S1's scope): in `TransferReceiptAuthorityGateTest`, add (a) `$this->user->syncPermissions(['inventory.view']);` then `$this->getJson('/api/v1/stock-transfers')->assertForbidden();` + `$this->getJson('/api/v1/stock-transfers/'.$this->transfer->id)->assertForbidden();`, and (b) `$this->user->syncPermissions(['inventory.transfers.complete']);` then `$this->getJson('/api/v1/stock-transfers/'.$this->transfer->id.'/reconciliation')->assertForbidden();` — a `complete`-only receiver must reach the list but NOT the variance surface, which is the exact discrimination §7.6 relies on.

### MINOR

**T-4 — The frontend read guard was not widened with the backend, so the any-of gate's stated purpose is undelivered at this pin, and it is not disclosed as a pre-S4 residual.** §7.6's rationale for the widening is that "the complete-only receiver's problem was reachability of the read routes, which the any-of gate solves". On the web, it is not solved: `apps/web/src/routes/index.tsx:1405` (list) and `:1425` (detail) still render `<RequirePermission permission="inventory.transfers.view">`, so a `complete`-only receiver or a `reconcile`-only supervisor is still bounced from the only shipped surface. Plan §10.4 (`plan-rev-8.md:5544`) explicitly assigns the fix to S4 ("become `permissions={['inventory.transfers.view','inventory.transfers.complete','inventory.transfers.reconcile']}` … All three, plus `stock-transfers/new`, get `moduleKey="inventory"`"), so this is a plan-directed deferral, not drift — and the same §10.4 line records that these three routes carry **no** `moduleKey` today (`grep -rn 'moduleKey="Inventory"' apps/web/src` → empty), i.e. the rule-12 both-layer gating for the `Inventory` module (a `compatible_extras` module for two verticals, `config/verticals.php:75,107`) is backend-only on this feature — also pre-existing and also S4's. Fix: one line in `docs/handoff/PROMOTION-CHECKLIST-2026-09-09-t2t3.md` stating that between pushes 1 and 4 the widened backend read gate has no web counterpart (and that these routes are not module-gated on the FE), so nobody reads a 403 on the page as a backend regression.

**T-5 — The receipt service trusts its callers for company scope: no tenant/company predicate inside `transact()` or `lockTransfer()`.** `StockTransferReceiptService.php:98` resolves the identity row with a bare `StockTransfer::query()->findOrFail($transferId)`, and `StockTransferMovementSupport.php:39-42` locks with `StockTransfer::query()->with('lines.batchAllocations')->lockForUpdate()->findOrFail($transferId)` — neither predicates `tenant_id` or `company_id`. It is safe **today** because the only two callers scope first (`StockTransferController.php:302/326` and `StockTransferService.php:313`, whose sole caller is the controller at `:240`), and because everything derived downstream comes off the loaded row (`findReceipt` `:154-155`, numbering `:322`, the line/lot re-checks `:196-199`, `:232`). But S2/S4 add a notification writer and a receiver projection, and the first caller that resolves a transfer id from a payload, a job argument or a deep link will cross companies silently. Fix: add `->where('tenant_id', …)->where('company_id', …)` (or accept a scoped `StockTransfer` rather than a string id) at both sites, with the `Str::isUuid()` guard moved down beside them so the service is safe standalone.

**T-6 — The location-denial body echoes the transfer's `location_id` to an actor who was just refused, now on two more routes.** `StockTransferController.php:388-401` returns `{error:{code:'LOCATION_ACCESS_DENIED', message, details:{location_id, user_id}}}` with 403; `receive` (`:305`), `close` (`:329`, `:331`) and the pre-existing `complete`/`cancel` all use it. The `user_id` is the caller's own, but `location_id` is a company row the actor has just been told they may not act on — a restricted receiver learns the UUID of the source location of a transfer they cannot touch. The shape is pre-existing (unchanged in this diff) and the leak is small (an opaque id inside the same company), but the diff triples its reach, and the sibling read path deliberately prefers 404 over 403 (`reconciliation` `:350-352` → `abort(404)` via `canSeeTransfer`). Fix, if the orchestrator wants consistency: drop `details` from the 403 body, or align `receive`/`close` on the 404-hide semantics used by the read route. Otherwise record the asymmetry as intentional.

**T-7 — A stray 7 MB SQLite database named after the PostgreSQL test DB sits in the worktree.** `apps/api/autoerp_test_u` (7,016,448 bytes, `file` → `SQLite 3.x database`, mtime 2026-09-10 12:25) is invisible to `git status` only because `.gitignore:81` already carries the rule `apps/api/autoerp_test_*`. It can only be produced by running phpunit with `DB_DATABASE=autoerp_test_u` and **without** `-c phpunit-pgsql.xml` — i.e. a command that looks like a PG leg and executes on SQLite. It did not corrupt this gate (every PG row I re-ran matches the handback exactly, see below), and the pre-existing ignore rule shows the trap is known. Recorded so no future reviewer accepts a transcript as proof of driver: match the counts, or re-run.

**T-8 — Environmental: the lane's PostgreSQL is on port 5432, not 5433.** `autoerp_test_u` lives on the **native** PostgreSQL 16 (`lsof -nP -iTCP:5432` → `postgres … 127.0.0.1:5432 (LISTEN)`, user `houssamr`, trust auth, `autoerp_test_u` present in `pg_database`). Port 5433 is the `autoerp_postgres` Docker container, whose role set is `autoerp`/`autoerp_secret` — `role "houssamr" does not exist` there, so every command in this register's brief that pins 5433 fails auth. The handback's command (`docs/handoff/HANDBACK-T2-receipt-spine-2026-09-09.md:20`) omits `DB_PORT` entirely and therefore resolves to the config default 5432, which is why it worked for the implementer. Fix: state the port explicitly in the handback and in the next dispatch brief (`DB_PORT=5432`), since 5433 is the documented locaplex-contended port.

## Input to the M-2 owner ruling (roles holding `complete`; cost of option b)

**Who can mint `partially_received` via the API today.** `POST /stock-transfers/{id}/receive` is gated on `can:inventory.transfers.complete` (`routes.php:115-117`). That permission is held by exactly two seeded roles: `manager` (`RolesAndPermissionsSeeder.php:604`) and `admin` (via `permissionNames()` `:584` → `Permission::all()` `:570`); the generated map agrees (`apps/web/src/hooks/permissionsMap.generated.ts:120` → `['admin','manager']`). No other seeded role, plus any per-user direct grant (`givePermissionTo`) an operator has made. So after the S1 deploy + seeder run, **every admin and every manager** can post a partial receipt and strand a transfer in a status the shipped web can neither name nor terminate.

**Is option (b) — gating `/receive` + `/close` until S4 — a one-line route change? No.** Three things break:
1. **Withholding the permissions cannot do it.** `/receive` is gated on `complete`, which `manager` already holds independently of this lane; removing `reconcile`/`close` from `:604` gates only `/close`. The permission lever cannot reach `partially_received` at all.
2. **Removing or flag-gating the routes is 7 lines of `routes.php` (`:115-121`) but reds most of the slice.** Nine of the fourteen manifest classes drive the feature through those two HTTP routes — `StockTransferReceiveTest`, `StockTransferReceiveDamageTest`, `StockTransferReceiveLotsTest`, `StockTransferReceiveValidationTest` (23 cases), `StockTransferCloseTest`, `TransferReceiptGlBoundaryTest`, `TransferReceiptSecondOfEverythingTest`, `TransferReceiptAuthorityGateTest`, `TransferCloseReplenishmentSettlementTest` — all via the `TransferReceiptFeatureTestCase` helpers `receive()` / `close()` (`StockTransferReceiveTest.php:145`, `:150`, which `postJson` the two routes). Gating them therefore also requires either a test-only flag-on path or rewriting those classes against the service, i.e. a re-dispatch, not a fix round.
3. **A cheaper block on the actual dead end exists, and it is not an authz lever.** Only `partially_received` is a web dead end (`closed_with_writeoff` / `closed_returned` are terminal — the badge is cosmetically wrong, but `canBeCancelled()` already refuses and nothing is stranded in `CARRYING_STATUSES`). One guard in `StockTransferReceiptService::validateReceive` refusing a receive whose `received + damaged` is less than `$line->remainingQuantity()` (`:200-207` is where the comparison already lives, as the over-receipt check) keeps `/receive` live for full receipts and mints `partially_received` never — at the cost of one new `TransferReceiptFailureReason` and a flag to lift it in S4. That preserves the fourteen classes' HTTP surface and every green PG leg below.

From the authz side I have no objection to option (a) (the three-line web widening) and note that it does **not** fix reachability by itself, because the FE route guard still requires `inventory.transfers.view` (T-4) — a complete-only receiver would still never see the widened badge map. Options (a) and T-4's disclosure travel together.

## PostgreSQL runs

Database `autoerp_test_u` on the native PostgreSQL 16, `127.0.0.1:5432` (see T-8), one file per invocation, serially, exclusive (`pg_stat_activity` showed no session on `autoerp_test_u` before the run and before the exclusive-DB class). Command exactly as the handback specifies, from the worktree root:
`DB_DATABASE=autoerp_test_u DB_CENTRAL_DATABASE=autoerp_test_u DB_USERNAME=houssamr DB_HOST=127.0.0.1 DB_PASSWORD='' apps/api/vendor/bin/phpunit -c apps/api/phpunit-pgsql.xml FILE`

| File | Result | Matches handback? |
|---|---|---|
| `tests/Feature/Inventory/TransferReceiptAuthorityGateTest.php` | OK (5 tests, 15 assertions), exit 0 | YES — handback `:35` |
| `tests/Feature/Inventory/TransferReceiptSecondOfEverythingTest.php` | OK (2 tests, 30 assertions), exit 0 | YES — handback `:34` |
| `tests/Feature/Inventory/StockTransferReceiveConcurrencyPostgresTest.php` | OK (3 tests, 43 assertions), exit 0 | YES — handback `:29` |
| `tests/Feature/Inventory/StockTransferIdempotencyCollisionPostgresTest.php` | OK (2 tests, 13 assertions), exit 0 | YES — handback `:47` |
| `tests/Feature/Migrations/TransferReceiptSchemaRerunPostgresTest.php` | OK (2 tests, 11 assertions), exit 0 | YES — handback `:37` |
| `tests/Feature/Inventory/TransferReceiptEventStreamTest.php` | OK (3 tests, 38 assertions), exit 0 | YES — handback `:30` |
| `tests/Feature/Inventory/TransferReceiptPatternQueryPostgresTest.php` | OK (2 tests, 10 assertions), exit 0 | YES — handback `:32` |
| `tests/Feature/Inventory/TransferLegacyCompletionBackfillTest.php` | Tests: 7, Assertions: 39, Skipped: 1, exit 0. The single skip is `test_capture_the_pre_change_reader_baseline` ("capture-only; set `T2T3_CAPTURE_BASELINE=1` on the PRE-CHANGE tree") — the PG-only interruption case DID run | YES — handback `:33`, and it corroborates the manifest note (citation 7) |
| `tests/Feature/Replenishment/TransferCloseReplenishmentSettlementTest.php` | OK (1 test, 8 assertions), exit 0 | YES — handback `:36` |
| `tests/Feature/Inventory/StockTransferCompleteConcurrencyPostgresTest.php` (exclusive DB confirmed) | OK (2 tests, 20 assertions), exit 0 | YES — handback `:41` |
| `tests/Feature/Console/ExportFrontendPermissionsMapCommandTest.php` (reviewer addition, citation 10) | OK (3 tests, 19 assertions), exit 0 | n/a |
| **`tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php`** (reviewer addition, convention 09) | **FAIL — Tests: 3, Assertions: 134, Failures: 1, exit 1** (`every_table_with_a_qualifying_unique_is_explicitly_classified`, three unclassified tables) | **Not claimed by the handback — see T-2** |

No environmental failure occurred; nothing was skipped for driver reasons except the capture-only case named above.

## Static gates

| Command | Outcome |
|---|---|
| `php tools/feature-lane-manifest-check.php` (slice tip) | exit 0 — "lane manifest OK — 1532 Feature classes in 74 groups … every `--filter` entry is anchored and uniquely matched against 1944 test classes"; 70 parked groups / 1267 classes; 1 debt class |
| Manifest arithmetic vs plan §6.7.3 (`git show` both revs) | `gated_ceiling 1253→1267`, `Inventory 129→141`, `Replenishment 8→9`, `Migrations 14→15` — all four exact; four raise-note keys added, none edited |
| `php artisan typescript:transform` then `diff` vs the committed file | **byte-identical**; `git status --short` empty afterwards |
| Scoped PHPStan over all 49 changed files under `apps/api/app` | `[OK] No errors` (level 8, `phpstan.neon`) |
| `bash --noprofile --norc` replay of `ci.yml:1141-1145` with a `php` stub | `php CALLED WITH: artisan test -c phpunit-pgsql.xml` (no filter) then `--filter…: No such file or directory`, exit 1 → **T-1** |
| `grep -rn "permission to view transaction destinations" apps/api apps/web packages` | empty — no stale assertion on the replaced 403 literal |
| `git diff … -- .../PROMOTION-CHECKLIST-2026-08-26.md` | `1 insertion(+), 0 deletions` — the successor pointer only |
| `git diff … apps/api \| grep -E "^\+.*(onQueue\|ShouldQueue\|Event::listen)"` | empty — the slice adds no queued path (rule 20 vacuously satisfied; verified structurally) |

## Scope census (authz lens)

55 changed files under `apps/api/app` + `apps/api/database`; no `Providers/`, no `bootstrap/app.php`, no `config/` change — so **no new middleware alias, no new route file, no new event subscription, no module-gating change**. The only authorization-surface files are `app/Http/Middleware/RequireAnyPermission.php` (one message literal), `app/Modules/Inventory/Presentation/routes.php` (three added routes + two widened gates, all inside the single rule-12 group at `:31`), `database/seeders/RolesAndPermissionsSeeder.php` (two permission definitions + one manager grant line) and `app/Modules/Inventory/Presentation/Controllers/StockTransferController.php` (three actions + `canAccessLocationOrFail` / `receiptPayload` / `receiptFailureResponse`). All four migrations are in `database/migrations/tenant/`, the correct tree for tenant-scoped tables under db-per-tenant; each new table carries both `tenant_id` and `company_id` with a company FK (`2026_09_09_100100…:20,44,68` and `:130-131`, `:155-156`, `:180-181`). Web/generated surface: `permissionsMap.generated.ts` (artisan), `generated.d.ts` (artisan, byte-reproducible), three `inventory.json` locales with one key each — matching §2.3 and handback `:204`'s S4-contention note. Nothing from S2/S3/S4: no `blind_receiving`, no `visibility_version`, no receiver-view route, no notification listener, no masking helper.

## Tree state after review

```
$ git status --short
(empty)
$ git rev-parse --short HEAD
93b106461
```
No file was edited, committed, merged or pushed by this reviewer. The regenerated `packages/shared/types/generated.d.ts` came back byte-identical, so nothing needed restoring; the pre-existing gitignored `apps/api/autoerp_test_u` SQLite artifact (T-7) and `.phpunit.result.cache` are untouched by `git status` by design. As the other two registers note, no production file differs between `c30dcdf9c` and `93b106461` (evidence-only handback commit).

---

**What to fix before merge:** move the three `ci.yml` comment lines above `php artisan test` so the `--filter` is actually passed (T-1), classify the three new receipt tables in `EXCLUDED_TABLES` so the convention-09 ratchet goes green on PostgreSQL (T-2), and pin the closed direction of the widened read gate plus the reconciliation gate (T-3); then take the M-2 ruling with the option-(b) cost above, and add the T-4 disclosure line to the lane promotion checklist.

Orchestrator: session_01AcU81as26G2apodoimmyq7 (reviewer = Claude Opus `tenancy-authz-reviewer` agent, PG runner owner for this lane).

## Orchestrator rulings (session_01AcU81as26G2apodoimmyq7, 2026-09-10)
- **T-1: FIX** — the three comment lines move above `php artisan test -c phpunit-pgsql.xml \`; verify with a stubbed `php` echoing argv (record the check in the handback). Plan §6.7.5 wording ("comment block ABOVE :1141") stands; the manifest checker gains nothing (shell parsing is out of its scope) — ticket for a `bash -n` + argv-echo CI self-check is optional.
- **T-2: FIX** — three `EXCLUDED_TABLES` entries with the parent-scoped reasons given; no waiver, no ceiling raise; re-run the ratchet on PG. Plan §7.15 citation 3 is corrected in rev 9 ("parent-scoped uniques are classified in `EXCLUDED_TABLES`").
- **T-3: FIX** (test-only, as specified). **T-4: DISCLOSE** in the lane promotion checklist. **T-5: FIX** — scope predicates (`tenant_id`, `company_id`) inside `transact()` and `lockTransfer()` (or accept a scoped model); `Str::isUuid` guard beside them. **T-6: RECORD as intentional** for S1 (pre-existing shape shared with `/complete` and `/cancel`); S4 decides 403-details vs 404-hide for the receiver surface. **T-7: RECORD** in the handback (delete the stray SQLite file locally). **T-8: FIX** the handback command (`DB_PORT=5432` explicit).
- **M-2 (inventory) — orchestrator default pending the owner:** option **(a)** minimal web widening in S1 (generated union replaces the hand-rolled one; badge rows; i18n keys; `canComplete` accepts `partially_received`) **plus** the T-4 read-guard note; frontend-conventions-reviewer joins the S1 gate r2. The tenancy input's option (d) (refuse partial receipts until S4 via a validation guard) is recorded as the fallback if the owner prefers zero web change in S1.
