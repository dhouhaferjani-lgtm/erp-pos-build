# Gate r1 — W-LOT-A-1a (tenancy-authz-reviewer, 2026-09-10)

**Audited HEAD:** `04e60530c` on `lane/w-lot-a-1a`, worktree `/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/w-lot-a-1a`, merge base `4373ba2f6` (8 commits, 66 files). Authority: `/Users/houssamr/Projects/syneriva/apps/erp/docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-9.md` §§2/3/6/7/8/9/10/13/14/15. All citations re-derived at HEAD. PostgreSQL 16 on `autoerp_test_w` (port 5433, exclusive — `pg_stat_activity` showed only `iziposcentral_rfqe2e` and `postgres` before I started); a temporary gitignored `apps/api/.env` was created for the runs and removed afterwards (`git status` clean; no tracked file touched).

I did **not** find the "broad regression" the stalled attempt described as a route/middleware matrix regression — the create/update/transfer/write-off matrix is intact (§6.4, verified line by line). The broad regression is elsewhere and is **BLOCKER-2** below: an unconditional wire-contract change in `BatchResource` plus an unconditional eager-load narrowing in `getExpiringProducts()`, both of which fire with `LOT_ACTION_PERMISSIONS_ENFORCE=false` on every existing tenant at Push 3.

---

## BLOCKER

**B-1 — the lane's only concurrency proof is hard-wired to one reviewer's private database and hard-FAILS on the repo's default SQLite lane.**
`apps/api/tests/Feature/Identity/LotActionPermissionDeltaTest.php:32-33`:
```php
self::assertSame('pgsql', DB::getDriverName());
self::assertSame('autoerp_test_w', DB::connection()->getDatabaseName());
```
`race()` is the harness behind `LotActionPermissionDeltaTest::test_concurrent_first_apply_creates_one_marked_role` and `GeneralManagerAssignmentTest::test_concurrent_assignment_and_membership_narrowing_are_serialized` — i.e. the sole evidence that `pg_advisory_xact_lock` (`LotActionPermissionDelta.php:131-136`) and the `lockForUpdate()` chain in `RoleController::assignRole` (`apps/api/app/Modules/Identity/Presentation/Controllers/RoleController.php:369-375`) actually serialize two real workers.
Failure scenario: any run against a different database — CI `backend-test-pgsql`, another reviewer's `autoerp_test_x`, the shared `autoerp_test`, the parked `feature-lane-tenancy/Identity` once its gate flips — fails on line 33 for an environmental reason unrelated to the invariant. Verified empirically: `./vendor/bin/phpunit -c phpunit.xml tests/Feature/Identity/LotActionPermissionDeltaTest.php` → `Tests: 12, Assertions: 46, Failures: 1` with `Failed asserting that two strings are identical. -'pgsql' +'sqlite'` at line 32. That is a red test committed into the default lane, not a skip.
Minimum correction: replace both assertions with a driver guard that **skips** (`if (DB::getDriverName() !== 'pgsql') { self::markTestSkipped(...); }`) and drop the database-name assertion entirely, or assert only that the name is not the shared default (`autoerp_test`). No committed test may name a session-private DB.

**B-2 — Push 3 is NOT behaviourally inert: two batch-read wire contracts change for every tenant while the flag is false, with no covering test and a stale frontend type.**
Three independent, non-flag-gated changes:
1. `apps/api/app/Modules/BatchExpiry/Presentation/Resources/BatchResource.php:40-41` now calls `scopedTotalQuantity()`/`scopedAvailableQuantity()` (`:68-90`). Previously these were `$this->total_quantity ?? 0` / `$this->available_quantity ?? 0`, i.e. the accessors `Batch::getTotalQuantityAttribute()` / `getAvailableQuantityAttribute()` at `apps/api/app/Modules/BatchExpiry/Domain/Entities/Batch.php:143-151`, which issue a **fresh company-wide `SUM`** and return a `float`. The new methods return a 4-dp **string** summed over the **loaded** relation only, and return `"0.0000"` when `relationLoaded('batchStock')` is false — which is the case on `POST /api/v1/batches` (`BatchController.php:211` loads only `product`). There is no `enforced()` guard on any of this.
2. `apps/api/app/Modules/BatchExpiry/Domain/Services/FEFOInventoryService.php:859` newly filters the `batchStock` **eager load** by `$locationId`; previously it was `->with(['product.unitOfMeasure', 'batchStock'])`, unfiltered. `BatchController::expiring()` (`:282,287`) passes the raw request `location_id` string into that method **when the flag is false**. So flag-off `GET /api/v1/batches/expiring?location_id=X` now returns a different `batch_stock` array and different totals than before the merge.
3. `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:345` and `BatchResource.php:60` replace `$stock->available_quantity` with `bcsub(...)`, also unconditionally.

The hand-authored consumer type was not updated: `apps/web/src/features/batches/types.ts:38-39` and `:322-324` still declare `total_quantity: number` / `available_quantity: number` with the doc comment *"PHP float aggregates … therefore arrive as JSON numbers, not strings"*. `ExpiryWriteOffPage.tsx:129,140,155,245` uses `available_quantity` as the write-off `max` and the over-quantity validation bound; it degrades gracefully only because `formatQuantity` happens to accept `string | number` (`apps/web/src/lib/decimal.ts:206-214`) — that is luck, not contract.
This contradicts plan §6.1 ("While false: … Newly location-scoped endpoints retain pre-activation read behavior") and it is undetected because the only inertness test asserts nothing (see M-1).
Failure scenario: Push 3 lands on staging/prod with the flag false; every batch payload changes JSON type on two fields and `expiring?location_id=` silently narrows its per-location rows and totals, with the frontend type asserting the opposite. The flag cannot roll this back.
Minimum correction: either gate all three under `$this->activation->enforced()` (keeping the legacy accessor path while false), **or** get an explicit orchestrator ruling that the totals change is deliberate at Push 3, update `apps/web/src/features/batches/types.ts:38-39,281,322-324` to `string`, and add a real flag-off HTTP payload test (see M-1).

---

## MAJOR

**M-1 — the inertness guard asserts nothing.** `apps/api/tests/Feature/BatchExpiry/BatchActionPermissionsTest.php:41-46`, `test_flag_off_preserves_pre_activation_access_and_payloads()`, instantiates `BatchActionAccess` directly and asserts `'{"legacy":true}'` from a synthetic `$next`. It issues no HTTP request, hits no route, and compares no payload — despite its name. It is the sole test named for the §6.1 contract, and it is exactly why B-2 slipped through. Minimum correction: make it a real `getJson()` against `/batches`, `/batches/{uuid}`, `/batches/expiring?location_id=`, `/products/{id}/batch-stock` with `enforce => false` and assert the full pre-activation payload shape (JSON types included) against a committed expectation.

**M-2 — the `technician` role permanently loses the Batches page, and no push restores it.** `apps/web/src/routes/index.tsx:1205,1218,1231,1244` replaces `RequirePermission moduleKey="inventory"` with `permission="batches.view"`, and `apps/web/src/components/organisms/Sidebar/Sidebar.tsx:243` adds `permission: 'batches.view'`. All eight batch permissions are now in `SERVER_AUTHORITATIVE_PERMISSIONS` (`apps/web/src/hooks/usePermissions.ts:35-43`), so there is **no** role-map fallback — only the server's `getAllPermissions()` counts. Pre-delta grants (`git show 4373ba2f6:apps/web/src/hooks/permissionsMap.generated.ts:18,122`) show `inventory.view` held by `admin, cashier, manager, operator, technician, viewer` but `batches.view` held only by `admin, cashier, manager`. The Push-4 delta restores `viewer`, `operator` and `cashier` (`apps/api/database/seeders/RolesAndPermissionsSeeder.php:92-94`) but **not** `technician`, which holds `inventory.view` at `RolesAndPermissionsSeeder.php:839` and no `batches.*` grant anywhere (`:834-856`). Practical blast radius is limited because `BatchExpiry` is a pharmacy/parapharmacy module (`apps/api/config/verticals.php:60,355`) and `technician` is an automotive role — but the seeder writes every role into every tenant DB, so a pharmacy tenant that assigns `technician` sees a silent denial. Minimum correction: add `'technician'` to the `foreach (['cashier','viewer','operator'])` grant loop at `RolesAndPermissionsSeeder.php:92`, or record an explicit owner ruling that technicians lose lot visibility.

**M-3 — the generated Roles DTO leaks `any` (rule 3).** `packages/shared/types/generated.d.ts:1008-1017` emits `permissions: Array<any>` because `RoleData::$permissions` (`apps/api/app/Modules/Identity/Application/DTOs/RoleData.php:19`) is a bare `array` whose `@param list<string>` docblock the transformer does not read. `RolesPage.tsx:18` then aliases this DTO and does `setSelectedPermissions([...role.permissions])` (`:157`) with no type safety; `pnpm typecheck` passes vacuously (verified: exit 0). Minimum correction: annotate the property with `#[LiteralTypeScriptType('string[]')]` (or `TypeScriptType`) and regenerate.

**M-4 — `/api/v1/partners/{partnerId}/batch-history` 500s on a non-UUID path segment, and now also on a bad date.** `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchTraceabilityController.php:106-112` passes `$partnerId`, `$request->input('product_id')`, `date_from` and `date_to` straight through with **no validation**; `apps/api/app/Modules/Document/Infrastructure/BatchTraceability/DocumentBatchTraceReaderAdapter.php:40,44-46,49` binds them into `whereRaw('partner_id = ?')`, `whereRaw('document_date >= ?')` and `where('product_id', …)` on PostgreSQL `uuid`/`date` columns → `SQLSTATE 22P02` / `22007`, a 500 for any authenticated module user. The `partnerId` hole is pre-existing (`git show 4373ba2f6:…BatchTraceabilityController.php`, old `backwardTrace`), but this diff re-homed the whole query into a new shared adapter and validates location IDs (`:129-132`) while leaving these four unvalidated. Contrast `forwardTrace` (`:46`), which does guard with `Str::isUuid`. Minimum correction: `Str::isUuid($partnerId)` → 404, and a `$request->validate(['product_id' => 'sometimes|uuid', 'date_from' => 'sometimes|date', 'date_to' => 'sometimes|date'])`.

**M-5 — backward-trace `product_id` changes `null` → `""`.** `apps/api/app/Shared/Contracts/BatchTraceability/BackwardDocumentBatchTraceData.php` declares `public readonly string $productId` (non-nullable) and the adapter casts `(string) $line->product_id` (`DocumentBatchTraceReaderAdapter.php:56`). `document_lines.product_id` is nullable (`apps/api/database/migrations/tenant/2025_11_30_080001_create_document_lines_table.php:16`), and the pre-diff controller emitted `$line->product_id` verbatim, i.e. `null`. §7.3 requires "existing nullability … preserved" — this one is not. Minimum correction: make `$productId` `?string` and drop the cast.

---

## MINOR

- **N-1** `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:362` adds `'uuid'` to the `posAvailableBatches` `location_id` rule outside the flag. Safe direction (422 instead of a PG cast 500) but still a flag-off behaviour change; mention it in the Push-3 release note.
- **N-2** `LOT_ACTION_PERMISSIONS_ENFORCE` is absent from `apps/api/.env.example` (verified: 0 matches). The only definition is `apps/api/config/lot_action_permissions.php:5`. Operators outside the §11 Dokploy protocol have no discoverable switch.
- **N-3** `apps/api/database/seeders/RolesAndPermissionsSeeder.php:113-118` writes the `WLOTA1A-RESEED` marker via `Log::channel('stderr')->info()` on **every** run, including the flag-off legacy path (`reason=enforcement_off`). With `SYNC_PERMISSIONS_ON_BOOT=true` this is permanent per-boot stderr noise for the lifetime of the flag. Observed on every PG run above.
- **N-4** `RolesAndPermissionsSeeder::currentTenantId()` (`:96-104`) now throws `missing_tenant_context` when neither `tenant('id')` nor a team id is set. `database/seeders/DatabaseSeeder.php:76` and `ProductionSeeder.php:70` call this seeder; I could not construct a failing in-repo path (every test I ran sets the team), so I flag it as unverified rather than asserting a break — but a plain `php artisan db:seed` outside tenancy now hard-fails where it previously ran.
- **N-5** The new `BatchPermissions.test.tsx` run surfaces `Each child in a list should have a unique "key" prop. Check the render method of BatchListPage` — the conditional wrapping at `apps/web/src/features/batches/pages/BatchListPage.tsx:91-99` sits next to it. Cosmetic; confirm it is pre-existing.

---

## §14 tenancy citations

- **Per-phase `tenant-databases.tsv` from `getTenantKey()` + canonical `getDatabaseName()`, host-copied, one-to-one `fail_gate`** — plan §11.2 `docs/superpowers/plans/2026-09-06-WLOTA-1a-permissions-roles-web-rev-9.md:2457` and the producer at `:2785`, validated at `:2789-2790`, consumed at `:2661,2867`. Resolver verified at HEAD: `apps/api/app/Modules/Tenant/Domain/Tenant.php:264`; prefix/suffix contract `apps/api/config/tenancy.php:62-63`; central connection `apps/api/config/tenancy.php:50`. **Document-only (no code in this diff); accurate.**
- **Delta/reseed/census/migration/snapshot/backup loops preserve status + full output, aggregate raw markers, copy to host before validation** — plan `:2455` (single `fail_gate` definition), `:2468` (`fail_gate()` body), §§11.2/11.5/11.6/11.9/11.11. **Document-only; accurate.**
- **No Task-1 middleware on create/update/transfer/write-off** — `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:24,26,31,32` (bare), `:19-20,35-36` (unchanged `can:batches.write-off`); asserted by `apps/api/tests/Feature/BatchExpiry/BatchActionPermissionsTest.php:33-38`. **VERIFIED.**
- **Null-team marked roles rejected on PostgreSQL and SQLite** — PG CHECK `apps/api/database/migrations/tenant/2026_09_06_205000_add_provisioning_source_to_roles.php:34,39-49`; SQLite INSERT/UPDATE triggers `:51-62`; application-level guard `apps/api/app/Modules/Identity/Application/Services/LotActionPermissionDelta.php:61-63`. Green on both drivers (9/9 each). **VERIFIED.**
- **One marker per tenant team** — partial unique index `…add_provisioning_source_to_roles.php:64-75`; post-state check `LotActionPermissionDelta.php:191-194`. **VERIFIED.**
- **Name, permission and delete mutations rejected for the marked role** — `apps/api/app/Modules/Identity/Presentation/Controllers/RoleController.php:241-243` (update), `:285-287` (destroy), `:95-98` (`PROVISIONED_ROLE_READ_ONLY`/422). **VERIFIED** (`GeneralManagerRoleProtectionTest` 4/4).
- **Roles index/show through snake-case `RoleData`, exact wire keys, marker-derived `is_provisioned_read_only`** — `RoleController.php:81-89` (`roleData()`), `:175` (index), `:195` (show), `:71-76` (`isMarkedProvisionedRole`, marker-derived not name-derived); DTO `apps/api/app/Modules/Identity/Application/DTOs/RoleData.php:12-24`. Keys preserved (`permissions` moves Collection→`list<string>`, identical JSON). **VERIFIED** (see M-3 for the `any`).
- **`RoleIndexResponseContractTest::test_roles_index_wire_keys_are_unchanged_and_is_provisioned_read_only_is_marker_derived` on PG** — `apps/api/tests/Feature/Identity/RoleIndexResponseContractTest.php`; `OK (1 test, 12 assertions)` on `autoerp_test_w`. **VERIFIED.**
- **`Log::channel('stderr')->info()` as registration evidence, backed by `php://stderr`** — emitter `apps/api/database/seeders/RolesAndPermissionsSeeder.php:113-118`; channel `apps/api/config/logging.php:97-104`; `LotActionReseedMarkerTest` `OK (4 tests, 18 assertions)` on PG. **VERIFIED.**
- **Grants, role ID and pivot cardinality unchanged after rejected updates** — `apps/api/tests/Feature/Identity/LotActionPermissionDeltaTest.php:170,175` (`model_has_roles` equality), `:153-158` (`orderedPermissionSnapshot` over `permissions`/`roles`/`role_has_permissions`), `:195-199` (full snapshot equality after a FAILED outcome). **VERIFIED.**
- **Exact A-1b/A-1c boundary** — no hold schema, no request route, no release/reject: `routes.php` has no `batches.recall.request` endpoint; the permission is provisioned dormant at `RolesAndPermissionsSeeder.php:82,90`. **VERIFIED.**
- **Exact wrapper normalisation for string `'1'`** — `apps/api/app/Console/Commands/ApplyLotActionPermissionDelta.php:58-61`; `ApplyLotActionPermissionDeltaCommandTest` `OK (9 tests, 24 assertions)`. **VERIFIED.**
- **`markedTenantCarriesWlota1aDelta()` checks team/name/guard/marker BEFORE the destructive sync seam** — `RolesAndPermissionsSeeder.php:105-111` invoked at `:44` in `run()`, ahead of `createLegacyRoles()` (`:46`) whose `syncPermissions()` is at `:634`. **VERIFIED.**
- **`RolesAndPermissionsSeederMarkedTenantTest::test_flag_off_reseed_after_delta_preserves_canonical_grants_for_marked_tenant` and `…_after_rollback_preserves_post_activation_state` on PG** — `apps/api/tests/Feature/Identity/RolesAndPermissionsSeederMarkedTenantTest.php:15,27`; `OK (2 tests, 4 assertions)`; each assertion is a whole-table snapshot comparison over `permissions`/`roles`/`role_has_permissions` against real legacy-seeder output. **VERIFIED.**

Additional §0/§8.4 addendum checks (all **VERIFIED**): legacy predicate is exactly `(tenant_id IS NULL OR tenant_id = :tenantId)` at `LotActionPermissionDelta.php:114-116` with no ownership write; mixed NULL+scoped matches fail closed at `:123-125` (`ambiguous_legacy_role_collision`) with the throw inside `DB::transaction` (`:50`) so rollback precedes the `catch` at `:96`; unmarked `general_manager` collision at `:118-121` and `:145-147`; the marked role is created tenant-scoped with `provisioning_source` at `:154-155`; custom grants preserved via additive `givePermissionTo($missing)` at `:176-178` with `syncPermissions()` reserved for genuinely new roles (`:174-175`); an unmarked NULL-team legacy role still inserts (`LotActionPermissionDeltaTest::test_null_team_unmarked_general_manager_is_never_adopted:202-208`); two-team discrimination at `:230-232`.

---

## Manifest / CI raise needed

`php tools/feature-lane-manifest-check.php` → **FAILED**, exactly five lines:

| Manifest group | Current ceiling | Required ceiling | Δ | Classes this lane adds |
|---|---|---|---|---|
| `BatchExpiry` (`feature-lane-inventory/BatchExpiry`) | 17 | **21** | +4 | `BatchActionPermissionsTest`, `BatchReadLocationScopeTest`, `BatchExpiringLocationScopeTest`, `BatchTraceReaderContractTest` |
| `Console` (`feature-lane-data-console/Console`) | 16 | **18** | +2 | `ApplyLotActionPermissionDeltaCommandTest`, `LotActionReseedMarkerTest` |
| `Identity` (`feature-lane-tenancy/Identity`) | 32 | **39** | +7 | `GeneralManagerAssignmentTest`, `GeneralManagerAssignmentWriterCensusTest`, `GeneralManagerRoleProtectionTest`, `LotActionPermissionDeltaTest`, `LotActionSeededRoleMatrixTest`, `RoleIndexResponseContractTest`, `RolesAndPermissionsSeederMarkedTenantTest` |
| `Migrations` (`feature-lane-data-console/Migrations`) | 14 | **15** | +1 | `RoleProvisioningSourceSchemaTest` |
| aggregate `gated_ceiling` | 1250 | **1264** | +14 | (sum of the above) |

**PG-only new classes that should enter the live `backend-test-pgsql` `--filter` allowlist** (precedent `.github/workflows/ci.yml:1132-1142`), since all four owning lanes are parked and the filter is the only live gate:
- `LotActionPermissionDeltaTest` — **empirically PG-only**: fails on `phpunit.xml` (1 failure at line 32). Note this is B-1; fix the guard first, then allowlist.
- `GeneralManagerAssignmentTest` — uses the same `race()` harness (`GeneralManagerAssignmentTest.php:18`), therefore PG-only by construction.
- `RolesAndPermissionsSeederMarkedTenantTest`, `LotActionSeededRoleMatrixTest`, `GeneralManagerRoleProtectionTest`, `RoleIndexResponseContractTest`, `GeneralManagerAssignmentWriterCensusTest`, `LotActionReseedMarkerTest`, `ApplyLotActionPermissionDeltaCommandTest`, `BatchReadLocationScopeTest`, `BatchExpiringLocationScopeTest`, `BatchTraceReaderContractTest` — plan §13 assigns all of them to the PostgreSQL lane; I ran each on PG only and did not driver-test them, so allowlist them as PG-lane per the plan.
- **Not** PG-only, do not add: `BatchActionPermissionsTest` (verified green on SQLite, 6/6) and `RoleProvisioningSourceSchemaTest` (verified green on both drivers, 9/9 each — its SQLite leg is load-bearing for the trigger branch). `BatchTraceabilityModuleBoundaryTest` is a no-DB Architecture class and is outside the Feature manifest.

The orchestrator makes these raises at merge.

---

## Verified fine

- **Route/middleware matrix (§6.4)** — every gated route in `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:15,16,23,25,27,30,39,40,43,46,49` carries the intended `BatchActionAccess:<perm>`; `store`/`update`/`transfer`/`writeOff` (`:24,26,31,32`) and both `can:batches.write-off` routes (`:19-20,35-36`) are untouched. `BatchActionAccess.php:20-26` allow-lists exactly the four permissions and 403s anything else; flag-off returns `$next()` before evaluation.
- **Rule 12** — the single group at `routes.php:13` carries `['api','auth:sanctum',SetPermissionsTeam::class,EnforceTokenTenantClaim::class,'module:BatchExpiry']`. No new group, no missing element.
- **Module gating on both layers** — backend `module:BatchExpiry`; frontend `ModuleGuard module="BatchExpiry"` retained on all four routes (`apps/web/src/routes/index.tsx:1204,1217,1230,1243`) and `module: 'BatchExpiry'` on the nav child (`Sidebar.tsx:243`). `BatchExpiry` is a declared module in the SoT `apps/api/config/verticals.php:47,60,355`.
- **Tenancy of the new trace adapters** — `DocumentBatchTraceReaderAdapter.php:77-79` applies `tenant_id` **and** `company_id` before any line predicate (the pre-diff forward query at `4373ba2f6:…BatchTraceabilityController.php` had **neither** — this diff closes a cross-company read); `PosBatchTraceReaderAdapter.php:46-51` applies both on the receipt. Effective location = line first, document fallback (`:80-85`); null-location receipts excluded when restricted (`whereIn` on a nullable column). `$locationIds === []` returns `[]` at `:19,35,64` and `:17,33` — fail-closed, never an omitted predicate. No `tenant_id` column-scoping fallback is assumed anywhere; no cross-tenant join. No central-connection read or write is introduced.
- **`null` vs `[]` membership scope (§6.2)** — `BatchController::resolvedReadLocationIds:56-73` returns `null` only when nothing was requested **and** `LocationContext::getAllowedLocationIds()` is `null`; otherwise it goes through `LocationScopeResolver::resolve` (`apps/api/app/Modules/Company/Services/LocationScopeResolver.php:31-46`) with **no bypass permission**, which throws `AuthorizationException` on an out-of-scope request. `BatchRepository::scopeVisible:38-46` applies the predicate only when `$locationIds !== null` and never converts `[]` into "no filter". `findVisibleByUuid` guards with `Str::isUuid` at the call site (`BatchController.php:95`).
- **Delete/recall/deactivate** — `destroy` and `recall` reach `findBatchOrFail` (company-scoped, UUID-guarded, `BatchController.php:103-135`) with the new permission boundary in front; deny-path evidence snapshots seven tables in PK order and asserts strict equality after the 403 (`BatchActionPermissionsTest.php:64-79`) — a status code alone is not accepted.
- **Architecture boundary** — `BatchTraceabilityController` imports only `App\Shared\Contracts\BatchTraceability\*`; `tests/Architecture/BatchTraceabilityModuleBoundaryTest.php` green (1/1) on the no-DB lane.
- **Permission-catalogue sync** — every permission used by a `can:`/`BatchActionAccess` guard (`batches.view/traceability/delete/recall/write-off`) exists in the seeder catalogue; the two new names (`batches.recall.request`, `treasury.manage_all_locations`, `RolesAndPermissionsSeeder.php:82`) are granted to at least one role (`:90-91`) and are consumed by no route or bypass anywhere in `apps/` — genuinely dormant, no privesc. Existing tenants still need the Push-4 `permissions:apply-lot-action-delta --apply` re-sync before those names exist in their DB.
- **GM assignment guard** — `apps/api/app/Modules/Identity/Application/Services/GeneralManagerAssignmentGuard.php:36-51` requires an active membership in the current company **and** `allowed_location_ids === null` on *every* active membership, under `lockForUpdate()`; called pre- and post-assign in both `RoleController::assignRole` (`:374,377`) and `UserController::store` (`:262,264`), and as `assertLocationChangeAllowed` on `UserController::update` (`:391,412`) so a GM cannot be narrowed afterwards. Team id is set from `$actor->tenant_id` and restored in `finally` in all three writers.
- **Convention 09** — Task-1: `BatchReadLocationScopeTest.php:175-220` creates company B through the real `POST /api/v1/companies`, A1/A2 and B1/B2 through the real `POST /api/v1/locations` with `pos_enabled` on the second, restricts the actor to A2, proves A2 is the *selected* location by asserting its exact stock and trace IDs, asserts A1/B absence, then re-runs the create and asserts 422 + `meta.outcome=already_exists` + exactly one company-A lot + seven-table snapshot equality. Task-2: `GeneralManagerAssignmentTest.php:90-104` second company with B1 and POS-enabled B2, rejection caused by the B2-restricted membership. Re-run/idempotency for the delta writer: `LotActionPermissionDeltaTest.php:230-232` (`APPLIED` then `ALREADY_APPLIED`, second team unaffected).
- **Precision (rule 19)** — `BatchResource.php:68-90` and `:60` and `BatchController.php:345` use `bcadd`/`bcsub` at `QuantityScale::SCALE` on `decimal:4`-cast strings; no `(float)`, no `number_format`. The scoped total is asserted as the exact 4-dp string `'3.1234'` at `BatchReadLocationScopeTest.php:211`. `posAvailableBatches` keeps its `regex:/^\d{1,11}(\.\d{1,4})?$/` quantity ceiling and the `(string)` pass-through (`BatchController.php:370,382`). Frontend adds `formatQuantity` and removes a `toFixed` (`BatchListPage.tsx:219`); no new `parseFloat`/`Number` on qty. (The two `parseFloat` calls at `BatchDetailPage.tsx:279,301` are pre-existing and untouched.) **Note:** the *semantic* change of these totals is B-2, not a precision defect.
- **PostgreSQL traps** — no new `latestOfMany`/`ofMany`; UUID route params guarded at `BatchController.php:95,105,418` and `BatchTraceabilityController.php:46` (the gap is M-4). PHPStan level 8 clean on every changed backend path (`No errors`).
- **Ticket** — `docs/superpowers/tickets/2026-09-09-role-controller-team-scope-annotations.md` exists and is accurate: `RoleController` annotations at `:158,192,236,280` do claim a Spatie team global scope that plain `Role::query()`/`findOrFail()` does not acquire (only `Role::findByParam()` applies `(tenant_id IS NULL OR tenant_id = team)`); the ticket correctly scopes the fix out of this lane and correctly notes db-per-tenant is a separate boundary that does not substantiate the claims.
- **Glossary / one-surface** — `docs/glossary.md:21` adds **General manager** with table, canonical surface and synonym; `:90-91` names `LotActionPermissionDelta` the sole role-definition writer and Settings → Users the sole assignment surface, Settings → Roles read-only. `RolesPage.tsx:18` replaces the hand-rolled `interface Role` with the generated DTO — a duplicate FE type removed, not added.
- **Flag false everywhere** — `apps/api/config/lot_action_permissions.php:5` defaults false; no `.env*`, compose, CI workflow, entrypoint or Dockerfile sets it true; the only `true` settings are inside tests. `LotActionPermissionActivation.php:15` requires strict `=== true`.
- **Test runs (all on `autoerp_test_w`, one file at a time)** — `BatchActionPermissionsTest` 6/6 (22 assertions) · `BatchReadLocationScopeTest` 5/5 (37) · `BatchExpiringLocationScopeTest` 2/2 (6) · `BatchTraceReaderContractTest` 3/3 (23) · `BatchTraceabilityModuleBoundaryTest` (sqlite lane) 1/1 · `LotActionPermissionDeltaTest` 12/12 (152) · `RolesAndPermissionsSeederMarkedTenantTest` 2/2 (4) · `GeneralManagerAssignmentTest` 7/7 (130) · `GeneralManagerRoleProtectionTest` 4/4 (18) · `RoleIndexResponseContractTest` 1/1 (12) · `LotActionSeededRoleMatrixTest` 5/5 (38) · `GeneralManagerAssignmentWriterCensusTest` 2/2 (3305) · `LotActionReseedMarkerTest` 4/4 (18) · `ApplyLotActionPermissionDeltaCommandTest` 9/9 (24) · `RoleProvisioningSourceSchemaTest` 9/9 (43) on PG **and** 9/9 (43) on SQLite · `ExportFrontendPermissionsMapCommandTest` 3/3 (19). Web: the six §13 vitest files, **6 files / 87 tests passed**; `tsc --noEmit` exit 0. Only failure observed in the whole round: `LotActionPermissionDeltaTest` on the SQLite lane (B-1).

---

## Merge instructions

1. Fix **B-1** (driver skip, drop the `autoerp_test_w` assertion) and re-run `LotActionPermissionDeltaTest` + `GeneralManagerAssignmentTest` on PG **and** on `phpunit.xml` to prove the skip path.
2. Resolve **B-2**: either flag-gate `BatchResource::scopedTotalQuantity()/scopedAvailableQuantity()`, the `getExpiringProducts()` eager-load filter and the `bcsub` per-row change, **or** obtain the orchestrator ruling, update `apps/web/src/features/batches/types.ts:38-39,281,322-324` to `string`, and rewrite `test_flag_off_preserves_pre_activation_access_and_payloads` (**M-1**) into a real HTTP payload comparison covering `/batches`, `/batches/{uuid}`, `/batches/expiring?location_id=`, `/products/{id}/batch-stock` and `POST /batches`.
3. Close **M-2** (grant `technician` `batches.view` or record the ruling), **M-3** (`LiteralTypeScriptType('string[]')` + regenerate), **M-4** (UUID/date validation on `backwardTrace`), **M-5** (`?string $productId`).
4. Only then apply the four group ceilings + the aggregate raise, and add the PG-only classes to the `backend-test-pgsql` filter per the table above.
5. **Do not merge this as one auto-deploying push to `origin/dev`.** §10 sequences the web bundle as Push 5, *after* the Push-4 delta grants `batches.view` to `viewer`/`operator`/`cashier`. A single combined promotion inverts that order and denies those roles the Batches page and sidebar entry until the delta runs (`routes/index.tsx:1205,1218,1231,1244` + `usePermissions.ts:35-43` leave no role-map fallback). Split the promotion, or run Push 4 before the web bundle serves.
6. Re-gate after the fixes; **B-1** and **B-2** both need re-verification with fresh runs.

VERDICT: CHANGES-REQUIRED
