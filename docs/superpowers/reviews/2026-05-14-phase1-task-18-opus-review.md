# Task 18 — Adversarial Code Review (Opus)

**Branch:** `feat/pos-fiscal-event-engine-phase1`
**Commit:** `1151ca94e` (HEAD)
**Worktree:** `/Users/houssamr/Projects/syneriva/apps/erp.fiscal-phase1`
**Reviewer:** Opus 4.7 (adversarial)
**Date:** 2026-05-16

---

## Verdict: **APPROVE**

Zero BLOCKERs, zero P1s, zero P2s. Three minor P3 doc-nits (none load-bearing).
Spec §5.0 + §7.3 + §17.3 + SoT §13.6/D16 contracts are met end-to-end; phpstan level 8 clean; 9/9 tests green; boundary imports clean (no Treasury / Accounting / Sales operational code reachable from the registry); singleton wiring shape is correct for Tasks 19/21/22 to consume; the canonical-PascalCase P1 lock survives via `test_canonical_pascalcase_token_required` + the per-event tenant/company lock survives via `test_resolver_called_with_event_tenant_and_company`.

This is the **second consecutive single-shot dual-APPROVE-eligible task** following Task 17. Phase 1 §7.3 projection seam is now buildable on the Task 18 contract.

---

## Findings

| # | Severity | Location | Finding | Recommended fix |
|---|----------|----------|---------|-----------------|
| F1 | CLEAN | `FiscalEventProjectionRegistry.php:39-79` | Boundary-clean implementation. The registry depends only on `FiscalEventProjector` + `ModuleActivationResolver` + the `FiscalEvent` model — never on Treasury / Accounting / Sales / Billing operational code. Verified by import-list grep (no Treasury class imports; the only "Treasury" tokens are documentation strings inside `*` comments). Satisfies spec §5.0 + §7.3 + SoT §13.6/D16. | — |
| F2 | CLEAN | `FiscalEventProjectionRegistry.php:62-78` | The two-gate filter matches the §7.3 algorithmic pseudocode exactly: `handlesEventType($event->event_type)` first (cheap projector-local check; resolver not consulted), then `requiresModule()` → `null` always passes, non-null calls `$this->resolver->isActive($module, $event->tenant_id, $event->company_id)`. No request-scoped tenant fallback (grep for `tenancy()`, `tenant()->`, `app(Tenant::class)` returns empty). The per-event tenant/company invariant is locked by `test_resolver_called_with_event_tenant_and_company` (`FiscalEventProjectionRegistryTest.php:112-139`). | — |
| F3 | CLEAN | `FiscalEventProjectionRegistry.php:42-57` | Singleton iterator hazard handled correctly. `app->tagged()` returns either `[]` (no tags — Phase 1 default) or a `RewindableGenerator` (after Tasks 21/22 tag projectors) — verified against `vendor/laravel/framework/.../Container.php:670-680`. The constructor's `is_array($projectors) ? array_values($projectors) : iterator_to_array($projectors, false)` handles both: the array branch preserves source ordering with reindexed keys (matters because the test injects `[new FakeTreasury, new FakePosCore]` and expects that order); the iterator branch materializes once with `preserve_keys=false`, yielding a `list<FiscalEventProjector>` as the PHPDoc promises. Materialize-once is the right choice for a singleton — re-iterating per call would still re-create the inner generator on a RewindableGenerator, but the singleton's process lifetime makes the captured list the source of truth either way, and freezing the registration set at boot is the more conservative semantic. | — |
| F4 | CLEAN | `FiscalEventProjectionRegistryTest.php:164-181` | Determinism lock present: `test_active_projectors_returned_in_registration_order` constructs `[FakeTreasury, FakePosCore]` (reverse of conventional happy-path order) and asserts the output is `['treasury_receipt_bridge', 'pos_core_receipt']`. This is the test that catches an accidental `sort()` / `usort()` / hash-bucket reordering. The conventional happy-path test (`test_pos_core_always_included_treasury_gated_when_active:41-54`) uses `assertEqualsCanonicalizing` — intentional, because the dedicated order test is the determinism authority. | — |
| F5 | CLEAN | `FiscalEventProjectionRegistryTest.php:86-101` | Canonical-PascalCase token lock present. `FakeLowercaseTreasury` returns `'treasury'`; the fake resolver reports only `'Treasury'` (PascalCase) active; the registry excludes the lowercase-requiring projector. This locks the spec §7.3 P1 contract that the registry does **not** normalize tokens and that the §17.3 test-lock applies at the registry boundary too (not only at the `ModuleActivationResolver` boundary, which has its own Task 17 lock). Wording on lines 90-94 is honest about which boundary this test asserts ("the registry honors the resolver's verdict — it does not normalize"). | — |
| F6 | CLEAN | `FiscalEventProjectionRegistryTest.php:141-162` | The resolver-call-suppression test (`test_resolver_not_called_for_always_active_projectors`) locks that `requiresModule() === null` skips the resolver entirely — defends against an accidental `$module ?? ''` plus an unconditional resolver call that would: (a) waste a config lookup per always-active projector, and (b) widen the contract to allow a resolver implementation that treats `''` as "any" (which `CompanyConfig::hasModule('')` would strict-`false` anyway, but the contract should never depend on that). The check uses a counter-tracking anonymous resolver — clean. | — |
| F7 | CLEAN | `FiscalEventProjectionRegistryTest.php:183-193` | Container-singleton resolution test (`test_resolves_from_container_singleton`) exercises the `FiscalServiceProvider` binding. With the tagged projector set empty (Phase 1 default), the registry resolves cleanly and yields `[]` from `activeProjectorsFor()`. This is the binding Task 19's `OutboxIngestor` will constructor-inject — Task 19's preflight is therefore green on this commit. | — |
| F8 | CLEAN | `FiscalServiceProvider.php:28-34` | Singleton binding shape is correct. `static fn (Application $app) ...` (no captured `$this`) avoids the rebinding-foot-gun. The closure resolves `$app->tagged(FiscalEventProjector::class)` lazily — important because the tag set is built up across **module** providers (POSServiceProvider in Task 21, TreasuryServiceProvider in Task 22), and those providers register **after** `FiscalServiceProvider::register()` per the per-task plan's incremental-wiring rule. The singleton is resolved on first `$app->make()` (in `OutboxIngestor`'s constructor or a test), at which point all `register()` calls have completed and the tagged set is correctly populated. (Phase 1: empty set; Phase 1 + Tasks 21/22: two real projectors.) | — |
| F9 | CLEAN | `FiscalEventProjector.php:10-80` | Interface docblock pins **all** load-bearing contracts: (a) canonical PascalCase token contract with the §7.3 reference (`requiresModule()` returns the exact `Vertical::defaultModules()` identifier, lines 29-34); (b) `(fiscal_event_id, projector_name)` idempotency contract referencing Task 9's `fiscal_event_projections` table (lines 36-39, 76-78); (c) per-event tenant/company gating ("against the EVENT's tenant/company — not request/user context", lines 25-27); (d) bounded-modules guardrail with SoT §13.6/D16 reference (lines 41-46). Every contract that Tasks 19/21/22/23/30 depend on is pinned in source rather than in spec-only prose. | — |
| F10 | CLEAN | `FiscalEventProjector.php:63-72` | `handlesEventType(FiscalEventType $type)` uses the enum type — not a string — so the wider-than-canonical-grammar code-smell (Task 16 4-BLOCKER class) is structurally unreachable on this seam. A projector cannot accidentally subscribe to a string that isn't a valid `FiscalEventType` case. | — |
| F11 | CLEAN | All Task 18 files | Standing code-smell scan — all clean: (1) no `(type) $array['key']` casts (no array indexing anywhere); (2) no wider-than-canonical contracts (registry trusts the projector's `requiresModule()` verbatim — does not normalize, which is correct per §7.3 ("if a future implementation needs case normalization it does so internally")); (3) no free-form string regex needed at the registry boundary — §7.3 explicitly assigns that responsibility to the projector + resolver, not the registry; (4) no `DTO::fromArray` (N/A); (5) no cross-tenant pollution — `$event->tenant_id` + `$event->company_id` are the **only** tenant sources; (6) singleton-iterator hazard handled; (7) never-throws — see F12. | — |
| F12 | CLEAN | `FiscalEventProjectionRegistry.php:62-78` | Never-throws standing pattern (Task 17 carry-forward) — registry calls `$this->resolver->isActive(...)` and Task 17's `DefaultModuleActivationResolver` pins "never throws" via its `try/catch (QueryException)` + null-tenant short-circuit (verified at `DefaultModuleActivationResolver.php:38-41,53-67`). So a malformed-UUID tenant on a fiscal event resolves to `isActive(...) → false`, the gated bridge is excluded, the ingest path proceeds — never crashes mid-projection-filter. The projector accessors `handlesEventType()` / `requiresModule()` will be trivial in Tasks 21/22 (production projectors) — see P3-3 below for a belt-and-braces note. | — |
| P3-1 | P3 | `FiscalEventProjector.php:31-34` | The interface docblock claims `test_canonical_pascalcase_token_required` "test-locks" the contract; technically what it locks is that the **registry** does not normalize tokens (the registry honors the resolver's verdict). The end-to-end "lowercase strict-compared false" lock lives in `ModuleActivationResolverTest`. Both locks together are what makes the contract durable, and the docblock cross-reference does correctly mention `ModuleActivationResolverTest (Task 17)` first — but a reader skimming the registry test alone might miss that the resolver test is doing the strict-compare proof. Wording polish only; not load-bearing. | Optional: tighten the docblock to "tested by `ModuleActivationResolverTest` (Task 17 — locks resolver strict-compare) + this registry's `test_canonical_pascalcase_token_required` (locks the registry does not normalize)". |
| P3-2 | P3 | `FiscalEventProjectionRegistry.php:44-49` | The constructor docblock notes "the iterable is captured once at construction (singleton lifetime)" but does not explicitly note the `RewindableGenerator` shape that `app->tagged()` returns. Future maintainers refactoring to "iterate the iterable per-call for hot-swappability" would need to understand that (a) singletons defeat hot-swap anyway, and (b) re-iterating a RewindableGenerator re-`make()`s the inner services on every call. Wording polish only. | Optional: add one line — "`app->tagged()` returns `[]` or a `RewindableGenerator` — both safe to `iterator_to_array` once; per-call re-iteration would re-`make()` each tagged service and defeat the singleton's purpose." |
| P3-3 | P3 | `FiscalEventProjector.php:55-72` | The interface does not explicitly pin "must not throw" on `name()` / `handlesEventType()` / `requiresModule()`. The Task 17 resolver pins never-throws on `isActive()`; the registry inherits that guarantee via the resolver edge. But projector accessors that throw would still surface as a 500 in the ingest path. Production projectors (Tasks 21/22) will be trivial fixed-string returns, so the risk is theoretical. Belt-and-braces defense-in-depth. | Optional: add to the interface docblock — "All three accessors (`name`, `handlesEventType`, `requiresModule`) must be **pure** and **must not throw** — they are called from the ingest hot path inside `OutboxIngestor.ingest()`'s transaction T1 (§7.2 Step 3); a thrown exception would roll back the fiscal-event insert. `apply()` may throw — failure semantics are owned by Task 23's `ApplyFiscalEventProjectionJob` retry contract (§7.5)." |

---

## Verified (grep + file checks)

| Check | Result |
|-------|--------|
| Test suite passes | `phpunit tests/Feature/Fiscal/FiscalEventProjectionRegistryTest.php` → 9 tests, 11 assertions, OK (5.6s) |
| PHPStan level 8 | `phpstan analyse --level=8` on all 4 Task 18 files → No errors |
| Boundary-clean imports | `grep "Treasury\|Accounting\|Sales\|Billing\|Inventory"` on registry + interface + provider → only doc-comment string mentions; no `use App\Modules\Treasury\...` or equivalent operational imports anywhere |
| No request-scoped tenant fallback | `grep "tenancy()\|tenant()->\|app(.*Tenant"` on registry + interface → empty |
| Provider wired in `bootstrap/providers.php` | `FiscalServiceProvider` registered at line 74 (existing — survives this commit's edits to `register()`) |
| Per-task ground rule §4.2 standing code-smells | None present (see F11) |
| Spec §7.3 algorithmic match | Registry algorithm (lines 62-78) matches §7.3 pseudocode (spec lines 399-405) exactly — `handlesEventType` first, then `requiresModule()` null-or-active gate, registration order preserved |
| §17.3 test-locks present | Canonical PascalCase token (`test_canonical_pascalcase_token_required:86-101`); resolver-reports-inactive (`test_treasury_bridge_excluded_when_resolver_reports_inactive:56-69`); per-event tenant/company (`test_resolver_called_with_event_tenant_and_company:112-139`); registration-order determinism (`test_active_projectors_returned_in_registration_order:164-181`); container singleton (`test_resolves_from_container_singleton:183-193`) |
| Task 19 preflight (binding shape) | Singleton resolves cleanly with empty tagged set (`test_resolves_from_container_singleton`); `OutboxIngestor` can constructor-inject `FiscalEventProjectionRegistry` and call `activeProjectorsFor($event)` to get `[]` in Phase 1 (zero projection rows ⇒ Task 19's "empty registry case" test maps cleanly) |
| Laravel `tagged()` return shape | Confirmed via `vendor/laravel/framework/.../Container.php:670-680`: returns `[]` when no tags, `RewindableGenerator` otherwise; constructor's array/iterator branch handles both |
| PHP covariance on fake projectors | PHP 8.4 — `?string` → `string` narrowing on `FakeTreasury::requiresModule()` + `FakeLowercaseTreasury::requiresModule()` is valid and idiomatic (phpstan green) |

---

## Spec-section coverage

| Spec section | Requirement | Implementation site | Test lock |
|---|---|---|---|
| §5.0 | "The engine, OutboxIngestor, and registry depend only on the `FiscalEventProjector` interface + the `ModuleActivationResolver` interface — never on Treasury, accounting, or sales code directly." | `FiscalEventProjectionRegistry.php:7-9` (only two non-domain imports) | Boundary import grep (Verified) |
| §7.3 | `FiscalEventProjector` interface — `name()`, `handlesEventType(type)`, `requiresModule(): ?string`, `apply(event)` | `FiscalEventProjector.php:48-80` | Interface present; existence verified by 9 fakes implementing it across the test file |
| §7.3 | `FiscalEventProjectionRegistry.activeProjectorsFor()` algorithm: `handlesEventType` filter → `requiresModule()` null = include → else `resolver.isActive(module, tenant_id, company_id)` | `FiscalEventProjectionRegistry.php:62-78` | `test_pos_core_always_included_treasury_gated_when_active`; `test_treasury_bridge_excluded_when_resolver_reports_inactive`; `test_only_projectors_handling_the_event_type_are_returned`; `test_resolver_not_called_for_always_active_projectors`; `test_resolver_called_with_event_tenant_and_company` |
| §7.3 | Canonical PascalCase module token contract — `requiresModule()` returns exact `Vertical::defaultModules()` token; lowercase strict-compared false | `FiscalEventProjector.php:29-34, 65-72` (interface docblock) | `test_canonical_pascalcase_token_required` (registry-side); `ModuleActivationResolverTest` (Task 17 — resolver-side strict-compare proof) |
| §7.3 | "the registry depends only on the `FiscalEventProjector` + `ModuleActivationResolver` interfaces — never on Treasury / accounting / sales code directly" | `FiscalEventProjectionRegistry.php:7-9` imports only `FiscalEvent` model + the two contract interfaces | Verified via grep (see above) |
| §7.3 (Phase 1 reality) | Empty tagged set is a valid Phase 1 state (Tasks 21/22 will add real projectors) | `FiscalServiceProvider.php:28-34` singleton binding + `FiscalEventProjectionRegistry.php:34-37` docblock | `test_empty_projector_set_yields_empty_active_list`; `test_resolves_from_container_singleton` |
| §7.3 (idempotency) | `apply()` idempotent on `(fiscal_event_id, projector_name)` via `fiscal_event_projections` (Task 9) | `FiscalEventProjector.php:36-39, 74-78` docblock | Locked at the interface contract level; runtime idempotency is Task 21/22/23's responsibility |
| §17.3 | "Canonical module-token contract (Opus v4 P1) — `isActive('Treasury', …)` returns true for a standard seeded tenant; lowercase 'treasury' fails" | `FiscalEventProjector.php:29-34` (contract pin) + `FakeLowercaseTreasury` test fixture | `test_canonical_pascalcase_token_required:86-101` (registry-side); end-to-end resolver lock in Task 17's test file |
| §17.3 | "`FiscalEventProjectionRegistry` — POS-core always included; Treasury bridge included only when resolver reports active; excluded when inactive" | `FiscalEventProjectionRegistry.php:62-78` algorithm | `test_pos_core_always_included_treasury_gated_when_active` + `test_treasury_bridge_excluded_when_resolver_reports_inactive` |
| SoT §13.6 / D16 | Bounded-modules asymmetric seam — pluggable projectors per `(tenant, company)`, bound only when active; engine + ingestion depend on no module's operational runtime | `FiscalEventProjector.php:41-46` docblock + `FiscalEventProjectionRegistry.php:28-33` docblock + zero Treasury imports | Verified — see F1, F2, F11 |
| Plan §1284 (Step 1 — failing test) | Three test cases: `test_pos_core_always_included_treasury_gated`, `test_treasury_bridge_excluded_when_resolver_reports_inactive`, `test_only_projectors_handling_the_event_type_are_returned` | All three present + named almost-verbatim (`test_pos_core_always_included_treasury_gated_when_active` is the only divergence — adds a clarifying suffix) | Tests pass |
| Plan §1284 (Step 3 — implementation) | `iterable<FiscalEventProjector>` + `ModuleActivationResolver` constructor; `activeProjectorsFor(FiscalEvent)` returns active subset | Constructor signature matches exactly (`FiscalEventProjectionRegistry.php:50-57`) | `test_resolves_from_container_singleton` exercises the container path |
| Plan §1284 (Step 3 — provider wiring) | "edit `FiscalServiceProvider::register()` to bind `FiscalEventProjectionRegistry` as a singleton constructed over `$this->app->tagged(FiscalEventProjector::class)` + the resolver" | `FiscalServiceProvider.php:28-34` matches exactly | Container singleton resolution verified by `test_resolves_from_container_singleton` |

---

## Notes for the next reviewer (Codex)

This is the second consecutive single-shot APPROVE-eligible task. The Codex 5-BLOCKER streak was paused at Task 17; please continue the pattern of running the standing-code-smell scan independently. The three P3 doc-nits above are entirely optional polish.

Specific areas a Codex pass might catch that this Opus pass might have missed:
1. A `RewindableGenerator`-specific edge case I haven't yet imagined under the empty-string-tag path (the `if (! isset($this->tags[$tag])) return [];` branch is hit in Phase 1 — verify the constructor's array branch handles `[]` cleanly: `is_array([]) → true` → `array_values([]) → []` → fine, confirmed by `test_resolves_from_container_singleton`).
2. Whether the `unset($tenantId, $companyId);` in `resolverReporting`'s anonymous class (test helper, line 214) is needed for PHPStan — yes, it's the standard "unused parameter" suppression pattern used elsewhere in the codebase.
3. Whether `FakeLowercaseTreasury` should *also* test the `assertEqualsCanonicalizing(['Treasury' => true, 'treasury' => false], ...)` scenario — i.e., what happens if a deployment's resolver had a case-insensitive aliasing layer. Per §7.3 ("if a future implementation needs case normalization it does so internally"), this is **out of scope** for Phase 1 — the contract says case normalization is implementation-internal, not registry-internal. The test as written is correct for the Phase 1 contract.

---

**End of Task 18 Opus review.**
