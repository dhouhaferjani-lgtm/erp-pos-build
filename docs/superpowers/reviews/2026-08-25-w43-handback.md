# W4-3 (P0) + W4-4 (P1) — handback

**Lane** `fix/campaign-w43-ap-opening-partner-ledger` · worktree
`/Users/houssamr/Projects/syneriva/apps/erp/.worktrees/w43-openings` · base `faf822f8a` (dev, N-6 Phase 1
merged) · **merged dev twice** (last at `2f239eaf4`, Session B Q-11) before the final commit.

Brief: `docs/sessions/session-A-2026-08-24/BRIEF-W4-3-ap-opening-partner-ledger.md`
Evidence: `docs/handoff/PLAYWRIGHT-first-tenant-campaign-wave4-critical-path-2026-08-24.md` §A.7/§A.8/§A.9,
§B.8/§B.9, §W4-3 (~887), §W4-4 (~919).

Gates requested: **treasury + imports (opening-balance lens)**.

---

## 1. What was wrong, in one paragraph

`ArApOpeningService::validateRow()` mapped the row's `document_type` to `DocumentType::Invoice` /
`CreditNote` for **both** the AR and the AP batch — only the *partner lookup* differed (`customers()` vs
`suppliers()`). An AP opening item was therefore a **customer invoice** on the shared `HIST-INV` sequence.
`PaymentController::store()` decides supplier-ness by `type === DocumentType::SupplierInvoice`, which such a
document can never satisfy, so paying the 500.000 TND owed to the supplier took the AR arm: `Dr 512 / Cr 411`,
repository movement direction **IN**, `receivable_balance = −500.000`, `GL 401` untouched, document marked
`paid`. Separately, the AR/AP batches posted **no GL at all** while the ACCOUNTING opening posted a lump
`Dr 411 / Cr 401` with `partner_id = NULL` on every line — so no opening balance could ever reach the partner
sub-ledger and the partner pages read `0.000` against real open items.

Both are now closed, plus the double-count trap the first fix would otherwise have opened.

---

## 2. Files changed

### Production

| File | What |
|---|---|
| `apps/api/app/Modules/Document/Application/Services/ArApOpeningService.php` | **:167-186** the `document_type` match is now side-aware (`$isAr ? Invoice : SupplierInvoice`, `$isAr ? CreditNote : SupplierCreditNote`) with the reasoning inline; **:494-503** `generateHistoricalDocumentNumber()` gains `HIST-SINV` / `HIST-SCN`; **:358-368** `postBatch()` posts the cutover journal entry per open item and collects the partner ids; **:383-389** refreshes the sub-ledger once per distinct partner; **:50** ctor takes `ArApOpeningLedgerService`; class + `postBatch` + `getPostPreview` docblocks and the operator-facing preview `note` (**:470**) rewritten — they still claimed "No GL entry will be created". |
| `apps/api/app/Modules/Accounting/Application/Services/ArApOpeningLedgerService.php` | **NEW, 283 lines.** `postOpeningEntry(Document, DateTimeInterface $cutoverDate, string $userId): ?JournalEntry` (**:99**) and `refreshPartnerBalances(string $companyId, array $partnerIds): void` (**:193**). Private `controlPurposeFor()` **:200**, `sourceTypeFor()` **:211**, `controlIsDebited()` **:224**, `moneyScale()` **:239**, `generateOpeningEntryNumber()` **:260**. |
| `apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php` | **:44-57** `CONTROL_ACCOUNT_BATCHES` const (declared :53) (keyed on `SystemAccountPurpose`, never on a code); **:204-224** `validateRow()` refuses a GL opening row on a partner control account with a message naming the batch to use instead. |
| `apps/api/app/Modules/Accounting/Application/Services/Reports/AgedPayablesService.php` | **:170-179** third arm added to `getOutstandingInvoices()`; **:181-226** new `historicalSupplierOpenItems()` (signature :205). |
| `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentController.php` | **:21** `Partner` import; **:467-475** the payment's partner loaded once (`Partner::query()` :471); **:499-532** `PAYMENT_DIRECTION_MISMATCH` (422, code at :518) before the supplier arm; **:1409-1433** private `documentSideMatchesPartner()` (signature :1426). |
| `apps/web/src/features/opening-balances/components/FileUpload.tsx` | **:52-67** `getTemplateContent()` — the GL opening sample CSV no longer shows `401000 Opening Payables` — it taught exactly the mistake the new guard refuses. |

### Tests

| File | Classes / notes |
|---|---|
| `apps/api/tests/Feature/Document/ArApOpeningLedgerTest.php` | **NEW**, 8 tests — the AR/AP opening arc. |
| `apps/api/tests/Feature/Treasury/OpeningItemPaymentDirectionTest.php` | **NEW**, 3 tests — the money end + both direction refusals. |
| `apps/api/tests/Feature/Accounting/OpeningControlAccountGuardTest.php` | **NEW**, 3 tests — the double-count guard, incl. a negative case proving ordinary accounts still pass. |
| `apps/api/tests/Feature/Document/ArApOpeningPostLifecycleTest.php` | **:132-159** fixture gains the 411/401 control accounts (the hand-built chart had only cash/bank/OBE). |
| `apps/api/tests/Feature/Import/PartiesImportBalancesTest.php` | **:68-72** seeds the chart of accounts; **:127-135** the supplier opening is now asserted as `SupplierInvoice` on `HIST-SINV` (this assertion previously *pinned the defect*); **:143-149** JSONB comparison made key-order-insensitive (PG-only, see §7). |
| `apps/api/tests/Feature/Treasury/PaymentTest.php` | **:719-732** mixed-allocation fixture partner corrected to `PartnerType::Both` (:731), which is what its own comment always claimed. |
| `apps/api/tests/feature-lane-manifest.json` | Document 82 → **83**, `gated_ceiling` 1167 → **1168**, with a named justification. |

---

## 3. Item → file:line

| Item | Where |
|---|---|
| W4-3 AP opening is a supplier-side document | `ArApOpeningService.php:182-183` |
| W4-3 `HIST-SINV` / `HIST-SCN` sequences | `ArApOpeningService.php:494-503` (`HIST-SINV` at :500) |
| W4-3 supplier payment takes the supplier arm | consequence of the type; verified end-to-end through `POST /payments` in `OpeningItemPaymentDirectionTest::test_paying_an_ap_opening_item_debits_the_payable_and_moves_the_cash_out` |
| W4-3 typed refusal, wrong direction | `PaymentController.php:499-532` + `:1409-1433`, code `PAYMENT_DIRECTION_MISMATCH` (:518) |
| W4-4 opening JE with the partner dimension | `ArApOpeningLedgerService.php:157-165` (control leg, partner-tagged) / `:167-175` (counterpart) |
| W4-4 `refreshPartnerBalance` after opening post | `ArApOpeningService.php:383-389` → `ArApOpeningLedgerService.php:193-199` → `PartnerBalanceService::refreshPartnerBalance()` |
| W4-4 aged AP lists HIST items | `AgedPayablesService.php:181-226` |
| W4-4 aged AR already listed HIST items, and now stops listing AP ones | `AgedReceivablesService.php:155` filters `type = Invoice`; the AP opening leaving that type is the fix. Before, aged AR grand total was **650.000** on a tenant owed 150.000 (proven in the red run, §6). |
| Double-count guard | `AccountingOpeningService.php:204-224` + const `:44-57` |

---

## 4. JE shapes

Dumped from the **throwaway PostgreSQL** database `autoerp_test_w43` (127.0.0.1:5433) by a scratch class that
drove the real services and the real `POST /payments` endpoint, then was deleted. Scenario: TN company, seeded
chart, AR opening 150.000 for a customer, AP opening 500.000 for a supplier, then the supplier paid in full
from a ledgered bank repository.

```
OB-2026-000001 | source_type=invoice           | posted | is_historical=t | Opening balance HIST-INV-2026-00001
    411   Clients                        Dr    150.000  Cr      0.000  partner=Nadia Chaabane
    119   Solde d'ouverture              Dr      0.000  Cr    150.000  partner=(none)

OB-2026-000002 | source_type=supplier_invoice  | posted | is_historical=t | Opening balance HIST-SINV-2026-00001
    401   Fournisseurs                   Dr      0.000  Cr    500.000  partner=Laboratoires Méditerranée SA
    119   Solde d'ouverture              Dr    500.000  Cr      0.000  partner=(none)

JE-2026-000001 | source_type=supplier_payment  | posted | is_historical=f | Supplier payment
    401   Fournisseurs                   Dr    500.000  Cr      0.000  partner=Laboratoires Méditerranée SA
    512   Banques                        Dr      0.000  Cr    500.000  partner=(none)
```

The campaign's inverted entry was `JE-2026-000007 (customer_payment) Dr 512 / Cr 411`. It is gone: the test
asserts `assertDatabaseMissing('journal_entries', ['source_type' => 'customer_payment'])`.

`119 Solde d'ouverture` is **resolved from `SystemAccountPurpose::OpeningBalanceEquity`**, never hardcoded —
it is `119` in Tunisia, `891 Bilan d'ouverture` in France, `1190` on the generic chart, and whatever the
country template assigned on a provisioned tenant. Same for `411` / `401`
(`CustomerReceivable` / `SupplierPayable`).

### Partner pages and repository, same run

```
DOC HIST-INV-2026-00001    invoice           posted  total=150.000  balance_due=150.000  hist=t  Nadia Chaabane
DOC HIST-SINV-2026-00001   supplier_invoice  paid    total=500.000  balance_due=0.000    hist=t  Laboratoires Méditerranée SA

PARTNER Nadia Chaabane                type=customer  receivable=150.000  payable=0.000
PARTNER Laboratoires Méditerranée SA  type=supplier  receivable=0.000    payable=0.000     <- was 500.000, settled

MOVEMENT dir=out amount=500.000 balance_after=-500.000 source_type=payment
REPOSITORY Compte bancaire balance=-500.000                                <- DOWN; the campaign saw it go UP

AGED AR grand_total = 150.0000
AGED AP grand_total = 0.0000      (the AP opening was paid in this scenario)
```

`AGED AP grand_total = 500.0000` **before** payment is asserted separately in
`ArApOpeningLedgerTest::test_aged_reports_list_the_opening_items_on_the_correct_side`.

The repository is negative because `payment_repositories.balance` is port-managed (not fillable; a pgsql
trigger forbids direct writes) so the fixture starts it at `0.000`, and a bank repository is allowed to go
overdrawn — the same shape the campaign's bank was in. What matters is the **sign of the movement**.

---

## 5. Design decisions a reviewer should challenge deliberately

**(a) The AR/AP open-item batch is now the single writer of the control accounts at cutover, and the GL
opening batch refuses them.** The brief specifies `Dr 119 / Cr 401` for AP and `Dr 411 / Cr 119` for AR, which
only makes sense if the open-item batch posts GL. But the campaign operator ALSO entered `411 150.000` and
`401 500.000` in the ACCOUNTING opening (§A.6) — and the shipped CSV template taught exactly that
(`401000,0.00,5000.00,Opening Payables`). Without a guard, the fix would have doubled both control accounts on
the very next tenant. So `AccountingOpeningService::validateRow()` now refuses a row on a partner control
account, at validation, before anything posts, naming the batch to use instead. This is the standard ERP rule
(control accounts are sub-ledger-only), and it makes the smoke sheet's row 0.4 satisfiable by construction.
**It is also the one behaviour change in this lane that an operator will notice.** If the reviewer or owner
prefers a warning over a refusal, that is a one-const change — but a warning leaves a wrong trial balance.

**(b) `source_type` is the document's own posting source, not `opening_balance`.** `PaymentController`
(`:549-560`) refuses to pay a supplier invoice unless a POSTED journal entry with
`source_type = 'supplier_invoice'` and `source_id = <document>` carries a Cr line on 401 tagged to the partner.
An opening supplier invoice IS a real payable and must be payable through that path, so it posts exactly that
entry. The alternative — widening the controller predicate — would have loosened a guard that exists to stop
paying unposted invoices.

**(c) The entry is created directly `Posted` with `is_historical = true`, never through
`sealAndPersistEntry()`** — the same shape `AccountingOpeningService::postBatch()` uses, because an opening
carries no fiscal hash and must stay out of the GL hash chain. The cost is that no `JournalEntryPosted` fires,
so `RefreshPartnerBalanceOnJournalEntryPosted` never runs; the batch therefore refreshes the sub-ledger
**explicitly**, once per distinct partner (`ArApOpeningService.php:383-389`). A reviewer should check that
pairing: it is the load-bearing half of W4-4 and it is not enforced by anything but the call site.

**(d) The entry carries the OPEN amount (`documents.balance_due`), not `total`.** A migrated invoice of
200.000 that the previous system already collected 50.000 against owes 150.000 at cutover. Pinned by
`ArApOpeningLedgerTest::test_the_opening_entry_carries_the_open_amount_not_the_original_total`. This agrees
with `Document::outstandingBalance()`, whose docblock already names `ArApOpeningService` as a legitimate
`balance_due` writer for this exact reason.

**(e) `PAYMENT_DIRECTION_MISMATCH` is placed BEFORE the supplier arm.** A mis-typed document is then refused
for what is actually wrong with it, rather than for the downstream symptom (`SUPPLIER_INVOICE_NOT_POSTED` — a
missing Cr-401 entry it could never have had). `PartnerType::Both` satisfies both sides. Every document type
outside AR/AP returns `true`: the guard answers one question and must not acquire opinions about advances,
expenses, POS or order-stage documents.

**(f) The new service duplicates ~20 lines of entry-number generation** rather than reaching into
`AccountingOpeningService`'s private `generateEntryNumber()`. It deliberately uses the **same** `OB-{year}-{seq}`
sequence and the **same** advisory-lock key `gl-ob-seq:{companyId}:{year}`, so the two services serialize
against each other; the docblock says so, because changing the key in one place re-opens the race. A new file
also keeps this lane out of `AccountingOpeningService::postBatch()`, which the **W4-2** lane is rewriting.

---

## 6. Red-first evidence

### Red run 1 — the AR/AP opening arc, before any production change

`php vendor/bin/phpunit tests/Feature/Document/ArApOpeningLedgerTest.php` → **`Tests: 8, Assertions: 9,
Failures: 7`** (`/tmp/w43/red-1-ledger.txt`). Highlights:

```
No journal entry was posted for opening document HIST-INV-2026-00001 — the sub-ledger cannot see it.
Failed asserting that null is not null.

-'150.000'     (partner receivable_balance)
+'0.000'

The AR opening must age as a receivable.
-'150.0000'
+'650.0000'    <- the AP opening polluting aged receivables, exactly as predicted
```

### Red run 2 — the payment direction guard

`php vendor/bin/phpunit tests/Feature/Treasury/OpeningItemPaymentDirectionTest.php` →
**`Tests: 3, Failures: 3`** (`/tmp/w43/red-2-payment.txt`):

```
test_a_customer_typed_document_owned_by_a_supplier_cannot_be_paid
  Expected response status code [422] but received 201.

test_a_supplier_typed_document_owned_by_a_customer_cannot_be_paid
  -'PAYMENT_DIRECTION_MISMATCH'
  +'SUPPLIER_INVOICE_NOT_POSTED'      <- refused for the symptom, not the cause
```

### Red run 3 — the control-account guard

`php vendor/bin/phpunit tests/Feature/Accounting/OpeningControlAccountGuardTest.php` →
**`Tests: 3, Failures: 2`** (`/tmp/w43/red-3-control.txt`); the third (ordinary account still accepted) was
green from the start, as it must be.

### Whole-lane red proof (`git apply -R`, no stash)

```
git diff dev HEAD -- apps/api/app apps/web/src > /tmp/w43/prod.patch   # 677 lines
git apply -R /tmp/w43/prod.patch
php vendor/bin/phpunit <the three new classes>
  ->  Tests: 14, Assertions: 18, Failures: 12
git apply /tmp/w43/prod.patch
git status --porcelain   ->  (empty)
```

The 12 failures name every W4-3/W4-4 behaviour. The **2 that stay green with production reverted are correct
and were checked**: `test_an_ar_opening_item_stays_a_customer_invoice` (AR was already `Invoice` — the test
guards against over-correcting the AR side) and `test_an_ordinary_balance_sheet_account_is_still_accepted`
(the new guard must not touch ordinary accounts). Sample:

```
8) OpeningItemPaymentDirectionTest::test_paying_an_ap_opening_item_debits_the_payable_and_moves_the_cash_out
Paying a supplier opening item must take the supplier arm — a customer_payment entry means the cash moved
the wrong way.
Failed asserting that null is not null.
```

---

## 7. Tests by path — final tree (dev merged at `2f239eaf4`)

**SQLite** (`phpunit.xml`), one process:

```
tests/Feature/Document/ArApOpeningLedgerTest.php
tests/Feature/Treasury/OpeningItemPaymentDirectionTest.php
tests/Feature/Accounting/OpeningControlAccountGuardTest.php
tests/Feature/Document/ArApOpeningPostLifecycleTest.php
tests/Feature/Import/PartiesImportBalancesTest.php
tests/Feature/Treasury/PaymentTest.php
tests/Feature/Accounting/Reports/AgedOutstandingSourceTest.php
tests/Feature/Accounting/OpeningBalanceBatchTest.php
tests/Feature/Accounting/OpeningBalanceBatchLifecycleHardeningTest.php
tests/Feature/Treasury/SupplierPaymentGuardTest.php
```
→ **`OK, Tests: 102, Assertions: 389, Skipped: 3`** (the 3 skips are pre-existing in
`OpeningBalanceBatchLifecycleHardeningTest`).

**PostgreSQL 16** — `-c phpunit-pgsql.xml`, throwaway `autoerp_test_w43` on `127.0.0.1:5433`
(`autoerp`/`autoerp_secret`), dropped and recreated before the run, **dropped after**:

```
DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=5433 DB_DATABASE=autoerp_test_w43 \
DB_USERNAME=autoerp DB_PASSWORD=autoerp_secret \
php vendor/bin/phpunit -c phpunit-pgsql.xml --do-not-cache-result <same list minus the hardening class>
```
→ **`OK (90 tests, 357 assertions)`**

Also run on PG: the three new classes alone → `OK (14 tests, 66 assertions)`.

Static gates on the final tree:

| Gate | Result |
|---|---|
| `./vendor/bin/pint --test` | `{"result":"pass"}` |
| `./vendor/bin/phpstan analyse` (level 8, incl. `DocumentStatusWriteOnlyViaStatusService`) | `[OK] No errors` |
| `php tools/deptrac-ratchet.php` | `PASS — 183 / 183, no boundary regression` |
| `php tools/feature-lane-manifest-check.php` | **EXIT=0** — `1409 Feature classes in 74 groups`; gated 1168 |

Never ran the full suite. Never more than one test process at a time. No migration in this lane.

### N-6 interaction, checked

`DocumentStatusService` is untouched. `ArApOpeningService` still writes `DocumentStatus::Posted` at document
**creation** — a birth-state write the N-6 docblock (`DocumentStatusService.php:48-54`) explicitly exempts —
and the PHPStan write-path rule (which forbids `Paid` outside `DocumentStatusService` and `Posted` from
`App\Modules\Treasury\`) is green. The historical exemption at `DocumentStatusService::wasNeverSealed():201`
keys on `is_historical`, not on `type`, so it continues to hold for the new `SupplierInvoice` /
`SupplierCreditNote` openings — verified by the AP opening reaching `paid` through the real endpoint in
`OpeningItemPaymentDirectionTest`. `RepairPaidNeverPostedDocumentsCommand:126` excludes `is_historical`
documents, so the type change does not widen its candidate set.

---

## 8. Manifest raise

`Document` **82 → 83**, `gated_ceiling` **1167 → 1168**, one named raise for `ArApOpeningLedgerTest`. The two
sibling classes do **not** raise anything: `OpeningItemPaymentDirectionTest` is `tests/Feature/Treasury` and
`OpeningControlAccountGuardTest` is `tests/Feature/Accounting`, both whole-directory lanes
(`treasury-spine-pgsql`), neither group ceilinged. Union arithmetic re-taken after the second dev merge:
dev's 1167 + this lane's +1 = 1168, and the checker confirms 1168 on the final tree.

---

## 9. Residuals — seen, NOT touched

1. **`AgedPayablesService` still cannot see an ordinary (non-historical) supplier invoice.** The report is
   purchase-order driven: `type = PurchaseOrder, status = Posted` plus the uninvoiced accrual on
   auto-generated received POs (`AgedPayablesService.php:153-168` / `:182-240`). A real `SupplierInvoice` is
   represented by its PO, so widening my new arm to every `SupplierInvoice` would double-count. My arm is
   narrowed to `is_historical = true` for exactly that reason. **The gap is real, pre-existing and out of
   lane** — a tenant with a supplier invoice raised without a PO has an invisible payable. Worth its own row.
2. **`payment_type` never says "supplier".** `PaymentController::store():893-896` picks
   `Advance`/`DocumentPayment` from allocation-emptiness alone and ignores `$isSupplierPayment`;
   `PaymentType::SupplierPayment` has **zero writers** in `app/`, yet `CashMovementsReportService:97,:328` and
   `Payment::scopeOutgoing():313` read it, and `PaymentType::reversalSupport():186` refuses to reverse it.
   The campaign flagged the same thing ("the correct and the inverted one are indistinguishable"). Fixing it
   is a data-shape change with reporting consequences — deliberately not done here.
3. **`JournalLineData` (`Accounting/Application/DTOs/JournalLineData.php`) does not carry `partner_id`.** The
   sub-ledger dimension this lane depends on is invisible to any consumer reading a journal entry through that
   DTO (e.g. the JE detail page). The stored column is correct; the read model omits it.
4. **`PartnerBalanceService::reconcileSubledger()` has no caller and no report surface.** It computes exactly
   the control-vs-subledger difference this lane makes meaningful (`:197-213`, incl. an
   `entries_without_partner` count). Surfacing it would let an operator *prove* the cutover ties.
5. **`AccountingOpeningService::postBatch()` still writes `partner_id => null` explicitly** (`:317-325`,
   `:343-351`). Correct now that control accounts are refused there, but if the guard is ever relaxed those
   two lines become the W4-4 defect again. Left as-is; the guard is the enforcement.
6. **`OpeningBalancePosted` has no listener anywhere** — the event is dispatched and nothing consumes it.
7. **`react-doctor` findings in `apps/web/src/features/opening-balances`** — 11 warnings (array-index key,
   giant component, 3 async handlers without re-entry guards, click-without-keyboard). **All pre-existing**;
   my change to `FileUpload.tsx` is a single string literal inside `getTemplateContent()`. Flagged because the
   commit hook surfaces them.
8. **`apps/web` typecheck / vitest were NOT run in this worktree** — no `node_modules` there. The FE change is
   a string-literal-only edit inside an existing function, no type surface, and no test references the
   template content (grepped).
9. **`PartiesImportBalancesTest` was PG-fragile.** It compared the `import_file_reference` JSONB column with
   `assertSame`; PostgreSQL does not preserve object key order, so it passed on the SQLite runner and failed on
   PG. Surfaced because this lane ran the class on PG for the first time — the `Import` group has **no pgsql CI
   lane**. Fixed in place (`assertEqualsCanonicalizing`), flagged here because the same pattern very likely
   exists in other sqlite-only Feature groups.
10. **W4-2 collision, one file.** That lane rewrites `AccountingOpeningService::postBatch()` (cash/bank lines →
    repository movements). This lane adds a const at `:44-57` and an `elseif` in `validateRow()` at `:204-224`
    — different regions, but the merger should expect a textual conflict if both land close together.

---

## 10. Not in scope, deliberately

No migration. No backfill command for tenants that already minted AP openings as `HIST-INV` customer invoices
— **the campaign tenant `tenant01a03028-…` has exactly one such row** (`HIST-INV-2026-00002`, 500.000, marked
`paid` by the inverted payment, with an inverted `JE-2026-000007` behind it). Repairing it means reversing a
posted payment entry and re-typing a posted document; that is a correcting-entry problem with an owner ruling
attached (R2-F4 / `CorrectingEntryService`), not something to fold into a P0 fix. The new
`PAYMENT_DIRECTION_MISMATCH` guard means that row can no longer be paid again in the wrong direction, which is
the containment. **Flagged for the owner sheet.**
