# Adversarial review: Country Defaults in the Super Admin Panel

Reviewed spec: `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md`

Review posture: pre-planning adversarial gate. The owner's locked product decisions are treated as constraints: framework + COA first, template library + country assignment, scoped central-admin role, and propagation to new tenants only.

## Findings register

| ID | Severity | Finding |
|---|---|---|
| F-01 | P1 blocker | Runtime treasury code resolves nine GL accounts by hardcoded literal code, disproving the spec's central safety premise |
| F-02 | P1 blocker | `requiredPurposes()` is not an operational-completeness gate; a published template can pass and still break core GL flows |
| F-03 | P1 blocker | The publish gate does not enforce purpose/type compatibility or system-account immutability |
| F-04 | P1 blocker | The proposed scoped role is incompatible with the current role schema and `super_admin` middleware |
| F-05 | P1 blocker | The stated bootstrap/deploy ordering does not exist in the actual container startup path |
| F-06 | P1 blocker | Rewiring only the two match sites leaves production/demo/migration consumers on the old seeder classes |
| F-07 | P1 blocker | “New tenants only” conflicts with rewiring `ChartOfAccountsService`, which is also used when an existing tenant creates another company |
| F-08 | P1 blocker | Existing-company creation swallows COA resolution failures and is outside the tenant-provisioning compensation flow |
| F-09 | P1 blocker | The central assignments UI has no authoritative central country catalog, and signup accepts codes absent from `CountriesSeeder` |
| F-10 | P1 blocker | The golden-parity test is not executable as specified and cannot compare raw parent IDs meaningfully |
| F-11 | P1 blocker | Forget-based `GlobalCache` invalidation cannot guarantee “the next tenant” sees a repointed assignment |
| F-12 | P1 blocker | The spec promises MFA-equivalent protection for an external editor, but current admin authentication is password-only |
| F-13 | P2 should-fix | Central-table safety depends on connection-pinned models, which the design does not make an explicit invariant |
| F-14 | P2 should-fix | Published/assignment/archive invariants are application-only and race-prone; assignment domain compatibility is missing |
| F-15 | P2 should-fix | The claimed idempotent seeder semantics are factually incomplete: reruns reparent existing rows |
| F-16 | P2 should-fix | The vertical-config “audit precedent” is not atomic and is insufficient for the stated certification/audit goal |
| F-17 | P2 should-fix | The design has no lifecycle for creating, disabling, or revoking the external accountant account |
| F-18 | P2 should-fix | Certification is not represented as domain data or an approval decision |
| F-19 | P3 nit | Several factual citations/names are stale or inaccurate |

### F-01 — P1 blocker — Runtime treasury code resolves nine GL accounts by hardcoded literal code

The spec's statement that “no business code resolves accounts by literal code” is false. `InstrumentAccountResolver::resolve()` queries `accounts.code`, and `accountCode()` maps nine operational purposes to literals such as `5312`, `5112`, `4035`, `403`, `6275`, `44566`, and `416` (`apps/api/app/Modules/Treasury/Application/Services/InstrumentAccountResolver.php:15-30`, `apps/api/app/Modules/Treasury/Application/Services/InstrumentAccountResolver.php:41-55`). This is not dead compatibility code: payment instruments, outbound instruments, lifecycle transitions, remittance, projections, and reconciliation inject this resolver (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentInstrumentController.php:39`, `apps/api/app/Modules/Treasury/Application/Services/OutboundInstrumentService.php:43`, `apps/api/app/Modules/Treasury/Application/Services/InstrumentLifecycleService.php:60`, `apps/api/app/Modules/Treasury/Application/Services/InstrumentRemittanceService.php:36`).

An accountant can therefore publish a structurally valid template that renumbers any of those accounts and immediately break cheque/effect posting and reconciliation for every new tenant receiving it. This invalidates the safety premise at spec line 27 and the claim that purpose validation alone makes arbitrary renumbering safe.

Required change: either extend the template/purpose model to cover all nine `InstrumentAccountPurpose` mappings and rewire `InstrumentAccountResolver`, or explicitly make those codes immutable certified invariants and validate them at publish time. The first option is consistent with the template-library goal; the second sharply limits what accountants may edit and must be disclosed in the editor.

There is a second literal-code surface in withholding certificates: `WithholdingDirection::glAccountBase()` returns `42236`/`42237`, and `WithholdingCertificate::getGLAccountCode()` emits a derived code such as `42236.10` (`apps/api/app/Modules/Taxation/Domain/Enums/WithholdingDirection.php:26-31`, `apps/api/app/Modules/Taxation/Domain/Entities/WithholdingCertificate.php:214-224`). It is a follow-up-domain concern, but it further disproves the global “no business code” assertion.

### F-02 — P1 blocker — `requiredPurposes()` is not an operational-completeness gate

`SystemAccountPurpose` has 41 cases, but `requiredPurposes()` returns only eleven (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:14-103`, `apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:155-175`). Core code unconditionally resolves purposes outside that list: inventory and GR-IR in goods receipt, plus purchase stamp duty, inventory, both purchase-price-variance accounts, and supplier payable in supplier-invoice posting (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1828-1829`, `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1982-1988`). Other operational paths use `PurchaseExpenses`, refund, voucher, tolerance, and rounding purposes as features activate.

Consequently, the proposed publish gate can approve a template that provisions successfully and later hard-fails ordinary procurement or inventory operations. `ChartOfAccountsService::validateCompanyAccounts()` merely loops the same eleven-value list (`apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:47-62`); it is not proof that the full seeded chart is operationally complete.

Required change: define an explicit versioned “provisioning-required purpose set” for the product/module set, with tests proving every unconditional `findByPurposeOrFail()` consumer is covered. Optional/module-gated purposes need declared prerequisites and publish validation tied to the modules a new tenant can receive. Do not equate today's `requiredPurposes()` helper with complete readiness.

### F-03 — P1 blocker — Purpose/type and `is_system` invariants are absent from the publish gate

The enum already defines the expected `AccountType` for every system purpose (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:177-205`), but the spec validates only that `type` is a valid enum value, not that it matches the assigned purpose. A template could publish `cash` as revenue, `vat_collected` as an asset, or `opening_balance_equity` as an expense and still pass.

The gate also does not require a purpose-bearing row to have `is_system=true`. Tenant account immutability is enforced solely by `is_system`; non-system rows can be updated (`apps/api/app/Modules/Accounting/Presentation/Controllers/AccountController.php:147-180`). Purpose assignment itself does not automatically promote the flag (`apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:102-122`). A newly provisioned purpose row with `is_system=false` is therefore mutable in the tenant UI despite being required for GL routing.

Required change: publish must enforce `row.type === purpose.expectedAccountType()` and `system_purpose !== null => is_system === true`, plus the complete operational-purpose rule from F-02. Include negative tests for every mismatch.

### F-04 — P1 blocker — The scoped-role design conflicts with current auth

`super_admins` already has a `role` column; it is not a new column (`apps/api/database/migrations/2025_12_01_194614_create_super_admins_table.php:14-24`). The current enum has `super_admin` and `support_approver`, not `owner` (`apps/api/app/Models/Enums/SuperAdminRole.php:7-11`). `EnsureSuperAdmin` is explicitly role-aware and rejects every role other than `super_admin` (`apps/api/app/Http/Middleware/EnsureSuperAdmin.php:27-49`). Therefore a `defaults_editor` route cannot remain layered on `['auth:sanctum-admin', 'super_admin', ...]` as the spec states: it will be rejected before the new capability middleware runs.

The proposed two-role model also omits the existing four-eyes `support_approver` role, which is actively routed through `central_admin_role` (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-45`) and seeded as a distinct account (`apps/api/database/seeders/SuperAdminSeeder.php:40-60`). Backfilling every existing row to `owner` would erase that distinction.

Required change: preserve the existing roles and semantics. Split “authenticated active central admin” from capability authorization, add `defaults_editor` to the existing enum, and put explicit capabilities on every central-admin route group. Existing fleet/billing/monitoring/vertical routes must remain full-admin only (`apps/api/routes/api.php:56-124`), while support-access routes must retain their current `super_admin`/`support_approver` matrix. Add a route-inventory test proving `defaults_editor` receives only auth profile/logout and country-defaults routes.

### F-05 — P1 blocker — The bootstrap does not run in the migration batch

The actual API entrypoint runs central migrations and rolling tenant migrations (`apps/api/docker/entrypoint.sh:118-145`). It runs the default database seeder only when `AUTO_SEED=true` **and** the central tenant count is zero; otherwise it skips seeding (`apps/api/docker/entrypoint.sh:169-190`). There is no unconditional central bootstrap-seeder hook. `ProductionSeeder` is not invoked by startup and does not currently contain this proposed bootstrap (`apps/api/database/seeders/ProductionSeeder.php:19-81`).

Thus, on an existing staging/production central DB, a single release will create empty template tables, skip the bootstrap, deploy resolver-reading code, and make new provisioning fail. The assertion that the bootstrap runs “in the same migration batch” is false: seeders are not migrations, and the deployed process has no such ordering guarantee. Startup also logs a central migration failure and continues (`apps/api/docker/entrypoint.sh:122-127`), so merely placing a step before runtime cache-building is not a hard release gate.

Required change: use a self-contained central data migration, an explicit fail-closed deploy command, or a two-release rollout (schema + verified bootstrap, then reader switchover). The release gate must verify three published templates, exact TN/FR/* assignments, row counts/hashes, and wildcard availability before reader code is enabled. “Skip if any templates exist” is not sufficient because one manually created draft would suppress the remaining bootstrap data.

### F-06 — P1 blocker — Seeder retirement misses direct and historical consumers

The two match sites are real (`apps/api/app/Modules/Tenant/Application/Services/TenantInitializationService.php:199-210`, `apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:134-148`), but they are not the only callers.

- `DatabaseSeeder` directly creates France and Tunisia seeders (`apps/api/database/seeders/DatabaseSeeder.php:83-89`, `apps/api/database/seeders/DatabaseSeeder.php:117-123`).
- `DemoTenantSeeder` directly creates the Tunisia seeder (`apps/api/database/seeders/DemoTenantSeeder.php:215-220`).
- `CoffeeShopSeeder` directly creates the Tunisia seeder (`apps/api/database/seeders/CoffeeShopSeeder.php:365-373`).
- `ParapharmacySeeder` dynamically instantiates a `ChartOfAccountsSeederContract` class (`apps/api/database/seeders/ParapharmacySeeder.php:627-638`); its default is France and `DemoPharmacySeeder` overrides it to Tunisia (`apps/api/database/seeders/ParapharmacySeeder.php:154-162`, `apps/api/database/seeders/DemoPharmacySeeder.php:97-113`).
- A historical tenant migration imports and instantiates `TunisiaChartOfAccountsSeeder` when rebuilding a tenant from scratch (`apps/api/database/migrations/tenant/2026_03_23_300000_fix_existing_tunisian_companies_tax_setup.php:5-8`, `apps/api/database/migrations/tenant/2026_03_23_300000_fix_existing_tunisian_companies_tax_setup.php:44-53`).

Deleting the classes in a “cleanup commit” would make fresh tenant migration replay fail on the historical migration and break multiple development/demo seeders. Merely leaving them in place means those paths continue bypassing the template library.

Required change: inventory and classify every direct caller. Adapt active seeders and `ChartOfAccountsSeederContract` to the new path; retain a permanent compatibility class for historical migration replay or make the historical migration independent of a deletable class without mutating already-shipped migration behavior. Update direct-seeder tests deliberately rather than assuming the two service rewires cover them.

### F-07 — P1 blocker — “New tenants only” conflicts with existing-tenant company creation

The spec says templates apply at tenant creation only, yet it also rewires `ChartOfAccountsService`. That service is called when an authenticated existing tenant creates another company (`apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php:153-163`). Therefore, after an assignment is repointed, a second company created inside an old tenant would receive the current central template, even though that tenant is supposed to retain its own local copy and be insulated from future template changes.

Required change consistent with the owner's ruling: define and persist the template version selected at **tenant creation** (for example, a central immutable assignment snapshot on the tenant directory row) and use that version for later companies in the same tenant, or explicitly state that “new tenants only” actually means “new companies” and obtain an owner correction. The current spec cannot simultaneously make both promises.

### F-08 — P1 blocker — Existing-company creation can succeed without a chart

Initial database-per-tenant registration does have the claimed compensation: tenancy is initialized before full tenant setup, and any exception ends tenancy, drops the tenant DB best-effort, and removes central rows (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:117-125`, `apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:187-215`, `apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:218-249`).

But the other rewired caller is materially different. `CompanyController` catches `RuntimeException` from COA seeding, logs a warning, continues tax provisioning, and returns the company (`apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php:153-165`). A missing central assignment/template can therefore create an existing tenant's new company with no COA. That contradicts the spec's “fails loudly” model and leaves later GL paths broken.

Required change: specify failure semantics for both callers. Company creation must roll back if required defaults cannot resolve/seed; do not rely on `TenantProvisioningService` compensation outside registration. Add tests for resolver failure in both initial tenant registration and additional-company creation.

### F-09 — P1 blocker — There is no central country catalog for the assignments matrix

`countries` is created by a **tenant** migration (`apps/api/database/migrations/tenant/2025_12_01_192409_create_countries_table.php:12-28`). `TenantProvisioningService` explicitly documents that the countries table is not central (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:74-80`), and `CountriesSeeder` writes through the unpinned `Country` model (`apps/api/database/seeders/CountriesSeeder.php:630-635`; `apps/api/app/Models/Country.php:12-20`). A central admin endpoint cannot safely treat that tenant table as its catalog.

Moreover, registration validates only “required string of size 2”; it does not require membership in `CountriesSeeder` (`apps/api/app/Modules/Identity/Presentation/Requests/RegisterRequest.php:69-74`). Such a country will correctly need the wildcard at provisioning, but it cannot appear in the proposed “CountriesSeeder list” assignment UI, so an exact assignment cannot be administered there.

Required change: define an authoritative central/static country-code provider for the admin matrix, define whether inactive/unknown ISO codes can receive exact assignments, and test an accepted two-letter code absent from `CountriesSeeder`. The wildcard must be visible and non-removable independently of the country list.

### F-10 — P1 blocker — Golden parity is under-specified and raw-row equality is impossible

All three seeders generate random UUIDs and then store those IDs in `parent_id`; the Tunisia seeder's second pass demonstrates the shape (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:74-99`). Seeding old and template paths into separate fresh companies/databases necessarily produces different `parent_id` values. If “except ids” excludes `parent_id`, the test fails to verify hierarchy parity; if it includes `parent_id`, equality always fails.

The source arrays are also private (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:120-126`, `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:117-123`, `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:101-107`), so a central bootstrap seeder cannot simply “import the arrays” without first refactoring the source, using reflection, or materializing rows by running tenant seeders. If both paths are changed to consume one extracted definition, the parity test can become tautological unless an independent frozen golden is retained.

Required change: define a canonical comparison projection such as `(code, name, type, parent_code, system_purpose, is_active, is_system, normalized balance, ordinal)`, resolving `parent_id` back to parent code. Produce immutable golden fixtures from the pre-refactor seeders before sharing any data source. Test exact row count, order, hierarchy, and all 41 purpose assignments, plus behavior for an arbitrary non-TN/FR code.

### F-11 — P1 blocker — Cache invalidation has an acknowledged stale re-cache race

The precedent uses `GlobalCache::remember()` and a plain `forget()` (`apps/api/app/Services/VerticalConfigService.php:64-70`, `apps/api/app/Services/VerticalConfigService.php:82-104`). `GlobalCache` is the correct tenancy-neutral cache manager, and the test suite proves it survives tenancy transitions (`apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:23-36`, `apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:177-203`). However, the codebase explicitly documents that forget-based invalidation has an in-flight read race: a read started before the forget can repopulate stale data after the forget and retain it for the TTL (`apps/api/app/Services/CompanyConfigService.php:95-103`).

For vertical modules that race self-heals after 24 hours. For country defaults it violates the stronger promise that assignment changes affect “the next tenant”: multiple later tenants can receive the old certified template for the entire cache TTL.

Required change: either do not cache provisioning resolution (tenant creation is low-frequency), or use versioned keys/generation tokens whose value changes atomically with the assignment transaction. Add a concurrency regression test where an old read overlaps a repoint and must not make the old assignment visible again.

### F-12 — P1 blocker — Current central-admin auth has no MFA gate

The current login validates email/password, checks `is_active`, and immediately issues a Sanctum token (`apps/api/app/Http/Controllers/Api/Admin/SuperAdminAuthController.php:18-56`). The `super_admins` schema has no MFA secret, challenge state, recovery codes, or verified-at field (`apps/api/database/migrations/2025_12_01_194614_create_super_admins_table.php:14-24`). Therefore “defaults_editor accounts are subject to the same policy” currently means password-only authentication, regardless of an owner-confirmed future requirement.

Required change: either include and test the actual MFA gate before external `defaults_editor` accounts can authenticate, or explicitly make external-account enablement blocked until the separate MFA requirement lands. The spec cannot rely on an unimplemented policy as a security mitigation.

### F-13 — P2 should-fix — Central connection pinning must be explicit

During registration the default connection is switched to the tenant DB before initialization (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:117-125`). The named `central` connection is intentionally never swapped (`apps/api/config/database.php:105-115`). The working precedent is safe because `VerticalConfig` uses Stancl's `CentralConnection` concern (`apps/api/app/Models/VerticalConfig.php:20-24`), and `AdminAuditLog` does the same (`apps/api/app/Models/AdminAuditLog.php:32-36`).

The design says the resolver “reads from the central connection,” but does not make connection pinning a model-level invariant for all three new models. An ordinary Eloquent model would query the tenant DB during provisioning and fail with a missing table.

Required change: state that `AdminTemplate`, `AdminTemplateAccount`, and `CountryTemplateAssignment` all use `CentralConnection` (or that every query is explicitly `DB::connection('central')`), and add a real tenancy-initialized test proving central resolution works while the default connection points at a tenant database.

### F-14 — P2 should-fix — Lifecycle invariants need atomic enforcement

The data model has independent `domain` columns on templates and assignments but specifies only “template must be published”; it does not state that `assignment.domain === template.domain`. This becomes a real corruption path as soon as the second domain ships. Likewise, “cannot archive/delete while assigned” and “assign only published” are status checks that no FK can enforce by itself.

Application-level check-then-write logic races: one request can verify that a template is unassigned while another assigns it, or verify `published` while another archives it. The existing vertical controller is not a transaction/locking precedent: it performs an upsert, invalidation, fanout, and audit as separate operations (`apps/api/app/Http/Controllers/Api/Admin/VerticalConfigController.php:102-139`).

Required change: specify central DB transactions and row locks for publish, archive, delete, and assignment repoint; validate same-domain assignment; lock both old/new assignment targets in deterministic order; and define whether archived-but-still-referenced rows remain resolvable. The wildcard invariant must also be enforced by mutation rules (not merely a health check) so the only fallback cannot be removed or pointed at a non-published/wrong-domain template.

### F-15 — P2 should-fix — Seeder reruns do rewrite existing rows

The spec says the exact existing semantics are “never rewrite existing rows; only promote `is_system`.” The first pass does preserve existing name/type/purpose and may promote `is_system` (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:38-58`). But the second pass updates `parent_id` for every definition present in the ID map, including pre-existing accounts (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:94-100`). The productized seed command explicitly reports this behavior as “reparented” and says seeders “re-issue parent links” (`apps/api/app/Console/Commands/SeedChartsCommand.php:47-50`, `apps/api/app/Console/Commands/SeedChartsCommand.php:110-123`).

Required change: decide whether `TemplateChartOfAccountsSeeder` preserves this reparenting behavior or adopts genuinely non-rewriting semantics. Document it precisely and test a rerun after a tenant manually changes an existing account's parent. Do not call the current behavior “only promote `is_system`.”

### F-16 — P2 should-fix — Audit writes are not atomic or certification-grade

The vertical-config precedent commits the config update and cache invalidations before calling `AdminAuditService` (`apps/api/app/Http/Controllers/Api/Admin/VerticalConfigController.php:107-139`). `AdminAuditService::log()` then performs a separate `AdminAuditLog::create()` (`apps/api/app/Services/AdminAuditService.php:84-114`). If audit insertion fails, the mutation has already happened; if a controller forgets to call the service, the model/observer does not enforce an audit row.

For a bulk rows endpoint, this is not a “full audit trail” unless the design defines an exact before/after representation and commits content mutation, lifecycle transition, assignment change, and audit record in one central transaction. `CrossTenantRoute` is only an architectural annotation; it carries a reason for scanners and is not itself an audit write (`apps/api/app/Shared/Architecture/CrossTenantRoute.php:10-17`, `apps/api/app/Shared/Architecture/CrossTenantRoute.php:30-40`).

Required change: make audit creation part of each mutation transaction, define stable action/entity identifiers and row-level diffs or content hashes, and test rollback when audit creation fails. Cache invalidation should occur after commit.

### F-17 — P2 should-fix — No external-account administration lifecycle is designed

The API surface contains template/assignment operations but no central-admin account provisioning, disable, credential reset, role change, or revocation endpoints. Today the only central-admin creation path is environment-driven `SuperAdminSeeder`; it creates the main admin and optional support approver from configuration (`apps/api/database/seeders/SuperAdminSeeder.php:19-38`, `apps/api/database/seeders/SuperAdminSeeder.php:40-60`).

Required change: specify how the chartered accountant account is created, activated, disabled, and recovered, and how all of its tokens are revoked on disable/role change. Manual SQL is not an adequate lifecycle for an external privileged actor.

### F-18 — P2 should-fix — “Certification” has no model or approval semantics

The problem statement invokes chartered-accountant review and per-country certification, but the proposed template header contains only status, provenance, timestamps, and `created_by` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:50-62`). It has no jurisdictional standard/version, effective date, certified/reviewed-by identity, review decision, qualification/evidence reference, expiry/supersession, or content hash. The same `defaults_editor` can edit, publish, and repoint assignments under the proposed API (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-140`), so “published” is not demonstrably a separate certification decision.

`AdminAuditLog` records generic action/entity/old/new JSON and request metadata (`apps/api/app/Models/AdminAuditLog.php:13-30`, `apps/api/app/Models/AdminAuditLog.php:40-57`); that does not by itself say what regulatory plan was certified or preserve a signed review artifact.

Required change: either narrow the goal to “reviewable/auditable configuration” and stop claiming certification, or add explicit certification metadata/workflow and define whether four-eyes approval is required. At minimum a published version needs a stable content hash, reviewer identity, reviewed standard/version, decision timestamp, and supersession link.

### F-19 — P3 nit — Citation and naming corrections

- `Account::findByPurpose()` is at `apps/api/app/Modules/Accounting/Domain/Account.php:238-242`; line 229 is the underlying `scopeWithPurpose()` predicate (`apps/api/app/Modules/Accounting/Domain/Account.php:221-242`).
- The match-site citations are accurate (`apps/api/app/Modules/Tenant/Application/Services/TenantInitializationService.php:199-210`; `apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:139-148`).
- The physical central database must not be specified as `synerivia_central`; current configuration deliberately uses a deployment-specific product database and explicitly warns not to name ERP resources after Synerivia (`apps/api/config/database.php:124-135`). Refer to the logical `central` connection instead.
- The tenant account API is not CRUD: it lists, shows, creates, and updates accounts, but has no account-delete route (`apps/api/app/Modules/Accounting/Presentation/routes.php:25-41`). Purpose mappings do have a delete/unassign route (`apps/api/app/Modules/Accounting/Presentation/routes.php:60-75`).

## Verified claims and non-findings

- The two duplicated TN/FR/default match sites cited by the spec exist exactly where claimed (`apps/api/app/Modules/Tenant/Application/Services/TenantInitializationService.php:199-210`; `apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:134-148`).
- `SystemAccountPurpose` has 41 enum cases, and tenant accounts have a unique `(company_id, system_purpose)` constraint plus a unique `(company_id, code)` constraint (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:14-103`; `apps/api/database/migrations/tenant/2025_12_06_002513_add_company_id_and_system_purpose_to_accounts.php:53-58`, `apps/api/database/migrations/tenant/2025_12_06_002513_add_company_id_and_system_purpose_to_accounts.php:84-87`).
- `GlobalCache` is the correct tenancy-neutral cache mechanism; the defect is the stronger instant-next-tenant guarantee under forget races, not the selection of `GlobalCache` (`apps/api/app/Services/VerticalConfigService.php:22-30`; `apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:23-36`).
- Central reads during initialized tenancy are possible and already proven by the `VerticalConfig` + `CentralConnection` precedent (`apps/api/app/Models/VerticalConfig.php:20-24`; `apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:177-203`).
- Initial registration failures do enter the existing compensation path and remove tenant DB/central directory rows best-effort (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:211-249`).
- The cited `6354` backfill is purpose-first and collision-aware: it skips an already mapped purpose, claims a compatible preferred-code row, or chooses a nearby free code (`apps/api/database/migrations/tenant/2026_08_07_100000_backfill_purchase_stamp_duty_account.php:96-145`, `apps/api/database/migrations/tenant/2026_08_07_100000_backfill_purchase_stamp_duty_account.php:193-214`). It is not itself evidence that arbitrary template renumbering is unsafe; the runtime treasury resolver in F-01 is.

## Gate conditions before implementation planning

At minimum, revise the spec to close F-01 through F-12, then re-review it before producing an implementation plan. The revision needs a complete consumer migration inventory, operational-purpose contract, current-role-compatible authorization matrix, real bootstrap rollout, tenant-version snapshot semantics, exact parity normalization, authoritative country source, atomic lifecycle/audit rules, and a cache strategy that cannot reintroduce stale assignments.

FINAL VERDICT: REJECT

## Round 2

Reviewed revision: Rev 2 at commit `a5d129ec2` (repository HEAD `a5d129ec25`).

Review posture: adversarial re-review before implementation planning. The three owner rulings are treated as constraints, not reopened: protected treasury codes in v1 with a later purpose-migration lane; current published assignment for every newly created company; certification metadata on published versions with four-eyes deferred.

### Round-1 closure register

| ID | Status | Round-2 verification |
|---|---|---|
| F-01 | PARTIAL | Rev 2 now acknowledges the literal treasury resolver and selects the owner's protected-code ruling (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:33-38,116-125`), matching the real code/type lookup (`apps/api/app/Modules/Treasury/Application/Services/InstrumentAccountResolver.php:25-30,41-70`). It is not closed because the gate incorrectly requires non-account withholding display codes, does not require protected rows to be tenant-immutable, and makes the publish-time country check vacuous in the normal lifecycle (R2-01, R2-07, R2-08).
| F-02 | PARTIAL | Rev 2 correctly stops treating the 11-value `requiredPurposes()` helper as completeness and proposes a separate versioned contract (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:116-125`; `apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:155-175`). The contract is still not enumerated, its grep/reflection guard does not account for the private `getAccountByPurpose()` wrapper (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:4582-4584`) and its many callers (`GeneralLedgerService.php:137-139,1720-1721`), and at least one frozen baseline cannot meet the implied set (R2-02).
| F-03 | CLOSED | The publish gate now expressly enforces purpose/type equality and `system_purpose != null => is_system` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:118-125`). Those are the actual type contract and tenant mutation boundary (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:177-205`; `apps/api/app/Modules/Accounting/Presentation/Controllers/AccountController.php:164-180`). Protected rows without purposes remain a separate new defect (R2-07), not a reopening of this finding.
| F-04 | PARTIAL | The revision preserves `super_admin` and `support_approver`, adds `defaults_editor`, separates active central-admin authentication, keeps existing full-admin groups restricted, and retains the support-access role matrix (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-178`). That matches the current enum/middleware topology (`apps/api/app/Models/Enums/SuperAdminRole.php:7-11`; `apps/api/app/Http/Middleware/EnsureSuperAdmin.php:42-59`; `apps/api/app/Http/Middleware/RequireCentralAdminRole.php:17-30`; `apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-46`). It remains ambiguous at the privileged editor-lifecycle endpoints, where the nonexistent `owner` role is named and no nested `super_admin` middleware is specified (R2-06); the UI entry path is also incomplete (R2-12).
| F-05 | PARTIAL | Rev 2 accurately describes the entrypoint and chooses a two-release central-data-migration rollout (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:162-168`). The entrypoint really does run central migrations, continue after migration failure, and conditionally seed only an empty installation (`apps/api/docker/entrypoint.sh:118-127,169-190`). The claimed per-template stable bootstrap key does not exist in the proposed schema (R2-04), and `country-defaults:verify` is not invoked by the auto-deploy entrypoint; the release-2 interlock is therefore a manual checklist condition, not the claimed self-guard (`apps/api/docker/entrypoint.sh:118-207`; `docs/factory/WORKFLOW.md:190-217`).
| F-06 | PARTIAL | The consumer table now covers both services, `DatabaseSeeder`, `DemoTenantSeeder`, `CoffeeShopSeeder`, the contract-driven pharmacy path, and the historical migration (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:135-145`). It still misses a live direct caller, `TunisianParapharmacySeeder` (`apps/api/database/seeders/TunisianParapharmacySeeder.php:68-75`), and a separate literal-code COA consumer used by the pharmacy seeders (`apps/api/database/seeders/ExpenseCategorySeeder.php:37-65,86-93`). See R2-09 and R2-10.
| F-07 | CLOSED | The locked rule is now unambiguous: every newly created company gets the current assignment, while existing company accounts are untouched (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:5-11,51-54,135-145`). The additional-company path creates and seeds inside its own transaction (`apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php:70-80,153-166`).
| F-08 | CLOSED | Rev 2 explicitly removes the swallow-and-continue behavior and requires rollback in both provisioning paths (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:146-150`). This is compatible with the existing additional-company transaction (`apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php:70-80,153-166`) and the registration compensation path (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:211-249`).
| F-09 | CLOSED | A versioned static central ISO provider plus visible, non-removable wildcard is now specified, including a test for an accepted code absent from `CountriesSeeder` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:97-107`). That addresses both the tenant-only country table (`apps/api/database/migrations/tenant/2025_12_01_192409_create_countries_table.php:12-28`) and the real size-only registration validation (`apps/api/app/Modules/Identity/Presentation/Requests/RegisterRequest.php:69-74`). Case normalization remains a separate new defect (R2-05).
| F-10 | PARTIAL | The revision supplies an ID-independent projection, frozen fixtures, exact hierarchy/order comparison, and a feasible scratch-schema exporter that does not reflect private arrays (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:151-156`). This resolves random UUID/parent-ID comparability. It is not fully executable as written because `is_active` is part of the canonical projection/hash but absent from the template-row schema (R2-11).
| F-11 | CLOSED | Provisioning resolution is now explicitly uncached (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127-133`), so it no longer inherits the documented forget/repopulate race (`apps/api/app/Services/CompanyConfigService.php:95-103`). The separate admin-list cache allowance does not affect provisioning correctness.
| F-12 | CLOSED | The revision chooses the exact acceptable mitigation from round 1: defaults-editor login is denied by a default-off feature flag until MFA exists (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-178`). That is necessary because current login is password plus active-state only (`apps/api/app/Http/Controllers/Api/Admin/SuperAdminAuthController.php:18-56`) and the central-admin schema has no MFA state (`apps/api/database/migrations/2025_12_01_194614_create_super_admins_table.php:14-24`).
| F-13 | CLOSED | All three new models are now explicitly required to use `CentralConnection`, with a tenancy-initialized regression test (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:60-64`). This matches the working `VerticalConfig` precedent (`apps/api/app/Models/VerticalConfig.php:20-24`) and the named connection that is never swapped (`apps/api/config/database.php:105-115`).
| F-14 | CLOSED | Rev 2 adds a composite `(template_id, domain)` guard backed by a unique `(id, domain)` parent key, central transactions, deterministic row locks, post-lock status checks, assignment-domain equality, reference-aware archive/delete, and an in-transaction wildcard rule (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:97-114`). The composite FK is structurally valid for the configured PostgreSQL central database (`apps/api/config/database.php:124-140`). R2-08 concerns certification/publish context, not the original assignment-domain or mutation-race defect.
| F-15 | CLOSED | The revision now states the real two-pass semantics, including reparenting pre-existing rows, and pins it with a rerun test (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:158-160`). That matches the current first-pass preserve/promote behavior and unconditional second-pass parent update (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:38-58,94-100`).
| F-16 | CLOSED | Audit creation is now service-owned and required inside the same central transaction, with before/after projections or hashes and post-commit cache work (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:198-200`). This deliberately improves on the current separate audit insert (`apps/api/app/Services/AdminAuditService.php:84-114`), while `AdminAuditLog` is already central-pinned (`apps/api/app/Models/AdminAuditLog.php:32-36`).
| F-17 | PARTIAL | Rev 2 adds create/disable/re-enable/reset flows, token revocation, and audit (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177-178,180-197`). The lifecycle is not safely authorized until the nonexistent `owner` wording is replaced by an explicit nested `central_admin_role:super_admin` rule (R2-06). Today the only provisioning path remains the environment-driven seeder (`apps/api/database/seeders/SuperAdminSeeder.php:19-60`).
| F-18 | PARTIAL | The locked metadata now exists on published versions and the publish action sets all four required fields (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:66-81,116-125`). The bootstrap nevertheless inserts already-published versions before any certifying actor is available, and country scope is not bound to the certification decision (R2-03, R2-08).
| F-19 | CLOSED | The corrected citations and names are now accurate: both match sites (`apps/api/app/Modules/Tenant/Application/Services/TenantInitializationService.php:199-210`; `apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:134-148`), `findByPurpose()` rather than its scope predicate (`apps/api/app/Modules/Accounting/Domain/Account.php:221-242`), logical `central` naming (`apps/api/config/database.php:124-135`), and the no-delete account API statement (`apps/api/app/Modules/Accounting/Presentation/routes.php:25-75`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:22-25,33,55,60-64`).

Round-1 closure count: **11 CLOSED / 8 PARTIAL / 0 OPEN**.

### New findings introduced or exposed by Rev 2

| ID | Severity | Finding |
|---|---|---|
| R2-01 | P1 blocker | The new protected-code gate requires withholding display codes that are not COA accounts, so none of the frozen templates can pass it |
| R2-02 | P1 blocker | The frozen France fixture and the new operational-purpose gate are mutually incompatible |
| R2-03 | P1 blocker | The data migration creates “published/certified” baselines before any certifier exists and bypasses the publish gate |
| R2-04 | P1 blocker | Bootstrap idempotency depends on a stable key that the data model does not contain |
| R2-05 | P1 blocker | The resolver does not specify country-code normalization and regresses the existing lowercase-safe company path |
| R2-06 | P1 blocker | Editor-account lifecycle authorization names a nonexistent role and can be implemented as defaults-editor self-administration |
| R2-07 | P2 should-fix | Protected literal-code rows are not required to be tenant-immutable |
| R2-08 | P2 should-fix | Publish-time per-country validation is unreachable in the normal lifecycle and certification has no jurisdiction binding |
| R2-09 | P2 should-fix | `ExpenseCategorySeeder` is an unhandled literal account-code consumer |
| R2-10 | P2 should-fix | The supposedly complete seeder-consumer inventory still omits `TunisianParapharmacySeeder` |
| R2-11 | P2 should-fix | The canonical fixture/hash projection contains a field the template schema cannot store |
| R2-12 | P2 should-fix | A permitted defaults editor is sent to, and can client-navigate through, full-admin pages immediately after login |

#### R2-01 — P1 blocker — Withholding display codes make the publish gate impossible

Rev 2 requires both `42236` and `42237` to exist in every relevant COA template (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:116-125`). Those values are not queried from `accounts`: `WithholdingDirection` returns a base string (`apps/api/app/Modules/Taxation/Domain/Enums/WithholdingDirection.php:26-31`), and `WithholdingCertificate` formats it into a display/PDF value such as `42236.10` (`apps/api/app/Modules/Taxation/Domain/Entities/WithholdingCertificate.php:214-224`). There is no account lookup or type predicate on this path.

Neither value exists in any frozen source array: the complete TN, FR, and Generic definitions occupy `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:125-318`, `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`, and `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:106-235`. Therefore the required golden-identical templates (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:151-156`) cannot pass the proposed publish rule, and release 1 cannot truthfully install three valid published templates.

Required change: keep the locked treasury account-code set, but remove `42236`/`42237` from COA existence/type validation unless a real posting path is first introduced that resolves those values against `accounts`. Treat the current withholding strings as document-format literals in their later domain lane.

#### R2-02 — P1 blocker — Frozen France parity conflicts with operational completeness

The new gate says it is derived from every unconditional purpose lookup (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:116-125`). Ordinary COGS posting unconditionally resolves `CostOfGoodsSold` whenever the computed COGS is positive (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1712-1721`). But the complete France source definition maps Inventory at `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:153-158` and returns at `FranceChartOfAccountsSeeder.php:339` without assigning `CostOfGoodsSold` anywhere (`FranceChartOfAccountsSeeder.php:124-339`).

The spec simultaneously requires exact frozen-seeder purpose parity (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:151-156`) and three published bootstrap templates (`:162-168`). The France template cannot both remain golden-identical and satisfy the advertised operational gate. The proposed grep/reflection guard is also underspecified: many throwing lookups are hidden behind `GeneralLedgerService::getAccountByPurpose()` (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:4582-4584`), including dynamic-purpose calls (`GeneralLedgerService.php:1455-1457,2565-2566`). PHP reflection of method metadata does not inventory that call graph.

Required change: enumerate the exact v1 required-purpose manifest in the spec, run it against all three frozen fixtures now, and resolve every failure deliberately. If baseline defects are corrected, keep two independent fixtures: immutable legacy-output goldens and explicitly reviewed/certified v1 template fixtures. Specify a real drift mechanism (for example an explicit consumer manifest plus AST/static-analysis ratchet), not an undefined “grep/reflect” test.

#### R2-03 — P1 blocker — Bootstrap bypasses certification and has no certifying actor

Publishing normally requires `standard_ref` and an acting admin, and sets `content_hash`, `certified_by`, and `published_at` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:76-79,116-125`). Release 1 instead has a central data migration directly import three templates already marked published (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:162-168`). On a fresh installation, central migrations run before optional seeding (`apps/api/docker/entrypoint.sh:122-127,169-190`), while the first super-admin is only created by `DatabaseSeeder` afterward (`apps/api/database/seeders/DatabaseSeeder.php:36-43`). The migration therefore cannot satisfy its `certified_by` FK with a real reviewer, and direct insertion also bypasses every publish-gate check.

This is not a four-eyes objection. It violates the locked v1 metadata decision by presenting unreviewed legacy extracts as certified published versions.

Required change: bootstrap drafts (or a distinct non-certified legacy status), then require an authenticated certification command/workflow that executes the same publish gate and records the actual reviewer before assignments and release-2 switchover become valid. `country-defaults:verify` must reject published rows with missing certification fields and must execute all current publish invariants, not only compare fixture hashes.

#### R2-04 — P1 blocker — The stable bootstrap key has no storage

Release 1 promises a per-template presence guard keyed by a “stable bootstrap key” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:162-168`). The proposed `admin_templates` schema contains only UUID id, domain, mutable name/description/status, provenance, certification fields, creator, and timestamps (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:66-81`). There is no bootstrap key or declared deterministic UUID. Name, hash, and domain cannot substitute: names are editable, content hashes change by version, and three templates will eventually share a domain.

Required change: add an immutable nullable unique `bootstrap_key` (or specify fixed deterministic IDs) and define collision behavior. The idempotency test must cover partial state: one baseline present, one conflicting manual draft, and retry after a rolled-back/failed import. Also correct the “self-guarding” claim: the real entrypoint logs migration failure and continues (`apps/api/docker/entrypoint.sh:122-127`), and it never invokes the proposed verifier (`entrypoint.sh:118-207`); the two-release process is safe only if the release-2 promotion gate is explicitly enforced outside that entrypoint.

#### R2-05 — P1 blocker — Country resolution can silently select the wrong template by case

The resolver specifies only exact country then wildcard (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127-133`); it does not require canonical uppercase input. Registration happens to uppercase before creating the company (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:74-75`), but additional-company validation accepts any two-character string (`apps/api/app/Modules/Company/Presentation/Requests/CreateCompanyRequest.php:19-27`) and persists it verbatim (`apps/api/app/Modules/Company/Presentation/Controllers/CompanyController.php:74-80`). The current COA service preserves correct behavior by calling `strtoupper()` before its match (`apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:141-147`).

Without an explicit resolver/assignment normalization invariant, an accepted `tn` additional company receives the wildcard Generic chart rather than TN. This directly violates the locked “every new company gets the current country assignment” ruling.

Required change: normalize and validate country codes at every write boundary and again inside `CountryTemplateResolver` (`trim` + uppercase), normalize assignment route parameters, and add lowercase TN/FR tests for both registration and additional-company creation.

#### R2-06 — P1 blocker — Editor lifecycle can become self-service privilege administration

Rev 2 correctly says there is no `owner` role and preserves the existing enum (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-175`; `apps/api/app/Models/Enums/SuperAdminRole.php:7-11`). It then calls editor lifecycle endpoints “owner-role” operations and lists them inside the country-defaults API (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177-178,180-197`). The enclosing country-defaults group explicitly allows both `super_admin` and `defaults_editor` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:174-176`). The current role middleware grants any role named in its parameters (`apps/api/app/Http/Middleware/RequireCentralAdminRole.php:15-30`); there is no implicit owner concept.

An implementation following only the stated group middleware lets a defaults editor create, re-enable, or reset credentials for peer privileged accounts. The phrase “owner role only” cannot enforce anything because that role does not exist.

Required change: rename this authorization to `super_admin only`, specify a nested `central_admin_role:super_admin` middleware on every `/editors` route, and extend the route-inventory test to assert a defaults-editor token gets 403 for every lifecycle verb.

#### R2-07 — P2 should-fix — Protected rows can still be renumbered after provisioning

The publish gate requires `is_system=true` only for rows carrying a `system_purpose` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:118-121`). The protected-code rule requires existence and type but not `is_system` (`:121`). Tenant immutability is solely the boolean check (`apps/api/app/Modules/Accounting/Domain/Account.php:137-143`; `apps/api/app/Modules/Accounting/Presentation/Controllers/AccountController.php:164-180`). Most instrument rows have no `SystemAccountPurpose`; their current safety comes from the legacy seed definitions explicitly setting `is_system`, for example TN `403`/`4035` and `413`/`416` (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:174-175,183-184`).

An editor can therefore publish a protected code with the correct type but `is_system=false`; it is locked in the central editor yet mutable in the tenant, defeating the owner-selected v1 protection.

Required change: every protected account row must also be active and `is_system=true`, enforced server-side at publish and assignment time and covered by negative tests.

#### R2-08 — P2 should-fix — Country-aware publishing is normally context-free

Assignments may reference only published templates (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:97-105`), while the normal lifecycle is draft edit, then publish, then assignment (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:109-123`). Consequently a draft reaching publish normally has no current assignments. “Countries the template … may be assigned to” is not represented anywhere, so the publish-time country-specific protected check at `:121` has no deterministic input. Assignment-time revalidation protects provisioning, but the template has already acquired published/certified metadata.

The same model does not bind certification to a jurisdiction: a template has one `standard_ref` and certifier (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:66-81`) but may be assigned to many exact countries and the wildcard. That cannot demonstrate the stated per-country certification without reopening the locked choice to store metadata on the published version.

Required change: add immutable certified-jurisdiction scope to each published version (for example `certified_country_codes`, with an explicit wildcard/non-TN scope) and validate the protected set for that scope at publish. Assignment must be allowed only within the certified scope and re-run the same validation. A published version may still retain the four locked metadata fields; no four-eyes workflow is required here.

#### R2-09 — P2 should-fix — A pharmacy seeder hardcodes editable account codes

`ExpenseCategorySeeder` maps semantic categories to literal TN codes `613`, `615`, `616`, `624`, and `626`, then queries `accounts.code` (`apps/api/database/seeders/ExpenseCategorySeeder.php:13-44,52-65`). If an accountant validly renumbers one of those non-protected rows in the TN template, the category silently falls back to `GeneralExpense` (`ExpenseCategorySeeder.php:54-69`), changing posting semantics rather than failing. This seeder is called by both pharmacy paths (`ExpenseCategorySeeder.php:86-93`; `apps/api/database/seeders/ParapharmacySeeder.php:649`; `apps/api/database/seeders/TunisianParapharmacySeeder.php:100-103`).

Required change: include this in the COA consumer migration. Replace literal lookup with stable semantic template metadata/purposes, or explicitly protect and certify those mappings; do not silently collapse a missing specialized account to GeneralExpense after a template edit.

#### R2-10 — P2 should-fix — One active direct seeder caller remains outside the inventory

The “complete inventory” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:135-145`) omits `TunisianParapharmacySeeder`, which creates an additional company and directly instantiates `TunisiaChartOfAccountsSeeder` when no accounts exist (`apps/api/database/seeders/TunisianParapharmacySeeder.php:53-75`). It also has dedicated feature tests that execute the seeder by class (`apps/api/tests/Feature/Seeders/SeededProductsHaveTaxRateTest.php:70-81`; `apps/api/tests/Feature/Seeders/DemoSeedersTaxTest.php:89-99`).

Required change: add this seeder and its tests to the migration table and route it through the template-backed path. Keep direct tests of the three frozen classes only where they deliberately verify historical compatibility; classify those tests explicitly so future cleanup cannot mistake them for active provisioning coverage.

#### R2-11 — P2 should-fix — `is_active` is hashed but cannot be stored

The canonical golden/content-hash projection includes `is_active` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:151-156`), and tenant accounts really have that persisted flag (`apps/api/database/migrations/tenant/2025_11_30_090000_create_accounts_table.php:13-23`); all three legacy seeders explicitly write it true, for example France at `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:76-90`. But `admin_template_accounts` has no `is_active` column (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:83-95`).

Required change: either add `is_active` to template content and define whether drafts may disable rows, or state that it is a derived constant `true` excluded from stored content and hash only the actual canonical template representation. Specify exact deterministic serialization and row ordering for SHA-256; a tuple name alone is not enough for a certification-grade cross-version hash.

#### R2-12 — P2 should-fix — Defaults-editor login lands on a forbidden UI

The backend design allows a defaults editor to authenticate and access only auth plus country-defaults routes (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:174-177`). The current login success handler unconditionally navigates to `/admin/dashboard` (`apps/web/src/features/admin/pages/AdminLoginPage.tsx:16-31`), the admin index also redirects there (`apps/web/src/routes/index.tsx:385-400`), and `RequireAdminAuth` checks authentication but no role (`apps/web/src/features/admin/components/RequireAdminAuth.tsx:8-16`). The current layout exposes all privileged links to every authenticated admin (`apps/web/src/features/admin/components/AdminLayout.tsx:8-17,50-70`), and the stored role union does not yet admit `defaults_editor` (`apps/web/src/features/admin/stores/adminAuthStore.ts:4-9`).

Rev 2 mentions role-filtered nav and route guards (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:202-208`) but does not specify a role-aware landing route or direct-navigation behavior.

Required change: send defaults editors to `/admin/country-defaults`, make the `/admin` index role-aware, wrap every full-admin client route in a `super_admin` guard (redirect or 403 page), extend the role type, and test direct URL navigation as well as sidebar filtering. Backend authorization remains authoritative.

### Verified Rev-2 additions with no separate finding

- The composite `(template_id, domain)` FK design is a valid database guard when the declared parent unique key is created first (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:97-105`). Published status still correctly remains an in-transaction application invariant because an FK cannot encode it (`:109-114`).
- Central reads during initialized tenancy are safe if all three proposed models actually use `CentralConnection`: the named central connection is never swapped (`apps/api/config/database.php:105-115`) and the existing tenancy-transition test proves the pattern (`apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:177-203`).
- Keeping `RequireCentralAdminRole` unchanged does not leak support-access routes to `defaults_editor`: the middleware checks active state and exact allowed strings (`apps/api/app/Http/Middleware/RequireCentralAdminRole.php:17-30`), and every support route lists only `super_admin` and/or `support_approver` (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-46`). If implementation refactors that middleware into a capability-only check, `EnsureCentralAdmin` must be added to the support group in the same change so the active-account check is not lost.
- The no-cache provisioning decision is correct for the stated next-company guarantee (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127-133`). `GlobalCache` itself does work across tenancy transitions (`apps/api/tests/Feature/Services/TenantConfigCacheTenancyTest.php:23-36,177-203`); it is simply unnecessary here.
- The historical `6354` migration remains purpose-first and collision-aware, so Rev 2 correctly leaves existing-company changes to tenant backfills rather than template propagation (`apps/api/database/migrations/tenant/2026_08_07_100000_backfill_purchase_stamp_duty_account.php:96-145,187-229`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:51-54`). Other preferred-code backfills likewise run against existing companies; on fresh tenant creation migrations run before the company exists (`apps/api/app/Modules/Tenant/Application/Services/TenantProvisioningService.php:117-140`).

FINAL VERDICT: REJECT

## Round 3

Rev 3 was reviewed against the Round-2 register and the eight Round-1 findings that remained partial. The result is **9 CLOSED, 11 PARTIAL, 0 OPEN**. The revision closes the concrete withholding-code, normalization, protected-row, direct-caller, deploy-model, legacy-fixture, and editor-lifecycle defects, but it leaves six new defects: three blockers in certification attribution, wildcard scope semantics, and expense-category migration, plus three should-fix gaps in bootstrap validation, canonical hashing, and the existing `support_approver` frontend path.

### Closure register

| ID | Verdict | Evidence |
|---|---|---|
| R2-01 | CLOSED | Rev 3 now states that `42236`/`42237` are withholding document-format literals rather than account rows and excludes them from the protected COA set (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:33-37`). The publish gate is limited to the nine codes actually queried by `InstrumentAccountResolver` and expressly excludes the withholding strings (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:128`; `apps/api/app/Modules/Treasury/Application/Services/InstrumentAccountResolver.php:25-30,41-70`). |
| R2-02 | PARTIAL | Rev 3 separates immutable legacy goldens from certified-v1 fixtures and explicitly records the France `CostOfGoodsSold` correction (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:164-167`), resolving the direct golden-versus-publish contradiction. It still does not enumerate the v1 operational-purpose contract: the “exact case list” is deferred to implementation planning (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125`), even though the cited private wrapper and dynamic call sites are live consumers (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1455-1457,1712-1721,2565-2566,4582-4584`). The design therefore cannot yet demonstrate that all three certified fixtures can pass the intended gate. |
| R2-03 | PARTIAL | The bootstrap now imports drafts only, and publication is routed through the normal gate with a real `certified_by` value (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177-180`), so migrations no longer manufacture certified rows. The alternative Artisan path accepts an `--admin=` identity without specifying authentication or proof that this administrator made the decision (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:178`), while the real HTTP path authenticates by password and active-account check before issuing a token (`apps/api/app/Http/Controllers/Api/Admin/SuperAdminAuthController.php:20-49`). Certification attribution remains forgeable as specified (R3-01). |
| R2-04 | PARTIAL | Rev 3 adds the missing immutable, unique `bootstrap_key` column (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:67-81`) and changes import idempotency to a per-key check with partial-state tests (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177`). A presence-only check still does not say what happens when the keyed row exists with the wrong domain, content, status, or legacy hash, nor where “set only by the bootstrap data migration” and immutability are enforced (R3-04). |
| R2-05 | CLOSED | Country input is now normalized with `trim()` plus uppercase both at assignment writes and inside the resolver, with lowercase registration and additional-company tests required (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:107,138,226`). This preserves the behavior previously provided only by `ChartOfAccountsService` before its country match (`apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:141-147`). |
| R2-06 | CLOSED | Rev 3 removes the nonexistent owner-role wording and requires nested `central_admin_role:super_admin` middleware on every editor-account lifecycle route (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:189-190,210`). That matches the exact-string role enforcement in `RequireCentralAdminRole` (`apps/api/app/Http/Middleware/RequireCentralAdminRole.php:15-30`), and the route-inventory test explicitly requires 403 for every `/editors` verb. |
| R2-07 | CLOSED | Every protected row must now be `is_system=true`, both at publish and assignment validation, with a negative test (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:128,226`). This matches the tenant mutation boundary, which rejects updates to system accounts (`apps/api/app/Modules/Accounting/Presentation/Controllers/AccountController.php:164-180`). |
| R2-08 | PARTIAL | Rev 3 adds immutable `certified_country_codes`, requires a non-empty scope at publish, and restricts assignment to that scope (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,109,118,123-130`). It does not define a coherent meaning for `*` across certification, assignment, and country-specific protected-code validation: `*` is simultaneously a stored scope member, an assignment fallback, and “the non-TN variant set” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:107-112,128`). The jurisdiction invariant is not implementable without the missing algebra (R3-02). |
| R2-09 | PARTIAL | `ExpenseCategorySeeder` is now in the consumer table and its silent fallback is supposed to become a loud failure (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:142-153,226`). But the template schema has no stable expense-category mapping field (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:86-100`), while the current seeder’s only association is the literal-code map plus `GeneralExpense` fallback (`apps/api/database/seeders/ExpenseCategorySeeder.php:30-69`). “Resolve against the template rows” cannot survive the renumbering this migration is meant to permit (R3-03). |
| R2-10 | CLOSED | The supposedly missing caller is now explicitly inventoried: `TunisianParapharmacySeeder`, both direct-class feature tests, and the historical-compatibility classification all appear in the migration table (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:150`). The active seeder really calls `ExpenseCategorySeeder` after its direct COA path (`apps/api/database/seeders/TunisianParapharmacySeeder.php:68-75,100-103`), so the listed scope covers the prior omission. |
| R2-11 | PARTIAL | The impossible `is_active` projection is fixed: it is now a derived constant `true`, is not stored, and is excluded from the hash (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:100,160-163`). The replacement canonical projection hashes `ordinal`, while the only persisted ordering field is named `sort_order` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:98,162-163`); the mapping and JSON canonicalization rules remain incomplete (R3-05). |
| R2-12 | PARTIAL | Rev 3 adds `defaults_editor` to the client role union, gives that role a country-defaults landing page, filters navigation, and requires direct-URL guards (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:216-227`). It specifies every full-admin route as `super_admin`-only without defining a landing page or allowed client route for the already-existing `support_approver`, even though that role must access support approvals (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-43`). The proposed shell would strand that role (R3-06). |
| F-01 | CLOSED | The runtime exception set is now accurately documented: the nine account-code lookups are protected in v1, withholding strings are excluded as document literals, and expense-category literals are separately inventoried (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:33-37`). The shared protected registry, account-type checks, `is_system=true`, editor locking, and publish/assignment validation now preserve the owner’s locked-code ruling (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:128-132,226`). |
| F-02 | PARTIAL | Rev 3 correctly rejects `requiredPurposes()` as the completeness contract and chooses an AST/PHPStan consumer ratchet (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125`). It nevertheless postpones the actual `ProvisioningRequiredPurposes::V1` membership to the implementation plan, so this design review cannot verify it against the 41-case enum or the live purpose consumers (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:7-47,155-205`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1455-1457,1712-1721,2565-2566,4582-4584`). |
| F-04 | PARTIAL | The backend design now cleanly adds `defaults_editor` without renaming `super_admin` or `support_approver`, keeps existing privileged groups on `EnsureSuperAdmin`, and uses an exact capability group only for country defaults (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:183-190`). Existing support routes cannot leak to the new role because they list their allowed roles exactly (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-46`; `apps/api/app/Http/Middleware/RequireCentralAdminRole.php:15-30`). The frontend shell still lacks the third-role route/landing matrix required for `support_approver` (R3-06). |
| F-05 | CLOSED | Rev 3 now describes the real deployment behavior—central migration failures are logged and startup continues, initial seeding is conditional, and no verifier is invoked—and explicitly makes the two-release switchover an external runbook gate (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:173-181`). That matches the actual entrypoint (`apps/api/docker/entrypoint.sh:118-127,169-190`). Draft import ships before reader switchover, and `country-defaults:verify` is a required Release-2 precondition rather than a fictional same-batch seeder. |
| F-06 | PARTIAL | Rev 3’s consumer inventory now includes both production services, `DatabaseSeeder`, demo seeders, the contract path, `TunisianParapharmacySeeder`, its tests, `ExpenseCategorySeeder`, and the historical migration (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:142-153`). The expense-category row is not executable after renumbering because no stable semantic link exists in the proposed data model (R3-03); therefore the inventory is complete but one migration strategy is not. |
| F-10 | CLOSED | Rev 3 replaces reflection over private seeder arrays with a one-time exporter that runs the frozen seeders in a scratch schema, retains immutable legacy goldens, and keeps a second certified-v1 fixture set with reviewed deltas (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:160-167`). That makes the original golden-parity test feasible and preserves evidence of the France correction. The separate canonical-field defect is recorded as R3-05 rather than reopening the exporter finding. |
| F-17 | CLOSED | The design now includes create, disable, re-enable, and credential-reset operations, token revocation on disable or role change, and audit for every lifecycle action (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:189-192,210,214`). Every lifecycle endpoint is nested under an explicit `super_admin`-only middleware rule and covered by the defaults-editor route-inventory denial test (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:189-190`). |
| F-18 | PARTIAL | Published versions now carry the required content hash, standard reference, reviewer, decision timestamp, supersession provenance, and immutable jurisdiction scope (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:74-80,118-130`). Draft-only bootstrap avoids fake migration-time certification (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177-180`). The Artisan flow can still attribute a decision to a named admin without authenticating that actor (R3-01), and wildcard jurisdiction meaning remains undefined (R3-02), so the stated per-country certification goal is not yet trustworthy. |

### New Rev-3 findings

#### R3-01 — P1 blocker — `country-defaults:certify --admin=` permits forged certification attribution

Rev 3 offers an Artisan certification path that “requires an explicit `--admin=` super-admin identity” and then promises that `certified_by` is always a real actor (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:178`). Naming an existing account proves only that the row exists; it does not prove that the named person reviewed or authorized the content. The ordinary admin path requires email/password verification, rejects inactive accounts, and issues an authenticated token (`apps/api/app/Http/Controllers/Api/Admin/SuperAdminAuthController.php:20-49`). No equivalent authentication, signed approval artifact, interactive confirmation by the actor, or non-impersonable operator binding is specified for the command.

Any operator with shell access sufficient to run Artisan can therefore select another administrator’s identifier and create certification metadata that falsely attributes the accounting decision to that person. This breaks G6’s reviewer-identity guarantee and the stated purpose of chartered-accountant certification, even if shell access is otherwise privileged.

Required change: remove arbitrary actor selection from the command. Prefer the authenticated UI. If a CLI path is mandatory, require a short-lived, purpose-bound authorization issued through the admin’s authenticated session (or a signed approval artifact), re-check the actor’s active role at execution, bind the artifact to template id + content hash + standard + normalized scope, consume it once, and record both certifier and executing operator. A mere `--admin` id/email must never set `certified_by`.

#### R3-02 — P1 blocker — `*` has no coherent certification-scope algebra

The model permits `certified_country_codes` to contain exact ISO codes “and/or `*`” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78`). Assignments use exact country then `*` fallback, and every assignment must be “within” that stored scope (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:107-112,138`). The protected-code rule then defines wildcard as “the non-TN variant set” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:128`), while bootstrap creates distinct `TN`, `FR`, and `*` assignments “within certified scopes” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:179`).

Those statements do not determine whether a certified `*` covers an exact `FR` assignment, an exact `TN` assignment, only the wildcard assignment row, all ISO countries except explicitly assigned ones, or all non-TN countries forever. They also do not define whether `["FR", "*"]` is redundant, contradictory, or a materially different scope. Because protected codes differ between TN and non-TN (`apps/api/app/Modules/Treasury/Application/Services/InstrumentAccountResolver.php:41-54`), choosing the wrong interpretation can certify a template against one variant and provision it into a country requiring another.

Required change: define one explicit scope algebra and encode it in a value object shared by publish, assignment, resolver verification, and UI. At minimum specify membership for exact assignment codes and the wildcard assignment, whether `*` includes TN, how exact exclusions/overrides behave, whether exact codes may coexist with `*`, and which protected-code variant must pass for every permitted country. Add a truth-table test covering scopes `["TN"]`, `["FR"]`, `["*"]`, and mixed scopes against assignments `TN`, `FR`, an unlisted ISO code, and `*`.

#### R3-03 — P1 blocker — The expense-category migration still cannot survive account renumbering

The current seeder links five semantic categories to literal codes `613`, `615`, `616`, `624`, and `626`, then silently falls back to the `GeneralExpense` purpose (`apps/api/database/seeders/ExpenseCategorySeeder.php:30-69`). Rev 3 says the replacement will “resolve against the template rows for the company’s country” and fail loudly when a specialized account is missing, while deferring semantic expense mapping to a later domain phase (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:152,233`). But a template account stores only code, name, type, parent code, optional system purpose, system flag, and ordering (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:86-100`). It has no stable category key, and the five specialized rows are identified in today’s seeder only by their editable codes.

After an accountant validly renumbers `613`, the proposed consumer has no deterministic way to discover which row now represents “Loyer.” Resolving by old code recreates the bug; resolving by display name is unstable and unversioned; resolving all five to `GeneralExpense` preserves the semantic collapse that Rev 3 says will be removed. “Fail loudly” changes the symptom but does not create the missing relationship.

Required change: bring the minimal stable mapping into this phase. Add a versioned expense-category account key/purpose to template content (with uniqueness and publish validation), or protect the five literal codes until the follow-up domain lands. Update `ExpenseCategorySeeder` to consume that stable mapping and add a test that renumbers a mapped account in a draft, publishes it, provisions a company, and verifies every category points to the renamed account rather than the fallback.

#### R3-04 — P2 should-fix — Presence-only `bootstrap_key` idempotency can bless corrupt or mismatched baselines

Rev 3 adds an immutable unique `bootstrap_key` that is “set only by the bootstrap data migration” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:74`) and defines import idempotency as a per-key presence check (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:177`). It does not require an existing keyed record to have the expected domain, draft status, legacy content hash, or complete child-row set before the importer skips it. Nor does it specify the database/application enforcement that prevents later key mutation.

A partially committed or manually repaired row with `coa.fr.legacy-v1` can therefore suppress the canonical France import. The proposed “retry after a failed import” test is insufficient unless the entire template plus child rows is one transaction and an existing key is verified rather than blindly accepted.

Required change: make each keyed import atomic and treat the key as an assertion, not just an existence flag. On key collision, recompute the canonical legacy projection and require exact domain, expected bootstrap status, and expected hash/row count; otherwise abort with a diagnostic. Enforce immutability and “bootstrap-only” assignment in the model/service and database where practical. Add tests for a keyed wrong-domain row, missing child rows, altered content, a published keyed row, and concurrent import attempts.

#### R3-05 — P2 should-fix — The canonical hash names a field that the template does not store

The content table stores `sort_order` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:88-99`), but the canonical projection and serializer use `ordinal` without defining it as an alias or derived value (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:160-163`). That leaves implementations free to hash a nonexistent property, array position, one-based index, or `sort_order`. The JSON rule also fixes field order and row joining but does not pin JSON escaping/Unicode normalization, so independently implemented exporters can still produce different bytes for the same names.

This is certification evidence, not merely a cache key: the bootstrap parity exporter, published `content_hash`, verify command, and certified fixtures all have to agree byte-for-byte (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:76,163-166,180`). The current definition cannot guarantee that.

Required change: use one field name end-to-end—prefer persisted `sort_order`—and specify its uniqueness/tie behavior. Define the exact byte-producing routine, including UTF-8 validity/normalization, JSON escaping flags, null representation, newline behavior, and whether a terminal newline exists. Put the serializer in one shared backend class used by exporter, publish, verify, and tests, with a fixed non-ASCII golden vector.

#### R3-06 — P2 should-fix — The role-aware frontend redesign strands `support_approver`

Rev 3 correctly preserves the existing `support_approver` role (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:45,185-187`) but describes the client changes only for `defaults_editor`: that role gets a special landing page, while “every full-admin client route” becomes `super_admin`-guarded (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:221`). The current client role union includes `support_approver` (`apps/web/src/features/admin/stores/adminAuthStore.ts:4-9`), login always lands on `/admin/dashboard` (`apps/web/src/features/admin/pages/AdminLoginPage.tsx:16-31`), `/admin` also redirects there, and `/admin/support-access` is a sibling route in the same shell (`apps/web/src/routes/index.tsx:385-411`). Backend support routes deliberately grant approval operations to `support_approver` (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-43`).

If the spec is implemented literally, the existing role is authenticated but sent to a newly forbidden dashboard and has no stated role-aware path to its approval page. Filtering navigation for only `super_admin` versus `defaults_editor` can also hide the one route it needs.

Required change: specify a three-role frontend capability matrix. Land `support_approver` on `/admin/support-access`, permit that route and only the actions its backend matrix allows, keep full-admin and country-default routes denied, and render only its support navigation. Add login, `/admin` index, direct-navigation, and mixed-role nav tests for all three roles.

FINAL VERDICT: REJECT — 9 CLOSED, 11 PARTIAL, 0 OPEN; 6 new findings (3 P1, 3 P2). Certification attribution, wildcard jurisdiction semantics, and the renumber-safe expense mapping remain implementation-planning blockers.

## Round 4

Rev 4 was checked against the six Round-3 findings, all eleven findings still partial after Round 3, every throwing purpose-resolution call site in `apps/api/app`, and the purpose assignments in all three frozen COA seeders. Carried-finding result: **12 CLOSED, 5 PARTIAL, 0 OPEN**. Five of the six Round-3 mechanisms are closed. The TN-only expense-code remedy is incomplete because the same literal-code seeder is an active France consumer. The new 16-purpose fixture deltas are correct as far as the listed set goes, but the required/conditional classification omits a seventeenth core, unprechecked throwing purpose: `SalesDiscount`.

### Carried-finding closure register

| ID | Verdict | Evidence |
|---|---|---|
| R3-01 | CLOSED | Rev 4 removes the Artisan certification path and requires publication exclusively through the authenticated HTTP/UI endpoint; `certified_by` may only be taken from that request's Sanctum actor (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:180-181`). This now matches the real login path, which validates password and active status before issuing the admin token (`apps/api/app/Http/Controllers/Api/Admin/SuperAdminAuthController.php:20-49`). |
| R3-02 | CLOSED | `certified_country_codes` is now exact-set XOR the sole value `*`; mixed scope is rejected, exact assignments require exact membership, and `*` is assignable only to the wildcard row and validated with the non-TN variant (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,123`). A shared `CertificationScope` value object and the requested exact/mixed/wildcard truth table are explicit (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:123,228`). |
| R3-03 | PARTIAL | The five TN codes are now protected for existence and expense type, locked in the editor, and consumed without fallback; missing `613` is a planned negative test (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:37,130,154`). That is a valid stable-code solution for TN. It does not cover the same seeder's France path: `ParapharmacySeeder` defaults to France and always calls `ExpenseCategorySeeder` (`apps/api/database/seeders/ParapharmacySeeder.php:141-162,630-650`), while the France chart lacks literal `624` (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:243-252`). Loud failure therefore breaks an inventoried consumer (R4-02). |
| R3-04 | CLOSED | Each keyed bootstrap import is now one transaction, and an existing key is treated as an assertion over domain, draft status, canonical hash, and row count; mismatches abort. The design also specifies key immutability/bootstrap-only assignment and tests wrong-domain, partial, altered, published, concurrent, and failed-retry states (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:74,179`). |
| R3-05 | CLOSED | Rev 4 consistently uses persisted `sort_order`, makes it unique per template, and specifies a single `CanonicalCoaSerializer`: ascending order, fixed field order, NFC UTF-8, explicit JSON flags, JSON null, LF joins, no terminal newline, and SHA-256 over those bytes. The same class is required for exporter, publish, verify, and tests, with a non-ASCII golden vector (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:98,164-165,228`). The runtime image installs `intl`, so NFC is feasible (`apps/api/Dockerfile:73`). |
| R3-06 | CLOSED | The frontend design now has a complete three-role matrix: role-specific landing pages, `support_approver` access to `/admin/support-access`, guards for all three route classes, role-filtered navigation, and landing/index/direct-navigation/nav tests for every role (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:223,229`). This mirrors the backend's exact support-approval matrix (`apps/api/app/Modules/SupportAccess/Presentation/routes.php:17-43`). |
| R2-02 | PARTIAL | Rev 4 finally enumerates the proposed manifest and accurately identifies the two France omissions within that 16-case set (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127,169`). The code/fixture audit below confirms those deltas, but `SalesDiscount` is incorrectly warning-only despite a core POS path making an unprechecked throwing lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3807-3827`). The operational gate is still incomplete (R4-01). |
| R2-03 | CLOSED | Draft bootstrap, authenticated publication, and verification are now cleanly separated. No migration creates a published row, no CLI can name a certifier, and Release 2 requires full gate re-execution and complete certification fields (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:179-183`). |
| R2-04 | CLOSED | The schema contains the immutable unique key and the importer now validates rather than merely observes an existing key, atomically covering the header and children with explicit collision and concurrency tests (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:74,179`). |
| R2-08 | CLOSED | Jurisdiction scope is immutable after publication, exact versus wildcard membership is now deterministic, all publish/assignment/verify/UI paths share the same value object, and the generic wildcard is explicitly not represented as exact-country certification (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,118-123`). |
| R2-09 | PARTIAL | Rev 4 chooses the allowed protected-code alternative and removes silent fallback for TN (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). The current literal map still runs from the default France `ParapharmacySeeder`, where `624` is absent and today falls back to `GeneralExpense` (`apps/api/database/seeders/ExpenseCategorySeeder.php:37-69`; `apps/api/database/seeders/ParapharmacySeeder.php:141-162,648-650`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:243-252`). The proposed loud failure makes that live seeder path non-executable (R4-02). |
| R2-11 | CLOSED | The stored/hash projection now uses `sort_order`, excludes derived `is_active`, and has a complete deterministic byte contract and shared implementation point (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:100,164-165`). |
| R2-12 | CLOSED | All three current roles now have an explicit client landing route, navigation set, and direct-route capability matrix; `support_approver` is no longer sent to or trapped behind the full-admin dashboard (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:223,229`; `apps/web/src/routes/index.tsx:385-411`). |
| F-02 | PARTIAL | The spec now names 16 purposes, describes the throwing consumers, separates conditional/soft cases, and requires an AST/PHPStan drift ratchet (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127`). The audit found that all 16 are reachable and their fixture delta is accurate, but the same stated standard also captures `SalesDiscount`; leaving it outside the required set means a publishable TN or FR chart can fail a normal POS account-charge flow (R4-01). |
| F-04 | CLOSED | Backend and frontend now both preserve `super_admin`, `support_approver`, and the new `defaults_editor`, with exact role matrices rather than broad authenticated-admin access (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:185-194,223`; `apps/api/app/Http/Middleware/RequireCentralAdminRole.php:15-30`). |
| F-06 | PARTIAL | The consumer table names both services, all demo/direct/contract callers, the expense-category consumer, and the historical replay migration (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:144-155`). The list is complete, but the proposed TN-only protection does not make its listed France parapharmacy consumer executable under loud failure (R4-02). |
| F-18 | CLOSED | Certification now has immutable content hash, standard, authenticated reviewer, timestamp, provenance, and a precise immutable scope. Bootstrap cannot self-certify, and wildcard metadata expressly denotes only the generic fallback rather than an exact jurisdiction (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:74-80,118-132,179-182`). |

### `ProvisioningRequiredPurposes::V1` manifest verification

Result: **FAIL — fixture deltas pass for the proposed 16, but completeness does not.** All 16 listed purposes have reachable throwing consumers. The frozen-seeder comparison is exactly as stated: Tunisia and Generic contain all 16; France lacks only `CostOfGoodsSold` and `GeneralExpense`. `SalesDiscount` is absent from TN and FR. However, the word “unconditional” is not literal in the proposed set: several included cases are data/flow gated. Under the actual stated criterion—ordinary supported operation, no optional module gate, and no precheck—`SalesDiscount` is a seventeenth required purpose, not a warning-only conditional.

| Proposed required purpose | Verified throwing path and reachability | Frozen fixtures |
|---|---|---|
| `Bank` | Paid expense chooses Bank for a bank repository and throws if absent (`apps/api/app/Modules/Expense/Application/Services/ExpenseService.php:671-695`; also `GeneralLedgerService.php:3941-3952`). Reachable but payment/repository gated. | TN / FR / Generic present. |
| `Cash` | Paid expense chooses Cash for the default/cash repository and throws if absent (`apps/api/app/Modules/Expense/Application/Services/ExpenseService.php:671-695`; `GeneralLedgerService.php:3941-3952`). Reachable but payment/repository gated. | TN / FR / Generic present. |
| `CustomerReceivable` | Invoice and credit-note accounting eagerly resolves AR before line processing (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:386-427,538-579`); the event listener invokes those paths for posted invoices/credit notes (`apps/api/app/Modules/Accounting/Listeners/InvoicePostedListener.php:18-33`). | TN / FR / Generic present. |
| `Inventory` | Positive goods receipt throws at lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1825-1830`); GR-IR clearing also resolves it eagerly (`GeneralLedgerService.php:1983-1989`). | TN / FR / Generic present. |
| `SupplierPayable` | Supplier-invoice GR-IR clearing eagerly resolves it with the other clearing accounts (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1983-1989`); unpaid expense posting also throws on it (`GeneralLedgerService.php:3941-3956`). | TN / FR / Generic present. |
| `VatCollected` | Invoice and credit-note accounting resolves it eagerly even before deciding line treatment (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:411-427,563-579`). | TN / FR / Generic present. |
| `VatDeductible` | Supplier-invoice GR-IR clearing eagerly resolves it (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1983-1989`); taxable expense posting has another throwing lookup (`GeneralLedgerService.php:3982-4002`). | TN / FR / Generic present. |
| `ProductRevenue` | Invoice and credit-note accounting resolves it eagerly before inspecting line kinds (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:411-445,563-579`). | TN / FR / Generic present. |
| `ServiceRevenue` | The same invoice and credit-note paths eagerly resolve it even for documents without service lines (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:420-445,572-579`). | TN / FR / Generic present. |
| `CostOfGoodsSold` | Physical-product invoices with positive COGS reach the throwing lookup (`apps/api/app/Modules/Inventory/Listeners/PostCOGSOnInvoice.php:43-80`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1688-1721`). The listener catches the failure and logs it rather than failing invoice posting (`PostCOGSOnInvoice.php:106-112`), so Rev 4's “ungated/every product invoice” wording is overstated, but the account is operationally required to avoid missing COGS. | TN / Generic present; FR missing. |
| `GeneralExpense` | An expense without a category account takes the throwing fallback (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3913-3938`). Reachable but category-data gated. | TN / Generic present; FR missing. |
| `OpeningBalanceEquity` | Opening posting throws only when the imported opening rows do not balance (`apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php:307-318`). Reachable but imbalance gated. | TN / FR / Generic present. |
| `PurchasePriceVarianceExpense` | Supplier-invoice GR-IR clearing eagerly resolves both PPV sides before testing the computed delta (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1978-1989`). | TN / FR / Generic present. |
| `PurchasePriceVarianceIncome` | Same eager GR-IR clearing lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1978-1989`). | TN / FR / Generic present. |
| `GoodsReceivedNotInvoiced` | Positive goods receipt and supplier-invoice clearing both throw on the GR-IR account (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1825-1830,1983-1989`). | TN / FR / Generic present. |
| `PurchaseStampDuty` | Supplier-invoice GR-IR clearing resolves the stamp account eagerly even when the calculated timbre may be zero (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1959-1965,1983-1989`). | TN / FR / Generic present. |

Spot-check of non-manifest throwing consumers found `PurchaseExpenses` correctly flow-gated behind a positive bonus-goods return (`apps/api/app/Modules/Procurement/Application/SupplierCreditNotePostingService.php:178-214`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2367-2370,2435-2453`). It also found the omitted `SalesDiscount` lookup on the core POS account-charge path: a positive transaction discount directly calls the throwing resolver with no account precheck (`GeneralLedgerService.php:3807-3827`), and the projection expects the resulting discount line (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryAccountChargeBridge.php:239-249`). TN and France contain no `SalesDiscount` purpose, while Generic does (`apps/api/database/seeders/GenericChartOfAccountsSeeder.php:208-212`; no matching purpose in `TunisiaChartOfAccountsSeeder.php:125-318` or `FranceChartOfAccountsSeeder.php:124-339`).

### New Rev-4 findings

#### R4-01 — P1 blocker — `SalesDiscount` is the missing seventeenth required purpose

Rev 4 defines required purposes as reachable throwing resolutions in ordinary operations with no optional module gate, yet labels `SalesDiscount` conditional and warning-only (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127`). The core POS account-charge method accepts a transaction discount and performs an unprechecked throwing `SalesDiscount` lookup whenever that amount is positive (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3807-3827`). Its projection also treats that purpose as an expected journal line (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryAccountChargeBridge.php:239-249`). This is materially the same kind of data-gated core path as included `Bank`/`Cash` (`GeneralLedgerService.php:3941-3952`), `GeneralExpense` (`:3923-3938`), and `OpeningBalanceEquity` (`apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php:311-318`).

The spec itself confirms that TN and FR lack this purpose and that the resulting POS operation fails (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:169,258`). Calling the correction merely “recommended” allows TN and FR templates to publish and Release 2 verification to pass while newly provisioned companies retain a known 500 path. That defeats the operational publish gate.

Required change: add `SalesDiscount` as case 17 in `ProvisioningRequiredPurposes::V1`, make it a publish blocker, add explicit TN and FR certified-fixture deltas, and include those deltas in `country-defaults:verify`. Keep the existing-tenant correction in the separate backfill ticket, consistent with N1. Also define the required-versus-warning decision rule in terms the AST ratchet can enforce; “flow-gated” cannot distinguish this call from several cases already marked required.

#### R4-02 — P1 blocker — TN-only expense-code protection makes the France parapharmacy seeder fail

Rev 4 protects `613`, `615`, `616`, `624`, and `626` only when scope includes TN, and changes `ExpenseCategorySeeder` from fallback to loud failure (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). But the inventoried `ParapharmacySeeder` is France by default (`apps/api/database/seeders/ParapharmacySeeder.php:134-162`), seeds the France chart, and unconditionally invokes `ExpenseCategorySeeder` (`ParapharmacySeeder.php:630-650`). The current category map always looks up all five exact codes (`apps/api/database/seeders/ExpenseCategorySeeder.php:37-65`). France has `613`, `615`, `616`, and `626`, but no exact `624` (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:243-252`). Today that missing lookup silently falls back; the proposed change turns it into a deterministic failure.

This path is not dead compatibility code: multiple feature suites seed `ParapharmacySeeder` directly, including `SeededProductsHaveTaxRateTest` and the dedicated seeder tests (`apps/api/tests/Feature/Seeders/SeededProductsHaveTaxRateTest.php:38`; `apps/api/tests/Feature/Seeders/ParapharmacySeederTest.php:45-134`). The consumer table expressly promises to migrate this contract path (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:153-154`).

Required change: choose and specify a country-aware mapping before loud failure. At minimum, add a France mapping to an actual PCG transport account (or a certified France fixture delta creating/protecting the intended `624`) and make the protected-code registry country-specific for every locale that runs this seeder. Make `ExpenseCategorySeeder` accept the resolved mapping rather than assuming the TN table globally. Add both TN and France template-backed seeder tests, including France's transport category; do not limit the negative test to missing TN `613`.

#### R4-03 — P2 should-fix — The acceptance suite names only one of the required France certification deltas

Rev 4 correctly says the France legacy fixture is missing two required purposes and requires certification edits for both `CostOfGoodsSold` and `GeneralExpense` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:169`). The testing section still calls out only “certified-fixture deltas (FR `CostOfGoodsSold`)” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:228`), and the risk table likewise mentions only the COGS correction (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:242`). This leaves the newly discovered `GeneralExpense` delta—and the TN/FR `SalesDiscount` delta once R4-01 is fixed—outside the explicit fixture acceptance contract.

Required change: make the certified-fixture test table exhaustive and data-driven from the required-purpose manifest. Assert TN/FR/Generic missing sets before certification, then assert the exact reviewed deltas and zero missing required purposes after certification. Name FR `CostOfGoodsSold`, FR `GeneralExpense`, and TN+FR `SalesDiscount`; make any future manifest addition fail fixture verification until each country delta is explicitly resolved.

FINAL VERDICT: REJECT — 12 CLOSED, 5 PARTIAL, 0 OPEN; 3 new findings (2 P1, 1 P2). The manifest still permits a known POS failure, and the TN-only expense-code remedy breaks the active France parapharmacy seeder path.

## Round 5

Rev 5 was reviewed as a design gate against the current code at commit `95a4fc4f0`. Result for the eight requested carried items: **4 CLOSED, 3 STILL PARTIAL, 1 REOPENED**. The SalesDiscount, TN/FR protected-code, and original four-delta changes are internally clear, but the new decision rule reclassifies two live advance purposes as required and makes the claimed exhaustive France fixture incomplete.

This remains a pre-implementation design review. The Rev-5 commit changes only this spec and the Round-4 register. The proposed PHPStan rule, template fixtures, and country-aware seeder do not yet exist: the registered PHPStan rules remain the four unrelated rules at `apps/api/phpstan.neon:33-37`; the current seeder still has one global literal map and silent fallback at `apps/api/database/seeders/ExpenseCategorySeeder.php:37-69`; and its current feature test is TN-only at `apps/api/tests/Feature/Expense/ExpenseCategorySeederTest.php:24-67`. Accordingly, CLOSED below means the design contract is sufficient for the carried finding, not that future implementation tests have already passed. Artifact-level claims are **UNVERIFIED until implementation**.

### Closure register

| ID | Status | Grounded result |
|---|---|---|
| R4-01 | STILL PARTIAL | Rev 5 correctly adds `SalesDiscount` as case 17 and a publish blocker (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127,169-174`). The rule correctly keeps the cited Bank/Cash, GeneralExpense, OpeningBalanceEquity, and SalesDiscount data-gated sites required (`apps/api/app/Modules/Expense/Application/Services/ExpenseService.php:671-695`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3919-3952,3807-3827`; `apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php:311-318`). It is nevertheless not exhaustive: the same rule captures `CustomerAdvance` and `SupplierAdvance`, and its tolerance example does not satisfy its own 4xx-precheck condition (R5-01/R5-02). |
| R4-02 | CLOSED | The normative gate now protects all five expense codes for both TN and FR, adds FR `624`, requires a country-aware registry mapping and loud failure, and names TN/FR publish and seeder tests (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). That matches the live premise: the France-default pharmacy path invokes the expense seeder (`apps/api/database/seeders/ParapharmacySeeder.php:141-162,630-650`), while the exhaustive France chart has `613`/`615`/`616`/`626` but no `624` (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:243-252`). The tests are acceptance requirements, not existing artifacts, as noted above. |
| R4-03 | REOPENED | Rev 5 now names TN `SalesDiscount` and FR `CostOfGoodsSold`/`GeneralExpense`/`SalesDiscount`/`624` in both the fixture contract and TDD list (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:169-174,233`). Those are the correct deltas against the stated 17 cases, but they are not exhaustive under Rev 5's own rule because France also lacks newly-required `CustomerAdvance` and `SupplierAdvance` (R5-01). |
| R3-03 | CLOSED | The stable-code option is now fully scoped to every active consumer country: TN and FR both get existence/type protection, FR gets the missing `624`, and the seeder contract is country-aware and fail-loud (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). This closes the France path proved by `apps/api/database/seeders/ParapharmacySeeder.php:141-162,630-650`. |
| R2-02 | STILL PARTIAL | The two-fixture model and explicit original deltas remain well specified (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:162-174`), but the operational manifest and resulting France baseline are incomplete under the new decision rule (R5-01). |
| R2-09 | CLOSED | Rev 5 resolves the literal-code consumer by deliberately locking the five semantic codes for both relevant countries, sourcing the country-specific map from the shared registry, and replacing fallback with failure (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). This directly covers the current lookup/fallback behavior at `apps/api/database/seeders/ExpenseCategorySeeder.php:37-69`. |
| F-02 | STILL PARTIAL | The manifest is now enumerated and has an explicit classification policy (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127`), but the policy yields at least 19 required purposes, not 17, and the proposed AST ratchet checks registration rather than the semantic correctness of the classification (R5-01/R5-02). |
| F-06 | CLOSED | The complete consumer inventory now gives the France pharmacy consumer an executable design: FR protection includes `624`, and `ExpenseCategorySeeder` receives a country-aware registry map instead of applying one TN table globally (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:144-155`; `apps/api/database/seeders/ParapharmacySeeder.php:630-650`). |

### REQUIRED-vs-CONDITIONAL rule verification

| Borderline | Result | Evidence |
|---|---|---|
| Bank / Cash | REQUIRED is correct | Paid expense settlement chooses one purpose by repository type and immediately calls `findByPurposeOrFail`; the preceding check validates the repository, not the account purpose (`apps/api/app/Modules/Expense/Application/Services/ExpenseService.php:671-695`). |
| GeneralExpense | REQUIRED is correct | A category-less expense takes the unprechecked throwing fallback (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3919-3938`). |
| OpeningBalanceEquity | REQUIRED is correct | An imbalanced opening is a data condition, followed by an unprechecked throwing lookup (`apps/api/app/Modules/Accounting/Application/Services/AccountingOpeningService.php:311-318`). |
| SalesDiscount | REQUIRED is correct | A positive transaction discount is a data condition, followed by the unprechecked throwing lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3807-3827`). |
| Refund compensation | CONDITIONAL satisfies the written 4xx branch | Both purposes are prechecked and absence throws `RefundCompensationRefusedException` (`apps/api/app/Modules/Fiscal/Application/Services/RefundCompensationService.php:184-193`), which extends `DomainException` (`apps/api/app/Modules/Fiscal/Domain/Exceptions/RefundCompensationRefusedException.php:35-42`) and is rendered as 422 (`apps/api/bootstrap/app.php:487-496`). |
| Tolerance | The rule does not classify its own example | The precheck records an alert and returns from an asynchronous projection; it does not return or throw a 4xx domain error (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-459,524-539`). |
| Customer / Supplier Advance | REQUIRED under the written rule, but omitted | Both are live core treasury operations with no module gate and no account-purpose precheck; details are in R5-01. |

### Certified-fixture verification

The four deltas newly named by Rev 5 are factually correct against the 17 names it lists: TN lacks `SalesDiscount`; FR lacks `CostOfGoodsSold`, `GeneralExpense`, and `SalesDiscount`, and its account definitions omit exact code `624`; Generic has all 17. The relevant definitions are at `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:170-240,253`, `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`, and `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:125-150,163-211`.

The result is still **FAIL** for exhaustiveness. TN and Generic already define both advance purposes (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:178-186`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:132-143`), but the complete France definition at `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339` defines neither. Once the decision rule is applied consistently, France's certified delta is therefore at least `CostOfGoodsSold` + `GeneralExpense` + `SalesDiscount` + `CustomerAdvance` + `SupplierAdvance` + protected code `624`.

### New Rev-5 findings

#### R5-01 — P1 blocker — The new rule makes both advance purposes required and invalidates the France fixture

Rev 5 says a purpose is REQUIRED whenever a throwing resolution is reachable from a core, non-module-gated operation without an account-absence precheck, and expressly says data gating does not downgrade it (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127`). `CustomerAdvance` meets that definition: the ordinary payment-allocation service posts an advance for an order allocation or excess payment (`apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:307-365`), and `createCustomerAdvanceJournalEntry` immediately performs the throwing lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:396-417`). The route is in the ordinary authenticated Treasury group with a permission check, not a module/feature gate (`apps/api/app/Modules/Treasury/Presentation/routes.php:35,253-259`).

`SupplierAdvance` also meets it: the ordinary prepayment-refund route calls `VendorRefundService` (`apps/api/app/Modules/Treasury/Presentation/routes.php:214-217`; `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRefundController.php:212-230`), which unconditionally reaches the GL reversal after its business guards (`apps/api/app/Modules/Treasury/Domain/Services/VendorRefundService.php:153-175`); the GL method then performs an unprechecked throwing `SupplierAdvance` lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:496-517`). Neither path prechecks the missing purpose.

France defines neither purpose in its complete account array (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`), while TN and Generic explicitly define both (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:178-186`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:132-143`). Therefore the statements that the manifest has 17 required cases, France lacks only three required purposes, and the four named certified edits are exhaustive are false (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-127,169-174,233,247`).

Required change: add `CustomerAdvance` and `SupplierAdvance` as required cases 18 and 19; add both to the FR pre-certification missing set, certified fixture, verification command, risk text, and existing-tenant backfill lane. If the owner intends advances to remain conditional, the decision rule needs a new objective distinction that classifies these live flows as conditional without also demoting SalesDiscount merely because its triggering business data is absent in some transactions.

#### R5-02 — P1 blocker — The advertised AST rule cannot enforce the semantic decision rule, and the tolerance example contradicts it

The normative predicate depends on whole-program facts—production reachability, whether a module is optional, and whether an account-absence precheck dominates the throwing lookup (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127`). The proposed PHPStan rule is narrower: it requires each throwing call site to be present in a manifest with some classification. That can catch an unregistered call, but it cannot catch a registered call assigned the wrong classification. The current PHPStan configuration also has no such rule yet (`apps/api/phpstan.neon:33-37`), so its feasibility depends entirely on a design contract that is presently underspecified.

The rule is not even exhaustive as prose. CONDITIONAL requires an optional module/feature gate or a precheck that produces a 4xx domain error, yet the cited tolerance path merely records a missing-purpose alert and returns (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-459,524-539`). The refund example really does throw a 422-mapped domain exception (`apps/api/app/Modules/Fiscal/Application/Services/RefundCompensationService.php:184-193`; `apps/api/bootstrap/app.php:487-496`), so the two examples do not instantiate the same branch. A purpose can therefore satisfy neither REQUIRED nor CONDITIONAL as written, and a manifest entry can pass the proposed ratchet while violating the policy.

Required change: make the classification contract mechanically decidable. At minimum, define explicit allowed gate kinds and evidence per call site, state what the rule can validate, and add a test that every CONDITIONAL entry has an allowed gate. Either admit a durable alert-and-return precheck as a separate conditional gate and specify its accounting safety, or make tolerance purposes required; do not cite it as a 4xx example. The ratchet must fail a deliberately misclassified registered call, not only an omitted call.

#### R5-03 — P2 should-fix — Rev 5 retains contradictory counts and country scope text

The manifest heading still says “16 REQUIRED-UNCONDITIONAL cases” immediately before adding case 17 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-126`), while later sections call it a 17-case manifest (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:169,247`). Separately, the runtime-inventory summary still says the five expense codes join only the TN protected set (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:37`), while the publish gate and consumer contract correctly say TN or FR (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,154`). These are implementation-facing contradictions in the same revision.

Required change: update the heading to the corrected required count after R5-01 and change the §1 summary to TN+FR. Keep one named constant/source of truth for the count and protected country set so future revisions cannot drift across sections.

FINAL VERDICT: REJECT — 4 CLOSED, 3 STILL PARTIAL, 1 REOPENED; 3 new findings (2 P1, 1 P2). Rev 5 fixes the originally identified SalesDiscount and France-624 omissions, but its replacement rule requires two additional France purposes, its fixture is no longer exhaustive, and its AST-enforcement claim does not enforce classification correctness.

## Round 6

Rev 6 was reviewed against commit `f5031ae17` and the live call graph. Result for the six requested carried items: **2 CLOSED, 4 STILL PARTIAL, 0 REOPENED**. R5-01's advance purposes, France fixture, and backfill lane are corrected, and R5-03's stale 16/TN-only text is corrected. The classification redesign is materially more honest, but the proposed 19/12/10 partition still fails its own gate rule on live voucher consumers, and `ALERT_AND_SKIP` is not durably fail-safe in the cited implementation.

### Closure register

| ID | Status | Grounded result |
|---|---|---|
| R5-01 | CLOSED | The manifest now adds `CustomerAdvance` and `SupplierAdvance` as cases 18/19 with the verified live paths (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-126`; `apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:307-365`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:396-417,496-517`; `apps/api/app/Modules/Treasury/Domain/Services/VendorRefundService.php:153-175`). France's pre-certification set, exact six-row FR delta, and existing-tenant backfill lane all include both purposes and code `624` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-177,266`). The frozen charts confirm TN/Generic have both and FR has neither (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:178-186`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:132-143`; complete FR definition `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`). |
| R5-02 | STILL PARTIAL | Rev 6 now defines the three gate kinds, limits PHPStan to registration, assigns semantic dominance to review, and specifies a conformance suite with a deliberately bad fixture (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127-128,236`). Refund compensation genuinely satisfies `DOMAIN_PRECHECK_4XX` (`apps/api/app/Modules/Fiscal/Application/Services/RefundCompensationService.php:184-193`; `apps/api/bootstrap/app.php:487-496`). But five live voucher purposes satisfy none of the three gates (R6-01), and the tolerance alert can fail without preventing the skip (R6-02). |
| R5-03 | CLOSED | The heading now says 19, and §1 now says the five literal expense codes are protected for TN and FR (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:37,125`). The normative protected-code sections remain consistent (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:131,155`). A narrower single-source wording defect remains as R6-04. |
| R4-01 | STILL PARTIAL | SalesDiscount and both advances are now correctly REQUIRED, but the required set is still incomplete: five ungated voucher-account purposes are live throwing consumers and must also be required under the same rule (R6-01). |
| R2-02 | STILL PARTIAL | The stated TN and FR fixture deltas are now factually correct for the 19 named purposes (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-177`). Moving the five voucher purposes to REQUIRED does not add fixture deltas because all three frozen charts already contain them (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:297-309`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:317-329`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:224-234`), but the operational manifest itself remains incomplete (R6-01/R6-03). |
| F-02 | STILL PARTIAL | The numeric arithmetic is correct—19 + 12 + 10 = the 41 enum cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:14-103`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-128`)—but the classification is not: at least five members of the unnamed CONDITIONAL bucket have no allowed gate, and the 12 entries/evidence are not enumerated for review (R6-01/R6-03). |

### Classification and fixture verification

- **Advance paths:** PASS. `PaymentAllocationService` reaches `CustomerAdvance` for order allocations and excess payments (`apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:307-365`); the supplier-prepayment refund reaches `SupplierAdvance` (`apps/api/app/Modules/Treasury/Domain/Services/VendorRefundService.php:153-175`). Both GL methods throw on absence (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:396-417,496-517`).
- **France baseline and certified delta:** PASS for the named 19-purpose manifest. FR lacks `CostOfGoodsSold`, `GeneralExpense`, `SalesDiscount`, `CustomerAdvance`, and `SupplierAdvance` across its complete definition and lacks exact code `624` (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`). TN lacks only `SalesDiscount`; Generic lacks none. The Rev-6 delta table at `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:170-177` matches those facts.
- **Arithmetic:** PASS numerically, FAIL semantically. The enum has 41 cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:14-103`), so 19 + 12 + 10 sums correctly. Five voucher purposes currently counted outside REQUIRED are nevertheless live and ungated, making the proposed membership wrong even though the total is 41 (R6-01).
- **Refund compensation:** PASS as `DOMAIN_PRECHECK_4XX`. Both purposes are checked before the throwing GL call, absence raises a domain exception (`apps/api/app/Modules/Fiscal/Application/Services/RefundCompensationService.php:184-193`), and the generic handler maps it to 422 (`apps/api/bootstrap/app.php:487-496`).
- **Tolerance:** FAIL as the stated durable `ALERT_AND_SKIP` contract. The projection prechecks and returns without half-posting (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-459,524-539`), but its durable audit write is wrapped in a catch-all that logs and continues when persistence fails (`TreasuryReceiptBridge.php:567-583,596-629`).

### New Rev-6 findings

#### R6-01 — P1 blocker — Five live voucher purposes have no allowed conditional gate

The rule says a live throwing consumer is REQUIRED unless an allowed gate dominates it (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127`). Voucher operations are not module-gated: the Voucher routes use authentication and permissions but no `module:` middleware (`apps/api/app/Modules/Voucher/Presentation/routes.php:20-42`), POS routes are likewise not module-gated (`apps/api/app/Modules/POS/routes.php:36-43`), and `ModuleName` has no Voucher case (`apps/api/app/Enums/ModuleName.php:14-40`). A permission check is authorization for a live feature, not an optional-module gate.

The consumers are live. The goodwill endpoint invokes issuance (`apps/api/app/Modules/Voucher/Presentation/Controllers/VoucherController.php:186-250`), which always creates a voucher GL entry (`apps/api/app/Modules/Voucher/Application/Services/VoucherIssuanceService.php:295-356`). Refund/exchange issuance uses `SalesReturnsClearing` + `VoucherLiability`, goodwill uses `MarketingGoodwillExpense` + `VoucherLiability`, redemption uses `VoucherLiability` + `PosTenderClearing`, and rounding uses `VoucherLiability` + `RoundingLossExpense`; all are resolved through the throwing wrapper (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2563-2566,2619-2648`). Redemption and rounding are reached by live voucher services (`apps/api/app/Modules/Voucher/Application/Services/VoucherRedemptionService.php:184-204,226-253`). None has a `DOMAIN_PRECHECK_4XX` or `ALERT_AND_SKIP` account-absence guard.

Therefore `SalesReturnsClearing`, `VoucherLiability`, `MarketingGoodwillExpense`, `PosTenderClearing`, and `RoundingLossExpense` meet REQUIRED exactly as SalesDiscount and the advances do. All three frozen charts already contain the five purposes (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:297-309`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:317-329`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:224-234`), so correcting the manifest does not alter certified fixture deltas.

Required change: add these five purposes to REQUIRED, making at least 24 required cases. Recompute and enumerate the residual CONDITIONAL/SOFT partition (if the other membership is unchanged, 24 + 7 + 10 = 41), and add direct manifest-conformance cases proving the voucher routes have no qualifying gate. Alternatively, introducing a real optional Voucher module gate would be an application-scope decision outside this review and must not be inferred from ordinary permission middleware.

#### R6-02 — P1 blocker — `ALERT_AND_SKIP` does not guarantee the durable remediation signal its safety argument requires

Rev 6 permits a purpose to remain warning-only when an asynchronous projection records a **durable** operator alert before returning, and justifies tolerance on the basis that the skipped entry is visible for remediation (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127`). The cited projection does precheck before posting and returns cleanly (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-459,524-539`). However, `recordTolerancePurposeMissingAlertSafely()` catches every failure of the `audit_events` transaction, writes only a warning log, and returns control to the caller (`TreasuryReceiptBridge.php:567-583`). The caller then skips the accounting entry. The intended durable row is written only on the success path (`TreasuryReceiptBridge.php:596-623`).

That leaves a reachable state with neither GL entry nor durable remediation record—the same silent-books-integrity class the manifest treats as REQUIRED elsewhere. A conformance test that merely detects an alert-and-return branch would bless this unsafe implementation.

Required change: either classify both tolerance purposes REQUIRED, or make the durable alert a fail-closed/retryable prerequisite to acknowledging the projection (for example, transactional outbox/idempotent retry semantics). The `ALERT_AND_SKIP` conformance test must inject alert-persistence failure and prove the projection does not silently acknowledge and discard the accounting consequence.

#### R6-03 — P2 should-fix — The 12 CONDITIONAL entries are still not reviewable or mechanically reproducible from the spec

Rev 6 enumerates all 19 REQUIRED purposes but gives only refund and tolerance examples for the claimed 12 CONDITIONAL purposes (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-128`). The proposed tuple requires a purpose, exact call site, classification, gate kind, and evidence citation for every entry, yet those 12 tuples and the 10 SOFT memberships are absent. There is no implementation artifact to supply the missing source of truth before planning; the current PHPStan configuration contains only unrelated rules (`apps/api/phpstan.neon:33-37`).

This omission hides real ambiguities. For example, `PurchaseExpenses` is reached only through the bonus-return GL method (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2311-2370`); Procurement explicitly has no module middleware (`apps/api/app/Modules/Procurement/Presentation/routes.php:20-23`), while bonus input is instead controlled by an application-level `PurchaseBonusGate` in a FormRequest (`apps/api/app/Modules/Procurement/Presentation/Requests/CreateSupplierInvoiceRequest.php:68-72,140-148`; `apps/api/app/Modules/Procurement/Application/PurchaseBonusGate.php:17-36`). That does not match the conformance rule's stated “route middleware exists” assertion for `MODULE_GATE` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:128`). Whether the call is conditional, soft/unreachable, or required changes the 12/10 membership.

Required change: enumerate the complete 41-row manifest in the design (or a committed reviewable data artifact), including all 12 CONDITIONAL and 10 SOFT entries with exact gate evidence. Run the stated conformance logic against that table before implementation planning; a bare count cannot establish completeness.

#### R6-04 — P3 nit — The “never restate counts” invariant is contradicted in the same revision

Rev 6 calls the manifest class the single source of truth and says sections “never restate counts,” while the same section states 19/12/10 and the risk table repeats “19-case” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-128,250`). The stale-count defect is fixed today, but the proposed anti-drift invariant is not actually followed.

Required change: either remove the “never restate counts” claim and require a test that asserted documentation counts match the manifest, or replace prose counts with references generated from the committed manifest artifact.

FINAL VERDICT: REJECT — 2 CLOSED, 4 STILL PARTIAL, 0 REOPENED; 4 new findings (2 P1, 1 P2, 1 P3). Rev 6 correctly fixes the advances and France fixture, but the purpose partition still excludes five ungated live voucher accounts and the tolerance skip can lose its supposedly durable alert.

## Round 7

Rev 7 (`24c51ad7d`) was checked against the complete purpose partition, all three frozen COA definitions, the live invoice/credit-note preflight, voucher and tolerance paths, and fresh caller searches for `UninvoicedDeliveryNoteService`. Requested carried-item result: **4 CLOSED, 4 OPEN**. Rev 7 closes the voucher, tolerance, count-conformance, and fixture-delta gaps, but the partition still misclassifies the two sales-rounding absorbers and the new country-dependent `SCOPE-REQUIRED` rule is unsafe for mixed exact scopes and underspecified for wildcard fallback.

### Closure register

| Item | Status | Grounded result |
|---|---|---|
| R6-01 | CLOSED | Rev 7 promotes the five voucher purposes to REQUIRED cases 20–24 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-126`). The cited route group has authentication and permissions but no module gate, and `ModuleName` has no Voucher member (`apps/api/app/Modules/Voucher/Presentation/routes.php:20-42`; `apps/api/app/Enums/ModuleName.php:14-40`). Voucher ledger posting resolves both selected purposes through the throwing wrapper (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2563-2566,2619-2648`). All three frozen charts define all five (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:297-309`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:317-329`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:224-234`), so the no-delta statement is correct (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:174`). |
| R6-02 | CLOSED | `PaymentToleranceExpense` and `PaymentToleranceIncome` are now REQUIRED cases 25–26, and the classification contract retains only `MODULE_GATE` and `DOMAIN_PRECHECK_4XX` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:126,131`). The demotion of `ALERT_AND_SKIP` is justified by the actual catch-and-log path, which can return after alert persistence fails (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:567-583`); hardening is explicitly deferred in §13.4 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:271`). Both purposes already exist in TN, FR, and Generic (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:255,293`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:261,308`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:183,213`). |
| R6-03 | OPEN | Rev 7 now enumerates 27 REQUIRED, one SCOPE-REQUIRED, two CONDITIONAL, and eleven SOFT entries, and the arithmetic matches the 41 enum cases (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-130`; `apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`). `PurchaseExpenses` is conservatively REQUIRED: its application gate is `PurchaseBonusGate`, not either allowed route/precheck gate (`apps/api/app/Modules/Procurement/Application/PurchaseBonusGate.php:17-36`; classification rule at `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:131`). The claimed runtime deadness of `UninvoicedDeliveryNoteService` is also verified below. However, the two rounding purposes do not meet the stated “nullable-tolerated” SOFT rationale (R7-01), so the partition is not semantically correct. |
| R6-04 | CLOSED | The replacement is an explicit design-level count-conformance test: the manifest class becomes the implementation source of truth and a test must assert that the spec table and class agree (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125`), in addition to the manifest-conformance suite listed in the test plan (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:240`). No implementation artifact exists yet, consistently with this pre-planning design gate; the test obligation itself is now concrete rather than a “never restate counts” assertion. |
| R5-02 | OPEN | Registration enforcement and the two allowed gate kinds are now stated honestly (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:131-132`), and the formerly problematic voucher/tolerance entries are REQUIRED. Classification correctness is still wrong for the two rounding absorbers: their account-absence path is a pre-seal 422, which fits the substance of `DOMAIN_PRECHECK_4XX`, not SOFT (R7-01). |
| R4-01 | OPEN | The earlier SalesDiscount, advance, voucher, tolerance, and PurchaseExpenses omissions are corrected in the 27 REQUIRED entries (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-126`). The same decision framework is not yet applied consistently to rounding-account absence, and `SCOPE-REQUIRED` has no safe rule for a template certified across timbre and non-timbre countries (R7-01/R7-02). |
| R2-02 | CLOSED | Rev 7 finally supplies the complete 41-name fixture partition and an exact per-country baseline statement (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-130,171-180`). The five voucher purposes, two tolerance purposes, and `PurchaseExpenses` are present in every frozen chart (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:229,255,293,297-309`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:240,261,308,317-329`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:167,183,213,224-234`). The rounding correction required by R7-01 adds no fixture delta: FR/Generic already contain both rounding purposes (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:278,313`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:193,216`), while TN contains the preferred stamp-duty purpose (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:199-200`). |
| F-02 | OPEN | The inventory is numerically exhaustive—41 enum cases and a 27 + 1 + 2 + 11 partition (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-130`)—but semantic completeness still fails: two entries labeled SOFT can refuse ordinary document posting, and the country-dependent alternative is not safe under all permitted scopes (R7-01/R7-02). |
| R7-01 | NEW | The rounding purposes are labeled SOFT even though their absence produces a pre-seal 422 on ordinary positive-residual documents (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`; `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-233`; `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:95-109`). |
| R7-02 | NEW | A mixed TN/FR template necessarily contains the TN stamp account, while runtime absorber selection is based on account presence rather than company country, so FR residuals can be posted to the stamp liability (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,123,127`; `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-225`). |
| R7-03 | NEW | Production has no caller, but feature tests instantiate and call `UninvoicedDeliveryNoteService`, contradicting the literal “zero callers in the codebase” wording (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129,272`; `apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`). |
| R7-04 | NEW | §13 contains two items numbered 4 and two numbered 5, making “§13.4” ambiguous (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:268-275`). |

### Partition and caller verification

- **Voucher five:** PASS. The ungated live throwing path and all three fixture definitions match the Rev-7 citations (`apps/api/app/Modules/Voucher/Presentation/routes.php:20-42`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2563-2566,2619-2648`; `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:297-309`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:317-329`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:224-234`). No certified-fixture delta is introduced.
- **Tolerance pair:** PASS. Both are present in every frozen chart at the lines cited in the closure table. Rev 7 correctly removes the failed `ALERT_AND_SKIP` safety argument: the precheck returns without posting (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-459,524-543`), while its alert wrapper can swallow persistence failure (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:567-583`).
- **PurchaseExpenses:** PASS as REQUIRED under the written gate taxonomy. The account is used by the bonus-return GL path cited by the spec, while the actual `PurchaseBonusGate` is an application/config check rather than the allowed route/module middleware gate (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:2311-2370`; `apps/api/app/Modules/Procurement/Application/PurchaseBonusGate.php:17-36`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:126,131`). All three charts already define it (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:228-229`; `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:239-240`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:166-167`).
- **UninvoicedRevenue caller search:** PASS only when phrased as **zero production callers**. A fresh exact-symbol search across `apps/api/app`, routes, bootstrap, config, database seeders, console/scheduling, providers, and jobs found only the class declaration (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:31`); its public calculation method is called only internally (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:159,220`). Feature tests do instantiate and call it (`apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`; `apps/api/tests/Feature/Compliance/UninvoicedDeliveryNoteScalingTest.php:9,29,41,83`), so the broader “zero callers in the codebase” wording is inaccurate (R7-03).
- **Arithmetic:** PASS numerically: the enum contains 41 cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`) and 27 + 1 + 2 + 11 = 41. It fails classification semantics because the rounding pair belongs in a gated operational category, not SOFT (R7-01).

### New Rev-7 findings

#### R7-01 — P2 should-fix — The rounding-difference absorbers are not “nullable-tolerated” SOFT purposes

Rev 7 labels `SalesRoundingDifferenceIncome` and `SalesRoundingDifferenceExpense` SOFT because their lookup is nullable (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`). The nullable result is not tolerated. For any positive invoice or credit-note residual, `AccountingService` first looks for `SalesStampDutyPayable`, otherwise selects the applicable rounding purpose; if that account is absent, it records `NoAbsorbingAccount` and refuses the plan (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-233`). `assertDocumentGlIsPostable()` converts that refusal into `UnpostableDocumentGlException` (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:82-96`), and the live posting service runs this preflight before sealing the document (`apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:95-109`). The exception is a `DomainException` (`apps/api/app/Modules/Accounting/Domain/Exceptions/UnpostableDocumentGlException.php:21-39`) and therefore renders as 422 (`apps/api/bootstrap/app.php:487-496`). Ordinary two-line French invoice and credit-note tests demonstrate that real tax-truncation residuals use the income and expense absorbers respectively (`apps/api/tests/Feature/Accounting/DocumentGlPreflightTest.php:199-246,249-297`).

This is the exact safety shape the design calls a dominating 4xx precheck, not a harmless nullable absence. The SOFT conformance assertion is too weak because it checks only for a registered throwing lookup (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:132`); it would bless a template that predictably returns 422 for ordinary rounding cases.

Required change: move both purposes out of SOFT. Under the current two-gate taxonomy, classify them CONDITIONAL with explicit `DOMAIN_PRECHECK_4XX` evidence and state that a nullable lookup whose absence itself produces the dominating 4xx qualifies; the partition then becomes 27 REQUIRED + 1 SCOPE-REQUIRED + 4 CONDITIONAL + 9 SOFT = 41. If product policy instead requires all ordinary invoices/credit notes to remain postable, make both SCOPE-REQUIRED for non-timbre variants. In either case, add conformance tests that remove each absorber and assert the intended publish/preflight result. No fixture delta is needed because FR and Generic already contain both and TN contains the stamp-duty absorber (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:278,313`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:193,216`; `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:199-200`).

#### R7-02 — P1 blocker — `SCOPE-REQUIRED` is unsafe for mixed exact scopes and undefined for wildcard timbre fallback

The scope algebra expressly permits multi-country exact scopes and tests `['TN','FR']`; it also says `*` is validated as the non-TN generic variant and serves every country without an exact assignment (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,112,123`). The new rule requires `SalesStampDutyPayable` whenever a certified scope includes a timbre country, “TN today” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127`). That row-based union is not safe for a single template certified for both TN and FR: satisfying TN puts the stamp account in every company seeded from the template, and the runtime chooses the stamp account solely because it exists, before considering either rounding account (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-225`). A French company provisioned from such a template will therefore post a tax-rounding residual to a stamp-duty liability and bypass the non-timbre rounding-tolerance branch. This is a silent accounting misclassification, not just a missing validation.

Wildcard behavior is also unspecified. Registration accepts any two-letter code and missing exact assignments fall through to `*` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:112`), but wildcard validation is hard-coded conceptually as “non-TN” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,135`). The only current runtime tax-configuration registry knows TN and FR (`apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:10-31`); TN's tenant seeder creates active invoice/credit-note stamp configurations (`apps/api/database/seeders/TunisiaTaxConfigurationSeeder.php:89-118,136-145`), while France's records are not stamp duty (`apps/api/database/seeders/FranceTaxConfigurationSeeder.php:68-76`). The design names no central, versioned authority from which publish, assignment, and verify can determine that a newly supported country is a timbre country. Such a country could continue resolving through the generic wildcard template even after tenant tax provisioning starts creating stamp duty.

Required change: define one versioned central country-accounting capability registry consumed by `CertificationScope`, publish, assignment, verify, and provisioning. Make absorber selection country-aware before allowing mixed timbre/non-timbre exact scopes, so the mere presence of a stamp account cannot alter FR behavior. A country marked timbre-capable must require an exact compatible assignment (or the wildcard must be certified against an explicitly defined superset, with country-aware runtime selection); it must not silently inherit a non-timbre wildcard. Add tests for `['TN','FR']`, `['*']` resolving an unknown non-timbre country, and a newly registered timbre country without an exact assignment.

#### R7-03 — P3 nit — “Zero callers in the codebase” is broader than the verified fact

The SOFT rationale and cleanup note both say `UninvoicedDeliveryNoteService` has zero callers in the codebase (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129,272`). Production search found no wiring beyond the class itself (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:31,112-220`), so dead/unwired **production** code is a fair classification. The test suite nevertheless constructs and calls it (`apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`; `apps/api/tests/Feature/Compliance/UninvoicedDeliveryNoteScalingTest.php:9,29,41,83`).

Required change: say “zero production callers” and define the caller scan/conformance assertion over production roots, while retaining the tests as non-production references.

#### R7-04 — P3 nit — The follow-up list has duplicate ordinal numbers

After adding the tolerance and dead-code tickets as items 4 and 5, §13 restarts at 4, 5, 6 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:268-275`). This makes ticket references such as “§13.4” ambiguous between treasury hardening and four-eyes certification.

Required change: renumber the final three entries to 6, 7, and 8, or use stable ticket identifiers instead of mutable ordinals.

FINAL VERDICT: REJECT — 4 CLOSED, 4 OPEN; 4 new findings (1 P1, 1 P2, 2 P3). Rev 7 closes the voucher, tolerance, count-conformance, and fixture-delta gaps, but the new scope-dependent certification rule can silently misclassify non-timbre rounding when one template spans TN and FR, and the rounding absorbers remain incorrectly labeled SOFT.

## Round 8

Rev 8 (`0ee2800ab`) was checked against the live accounting preflight, refund precheck, country-tax provisioning registries and seeders, all 41 enum cases, and fresh production-root caller searches. Requested carried-item result: **7 CLOSED, 1 OPEN**. The rounding and manifest fixes are sound, the dead-code wording is now exact, and the follow-up numbering is repaired. R7-02 is not closed: the new stamp guard covers mixed scopes and `*`, but not exact non-timbre scopes, while the resolver section still mandates unconditional wildcard fallback.

### Closure register

| Item | Status | Grounded result |
|---|---|---|
| R7-01 | CLOSED | The rounding pair is now CONDITIONAL under `DOMAIN_PRECHECK_4XX`, with the nullable-lookup/direct-refusal shape explicitly admitted and a required negative test for each absorber (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129,132-133`). This matches the live path: absence sets `NoAbsorbingAccount`, preflight throws before sealing, and the exception renders 422 (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:82-96,212-233`; `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:95-109`; `apps/api/app/Modules/Accounting/Domain/Exceptions/UnpostableDocumentGlException.php:21-39`; `apps/api/bootstrap/app.php:487-496`). The existing positive-path tests demonstrate that ordinary invoice and credit-note residuals select the two different absorbers (`apps/api/tests/Feature/Accounting/DocumentGlPreflightTest.php:199-246,249-297`). Because this remains a pre-implementation design, the remove-each-absorber tests are specified obligations rather than existing test methods. |
| R7-02 | OPEN | Rev 8 introduces `CountryAccountingCapabilities` v1 `{TN}`, rejects timbre/non-timbre mixed exact scopes, defines `*` as non-timbre, prohibits the stamp purpose on wildcard templates, and says timbre countries require exact assignment (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,123,127-128`). Two safety holes remain: an exact non-timbre template is not prohibited from carrying the stamp purpose (R8-01), and §5.1 plus the country-catalog text still specify unconditional exact→`*` fallback (R8-02). |
| R7-03 | CLOSED | Both occurrences now say “zero production callers,” explicitly acknowledge feature-test use, and define the conformance scan roots as `app/`, `routes/`, `config/`, `database/`, and `bootstrap/` with tests excluded (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:130,273`). A fresh exact-symbol scan of those roots found only the class itself and its internal calls (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:31,112,159,220`); feature tests instantiate it directly (`apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`; `apps/api/tests/Feature/Compliance/UninvoicedDeliveryNoteScalingTest.php:9,29,41,83`). |
| R7-04 | CLOSED | §13 is now sequentially numbered 1 through 8, with treasury hardening unambiguously item 4 and four-eyes certification item 6 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:267-276`). |
| R6-03 | CLOSED | The complete named partition is now 27 REQUIRED, one SCOPE-REQUIRED, four CONDITIONAL, and nine SOFT (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-131`), matching the 41 live enum cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`). The two newly CONDITIONAL members have the verified pre-seal 422 evidence above; the refund pair has its dominating purpose precheck and 422-mapped exception (`apps/api/app/Modules/Fiscal/Application/Services/RefundCompensationService.php:184-193`; `apps/api/app/Modules/Fiscal/Domain/Exceptions/RefundCompensationRefusedException.php:35`; `apps/api/bootstrap/app.php:487-496`). No remaining partition member violates the written REQUIRED-versus-CONDITIONAL rule. |
| R5-02 | CLOSED | The design retains exactly two gate kinds, accurately limits PHPStan/AST to registration, assigns classification evidence to the PHPUnit conformance suite, and requires a deliberately misclassified failing fixture (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:132-133`). Rounding absence now fits the documented 4xx gate in the real preflight (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:82-96,212-233`), so the last known semantic misclassification is removed. |
| R4-01 | CLOSED | All operational omissions found in Rounds 4–7 remain in the 27 REQUIRED set, and the rounding pair is no longer mislabeled SOFT (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-131`). The classification totals cover every live enum member (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`). The remaining defects concern country-scope enforcement, not a missing operational-purpose case. |
| F-02 | CLOSED | The operational-purpose contract is now a complete 41-member partition with explicit evidence-bearing classifications and conformance responsibilities (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-133`); its arithmetic, 27 + 1 + 4 + 9, equals the 41 enum cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`). Fresh review found no fifth CONDITIONAL or tenth SOFT member that trips the rule. |
| R8-01 | NEW | Exact non-timbre scopes may still include `SalesStampDutyPayable`, even though runtime selects it solely by presence and will mispost rounding residuals (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,128,134-138`; `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-225`). |
| R8-02 | NEW | The new “timbre never falls back” invariant conflicts with the still-normative country-catalog and resolver algorithms that send missing exact codes to `*` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,112,128,146`). |
| R8-03 | NEW | The registry is called versioned, but certification stores no registry version and the design has no drift contract against the separate live country-tax registry/seeder facts (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:67-82,127,195`; `apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:10-31`). |

### Rev-8 verification

- **Rounding classification:** PASS. For a positive residual, the live service first looks for `SalesStampDutyPayable`, then the document-type-specific rounding purpose, and refuses if neither exists (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-233`). The refusal is thrown before sealing (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:82-96`; `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:95-109`) and maps to 422 (`apps/api/app/Modules/Accounting/Domain/Exceptions/UnpostableDocumentGlException.php:21-39`; `apps/api/bootstrap/app.php:487-496`). Rev 8 therefore correctly moves the two absorbers from SOFT to CONDITIONAL and requires one removal/refusal conformance case per absorber (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`).
- **Partition arithmetic and membership:** PASS. The enum contains 41 cases (`apps/api/app/Modules/Accounting/Domain/Enums/SystemAccountPurpose.php:17-103`), and the named spec sets total 27 + 1 + 4 + 9 = 41 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:125-131`). Moving the rounding pair changes no certified-fixture delta: FR and Generic already contain both (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:278,313`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:193,216`), and TN contains the stamp absorber (`apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:199-200`).
- **Capabilities rule:** PARTIAL. The new registry, mixed-scope rejection, wildcard prohibition, exact-assignment rule, and requested truth-table cases are all stated (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,123,127-128`). The actual accounting behavior proves why the negative stamp rule must apply to every non-timbre scope, not only `*`: account presence alone selects the stamp liability (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:218-225`). Sections 3.3 and 5.1 also retain the old unconditional wildcard algorithm (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:112,146`).
- **Production-caller and numbering edits:** PASS. The exact class is absent from all stated production roots except its own definition/internal calls (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:31,112,159,220`), while the acknowledged feature tests call it directly (`apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`; `apps/api/tests/Feature/Compliance/UninvoicedDeliveryNoteScalingTest.php:9,29,41,83`). Follow-ups are uniquely numbered 1–8 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:267-276`).

### New Rev-8 findings

#### R8-01 — P1 blocker — The stamp-purpose prohibition does not cover exact non-timbre templates

Rev 8 fixes mixed scopes and says only a `*` template “must not contain a stamp-duty-purposed account” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,128`). An exact non-timbre scope such as `['FR']` is valid (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:123`), and the exhaustive publish-gate rules impose purpose/type compatibility and `is_system=true` but no negative stamp-purpose rule on that scope (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:134-138`). The editor exposes a general purpose dropdown (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:233-234`), so a correctly typed/system-marked `SalesStampDutyPayable` row can be added to an FR-only template and certified.

That recreates the exact accounting defect R7-02 identified. `AccountingService` does not inspect country or tax capability; for any positive residual it chooses the stamp account solely because the purpose exists, before considering the rounding accounts or tolerance (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-230`). The supposedly safe FR-only template would silently credit a stamp liability for ordinary tax-rounding dust.

Required change: for **every** non-timbre certified scope—wildcard or exact—the publish gate must reject any `SalesStampDutyPayable` row. Assignment and `country-defaults:verify` must re-run the same invariant. Add negative tests for `['FR']` and another exact non-timbre country containing the stamp purpose, plus positive TN and non-timbre-with-rounding controls.

#### R8-02 — P1 blocker — The resolver specification still commands timbre countries to use wildcard fallback

The new normative rule says a timbre-capable country never falls back to `*` and must fail loudly without an exact assignment (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,128`). But the country-catalog section still says unknown two-letter codes resolve to wildcard and requires a test that a code absent from `CountriesSeeder` does so (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:112`). More directly, the dedicated resolver contract still ends with the unconditional algorithm “Resolution: exact country → `*`” (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:142-148`). G2 likewise describes wildcard fallback without the capability exception (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:41-46`). Both registration and additional-company validation accept arbitrary two-character country strings (`apps/api/app/Modules/Identity/Presentation/Requests/RegisterRequest.php:69-74`; `apps/api/app/Modules/Company/Presentation/Requests/CreateCompanyRequest.php:19-25`), so this is a live decision point, not unreachable prose.

An implementation following §5.1 will bypass the new safety rule and seed the non-timbre generic chart for a newly registered timbre country. The contradictory truth-table sentence in §4.2 does not make the resolver algorithm unambiguous.

Required change: make §5.1 authoritative and explicit: normalize → exact assignment if present → consult `CountryAccountingCapabilities` → throw a typed provisioning exception if timbre → otherwise resolve pinned `*`. Qualify G2 and §3.3 the same way. Test both registration and additional-company creation for a registry-declared timbre code with no exact assignment, alongside the existing unknown non-timbre wildcard case.

#### R8-03 — P2 should-fix — The “versioned” capability registry has neither certification provenance nor a drift contract

Rev 8 declares `CountryAccountingCapabilities` versioned and v1=`{TN}` (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:127`), but the complete `admin_templates` schema stores the certified codes and standard only—no capability-registry version (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:67-82`). Published scope is immutable (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,118`), while deploy verification re-executes the current gate (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:195`). The design does not say whether an old certification is interpreted under its original registry version or silently reinterpreted when the timbre set changes.

The new class is also not the only executable country-tax authority. Today `CompanyTaxProvisioningService` selects tenant tax seeders through the separate `CountryTaxConfigurationRegistry` (`apps/api/app/Modules/Taxation/Application/Services/CompanyTaxProvisioningService.php:22-35`; `apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:10-31`); the TN seeder creates active invoice/credit-note stamp configurations (`apps/api/database/seeders/TunisiaTaxConfigurationSeeder.php:86-121,136-145`), while France writes `is_stamp_duty=false` (`apps/api/database/seeders/FranceTaxConfigurationSeeder.php:50-76`). No specified test prevents a future tax-seeder change from drifting away from the central capability flag, which would defeat the resolver guard.

Required change: persist the capability-registry version used at publication (and include it in certification/audit evidence), define how verify/provisioning handle a newer registry version and recertification, and add a drift test tying every supported country’s effective tenant stamp configuration to `CountryAccountingCapabilities`. Adding a timbre tax seeder without the matching capability must fail CI and deployment verification.

FINAL VERDICT: REJECT — 7 CLOSED, 1 OPEN; 3 new findings (2 P1, 1 P2). Rev 8 closes the purpose-classification and editorial findings, but its core timbre safety rule is still bypassable by an exact non-timbre template and contradicted by the resolver’s own fallback algorithm.

## Round 9

Rev 9 (`36de1a917`) was checked against the full scope/version contract and the live resolver consumers, company tax provisioning, tenant tax-configuration API, tax calculation, credit-note persistence, and GL posting paths. Requested carried-item result: **2 CLOSED, 2 OPEN**. R8-01 and R8-02 are closed as written. R8-03 and therefore the end-to-end R7-02 safety invariant remain open: stale exact assignments bypass current capability checks, and the proposed seeder-only drift test does not cover tenant-authored tax configurations that drive the same GL requirement.

### Closure register

| Item | Status | Grounded result |
|---|---|---|
| R8-01 | CLOSED | Rev 9 applies the negative rule to every non-timbre scope, exact or wildcard, requires it at publish, assignment, and `country-defaults:verify`, and names the requested FR/second-country negatives plus TN and non-timbre-rounding positives (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`). This is the correct template invariant for the live presence-based absorber selection (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-230`). |
| R8-02 | CLOSED | G2 now qualifies wildcard fallback, §3.3 defers timbre behavior to §5.1, and §5.1 is an explicit authoritative sequence: normalize, exact assignment, current capability check with `TimbreCountryRequiresExactAssignmentException`, then pinned wildcard (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:44,113,147-152`). A fresh search found no remaining unconditional “exact → `*`” resolver contract. Both live input surfaces accept arbitrary two-character codes, so the named two-path tests are appropriately required (`apps/api/app/Modules/Identity/Presentation/Requests/RegisterRequest.php:69-74`; `apps/api/app/Modules/Company/Presentation/Requests/CreateCompanyRequest.php:19-25`; `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`). |
| R8-03 | OPEN | The schema now stores immutable `capability_registry_version`, publication records it, verification reports drift, and the tax-seeder/capability CI equivalence is specified (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:67-83,128,201`). The transition policy is unsafe: an existing exact assignment keeps serving and is returned before the resolver consults current capabilities (R9-01). The drift test also inventories only registered seeders, while authorized tenant users can create/update applicable document-level stamp configurations directly (R9-02). |
| R7-02 | OPEN | Steady-state template rules are now complete: scopes cannot mix capabilities, every non-timbre scope forbids `SalesStampDutyPayable`, and timbre countries without an exact assignment fail (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,124,128-129,147-152`). Full closure still fails across lifecycle transitions and the live tax-settings surface: stale exact assignments and tenant-authored document taxes can invalidate the certified COA/runtime pairing (R9-01/R9-02). |
| R9-01 | NEW | “Existing assignments keep serving” conflicts with exact-first resolution: a stale template is returned before the current capability check, so new companies can be provisioned under an obsolete capability certification (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79,147-152,201`). |
| R9-02 | NEW | The CI drift test covers seeders only, but the live management API permits country-scoped users to create/update active `DOCUMENT_TOTAL` and `is_stamp_duty` configurations, and every document-level total is persisted as `stamp_duty_amount` on credit notes (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:201`; `apps/api/app/Modules/Taxation/Presentation/Controllers/TaxConfigurationController.php:70-117,127-155`; `apps/api/app/Modules/Document/Presentation/Controllers/CreditNoteController.php:284-305`). |

### Rev-9 verification

- **All-scope stamp negative:** PASS at the template boundary. The rule expressly covers exact and wildcard non-timbre scopes, re-runs on assignment and verify, and lists the requested controls (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129`). The live accounting reason remains exactly as cited: any present `SalesStampDutyPayable` account wins before the rounding pair (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:218-230`).
- **Resolver algorithm:** PASS in steady state. G2 and the catalog defer to the authoritative algorithm (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:44,113`), whose four steps are unambiguous (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:147-152`). The requested typed failure is specified for both registration and additional-company paths (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:129,169-172`).
- **Capability provenance:** PASS structurally, FAIL operationally. The stored immutable version and seeder drift test now exist in the design (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79,201`), but the claim that existing assignments remain safe is contradicted by exact-first resolution (R9-01), and seeder parity is not equivalent to the actual mutable tax-configuration surface (R9-02).

### New Rev-9 findings

#### R9-01 — P1 blocker — Version drift blocks new assignments but not new-company provisioning through stale exact assignments

Rev 9 says a published template is interpreted under its stored capability version and, on drift, needs recertification only before a **new assignment**; existing assignments keep serving (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79`). The resolver, however, returns an exact assignment at step 2 and consults the current `CountryAccountingCapabilities` only when no exact assignment exists at step 3 (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:147-152`). Nothing checks the exact template’s stored version before it is used. The release verifier merely “reports” version drift while the entrypoint remains explicitly ungated (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:196,201-202`).

This breaks both direction changes. If a country changes from non-timbre to timbre, its old exact template was required to omit `SalesStampDutyPayable`; nevertheless new tenants/additional companies continue receiving it from the existing assignment, while current tax provisioning selects the current country seeder (`apps/api/app/Modules/Taxation/Application/Services/CompanyTaxProvisioningService.php:22-50`; `apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:10-31`). A credit note with positive stamp amount then reaches the throwing stamp-purpose lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:261-291`). If a country changes from timbre to non-timbre, the stale exact template still contains the stamp purpose, and ordinary rounding residuals are silently directed there solely by account presence (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-230`). In both cases, the design creates new companies from a certification it already knows is stale.

Required change: capability-version drift must block **provisioning**, not just new assignment mutations. Before returning an exact or wildcard assignment, the resolver must require the assigned template’s `capability_registry_version` to equal the current registry version (or prove a defined backward-compatible transition) and throw a typed recertification-required exception otherwise. `country-defaults:verify` must fail non-zero on drift, and the deployment/runbook must prevent a new registry version becoming active until each affected assignment is clone→publish→re-pointed. Tests must cover non-timbre→timbre and timbre→non-timbre transitions through both registration and additional-company creation; existing companies remain untouched per the owner ruling.

#### R9-02 — P1 blocker — Seeder drift is not runtime drift, and generic document-level taxes are treated as stamp duty

The proposed CI equivalence examines `CountryTaxConfigurationRegistry` seeders and active `is_stamp_duty=true` rows (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:201`). That does not describe all reachable runtime tax data. Authenticated tenant users with `taxation.tax_configurations.manage` can create and update country-scoped tax configurations (`apps/api/app/Modules/Taxation/routes.php:16-31`). Both endpoints accept `applies_to=DOCUMENT_TOTAL`, `is_active`, and `is_stamp_duty` without a country-capability check (`apps/api/app/Modules/Taxation/Presentation/Controllers/TaxConfigurationController.php:70-117,127-155`); the frontend exposes the stamp-duty toggle (`apps/web/src/components/organisms/TaxConfigFormModal/TaxConfigFormModal.tsx:258-270`), and the existing feature test proves the management API persists that flag (`apps/api/tests/Feature/Taxation/TaxConfigurationManagementTest.php:139-157`). A non-timbre company can therefore acquire an active stamp configuration after receiving a certified chart that is forbidden to contain the corresponding payable purpose. Seeder CI remains green.

There is a second, broader mismatch: the calculation service sums **every** applicable `DOCUMENT_TOTAL` configuration into `documentTaxTotal` without filtering on `is_stamp_duty` (`apps/api/app/Modules/Taxation/Domain/Services/TaxCalculationService.php:273-299`). Credit-note confirmation persists that whole value as `stamp_duty_amount` (`apps/api/app/Modules/Document/Presentation/Controllers/CreditNoteController.php:284-305`; the service path does the same at `apps/api/app/Modules/Document/Application/Services/CreditNoteService.php:96-116`). General Ledger then treats any positive value as actual timbre and performs the throwing `PurchaseStampDuty`/`SalesStampDutyPayable` lookups (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:261-291`). Thus even a non-stamp document-level tax can trigger the account that Rev 9 correctly forbids on non-timbre templates.

Required change: the capability invariant must govern the live tax-management API, not only seeders—reject creation, activation, or conversion to stamp duty for non-timbre countries and align the UI. Separately, calculate and persist `stamp_duty_amount` from `is_stamp_duty=true` taxes only; generic document-level taxes need their own correctly named total and GL treatment or must be rejected until that domain is designed. Add FR/non-timbre store+update negatives, a TN positive, and an end-to-end non-stamp `DOCUMENT_TOTAL` credit-note test proving it never enters the stamp-purpose path. This necessarily narrows the current tenant API, so §2 N3 (“no tenant-facing changes,” `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:54`) must be amended or the accounting runtime must become capability-aware instead.

FINAL VERDICT: REJECT — 2 CLOSED, 2 OPEN; 2 new findings (2 P1). Rev 9 closes the static template and fallback defects, but its version-drift policy still provisions from stale exact assignments, and its seeder-only capability check ignores the tenant-authored document-tax data that actually drives stamp accounting.

## Round 10

Rev 10 (`7727cbdd1`) was checked against the revised capability-version contract, both provisioning consumers, and every handover item A–J. Requested carried-item result: **2 CLOSED, 2 OPEN**. The exact and wildcard resolver branches are now fail-closed on version drift, and the owner-ruled G+H disposition is accurately recorded as a certification-claim precondition. R9-01/R8-03 are not fully closed because the prescribed pre-activation recertification sequence cannot produce a template certified under a registry version that is not active yet, and §6 still retains the superseded “reported/before new assignments” wording. The handover's core inventory is mostly sound, but C, E, and F need corrections before another session executes it.

### Closure register

| Item | Status | Grounded result |
|---|---|---|
| R9-01 | OPEN | The authoritative resolver now checks the exact assignment's stored version before use and applies the same check to wildcard; it names `TemplateRecertificationRequiredException` and requires both capability-transition directions through registration and additional-company creation (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:148-153`). The schema contract also says drift fails `country-defaults:verify` non-zero (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79`). However, its “recertify and re-point before the new version becomes active” sequence is not executable under the same set-at-publish/current-version model, and §6 still downgrades drift to a report needed only before new assignments (R10-01). |
| R9-02 disposition | CLOSED | N3 accurately names both residual defects, links handover G+H, states that tenant-authored tax data can still reach stamp accounting, and makes G+H a precondition for the per-country certification claim without expanding this template-scoped feature (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:50-54`). The live API accepts `is_stamp_duty` without a capability guard (`apps/api/app/Modules/Taxation/Presentation/Controllers/TaxConfigurationController.php:70-117,127-155`), and all document-total configurations are accumulated without consulting that flag (`apps/api/app/Modules/Taxation/Domain/Services/TaxCalculationService.php:273-299`), so the residual-risk statement is precise. |
| R8-03 | OPEN | Rev 10 has the required immutable provenance and fail-closed resolver/verify intent (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79,148-153`), while the seeder/capability parity check remains specified (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:202`). Lifecycle operation is still internally inconsistent: publication records the version certification was decided under, yet the runbook requires certification under a not-yet-active version, and the deploy section retains stale transition semantics (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79,202`; R10-01). |
| R7-02 (template layer) | CLOSED | At the template boundary, non-timbre exact and wildcard scopes forbid `SalesStampDutyPayable`, timbre countries cannot fall back to wildcard, and stale exact or wildcard templates cannot provision (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:78,128-129,148-153`). That closes the template-layer safety invariant against the live presence-first absorber selection (`apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:212-230`). End-to-end tax-surface certification remains expressly conditional on G+H, as required by the locked ruling (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:54`). |

### Rev-10 resolver verification

- **Exact and wildcard drift:** PASS as a design contract. Step 2b checks an exact assignment before use, step 4 explicitly applies the same check to the pinned wildcard, and the typed exception is named (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:148-152`).
- **Verify behavior:** PARTIAL. The data-model section says drift makes `country-defaults:verify` fail non-zero (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79`), but the deploy section still says drift is merely “reported” and requires recertification only before new assignments (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:202`).
- **Transition tests:** PASS as a stated implementation obligation: both non-timbre→timbre and timbre→non-timbre must refuse provisioning until re-pointed through both registration and additional-company creation (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:150-153`). The two current consumers remain the new-tenant path and additional-company path (`apps/api/app/Modules/Tenant/Application/Services/TenantInitializationService.php:199-210`; `apps/api/app/Modules/Accounting/Application/Services/ChartOfAccountsService.php:134-148`).
- **Registry-bump runbook:** FAIL. The requested no-stale-provisioning guard is sound, but the prescribed pre-activation preparation has no staged-version mechanism (R10-01).

### Handover A–J fact-check

| Item | Result | Grounded result |
|---|---|---|
| A | PASS | France's complete chart has no `CostOfGoodsSold` purpose (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`). The registered invoice listener calls the throwing COGS builder for physical-product lines and catches every resulting failure (`apps/api/app/Modules/Inventory/Listeners/PostCOGSOnInvoice.php:43-80,106-115`; registration at `apps/api/app/Modules/Inventory/Providers/InventoryServiceProvider.php:74-78`); the builder resolves COGS only after a positive total (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:1680-1721`). The separate write-off probe also returns false when COGS is absent (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:4347-4350`). |
| B | PASS | France lacks `GeneralExpense` across its definition (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`), and ordinary expense posting uses the throwing purpose fallback when no category account exists (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3913-3938`). The HTTP controller catches only `LinkedCostException`, so this bare `RuntimeException` reaches the catch-all 500 renderer (`apps/api/app/Modules/Expense/Presentation/Controllers/ExpenseController.php:213-238`; `apps/api/app/Modules/Accounting/Domain/Account.php:246-262`; `apps/api/bootstrap/app.php:919-938`). |
| C | PARTIAL | The account inventory and throwing lookup are correct: TN and FR omit `SalesDiscount`, Generic defines it (`apps/api/database/seeders/GenericChartOfAccountsSeeder.php:203-212`; complete TN/FR definitions at `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:121-311` and `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`), and a positive POS transaction discount resolves it without a precheck (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:3807-3827`). The “500 on POS account-charge” outcome is wrong for the device-authored production path: ingestion commits the event and queues projections after commit, while a throwing projector retries and eventually dead-letters (`apps/api/app/Modules/Fiscal/Application/Services/OutboxIngestor.php:228-241,891-977`; `apps/api/app/Modules/Fiscal/Application/Jobs/ApplyFiscalEventProjectionJob.php:391-421,433-448`; R10-02). |
| D | PASS | Order allocations and excess payments unconditionally reach the customer-advance GL builder when their business amounts are positive (`apps/api/app/Modules/Treasury/Application/Services/PaymentAllocationService.php:307-365`), whose first account dependency is the throwing `CustomerAdvance` lookup (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:396-417`). The direct smart-payment endpoint has authentication/permission middleware but no module gate (`apps/api/app/Modules/Treasury/Presentation/routes.php:35,253-259`). France lacks the purpose while TN and Generic define it (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`; `apps/api/database/seeders/TunisiaChartOfAccountsSeeder.php:180-186`; `apps/api/database/seeders/GenericChartOfAccountsSeeder.php:136-143`). |
| E | PARTIAL | The substantive defect is correct: the prepayment-refund service reaches the supplier-advance reversal after its domain guards, and the GL builder throws before writing when the purpose is absent (`apps/api/app/Modules/Treasury/Domain/Services/VendorRefundService.php:145-186`; `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:496-517`; complete FR chart at `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`). The handover citation `PaymentRefundController.php:212-230` points to refund-history code and a docblock, not the caller; the real route/action evidence is `apps/api/app/Modules/Treasury/Presentation/routes.php:214-217` and `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRefundController.php:231-262` (R10-04). |
| F | FAIL | The five literal mappings and silent fallback exist (`apps/api/database/seeders/ExpenseCategorySeeder.php:37-69`), France has `613/615/616/626` but not `624` (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:243-252`), and the France-default parapharmacy path runs the category seeder (`apps/api/database/seeders/ParapharmacySeeder.php:141-162,630-650`). But France also lacks `GeneralExpense`; consequently the fallback is `null`, and the seeder skips the Transport row rather than collapsing it to general expense (`apps/api/database/seeders/ExpenseCategorySeeder.php:54-69`; complete FR chart at `apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339`; R10-03). |
| G | PASS | Store accepts `applies_to`, `is_active`, and `is_stamp_duty`; update accepts the two flags, and neither path checks country capability (`apps/api/app/Modules/Taxation/Presentation/Controllers/TaxConfigurationController.php:70-117,127-155`). Both writes require the named permission (`apps/api/app/Modules/Taxation/routes.php:16-31`), the UI exposes the toggle (`apps/web/src/components/organisms/TaxConfigFormModal/TaxConfigFormModal.tsx:258-270`), and the feature test proves persistence (`apps/api/tests/Feature/Taxation/TaxConfigurationManagementTest.php:139-157`). The current seeder registry and TN/FR stamp facts also match the handover (`apps/api/app/Modules/Taxation/Application/Registries/CountryTaxConfigurationRegistry.php:10-31`; `apps/api/database/seeders/TunisiaTaxConfigurationSeeder.php:85-150`; `apps/api/database/seeders/FranceTaxConfigurationSeeder.php:52-78`). |
| H | PASS | Every applicable `DOCUMENT_TOTAL` tax increments `documentTaxTotal` while `is_stamp_duty` is copied only as row metadata (`apps/api/app/Modules/Taxation/Domain/Services/TaxCalculationService.php:273-299`). Both credit-note confirmation implementations persist the aggregate as `stamp_duty_amount` (`apps/api/app/Modules/Document/Presentation/Controllers/CreditNoteController.php:284-307`; `apps/api/app/Modules/Document/Application/Services/CreditNoteService.php:96-116`), and a positive value triggers both throwing stamp-purpose lookups (`apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:261-291`). |
| I | PASS | Both tolerance paths precheck and return without posting when a purpose is absent (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:441-463,524-543`). The alert wrapper catches every persistence failure and returns after only logging, while the durable `audit_events` write exists only in the success path (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:565-627`). |
| J | PASS | A fresh exact-symbol search over the stated production roots found only the service's own declaration/internal methods (`apps/api/app/Modules/Compliance/Services/UninvoicedDeliveryNoteService.php:31,112,159,220`). Direct consumers remain confined to feature tests (`apps/api/tests/Feature/Compliance/UninvoicedDNReportTest.php:13,39,53,203-229`; `apps/api/tests/Feature/Compliance/UninvoicedDeliveryNoteScalingTest.php:9,29,41,83`). |

Handover total: **7 PASS, 2 PARTIAL, 1 FAIL**.

### New Rev-10 findings

#### R10-01 — P1 blocker — The registry-bump runbook requires an impossible publication order

The schema contract says `capability_registry_version` is set at publication to the registry version under which certification was decided, and the resolver compares it to the **current** registry version (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79,148-152`). The same row requires operators to clone, recertify, and re-point every affected assignment **before** the new registry version becomes active (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79`). With no staged/future registry-version concept anywhere in the design, publication before activation can record and validate only the old version; once the new version becomes current, those just-published templates are immediately stale. Activating first would make the resolver correctly fail closed until recertification, but violates the stated pre-activation sequence and creates the provisioning outage the runbook purports to avoid.

The deploy contract also retains Rev-9 semantics: it says `verify` merely reports drift and recertification is required only before new assignments (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:202`), contradicting the non-zero/provisioning-block language at `docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:79`.

Required change: define one executable transition protocol. Either introduce staged registry versions plus an explicit active-version pointer, allow publish/certification against a nominated inactive version using that version's capability data, re-point atomically, and activate last; or explicitly accept a fail-closed maintenance sequence (activate → provisioning blocked → clone/publish/re-point) and remove the impossible “before active” promise. In both cases, make §6 say drift fails `country-defaults:verify` non-zero and blocks provisioning, and add a transition test that exercises the chosen activation ordering—not only the eventual resolver refusal.

#### R10-02 — P2 should-fix — Handover C mislabels an asynchronous projection failure as an HTTP 500

`TreasuryAccountChargeBridge` does perform the unprechecked posting call (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryAccountChargeBridge.php:59-101`), so the missing-purpose defect and backfill are real. But device-authored events are stored before projection jobs are enqueued after commit (`apps/api/app/Modules/Fiscal/Application/Services/OutboxIngestor.php:228-241,960-977`). A throwing bridge advances retry accounting and is retried, then dead-lettered after exhaustion (`apps/api/app/Modules/Fiscal/Application/Jobs/ApplyFiscalEventProjectionJob.php:391-421,433-448`). It does not turn the already-completed POS ingestion into the claimed “500 on POS account-charge.”

Required change: retitle handover C and its consequences as a retrying/dead-lettered Treasury GL projection (the printable/device event may already exist while its accounting projection is absent), and point the fixing session at projection recovery/backfill behavior in addition to seeding the missing account. Correct the same “500s” shorthand in the spec's follow-up inventory (`docs/superpowers/specs/2026-08-08-country-defaults-super-admin-design.md:278`).

#### R10-03 — P2 should-fix — Handover F describes a fallback that cannot occur on the current France chart

The category seeder first resolves a nullable `GeneralExpense` fallback, uses it when literal `624` is absent, and skips insertion if the result is still null (`apps/api/database/seeders/ExpenseCategorySeeder.php:54-69`). France lacks both literal `624` and the `GeneralExpense` purpose (`apps/api/database/seeders/FranceChartOfAccountsSeeder.php:124-339,243-252`). Therefore the France Transport category is silently **omitted**, not linked to GeneralExpense. `Fournitures & Divers`, whose mapping is intentionally null, is omitted for the same reason. This matters to the fixing lane because adding only `624` repairs Transport but does not repair the null-mapped category; the separate GeneralExpense backfill is also required.

Required change: rewrite handover F with the actual skip behavior and coordinate it explicitly with B. Add tests that first pin the current missing-category state, then prove the combined `624` + `GeneralExpense` backfills create all expected categories without fallback ambiguity.

#### R10-04 — P3 nit — Handover E's controller citation points at the wrong method

The cited `PaymentRefundController.php:212-230` covers `getRefundHistory()` and the next method's docblock, not the prepayment-refund call (`apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRefundController.php:211-230`). The actual action calls `VendorRefundService::refundPrepayment()` at `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRefundController.php:231-262`, and its route is `apps/api/app/Modules/Treasury/Presentation/routes.php:214-217`.

Required change: replace the handover citation so the main fixes session lands on the live caller.

FINAL VERDICT: REJECT — 2 CLOSED, 2 OPEN; 4 new findings (1 P1, 2 P2, 1 P3). Rev 10 closes stale-template provisioning at the resolver boundary and correctly records the G+H scope ruling, but its registry transition cannot be executed in the stated order, §6 contradicts the new fail-nonzero rule, and the handover needs three factual/citation corrections before it is safe to execute.
