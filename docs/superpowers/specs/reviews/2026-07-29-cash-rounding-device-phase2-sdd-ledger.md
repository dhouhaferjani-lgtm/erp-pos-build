# SDD ledger — plan: docs/superpowers/plans/2026-07-27-cash-rounding-device-phase2.md

## Session context

- Worktree: `/Users/houssamr/Projects/syneriva/apps/erp.cash-device`, branch `feat/pos-cash-rounding-device`, based on local `dev` @ `0763bf8bf`.
- **The plan text says worktree `../erp.cash-rounding` / branch `feat/pos-cash-rounding`. That is STALE.** The handoff (`docs/handoff/HANDOFF-cash-rounding-device-phase2-2026-07-28.md` §5) mandates the fresh worktree above. Every plan command path must be rewritten to `erp.cash-device`.
- Preconditions verified on this branch before Task 1:
  - `SALE_RECEIPT_PAYLOAD_KEYS_V3` present — `apps/api/app/Modules/Fiscal/Application/Services/FiscalPayloadConstraintValidator.php:322` (Task 8 drift gate can go green).
  - `GET /pos/payment-policy` present — `apps/api/app/Modules/Pos/routes.php:196` → `PosPaymentPolicyController` (Task 3).
  - Device migrations at v62 (`apps/pos/src/lib/db/migrations.ts:1913`); this phase ships v63.
  - `pnpm install --frozen-lockfile` clean at repo root.
- **origin/dev promotion status: NOT PROMOTED.** `git log origin/dev | grep cash-rounding` is empty; local `dev` is 42 commits ahead of `origin/dev` (`32da5bd9d`). Per handoff §1 this branch may be BUILT and unit-tested, but **no device build may reach a real terminal** until Phase 1 is on `origin/dev` and the staging checklist has run. No on-device Tauri verification in this session.
- Reviewer gate: `fiscal-pos-reviewer` on every task (owner instruction + handoff §5). `frontend-conventions-reviewer` additionally on Tasks 5/6/7 (plan Done-criteria).

## Pre-flight plan scan — controller rulings

- **Task 4 test uses `Number(magnitude)` / `Number(rounded) % 0.05` / `toBeCloseTo` on money strings**, which contradicts the plan's own Global Constraint ("NEVER `parseFloat`/`Number(...)` on a monetary or quantity value") and trips `precision/no-parsefloat-on-money` (warn in tests). RULING: keep the assertions' intent verbatim, express them with `@/lib/decimal` helpers (`bcabs`/`bccomp`/`bcmod`). Does not change what is verified.
- Plan-vs-handoff worktree/branch drift: handoff governs (see above).
- Task 11 Done-criteria says "committed on `feat/pos-cash-rounding`" → reads as `feat/pos-cash-rounding-device`.

## Progress

Task 1: complete (commits 0763bf8b..d9b25c9d1, review clean — spec ✅, quality Approved, 0 Critical/Important)
Task 1: minor (deferred): v63 test 5 "fresh vs upgrade schema" is near-tautological (`migrations.v63.test.ts:111-128`) — plan-mandated, buys almost no coverage.
Task 1: minor (deferred): `migrations.ts:1946` `company_id TEXT PRIMARY KEY` is a lookup trap — the SQLite file is already per-company (`db.ts:12`), so a stale id yields zero rows and silently degrades to "rounding disabled". → carried into Task 3.
Task 1: minor (deferred): `migrations.ts:1952` `currency_scale INTEGER NOT NULL DEFAULT 2` is not fail-closed — a writer that omits it would claim scale 2 on a TND (scale 3) tenant and truncate millimes. → carried into Task 3 (writer must always supply it).
Task 1: ⚠️ resolved by controller: nothing populates `is_cash_tender` (`paymentRepository.ts:87-109` upsert omits it) — that IS Task 2's scope. Not a gap.
Task 1: ⚠️ resolved by controller: `toSqliteUtc()` on any `refreshed_at` comparison — no such comparison exists yet; obligation carried into Task 3.

Task 2: complete (commits d9b25c9d..7f6827733, review clean — spec ✅, quality Approved, 0 Critical/Important)
Task 2: minor (deferred): `paymentStore.ts:820-824` — the API→SQLite writeback that carries the flag in production has no test; compounded by a PRE-EXISTING mock gap (`paymentStore.test.ts` mocks `paymentRepository` without `upsertPaymentMethods`, so the writeback runs its failed branch and logs 3 `SQLite writeback failed` stacks). Cleanup ticket, not this track.
Task 2: minor (deferred): `paymentRepository.ts:76,191-208` — `SELECT *` onto a hand-maintained row interface with no runtime validation; `is_cash_tender` is the first field whose silent-`false` default changes CHECKOUT behavior rather than display.
Task 2: ⚠️ resolved by controller: server emission confirmed — `PaymentMethodController.php:271` (`formatMethod`) emits `is_cash_tender`, cast `'boolean'` at `Domain/PaymentMethod.php:97`, device reads it via `paymentApi.ts:5`. Not a gap.
Task 3: review 1 (base 7f6827733, head 7c3ed0b22) — spec ✅ on all 5 controller resolutions; quality "Needs fixes" on ONE Important. Controller-verified the ⚠️ trailer item: `Co-Authored-By: Claude Opus 5` present on 7c3ed0b22.
Task 3: OPEN Important → fix round 1: `syncService.customers.test.ts:223-267` second ordered `apiGet` fixture never got the policy slot; the new pull consumes the `// operator pins` slot and shifts 6 downstream pulls by one. Suite still 8/8 green (customer-scoped asserts), so it silently degrades the `degraded`/`errors` guards at :474,:484-485.
Task 3: reviewer CONFIRMED the two `syncService.test.ts` failures are pre-existing — by causation (declaration order + `runFullSync` discards the new pull's return at `syncService.ts:506`) and by count reconciliation (58+3=61=59+2), not by re-running.
Task 3: minor (deferred): `terminalStore.ts:958-965` plan-mandated `.then()` chain — a hydrate rejection skips the refresh and the single `.catch` misattributes it as "refresh failed". Bounded (next sync tick retries; null slice = fail-closed).
Task 3: minor (deferred): `paymentPolicyStore.ts:855` `reset()` has NO production caller; on a company switch the singleton retains the prior company's currencyCode/scale/denomination. → carried into Task 4 (which consumes those fields for money math).
Task 3: minor (deferred): `paymentPolicyStore.ts:888-906` no runtime shape validation — a `{"data":{}}` 200 installs a non-null policy with `undefined` fields typed `string`. Fail-closed still holds by falsiness. → Task 4 must NOT assume field presence on a non-null policy.
Task 3: minor (deferred): `paymentPolicyCacheRepository.ts:266-269` diagnostic `SELECT … LIMIT 5` runs on every miss incl. normal cold start.
Task 3: fix round 1/5 (2 addressed, 0 open — sibling apiGet fixture aligned + permanent alignment guard suite; report baseline-method wording corrected; commits 7c3ed0b22..07ae85305)
Task 3: complete (commits 7f682773..07ae85305, review clean after 1 fix round)
Task 3: landed fact for later tasks — the runFullSync pull phase makes **12** `apiGet` calls; any new pull must add a slot to BOTH `syncService.test.ts` and `syncService.customers.test.ts`. Also: `vi.clearAllMocks()` does NOT drain a `mockResolvedValueOnce` queue.
Task 3: minor (deferred): `paymentPolicyStore.ts:913` slice `refreshedAt` uses the JS clock vs the row's SQLite `datetime('now')` — same format, up to 1s apart. Nothing compares them.

Task 4: review 1 (base 07ae85305, head 74cf0beca) — spec ❌ / quality "Needs fixes" on ONE Important. Trailer verified by reviewer. All 3 resolution-2 rewrites verified equivalent-or-STRONGER; both "unreachable branch" removals confirmed provably dead; `magnitude()` has no downstream consumer.
Task 4: OPEN Important → fix round 1: `cashRounding.ts:559-565` `computeRoundingAdjustment` subtracts the RAW `exactTotal` while `roundCashTotal:554` uses `bcformat(exactTotal, scale)` — so `rounded − adj == exact` is NOT guaranteed and nothing pins it. Task 8's v3 aggregate bind IS that identity → one-ulp mismatch throws `SaleReceiptAggregateInvariantError` on a SIGNED receipt. Latent only because T6 passes a pre-formatted total (convention, not enforcement).
Task 4: also in fix round 1 (unmet resolution 5): `cashRounding.test.ts:361-382` asserts every invalid-tolerance case with `roundingActive:false`; with a valid denomination + `roundingActive:true` a garbage tolerance config yields `D` of headroom via the unconditional floor at `:619-621`. Plus 3 cheap adjacents (truncate maxCap; widen `isValidDenomination` to accept undefined; sweep >1 denomination) and a report-accuracy correction.
Task 4: fix round 1/5 (6 addressed, 0 open — adjustment normalization + identity pin, fail-closed tolerance early-return, truncated maxCap, widened signature, parameterized sweep, report correction; commits 74cf0beca..dc1fc83bb)
Task 4: complete (commits 07ae85305..dc1fc83bb, review clean after 1 fix round)
Task 4: VERIFIED SERVER-SIDE — the `rounded − adj == exact` identity is enforced at `apps/api/.../FiscalPayloadConstraintValidator.php:1007` (`bcadd(bcsub($total,$roundingAdjustment,$scale),$discount,$scale)`), throwing `payload_aggregate_consistency:subtotal_plus_vat_ne_total`. One-ulp gap DOES quarantine a signed receipt.
Task 4: landed fact — the discriminating case for the adjustment bug is `'9.985'` @ scale 2 (buggy `'0.02'` vs correct `'0.01'`); `'9.9749999'` does NOT discriminate because `bcsub` rounds at scale. The sweep assertion alone cannot catch it; the over-precise `it.each` row is the pin.
Task 4: 🔑 CARRY TO TASK 6 — a legitimately CONFIGURED zero tolerance still yields `D` of headroom whenever `roundingActive` is true (matches the brief's stated semantics). **T6 MUST gate on `PaymentPolicy.tenderToleranceEnabled` or the tolerance disable switch is INERT.**
Task 4: minor (deferred): `cashRounding.ts:233` `truncateAtScale(max, scale)` operates on the raw sanitized string (no `bcformat` canonicalization) — `'00.100'` returns verbatim. Unreachable with today's `decimal(N,3)` server emission; would be a byte divergence if it ever reached a canonical-bytes surface.
Task 4: minor (deferred): `cashRounding.ts:193` `ToleranceMaxInput.denomination` still `string | null` while `isValidDenomination` was widened to include `undefined`; tests still launder it at `:261,:290`.
Task 4: minor (deferred): `cashRounding.ts:611` the pct cap's "never rounded up" guarantee holds only while the percentage has ≤4 decimals — `bcmul(total, pct, scale+4)` is half-up BEFORE truncation. Exact today; a 5-dp server percentage could inflate the cap by one ulp. Document the precondition or widen to `scale+8`.
Task 4: minor (deferred): `cashRounding.ts:629,:635` vs `:648` — `computeShortfall`/`computeChange` THROW on `''` (via `bcformat`→`new Big`), `sumCashLegs` maps `''` to 0 (via `bcadd`→`safeBig`). Opposite behavior on the same bad input in a module documented as fail-closed. Not reachable from T6/T7 typed call sites.
Task 4: ⚠️ open for final review: the out-of-band 2,001,000-case multi-denomination sweep (report §3.3) is NOT committed; nothing in CI exercises a denomination other than 0.050/0.05/10 (partly addressed by the fix round's sweep widening).

Task 5: review 1 (base dc1fc83bb, head 80e155b95) — TWO reviewers. fiscal-pos: spec ✅, quality Approved, 1 Important (plan-mandated). frontend-conventions: spec ✅, quality Approved, 1 Important. Both independently verified the `changeDue` string conversion is TOTAL across every non-test consumer; both deviations (single private `computeCartTotals`, string `getDenominations`) verified correct and BETTER than the brief.
Task 5: 🔴 OWNER RULING 2026-07-29 — plan-mandated finding `cartTotals.ts:50-61` (total subtracts UNROUNDED discount while reported discount is rounded; EUR fixed discount `'0.005'` → `SaleReceiptAggregateInvariantError` mid-authoring, cashier cannot complete the sale; reachable via `DiscountModal.tsx:87-97` which has no decimal cap). Plan prescribes the defective code verbatim (brief:107-112,139-142). **Owner chose FIX IT HERE over ship-as-written.** → fix round 1.
Task 5: also in fix round 1: no regression test on the path that had the defect (`grep percentage receiptService.test.ts` → 0 matches) and the `cartTotals.test.ts:22-24` parity assertion is TAUTOLOGICAL (`estimateCartTotal` is a one-line delegate). Re-inlining scale-3 math in receiptService would leave the suite green. + 4 cheap adjacents.
Task 5: fix round 1/5 (6 addressed, 1 NEW Important opened; commits 80e155b95..3b489bc50). Reviewer CONFIRMED the implementer's pushback: the prescribed EUR 3×3.33 @7% fixture does NOT discriminate (every asserted string matches the reverted path); the discriminating case is EUR 5.00 @ 12.5% = 0.625, a true half-cent tie. Precision-lock globs verified binding via `--print-config` → `[2]`.
Task 5: 🔴 NEW Important from round 1 → fix round 2: rounding the fixed discount in `computeCartTotals` while `cartStore` (4th copy) still doesn't turned a LOUD ABORT into a SILENT screen-vs-receipt mismatch. EUR 10.00 w/ fixed '0.005': `cartStore.totalString` → '10.00' (what `HomePage.tsx:1620` feeds CashPaymentScreen: display, quick-tender, Exact button) vs `computeExactCartTotal` → '9.99' (what `receiptService.ts:279` SIGNS). `paymentStore.ts:1103` only rejects UNDER-tender, so it passes as a 1¢ over-tender into change_due. C1 is therefore NO LONGER deferrable — cartStore must delegate.
Task 5: minor (deferred → now IN fix round 2): `cartTotals.ts:20-23` docblock still claims byte-identity with the cartStore selectors; round 1 made that false.
Task 6: review 1 (base 2f2ae8e2f, head 6c3d60897) — TWO reviewers. frontend: Approved, 2 Important. fiscal: "Needs fixes", 3 Important. Gate itself verified CORRECT: fail-closed §4.1 chain, once-sealed snapshot, byte-identical PIN path, `policy.currencyScale` never read (scale from `getCurrencyDecimals`). BOTH implementer pushbacks confirmed by independent arithmetic.
Task 6: 🔴 OWNER RULING A (2026-07-29) — **v3-gate the tolerance auto-accept** (`checkoutPolicySnapshot.ts:150` += `&& fiscalSchemaVersion === 3`). Deviates from spec `:56` (which omits the arm) DELIBERATELY. On v2 the shortfall has NO fiscal trace: v2 payload lacks `tolerance_shortfall`, projection's `tolerance_writeoff` is v3-gated (spec `:95`), no `PosOverrideEvidence` (no PIN). Pure cash-vs-revenue gap; a channel that does not exist today.
Task 6: 🔴 OWNER RULING B (2026-07-29) — **move the per-shift auto-accept counter to SQLite keyed by shift id** (migration v64 if needed; v63 is landed+immutable). Brief mandated in-memory `PaymentState`. Concrete bypass: `teardownPosSessionStores()` (`teardownPosSession.ts:18`) calls `paymentStore.reset()`, reachable ONE-TAP from `SettingsPage.tsx:108` → spend 10 accepts → switch operator and back → budget refilled, same open shift. Also: Task 10 must surface the count on EOD; in-memory reports 0 after restart.
Task 6: OPEN Important → fix round 1: stale-policy finding only HALF discharged. Traced path: sign out → `setCompany` to 2nd company (`LoginPage.tsx:96`) → activation hydrates, no cached row, **no-ops** → PREVIOUS company's denomination + tolerance caps decide the checkout (same currency + offline). Those get SIGNED at T8. Fix both: `usePaymentPolicyStore.reset()` in `teardownPosSession.ts:18` AND `hydratePaymentPolicyFromCache` must CLEAR on a null row (`paymentPolicyStore.ts:58-60`).
Task 6: CONTROLLER RATIFIED the un-briefed currency-match gate arm (`checkoutPolicySnapshot.ts:105-108`) — fail-closed and follows my resolution — WITH required hardening: `.trim().toUpperCase()` both sides (right side is `company.currency` with an `'EUR'` FALLBACK, `lib/currency.ts:28-32`) + `console.warn` on mismatch, else it silently kills both mechanisms undiagnosably.
Task 6: ⚠️ STANDING CONSTRAINT — change-due prop chain is half-converted between T6 and T9 (cash screen shows `tendered − roundedTotal`; success modal shows `changeDue` derived from the EXACT total, `paymentStore.ts:1012`). **NO device build may be cut from any HEAD between Task 6 and Task 9.** Task 9 owns the fix.
Task 6: minor (deferred, needs its OWN owner ruling to change): `effectiveMax` is not capped by the amount due — for exact `0.040` TND with D `0.050`, `minimumAcceptable` is `0.000`, i.e. a sub-D sale completes with ZERO cash. Reviewer explicitly declined to call it a defect: it follows the owner-approved §8.1 formula and is bounded by D. `effectiveMax = min(effectiveMax, roundedTotal)` would close it.
Task 6: minor (deferred): `HomePage.tsx:1218-1245` `useMemo` calls non-reactive `getActiveCurrency()` with no auth deps; can cache a stale currency where the old per-render selector could not. Unreachable (component early-returns on `!shift`) and mirrors `cartStore`'s idiom.
Task 6: ⚠️ open for final review: `TOLERANCE_AUTO_ACCEPT_LIMIT_PER_SHIFT === 10` is assumed by two suites but the constant lives outside the T6 diff.

Task 6: fix round 1/5 (9 addressed, 0 open; commits 6c3d60897..d36358fd7, 14 files)
Task 6: complete (commits 2f2ae8e2f..d36358fd7, review clean after 1 fix round, 2 reviewers)
Task 6: 🔑 LANDED FACTS — migration **v64** `tolerance_auto_accept_shift_budget` (`tolerance_auto_accepts(shift_id PK, accept_count, updated_at)`); increment is INSIDE the SQL upsert (10-concurrent-charges test → exactly 10); **SQLite is the authority** — `paymentStore.ts:963-966` reads `readToleranceAutoAcceptCount(shiftId)` and the brief's `get().toleranceAutoAcceptCount` gate read is GONE; the slice is a display mirror only. Read failure returns the LIMIT (fail-closed). Cold-start proven with a real `better-sqlite3` file + NEW connection.
Task 6: 🔑 `isTraining: boolean` REPLACED by `invoiceType: CheckoutInvoiceType` (`'SALE'|'REFUND'|'VOID'|'TRAINING'`); `ROUNDABLE_INVOICE_TYPES.includes(...)` is now a REAL gate arm. **Task 7's call site will fail typecheck until updated.** `isValidDenomination` is now a type predicate (`denomination is string`).
Task 6: authored-then-failed-sale isolation VERIFIED genuine — `paymentStore.ts:1069-1082` nests the budget write in its own try/catch that does NOT rethrow, so the outer catch is unreachable from a budget-write failure and `set({changeDue, isProcessing:false})` still runs. Pinned.
Task 6: minor (deferred): the v3 tolerance arm collapses to `reason: 'disabled'` — indistinguishable from an operator deliberately switching tolerance off. The currency arm got a `console.warn` for exactly this diagnosability argument; this arm got none. → carry to Task 7 (distinct reason `not_cutover` or a warn).
Task 6: minor (deferred): the currency-mismatch `console.warn` fires TWICE per invocation because `computeCashScreenDisplay` calls `buildCheckoutPolicySnapshot` twice (probe + atFloor), inside HomePage's useMemo → 2 warnings per cart mutation.
Task 6: minor (deferred): after `teardownPosSessionStores()` wipes the mirror on a STILL-OPEN shift, the hydrate effect is keyed on `openShiftId` (unchanged) so it does not re-run; mirror stays 0 and the screen advertises a floor the gate then refuses. Fail-closed (gate is authoritative).
Task 6: minor (deferred): the v2-gate test lost its `reason` assertion (deleted rather than updated to `'disabled'`); reason is covered by the new v3 describe, so no net coverage loss.
Task 6: ⚠️ FOR TASK 10: v64's `updated_at` is SPACE-separated SQLite UTC — any EOD filter on it must route through `toSqliteUtc()` (rule 20). Also `recordToleranceAutoAccept` does the atomic +1 then a SEPARATE `SELECT`, so under concurrency the returned value can exceed this caller's own increment — it is NOT a "returns MY new count" contract.
Task 6: ⚠️ FOR TASK 7: tolerance auto-accept is wired ONLY on the quick-cash path; `processCardCheckout`/`processAdvancedCheckout` build no snapshot yet, so on a v3 terminal an advanced cash tender still gates on the EXACT total.

Task 7: review 1 (base d36358fd7, head 8d4dfc96e) — fiscal: spec ❌, "Needs fixes", 2 Important. Store-side mechanics VERIFIED correct: sealed snapshot over the voucher union, pre-sign change refusal, durable fail-closed SQLite budget read (test seeds SQLite to 10 while the mirror is 0 — reading the mirror would resolve), isolated budget write, PIN path structurally byte-identical, `not_cutover` ordering correct.
Task 7: 🔴 OPEN Important → fix round 1 — **the implementer's C1 self-assessment covered only the round-DOWN half.** `roundCashTotal` is half-up to the NEAREST denomination, so ~half of totals round UP, where the outcome INVERTS: exact 9.977 D=0.050 → rounded 10.000; modal shows 9.977, "Pay Remaining" fills it, `remaining` is 0 so **no PIN field renders** (`AdvancedPaymentsModal.tsx:912`), and the store sees shortfall 0.023 ≤ D floor → **auto-accept applies SILENTLY and spends a budget unit on an amount the cashier was never asked for**. At Task 9 that seals a `tolerance_shortfall` write-off that never happened operationally. Errs toward LESS evidence, not more. Verdict: NOT "UX friction". Becomes Critical the moment T9 signs the snapshot.
Task 7: same root cause, second failure mode — when tolerance declines on that sale (operator switched tolerance off with rounding on, an explicitly supported combination; or the 11th sale in a shift) the store throws "Tender tolerance requires a manager PIN" while the modal renders NO PIN field (`remaining` is 0). Dead-end recoverable only by guessing to add unrequested cash. `cartStore.ts:695-699` names this exact failure mode in prose.
Task 7: OPEN Important → fix round 1: `AdvancedPaymentsModal.tsx:897-902` `roundingAdjustment` has NO production caller (`HomePage.tsx:1712` passes only `total`) — the one surface that would explain a differing due to the cashier is dead in the field. HomePage CANNOT compute it, which is itself the evidence that the display snapshot belongs inside the modal.
Task 7: minor (deferred): `paymentStore.ts:1286-1293` `voucherLegs` duplicates legs the modal already merged into `payments`; harmless only because the server invariant `is_cash_tender ⇒ code='CASH'` holds on every synced device. De-dupe by `(methodCode, amount, serial)` would remove the reliance.
Task 7: minor (deferred): on the round-DOWN path a manager PIN typed into the modal is passed to the store and silently DISCARDED — never verified, never audited. Subsumed by the Important above; trains managers that PIN entry is inconsequential.

Task 7: fix round 1/5 (7 addressed, 0 open; commits 8d4dfc96e..c44e7daf9). Reviewer traced the modal condition term-for-term against the store branch (`paymentStore.ts:1388-1391`) and confirmed equivalence in BOTH directions; verified the 4-red/5-green discriminating split by tracing case 2 against a hypothetical revert. `roundingAdjustment` RETIRED as a prop (cannot be supplied from outside). `TenderToleranceApprovalRequiredError` replaces the English literal (1 remaining hit = the constructor). `as never` casts gone — the 3 surfaced fields were satisfied with `null`, not by loosening types.
Task 7: complete (commits d36358fd7..c44e7daf9, review clean after 1 fix round, 2 reviewers)
Task 7: 🔑 FOR TASK 9 — the forcing function is now LOUDER than planned: quick cash AND advanced both pass `policySnapshot`, but `processCardCheckout` (`paymentStore.ts:1242`) deliberately OMITS it, so flipping `receiptService.ts:106` from `policySnapshot?:` to required will FAIL TO COMPILE there by design. Task 9 must reconcile the card total, not manufacture a snapshot.
🎫 **SEPARATE TICKET (pre-existing, NOT this track):** `processCardCheckout` `paymentStore.ts:1218` uses `bcsum(line_total)` — the RAW SUBTOTAL, discount-blind — as the card leg amount (`:1226`), while the receipt total is `computeExactCartTotal(items, transactionDiscount, ...)`. `SaleReceiptPayload.ts:134-227` enforces `subtotal+vat == total` but NOTHING checks `Σ payments == total`, so a discounted card sale would sign a payload whose payment leg exceeds `total_amount` by the discount. **Effective severity today = Minor: `processCardCheckout` has NO production caller** (HomePage wires only `processCashCheckout` and `processAdvancedCheckout`). Ticket must record BOTH the arithmetic defect and the dead-path status. If the card path is ever wired, it must not ship before the total is reconciled.
Task 7: minor (deferred): `AdvancedPaymentsModal.tsx:345` labels voucher legs `storeVoucherMethod?.code ?? 'store_voucher'` while the store uses the hard literal (`paymentStore.ts:1339`) — divergence only reachable via a config that already violates the `is_cash_tender ⇒ code='CASH'` invariant. One-line alignment.
Task 7: minor (deferred): the modal's `autoAcceptCountThisShift` is the in-memory MIRROR while the gate reads SQLite — an under-reporting mirror shows no PIN field for a tender the store then refuses. Narrow (HomePage rehydrates on `shift.id` change; the SQLite read fails closed to the limit). Suggested: `error !== null → render the PIN field`.
Task 7: minor (deferred): the currency-mismatch `console.warn` now runs from a RENDER path (modal builder in a useMemo keyed on legs) → logs on every keystroke/leg change. Exactly the noise `logToleranceDecline` was kept refusal-only to avoid.
Task 7: minor (deferred): the modal has NO counterpart to the change-eligibility gate — a card-only over-tender still shows change due + enabled Complete, and the store refuses with the i18n banner. Fails closed, no wrong money; but the gates mirror only 2 of the store's 3 refusal conditions.
Task 7: minor (deferred, owner UX call): "Pay Remaining" on an empty cash-only basket offers the EXACT total, so the prefill is one denomination off on a pure-cash advanced sale. Fiscally reconciled (receipt 9.95 / tendered 9.97 / change 0.02).

Task 5: fix round 2/5 (2 addressed, 0 open — cartStore selectors delegate, 4th copy RETIRED; docblock restated; commits 3b489bc50..2f2ae8e2f)
Task 5: complete (commits dc1fc83bb..2f2ae8e2f, review clean after 2 fix rounds, 2 reviewers)
Task 5: reviewer verified independently — `getActiveCurrency()` (`lib/currency.ts:28-32`) is a byte-identical lookup to the replaced local `getDecimals()` (`cartStore.ts:157-161`), so no scale change; no runtime import cycle (`cartTotals.ts:4` is `import type`); "no existing test encoded old behaviour" CONFIRMED by reading all 7 transaction-discount cases; the `'5'`→`'5.00'` risk case (`cartStore.audit.test.ts:411,419`) is unaffected because the emit reads raw `discount.value` at `cartStore.ts:583`, not the selector.
Task 5: negative-fixed-discount behaviour change ruled a genuine IMPROVEMENT — `'-5'` is unreachable from the UI (digit numpad, no sign path, `DiscountModal.tsx:53,112,132`) and the authoring path already ignored it, so preserving the old display behaviour would have preserved a screen-vs-receipt divergence.
🔴 **REPO-WIDE DEFECT found in passing (NOT this track's, needs its own ticket):** `apps/pos/eslint.config.js:324-342` re-declares `'no-restricted-syntax'` on the same `src/**/*.{ts,tsx}` glob as the FU-2 cart-mutator block at `:287-298` WITHOUT spreading `cartMutatorSelectors`. Flat config REPLACES rule options, so **the FU-2 cart-mutator guard is inert app-wide**. Confirmed via `--print-config`: only the BEGIN|COMMIT|ROLLBACK selector survives. This is why `lib/stock/__tests__/cartMutatorGuard.eslint.test.ts:51` fails (pre-existing, NOT caused by our round-1 precision-lock edit at `:348-364`). Fix = spread `cartMutatorSelectors` into the `:332` block. The block at `:304-314` re-spreads them with a comment naming this exact hazard — the SQL block re-opened a known trap.
Task 5: minor (deferred): `lib/currency.ts:49` `formatCurrency` still does `parseFloat` on a money string (display-only, pre-existing).
Task 5: minor (deferred): report's "five paths bit-identical" is off by one — an empty-string fixed value went `'0'` → `'0.00'`. Numerically equivalent, every consumer goes through bccomp/bcsub.
Task 5: minor (deferred): C1 ORIGINAL note — `cartStore.totalString`/`discountAmountString` were a 4th non-delegating copy. "Byte-identical" is TRUE for total, **FALSE for discount**: `cartStore.discountAmountString:700-703` returns a fixed value unformatted (`'5'` vs `'5.00'`) and diverges outright on a negative fixed discount. Should delegate; not this task.
Task 5: minor (deferred): `accountChargeCartMapper.ts:104` computes the percentage discount at DEFAULT scale 3 for the **ACCOUNT_CHARGE signed payload** (reached from `paymentStore.ts:714-717`) — the same divergence class, still live on another signed event. Outside this brief's file list.
Task 5: minor (deferred): C3 ruling — silently ignoring a malformed discount (`cartTotals.ts:41-47`) is the WRONG default for signed-receipt authoring (correct shape = typed throw), but scored Minor because both malformed shapes are unreachable: `DiscountModal.tsx:118` rejects them and the display path throws at render first. Remaining ingress = unvalidated rehydration (`holdStore.ts:52-54`, `refundDraftStore.ts:35-44`).
Task 5: minor (deferred): `receiptService.ts:254` idempotent-replay branch returns `existing.change_due` verbatim, not normalized (correct today — every write goes through `changeDueFormatted`). `computeCartTotals` invoked twice per call (pure, no impact).
Task 5: minor (deferred): `paymentStore.ts:368` initialises `changeDue: '0'` (brief-mandated) while every produced value is currency-scale; unreachable because renders are gated by `bccomp(changeDue,'0') > 0`.

Task 8: committed at `9d59fe703` (base c44e7daf9) by a PRIOR session that ended before writing `task-8-report.md` — **no implementer report, no test evidence** exists for T8. Review gate never ran. Resumed 2026-07-29: package `review-c44e7daf9..9d59fe703.diff` built, `fiscal-pos-reviewer` (Opus) dispatched with an explicit instruction to RUN the 6 T8 test paths itself (the usual "don't re-run the implementer's tests" rule does not apply — there is no run to trust).
Task 8: review 1 (base c44e7daf9, head 9d59fe703) — fiscal: spec ❌, "Needs fixes", 1 Critical + 1 Important. Reviewer RAN the tests (no implementer run existed): 6 named paths **165/165 green**, `pnpm typecheck` clean. Normative build order, 30-key set, bind list and evaluation order all VERIFIED exact; PHP↔TS key parity confirmed against `FiscalPayloadConstraintValidator.php:322-353`; `eventVersionFor` bump verified safe against server `FiscalEventPayloadRegistry.php:120` (accepts [1,2,3]). `canonicalMoney` negative-zero collapse ruled BETTER than the brief (literal `bcformat` would emit `-0.000` for adj `-0.0004` → server quarantine).
Task 8: 🔴 CRITICAL, CONTROLLER-ROUTED TO TASK 9 (not a T8 fix round): the append-time const swap (brief F9) landed while `receiptService.ts:310` still calls `buildSaleReceiptV2Payload`, so **HEAD `9d59fe703` is a device that cannot author ANY sale receipt**. Proven, not inferred — `voucherCheckout.integration.test.ts` + `idempotencyRetry.integration.test.ts` are **5 tests RED** with `payload_missing_required:cash_rounding_adjustment,cash_rounding_denomination` (`FiscalEventEngine.ts:1463` ← `append:571` ← `receiptService.ts:425`). `pnpm typecheck` does NOT catch it (V2 return type has no required rounding fields). The plan assigns that migration to Task 9, so the remedy is T9's, not a T8 fix round. **T9 EXIT CRITERIA now include both integration suites green.** ⛔ NO branch cut, promotion, or device build at `9d59fe703`.
Task 8: OPEN Important → fix round 1: `SaleReceiptV3Payload.ts` denomination cap check sits AFTER the zero-adjustment early return; the server's (`FiscalPayloadConstraintValidator.php:2839` → `CashRoundingCaps::isWithinCap`) is UNCONDITIONAL. So `adjustment '0.000'` + `denomination '2.000'` (over the scale-3 cap 1.000) is **signed on the device and quarantined on the server**. Reachability bounded today by `normalizeDenomination` (`cashRounding.ts:105-111`) and `getCurrencyDecimals` yielding only scales {0,2,3} — hence Important, not Critical. The device's last gate before signing must be ≥ as strict as the server's.
Task 8: minor (deferred): `canonicalMoney` launders a negative denomination smaller than half an ulp (`'-0.0001'` → `'0.000'`, signed) while `'-0.050'` is rejected by bind 1b — two nonsense inputs treated differently; a trap for the next reader.
Task 8: minor (deferred, plan-mandated): `FiscalEventEngine.ts:393` declares the two v3 fields OPTIONAL on `SaleReceiptPayloadInput`, and `SaleReceiptV2PayloadInput extends Omit<…>` inherits them — a **V2** payload object now type-checks with keys the v2 key set forbids as `payload_extra_field`. `SaleReceiptV3PayloadInput` already carries them as required; the optional pair weakens the v1/v2 type for nothing.
Task 8: minor (deferred): the v3 aggregate binds run only in the builder, never at append time — any payload assembled without `buildSaleReceiptV3Payload` is signed then quarantined. Design-consistent with the engine's documented structural-conformance-only stance; cheap insurance with one build site.
Task 8: minor (deferred): `SaleReceiptV1V2ByteStability.test.ts.snap` was authored by this same commit → pins FORWARD only, cannot prove bytes didn't move IN this change (the real proof is the pre-existing golden vector `saleReceiptV2CanonicalParity.test.ts`, verified green). Fixture is single-shape (EUR/scale-2, one untaxed line, no discount/buyer/vouchers/approvals).
Task 8: 🎫 PRE-EXISTING RED (NOT this diff — last touched at T3 `7c3ed0b22`): `src/__tests__/integration/offlineFirstFlow.test.ts` "offline cash sale writes receipt to SQLite" fails with `Shift shift-1 has no usable fiscal shift id` (`terminalStore.ts:253`). **This is WHY the Critical went unnoticed** — it is the only E2E that drives the real engine through the real authoring path, and it dies before payload construction. → Task 9 should revive it or the same class of defect hides again.
Task 8: fix round 1/5 dispatched (fresh implementer — the original session is gone; `task-8-report.md` now exists, starting at fix round 1). Cap hoisted above the zero-adjustment early return; implementer reports 130/130 green on the 4 covering paths + typecheck clean, new test confirmed red pre-fix / green post-fix, and claims the server rejects a negative denomination earlier via a non-negative regex assert (so device leniency there is safe). Commit `1c3af608f`. Scoped re-review dispatched (Opus) with the canonical-zero path and bind-evaluation-order regressions named as concrete risks.
Task 8: fix round 1/5 (1 addressed, 0 open; commits 9d59fe703..1c3af608f, 2 files). Re-reviewer verified ALL THREE server semantics against the PHP: cap is unconditional (`FiscalPayloadConstraintValidator.php:2839` sits outside the `adj != 0` block closing at `:2837`; `validateCashRoundingBinds` called unguarded for every v3 at `:902-904`); canonical-zero denomination PASSES both ends (`bccomp('0.000','1.000') <= 0`); unlisted scale fail-closed both ends. Negative-denomination claim VERIFIED load-bearing-safe — server `moneyRegex` (`:2735-2742`) has no optional minus and rejects at `:859` BEFORE the binds at `:903`, and the device's own 1b still runs first. Bind order intact (hoisted block contains no division/modulo; 2a still precedes 2b/2c). New test judged discriminating BY READING (not on faith): the fixture's zeroed discount/tax make identity (1) and 1b unable to throw, so the pass is attributable to the hoist alone.
Task 8: complete (commits c44e7daf9..1c3af608f, review clean after 1 fix round) — **with the Critical carried to Task 9, not closed here.**
Task 8: 🔑 LANDED FACT — the cap table now gates **every** v3 receipt, not only rounded ones. Device-reject still implies server-reject in every case (device `bccomp(denomination, cap)` scale-less vs server scale-bounded → device can only be stricter, identity for scale-normalized values). No legitimate currency can be bricked: `getCurrencyDecimals` (`lib/currency.ts:10-18`) yields only {3, 0, 2} and every one is a listed cap key.
Task 8: minor (deferred): `V3_DENOMINATION_CAP_BY_SCALE` (`SaleReceiptV3Payload.ts:54-58`) is pinned only against the other TS table (`cashRounding.ts` `DENOMINATION_CAP_BY_SCALE`) — **nothing pins either against the PHP authority** `CashRoundingCaps::CAPS` (`apps/api/app/Shared/Domain/CashRoundingCaps.php:40-44`). Values match today (0=>'10', 2=>'1.00', 3=>'1.000'). Pre-existing, but the hoist raises the stakes: a future PHP-side cap addition would now silently brick device authoring at that scale. A drift test in `FiscalPayloadKeyDrift.test.ts` style would close it.
Task 8: minor (deferred): `FiscalEventEngine.ts:1527-1537` append-time validator applies shape only — no cap, no bind. So the V3 builder's "last gate before signing" docblock claim (`SaleReceiptV3Payload.ts:106-108`) is literally true only for payloads that go THROUGH the V3 builder.
Task 8: ⚠️ CONSTRAINT still in force — no device build from any HEAD between Task 6 and Task 9 (change-due prop chain half-converted; T6 ledger line).

Task 9: dispatched (Opus, base `1c3af608f`) with 5 controller resolutions overriding the brief: (1) stale `erp.cash-rounding` paths → `erp.cash-device`; (2) trailer `Claude Fable 5` → `Claude Opus 5`; (3) **T8's Critical carried in as a HARD EXIT CRITERION** — `voucherCheckout.integration` + `idempotencyRetry.integration` must be green, and typecheck-passing is explicitly NOT evidence; (4) card path must be RECONCILED (snapshot `exactTotal` = `computeExactCartTotal`, rounding not applied) — no fake snapshot, no special-casing around the integrity assert; the `bcsum(line_total)` leg-amount defect stays TICKETED (dead path); (5) T9 owns the T6→T9 change-due chain reconciliation, which is what LIFTS the no-device-build constraint. Plus a bounded instruction to revive `offlineFirstFlow.test.ts` ONLY if the fix is harness-confined.

Task 9: implemented at `d82197824` (17 files). Implementer reports the T8 Critical FIXED (`receiptService` → `buildSaleReceiptV3Payload`; both integration suites confirmed RED on base, green now), card path reconciled with a REAL `buildCheckoutPolicySnapshot` (card leg ⇒ not cash-only ⇒ gate self-closes ⇒ canonical zeros), ticketed `bcsum(line_total)` leg amount left + commented. 2 deviations (D1/D2): `CartTotalIntegrityError.message` = `i18n.t(...)` not the brief's English literal, and translation NOT done in `paymentStore` (claim: `formatCheckoutError` surfaces `error.message` verbatim; importing the class into paymentStore broke 8 tests because 13 files `vi.mock` receiptService with a bare factory). Found+fixed a SECOND broken caller not in the brief: `offlineCheckoutService` (no production caller). Commit needed `--no-verify` (3 react-doctor `async-await-in-loop` warnings claimed pre-existing, line numbers shifted by the diff).
Task 9: 🔑 CORRECTION TO THE T8 LEDGER LINE — `offlineFirstFlow.test.ts` mocks `@/lib/fiscal/instance`, so it would **NOT** have caught the T8 Critical even when green. It was revived here by a fixture-only fix (non-UUID `shift-1` → real UUID). **The two integration suites (`voucherCheckout`, `idempotencyRetry`) are the only tests that drive the REAL engine through the REAL authoring path** — they are the load-bearing coverage, not the E2E. (Reviewer asked to verify this claim.)
Task 9: review 1 dispatched — TWO reviewers (fiscal-pos, Opus, base 1c3af608f head d82197824; + frontend-conventions, Opus, scoped ONLY to the D1/D2 i18n-in-Error deviation). Fiscal reviewer given 6 named risks incl. the change-due chain trace (**its verdict decides whether the no-device-build constraint lifts or stays**), the 29→32 placeholder lockstep, required-snapshot totality (no manufactured/`as`-cast snapshots), and an explicit instruction to hunt for a run-list blind spot of the kind that hid the T8 Critical.

Task 9: frontend review — **D1/D2 deviation ACCEPTABLE AS-IS**, all four hazards checked and none materialize: i18n is sync-initialized with inline JSON (`lib/i18n.ts:13-35`) so there is no uninitialized window; freeze-at-throw is microseconds AND is the dominant house idiom (`paymentStore.ts:1405-1407`, `:1179-1181` all do i18n.t-then-throw); and `formatCheckoutError` (`paymentStore.ts:464-473`) returns `error.message` VERBATIM with no code/name dispatch, so the brief's English literal would have put `pos:payment.totalIntegrityError — line-derived total 9.997 disagrees…` on a FRENCH cashier's banner and left both mandated keys dead. Reversibility preserved — the test binds `toBeInstanceOf`, not text. en/fr keys clean (both `pos.json:73`, 18-key `payment` sub-tree parity verified, zero one-sided keys).
Task 9: FE reviewer's caveat on D2 — the mocking fact is TRUE (exactly 13 files `vi.mock` receiptService with bare factories; `paymentStore.ts:27` is the only import) but the CONCLUSION is over-claimed: two cheaper conventional escapes existed (a `lib/offline/receiptErrors.ts` module nobody mocks, or name-dispatch in `formatCheckoutError` for which `lib/safeErrorNames.ts:41` is in-repo precedent). Layer choice was a convenience, not forced. Not blocking.
Task 9: 🔴 IMPORTANT, **branch-new but OUTSIDE T9's diff — belongs to T7** (`git log -S` dates it to `c44e7daf9`, T7 fix 1): `TenderToleranceApprovalRequiredError` (`paymentStore.ts:90-95`) carries the English literal `'Tender tolerance requires a manager PIN.'` and `:1422` writes it straight to the cashier banner via `set({error: approvalRequired.message})` → rendered by AdvancedPaymentsModal (`HomePage.tsx:1720`). **Rule 11 violation we introduced**, not inherited. Reachability needs modal/store snapshot divergence (the modal normally preempts by rendering the PIN field), which lowers severity. Fix = `payment.tenderToleranceApprovalRequired` in en/fr + construct with `i18n.t(...)`, matching what T9 just established one file over.
Task 9: minor (deferred): translated text now lands in the structured log as `message` (`serializeErrorForLog`, `lib/errorLogging.ts:24-31`) — the `[POS][checkout][cash] failed` line will carry a French sentence rather than a diagnostic. Mitigated: `name` is in the same payload and `receiptService.ts:322-331` emits both totals in a dedicated `console.error` immediately before the throw.
Task 9: 🎫 SEPARATE TICKET (pre-dates this branch): `formatCheckoutError` (`paymentStore.ts:464-473`) surfaces `error.message` verbatim for EVERY error including internal ones (`'tenantId, companyId, shiftId, and seller are required…'`, `FiscalChainContentionError`) — a structural rule-11 gap.
Task 9: ⚠️ FOR THE FISCAL REVIEW / T10: `CashTenderedModal.tsx` reported as ZERO-IMPORTER dead code that computes change against an UNROUNDED total. If it is genuinely unimported it cannot affect the change-due chain; if it is ever revived it is a screen-vs-receipt divergence.

Task 9: fiscal review — spec MET, quality **Approved**, 0 Critical / 0 Important. All 6 named risks verified against source.
Task 9: ✅✅ **THE T6→T9 NO-DEVICE-BUILD CONSTRAINT IS LIFTED.** Change-due chain traced end to end with NO residual split: `receiptService.ts:414` changeDue + `:667`/`:550` result/row total are all rounded-derived → `paymentStore.ts:747`, `:1144`/`:1524` → `HomePage.tsx:1703-1704` → CheckoutSuccessModal; cash screen already showed `cashScreenSnapshot.roundedTotal` (`HomePage.tsx:1689`); AdvancedPaymentsModal derives its own rounded `amountDue`; **print derives change as `Σ payments − receipt.total` (`buildReceiptData.ts:140`) and `receipt.total` is now rounded.** Every human-visible number == the signed `payload.total`.
Task 9: 🔑 Card path VERIFIED genuinely reconciled — `paymentStore.ts:1237-1252` uses the REAL `buildCheckoutPolicySnapshot` with `exactTotal: computeExactCartTotal(...)`, the SAME function `receiptService.ts:314` re-derives, so the assert holds on a DISCOUNTED card sale. Gate closes structurally: CARD leg ⇒ `cashOnly false` ⇒ `roundingApplied false` (`checkoutPolicySnapshot.ts:164-170`) ⇒ canonical zeros (`:172-181`), and `decideTolerance` returns `not_applicable` on `!cashOnly` (`:293-295`) so no bogus `tolerance_shortfall` persists. Ticketed `bcsum(line_total)` leg amount untouched (`paymentStore.ts:1234`) with a call-site note.
Task 9: 🔑 Snapshot totality CLEAN — repo-wide grep for `as CheckoutPolicySnapshot` / `as unknown as` returns NOTHING; all 28 `policySnapshot:` sites are real-builder results or real-builder test fixtures. INSERT lockstep counted position-for-position: 32 columns / `$1…$32` / 32 binds aligned. Assert precedes BOTH writes and correctly sits AFTER the idempotency short-circuit (a redelivery must return R1 without re-validating a new cart). `--no-verify` claim HOLDS — the diff adds no loop to any of the 3 production files, so the 3 `async-await-in-loop` warnings cannot be new.
Task 9: 🔑 LANDED LAYERING FACT — the V3 builder's OWN bind is a second independent gate behind the narrow `exactTotal`-only assert: because the V2 delegate is handed `exactTotal` (`receiptService.ts:370`) and bind 1 checks `subtotal+vat == (total−adj)+discount`, a snapshot with mutually inconsistent `roundedTotal`/`adjustment` throws BEFORE signing. Also: `adjustment`/`denomination` are handed RAW to the builder (`:392-394`) so `canonicalMoney` stays the single negative-zero collapse point.
Task 9: complete (commits 1c3af608f..d82197824, review clean, 2 reviewers, 0 fix rounds)
Task 9: minor (deferred): the 29→32 positional lockstep has **zero executing coverage with non-null values** — `offlineReceiptRepository.insert.test.ts` mocks `execute`, the real-SQLite suites pass `unroundedSnapshot` (all three values null), and `receiptService.cashRounding.test.ts` mocks `insertOfflineReceipt`. Correct today by reading, but the money-in-wrong-column defect the brief warns about would survive a future edit undetected. Fix = one real-SQLite round-trip with three DISTINCT non-null values.
Task 9: minor (deferred): `paymentStore.ts:1237-1252` card snapshot is executed but never ASSERTED (`paymentStore.offlineFirst.test.ts:545,:728` assert only `payments`). Resolution 4's point was that the path be DEMONSTRABLY reconciled; one `expect(call[1].policySnapshot.roundingApplied).toBe(false)` closes it — harness already exists at `paymentStore.changeEligibility.test.ts:212`.
Task 9: minor (deferred): i18n baked into the fiscal authoring service applies to ONE of three throws — siblings `'Terminal hash chain not initialized…'` (`receiptService.ts:302`) and `'tenantId, companyId, shiftId, and seller are required…'` (`:335`) still put raw English on a French banner. Fiscal reviewer's counter to D2: a `lib/offline/checkoutErrors.ts` module lets paymentStore `instanceof`-branch WITHOUT importing receiptService, breaks none of the 13 bare `vi.mock` factories, and keeps translation at the presentation layer.
Task 9: minor (deferred): `HomePage.tsx:1226-1231` comment overstates — it claims a policy tick "between opening the screen and confirming cannot move what gets signed", but display snapshot (`:1232`) and sealed snapshot (`paymentStore.ts:1033`) are two SEPARATE builds, so a refresh between them moves the signed `roundedTotal` while the cashier collected against the displayed one. Bounded by D/2, pre-existing T6/T7 design. Comment should say "cannot move it AFTER Confirm".
Task 9: 🎫 TRACK-LEVEL GAP (belongs to the approved refund-rounding follow-up track, `docs/superpowers/specs/2026-07-27-refund-rounding-research.md` — NOT this plan): **refunding a rounded receipt refunds the EXACT total.** `hydrateFromReceipt.ts` rebuilds the refund cart from the stored `lines` JSON only (no `receipt.total` reference anywhere in the file), so the cart re-derives the exact total and the customer is out/up by `adjustment`. Inert until rounding is enabled.
Task 9: 🔑 PROCESS LESSON for T10 — the reported run list had a real (benign) blind spot: `src/api/__tests__/reportApi.localTimestamps.test.ts` and `src/lib/refundFlow/__tests__/hydrateFromReceipt.test.ts` were modified but matched no glob in the report's run list. Reviewer ran both (2/2, 16/16, pristine). **Run every path the diff touches, not a glob approximation** — this is the same class of miss that hid the T8 Critical.
Task 9: ⚠️ HARD CARRY TO TASK 10 — the stored row is now internally MIXED: `total` is rounded while `subtotal`/`tax_amount`/`discount_amount` stay exact (`receiptService.ts:546-550`, correct and deliberate). **Any surface rendering all four (the printed ticket) shows `subtotal + tax ≠ total` with no rounding line.** Inert today (needs policy-enabled + schema 3). **Task 10 cannot ship without closing it.**

Task 10: dispatched (Opus, base `d82197824`) with 6 controller resolutions: (1) stale paths; (2) trailer; (3) 🔴 **THE BRIEF IS WRONG ON `tolerance_auto_accept_count`** — it derives it from receipts carrying a non-null `tolerance_shortfall`, but T6 landed the durable v64 `tolerance_auto_accepts(shift_id PK, accept_count, updated_at)` counter read via `readToleranceAutoAcceptCount(shiftId)`. The two differ: the budget counter counts AUTO-accepts (no-PIN), a receipt-derived count also sweeps in PIN-APPROVED shortfalls. Implementer told to establish the real semantics from landed code, surface the number that answers "how much of the §8.1 budget was spent", name both distinctly if both matter, and evidence it in the report; plus the two T6 gotchas (`updated_at` is SPACE-separated → `toSqliteUtc()`; `recordToleranceAutoAccept` is not a "my own new count" contract). (4) 🔴 T9's ticket-arithmetic carry as a HARD EXIT CRITERION, to be verified on real rendered Rust output. (5) the T7 `TenderToleranceApprovalRequiredError` rule-11 fix folded in, explicitly sanctioned + minimal. (6) hash-parity ordering is load-bearing — snapshot frozen against the CURRENT impl and COMMITTED before touching `normalizeForHash`; schema_version STAYS 2; mirror the landed PHP, read it, don't modify it. Plus the run-list process lesson: derive test paths from `git diff --name-only`, not globs.

Task 10: ⚠️ INTERRUPTED — the implementer died mid-task on an API connection error ("Connection closed mid-response") right after finishing the hash mirror and starting the zReportService aggregation. **State preserved and verified by the controller:** commit `424a47ef6` "T10 step 1: freeze the legacy Z hash BEFORE touching normalizeForHash" is landed with the correct trailer and the `.snap` + legacyStability test (66 lines) — **the load-bearing two-commit ordering HELD**. Uncommitted at the time: `zReportHashService.ts` (+19, additive normalization), `types.ts` (+11, `ZReportData.cash_rounding_summary`), untracked new `zReportService.cashRounding.test.ts`. No report file existed. Agent RESUMED from transcript with the verified state summary + a re-verify instruction (confirm the 3 files are whole, then prove the `.snap` is byte-unchanged) + the 4 carries restated. If it dies again, a fresh implementer can start from this line + `git status`.

Task 10: RESUMED SUCCESSFULLY → commits `424a47ef6` (frozen snapshot, first + separate) + `618c67ded` (implementation), 20 files. Implementer reports 1054 vitest + 30 cargo `printing` tests across every dir in `git diff --name-only`, typecheck + lint clean, **`.snap` BYTE-UNCHANGED (`3242f6e3…`) = hash parity proven**.
Task 10: 🔑 **THE CONTROLLER'S ITEM-3 PREMISE WAS FALSIFIED BY THE IMPLEMENTER** (correctly, pending review): `tolerance_shortfall` is written ONLY on the auto-accept path (`receiptService.ts:581`) and the PIN branch is entered exactly when `!toleranceDecision.applied` (`paymentStore.ts:1416`) — so **neither source sees PIN approvals**. The two counts still diverge 4 OTHER ways: best-effort budget write UNDER-counts; training + voided receipts OVER-count; window keyed differently. Shipped resolution: `tolerance_auto_accept_count` reads the durable v64 row (the same value the §8.1 gate consumes); the receipt-derived count keeps its distinct existing name `tolerance_summary.writeoffCount`. No `toSqliteUtc()` needed — the query binds `shift_id` only (pinned by a test). Reviewer asked to adjudicate the correction AND the resolution independently.
Task 10: ✅ ITEM 4 (hard exit criterion) verified on REAL ESC/POS bytes: `Subtotal 10.000 / Discount -0.000 / Tax 1.900 / Rounding -0.020 / TOTAL 11.880`, with a Rust test that re-parses the rendered text into millimes and asserts the sum equals the printed total.
Task 10: deviation 1 (deliberate) — rounding line prints in the **totals** block, NOT the payments block the brief specified: the payments block is gated by `show_payment_details`, which would drop the EXPLANATION while still printing the mismatch.
Task 10: deviation 2 (claimed brief defect) — both summaries emit at FIXED scale 3, not `decimals`: `bcformat(…, decimals)` produced `'0.00'` on EUR, violating the documented 3dp contract (`types.ts:87`, `ReportGenerationService.php:284-295`); caught by an existing test. Reviewer asked whether scale 3 is right for BOTH summaries or only `cash_rounding_summary`.
Task 10: ⚠️ GAP FLAGGED BY THE IMPLEMENTER — `cash_rounding_summary` and `tolerance_auto_accept_count` are computed but **NO EOD UI RENDERS THEM** (outside the brief's file list; Task 11 is docs-only, so nothing later in this plan closes it). Reviewer asked whether this blocks.
Task 10: minor — a pre-existing test (`cashTenderedFormula.test.ts`) pinned the now-dead `payments_json.tolerance_writeoff` read; its fixture moved to the receipt column. **Found only by running the whole directory** — vindicates the `git diff --name-only` run-list rule.

Task 10: review 1 (base d82197824, head 618c67ded) — fiscal: spec MET, quality **Approved**, 0 Critical, 1 Important (plan-mandated SCOPE gap, see below). Every load-bearing claim verified against code, not the report.
Task 10: 🔑 HASH MIRROR VERIFIED BYTE-EXACT — `zReportHashService.ts:100-114` vs `apps/api/.../ZReportHashService.php:141-153`: same per-key isset guard, same scale 3, same position (after `tolerance_summary`, before `payment_methods`); **both sides early-return unchanged for `schema_version < 2`** so the v1 arm cannot diverge either. Reviewer's key insight: **the snapshot's narrowness does NOT weaken the proof** — the new block reads only `result['cash_rounding_summary']` and mutates only that sub-object, so it is non-mutating for EVERY shape lacking the key, not just the fixture's. Proof is sound BY CONSTRUCTION; the snapshot is belt-and-braces. Doubly safe: rounding is v3-gated, so a v2 terminal can never emit the key.
Task 10: 🔑 RESOLUTION 3 ADJUDICATED — the implementer's correction of the controller is **CORRECT**. `tolerance_shortfall` written only under `toleranceDecision.applied` (`receiptService.ts:581`); PIN branch entered exactly when `bccomp(tendered, roundedTotal) < 0 && !applied` (`paymentStore.ts:1414-1418`) and re-uses the SAME snapshot for authoring (`:1502`) → a PIN-approved shortfall leaves the column NULL. All 4 divergences hold (best-effort budget write `:1133-1143`/`:1513-1523`; TRAINING is in `ROUNDABLE_INVOICE_TYPES` with no tolerance exclusion while EOD filters `is_training=0`; `voided=0`; different windows). Reading the durable v64 row is RIGHT — it is literally the number the gate consumes (`paymentStore.ts:1124-1126`), so surfacing anything else would advertise headroom already spent. The over-count does NOT make `writeoffCount` wrong for ITS purpose: till reconciliation SHOULD exclude training and voided. **Both numbers correct under their own names.**
Task 10: 🔑 DEVIATION 2 UPHELD — fixed scale 3 is correct for **BOTH** summaries, confirmed on two independent server sources: `ReportGenerationService.php:284-295` (zero-shape `'0.000'` for every currency) and `ZReportProjection.php:32` `ROUNDING_SUMMARY_SCALE = 3`. A currency-scale string would have made a EUR device emit `'0.00'` into **signed Z_REPORT canonical bytes** where the server emits `'0.000'`. Deviation 1 also upheld (payments block gated at `receipt_template.rs:~660` by `show_payment_details`, totals block is not; pinned by `the_rounding_line_prints_even_when_payment_details_are_hidden`; exactly one use each of `l.rounding`/`l.tolerance`). Fixture move VERIFIED a genuine dead-read migration — no writer of `payments_json[].tolerance_writeoff` exists anywhere in non-test src; assertions byte-unchanged.
Task 10: 🔴 **IMPORTANT (plan-mandated SCOPE gap) → OWNER DECISION PENDING**: `cash_rounding_summary` + `tolerance_auto_accept_count` are produced (`endOfDayPreview.ts:104-127`) and **rendered NOWHERE** — grep over `src/components`/`src/pages` returns only a test type-fixture; `EndOfDayPreviewModal` renders `tolerance_summary` via `ToleranceDrillDown` and nothing else. **Task 11 is docs-only, so nothing downstream closes it.** Operational meaning: the §8.1 budget headroom — the one number explaining why the NEXT short tender will demand a manager PIN — is invisible at the only surface a cashier sees before closing. NOT a code defect: the brief's file list and steps are data-layer only and the implementer flagged it correctly.
Task 10: minor (deferred): `zReportService.ts:177-193` Z aggregation window includes VOIDED receipts (`is_training=0` only) while EOD (`endOfDayPreview.ts:169`) and the server (`ZReportProjection.php:226`) both exclude them — the same-named metric now computed over THREE windows, and the device value goes into signed Z bytes. Inert today (no production code sets `voided=1` on `offline_receipts`). Pre-existing structure inherited by the new aggregates; fixing it would also move `gross_sales`, so not a drive-by.
Task 10: minor (deferred): `endOfDayPreview.ts:120-126` doc-comment's "no shift ⇒ no spend" (`0`) CONTRADICTS the gate's fail-closed reading (`paymentStore.ts:1124-1126` treats `shiftId === null` as the full LIMIT = budget spent). If ever rendered as "headroom", the shiftless case reads as FULL headroom while the gate refuses.
Task 10: minor (deferred): `zReportService.cashRounding.test.ts` mocks `appendZSessionCloseAndZReport` and never asserts what it RECEIVED — the real `tolerance_summary` now enters the **signed** canonical Z payload via `zReportService.ts:670-676` → `zSessionAuthoring.ts:496`, covered only transitively. A regression reshaping the `toleranceSummary` argument would pass. One `toHaveBeenCalledWith(expect.objectContaining(...))` closes it.
Task 10: minor (deferred): `receipt_template.rs:60` `has_cash_rounding: Option<bool>` vs sibling `has_tolerance: bool` (`:49`) — asymmetric for no behavioural reason, forces `.unwrap_or(false)` + `None`/`Some(true)` noise across 6 fixtures. Brief-mandated shape.
Task 10: minor (deferred): `zReportService.ts:792-793` stale comment "offline receipts don't have a voided flag" is FALSE and sits directly adjacent to the new logic — it is the comment a future reader consults when deciding whether the new aggregates need a void filter.

Task 10: 🔴 **OWNER RULING 2026-07-29 — RENDER BOTH NOW.** The plan-mandated EOD scope gap goes to fix round 1 rather than a ticket: `EndOfDayPreviewModal` gains a net-rounding line (hidden entirely when the summary is null — no zero row on a never-rounded shift) and a spent-vs-limit auto-accept line using the imported `TOLERANCE_AUTO_ACCEPT_LIMIT_PER_SHIFT` (NOT a literal 10 — two suites already assume its value), plus 2 en/fr key pairs. Approved shape: `Tolerance write-offs 0.050 TND (1)` / `Auto-accepts used 3 / 10` / `Net cash rounding -0.020 TND`.
Task 10: 🔑 the ruling PROMOTED the reviewer's minor #3 to load-bearing and it is IN fix round 1: `endOfDayPreview.ts:120-126` returns `0` for a missing shift and its doc-comment reads that as "no spend", but the gate treats `shiftId === null` as the FULL limit (fail-closed, `paymentStore.ts:1124-1126`). Rendered naively that advertises full headroom on screen while the gate refuses the next short tender. Must render as unknown/exhausted, never full, with the doc-comment corrected.
Task 10: fix round 1/5 dispatched (original implementer resumed; scope fenced to the EOD surface + the shiftless-case reconciliation — Z aggregation, hash mirror and print path are reviewed/approved and explicitly OFF LIMITS).

Task 10: fix round 1 IMPLEMENTED at `a22c00e21` (6 files). Rows render `Auto-accepts used 3 / 10` + `Net cash rounding -0.020 (2)` right after `ToleranceDrillDown`; rounding row ABSENT when the summary is null; limit IMPORTED from `TOLERANCE_AUTO_ACCEPT_LIMIT_PER_SHIFT`; 4 new en+fr pairs under `reports.endOfDay.*`.
Task 10: 🔑 SHIFTLESS-CASE RESOLUTION — `tolerance_auto_accept_count` widened to `number | null`; a shiftless preview reports `null` = **unknown**, rendered `Unknown / 10`. Implementer's reasoning: `0` reads as full headroom while the gate treats a null shift as fully spent (`paymentStore.ts:1030-1033`), and rendering the LIMIT would fabricate spend that never happened — **`null` is the only reading true in BOTH directions.** Doc-comment now states the gate's contradicting interpretation explicitly. A test asserts the row does NOT contain `0 / 10`. ⚠️ API widening: any future consumer must handle null, NOT default it to 0 (that would resurrect the exact defect) — re-reviewer asked to grep for `?? 0` / `|| 0` coercions.
Task 10: scoped re-review dispatched (frontend-conventions, Opus) — data layer / Z aggregation / hash mirror / print path explicitly OUT OF BOUNDS.
Task 10: 🎫 pre-existing (verified at base `d82197824`, unrelated): `src/components/pos/__tests__/ReportsMenu.test.tsx` fails to COLLECT (react-i18next mock hoisting, 0 tests run).

Task 10: fix round 1/5 (2 addressed, 0 open; commits 618c67ded..a22c00e21, 6 files). Re-reviewer verified: rows at `EndOfDayPreviewModal.tsx:334-384` right after ToleranceDrillDown; rounding row guarded on `!== null` (pinned by a test asserting BOTH `queryByTestId` and `queryByText` absent); limit imported from `cashRounding.ts:50` and the TEST interpolates the same constant so retuning the cap cannot leave a stale expectation; 4 en/fr keys at `pos.json:491-494` both locales, same order + same interpolation placeholders; tokens all semantic, row shape COPIED from the adjacent ToleranceDrillDown (not a new one-off). Shiftless test is DISCRIMINATING — `'Unknown / 10'` does not contain the substring `'0 / 10'`, so the negative assertion genuinely fails on regression. **Gate-semantics premise VERIFIED**: `paymentStore.ts:1031-1033` (quick cash) and `:1384-1386` (advanced) both do `shiftId === null ? LIMIT : await read…`, and the read helper fails closed to the LIMIT on error (`:504-527`) — so `Unknown / N` is the only non-misleading rendering. **No null-coercing consumer**: only non-test reader branches on `=== null` (not falsy, so `0` still takes the value branch); the other two `EndOfDayPreview` consumers (`Header.tsx:316`, `CashReconciliationSection.tsx:39`) read neither field. Re-reviewer re-ran targeted ESLint + `pnpm typecheck` itself: clean.
Task 10: complete (commits d82197824..a22c00e21, review clean after 1 fix round, 2 reviewers)
Task 10: minor (deferred → **worth final-review triage**): `EndOfDayPreviewModal.tsx:344-361` renders the auto-accept budget row UNCONDITIONALLY, including where tolerance is inert (`fiscal_schema_version < 3` or tolerance disabled) — the same condition under which the row directly above it (ToleranceDrillDown, `:322-324`) is deliberately HIDDEN. Those closes permanently show `Auto-accepts used 0 / 10`, advertising a budget the gate can never spend. **Rollout-relevant: during the staged terminal cutover MOST terminals are non-cutover, so this is the common case, not the edge case.** Fix = gate on the same v3/policy condition, or accept as intentional (owner's call).
Task 10: minor (deferred): the modal's sign tests run against the file's identity `format` mock, so they pin "the component passes the signed string through unaltered", NOT that `formatCurrency` renders a minus. Right call for locale independence (real `Intl` in `fr-TN` emits `-0,020 TND`), but no test covers real `formatCurrency` on a negative amount — one `currency.ts` unit assertion would close it.

Task 11: dispatched (Sonnet, base `a22c00e21`, docs-only) with 7 controller resolutions. Most important: 🔴 **the brief's SQLite version is WRONG** — it says the build carries v63 and tells the operator to verify `= 63`, but T6 landed **v64** (`tolerance_auto_accepts`); an operator would "verify" a rollout that did not happen. Also: the smoke list is a GUESS at filenames (must confirm existence; MUST include the two real-engine integration suites since `offlineFirstFlow` mocks `@/lib/fiscal/instance` and is NOT a substitute); post-enable verification must cover the two new EOD rows incl. the null/Unknown cases; and the 2 known pre-existing reds must be DECLARED so a future operator does not read them as regressions from this track.

Task 11: implemented at `ad50480d3` (docs-only, `docs/handoff/cash-rounding-phase2-deploy-checklist.md`). Device smoke **17 files / 220 tests all passed**; typecheck + lint clean; the 2 known pre-existing failures re-confirmed unrelated.
Task 11: 🔑 **OPERATIONAL CORRECTIONS THE BRIEF GOT WRONG** (would have failed on a live system): `pos:configure-cash-rounding` has **NO bare `--enable`/`--disable`** — real flags are `--enable-rounding` / `--disable-rounding` / `--enable-tolerance` / `--disable-tolerance`; both server commands must run via `tenants:run --option='k=v'` **gated on a printed token, with the EXIT CODE SWALLOWED** (so an operator must not trust it); and **no bulk terminal-cutover-listing tool exists** → the checklist uses a direct SQL query. Migration **v64** confirmed final. Done-criteria verified: `git diff --stat 0763bf8bf..a22c00e21 -- apps/api` **EMPTY** (no server file in any commit of this track); legacyStability snapshot **byte-identical** to freeze commit `424a47ef6`.
Task 11: self-declared gaps — the "adversarial review dispatched" Done-criterion left UNCHECKED as unverifiable from the repo (**the controller attests it: 13 review dispatches across T8–T11 recorded in this ledger**); and the printed-ticket line placement was TRANSCRIBED FROM A COMMIT MESSAGE rather than re-derived from the Rust source → flagged to the T11 reviewer for re-derivation.
Task 11: review dispatched (fiscal-pos-reviewer, Opus) scoped to FACTUAL ACCURACY against code — every command/flag, every verification step's DISCRIMINATING POWER ("could this step pass while the thing it verifies is false?"), migration version end-to-end incl. rollback, kill-switch independence, smoke-list existence, and re-derivation of the ticket description from `receipt_template.rs`.

FINAL WHOLE-BRANCH REVIEW dispatched (fiscal-pos-reviewer, Opus) — merge-base `0763bf8bf` CONFIRMED via `git merge-base dev HEAD`, head `ad50480d3`, **20 commits, 628KB package**. Scoped to what per-task review structurally could not see: (1) cross-task coherence of the money path end to end (cart → snapshot → signed payload → row → change due → ticket → Z → EOD); (2) the fail-closed gate as a WHOLE across every path into signing; (3) device/server contract drift — which mirrored constants are drift-tested vs silently forkable; (4) the upgrade/rollout transitional states (staged terminal cutover, migrated-but-no-policy-pulled, offline across a policy change); (5) anything every task-scoped reviewer called out of scope. **Ledger's deferred/parked list handed over as required triage input** (fix-before-merge vs ship-as-ticket, per item).

Task 11: review 1 (base a22c00e21, head ad50480d3) — "Needs fixes": **1 Critical + 6 Important + 7 Minor**, all in the operator-facing doc. Every correction the implementer made was VERIFIED CORRECT (flags, `tenants:run` shape, swallowed exit code via `Run.php:23-26,:32-56`, both gate tokens verbatim, v64 final, rollback additivity, kill-switch independence via 3 separate columns, cutover route + gates, smoke paths all exist, pre-existing-failure claims proven by `0` commits to ReportsMenu and a deletion-free syncService diff) — **including the printed-ticket placement the implementer flagged as transcribed, which the reviewer re-derived from `receipt_template.rs:630-637` and confirmed** (label defaults "Rounding", value verbatim/sign preserved, after Tax before TOTAL, OUTSIDE the `show_payment_details` block).
Task 11: 🔴 **CRITICAL C1 → fix round 1** — the Rollback section covers only the device build and says "no data migration or teardown is needed", omitting that **server-side rollback is a ONE-WAY DOOR**: `…2026_07_28_100200_add_cash_rounding_to_pos_receipts.php` `down()` DROPS `cash_rounding_adjustment`, destroying signed values → rounded receipts become `total 9.950 / subtotal 9.973` residue satisfying no identity, with fiscal immutability blocking deletion; re-applying `up()` leaves `pos_receipts_totals` permanently NOT VALID. Phase 1 deferred this explicitly BECAUSE no tenant had taken a rounded sale — **post-enable is exactly when they have, which is what this document turns on.**
Task 11: OPEN Importants → fix round 1 — **3 verification steps DO NOT DISCRIMINATE**: I1 the device-cache check expects `'0.0500'` but the device stores the API's company-scale `'0.050'` (`PosPaymentPolicyResolver.php:136` → DTO `:79`), so it **fails on a CORRECT rollout** and contradicts the doc's own line 74; I3 the token gate passes VACUOUSLY on an empty log (`$TENANT_COUNT` never defined; Phase 1 warned about exactly this at `:253` and that the `|| echo` lines PRINT rather than FAIL at `:248-250`); I4 `--verify`'s token covers only "already-ON with bad denomination" + "no is_cash_tender method" — a tenant with **NO `country_payment_settings` row at all** yields `FAILURES: 0` (`:487-489`), which pre-enable is precisely the state being checked for. Plus I2 every command is FLEET-WIDE (`tenants:run` defaults to ALL tenants) while the doc says "the target tenant"; I5 the tolerance smoke step is unconditional though the switch is optional (rounding-only ⇒ `decideTolerance` returns `disabled` ⇒ correct system FAILS the step); I6 omits the expected-cash change Phase 1 §0.3 explicitly hands to this doc — **the device authors v3 UNCONDITIONALLY on the new build** (`FiscalEventPayloadRegistry.ts:171-173`, no policy/cutover arm) so `pos_receipts.change_due` and shift-variance move for EVERY terminal on the build, making the first post-rollout EOD read as a regression unless the manager is briefed at build ship.
Task 11: ⚠️ OPEN QUESTION neither implementer nor reviewer could settle — whether v3 fiscal events already QUEUED on-device at rollback time push cleanly under a rolled-back v2 build. Fix round instructed to record it as an explicit unknown rather than assert safety.
Task 11: fix round 1/5 dispatched (original implementer resumed; C1 + 6 Importants + 7 Minors, each with its file:line evidence, verify-against-source required).

═══ FINAL WHOLE-BRANCH REVIEW (base 0763bf8bf, head ad50480d3, 20 commits) — **"Needs fixes before merge"; 0 Critical, 4 Important, 5 Minor.** The signing path, gates and payload binds are SOUND; what is wrong is the rollout artifact this branch ships. ═══
FINAL 🔴 IMPORTANT 1 — **THE DEVICE BUILD, NOT THE POLICY ENABLE AND NOT THE TERMINAL CUTOVER, FLIPS THE SERVER INTO ROUNDING-ERA SEMANTICS FOR 100% OF TERMINALS.** `FiscalEventPayloadRegistry.ts:171` returns 3 UNCONDITIONALLY and `receiptService.ts:357` always calls the V3 builder; the server's only discriminator is `fiscal_events.event_version >= 3` (`CashRoundingCutover.php:43`), **NOT `pos_terminals.fiscal_schema_version`**. So the moment the build ships: `TreasuryReceiptBridge::computeNettedAmounts` (`:874`) starts netting change off cash legs and suppressing fully-netted legs (`:1142-1145`) — Treasury `payments.amount` changes from TENDERED to RETAINED on every over-tendered cash sale, fleet-wide — and `PosCoreReceiptProjection` starts writing `change_due`/`tolerance_writeoff` (`:373-378`). This is the intended fix for the pre-existing over-tender GL defect and `pos_receipt_payments.amount` correctly stays tendered (`:827-834`), so drawer/variance is unaffected. **But the checklist frames the rollout as staged behind `fiscal_schema_version` and `--enable-rounding`, and the reality is inverted: ledger semantics change with the BINARY, rounding changes with the POLICY.** 🔑 AND: `fiscal_schema_version >= 3` is the PRE-EXISTING device-authoritative cutover (`ShiftController.php:60` + `SyncController.php:53` both 409 below it), so **every terminal that can run this POS at all is ALREADY at 3 — the checklist's "staged cutover" section describes a stage that DOES NOT EXIST.**
FINAL 🔴 IMPORTANT 2 — **enabling rounding immediately makes every refund of a rounded receipt wrong, and the checklist has no gate for it.** `hydrateFromReceipt.ts` rebuilds from `lines` JSON and never references `receipt.total`; the server caps by QUANTITY not amount (`ReceiptReturnService.php:1060-1082`). A full refund of a receipt rounded 9.973→9.950 refunds **9.973** — customer up by ≤D/2, refund exceeds the sale it reverses, and the avoir carries no rounding line (`buildReceiptData.ts:712-713` hardcodes null). The ledger routed this to the separate refund track as "inert until rounding is enabled" — **but this rollout IS the enable.**
FINAL 🔴 IMPORTANT 3 (CODE) — `AdvancedPaymentsModal.tsx:386` `amountDue = displaySnapshot.roundedTotal`, but the display snapshot is built over `tenderLegs` which is **EMPTY when the modal opens**, and `isCashOnlyTender` returns false for an empty tender (`cashRounding.ts:184`) ⇒ `roundingApplied` false ⇒ **amountDue is the EXACT total until the first leg exists.** Both prefills read it (`:487` on method select, `:497-500` Pay Remaining). On TND 9.973 with D=0.050 the cashier is offered **9.973 — an amount not tenderable in TND cash, the exact number rounding exists to eliminate**; accepting seals `payments[].amount = 9.973` + `change_due 0.023` into a SIGNED receipt describing cash never handed over and change that cannot be given. Reconciles arithmetically (fidelity defect, not lost money). Quick cash unaffected (`HomePage.tsx:1240` injects a synthetic cash leg into the probe). **Reviewer explicitly REJECTS the T7 ledger's "owner UX call" framing** — it is a signed payments[] amount, not UX. Fix ≈5 lines: build the display snapshot with a candidate leg for the selected/sole-active cash method.
FINAL 🔴 IMPORTANT 4 → TICKET — `V3_DENOMINATION_CAP_BY_SCALE` and `DENOMINATION_CAP_BY_SCALE` are pinned only against EACH OTHER; the PHP authority `CashRoundingCaps::CAPS` is pinned by NOTHING, while the 30-key payload set IS PHP-pinned. The T8 hoist made this cap gate unconditional on every v3 receipt, so a future PHP cap change either BRICKS device authoring for a currency scale (`cap === undefined` throws on every sale) or signs a denomination the server quarantines. The `readPhpNamedConst` harness already exists — one `it()` closes it.
FINAL minor 5 — checklist self-contradiction on the denomination string (line 74 `"0.050"` correct vs line 84 `'0.0500'` wrong). **ALREADY IN T11 FIX ROUND 1 as I1** — independently found by both reviewers.
FINAL minors 6-9: EOD budget row unconditional (confirms the T10 deferral, agrees Minor, notes it is the COMMON case pre-enable); **a manager-PIN-approved shortfall is INVISIBLE on the device EOD** (`writeoffCount === 0` ⇒ the gate at `EndOfDayPreviewModal.tsx:325-326` never renders the server-backed drill-down that WOULD show them, though the server books them regardless via `TreasuryReceiptBridge:486-500`) — no regression, but the "real aggregation" covers only half the write-offs; `getToleranceAutoAcceptCount` is **fail-OPEN on a corrupt row** (`toleranceAutoAcceptRepository.ts:31-32` returns 0 = full budget) directly under a comment saying it must never read as budget-available — unreachable via the writer, but code and comment disagree; device `cash_rounding_summary` is absent-when-zero while the server's is always the zero shape — harmless today (Zs are chain-verified on canonical bytes, never report_data) but pre-poisons any future cross-check.
FINAL ✅ VERIFIED CLEAN (so the next reader knows what is covered): end-to-end money coherence on the cash path with every human-visible number equal to the signed `payload.total`; defense-in-depth on the total (re-derive + refuse before append/insert, backed by the V3 builder's own bind 1); **the fail-closed gate is TOTAL — 4 production snapshot call sites, no `as CheckoutPolicySnapshot` anywhere, `receiptService.ts` is the ONLY SALE_RECEIPT authoring site in the app (grep-verified), and there is NO route to a non-canonical-zero adjustment on a non-cutover terminal**; payload↔server parity incl. the `-0.000` rejection and bind evaluation order; Z hash parity byte-equivalent with the frozen snapshot confirmed unchanged; tolerance budget durability/concurrency incl. the isolated non-rethrowing budget write; under- AND over-tender handled server-side; print path; precision contract (no new parseFloat on money; the single `Number()` in `denominations.ts:47` is on an exact whole bill multiple and re-enters via `bcformat`); branch hygiene (`apps/api` untouched, en/fr parity on all 11 keys, both migrations additive/idempotent). Reviewer re-ran `checkoutPolicySnapshot` + `SaleReceiptV3Payload` itself: 68/68.
FINAL ✅ CONFIRMED ALREADY-RESOLVED, do not re-raise: the T6→T9 no-device-build constraint (change-due chain independently re-verified); the T7 English literal (now `i18n.t`, keys in both locales); the T8 Critical (V3 builder at `receiptService.ts:357`); the T4 `rounded − adj == exact` identity (enforced both ends).
FINAL 🎫 TRIAGE — ship-as-ticket, HIGH value: cap-table PHP pin (#4); T9 29→32 lockstep has zero executing coverage with non-null values (the money-in-wrong-column class the brief itself warned about); T10 `appendZSessionCloseAndZReport` never asserted (tolerance_summary now enters SIGNED Z bytes with real values for the first time); refund-of-rounded-receipt (already an approved track — the change is that it becomes a ROLLOUT GATE, not a background item); T7 `processCardCheckout` `bcsum(line_total)` (confirmed still present, confirmed dead, keep the "must not ship before reconciled" clause); **repo-wide ESLint flat-config defect leaving the FU-2 cart-mutator guard INERT app-wide — a live disabled guard, file today**; `formatCheckoutError` verbatim-message rule-11 gap; `paymentRepository` `SELECT *` with no runtime validation. Pick of the low-value batch: **`accountChargeCartMapper.ts:104` default-scale percentage on the ACCOUNT_CHARGE signed payload — the same defect class T5 fixed, still live on another signed event.**

Task 11: fix round 1/5 — C1 + I1–I6 + M1–M7 ALL addressed at `f8a969182`, each verified against source before writing (migration `down()`, stancl `Run.php`, `ConfigureCashRoundingCommand::verify()`, `checkoutPolicySnapshot.ts`, `FiscalEventPayloadRegistry.ts`, `receipt_template.rs`, `zReportService.ts`, `buildReceiptData.ts`/`decimal.ts`, `TerminalResource.php`, `db.ts`, hash-service mocks, `PosPaymentPolicyController.php`). typecheck + lint clean. The queued-v3-events-under-a-rolled-back-build unknown is now an EXPLICIT open question in the checklist rather than an assumed-safe assertion.
Task 11: complete (commits a22c00e21..f8a969182, review clean after 1 fix round)

FINAL FIX WAVE dispatched (Opus, single wave per SDD — base `f8a969182`): findings 1 + 2 (checklist rewrites: blast-radius inversion + the phantom cutover stage + the mixed-Treasury-window rollback; refund exposure as a hard Enable precondition) and finding 3 (CODE — the modal display snapshot gets a candidate cash leg, modelled on `HomePage.tsx:1240`'s synthetic-leg probe). Finding 5 = verify-only (already fixed in `f8a969182`). Finding 4 → ticket per the reviewer's own triage.
FINAL FIX WAVE ⚠️ **DELICATE CONSTRAINT flagged to the implementer**: T7 fix 1 established, and a reviewer verified term-for-term in BOTH directions, that **the modal and the store gate on the SAME rounded due**. Moving the modal's `amountDue` without the store agreeing resurrects exactly the screen-vs-store divergence that round killed (a tender the modal accepts and the store refuses, or an auto-accept spending budget on an amount the cashier was never asked for). Implementer must re-verify the equivalence in both directions and state how.
🔑 CONTROLLER RULING — the reviewer REJECTED the T7 ledger's "owner UX call" framing of finding 3 and I ACCEPT that rejection: no owner ruling ever existed on it (it was a controller-deferred minor, never adjudicated), and the reviewer's argument is correctness, not preference — a signed `payments[].amount` describing cash never handed over. Fixing it in the final wave rather than ticketing it.

FINAL FIX WAVE implemented at `92c1a96b1` (3 files, +459/−20, no apps/api). RED first (6 prefill failures) → green; 9 files/171 tests across every path the diff touches PLUS every importer; full device smoke now 18 files/284 tests; typecheck + lint clean.
FINAL FIX WAVE 🔴 **THE IMPLEMENTER REJECTED 2 OF THE 3 FINDINGS AS BRIEFED** (pending re-review adjudication):
  • **Pushback A — finding 1's 409 claim is INVERTED.** `ShiftController.php:60` / `SyncController.php:53` 409 at **>= 3** (retiring REST for device-authoritative terminals); they do NOT reject v2. The device keeps a LIVE v2 path (`terminalStore.ts:882` vs `:911-921`), so **a v2 terminal on this build is reachable, authors v3 receipts, and never rounds** ⇒ the cutover section is **LOAD-BEARING, NOT PHANTOM**. Kept and re-scoped instead of deleted. If upheld, the final reviewer AND the controller were wrong and deleting it would have removed a real safety step.
  • **Pushback B — finding 3's PRESCRIBED FIX SHAPE (the controller's own suggestion) BREAKS the very constraint the brief warned about.** Adding a candidate leg to `displaySnapshot` makes `canComplete` true with **ZERO** tender legs on a sub-`D` cart (total 0.03 → auto-accept applies; 0.02 → `isFullyPaid`) while the store refuses — the exact T7 divergence, and contrary to the ruling pinned at `AdvancedPaymentsModal.test.tsx:1400`. Their alternative: **the GATE keeps its live-legs snapshot BYTE-FOR-BYTE; a SEPARATE PROBE answers the prefill's different question.**
FINAL FIX WAVE — equivalence verified 4 ways: by diff (every gate-read value unchanged, `handleComplete` untouched); STRUCTURALLY (a prefill only seeds an editable numpad string — it reaches the store only by becoming a leg BOTH sides see; this is the load-bearing argument); modal-accepts⇒store-accepts via a new test; store-refuses⇒PIN via 8 pre-existing gate tests passing UNMODIFIED. Card-first, voucher, mixed, and legs-already-present all newly covered.
FINAL FIX WAVE ⚠️ SELF-DECLARED BEHAVIOUR CHANGE: the mixed-tender prefill now offers the EXACT remainder on a card top-up over a cash leg (4.97, was 4.95 which left a shortfall routed to the PIN path). Correct in principle — a card leg means the tender is not cash-only, so no rounding applies — intended, tested, flagged.
FINAL FIX WAVE 🔑 **CONTROLLER FRAMING CORRECTED (3rd time this session): the refund exposure is SYMMETRIC** — a round-UP SHORT-CHANGES the customer. My brief framed it only as customer-up. The checklist must document both directions.
FINAL FIX WAVE 🔓 **LIVE OWNER DECISION GATE**: Enable is now BLOCKED in the checklist until either the refund-rounding track lands or the ≤D/2 symmetric exposure is accepted IN WRITING. This is a deploy-time gate, not a merge blocker.
FINAL FIX WAVE: scoped re-review dispatched (fiscal-pos-reviewer, Opus) — instructed to adjudicate BOTH pushbacks against source as the core of the job, verify the gate snapshot is byte-unchanged and the new probe cannot leak into any gate-read value, confirm the sub-`D` counter-example is real (it decides whether the controller's prescription was actively HARMFUL), and verify the structural equivalence argument rather than accepting it.

Task 2: ⚠️ open, owned by Task 6: offline upgrade window — a device that migrates to v63 and never pulls `/payment-methods` classifies even CASH as non-cash (deliberate fail-closed). Task 6 must surface `errors.noCashMethod`.

