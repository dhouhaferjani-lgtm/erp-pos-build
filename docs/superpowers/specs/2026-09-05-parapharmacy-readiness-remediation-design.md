# Multi-branch parapharmacy remediation design

**Version:** v2 — Codex spec fix round 1, 2026-09-05. **Status:** proposed remediation; awaiting Fable gate r2 and the open owner decisions. **Implementation is not authorized.**
**Source baseline:** `b9a5565aa`; citations describe that snapshot, not a verified deployment. **Target:** Tunisia. Legal-company structure and first-client sales surfaces remain unconfirmed.
**Orchestration:** Claude Fable 5.1 is the orchestrator of record, sequences lanes, runs reviewer gates, and alone commits, merges into local `dev` or promotes to `origin/dev`. Codex leaves these documents in the working tree and stops; no commit, merge, rebase, push, implementation or reviewer invocation belongs to this round.

**Change log, v1 → v2:** consolidated every handover lot gap into W-LOT (W5/W6 are internal references, not separate lanes); applied R2 module gating and R3 read-only POS lot display in iteration 1; retained architecture option 1 under R4; added W0's seven archaeology guards; corrected F-A..F-F (one tender binding, open D7, GL writer census, fiscal-derived session reconciliation, narrowed permissions and precise projection recovery gap); extended benchmarks B9/B10; consolidated ten open owner decisions; registered lot evidence and session reconciliation in the glossary. The original r1 review remains an immutable review of v1.

**Summary.** Preserve offline fiscal POS and online B2B ownership. Repair access and tender controls, atomic B2B accounting, durable POS obligations and honest reconciliation. W-LOT completes the lot model across warehouse, counting, metadata, projection and register surfaces; its captured-lot iteration remains gated by D3/D7. This is a program specification and dependency outline, not an executable task plan.

## 1. Industry baseline (benchmark-first — convention 10)

Reference systems: Odoo 19.0 documentation, with an explicitly versioned Odoo 18 POS display reference for B9/B10; ERPNext current unversioned documentation (version not asserted); Dolibarr current unversioned official wiki/search extracts. Accessed 2026-09-05. These are workflow references, not evidence of NF525/NACEF compliance or a claim that all three products implement our exact offline guarantees. **NV** means the specific guarantee was not verified in that system, rather than presumed present.

| ID | User guarantee | Odoo | ERPNext | Dolibarr | AutoERP today (repository-relative path:line) | Decision |
|---|---|---|---|---|---|---|
| B1 | A stock movement for a tracked product records its lot | Lot-grain inventory [S1] | Optional batch dimension in stock ledger [S2] | Tracked movements require lot/serial [S3] | `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052` chooses FEFO after sale | DIVERGE in R3 iteration 1: estimated FEFO only; captured capability is W-LOT/W6 iteration 2 behind D3/D7 |
| B2 | A physical count can distinguish two lots of one SKU | Separate lot lines [S1] | Batch reconciliation supported [S4] | Lot-specific count behavior NV | `apps/api/app/Modules/Inventory/Domain/InventoryCountingItem.php:79` lacks lot dimension | MATCH W-LOT/W5 |
| B3 | Payment-terminal takings use a bank/settlement destination | Bank journal required for terminal [S5] | Exact terminal restriction NV | Exact terminal restriction NV | `apps/api/app/Modules/Treasury/Application/Services/TenderRepositoryResolver.php:157` allows mapped drawer | MATCH W2 |
| B4 | Stock mutation requires an action permission | Exact recall permission NV | Exact recall permission NV | Inventory requires stock-movement permission [S6] | `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:29` lacks recall permission | MATCH W-LOT |
| B5 | Accounting remains linked to its business source | Exact callback durability NV | Source voucher on ledger [S7] | Exact callback durability NV | `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:723` uses after-commit callback | MATCH source completeness, W3/W4; implementation mechanism is our design |
| B6 | Cancelling posted work preserves original evidence | Exact mode NV | Reversals preserve history [S8] | Exact mode NV | `apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:143` records reversal linkage | ALREADY preserve reversal design; extend same discipline to repairs |
| B7 | Changing lot tracking does not silently reinterpret stock history | Explicit adjustment procedure [S9] | Exact edit-after-use rule NV | Exact edit-after-use rule NV | `apps/api/app/Modules/BatchExpiry/Presentation/Requests/UpdateBatchRequest.php:22` permits identity/expiry edits | MATCH controlled correction, W-LOT/W5 |
| B8 | Staff branch scope survives alternate endpoints; replays do not double effects | Exact API scope/replay NV | Exact API scope/replay NV | Exact API scope/replay NV | `apps/api/app/Modules/Treasury/Presentation/Controllers/CashPositionController.php:75` scopes locations; repository list `PaymentRepositoryController.php:39` does not | MATCH project guarantees, W1/W2/W4; no unsupported competitor-equivalence claim |
| B9 | Cashier can see lot identity alongside a tracked cart item | Odoo 18 shows imported tracking numbers below products and permits editing [S10]; 19 exact placement NV | Inline batch editor supports POS Invoice [S11]; register cart placement NV | TakePOS exact lot placement NV [S12] | `apps/pos/src/types/product.ts:50` has no lot data; cart renders `CartLineItem` at `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:282` | MATCH visibility in W-LOT/W6 iteration 1; deliberately DIVERGE from editable capture: read-only suggestion per R3 |
| B10 | Cashier sees the next FEFO lot and expiry before adding an item, with the same guidance on its cart line | Read-only FEFO lot/expiry on product tile NV [S10] | Read-only FEFO tile/expiry NV [S11] | Read-only FEFO tile/cart expiry NV [S12] | `apps/pos/src/types/product.ts:50` lacks branch lot eligibility; tile uses `ProductCard` at `apps/pos/src/components/organisms/ProductGrid/ProductGrid.tsx:760` | ADD owner-required R3 guidance, W-LOT/W6; module-gated, cached, no selection and no sealed lot fields; no competitor-equivalence claim |

Sources: [S1 Odoo inventory adjustments](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/warehouses_storage/inventory_management/count_products.html); [S2 ERPNext stock specification](https://github.com/frappe/erpnext/blob/develop/erpnext/stock/spec/README.md); [S3 Dolibarr lot/serial module](https://wiki.dolibarr.org/index.php/Module_Lot_/_Serial); [S4 ERPNext stock reconciliation](https://docs.frappe.io/erpnext/stock-reconciliation); [S5 Odoo POS payment methods](https://www.odoo.com/documentation/19.0/applications/sales/point_of_sale/payment_methods.html); [S6 Dolibarr inventories](https://wiki.dolibarr.org/index.php/Inventories); [S7 ERPNext general ledger](https://docs.frappe.io/erpnext/general-ledger); [S8 ERPNext immutable ledger](https://docs.frappe.io/erpnext/immutable-ledger-in-erpnext); [S9 Odoo reassignment of lot/serial numbers](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/product_management/product_tracking/reassign.html).

Additional POS sources, checked 2026-09-05: [S10 Odoo 18 POS serial numbers and lots](https://www.odoo.com/documentation/18.0/applications/sales/point_of_sale/shop/serial_numbers.html); [S11 ERPNext inline serial/batch editor](https://docs.frappe.io/erpnext/use-inline-serial-batch-editor); [S12 Dolibarr TakePOS](https://wiki.dolibarr.org/index.php/Module_Point_of_sale_(TakePOS)). The Odoo 19 POS page could not be fetched reliably; the 18 display behavior is not asserted for 19. ERPNext's POS Invoice editor is not proof of register tile behavior. These sources do not establish offline FEFO cache freshness or captured physical evidence.

Second-of-everything: each affected catalogue lane must exercise second-company creation through the real provisioning path, a second POS-enabled location, and a repeated mutation with unchanged quantities/balances. Two lots and two terminals are additional mandatory fixtures here. Cross-tenant negative cases are separate from second-company cases.

## 2. Scope, alternatives and decisions

### 2.1 Confirmed intent and owner rulings

POS remains offline-first fiscal authoring; web sales documents remain online B2B, with future offline B2B excluded. Account-purpose configuration remains the way to map business meaning to account numbers. This round revises documents only.

- **R1:** one A-to-Z W-LOT package owns the lot parts formerly split across W1/W5/W6, including all handover residuals. Internal W5/W6 labels survive solely for review traceability.
- **R2:** every lot UI, API field/route and projection leg honors `BatchExpiry`; a product's tracking flag or parapharmacy vertical alone grants no entitlement.
- **R3:** iteration 1 displays the FEFO suggestion (batch number/expiry or available cached identifier) on the tile and cart line, read-only. No selection or sealed lot payload change. Captured checkout is iteration 2 behind D3/D7.
- **R4:** retain architecture option 1, targeted corrections.

### 2.2 Architecture alternatives

1. **Owner-selected: targeted corrections to existing boundaries.** Preserve the modular monolith, separate POS/B2B authoring, shared inventory/treasury contracts, transactional B2B posting and recoverable POS projections.
2. **Containment-only pilot:** possible only under explicit owner-approved scope and operating restrictions; it does not resolve missing financial effects or turn estimates into physical traceability.
3. **Unified event-sourced rewrite:** excluded. No new general posting DSL, microservices, replacement chart engine or unified POS/B2B lifecycle.

### 2.3 Single open owner-decisions register

All ten rows remain **OPEN**. Recommended defaults are proposals, not decisions. R1–R4 above do not resolve these rows. Review decisions 2–4 retain identifiers RD2–RD4; review decision 1 is D7 and review decision 5 is D1–D6.

| ID | Status | Decision and recommended default | Alternative and consequence / gate |
|---|---|---|---|
| D1 | OPEN | Confirm branches are locations of one legal company | Separate legal entities require intercompany design; never model cross-company custody as an internal branch transfer |
| D2 | OPEN | Confirm POS/B2B usage and exact enabled tenders; release only the selected surfaces/methods | Either/both surfaces allowed after their acceptance; unused paths remain excluded, not marked tested |
| D3 | OPEN | Keep R3 iteration 1 as suggestion-only shelf guidance; recommend captured-lot capability as iteration 2 when physical traceability is required | Staying on shelf control requires a written expiry/recall procedure, estimate/unknown labels, no software-captured traceability claim and a dated revisit; if capture is a launch requirement, defer launch of that use case until iteration 2 |
| D4 | OPEN | Recommend refreshing branch eligibility before opening a session; permit cached guidance during that session with visible snapshot age/staleness; require refresh before next session | Owner may choose a stricter age cutoff or longer offline tolerance, each with explicit unavailable/stale behavior; applies to iteration 1 cache and later capture. No silent hard cutoff or invented expiry; no guarantee of knowing recalls issued while offline |
| D5 | OPEN | Branch managers operate allowed custody; central treasury has explicit all-location authority | Company-wide manager custody is possible only as an explicit policy; approve source/destination reach before W1 execution |
| D6 | OPEN | Exclude cross-branch returns from initial release | Enabling requires receiving-branch stock/lot disposition, original provenance, payout custody and two-branch acceptance |
| D7 | OPEN | Iteration 2 only: recommend optional line-level lot fields in a new `SALE_RECEIPT` vN+1, required by captured-mode capability | Inline evidence uses the receipt's SQLite transaction/outbox and ordering; sealed errors need append-only corrections. A separate versioned evidence stream preserves fiscal shape but adds linkage/hash checks, local atomicity, reordered arrivals and conflicts. It still needs append-only corrections. Neither transport is selected here; no iteration 1 lot version/outbox work depends on D7 |
| RD2 (review 2) | OPEN | Recommend restricting company-wide `batches.recall` to designated central inventory authority | Keeping seeded manager recall makes it intentionally company-wide despite branch membership; either option still needs action checks. Blocks final W-LOT role defaults |
| RD3 (review 3) | OPEN | Recommend reversing H-3's unmapped noncash → drawer day-one pin: unavailable until valid settlement configuration | Retaining fallback preserves the known CARD→drawer risk; electronic tender launch must remain excluded. Do not change the pin/seed behavior without this ruling |
| RD4 (review 4) | OPEN | Recommend delivering fiscal-derived W7 first and deferring repository-balance comparison until float and drops are booked | Funding Treasury float/drawer-operation coverage first enables the separate repository check after source/idempotency/opening-baseline acceptance. Neither option permits comparing ledgers with different coverage or enabling the disabled variance writer prematurely |

NF525/NACEF names describe intended product use. This spec makes no certification claim and does not invent Tunisia tax, retention or receipt-format requirements. Those require review of the actual deployment/configuration under the existing owner gate.

## 3. Architecture and invariants

### 3.1 Ownership

| Owner | Authority | Must not do |
|---|---|---|
| Fiscal POS | Local immutable receipt/session authoring and durable synchronization | Depend on server GL availability to complete offline checkout; rewrite sealed bytes |
| Web B2B | Document transitions, deliveries, invoices, credit notes and terms | Duplicate a POS sale as another revenue-producing invoice automatically |
| Inventory | Movement, custody, lot allocation, count discrepancy and valuation inputs | Claim a computed FEFO allocation was physically scanned; derive missing lot identity from aggregate counts |
| Treasury | Payments, allocations, custody, instruments and settlement | Route electronic money to physical cash merely to avoid an error |
| Accounting | Journal interpretation of operational facts, account-purpose resolution and reversals | Make old posted entries change when configuration changes |

Existing `apps/api/app/Modules/Accounting/Application/Services/PartnerBalanceService.php:35` uses posted journal lines for balances (audit architecture context), and `apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:50` coordinates with GL inside a transaction. Preserve that behavior in this program. “Derived accounting” does not mean an optional or disposable ledger in the current system.

**Vocabulary:** Company, Location, Lot (batch), Repository, Payment, Document and Shift use [the canonical glossary](../../glossary.md). Lot evidence and Session reconciliation are registered there in this spec round; their proposed storage/write paths are explicitly distinguished from shipped behavior.

### 3.2 Global requirements

- Existing sealed fiscal bytes, classes, version schemas and posted journal amounts are immutable. New protocol capabilities use explicit new versions or separate evidence records; old readers/verifiers remain supported.
- Every operational effect has a stable source identity. Re-delivery yields the prior semantic result; conflicting content under the same identity is a visible conflict, never silently overwritten.
- Acknowledging a POS fiscal event means its event and mandatory projection obligations are durable, not that all projections have completed. UI distinguishes received, pending, blocked and applied.
- Tenant DB, company, terminal and location provenance must be explicit in workers. Never use HTTP-only scope resolution inside projection workers.
- Strings/BCMath for monetary values and quantities. Currency resolution takes the entity currency; TND uses its configured scale, quantities use the existing quantity precision contract. Preserve TTC POS versus HT B2B semantics.
- Use existing movement ports, fiscal projections, permissions and operator surfaces. Extend these rather than create a second source of inventory, money or outstanding balances.
- Restrict only new commands when policy changes; preserve already sealed offline facts. Revoked staff/configuration on synchronization is an exception requiring attribution and review, not permission to delete history.
- Accounting failure, incomplete synchronization and unknown lot allocation must never render as a clean zero balance or a reconciled close.

## 4. Work packages and fixing measures

Paths are relative to the repository. Named new components below are proposed additions, not existing code. File maps identify seams for the later task-level plan; they are not authorization to edit.

### W0 — Guards against the five recurring mechanisms

Source: [root-cause archaeology](../reviews/2026-09-05-parapharmacy-remediation-root-cause-archaeology.md). These are requirements for later implementation and reviewer gates; no guard/test is implemented in this round. A guard closes its mechanism only when its own liveness check fails on a deliberately introduced violation, not merely because its file exists.

| Guard | Mechanism addressed | Required deliverable and acceptance |
|---|---|---|
| G1 Route action-permission ratchet | M2: enforcement point absent on bodyless routes (F2) | Enumerate mutating module routes, including DELETE and path-only POST. Accept permission-bearing `can:`/`permission:` middleware (including inherited groups), a verified FormRequest permission check, or a reviewed explicit equivalent; authentication/module gating alone is insufficient. Use a shrink-only legacy-debt baseline, patterned on `TenantOnlyUniqueOnCatalogueTablesRatchetTest`. A new unchecked mutation fails; existing checked create/update paths must not become false positives. W-LOT removes recall/destroy debt |
| G2 Location-scope coverage census | M2 opt-in omissions and M3 incomplete surface coverage (F3) | Inventory every company-scoped read endpoint and annotate scoped / bypass-declared / unscoped with its permission and primary service. Baseline unscoped legacy debt; fail additions or undeclared bypasses. W1 covers repository reads plus a companion mutation inventory including transfers and adjustments; proving one cash-position page is scoped is insufficient |
| G3 Full resolver matrix | M3 fixing one direction only; M5 H-3 pin (F4) | W2 must exercise cash/electronic × drawer/settlement (all four cells), unmapped per class, maturity custody separately, second branch/company and replay. Name H-3 reversal and gate it on RD3; do not turn the old fallback into an acceptance requirement |
| G4 Lot-model completeness and vocabulary | M1 lot model stopped before POS/counts (F1/F7/F8/F6) | Register lot evidence with `operator_captured`, `system_fefo_estimate`, `unknown`; inventory every producing/reading surface and preserve provenance through DTO/UI/export/return paths. W-LOT must cover every ordered gap and reject any surface presenting estimated FEFO as captured |
| G5 Re-ratify seal/GL atomicity with PostgreSQL | M4 early seal-wins decision outlived its audit-listener purpose (F5.1) | W3 supplies the writer/lock census and a real PostgreSQL concurrent posting leg demonstrating no sealed invoice without its GL and no chain collision. SQLite-only acceptance is insufficient |
| G6 Standing projection recovery scenarios | M3 retrying failures while overlooking lost work (F5.2) | Every later projection lane's acceptance contains: registry throws at ingest; enqueue lost after commit (including pending/attempts=0); worker dies in running, including after effect commit. State exactly-once semantic result or visible blocked outcome; a justified not-applicable row needs reviewer evidence, not omission |
| G7 Tests assert data meaning | M5 tests froze limitations as correctness (F4/F6/F7) | Any intentionally retained limitation test gets `_pins_limitation_` or an equivalently grep-able marker and a linked decision/ticket/exit criterion. Census H-3, DEFAULT top-up and Z copied-total pins; replace resolved pins with economic/inventory meaning assertions. Do not delete useful regression coverage or rename it to imply the defect is fixed |

**Dependencies:** Fable sequences guard rollout and baselines alongside affected packages. G1/G2 are repository-wide coverage censuses, not permission to remediate unrelated modules in this program. Future guards must preserve explicit fiscal ingress authority and service-level equivalents rather than force HTTP user permissions into workers.

### W1 — Treasury location scope and custody

**Findings:** audit F3; B8; review F-E. Lot action enforcement is owned only by W-LOT, narrowed to recall/destroy/read routes; existing authorized batch create/update/transfer/write-off remain regression coverage.

**Baseline:** `apps/api/app/Modules/Treasury/Presentation/Controllers/CashPositionController.php:75` filters locations; repository list/detail are company-scoped at `PaymentRepositoryController.php:39,58`. `RepositoryTransferService.php:43,118` resolves custody without staff location authorization (full service prefix: `apps/api/app/Modules/Treasury/Application/Services/`). The previously omitted adjustment route is `apps/api/app/Modules/Treasury/Presentation/routes.php:98`; its adapter builds a company/user intent at `RepositoryAdjustmentController.php:58` under the same controller prefix. The adapter alone does not prove downstream location enforcement: require the request/service census before claiming that path is safe.

For repository list/detail/balance/transactions/movements and manual adjustments, apply `LocationScopeResolver` and `LocationScopeBoundary`. A restricted user sees repositories in allowed locations. Locationless central repositories are not exposed merely because location is null. Propose `treasury.manage_all_locations` as the endpoint bypass, with owner/admin default subject to D5, through the existing role surface. Preserve explicit unrestricted membership (`LocationScopeResolver.php:31,53`, under `apps/api/app/Modules/Company/Services/`); do not reinterpret it as a restricted location list. Generic `repositories.view`, `treasury.transfer` and manager role do not bypass scope.

Transfer authorization uses persisted repository identities, not caller-supplied location labels. Require permission over source custody. A branch manager may transfer to an allowed branch repository or to an explicitly configured company central collection destination; reverse outflow from that central destination still requires central authority. Model destination eligibility as a repository setting on the existing repository surface, not a UI-only filter. Destination selection is a mode of the existing repository index; no second destination catalogue. For another branch destination, require that location in the user's allowed set or the explicit bypass. Company-global bank identifiers/balances must not leak through destination pickers; expose only permitted destination names/IDs.

No side effects on denial. API detail denial uses the established scoped-not-found convention; rejected explicit out-of-scope list selections use the existing forbidden envelope. Reauthorize inside mutation orchestration against current persisted source custody, so metadata changes cannot bypass a stale precheck.

**Seams:** under `apps/api/app/Modules/`: `Treasury/Presentation/Controllers/PaymentRepositoryController.php`, `RepositoryMovementController.php`, `RepositoryTransferController.php`, **`RepositoryAdjustmentController.php`**; adjustment request and `RepositoryAdjustmentService` public orchestration; `Treasury/Presentation/routes.php:98–100`; `Treasury/Application/Services/RepositoryTransferService.php`; `Company/Services/LocationScopeResolver.php`, `LocationScopeBoundary.php`; permission seeder and existing repository forms.

**Acceptance:** A-only manager cannot list/read B's drawer through any alternate endpoint, adjust it or transfer from it; allowed A→central transfer succeeds while central→A requires central authority; direct IDs and repository metadata changes cannot evade scope; company/tenant mismatch fails; denial leaves adjustment, movement and GL snapshots unchanged; explicit unrestricted membership and authorized bypass still work; permission-cache refresh updates access. D5 fixes the final policy.

### W2 — One tender destination binding

**Finding:** F4; B3/B8; review F-A. Electronic tender enablement remains gated by D2/RD3.

**Baseline:** the device chooses the first active bank/virtual repository at `apps/pos/src/stores/paymentStore.ts:1185`. The canonical payment carries the method but no destination at `apps/api/app/Modules/Fiscal/Domain/DTOs/Canonical/PaymentDTO.php:27–34`; the receipt bridge explains discarding the local destination at `apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:379`. `TenderRepositoryResolver.php:157,198` (under `apps/api/app/Modules/Treasury/Application/Services/`) accepts noncash→drawer. H-3 explicitly pins seeded fallback to `CASH-01` at `apps/api/tests/Feature/Tenant/CleanRegistrationDownstreamAssumptionsTest.php:133–151`. These are corrections to intentional existing behavior, not absent functionality.

Extend payment-method routing classification only as far as the resolver needs: proposed `cash`, `maturity_instrument`, `settlement`. Validate it against existing flags and forbid conflicting activation. Vouchers and customer-account legs stay on their dedicated paths outside this resolver (receipt-bridge routing census must preserve those branches); no five-value enum or replacement payment engine.

Resolve one binding from company/branch/terminal tender policy. **Retire the first-active device pick**, including alternate card/mixed-tender entry points found by the later census. Device authoring, local receipt persistence and server projection must use the same method/destination binding; the policy cache is distribution of that binding, not a competing authority. The device cannot substitute the first repository it happens to hold.

Evaluate the existing sealed precedent first: `apps/api/app/Modules/Treasury/Application/Projections/TreasuryAccountPaymentBridge.php:406–418` requires and validates a sealed repository reference within tenant/company. Recommended W2 transport is resolved `repository_id` plus the necessary binding revision on a **new compatible SALE_RECEIPT version**, validated against tenant, company, terminal/branch authority, class, currency and compatible GL purpose. Retain the authored binding for replay. This avoids a second payment-evidence stream and closes device/server disagreement. An unsealed local field alone provides no replay proof; a separate payment snapshot adds lifecycle/linking cost without demonstrated benefit. The execution plan must confirm canonical version/cutover and local sync-ID handling before adopting the new version. W2 tender versioning is independent of D7's lot evidence choice; R3 forbids iteration 1 **lot** payload changes, not an independently gated W2 tender version.

Cash must reach permitted physical cash custody at its location; electronic settlement must reach an active mapped bank/virtual clearing repository with compatible GL/currency, never a drawer. Clearing means awaiting settlement, not cleared bank cash. Unmapped cash and unmapped settlement are separate matrix cases: preserve legitimate branch-cash provisioning, while new settlement use stays unavailable without a binding. Maturity instruments retain portfolio/custody semantics. Preserve mixed-tender change netting and rounding. No fictitious bank balance or guessed destination.

**Explicit H-3 reversal proposal (RD3, OPEN):** the old seeded noncash→drawer expectation must be superseded, with day-one seed/configuration readiness reporting an unavailable electronic tender until configured. Do not silently change that test or defaults before the owner rules. If RD3 retains fallback, exclude electronic tenders from this client's launch.

Already-projected legs resolve the existing payment before remapping (`TreasuryReceiptBridge.php:1297–1310`), and offline frozen-destination replay already has an alert policy (`apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:91–96`); preserve both. The new gap is authoring-to-first-projection drift. For unprojected legacy receipts lacking a binding, use an explicit tenant/company cutover mapping; ambiguous cases remain durable and blocked. Never use today's default as historical evidence. Reclassifying erroneous historical payments requires compensating movements, not backfill changes to sealed tenders or existing effects. Deactivation/frozen-state review must preserve the source event.

**Seams:** payment method configuration/DTO, `TenderRepositoryResolver`, both Treasury bridges, terminal policy sync, `apps/pos/src/stores/paymentStore.ts`, offline authoring/local receipt/sync mapping, canonical payment schemas and version cutover. Census every cash/card/mixed-tender writer before declaring the old picker retired.

**Acceptance matrix:** cash→drawer allowed, cash→settlement denied; electronic→settlement allowed, electronic→drawer denied even when a bank exists; unmapped per class; maturity custody; voucher/account bypass paths; two branches and second-company provisioning. A 100.000 TND card sale increases clearing by 100.000 and cash by 0.000. Remap between authoring and first sync must not change destination; existing projected replay remains unchanged; stale/forged binding, legacy ambiguity, mixed-tender change and duplicate/out-of-order delivery produce one authorized economic result or a visible exception.

### W3 — Atomic web B2B invoice accounting

**Finding:** F5 first case; B5/B6; review F-C. **Required if B2B invoices/credit notes are used.**

**Baseline:** after-commit dispatch at `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:723–726` leaves the invoice GL outside the source commit. `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:591–609` reads chain hash and unlocked max+1 (`apps/api/app/Modules/Accounting/Domain/JournalEntry.php:140–146`), while `apps/api/database/migrations/tenant/2026_07_08_100400_add_chain_sequence_unique_index_to_journal_entries.php:35–38` enforces company/sequence uniqueness. Concurrent listener posts can therefore collide after sealing; this is a source-derived failure mechanism, not a reproduced PostgreSQL incident.

Choose synchronous posting inside the document transaction for this online workflow. The legal document seal, its document-source GL, advance clearing and paid-state transition must either commit together or all roll back. Do not merely add more preflight checks before the current after-commit callback.

Expose a narrow shared accounting contract for idempotent document posting, returning the existing journal identity on retry. `DocumentPostingService` calls it inside its existing transaction; the accounting implementation validates source identity, period and account purposes and posts balanced legs using one company-chain locking convention shared with other GL writers. **No lock order is prescribed by this spec.** Before the execution plan, census every GL writer and nested path, including invoice/credit-note posting, advance clearing, stock/treasury writers and any path acquiring Document locks after GL keys. Record transaction owners and lock acquisition sequences. Today the document chain lock at `DocumentPostingService.php:673–678` precedes advance GL at `:239`, which takes tenant numbering then company chain keys at `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:5722–5731`. A GL-first rule could invert a live path. Choose and document one compatible order after the census; unresolved inversion blocks the plan. Replace unlocked allocation through the shared serialization contract, not a second allocator.

Require a read-only duplicate census before a proposed partial unique index for `source_type = Document`, following the per-source pattern (`apps/api/database/migrations/tenant/2026_06_26_120000_unique_journal_entries_source_procurement.php:46–47`). The base journal migration at `apps/api/database/migrations/tenant/2025_11_30_100000_create_journal_entries_table.php:44` supplies a nonunique source index; review r1's migration census found no Document-specific unique index. Recheck live schema and reversal/advance source discriminators before choosing the predicate. Do not impose global source uniqueness on unrelated journal types or delete/reseal historical duplicates. A conflicting existing document journal is blocked, not reused blindly.

Retire `InvoicePostedListener` as a second money writer for the new path. Preserve the existing `InvoicePosted` event class and other registered consumers. Persist any mandatory audit-event delivery obligation transactionally before deferring notification work; nonfinancial after-commit consumers may remain asynchronous. The later plan must account for all current event consumers, rather than remove the event or dispatch duplicate source events.

Prior sealed invoices missing GL need a read-only census. Recovery is explicit per source, idempotent, and blocked on ambiguity or closed/filed period rules. Do not silently book old invoices into today's period, modify their seal, or bulk replay already-booked events. Manual recovery requires the source amount/account snapshot and an accountant-compatible period decision where necessary.

**Seams:** `Document/Domain/Services/DocumentPostingService.php`; `Accounting/Application/Services/AccountingService.php`; `Accounting/Listeners/InvoicePostedListener.php`; `app/Providers/EventServiceProvider.php`; shared accounting contracts; journal source uniqueness/chain locks. No POS checkout code belongs in this package.

**Acceptance:** inject failure during account resolution, journal insert, advance clearing and final document transition: no partial seal/journal survives; retry after a lost HTTP response yields one seal and one journal; real PostgreSQL concurrent invoices (including advance-clearing paths) in one company cannot collide/fork the journal chain, deadlock through an inverted order, or leave a seal without GL; second-company chains remain independent; fully prepaid and partial-advance invoices, credit notes and closed periods retain correct semantics.

### W4 — Durable POS projection obligations and recovery

**Finding:** F5 second case; B5/B8; review F-F. **Required for POS use.**

Keep local sealing and server projection asynchronous. **The existing seeding is already transactional:** `apps/api/app/Modules/Fiscal/Application/Services/OutboxIngestor.php:1025–1046` persists projection rows in the event transaction and dispatches after commit. **The seeding hole is specifically registry fail-closed exclusion:** `apps/api/app/Modules/Fiscal/Application/Services/FiscalEventProjectionRegistry.php:248–267` catches module-resolution failure and omits the projector. Treasury is module-gated (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:214`), while POS-core is not (`FiscalEventProjectionRegistry.php:240–247`). Do not rebuild the transaction or invent missing POS-core seeding.

Preserve durable obligations for expected projectors using a recorded activation decision; when activation cannot be resolved, retain blocked work/configuration uncertainty rather than treating failure as disabled. A verified disabled module still authorizes no module effects. POS-core is mandatory for eligible events, Treasury for the approved Treasury-enabled deployment. W-LOT's lot legs require `BatchExpiry` separately; do not make them universally mandatory to fix a Treasury registry gap. Unknown entitlement means no lot writes and visible blocked resolution, not a guessed enablement. Record historical/cutover decisions so replay cannot silently reclassify disabled and failed-resolution cohorts.

The scheduled retry currently selects dead-lettered or pending rows with at least five attempts (`apps/api/app/Modules/Fiscal/Infrastructure/Commands/RetryFiscalProjectionsCommand.php:299–307`; scheduled at `apps/api/routes/console.php:57`). Add scheduled, tenant-aware recovery for **pending rows with lost enqueue, including attempts=0**, and **orphaned running rows**. Reuse dispatcher/job dependency, overlap/advisory locking and dead-letter rules. Claim work atomically; running-age expiry must exceed the real lease/job timeout and prove abandonment so an active worker is not stolen. Persist a recovery reason and use bounded batches/backoff. Worker death after effect commit but before status update must converge through existing effect idempotency. Redis order alone is not a dependency contract.

Existing event re-ingestion must re-drive missing required obligations using the event's recorded/cutover policy. Add a completeness census comparing accepted source events to expected projector names and results; treasury self-balance alone is insufficient when both a payment and its journal are absent. Preserve parse/integrity quarantine suppression rules: recovery must not accidentally apply quarantined invalid events. Training receipts must remain contained.

Existing dead-letter UI is the primary resolution surface. Show fiscal accepted plus financial pending/blocked status, age and reason; emit actionable alerts after the configured retry ceiling. Enumerate exception cohorts for operator-led backfill, never call current module configuration historical truth.

**Seams:** `FiscalEventProjectionRegistry.php`, `OutboxIngestor.php`, `FiscalEventProjectionDispatcher.php`, `ApplyFiscalEventProjectionJob.php`, retry/enqueue commands; fiscal projection schema if a policy snapshot is required; `routes/console.php`, `config/horizon.php`; existing projection-status UI.

**Acceptance:** central config lookup failure at ingest; Redis unavailable after commit; worker killed after effect commit but before status update; duplicate delivery; treasury runs before receipt dependency; wrong tenant worker context; quarantined/training event; module change between ingest and replay. Every eligible case converges to one complete set of effects or a visible blocked state, never missing work reported as complete.

### W-LOT — Complete batch management A-to-Z (R1)

**Single package / single acceptance gate:** audit F1/F2/F7/F8 and the lot portion of F6; B1/B2/B4/B7/B9/B10; review F-B/F-E. All former W1 lot authorization and W5/W6 work lives here. W5 and W6 below are internal sub-sections for v1 traceability, never separately competing lot lanes. Fable sequences this package; iteration 2 remains gated, so iteration 1 completion must never be reported as captured-lot completion.

**R2 entitlement invariant:** every device/web lot surface uses `hasModule('BatchExpiry')` (web routes also `moduleKey`); every lot API route uses `module:BatchExpiry` alongside action/company/location checks. Shared product/sync routes must omit lot fields and lot queries without entitlement. Projection/worker lot branches use explicit tenant/company module resolution, not HTTP middleware or a bound request context. A tenant without `BatchExpiry` sees **no lot UI and receives no new lot projection legs**, while its authorized aggregate/fiscal/treasury behavior continues. `requires_batch_tracking` and vertical alone do not enable the module. Apply to sales, refunds, counts, corrections, suggestions, traceability/export and caches. Existing module-off history is retained; unknown activation is blocked for lot work with an explicit reason. Disabling a module must neither erase existing evidence nor later replay skipped events into fabricated lot history; activation/cutover handling is shared with W4.

**Evidence qualification:** module route middleware exists at `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:12`; the device helper at `apps/pos/src/stores/productStore.ts:116` has coverage at `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts:5–30`. However, the inspected sale helper at `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2026` checks the product flag via `apps/api/app/Modules/BatchExpiry/Domain/Services/FEFOInventoryService.php:930–934`. That is not evidence of complete entitlement enforcement. Preserve the handover's no-module behavior as the required invariant and census upstream/return legs before claiming it already holds everywhere. Follow [vertical module gating](../../architecture/vertical-module-gating.md) and CLAUDE rule 12.

#### Ordered gaps and disposition

Each current-code claim below is anchored at `b9a5565aa`. Ticket line numbers from earlier snapshots are not silently reused as current locations.

| Order | Gap and current evidence | Required disposition |
|---|---|---|
| L1 | F2 bodyless recall/destroy and read routes: `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:14–48`; `apps/api/database/seeders/RolesAndPermissionsSeeder.php:407,632` seeds recall and grants it to manager | Enforce recall `batches.recall`, deactivation `batches.delete`, read `batches.view` and traceability `batches.traceability`; preserve module/company/scope checks. RD2 governs manager defaults. Preserve existing create/update/transfer/write-off checks, rather than describing them as absent |
| L2 | F8 editable number/expiry: `apps/api/app/Modules/BatchExpiry/Presentation/Requests/UpdateBatchRequest.php:20–24`, controller `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:165` | Freeze ordinary identity/expiry edits after first movement; append permissioned, reasoned correction history and preserve old evidence |
| L3 | Issue/transfer stringify a float accessor at `apps/api/app/Modules/BatchExpiry/Application/Services/BatchStockService.php:471,515–516`; accessor `apps/api/app/Modules/BatchExpiry/Domain/Entities/BatchStock.php:44–46` | Close residual (a) of [F7 FEFO ticket](../tickets/2026-08-08-f7-fefo-residuals.md) using validated exact raw decimal/numeric-string availability for both sufficiency guards; retain locking, reservation subtraction and quantity precision |
| L4 | F7 parent count lacks lot at `apps/api/app/Modules/Inventory/Domain/InventoryCountingItem.php:79–105`; DEFAULT target top-up pinned at `apps/api/tests/Feature/Inventory/InventoryCountingDefaultBatchTest.php:130–131`; heuristic shortage `apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:1551–1594` | W5 below supersedes [T22/OQ-7](../tickets/2026-08-19-counting-lane-c2-default-lot-inflation.md): actual lot observations determine both gain and loss; no whole-aggregate DEFAULT inflation. Old pin is a limitation, not a required target behavior |
| L5 | Estimate stored in receipt allocations at `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052–2081,2104–2114`; exposed without provenance at `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchTraceabilityController.php:80–88` | Label server FEFO `system_fefo_estimate` everywhere shown; missing physical identity remains `unknown`. Do not backfill `operator_captured` from estimated allocations |
| L6 | Read-only detector exists at `apps/api/app/Console/Commands/LotLedgerDriftCensusCommand.php:26–35,52`; no scheduling closure recorded in [W4R2 fiscal r2 N-5](../reviews/2026-08-25-w4r2-live-projection-gate-r2-fiscal.md#n-5-minor--owner-the-detector-is-shipped-but-nothing-schedules-it) | Schedule existing census per enabled tenant/company with `--fail-on-drift`, bounded overlap and actionable alert ownership; proposed daily run plus release/close checks. No automatic repair. Show coverage, last success and unresolved drift, not a false clean status after a missed run |
| L7 | F1 device product contract lacks lot state at `apps/pos/src/types/product.ts:50–119`; post-sale FEFO is non-strict at `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052–2081` | W6 iteration 1 adds branch eligibility cache and read-only tile/cart suggestion under R2/R3/D4; does not change sealed lot payload or physical traceability status |
| L8 | Physical capture absent from product contract above; fiscal engine is caller-transactional at `apps/pos/src/lib/fiscal/FiscalEventEngine.ts:10–11` | W6 iteration 2, only after D3/D7: captured lot evidence and projection consumption; evidence transport remains open |

For L1, existing FormRequest counterevidence is `CreateBatchRequest.php:22`, `UpdateBatchRequest.php:13`, `TransferBatchStockRequest.php:21` under `apps/api/app/Modules/BatchExpiry/Presentation/Requests/`. New enforcement scope is recall, destroy and reads. Company recall intentionally reaches branches; membership alone grants none, while a role carrying `batches.recall` does until RD2 changes it. Deactivation preserves rows/history and requires explicit stock disposition. Batch read/stock/suggestion endpoints must respect authorized location visibility without exposing other-branch holdings.

For L3, use the raw generated availability value or one exact string accessor, not a float round-trip; compare quantity-scale strings under the current locks. Verify on PostgreSQL at sufficiency boundaries and fractional quantities; SQLite generated-column behavior is not precision proof. The other F7 residuals require a current-source census before being counted as open or absorbed: for example the earlier tie-breaking observation is no longer accurately described by `FEFOInventoryService.php:95`, which now includes `created_at`. W-LOT owns disposition of any still-live lot residual found in that census; no historical ticket is silently marked closed by this spec.

For L5, inventory the receipt detail, forward/backward customer trace, exports, returns and any cached/web/device copies. Annotation must accompany every inferred allocation, including legacy rows. A shelf suggestion is guidance, a server allocation is an estimate, and `operator_captured` means recorded operator evidence, not independent proof of the physical world. Return handling must retain that distinction. Reuse the existing receipt allocation writer and traceability surfaces, not a second traceability application.

#### W5 — Lot-grain counts and controlled correction (inside W-LOT)

Keep `InventoryCountingItem` as the product/variant/location parent and add child lot-count observations rather than duplicate product-level totals or change every existing item key. For tracked products, each finalized observation carries batch identity, observed quantity, count time/movement marker and operator. The parent total is derived from accepted child observations. Missing row is not zero; an explicit zero means physically absent. Unknown physical lots require a recorded identification decision before finalization; do not invent expiry or silently assign DEFAULT.

Apply discrepancies to their actual lot through the stock adjustment service. Retain current aggregate cost/opening/replay rules, locking the product cost basis and relevant lot/stock rows consistently. Remove FEFO as a substitute for lot identity on new tracked counts. Sum of lot movements must match the aggregate adjustment for each product/variant/location. Finalization also checks absolute Σ lots against aggregate, so pre-existing drift cannot be hidden by a balanced delta. Re-derive the old DEFAULT test from identified lot observations; an explicit observed DEFAULT/unidentified opening cohort is not permission to top it up to the entire aggregate. An unchanged product total can still require offsetting lot corrections: booked 10+10 versus observed 15+5 produces +5/−5 lot movements and no net aggregate/GL quantity gain. Preserve valuation semantics and evidence for that reattribution. Re-run produces no additional movement or GL.

Reuse the existing count-as-of and movement replay guards (`apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:65–75`). If offline receipts can still alter the counted interval, finalization must require synchronized participating terminals or an explicitly provisional count that cannot claim reconciled final stock. Reuse `apps/api/app/Modules/Inventory/Application/Services/LateSyncResidualDetector.php:16–24` for the existing read-only late-sync signal rather than invent a second detector. A late pre-count movement reopens a reconciliation exception; do not silently change a finalized operator observation or apply its effect twice. Untracked products keep the existing counting flow. In-progress legacy tracked counts require completion under documented legacy semantics or cancellation/restart into lot mode; migration cannot fabricate observations.

After a lot has stock movements, ordinary update cannot change its identity or expiry. Add a permissioned correction action on its existing detail screen recording old/new values, reason, actor, timestamp and evidence reference. Use optimistic version checking to reject stale corrections. Corrections append history; original receipt/sale snapshots remain unchanged. Identity change must not merge two lots with existing histories. Deactivation of a lot with stock requires explicit disposition, not hidden removal from eligibility. Recall is a separate action, not an editable boolean.

**Seams:** `InventoryCountingItem.php`, count submit/review DTOs and services, `StockAdjustmentService.php`; additive tenant child table; `BatchExpiry/Presentation/Requests/UpdateBatchRequest.php`, `BatchController.php`, batch repository; existing `apps/web/src/features/inventory-counting` and `features/batches` surfaces.

**Acceptance:** 10 short-dated + 10 long-dated booked, actual 10 + 5: only long-dated lot decreases by 5; explicit zero versus omitted lot; surplus unknown lot; variant; count interrupted by sale/transfer; late sync; repeated finalize; active legacy count; concurrent correction; unauthorized expiry extension; two-company/two-location stock and GL reconcile.

#### W6 — POS lot guidance now, captured evidence later (inside W-LOT)

**R2 applies in both iterations:** device `hasModule('BatchExpiry')`, web `hasModule`/`moduleKey`, API `module:BatchExpiry`, gated sync fields and explicitly scoped worker lot legs. With the module absent: no lot tile/cart content, selection, lot-cache payload or projection legs. Null/unknown config is not permission to render or write lot data. Extend `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts` plus actual device/render/projection coverage; the helper test alone cannot prove the feature is gated.

**Iteration 1 — owner-ruled R3, display only.** Extend the existing product sync/cache with branch/product/variant lot eligibility: identifier, batch number where available, expiry (nullable/unknown honestly represented), active/recall eligibility, exact available quantity and snapshot time/revision. Scope by tenant/company/location; location/company change clears or replaces stale cached data. Derive the same deterministic FEFO suggestion for tile and cart from the same cache snapshot; use the established eligibility/removal order, do not maintain two independent FEFO algorithms. Offline guidance requires no synchronous checkout request.

The product tile and cart line show a read-only “Suggested lot” number and expiry, or the available cached identifier with unknown expiry clearly indicated. No selector, editable lot field, scan-to-capture behavior or confirmation is introduced. An ordinary SKU scan is not lot evidence. The cashier can use the suggestion to choose shelf stock operationally. Local known expiry/recall excludes a lot from suggestions, but this mode cannot establish which physical lot leaves the shelf.

Cache refresh, cart quantity/variant changes, date rollover and known recall changes recompute guidance from the same snapshot and make a changed/unavailable suggestion visible. If quantity spans multiple lots, show the next lot and that it does not cover the full line, without presenting one lot as the entire physical allocation. Empty cache/no eligible lot renders “suggestion unavailable”; unknown expiry never becomes a fabricated date or “safe” assertion. Surface snapshot age and staleness under D4. While D4 is open, no approved freshness cutoff or extra checkout refusal is implied. Existing SKU-level checkout semantics stay in force; unavailable guidance is not proof of safety or stock availability. No local suggestion reservation or global no-oversell promise is introduced.

**Iteration 1 leaves sealed payloads and the receipt's lot-evidence contract unchanged.** No lot fields, evidence outbox or captured-lot projection dependency. Server FEFO remains the authoritative computed allocation and is labelled `system_fefo_estimate`, even when it matches the displayed suggestion. Offline sales/order/timing may make the allocation differ; keep the receipt, report shortfall/drift via L6 and never promote the device suggestion into `operator_captured`. D3 governs whether shelf controls suffice for the intended release.

**Iteration 2 — conditional on owner D3 and D7.** `captured_lot` records scanned/confirmed lot identity and exact quantity using branch cache and approved freshness rules; `shelf_control` remains explicitly estimated. New captured-mode authoring must persist receipt and required evidence atomically in the caller's SQLite transaction. D7 chooses either new sealed line fields (recommended) or a separate versioned linked record; this spec does not select the stream. Inline fields use fiscal event integrity; a sidecar must bind terminal/company/location, receipt event ID/hash, line/product/variant, quantities, capture method and cache revision, enforce duplicate/conflict checks, and tolerate either arrival order. Both require mixed-version capability validation and append-only corrections; neither edits historical sealed bytes.

Regardless of D7, the receipt projection remains the single owner of sale stock effects. It consumes valid captured lots without substituting later FEFO as captured truth. Missing/conflicting evidence, insufficient lot balance or changed eligibility creates durable inventory reconciliation work while preserving the fiscal receipt and money effects; do not block all POS-core/Treasury work waiting for optional evidence. Track aggregate versus lot completion separately, reuse movement/source idempotency, and never decrement the aggregate twice when evidence later arrives. Resolution appends reason and movement links. A post-sale recall is not retroactive proof the sale was invalid; delayed-sale eligibility uses sale-time evidence where known and otherwise records uncertainty.

Offline terminals cannot guarantee global availability; simultaneous consumption becomes a visible conflict unless a separately approved allocation design exists. Refunds use original captured provenance where available plus actual returned lot/disposition; legacy estimates stay estimates. D6 separately gates cross-branch returns. No iteration 2 task plan or schema starts before D3/D7 and Fable dispatch.

**Seams:** `apps/pos/src/types/product.ts` plus server DTO generation/product sync/cache; canonical tile `apps/pos/src/components/molecules/ProductCard/ProductCard.tsx` and cart `apps/pos/src/components/molecules/CartLineItem/CartLineItem.tsx` (composed at ProductGrid:760 / TransactionCart:282 cited in B9/B10); alternate existing list/table renderers for consistent module hiding, no new concept surface; `PosCoreReceiptProjection`, FEFO suggestions, allocation DTOs and existing web receipt/lot screens. Iteration 2 alone adds the chosen fiscal version or evidence ingress/storage and local persistence migration. Iteration 1 may need an additive cache schema, never a sealed-lot migration.

#### W-LOT acceptance matrix (later execution; nothing run in this round)

All applicable rows require **two companies through real provisioning, two locations, two real lots, and re-run idempotency**; add two terminals for POS rows and separate cross-tenant denial fixtures. Module-off and missing-config fixtures are mandatory, not optional vertical smoke tests.

| Coverage | Discriminating scenario | Required outcome |
|---|---|---|
| L1 action authority | Cashier/read-only staff recall/delete; manager with allowed versus denied RD2 role; direct trace/stock IDs across company/location | Denied mutations change no stock/history; permitted company recall spans branches; read visibility honors authority; existing gated create/update/transfer/write-off remain valid |
| R2 all layers | Module disabled, missing config, enabled extra, revocation with cached data; sale/refund/replay in worker with no CompanyContext | No unauthorized lot UI/data/legs; authorized aggregate/fiscal behavior preserved; activation uncertainty visible; old evidence not destroyed |
| L2 used-lot correction | Movement exists; ordinary expiry extension/rename; concurrent authorized correction | Ordinary mutation refused; old/new values, actor, reason and evidence retained; stale update rejected; no merging used histories |
| L3 exact availability | Fractional issue and transfer at exact sufficiency/shortfall boundary, reservations, concurrent movement, PostgreSQL decimals | Exact comparison, no float/scientific-notation guard failure; no overdraw or double transfer; quantity and custody invariant preserved |
| L4 actual count | Booked short/long 10+10, observed 10+5; also surplus, explicit zero, missing row, 15+5 with same aggregate and variants | Correct lot alone changes, or offsetting lot corrections when appropriate; parent is child sum; no FEFO shortage guess or whole-aggregate DEFAULT top-up; Σ lots equals aggregate at accepted finalization |
| L4 drift and timing | Pre-existing DEFAULT inflation, unknown physical lot, legacy active count, sale/transfer during count and late pre-count receipt | Identification/reconciliation blocks false finality; existing late-sync detector reused; no invented lot observations/expiry; repeat finalize has no extra movement/GL |
| L5 provenance | Existing inferred allocation, absent allocation, current tile suggestion, forward/backward trace, export and return | Every FEFO allocation displayed as `system_fefo_estimate`; absent identity `unknown`; nothing relabelled captured; estimates cannot certify physical traceability |
| L6 scheduled detection | Drift, clean cohort, missed/failed run, duplicate scheduler and wrong tenant, contained lot-arm failure | Scoped read-only census, alert and last-success visibility; no correction writes; projection can preserve fiscal sale while drift stays visible |
| L7 iteration 1 | Two lots, tile→cart, quantity exceeds first lot, variant/location switch, expiry rollover, recall/cache refresh, stale/empty cache and offline retry | Same read-only next-lot guidance with number/expiry or available identifier; changes/unavailability honest; no lot selector or capture, no sealed-lot bytes changed; server estimate may differ |
| L8 iteration 2, D3/D7 only | Local crash boundaries, duplicate/conflicting evidence, either delivery order for sidecar if selected, cross-terminal forgery, late expiry/recall, disconnected contention and original-lot refund | Atomic local required evidence; immutable fiscal facts; one aggregate/lot effect or durable exception; no FEFO substitution labelled captured |

W-LOT's later handback must map every L1–L8 item to evidence, explicit gated deferral or unresolved finding. T22/OQ-7 is superseded in design by L4, not declared implemented/closed today. A balanced lot ledger is quantity consistency, not proof of physical lot capture.

### W7 — Session reconciliation against fiscal-derived totals

**Finding:** F6; review F-D. **Required for a trusted POS branch close.** Session reconciliation means comparing authored device totals with independently derived fiscal-event totals, not bank reconciliation, orphan-session closure or a Treasury balance comparison. Its canonical noun/write path/surface is registered in [the glossary](../../glossary.md).

Retain signed device close/Z records. Proposed `PosSessionReconciliationService` owns one result store (`pos_session_reconciliations`, proposed) keyed by tenant/company/terminal/session/Z identity, with append-only run history, input fingerprint and states `pending_sync`, `blocked`, `mismatch`, `matched`. The existing shift/Z detail is the primary operator surface. Show each check's coverage and provenance. A fiscal-totals `matched` badge must not claim captured lots or reconciled Treasury custody; lot quantity/evidence status and the separate repository check stay visible. A broad “fully reconciled” badge requires all launch-selected checks and their dependencies, without hiding an incomplete leg.

**Reuse and extend `apps/api/app/Modules/POS/Application/Services/ShiftExpectedCashService.php:27–59,252–299` as the single shared derivation for this work.** It already handles fiscal cash movements, net cash takings, opening float and account collections. Its comments distinguish v3 fiscal-session events from v2 drawer operations and acknowledge remaining legacy consumer boundaries; do not claim all historical report code already shares it. Add any missing membership-aware input selection to the shared derivation rather than copy its arithmetic into a new reconciliation service. The reconciliation service orchestrates completeness/comparison and stores results; it is not a second expected-cash calculator. Preserve explicit bounded shift end and terminal authority (`ShiftExpectedCashService.php:61–80`).

Compare device tender/cash totals against that fiscal derivation. Derive sales and refund VAT separately by rate/category from verified canonical receipts; compare like-for-like headers/buckets using shared precision and rounding rules. Do not simply enable the invalid sum check skipped for refunds at `apps/api/app/Modules/Fiscal/Application/Services/FiscalPayloadConstraintValidator.php:1071–1073`. A changed classification or unresolved source cannot silently turn a value into zero; use W2 authored/cutover classification and block ambiguous history.

**Membership has useful existing checks, not a blank slate:** `apps/pos/src/lib/offline/zReportService.ts:749–752` uses legacy receipt hash/hash-sequence bounds, not canonical operational sequence numbers. `apps/api/app/Modules/POS/Application/Projections/ZReportProjection.php:288–294` excludes refunds from the receipt count and `:305–319` waits for projected verified sales in a device-time window. Preserve these checks, but they do not establish complete membership of refunds, collections and cash-operation streams. A later plan must define a versioned close manifest covering every contributing stream with identities/hashes/counts and explicit sequence namespaces. Reuse proven bounds where sufficient; never treat receipt count as all-event count or timestamp inference as full completeness. Historical insufficient bounds remain explicitly limited/pending.

Late members within a declared close boundary trigger a new fingerprint and recomputation. Outside-boundary events naming the session create exceptions, not deletion of fiscal facts or rewritten Z totals. Projection/evidence incompleteness keeps the relevant check pending/blocked. Concurrent receipt/projection arrivals cannot produce a clean badge from a mixed input set: evaluate a stable input snapshot and invalidate the result when its dependencies change.

**Separate optional check: repository-balance coverage, gated by RD4.** `apps/api/app/Modules/Treasury/Application/Listeners/PostShiftCashVarianceAdjustment.php:50–56` explicitly ships the variance GL listener disabled because Treasury lacks opening-float and mid-shift-operation booking. Do not compare today's repository balance to whole-drawer expected cash as if coverage matched, and do not enable this listener as a shortcut. W7's fiscal comparison can proceed independently after its prerequisites.

If RD4 funds repository coverage, its later plan must book opening float and drawer movements (including drops) through existing Treasury services with stable source identities, correct counter-custody, opening-balance alignment and replay protection. A shift's float count is not automatically new company money: reconcile it to the existing drawer opening or a safe→drawer transfer, never double-book it on every shift. Only then compare repository opening plus covered movements against the corresponding fiscal/physical interval. Include back-office drawer movements once, attribute shared drawers across terminals and avoid counting two opening floats. Bank settlement is a different interval/check. Until coverage is accepted, show repository comparison as unavailable/not enabled, never matched. No new Treasury booking policy is approved by this spec.

**Seams:** shared `ShiftExpectedCashService`; `ZSessionLifecycleProjection`, `ZReportProjection`, fiscal payload validator and `OutboxIngestor`; proposed reconciliation result service/table and existing shift/Z detail; device close manifest/version cutover. Reuse expected-cash derivation, Z authoring and existing exception surfaces. No new orphan-close reconciler, bank-reconciliation surface or automatic variance money writer.

**Acceptance:** taxed sale plus partial refund, multiple rates, rounded cash/card, account collection, opening float and fiscal drops, verified membership gaps, missing projection, late valid member, out-of-manifest receipt, refund-only negative VAT bucket, duplicate Z and legacy insufficient bounds. Sealed hashes with wrong economic totals must mismatch. Lot estimate status remains explicit even when fiscal totals match. Before RD4 coverage, repository comparison is unavailable; after it, actual booked float/drops/back-office movements and two terminals sharing custody reconcile without double entries. Real independent expected values are required, not copies of device fields.

### W8 — Client-specific release proof and migration discipline

**Scope:** integrate W0/W1–W4/W-LOT/W7 without expanding into a new product; W5/W6 are internal W-LOT references. Only owner-selected iteration 2/RD4 capabilities enter release scope. Run against the chosen release artifact, PostgreSQL tenant topology and actual terminal build.

- Additive tenant migrations first, then compatible server readers, then explicit per-company/terminal capability enablement. Keep old fiscal readers. Do not enable new tender/lot/close authoring before its server acceptance and recovery paths exist.
- Census existing repository routing, sealed documents without GL, missing projections, DEFAULT/unknown lots, in-progress counts and unresolved closes. Backfills may annotate provenance/capabilities; financial corrections require explicit compensating operations. Never fabricate captured lots or approvals.
- Back up and rehearse restore before cutover. Rollback disables new authoring capability while retaining ingestion/readers for already emitted new versions and all outbox data. No destructive migration rollback over financial history.
- Extend the existing onboarding campaign with two-branch/two-terminal journeys and real device smoke. Synthetic fiscal vectors are useful but do not replace Tauri/SQLite/printer/offline/crash testing.
- Gate on every required leg's result and candidate revision, not an empty findings list or process exit alone. Any required skipped/blocked leg means incomplete evidence.
- Record the Tunisia accounting/configuration review and existing owner launch gates on the actual release. Do not copy an old blank checklist as fresh approval.

## 5. Dependencies and review boundaries

This table describes dependencies, not authority to dispatch work or a fixed parallel schedule. Fable alone sequences lanes from accepted specs and briefs. W-LOT is one package even if Fable breaks its ordered work into implementation tasks.

| Package | Prerequisites and plan gates | Completion boundary |
|---|---|---|
| W0 | Fable r2 and guard/baseline review | Seven live guards/standing acceptance obligations linked to their mechanisms; unrelated legacy debt remains explicit |
| W1 | D1/D5 and G1/G2 endpoint/custody census | Consistent repository reads, transfers and adjustments under approved scope |
| W2 | D2/RD3, writer/binding census and fiscal version cutover review | One binding from authoring through first projection/replay; explicit seed behavior reversal only after ruling |
| W3 | B2B selected by D2; complete writer/lock and duplicate-source censuses | Atomic seal/GL/advance state, shared allocator and PostgreSQL concurrency proof |
| W4 | Activation/cutover contract shared with W-LOT; G6 standing scenarios | Registry failure cannot erase work; lost enqueue and abandoned running recover |
| W-LOT L1–L6 / W5 | R1/R2; RD2 role ruling; approved inventory contracts; W0 guards | Lot permissions, used-lot history, exact issue/transfer guards, actual counts, provenance and scheduled drift detection |
| W-LOT L7 / W6 iteration 1 | R3; R2 module checks; D4 freshness; branch cache and consistent suggestion rules; L3/L5/L6 | Read-only tile/cart guidance, unchanged sealed lot contract; D3 still governs suitability for launch |
| W-LOT L8 / W6 iteration 2 | D3/D7 owner decisions, D4 freshness, W4 recovery and reliable lot balances | Captured evidence only through chosen transport, fiscal money preserved on inventory exceptions |
| W7 fiscal comparison | W2 classification, W4 source completeness, manifest contract; W-LOT status integration for lot-enabled launch | Independently derived session totals and honest per-check coverage; no assumed Treasury balance parity |
| W7 repository comparison | RD4 and separately accepted Treasury float/drop coverage | Comparable custody intervals, no duplicate float/variance entries |
| W8 | Every launch-selected package, exclusions recorded under D1–D7/RD2–RD4 | Same-candidate two-company/two-branch/two-terminal acceptance and owner launch gate |

Every later plan must name files/contracts, focused discriminating tests, migration/cutover/backfill order, operational alerts, compatibility and Fable's reviewer gates. Shared permission, GL and module-policy seams must be reconciled by Fable before integration. An approved spec or program dependency table is not implementation authorization.

## 6. Required evidence for later implementation

| Requirement | Evidence | Existing anchors to extend (backend paths under `apps/api/`) |
|---|---|---|
| W0 | Ratchet liveness, coverage baselines, grep-able limitation register and standing recovery rows | `TenantOnlyUniqueOnCatalogueTablesRatchetTest`; archaeology G1–G7 |
| W1 | Real HTTP authorization plus unchanged movement/GL snapshots for denied reads/mutations | `tests/Feature/Identity/UserManagement/UserLocationAccessTest.php`, `tests/Feature/Treasury/RepositoryTransferEndpointTest.php`; adjustment endpoint coverage |
| W2 | Full cash/electronic destination matrix, one binding, remap-before-sync and legacy replay | `tests/Feature/Treasury/BranchCashRepositoryRoutingTest.php`, `TenderRepositoryResolverTest.php`, `PaymentRepositoryCompanyScopeTest.php`; `tests/Feature/Tenant/CleanRegistrationDownstreamAssumptionsTest.php` H-3, RD3 open |
| W3 | Fault injection inside document transaction; PostgreSQL concurrent writers, unique source and chain continuity | `tests/Feature/Accounting/DocumentGlPreflightTest.php`, `InvoicePostedListenerTest.php`; new PostgreSQL concurrency leg after census |
| W4 and every projection lane | Registry throw; enqueue lost after commit with attempts=0; worker dies in running/between effect and status; dependencies, quarantine and training | `tests/Feature/Fiscal/FiscalEventProjectionRegistryTest.php`, `ApplyFiscalEventProjectionJobTest.php`, `FiscalEventProjectionDispatcherTest.php`, `RetryFiscalProjectionsCommandTest.php` |
| W-LOT L1–L6 / W5 | Complete acceptance matrix above; real-lot count meaning, exact PostgreSQL availability, provenance and scheduled read-only drift | `tests/Feature/Security/BatchExpiryModuleAccessControlTest.php`, `tests/Feature/Inventory/InventoryCountingDefaultBatchTest.php`, `tests/Feature/BatchExpiry/BatchStockServiceVariantTest.php`; existing drift and late-sync detectors |
| W-LOT W6 iteration 1 | Actual tile/cart rendering, module on/off, shared cache age/eligibility, no selection or sealed-lot change | `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts`, existing ProductCard/CartLineItem tests and SQLite/device integration; `tests/Feature/Fiscal/PosCoreReceiptProjectionBatchLotTest.php` |
| W-LOT W6 iteration 2 only | D7-dependent local atomicity/cutover, single aggregate writer and evidence conflict recovery | Same receipt projection anchors plus new chosen-transport/device tests; not an iteration 1 requirement |
| W7 | Independent economic expected values, stream completeness, coverage labels; RD4 separate check | `tests/Feature/Fiscal/ZReportProjectionTest.php`, `FiscalPayloadConstraintValidatorTest.php`, `tests/Feature/POS/ZReportRefundVatDisclosureTest.php`; shared expected-cash and new reconciliation tests |
| W8 | Two-branch business journey and recovery on actual candidate | `docs/qa/ONBOARDING-CAMPAIGN.md`, `scripts/campaign-onboarding.sh`, existing real-device smoke and restore runbooks |

SQLite-only passing results cannot establish PostgreSQL row-lock/precision/constraint guarantees. Required skipped or blocked legs are incomplete evidence. During future implementation use focused tests and repository preflight/CI before Fable promotion, including React diagnostics for actual React changes. **No tests, code, migrations, deployment or runtime configuration changes are performed by this spec fix round.**

## 7. Handoff and stop condition

Inputs were read in the handover's order: [audit](../../sessions/2026-09-05-parapharmacy-readiness-audit.md), v1 of this spec, [Fable r1 CHANGES-REQUIRED](../reviews/2026-09-05-parapharmacy-remediation-fable51-review.md), [root-cause archaeology](../reviews/2026-09-05-parapharmacy-remediation-root-cause-archaeology.md). Authority is the [owner's fix-round handover](../../handoff/HANDOVER-parapharmacy-remediation-codex-spec-fix-round-2026-09-05.md).

Codex's deliverables end with this v2, the two glossary entries and [fix-round notes](../reviews/2026-09-05-parapharmacy-remediation-codex-fix-round-1-notes.md). **Stop here. Fable runs gate r2, records the verdict, obtains owner decisions and writes subsequent briefs. Codex does not invoke reviewers, implement, test, commit, merge, rebase or push in this round.** Neither r1 correction nor r2 acceptance grants launch approval.
