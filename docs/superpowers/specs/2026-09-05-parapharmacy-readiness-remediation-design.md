# Multi-branch parapharmacy remediation design

**Version:** v3 — Codex spec fix round 2, 2026-09-05. **Status:** proposed remediation; awaiting Fable gate r3 and the open owner decisions. **Implementation is not authorized.**
**Source baseline:** gate-r2 evidence at `f75aa5023` (the reviewed product source matches the prior `b9a5565aa` baseline); citations describe that snapshot, not a verified deployment. Later execution must re-census its dispatched revision. **Target:** Tunisia. Legal-company structure and first-client sales surfaces remain unconfirmed.
**Orchestration:** Claude Fable 5.1 is the orchestrator of record, sequences lanes, runs reviewer gates, and alone commits, merges into local `dev` or promotes to `origin/dev`. Codex leaves these documents in the working tree and stops; no commit, merge, rebase, push, implementation or reviewer invocation belongs to this round.

**Change log, v2 → v3:** corrected r1's false voucher/account bypass premise; added OPEN D8/D9 and one D7/D8 receipt-version roadmap; added non-money-destination tender classification and binding repair; made terminal configuration a prerequisite to repository scoping; added L9 DEFAULT identification/split before L4; made R2 an explicit worker-entitlement/cache/census fix; added L1 web permissions and seeded-role deltas. Closed the r2 majors with recovery-service consolidation, NearExpirySlot reuse, all-writer GL concurrency, company-scoped repository validation, three-producer provenance, complete float retirement, flat lot reattribution/lock semantics, exact effective device availability, and corrected permission/location ratchets. Glossary and round-2 notes contain the new concepts and all twelve OPEN decisions. Gate-r2's Preserve paragraph is copied verbatim in §3.3; prior reviews remain immutable.

**Change log, v1 → v2:** consolidated every handover lot gap into W-LOT (W5/W6 are internal references, not separate lanes); applied R2 module gating and R3 read-only POS lot display in iteration 1; retained architecture option 1 under R4; added W0's seven archaeology guards; corrected F-A..F-F (one tender binding, open D7, GL writer census, fiscal-derived session reconciliation, narrowed permissions and precise projection recovery gap); extended benchmarks B9/B10; consolidated ten open owner decisions; registered lot evidence and session reconciliation in the glossary. The original r1 review remains an immutable review of v1.

**Summary.** Preserve offline fiscal POS and online B2B ownership. Repair access and tender controls, atomic B2B accounting, durable POS obligations and honest reconciliation. W-LOT completes the lot model across warehouse, counting, metadata, projection and register surfaces; its captured-lot iteration remains gated by D3/D7. This is a program specification and dependency outline, not an executable task plan.

## 1. Industry baseline (benchmark-first — convention 10)

Reference systems: Odoo 19.0 documentation, with an explicitly versioned Odoo 18 POS display reference for B9/B10; ERPNext current unversioned documentation (version not asserted); Dolibarr current unversioned official wiki/search extracts. Accessed 2026-09-05. These are workflow references, not evidence of NF525/NACEF compliance or a claim that all three products implement our exact offline guarantees. **NV** means the specific guarantee was not verified in that system, rather than presumed present.

| ID | User guarantee | Odoo | ERPNext | Dolibarr | AutoERP today (repository-relative path:line) | Decision |
|---|---|---|---|---|---|---|
| B1 | A stock movement for a tracked product records its lot | Lot-grain inventory [S1] | Optional batch dimension in stock ledger [S2] | Tracked movements require lot/serial [S3] | `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052` chooses FEFO after sale | DIVERGE in R3 iteration 1: estimated FEFO only; captured capability is W-LOT/W6 iteration 2 behind D3/D7 |
| B2 | A physical count can distinguish two lots of one SKU | Separate lot lines [S1] | Batch reconciliation supported [S4] | Lot-specific count behavior NV | `apps/api/app/Modules/Inventory/Domain/InventoryCountingItem.php:79` lacks lot dimension | MATCH W-LOT/W5 |
| B3 | Payment-terminal takings use a bank/settlement destination | Bank journal required for terminal [S5] | Exact terminal restriction NV | Exact terminal restriction NV | `apps/api/app/Modules/Treasury/Application/Services/TenderRepositoryResolver.php:157` allows mapped drawer | MATCH W2 |
| B4 | Stock mutation requires an action permission | Exact recall permission NV | Exact recall permission NV | Inventory requires stock-movement permission [S6] | `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:29` lacks recall permission | MATCH W-LOT |
| B5 | Accounting remains linked to its business source | Exact callback durability NV | Source voucher on ledger [S7] | Exact callback durability NV | `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:723` uses after-commit callback | MATCH source completeness, W3/W4; implementation mechanism is our design |
| B6 | Cancelling posted work preserves original evidence | Exact mode NV | Reversals preserve history [S8] | Exact mode NV | `apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:143` records reversal linkage | ALREADY preserve reversal design; extend same discipline to repairs |
| B7 | Changing lot tracking does not silently reinterpret stock history | Explicit adjustment procedure [S9] | Exact edit-after-use rule NV | Exact edit-after-use rule NV | `apps/api/app/Modules/BatchExpiry/Presentation/Requests/UpdateBatchRequest.php:22` permits identity/expiry edits | MATCH controlled correction and identification/split, W-LOT L2/L9/W5 |
| B8 | Staff branch scope survives alternate endpoints; replays do not double effects | Exact API scope/replay NV | Exact API scope/replay NV | Exact API scope/replay NV | `apps/api/app/Modules/Treasury/Presentation/Controllers/CashPositionController.php:75` scopes locations; repository list `PaymentRepositoryController.php:39` does not | MATCH project guarantees, W1/W2/W4; no unsupported competitor-equivalence claim |
| B9 | Cashier can see lot identity alongside a tracked cart item | Odoo 18 shows imported tracking numbers below products and permits editing [S10]; 19 exact placement NV | Inline batch editor supports POS Invoice [S11]; register cart placement NV | TakePOS exact lot placement NV [S12] | `apps/pos/src/types/product.ts:50` has no lot data; cart renders `CartLineItem` at `apps/pos/src/components/organisms/TransactionCart/TransactionCart.tsx:282` | MATCH visibility in W-LOT/W6 iteration 1; deliberately DIVERGE from editable capture: read-only suggestion per R3 |
| B10 | Cashier sees the next FEFO lot and expiry before adding an item, with the same guidance on its cart line | Read-only FEFO lot/expiry on product tile NV [S10] | Read-only FEFO tile/expiry NV [S11] | Read-only FEFO tile/cart expiry NV [S12] | `apps/pos/src/types/product.ts:50` lacks branch lot eligibility; tile uses `ProductCard` at `apps/pos/src/components/organisms/ProductGrid/ProductGrid.tsx:760` | ADD owner-required R3 guidance, W-LOT/W6; module-gated, cached, no selection and no sealed lot fields; no competitor-equivalence claim |

Sources: [S1 Odoo inventory adjustments](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/warehouses_storage/inventory_management/count_products.html); [S2 ERPNext stock specification](https://github.com/frappe/erpnext/blob/develop/erpnext/stock/spec/README.md); [S3 Dolibarr lot/serial module](https://wiki.dolibarr.org/index.php/Module_Lot_/_Serial); [S4 ERPNext stock reconciliation](https://docs.frappe.io/erpnext/stock-reconciliation); [S5 Odoo POS payment methods](https://www.odoo.com/documentation/19.0/applications/sales/point_of_sale/payment_methods.html); [S6 Dolibarr inventories](https://wiki.dolibarr.org/index.php/Inventories); [S7 ERPNext general ledger](https://docs.frappe.io/erpnext/general-ledger); [S8 ERPNext immutable ledger](https://docs.frappe.io/erpnext/immutable-ledger-in-erpnext); [S9 Odoo reassignment of lot/serial numbers](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/product_management/product_tracking/reassign.html).

Additional POS sources, checked 2026-09-05: [S10 Odoo 18 POS serial numbers and lots](https://www.odoo.com/documentation/18.0/applications/sales/point_of_sale/shop/serial_numbers.html); [S11 ERPNext inline serial/batch editor](https://docs.frappe.io/erpnext/use-inline-serial-batch-editor); [S12 Dolibarr TakePOS](https://wiki.dolibarr.org/index.php/Module_Point_of_sale_(TakePOS)). The Odoo 19 POS page could not be fetched reliably; the 18 display behavior is not asserted for 19. ERPNext's POS Invoice editor is not proof of register tile behavior. These sources do not establish offline FEFO cache freshness or captured physical evidence.

Second-of-everything: each affected catalogue lane must exercise second-company creation through the real provisioning path, a second POS-enabled location, and a repeated mutation with unchanged quantities/balances. Two lots and two terminals are additional mandatory fixtures here. Cross-tenant negative cases are separate from second-company cases.

## 2. Scope, alternatives and decisions

### 2.1 Confirmed intent and owner rulings

POS remains offline-first fiscal authoring; web sales documents remain online B2B, with future offline B2B excluded. Account-purpose configuration remains the way to map business meaning to account numbers. This round revises documents only.

- **R1:** one A-to-Z W-LOT package owns the lot parts formerly split across W1/W5/W6, including all handover residuals. Internal W5/W6 labels survive solely for review traceability.
- **R2:** fix worker/projection entitlement enforcement as well as device/web gates. A product flag is insufficient. Parapharmacy currently receives BatchExpiry through its vertical default; extras are additive-only. D9 leaves deactivation versus vertical-always-on OPEN; W-LOT defines the effective-company check and cache fix.
- **R3:** iteration 1 displays the FEFO suggestion (batch number/expiry or available cached identifier) on the tile and cart line, read-only. No selection or sealed payload change in iteration 1. D8 cannot silently narrow this ruling. Captured checkout is iteration 2 behind D3/D7.
- **R4:** retain architecture option 1, targeted corrections.

### 2.2 Architecture alternatives

1. **Owner-selected: targeted corrections to existing boundaries.** Preserve the modular monolith, separate POS/B2B authoring, shared inventory/treasury contracts, transactional B2B posting and recoverable POS projections.
2. **Containment-only pilot:** possible only under explicit owner-approved scope and operating restrictions; it does not resolve missing financial effects or turn estimates into physical traceability.
3. **Unified event-sourced rewrite:** excluded. No new general posting DSL, microservices, replacement chart engine or unified POS/B2B lifecycle.

### 2.3 Single open owner-decisions register

All twelve rows remain **OPEN**. Recommended defaults are proposals, not decisions. R1–R4 above do not resolve these rows. Review decisions 2–4 retain identifiers RD2–RD4; review decision 1 is D7 and review decision 5 is D1–D6.

| ID | Status | Decision and recommended default | Alternative and consequence / gate |
|---|---|---|---|
| D1 | OPEN | Confirm branches are locations of one legal company | Separate legal entities require intercompany design; never model cross-company custody as an internal branch transfer |
| D2 | OPEN | Confirm POS/B2B usage and exact enabled tenders; release only the selected surfaces/methods | Either/both surfaces allowed after their acceptance; unused paths remain excluded, not marked tested |
| D3 | OPEN | Keep R3 iteration 1 as suggestion-only shelf guidance; recommend captured-lot capability as iteration 2 when physical traceability is required | Staying on shelf control requires a written expiry/recall procedure, estimate/unknown labels, no software-captured traceability claim and a dated revisit; if capture is a launch requirement, defer launch of that use case until iteration 2 |
| D4 | OPEN | Recommend refreshing branch eligibility before opening a session; permit cached guidance during that session with visible snapshot age/staleness; require refresh before next session | Owner may choose a stricter age cutoff or longer offline tolerance, each with explicit unavailable/stale behavior; applies to iteration 1 cache and later capture. No silent hard cutoff or invented expiry; no guarantee of knowing recalls issued while offline |
| D5 | OPEN | Branch managers operate allowed custody; central treasury has explicit all-location authority | Company-wide manager custody is possible only as an explicit policy; approve source/destination reach before W1 execution |
| D6 | OPEN | Exclude cross-branch returns from initial release | Enabling requires receiving-branch stock/lot disposition, original provenance, payout custody and two-branch acceptance |
| D7 | OPEN | Iteration 2 only: recommend optional line-level lot fields in a new `SALE_RECEIPT` vN+1, required by captured-mode capability | Inline evidence uses the receipt's SQLite transaction/outbox and ordering; sealed errors need append-only corrections. A separate versioned evidence stream preserves fiscal shape but adds linkage/hash checks, local atomicity, reordered arrivals and conflicts. It still needs append-only corrections. Neither transport is selected here; no iteration 1 lot version/outbox work depends on D7 |
| D8 | OPEN | Tender-binding transport: recommend a required validated sealed repository/binding reference for money-destination legs in the single future SALE_RECEIPT version, subject to the owner confirming timing under R3 | Alternative: no sealed change, with an unsealed authored policy-revision snapshot plus durable server binding. This avoids sealing Treasury policy but needs authenticated linkage, immutable server revisions and conflict detection; an unsealed field alone is not fiscal evidence. Sealed bindings need append-only repair if unresolvable. Both options use one D7/D8 roadmap; no iteration-1 payload change is authorized while this remains open |
| D9 | OPEN | Recommend retaining BatchExpiry as always-on for parapharmacy, matching current vertical defaults | Alternative: make it deactivatable with an explicit effective-module exclusion model, server/device invalidation and historical-lot cohort policy; additive extras alone cannot disable it. Worker entitlement fixes and retail module-off acceptance are required either way; no deactivation rollout before this ruling |
| RD2 (review 2) | OPEN | Recommend restricting company-wide `batches.recall` to designated central inventory authority | Keeping seeded manager recall makes it intentionally company-wide despite branch membership; either option still needs action checks. Blocks final W-LOT role defaults |
| RD3 (review 3) | OPEN | Recommend reversing H-3's electronic-settlement → drawer fallback: electronic use unavailable until valid settlement configuration; does not govern voucher/loyalty/account destinations | Retaining fallback leaves that electronic day-one behavior in place as an explicit limitation; this client’s electronic tender launch remains excluded pending safe configuration. Do not change the pin/seed behavior without this ruling |
| RD4 (review 4) | OPEN | Recommend delivering fiscal-derived W7 first and deferring repository-balance comparison until float and drops are booked | Funding Treasury float/drawer-operation coverage first enables the separate repository check after source/idempotency/opening-baseline acceptance. Neither option permits comparing ledgers with different coverage or enabling the disabled variance writer prematurely |

NF525/NACEF names describe intended product use. This spec makes no certification claim and does not invent Tunisia tax, retention or receipt-format requirements. Those require review of the actual deployment/configuration under the existing owner gate.

## 3. Architecture and invariants

### 3.1 Ownership

| Owner | Authority | Must not do |
|---|---|---|
| Fiscal POS | Local immutable receipt/session authoring and durable synchronization | Depend on server GL availability to complete offline checkout; rewrite sealed bytes |
| Web B2B | Document transitions, deliveries, invoices, credit notes and terms | Duplicate a POS sale as another revenue-producing invoice automatically |
| Inventory | Movement, custody, lot allocation, count discrepancy and valuation inputs | Claim a computed FEFO allocation was physically scanned; derive missing lot identity from aggregate counts |
| Treasury | Payments, allocations, custody, instruments and settlement | Route electronic money to physical cash merely to avoid an error |
| Accounting | Journal interpretation of operational facts, account-purpose resolution and reversals | Make old posted entries change when configuration changes |

Existing `apps/api/app/Modules/Accounting/Application/Services/PartnerBalanceService.php:35` uses posted journal lines for balances (audit architecture context), and `apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:50` coordinates with GL inside a transaction. Preserve that behavior in this program. “Derived accounting” does not mean an optional or disposable ledger in the current system.

**Vocabulary:** Company, Location, Lot (batch), Repository, Payment, Document and Shift use [the canonical glossary](../../glossary.md). Lot evidence, Lot identification, Tender classification, Tender binding, Tender binding resolution and Session reconciliation are registered there; their proposed storage/write paths are explicitly distinguished from shipped behavior.

### 3.2 Global requirements

- Existing sealed fiscal bytes, classes, version schemas and posted journal amounts are immutable. New protocol capabilities use explicit new versions or separate evidence records; old readers/verifiers remain supported.
- Every operational effect has a stable source identity. Re-delivery yields the prior semantic result; conflicting content under the same identity is a visible conflict, never silently overwritten.
- Acknowledging a POS fiscal event means its event and mandatory projection obligations are durable, not that all projections have completed. UI distinguishes received, pending, blocked and applied.
- Tenant DB, company, terminal and location provenance must be explicit in workers. Never use HTTP-only scope resolution inside projection workers.
- Strings/BCMath for monetary values and quantities. Currency resolution takes the entity currency; TND uses its configured scale, quantities use the existing quantity precision contract. Preserve TTC POS versus HT B2B semantics.
- Use existing movement ports, fiscal projections, permissions and operator surfaces. Extend these rather than create a second source of inventory, money or outstanding balances.
- Restrict only new commands when policy changes; preserve already sealed offline facts. Revoked staff/configuration on synchronization is an exception requiring attribution and review, not permission to delete history.
- Accounting failure, incomplete synchronization and unknown lot allocation must never render as a clean zero balance or a reconciled close.

### 3.3 Gate r2 Preserve list — verbatim

Redelivery idempotency via `insertReceiptOnConflictDoNothing` (:461-466); `fiscal-projections` queue covered at `config/horizon.php:209`; cheques, change netting, cash rounding, per-leg idempotency (`TreasuryReceiptBridge:1297-1310`), frozen replay (`TreasuryMovementService:91-96`); W4's precise seeding statement; W7 RD4 gating in three places; W1 narrowing + adjustments route + RD2 open; DEFAULT-pin demotion; refusal to promote a suggestion into `operator_captured`; `product_batches` uniques already carry `company_id`; BatchExpiry route group is rule-12 compliant.

## 4. Work packages and fixing measures

Paths are relative to the repository. Named new components below are proposed additions, not existing code. File maps identify seams for the later task-level plan; they are not authorization to edit.

### W0 — Guards against the five recurring mechanisms

Source: [root-cause archaeology](../reviews/2026-09-05-parapharmacy-remediation-root-cause-archaeology.md). These are requirements for later implementation and reviewer gates; no guard/test is implemented in this round. A guard closes its mechanism only when its own liveness check fails on a deliberately introduced violation, not merely because its file exists.

| Guard | Mechanism addressed | Required deliverable and acceptance |
|---|---|---|
| G1 Route action-permission ratchet | M2: absent action enforcement | Enumerate Laravel’s booted route collection (`Route::getRoutes()`), including provider and non-Presentation routes. Recognize direct/inherited `can:`, `require.any.permission:` and verified FormRequest permission authorization; no `permission:` alias exists (`apps/api/bootstrap/app.php:113–121`). Controller-inline, service-authority and deliberately public routes require an explicit per-route classification/reason/evidence list, never an automatic equivalent waiver. Use shrink-only debt and a new unchecked DELETE liveness probe; run in the default lane, not only PostgreSQL |
| G2 Location-scope coverage census | M2 opt-in omissions and M3 partial coverage | Census route/method, not controller: scoped / bypass-declared / unscoped, with authority and service. Accept resolver enforcement in controllers/FormRequests and verified `ValidLocationAccess` rules (`apps/api/app/Rules/ValidLocationAccess.php:69–79`); an unused middleware alias earns no coverage. Use the corrected 15-file downstream baseline below plus the Company scope-owner consumer. W1 includes terminal sync consumers and mutation custody; L1 explicitly contrasts batch expiring versus expired |
| G3 Full resolver matrix | M3 fixing one direction only; M5 H-3 pin (F4) | W2 must exercise cash/electronic × drawer/settlement (all four cells), unmapped per class, maturity and non-money-destination classes separately, second branch/company and replay. Distinguish explicit CARD→drawer mapping from fallback with no settlement repository. Gate electronic H-3 reversal on RD3; do not turn the old fallback into an acceptance requirement |
| G4 Lot-model completeness and vocabulary | M1 lot model stopped before POS/counts (F1/F7/F8/F6) | Register lot evidence with `operator_captured`, `system_fefo_estimate`, `unknown`; inventory every producing/reading surface and preserve provenance through DTO/UI/export/return paths. W-LOT must cover every ordered gap and reject any surface presenting estimated FEFO as captured |
| G5 Re-ratify seal/GL atomicity with PostgreSQL | M4 early seal-wins decision outlived its audit-listener purpose (F5.1) | W3 supplies the writer/lock census and a real PostgreSQL concurrent posting leg demonstrating no sealed invoice without its GL and no chain collision. SQLite-only acceptance is insufficient |
| G6 Standing projection recovery scenarios | M3 retrying failures while overlooking lost work (F5.2) | Every later projection lane's acceptance contains: registry throws at ingest; enqueue lost after commit (including pending/attempts=0); worker dies in running, including after effect commit. State exactly-once semantic result or visible blocked outcome; a justified not-applicable row needs reviewer evidence, not omission |
| G7 Tests assert data meaning | M5 tests froze limitations as correctness (F4/F6/F7) | Any intentionally retained limitation test gets `_pins_limitation_` or an equivalently grep-able marker and a linked decision/ticket/exit criterion. Register the marker in `docs/conventions/` in the first authorized implementation lane; mark H-3 regardless of RD3 outcome (no test edit in this spec round). Census DEFAULT top-up and Z copied-total pins; replace resolved pins with economic/inventory meaning assertions. Do not delete useful regression coverage or rename it to imply the defect is fixed |

**G1 measured starting evidence:** authz r2 M-1 records 559 mutating route statements, 205 without inline `can:`/`require.any.permission`, and approximately 77 with no visible check. These are static-review measurements, not 77 proven vulnerabilities or the final booted-router baseline. Classify public auth routes, inherited gates and unconditional versus conditional controller checks before fixing the shrink-only ceiling. Pattern the written exceptions/liveness on `apps/api/tests/Architecture/TenantOnlyUniqueOnCatalogueTablesRatchetTest.php:55` and `TenantOnlyUniqueRatchetLivenessTest.php:30–45`; do not inherit the latter’s PostgreSQL-only skip. Record every reviewed equivalent’s controlling branch, not just the presence of `can()` text.

**G2 corrected starting inventory:** the r2 baseline includes **15 downstream consumer files**, not just CashPositionController: Accounting `GetCashMovementsRequest` and `ReportsController`; Expense `ExpenseController`, `ExpenseAnalyticsController`, `ExpenseExportController`; BatchExpiry `BatchController`; Replenishment `ReplenishmentRequestController`; POS `AnalyticsController`, `ReportController`; Inventory `StockMovementController`, `StockLevelController`, `StockMatrixController`; Treasury `CashPositionController`, `MaturingInstrumentsController`, `PaymentInstrumentController`. Include the scope-owning Company `LocationController` separately, so this enumeration contains 16 consumers when that owner is counted. The verified file inventory is in [authz r2 M-6](../reviews/2026-09-05-parapharmacy-remediation-gate-r2-authz.md); partial method coverage is concrete at `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:215–236` (expiring unscoped) versus `:252` (expired scoped). Baseline the routes reached through these files; injection alone is not scope enforcement.

**Dependencies:** Fable sequences guard rollout and baselines alongside affected packages. G1/G2 are repository-wide coverage censuses, not permission to remediate unrelated modules in this program. Future guards must preserve explicit fiscal ingress authority and service-level equivalents rather than force HTTP user permissions into workers.

### W1 — Treasury location scope and custody

**Findings:** audit F3; B8; review F-E. Lot action enforcement is owned only by W-LOT, narrowed to recall/destroy/read routes; existing authorized batch create/update/transfer/write-off remain regression coverage.

**Baseline:** `apps/api/app/Modules/Treasury/Presentation/Controllers/CashPositionController.php:75` filters locations; repository list/detail are company-scoped at `PaymentRepositoryController.php:39,58`. `RepositoryTransferService.php:43,118` resolves custody without staff location authorization (full service prefix: `apps/api/app/Modules/Treasury/Application/Services/`). The previously omitted adjustment route is `apps/api/app/Modules/Treasury/Presentation/routes.php:98`; its adapter builds a company/user intent at `RepositoryAdjustmentController.php:58` under the same controller prefix. The adapter alone does not prove downstream location enforcement: require the request/service census before claiming that path is safe.

For human repository list/detail/balance/transactions/movements and manual-adjustment HTTP authorization, apply `LocationScopeResolver` and `LocationScopeBoundary` only after the terminal-consumer transition below is ready. A restricted user sees repositories in allowed locations. Locationless central repositories are not exposed merely because location is null. Propose `treasury.manage_all_locations` as the endpoint bypass, with `admin` default and any additional designated grants subject to D5, through the existing role surface. Preserve explicit unrestricted membership (`LocationScopeResolver.php:57–60`, under `apps/api/app/Modules/Company/Services/`); do not reinterpret it as a restricted location list. Generic `repositories.view`, `treasury.transfer` and manager role do not bypass scope.

Transfer authorization uses persisted repository identities, not caller-supplied location labels. Require permission over source custody. A branch manager may transfer to an allowed branch repository or to an explicitly configured company central collection destination; reverse outflow from that central destination still requires central authority. Model destination eligibility as a repository setting on the existing repository surface, not a UI-only filter. Destination selection is a mode of the existing repository index; no second destination catalogue. For another branch destination, require that location in the user's allowed set or the explicit bypass. Company-global bank identifiers/balances must not leak through destination pickers; expose only permitted destination names/IDs.

No side effects on denial. API detail denial uses the established scoped-not-found convention; rejected explicit out-of-scope list selections use the existing forbidden envelope. Authorize user commands at the HTTP adapter/FormRequest boundary and pass an already-authorized, scoped intent. `LocationScopeResolver.php:12–16` is HTTP-only; it must never be called inside a shared worker service. The orchestration validates the intent’s explicit tenant/company/source repositories and checks their persisted location/version against the authorized boundary inside the transaction, refusing stale custody; it does not re-resolve a user from CompanyContext. Queued/console callers use an explicit system-authority intent and persisted source provenance. `apps/api/app/Modules/Treasury/Application/Listeners/PostShiftCashVarianceAdjustment.php:199,267` is a queued adjustment consumer; no HTTP principal is manufactured for it and its RD4-disabled state remains unchanged.

**Device-consumer gate, before scoping `/payment-repositories`:** census `apps/pos/src/lib/sync/syncService.ts:1256`, `apps/pos/src/api/paymentApi.ts:8–9`, SQLite hydration at `apps/pos/src/stores/paymentStore.ts:950–962`, cash/card selectors at `:1000–1007,1185–1192,1556–1563`, refunds at `apps/pos/src/stores/refundCheckoutStore.ts:1733,1743`, and alternate consumers. Today a narrowed human list can remove all locationless settlement rows and abort card checkout; a supervisor-populated device cache is not a cashier’s authorization.

**Device projection design:** distribute W2 tender bindings through a terminal-scoped tender-config projection of the same repository/method catalogue, integrated into terminal policy sync; the human repository index is not its authority. Payload contains only allowed method/binding revision, destination ID/type, required display label/currency and terminal/company/location scope, never balances, bank account numbers or transaction drilldowns. Terminal identity/config entitlement supplies its allowed destinations independently of the current cashier’s balance-view scope; checkout still validates the operator’s selling/refund authority. This is a transport projection, not a new editable repository catalogue. Remove device use of the human index and first-active picks before enabling human scoping. Bind/invalidate local configuration on tenant/company/terminal/location changes and operator authority refresh; a supervisor’s cache cannot widen a cashier’s commands. A cold device must obtain the projection while connected before offline selling; “cold-cache offline” acceptance means hydrated then restarted offline, not an impossible networkless first provisioning.

**Permission and catalogue rollout:** add `treasury.manage_all_locations` to the permission catalogue and grant `admin` via the real `Permission::all()` path (`apps/api/database/seeders/RolesAndPermissionsSeeder.php:547`). Seed/grant every existing staging/production tenant and invalidate permission caches **before** enabling scoping; D5 determines any extra role grants. Actual seeded roles are admin/manager/cashier/viewer/technician/operator/accountant, not owner. A tenant missing the permission/reseed fails the enablement readiness check rather than silently cutting off operators. Preserve customized grants through a reviewed per-tenant delta/reseed preview.

**Company-code validation fix:** `apps/api/app/Modules/Treasury/Presentation/Controllers/PaymentRepositoryController.php:80,170–172` validates code tenant-wide, but `apps/api/database/migrations/tenant/2025_12_30_195300_fix_multi_company_unique_constraints.php:31–35` already makes the database unique key `(company_id, code)`. Align create/update validation to company scope; do not add a redundant DB migration. Prove A and B can each create/update `CASH-01` through real APIs while same-company duplication fails and rerun preserves IDs/balances. Add the explicit company predicate to the transactions payment query at `PaymentRepositoryController.php:356–358` while retaining repository ownership validation.

**Seams:** under `apps/api/app/Modules/`: `Treasury/Presentation/Controllers/PaymentRepositoryController.php`, `RepositoryMovementController.php`, `RepositoryTransferController.php`, **`RepositoryAdjustmentController.php`**; adjustment request and `RepositoryAdjustmentService` public orchestration; `Treasury/Presentation/routes.php:98–100`; `Treasury/Application/Services/RepositoryTransferService.php`; `Company/Services/LocationScopeResolver.php`, `LocationScopeBoundary.php`; permission seeder and existing repository forms; the device sync/API/store/refund consumers and terminal projection above.

**Acceptance:** A-only manager cannot list/read B's drawer through any alternate endpoint, adjust it or transfer from it; allowed A→central transfer succeeds while central→A requires central authority; direct IDs and repository metadata changes cannot evade scope; company/tenant mismatch fails; denial leaves adjustment, movement and GL snapshots unchanged; explicit unrestricted membership and authorized bypass still work; permission-cache refresh updates access. D5 fixes the final policy. Also require a restricted cashier at the second branch to complete enabled cash/card/refund flows online from an empty SQLite cache and after hydration/restart offline; no human bank-balance access is needed. Supervisor→cashier, terminal/company changes and stale bindings cannot leak authority. Exercise an authorized system adjustment with no CompanyContext/user under an isolated enabled fixture, without enabling the production RD4 flag. A not-yet-reseeded tenant is blocked from activating the scoping feature; after seed/grant it passes. Company-code duplicate/rerun cases above are mandatory.

### W2 — One tender binding, with explicit economic meaning

**Findings:** F4; r2 B1/B2/B3 and Treasury MAJOR-1/2/5/6. D2/RD3 govern launch tenders, D8 governs binding transport, and W1’s device projection is a shared prerequisite.

**Correction of record:** r1 F-A and v2 incorrectly said vouchers/account legs bypass the resolver. `apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:480` loops **every `payments[]` leg** through canonical leg projection; voucher redemption being owned elsewhere at `:105–109` does not exempt its tender leg. `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:287–288` explicitly describes vouchers as payment legs. Remove the false bypass assumption; any new non-money branch/filter is W2 implementation work, not shipped behavior.

The device still picks the first active bank/virtual repository (`apps/pos/src/stores/paymentStore.ts:1185`) while canonical payment has no destination (`apps/api/app/Modules/Fiscal/Domain/DTOs/Canonical/PaymentDTO.php:27–34`). The mapped resolver branch permits electronic→drawer at `apps/api/app/Modules/Treasury/Application/Services/TenderRepositoryResolver.php:157–171`; the unmapped fallback only reaches a drawer when no settlement repository exists at `:198–207`. H-3 pins the latter at `apps/api/tests/Feature/Tenant/CleanRegistrationDownstreamAssumptionsTest.php:133–151`.

#### Classification and every-leg census

**Tender classification is one derived value object, not a fourth editable column.** Its canonical derivation uses `payment_methods.is_cash_tender`, `has_maturity`, `instrument_kind` plus the typed tender operation/redemption handler’s economic meaning. `is_cash_tender` remains the cash authority; preserve cheque/effet handling from `apps/api/app/Modules/Treasury/Application/Projections/Concerns/HandlesMaturityTenderLeg.php:33–48`. An explicit supported-method/operation mapping covers non-money legs; never classify “everything else” as settlement, use display names heuristically, or keep an independently writable classification flag in sync with the other three. Conflicting/unsupported semantics are a visible configuration exception, not a guessed bank destination. Register the derived noun and single writer in the glossary.

Four routing classes: `cash`, `maturity_instrument`, `electronic_settlement`, **`non_money_destination`**. The last class dispatches by typed operation to liability, receivable or instrument-collection accounting; it does not manufacture a payment-repository movement. It is not a promise that vouchers have no economic value. W2 must add the explicit routing branch before generic repository resolution and reuse the existing domain redemption/obligation writer to avoid duplicate GL/redemption. Non-money IDs/settlement references must not be shoehorned into a required `repository_id`.

The later plan must census the complete seeder **and** bridge/device/custom-method consumers. The following is the required seed baseline (`apps/api/database/seeders/PaymentMethodSeeder.php:88–208,217–370,377–445`), not proof that all paths currently post correctly:

| Seeded method / operation | Required class and destination rule |
|---|---|
| CASH (Tunisia, France, default) | `cash`: terminal/location drawer custody via the branch binding; retain change/rounding semantics |
| CHECK; TRAITE / LCR / BILL_EXCHANGE | `maturity_instrument`: cheque/effet portfolio and existing instrument lifecycle; never collapse paper into electronic settlement |
| TRANSFER, CARD, WALLET, PAYPAL | `electronic_settlement`: bank/clearing route under the selected RD3 policy; provider clearing is not cleared cash |
| DIRECT_DEBIT (`has_maturity`, `InstrumentKind::Other`) | Scheduled bank-collection semantics under `electronic_settlement`, preserving mandate/maturity timing; do not route it through cheque/effet handling merely because `has_maturity` is true. Census its live callers before enablement |
| LOYALTY (Tunisia, `:193–208`) | `non_money_destination`: extinguish the appropriate loyalty obligation through its domain writer; zero drawer/bank-clearing inflow |
| MEAL_VOUCHER (France, `:337–351`) | `non_money_destination`: record issuer/to-collect portfolio claim and later remittance/fees, not bank-clearing cash at redemption; maintain physical custody where applicable |
| Voucher/store-credit redemption and any customer-account leg on `payments[]` (not added by this seed list) | `non_money_destination`: existing liability/redemption or AR charge semantics, never guessed cash. Census adapters and payload builders for actual supported forms; no general voucher/account bypass exists in today’s loop |
| Separate ACCOUNT_CHARGE / ACCOUNT_PAYMENT events | Preserve event ownership: charge raises an obligation, collection moves real tender money. `apps/pos/src/stores/paymentStore.ts:899–929` authors a separate account-charge event; this does not prove no alternate on-account sale leg exists. ACCOUNT_PAYMENT collection uses its actual tender class, never the non-money class solely because its name includes account |

Record every discovered custom/legacy method and emitted `method_code`, its operation handler, destination/GL/redemption owner, change eligibility, refund path and migration disposition. An unsupported custom semantic cannot be enabled merely by having `is_active=true`; resolve it visibly before the chosen D2 scope is released. **RD3 applies only to electronic settlement**, not loyalty, voucher, cheque/effet or account obligations.

#### Binding and D8 transport gate

**Retire every first-active device pick**, including cash, card, mixed and refund paths found by W1/W2’s consumer census. Publish one scoped binding revision through W1’s terminal projection. Authoring/local persistence/first projection/replay reference the same binding; no independent device choice and no use of the human balance list as configuration authority.

**D8 is OPEN; W2 selects no transport here.** If the owner selects sealed transport, the new coordinated SALE_RECEIPT version requires `repository_id` and the binding revision **for money-destination legs**, with UUID, required-value and cached tenant/company/terminal/class/currency validation **on the device before sealing**, plus server validation. The ACCOUNT_PAYMENT precedent is incomplete: `apps/pos/src/lib/fiscal/FiscalEventEngine.ts:2844` accepts an optional string, while `apps/api/app/Modules/Treasury/Application/Projections/TreasuryAccountPaymentBridge.php:406–419` throws on null/unresolvable destination. Do not copy that mismatch or rewrite its frozen vectors; this package’s new validator must reject null/garbage/stale-unsupported bindings before authoring succeeds. Non-money legs require their appropriate typed evidence, not a fake repository UUID.

If D8 selects unsealed policy metadata, persist the authored binding revision and receipt/leg identity atomically with local authoring, bind it to authenticated terminal/company and event ID/hash at sync, and resolve against an immutable server policy-revision record. Current mutable mapping is not that historical record. Missing/conflicting metadata yields blocked work, not an arbitrary fallback. This metadata proves operational attribution under the terminal trust model, not sealed fiscal bytes. Both branches require generated DTOs, capability/version checks, duplicate/conflict semantics and the repair path below. **R3 iteration 1 keeps the sealed payload unchanged.** Neither W2 nor W6 can introduce a version bump before the owner rules on D8 and the single D7/D8 roadmap (§5); a changed timing under R3 requires an explicit owner ruling, never reinterpretation in an implementation brief.

#### Electronic routing policy — explicitly conditional on RD3

**Under RD3-reverse (proposal):** an electronic tender must have a mapped compatible bank/virtual clearing binding; explicitly mapped CARD→drawer is refused and an unmapped electronic method with only CASH-01/SAFE-01 is unavailable for new use. Readiness explains the missing configuration before checkout. A 100.000 TND card sale then moves clearing by 100.000 and cash by 0.000.

**Under RD3-retain:** preserve the day-one electronic drawer fallback as a marked limitation; do not implement the reverse policy implicitly. Exclude unsafe/unconfigured electronic methods from this client’s launch under D2. Neither outcome changes non-money-destination semantics. Cash stays physical custody; preserve cheques, change netting, cash rounding and maturity portfolio behavior. H-3 gets a `_pins_limitation_` marker in the first authorized W0 lane regardless of RD3 outcome, without changing its expected behavior before the ruling; no test is edited in this round.

#### Unresolvable binding and historical repair

Already-projected legs resolve the existing payment before remapping (`TreasuryReceiptBridge.php:1297–1310`), and offline frozen-destination replay already has an alert policy (`apps/api/app/Modules/Treasury/Application/Services/TreasuryMovementService.php:91–96`); preserve both. A frozen-but-valid repository is not automatically an unresolvable binding. For an absent, deactivated, foreign or otherwise unusable binding at first projection, keep the Treasury leg durably **blocked** with exact source/leg/revision/reason. **Blocked Treasury never blocks POS-core receipt/revenue/stock projection.** No sealed bytes or current mappings are silently rewritten.

Add an explicit **Tender binding resolution** action on the existing blocked-projection detail. Proposed append-only `tender_binding_resolutions` stores source event/leg/hash, original binding, authorized replacement, tenant/company, actor, reason/evidence, timestamp and predecessor revision; primary writer is the existing Treasury recovery orchestration through a narrow public contract. Require `treasury.manage` plus source-custody authority (or explicit D5 bypass); validate replacement company, economic class, currency and destination compatibility. Lock the source leg/resolution revision; identical resolution key/content is idempotent, conflicting content is refused. The bridge consumes this authored resolution on replay and posts exactly once. This is an operational correction record **for a never-projected leg**, not a compensating movement for money that never existed. If an effect is already posted, refuse rebind and require the established compensating financial correction. Preserve old event/binding/resolution history and fiscal attribution. Unknown entitlement or configuration remains blocked until explicitly resolved.

Legacy unprojected events without authored binding require a recorded company-scoped cutover decision or per-leg resolution; never reinterpret them from today’s defaults. Before any mutation of `is_cash_tender` or related netting semantics, drain and verify **no unsettled projections on any terminal in the affected company**, including offline pending outboxes, or postpone that mutation. `TreasuryReceiptBridge.php:961–967` warns mutable cash classification can otherwise conflict with already-applied netting. Immutable policy readers and the selected D8 cutover must exist before new authoring; old-version readers and resolutions survive rollback.

**Acceptance:** full class/method matrix above, two branches/companies and repeat delivery. Distinguish explicit CARD→drawer mapping with a bank present from unmapped CARD with no settlement repository; assert the selected RD3 branch, not an assumed reversal. Include MEAL_VOUCHER portfolio, LOYALTY liability and account-charge/collection separation with zero invented clearing; mixed tender and refund preserve each class. A new sealed validator refuses null/garbage binding before local seal; an offline-valid binding deactivated before sync yields blocked Treasury plus intact POS-core, then one permissioned rebind/payment/GL/movement on repeat recovery. Deny foreign/stale/concurrent rebind and never reroute an existing payment. Include classification-change deploy with unsettled work, cold-cache restricted cashier and missing custom-method semantics. A passing fallback-with-bank case alone proves no fix.

**Seams:** payment-method seeder/configuration and single derived classification; bridge’s every-leg loop and domain handlers; `TenderRepositoryResolver`; account payment bridge/validator; W1 terminal projection and all device authoring/refund/sync consumers; existing projection recovery detail plus binding-resolution record; D7/D8 canonical roadmap and legacy policy readers. No separate editable destination or classification catalogue.

### W3 — Atomic web B2B invoice accounting

**Finding:** F5 first case; B5/B6; review F-C. **Required if B2B invoices/credit notes are used.**

**Baseline:** after-commit dispatch at `apps/api/app/Modules/Document/Domain/Services/DocumentPostingService.php:723–726` leaves the invoice GL outside the source commit. `apps/api/app/Modules/Accounting/Application/Services/AccountingService.php:591–609` reads chain hash and unlocked max+1 (`apps/api/app/Modules/Accounting/Domain/JournalEntry.php:140–146`), while `apps/api/database/migrations/tenant/2026_07_08_100400_add_chain_sequence_unique_index_to_journal_entries.php:35–38` enforces company/sequence uniqueness. Concurrent listener posts can therefore collide after sealing; this is a source-derived failure mechanism, not a reproduced PostgreSQL incident.

Choose synchronous posting inside the document transaction for this online workflow. The legal document seal, its document-source GL, advance clearing and paid-state transition must either commit together or all roll back. This deliberately makes a missing purpose/closed period/invalid chart block invoice sealing instead of sealing without GL; return an actionable translated refusal naming the missing purpose or period, following the refusal pattern at `apps/api/app/Modules/Treasury/Presentation/Controllers/RepositoryAdjustmentController.php:71–79`. Do not merely add more preflight checks before the current after-commit callback.

Expose a narrow shared accounting contract for idempotent document posting, returning the existing journal identity on retry. `DocumentPostingService` calls it inside its existing transaction; the accounting implementation validates source identity, period and account purposes and posts balanced legs using one company-chain locking convention shared with other GL writers. **No lock order is prescribed by this spec.** Before the execution plan, census every GL writer and nested path, including invoice/credit-note posting, advance clearing, stock/treasury writers and any path acquiring Document locks after GL keys. Record transaction owners, lock acquisition sequences and lock hold time inside the document transaction, including expected POS projection contention. Today the document chain lock at `DocumentPostingService.php:673–678` precedes advance GL at `:239`, which takes tenant numbering then company chain keys at `apps/api/app/Modules/Accounting/Domain/Services/GeneralLedgerService.php:5722–5731`. A GL-first rule could invert a live path. Choose and document one compatible order after the census; unresolved inversion blocks the plan. Replace unlocked allocation through the shared serialization contract, not a second allocator.

Require a read-only duplicate census before a proposed partial unique index for `source_type = Document`, following the per-source pattern (`apps/api/database/migrations/tenant/2026_06_26_120000_unique_journal_entries_source_procurement.php:46–47`). The base journal migration at `apps/api/database/migrations/tenant/2025_11_30_100000_create_journal_entries_table.php:44` supplies a nonunique source index; review r1's migration census found no Document-specific unique index. Recheck live schema and reversal/advance source discriminators before choosing the predicate. Do not impose global source uniqueness on unrelated journal types or delete/reseal historical duplicates. A conflicting existing document journal is blocked, not reused blindly.

Retire `InvoicePostedListener` as a second money writer for the new path. Preserve the existing `InvoicePosted` event class and other registered consumers. Persist any mandatory audit-event delivery obligation transactionally before deferring notification work; nonfinancial after-commit consumers may remain asynchronous. The later plan must account for all current event consumers, rather than remove the event or dispatch duplicate source events.

Prior sealed invoices missing GL need a read-only census. Recovery is explicit per source, idempotent, and blocked on ambiguity or closed/filed period rules. Do not silently book old invoices into today's period, modify their seal, or bulk replay already-booked events. Manual recovery requires the source amount/account snapshot and an accountant-compatible period decision where necessary.

**Seams:** `Document/Domain/Services/DocumentPostingService.php`; `Accounting/Application/Services/AccountingService.php`; `Accounting/Listeners/InvoicePostedListener.php`; `app/Providers/EventServiceProvider.php`; shared accounting contracts; journal source uniqueness/chain locks. No POS checkout code belongs in this package.

**Acceptance:** inject failure during account resolution, journal insert, advance clearing and final document transition: no partial seal/journal survives; retry after a lost HTTP response yields one seal and one journal; real PostgreSQL concurrent invoices (including advance-clearing paths) in one company cannot collide/fork the journal chain, deadlock through an inverted order, or leave a seal without GL; also run invoice-versus-POS `TreasuryReceiptBridge`, invoice-versus-`RepositoryAdjustmentService`, and invoice-versus-procurement/supplier-invoice writers on PostgreSQL in one company, with bounded progress and recorded lock wait/hold times; second-company chains remain independent; fully prepaid and partial-advance invoices, credit notes and closed periods retain correct semantics.

### W4 — Durable POS projection obligations and recovery

**Finding:** F5 second case; B5/B8; review F-F. **Required for POS use.**

Keep local sealing and server projection asynchronous. **The existing seeding is already transactional:** `apps/api/app/Modules/Fiscal/Application/Services/OutboxIngestor.php:1025–1046` persists projection rows in the event transaction and dispatches after commit. **The seeding hole is specifically registry fail-closed exclusion:** `apps/api/app/Modules/Fiscal/Application/Services/FiscalEventProjectionRegistry.php:248–267` catches module-resolution failure and omits the projector. Treasury is module-gated (`apps/api/app/Modules/Treasury/Application/Projections/TreasuryReceiptBridge.php:214`), while POS-core is not (`FiscalEventProjectionRegistry.php:240–247`). Do not rebuild the transaction or invent missing POS-core seeding.

Preserve durable obligations for expected projectors using a recorded activation decision; when activation cannot be resolved, retain blocked work/configuration uncertainty rather than treating failure as disabled. A verified disabled module still authorizes no module effects. POS-core is mandatory for eligible events, Treasury for the approved Treasury-enabled deployment. W-LOT's lot legs require `BatchExpiry` separately; do not make them universally mandatory to fix a Treasury registry gap. Unknown entitlement means no lot writes and visible blocked resolution, not a guessed enablement. Record historical/cutover decisions so replay cannot silently reclassify disabled and failed-resolution cohorts.

The scheduled retry currently selects dead-lettered or pending rows with at least five attempts (`apps/api/app/Modules/Fiscal/Infrastructure/Commands/RetryFiscalProjectionsCommand.php:299–307`; scheduled at `apps/api/routes/console.php:57`). Add scheduled, tenant-aware recovery for **pending rows with lost enqueue, including attempts=0**, and **orphaned running rows**. Reuse dispatcher/job dependency, overlap/advisory locking and dead-letter rules. Claim work atomically; running-age expiry must exceed the real lease/job timeout and prove abandonment so an active worker is not stolen. Persist a recovery reason and use bounded batches/backoff. Worker death after effect commit but before status update must converge through existing effect idempotency. Redis order alone is not a dependency contract.

**One recovery implementation, two explicit authorities:** `apps/api/app/Modules/Fiscal/Infrastructure/Commands/EnqueueResolvedEventProjectionsCommand.php:245,358,439–440` already creates missing rows and republishes pending rows at any attempt count. It currently selects parsed events using today’s active projectors; it is manual, requires `--actor-id`/`fiscal.events.resolve_quarantine`, has no `--all-tenants` mode (`:53–58,96–100`) and excludes running work (`:44`). Lost pending work is unrecovered by the **scheduled** path, not by every available tool.

Extend the shared recovery orchestration behind this command/dispatcher so manual enqueue, scheduled retry and ingest re-drive use one obligation/cohort/claim implementation. Retain the command as a manual adapter with its permission/tenant restrictions; the scheduled adapter uses explicit system authority, bounded tenant enumeration and no fabricated actor. Do not simply schedule the manual command or add a parallel row-seeding service. Replace its current-activation reseeding with the event’s recorded policy or an explicit legacy cutover resolution before either adapter runs. Preserve parsed/verified/quarantine/training guards and require current effective entitlement for any newly applied module leg; entitlement loss/uncertainty retains blocked historical obligations rather than manufacturing or erasing them. Both adapters share missing-row idempotency and lease-aware orphan-running recovery; the scheduled adapter cannot release quarantine. Existing `fiscal-projections` queue/Horizon coverage remains in use. Acceptance compares manual and scheduled cohorts under an activation change, permission denial and system context, and verifies that neither invents historical effects.

Existing event re-ingestion must re-drive missing required obligations using the event's recorded/cutover policy. Add a completeness census comparing accepted source events to expected projector names and results; treasury self-balance alone is insufficient when both a payment and its journal are absent. Preserve parse/integrity quarantine suppression rules: recovery must not accidentally apply quarantined invalid events. Training receipts must remain contained.

Existing dead-letter UI is the primary resolution surface. Show fiscal accepted plus financial pending/blocked status, age and reason; emit actionable alerts after the configured retry ceiling. Enumerate exception cohorts for operator-led backfill, never call current module configuration historical truth.

**Seams:** `FiscalEventProjectionRegistry.php`, `OutboxIngestor.php`, `FiscalEventProjectionDispatcher.php`, `ApplyFiscalEventProjectionJob.php`, `EnqueueResolvedEventProjectionsCommand.php` manual adapter and shared system recovery/dispatcher; retry command; fiscal projection schema if a policy snapshot is required; `routes/console.php`, `config/horizon.php`; existing projection-status UI.

**Acceptance:** central config lookup failure at ingest; Redis unavailable after commit; worker killed after effect commit but before status update; duplicate delivery; treasury runs before receipt dependency; wrong tenant worker context; quarantined/training event; module change between ingest and replay. Every eligible case converges to one complete set of effects or a visible blocked state, never missing work reported as complete.

### W-LOT — Complete batch management A-to-Z (R1)

**Single package / single acceptance gate:** audit F1/F2/F7/F8 and the lot portion of F6; B1/B2/B4/B7/B9/B10; review F-B/F-E. All former W1 lot authorization and W5/W6 work lives here. W5 and W6 below are internal sub-sections for v1 traceability, never separately competing lot lanes. Fable sequences this package; iteration 2 remains gated, so iteration 1 completion must never be reported as captured-lot completion.

**R2 is an implementation fix, not a claim that today’s worker is gated.** At the reviewed snapshot, parapharmacy includes BatchExpiry in `apps/api/config/verticals.php:344–355`; `apps/api/app/Services/CompanyConfigService.php:71` unions defaults and extras with no default-disable mechanism. `apps/api/app/Modules/Fiscal/Application/Services/DefaultModuleActivationResolver.php:50–56` ignores companyId and resolves tenant-level configuration. `PosCoreReceiptProjection::requiresModule()` returns null (`apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:221–224`); its lot branch checks only the product flag (`:2026`, `apps/api/app/Modules/BatchExpiry/Domain/Services/FEFOInventoryService.php:930–934`). That permits unentitled lot writes. The flag is accepted without BatchExpiry by `apps/api/app/Modules/Product/Presentation/Requests/CreateProductRequest.php:226` / `UpdateProductRequest.php:207`, and web gating uses BatchExpiry **or Inventory** at `apps/web/src/features/inventory/ProductForm.tsx:173`. Correct these reachable paths, not just empty module-off fixtures.

**Required worker/company boundary:** every sale/refund/count/identification/correction/transfer lot arm must explicitly resolve BatchExpiry for its persisted tenant **and company** before queries or writes; retain the always-active POS-core projector for ordinary fiscal/aggregate work. Extend the public module resolver so it validates company→tenant ownership and returns that company’s effective entitlement instead of discarding companyId. Effective company entitlement currently inherits the tenant’s vertical/default/extra set; this is a company-aware enforcement fix, **not** an invented independent per-company licence/toggle. Any independently configurable company entitlement would require a separate owner-approved model. HTTP/device/worker must consume the same effective decision/revision; no HTTP CompanyContext inside workers. D9 chooses whether parapharmacy ever gains a disable mechanism.

**Cache and revocation:** `CompanyConfigService.php:34,52` caches for 24 hours; `:89–103` supplies `invalidateForTenant()` and documents a forget/repopulate race. Every in-scope entitlement change must invalidate through that owner, including vertical fanout, and publish a new effective revision to devices. Require a monotonic revision check or equivalent race-safe invalidation so an in-flight stale read cannot re-cache a revoked grant for another 24 hours. Capture/check the effective revision at the transactional lot-write boundary; a concurrent change serializes with that boundary or leaves the leg blocked, never silently writes under an obsolete grant. Offline devices cannot know a remote revision before refresh: D4 governs that limitation, while server writes enforce the current authoritative decision. Test server cache invalidation/repopulation, device stale cache and in-flight worker separately.

**Both layers:** all lot routes keep `module:BatchExpiry` alongside permissions, web routes/fields and device data/render paths use `hasModule('BatchExpiry')`/`moduleKey`, and shared sync endpoints omit lot fields/queries when not entitled. Fix product write validation/UI: no new `requires_batch_tracking=true` without entitlement; an existing unentitled flag can be disabled or retained as legacy data during unrelated updates but never authorizes a lot leg. Do not erase historical stock/evidence to clear the flag. Module-off fixtures use **retail** with flagged products and seeded historical lot balances (`apps/api/tests/Feature/Security/BatchExpiryModuleAccessControlTest.php:23–29`), not an impossible parapharmacy disable toggle. D9-enabled deactivation would add that fixture later.

An unentitled company has no new lot UI/data/projection legs while authorized aggregate/fiscal/treasury behavior continues. Existing unentitled lot history is retained and classified in W8; newly applying old lot obligations requires an explicit cutover/entitlement resolution, not today’s activation alone. Unknown entitlement is visible blocked lot work, not guessed enabled/disabled. The drift census cohort is **effective module-entitled company × batch-tracked product**: `apps/api/app/Modules/BatchExpiry/Application/Services/LotLedgerDriftCensus.php:81` currently filters only the product flag and must be fixed, in the shared service as well as command/scheduler. Unentitled tuples may be reported as a separate non-alerting “not entitled” cohort, never daily actionable drift or clean entitled reconciliation. Retain existing module middleware at `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:12` and device helper/test anchors `apps/pos/src/stores/productStore.ts:116` / `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts:5–30`. Canonical config wins over stale architecture prose.

#### Ordered gaps and disposition

Each current-code claim below is anchored at the reviewed `f75aa5023` source. Ticket line numbers from earlier snapshots are not silently reused as current locations.

| Order | Gap and current evidence | Required disposition |
|---|---|---|
| L1 | F2 bodyless recall/destroy and read routes: `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:14–48`; `apps/api/database/seeders/RolesAndPermissionsSeeder.php:407,632` seeds recall and grants it to manager | Both backend and web: enforce recall `batches.recall`, deactivation `batches.delete`, read `batches.view` and traceability `batches.traceability`; preserve module/company/scope checks. Sidebar/routes/actions and seeded-role deltas are specified below; RD2 governs manager recall. Fix expiring’s missing location scope as well as preserving expired’s existing scope. Preserve existing create/update/transfer/write-off checks, rather than describing them as absent |
| L2 | F8 editable number/expiry: `apps/api/app/Modules/BatchExpiry/Presentation/Requests/UpdateBatchRequest.php:20–24`, controller `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchController.php:165` | Freeze ordinary identity/expiry edits after first movement; append permissioned, reasoned correction history and preserve old evidence |
| L3 | Issue/transfer stringify a float accessor at `apps/api/app/Modules/BatchExpiry/Application/Services/BatchStockService.php:471,515–516`; accessor `apps/api/app/Modules/BatchExpiry/Domain/Entities/BatchStock.php:44–46` | Close residual (a) of [F7 FEFO ticket](../tickets/2026-08-08-f7-fefo-residuals.md) by converting/removing every float quantity accessor/writer and migrating all consumers, including both sufficiency guards, BatchStock reservation/adjustment methods, Batch aggregates/Resource and dormant FEFO total; require the complete census/guard below |
| L9 (before L4) | Opening import backs tracked stock with DEFAULT (`apps/api/app/Modules/Import/Services/ProductOpeningStockPhase.php:71–77`; `apps/api/app/Modules/BatchExpiry/Application/Services/BatchStockService.php:31,104`); used-lot edit freeze cannot identify/split that holding | Add permissioned Lot identification on existing batch detail: conserve quantity/value while splitting DEFAULT/unknown holding into identified lots, with its own document, flat stock justification and append-only lineage. Complete before L4 counting and usable L7 opening-stock guidance |
| L4 | F7 parent count lacks lot at `apps/api/app/Modules/Inventory/Domain/InventoryCountingItem.php:79–105`; DEFAULT target top-up pinned at `apps/api/tests/Feature/Inventory/InventoryCountingDefaultBatchTest.php:130–131`; heuristic shortage `apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:1551–1594` | W5 below supersedes [T22/OQ-7](../tickets/2026-08-19-counting-lane-c2-default-lot-inflation.md): actual lot observations determine both gain and loss; no whole-aggregate DEFAULT inflation. Old pin is a limitation, not a required target behavior |
| L5 | Estimate stored in receipt allocations at `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052–2081,2104–2114`; exposed without provenance at `apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchTraceabilityController.php:80–88` | Also cover FEFO-derived `document_lines.batch_id` and `stock_transfer_line_batch_allocations` (producer citations below). Label server FEFO `system_fefo_estimate` everywhere shown; missing physical identity remains `unknown`. Do not backfill `operator_captured` from estimated allocations |
| L6 | Read-only detector exists at `apps/api/app/Console/Commands/LotLedgerDriftCensusCommand.php:26–35,52`; no scheduling closure recorded in [W4R2 fiscal r2 N-5](../reviews/2026-08-25-w4r2-live-projection-gate-r2-fiscal.md#n-5-minor--owner-the-detector-is-shipped-but-nothing-schedules-it) | Fix the service’s missing entitlement filter (`LotLedgerDriftCensus.php:81`), then schedule existing census per entitled company × tracked product with `--fail-on-drift`, bounded overlap and actionable alert ownership; proposed daily run plus release/close checks. No automatic repair. Show coverage, last success and unresolved drift, not a false clean status after a missed run |
| L7 | F1 device product contract lacks lot state at `apps/pos/src/types/product.ts:50–119`; post-sale FEFO is non-strict at `apps/api/app/Modules/POS/Application/Projections/PosCoreReceiptProjection.php:2052–2081` | W6 iteration 1 adds branch eligibility cache and read-only tile/cart suggestion under R2/R3/D4; does not change sealed lot payload or physical traceability status |
| L8 | Physical capture absent from product contract above; fiscal engine is caller-transactional at `apps/pos/src/lib/fiscal/FiscalEventEngine.ts:10–11` | W6 iteration 2, only after D3/D7: captured lot evidence and projection consumption; evidence transport remains open |

For L1, existing FormRequest counterevidence is `CreateBatchRequest.php:22`, `UpdateBatchRequest.php:13`, `TransferBatchStockRequest.php:21` under `apps/api/app/Modules/BatchExpiry/Presentation/Requests/`. New enforcement scope is recall, destroy and reads. Company recall intentionally reaches branches; membership alone grants none, while a role carrying `batches.recall` does until RD2 changes it. Deactivation preserves rows/history and requires explicit stock disposition. Batch read/stock/suggestion endpoints must respect authorized location visibility without exposing other-branch holdings.

**L1 web and role rollout:** existing sidebar Batches entry is module-only at `apps/web/src/components/organisms/Sidebar/Sidebar.tsx:222`; write-off already has a permission at `:223`. Add the `batches.view` entry/route gate through existing `RequirePermission`/`usePermissions`, and permission-gate recall, deactivate and traceability actions in `apps/web/src/features/batches/pages/BatchListPage.tsx` / `BatchDetailPage.tsx`. Keep module gates too. A cashier with view only must not see recall/delete affordances; a role without view sees no Batches entry and cannot enter its route, while direct HTTP access returns 403. Product-embedded batch/expiry fields and stock/trace tabs follow the same read permissions. Fix `BatchController::expiring()` location filtering (`:215–236`) without regressing `expired()` (`:252`), and prove both routes independently.

All seven original `batches.*` permissions already exist (`apps/api/database/seeders/RolesAndPermissionsSeeder.php:403–409`). The following is the **proposed seeded-role delta**, subject to spec/owner acceptance; RD2 remains OPEN for manager recall. No extra role decision is silently presumed from a generic branch membership. “None” means no new grant; review tenant-custom roles separately.

| Seeded role | Current batches grants (`RolesAndPermissionsSeeder.php`) | Proposed L1 grants/delta |
|---|---|---|
| admin | All via `Permission::all()` (`:547`) | Retain all seven; no new owner role |
| manager | All seven (`:631–632`) | Retain view/create/update/delete/write-off/traceability; recall only as RD2 rules, never silently removed/retained as the final choice |
| cashier | view (`:692`) | Retain view only; no recall/delete/traceability |
| viewer | None (`:705`) | Add view for read-only stock visibility; no traceability/customer history |
| technician | None (`:745`) | None; hide Batches entry/actions |
| operator | None (`:770–801`) | Add view only; no traceability or mutation grant |
| accountant | None (`:804`) | None by default; accounting access is not lot/customer-history authority |

Preview/diff the approved grants for each tenant, preserve deliberate custom-role overrides, rerun the tenant permission/grant migration or scoped seeder step on existing staging/production tenants and invalidate permission caches **before** guards activate. No new permission catalogue entry is required for these seven. The new L9-specific permission is separate, described below. Gate activation on missing-seed/grant readiness rather than ship visible-but-403 pages. Exercise fresh tenant and existing tenant before/after the approved role delta on both web and API.

**L3 complete float census and retirement:** `apps/api/app/Modules/BatchExpiry/Domain/Entities/BatchStock.php:44–84` includes the float availability accessor, `hasAvailableStock`, `reserve(float)`, `releaseReservation(float)` and **`adjustQuantity(float)` which persists a float-derived value**. Convert to validated quantity-scale numeric strings/BCMath or remove in favor of the exact generated column; migrate every live caller. Include `apps/api/app/Modules/BatchExpiry/Domain/Entities/Batch.php:143–151` aggregate accessors, outputs at `apps/api/app/Modules/BatchExpiry/Presentation/Resources/BatchResource.php:40,59`, and `apps/api/app/Modules/BatchExpiry/Domain/Services/FEFOInventoryService.php:940–957` dormant float total. A dormant method must be removed/converted so the next caller cannot reintroduce drift. Both issue/transfer guards stay under lock; reserve/release must preserve exact reservation bounds.

Census list/show/create/patch, expired/expiring/recall/transfer/write-off and product batch-stock consumers, web batch types, generated DTOs and device sync; migrate number→string deliberately end-to-end, not only two service lines. Require a live static/architecture guard against new float quantity accessors/consumers, and PostgreSQL discrimination of raw decimal versus float behavior. Record all F7 residual (a–g) dispositions: precision/mutator/consumer fixes and PostgreSQL coverage belong here; SQLite scientific-notation fixture limitations are not production semantics, and response expiry-status casing must have one generated contract across consumers. Do not re-open resolved FEFO tie ordering: `FEFOInventoryService.php:95` already includes `created_at`; preserve it. No still-live residual may disappear behind “census later.”

**L5 three allocation producers, one provenance vocabulary:** label inferred rows from (1) `pos_receipt_line_batch_allocations` / POS receipt projection; (2) `document_lines.batch_id` via `apps/api/app/Modules/Document/Domain/Services/Conversion/Converters/SalesOrderToDeliveryNoteConverter.php:346,477`, `DeliveryNoteFromDocumentFactory.php:123` and `apps/api/app/Modules/Inventory/Application/Services/StockReservationService.php:410–414,751`; (3) `stock_transfer_line_batch_allocations` via `apps/api/app/Modules/Inventory/Application/Services/StockTransferService.php:789,947–989`. Include **both** `document_sales` (`apps/api/app/Modules/BatchExpiry/Presentation/Controllers/BatchTraceabilityController.php:60–74`) and `pos_sales` (`:80–88`), backward trace, receipt/document/transfer details (`apps/web/src/features/stock-transfers/pages/StockTransferDetailPage.tsx`), exports and returns. All inferred FEFO is `system_fefo_estimate`; missing evidence is `unknown`. Do not label all document rows estimated blindly if actual operator evidence exists: establish producer provenance and retain it. A transfer allocation proves recorded custody allocation, not necessarily a physical batch scan. The glossary declares all three existing writers/stores and shared read semantics; no second traceability application.

#### L9 — DEFAULT/unknown lot identification and split (before L4)

Add **Lot identification** to existing batch detail as a permissioned Identify/split action, reachable from the count’s unknown-lot prompt by linking to that same action. Proposed `LotIdentificationService` owns `lot_identifications` (document/header plus typed allocation detail, schema to be planned), with stable tenant/company operation UUID, source batch, product/variant/location, source quantity/version, destination numbers/expiries/quantities, operator, reason/evidence and append-only source→destination lineage. Add `batches.identify` as a distinct action permission: seed it before activation, default `admin` only, explicitly grant designated inventory staff through existing role management. Keep BatchExpiry and location-custody gates on API and web; this is not an ordinary post-use rename or recall-authority grant.

Split all or part of the **available unreserved** DEFAULT/unknown holding at one location into identified lots of the same company/product/variant/location. A reserved, concurrently consumed, overdrawn or unresolved source cannot be reassigned silently; refuse with an actionable resolution path. New destination lots get explicit observed numbers and evidence-supported expiry; no invented expiry, no merging two used lot histories, no rewriting the original opening receipt or old lot metadata. If the target number already belongs to a used history, stop for reviewed correction rather than treating it as a blind merge. A full split retains the now-empty source lot row and history; zero-quantity identification is refused; a partial split leaves its residual unknown holding explicit. Correctly identified targets may then be counted under L4. Missing physical identity stays unresolved instead of fabricating readiness.

The operation writes one justifying document and **one flat aggregate `stock_movements` row** (`quantity=0`, equal before/after), paired with quantity-conserving source debit/destination lot credits. Propose a versioned enum reason `LotIdentification` / `reference_type=lot_identification`, explicitly classified as no-GL; do not abuse stock gain/loss or opening reasons. Use W5’s shared lock census and quantity-preserving reattribution port. Product/variant WAC, aggregate on-hand, reservations and every GL balance remain unchanged. Do not treat historical quantity inflation as identification: if Σ lots differs from aggregate, report/block the split until an evidenced discrepancy resolution establishes a valid source cohort.

Re-run of the same operation UUID and content returns the original result; conflicting content/version fails. A later correction/reversal appends lineage, and moved/consumed destination stock requires a new evidence decision rather than rewriting history. Acceptance must prove DEFAULT→two real lots at two locations/companies, partial and full split, unchanged aggregate/WAC/GL, preserved opening/source history, reserved/foreign/stale/conflicting refusal and zero extra document/movements on retry. L9 is required before L4 and usable L7 guidance for imported opening cohorts; L2’s freeze must not ship without this replacement operator path.

#### W5 — Lot-grain counts and controlled correction (inside W-LOT)

Keep `InventoryCountingItem` as the product/variant/location parent and add child lot-count observations rather than duplicate product-level totals or change every existing item key. For tracked products, each finalized observation carries batch identity, observed quantity, count time/movement marker and operator. Proposed observation uniqueness is `(tenant_id, company_id, counting_item_id, batch_id)` with persisted location and validated parent/batch/product/variant/location ownership; tenant-only uniqueness is forbidden. Catalogue-versus-evidence table classification must be recorded under convention 09. The parent total is derived from accepted child observations. Missing row is not zero; an explicit zero means physically absent. Unknown physical lots require the L9 identification/split operation before finalization; do not invent expiry or silently assign DEFAULT.

Apply discrepancies to their actual lot through the stock adjustment service. Retain current aggregate cost/opening/replay rules, locking the product cost basis and relevant lot/stock rows consistently. Remove FEFO as a substitute for lot identity on new tracked counts. Sum of lot movements must match the aggregate adjustment for each product/variant/location. Finalization also checks absolute Σ lots against aggregate, so pre-existing drift cannot be hidden by a balanced delta. Re-derive the old DEFAULT test from identified lot observations; an explicit observed DEFAULT/unidentified opening cohort is not permission to top it up to the entire aggregate. An unchanged product total can still require offsetting lot corrections: booked 10+10 versus observed 15+5 writes **one** justifying aggregate `stock_movements` row with `quantity=0` and `quantity_before === quantity_after`, paired with −5/+5 lot legs. Use existing `MovementReason::CountCorrection` and `StockMovementReferenceType::InventoryCounting` with the counting ID (`apps/api/app/Modules/Inventory/Application/Listeners/ApplyStockAdjustmentsOnCountingCompleted.php:257,376`) and display reference `COUNT_REPLAY` (`StockAdjustmentService.php:69`), linked to the finalized count evidence. This is the flat movement/lot-leg pair, not two non-flat aggregate adjustments. `apps/api/app/Modules/Inventory/Domain/StockMovement.php:183–192` derives flat and `apps/api/app/Modules/Inventory/Application/Services/InventoryGlPostingService.php:37–52` skips GL only for flat. Preserve the zero-quantity precedent at `apps/api/app/Console/Commands/RepairPhantomDefaultBatchesCommand.php:51–60`. The existing `postCountCorrection()` zero-adjustment return at `StockAdjustmentService.php:1410–1415` remains correct for a true no-op; add a distinct lot-only reattribution branch when actual lot deltas are nonzero, rather than remove the no-op guard. Exactly one aggregate justification, zero journal entries, unchanged WAC; no artificial gain/shrinkage P&L pair. Re-run produces no additional movement or GL.

Reuse the existing count-as-of and movement replay guards (`apps/api/app/Modules/Inventory/Domain/Services/StockAdjustmentService.php:75–78,1390` for replay collaborators/postCountCorrection). If offline receipts can still alter the counted interval, finalization must require synchronized participating terminals or an explicitly provisional count that cannot claim reconciled final stock. Reuse `apps/api/app/Modules/Inventory/Application/Services/LateSyncResidualDetector.php:16–24` for the existing read-only late-sync signal rather than invent a second detector. A late pre-count movement reopens a reconciliation exception; do not silently change a finalized operator observation or apply its effect twice. Untracked products keep the existing counting flow. In-progress legacy tracked counts require completion under documented legacy semantics or cancellation/restart into lot mode; migration cannot fabricate observations.

**Inventory lock census, required before plan:** enumerate every writer touching `inventory_batch_stock`, `stock_levels`, reservations and product cost, including counts, identification, receipts, transfers, FEFO/POS, correction/reversal and WAC. Record transaction owner, lock scope/order and hold time. Respect the established advisory → stock_level rows → product row-last constraint (`apps/api/app/Modules/Inventory/Application/Services/WeightedAverageCostService.php:87–95`); integrate lot/observation locks consistently only after the complete census. `FEFOInventoryService.php:264–272` uses `FOR UPDATE SKIP LOCKED`; this can skip a count-locked earliest lot and divert a sealed sale or report shortfall. Required design outcome: serialize same-product count-finalize/identification and POS lot consumption through their common orchestration lock **before** the FEFO query, so count lock contention cannot silently select another lot. If a genuine delayed pre-count sale remains outstanding, keep the count provisional and record the reconciliation exception; no “reconciled final stock” on incomplete inputs. A real PostgreSQL count/identification-versus-sale/transfer leg must prove progress, no deadlock, no silent SKIP LOCKED diversion and no duplicate GL. W3’s GL writer census must reconcile nested stock→GL locks before dispatch.

T22 item 4’s old “negative counts move no lots” statement is already superseded by the live heuristic at `StockAdjustmentService.php:1530–1594`; preserve that counterevidence and replace heuristic attribution with actual observations, without claiming the existing negative leg as newly delivered.

After a lot has stock movements, ordinary update cannot change its identity or expiry. Add a permissioned correction action on its existing detail screen recording old/new values, reason, actor, timestamp and evidence reference. Use optimistic version checking to reject stale corrections. Corrections append history; original receipt/sale snapshots remain unchanged. Identity change must not merge two lots with existing histories. Deactivation of a lot with stock requires explicit disposition, not hidden removal from eligibility. Recall is a separate action, not an editable boolean.

**Seams:** `InventoryCountingItem.php`, count submit/review DTOs and services, `StockAdjustmentService.php`; additive tenant child table; `BatchExpiry/Presentation/Requests/UpdateBatchRequest.php`, `BatchController.php`, batch repository; existing `apps/web/src/features/inventory-counting` and `features/batches` surfaces.

**Acceptance:** 10 short-dated + 10 long-dated booked, actual 10 + 5: only long-dated lot decreases by 5; explicit zero versus omitted lot; surplus unknown lot; variant; count interrupted by sale/transfer; late sync; repeated finalize; active legacy count; concurrent correction; unauthorized expiry extension; two-company/two-location stock and GL reconcile.

#### W6 — POS lot guidance now, captured evidence later (inside W-LOT)

**R2 applies in both iterations:** device `hasModule('BatchExpiry')`, web `hasModule`/`moduleKey`, API `module:BatchExpiry`, gated sync fields and explicitly scoped worker lot legs. With the module absent: no lot tile/cart content, selection, lot-cache payload or projection legs. Null/unknown config is not permission to render or write lot data. Extend `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts` plus actual device/render/projection coverage; the helper test alone cannot prove the feature is gated.

**Iteration 1 — owner-ruled R3, display only.** Extend the existing decimal-string stock distribution/cache/availability path: `apps/pos/src/types/stockDistribution.ts:1–13`, `apps/pos/src/lib/stock/gridStock.ts:31–38`, `apps/pos/src/lib/db/repositories/locationStockRepository.ts`, and `apps/pos/src/components/organisms/ProductGrid/useStockDisplay.ts:57–71`. `types/product.ts:47,56` / SQLite’s legacy `stock_quantity INTEGER` at `apps/pos/src/lib/db/migrations.ts:29` are evidence of today’s product contract gap, **not the precision precedent**. Require an additive local cache migration in the later implementation: lot quantities are canonical scale-4 decimal strings in SQLite TEXT, server DTO, transport, calculation and display inputs; no REAL/integer stock field, `Number()` or `parseFloat`. The cache stores tenant/company/location/product/variant, lot identifier/number, nullable evidence-supported expiry, active/recall status, **available quantity net of server reservations**, snapshot coverage/time/revision and entitlement revision. A company/location switch clears/replaces its scoped cache. Human quantities use `quantity_decimals` / `formatQuantity` (product contract `:112–119`), not fixed four-decimal UI formatting.

**Effective suggestion, not a static snapshot:** reuse/extend `apps/pos/src/lib/stock/stockGate.ts:45–94` and its availability selector pattern; `gridStock.ts:6–9` explicitly warns the tile snapshot omits pending receipts/cart. Deterministically distribute this device’s completed sales not yet included in the snapshot and its cart demand over eligible cached lots in the server’s FEFO order, using exact quantities net of reservations. This local depletion is a read-time advisory calculation, not a lot ledger or captured allocation. Use receipt/event IDs and snapshot coverage markers to remove a pending sale’s subtraction only when its effect enters the server snapshot; an ingest ACK alone does not prove inclusion. No double subtraction across resync/restart. Unknown coverage means unavailable/uncertain guidance, not inventing a reconciliation watermark. Refunds add availability only under the stock gate’s accepted disposition/projection rules, never merely because money was refunded.

Use one suggestion calculation across views. For a cart line, allocate its demand after preceding lines in stable cart order without subtracting that same line twice; the tile’s next-add suggestion accounts for the whole current cart. They can legitimately differ when the cart already consumes the first lot; this is explicit next-add versus existing-line guidance, not contradictory algorithms. Example: L1=2, L2=5, cart holds two units from the advisory L1 allocation: cart shows L1 and tile suggests L2; after completing that sale offline, the next customer still sees L2. A server-reserved L1 is excluded from both. Local completed demand and known refresh/expiry/recall changes recompute guidance without a synchronous checkout request.

**One render surface:** fill `apps/pos/src/components/organisms/ProductGrid/NearExpirySlot.tsx:1–27`, already mounted by `molecules/ProductCard/ProductCard.tsx:566–567`, `organisms/ProductGrid/ProductListRow.tsx:116–117` and `organisms/ProductGrid/ProductTable.tsx:257–258`. W6 takes ownership of the deferred “Spec 2” lot/near-expiry plumbing reserved by [the caisse presentation spec](2026-07-08-caisse-visual-presentation-redesign-design.md) at `:56,204`; its R3 suggestion fills that existing slot and uses one expiry/lot model, never a parallel chip. Preserve its established presentation placement; if an expiry warning threshold applies, combine it with the suggestion rather than add another source. CartLineItem consumes the same presentation/data semantics on its existing line. The later renderer census includes legacy `components/pos/ProductCard.tsx:22–23`, `components/pos/ProductGrid.tsx:54` and `molecules/BarcodeChooserModal/BarcodeChooserModal.tsx:151`; gate or retire active alternates explicitly so module-off data cannot leak.

The product tile and cart line show a read-only “Suggested lot” number and expiry, or the available cached identifier with unknown expiry clearly indicated. No selector, editable lot field, scan-to-capture behavior or confirmation is introduced. An ordinary SKU scan is not lot evidence. The cashier can use the suggestion to choose shelf stock operationally. Local known expiry/recall excludes a lot from suggestions, but this mode cannot establish which physical lot leaves the shelf.

Cache refresh, cart quantity/variant changes, date rollover and known recall changes recompute guidance from the snapshot plus the effective-demand overlay above and make a changed/unavailable suggestion visible. If quantity spans multiple lots, show the next lot and that it does not cover the full line, without presenting one lot as the entire physical allocation. Empty cache/no eligible lot renders “suggestion unavailable”; unknown expiry never becomes a fabricated date or “safe” assertion. Surface snapshot age and staleness under D4. While D4 is open, no approved freshness cutoff or extra checkout refusal is implied. Existing SKU-level checkout semantics stay in force; unavailable guidance is not proof of safety or stock availability. No local suggestion reservation or global no-oversell promise is introduced.

**Iteration 1 leaves the sealed payload unchanged.** No lot fields, evidence outbox or captured-lot projection dependency. Server FEFO remains the authoritative computed allocation and is labelled `system_fefo_estimate`, even when it matches the displayed suggestion. Offline sales/order/timing may make the allocation differ; keep the receipt, report shortfall/drift via L6 and never promote the device suggestion into `operator_captured`. D3 governs whether shelf controls suffice for the intended release.

**Iteration 2 — conditional on owner D3 and D7.** `captured_lot` records scanned/confirmed lot identity and exact quantity using branch cache and approved freshness rules; `shelf_control` remains explicitly estimated. New captured-mode authoring must persist receipt and required evidence atomically in the caller's SQLite transaction. D7 chooses either new sealed line fields (recommended) or a separate versioned linked record; this spec does not select the stream. Inline fields use fiscal event integrity; a sidecar must bind terminal/company/location, receipt event ID/hash, line/product/variant, quantities, capture method and cache revision, enforce duplicate/conflict checks, and tolerate either arrival order. Both require mixed-version capability validation and append-only corrections; neither edits historical sealed bytes.

Regardless of D7, the receipt projection remains the single owner of sale stock effects. It consumes valid captured lots without substituting later FEFO as captured truth. Missing/conflicting evidence, insufficient lot balance or changed eligibility creates durable inventory reconciliation work while preserving the fiscal receipt and money effects; do not block all POS-core/Treasury work waiting for optional evidence. Track aggregate versus lot completion separately, reuse movement/source idempotency, and never decrement the aggregate twice when evidence later arrives. Resolution appends reason and movement links. A post-sale recall is not retroactive proof the sale was invalid; delayed-sale eligibility uses sale-time evidence where known and otherwise records uncertainty.

Offline terminals cannot guarantee global availability; simultaneous consumption becomes a visible conflict unless a separately approved allocation design exists. Refunds use original captured provenance where available plus actual returned lot/disposition; legacy estimates stay estimates. D6 separately gates cross-branch returns. No iteration 2 task plan or schema starts before D3/D7 and Fable dispatch.

**Seams:** generated server lot-stock DTO and existing location-stock distribution/sync/repository; additive SQLite TEXT cache schema; stockGate/availability plus suggestion selector; NearExpirySlot and its three mounted views, CartLineItem and active alternate-renderer census; module/permission caches; FEFO, receipt/document/transfer allocation provenance. Iteration 2 alone adds D7’s chosen captured-evidence transport/storage. D7/D8 share one version roadmap; there is no independent iteration-1 fiscal schema change.

#### W-LOT acceptance matrix (later execution; nothing run in this round)

All applicable rows require **two companies through real provisioning, two locations, two real lots, and re-run idempotency**; add two terminals for POS rows and separate cross-tenant denial fixtures. Module-off and missing-config fixtures are mandatory, not optional vertical smoke tests.

| Coverage | Discriminating scenario | Required outcome |
|---|---|---|
| L1 action authority | Cashier/read-only staff recall/delete; manager with allowed versus denied RD2 role; direct trace/stock IDs across company/location | Denied mutations change no stock/history; permitted company recall spans branches; read visibility honors authority; existing gated create/update/transfer/write-off remain valid; web Sidebar/route/actions match API denial; fresh/existing tenant role deltas/reseed are verified; expiring and expired scope checked separately |
| R2 all layers | Entitled parapharmacy and module-off retail with flagged products/historical lots; unknown config; company mismatch; 24h server cache populated then invalidated, stale repopulation/in-flight worker and stale device cache; D9 deactivation only if chosen | Worker checks effective company entitlement independently of product flag; no unentitled lot UI/data/legs or false census alerts; authoritative revision cannot be revived by stale cache; authorized aggregate/fiscal behavior and old evidence preserved |
| L2 used-lot correction | Movement exists; ordinary expiry extension/rename; concurrent authorized correction | Ordinary mutation refused; old/new values, actor, reason and evidence retained; stale update rejected; no merging used histories |
| L3 exact availability | Fractional issue and transfer at exact sufficiency/shortfall boundary, reservations, concurrent movement, PostgreSQL decimals | Exact comparison, no float/scientific-notation guard failure; no overdraw or double transfer; quantity and custody invariant preserved; reserve/release/adjustQuantity, Batch aggregates/Resource and dormant FEFO total are exact or retired; all consumers and float-guard liveness covered |
| L9 identification before L4 | DEFAULT→two identified lots in two companies/locations; partial/full split, reserved stock, foreign source/target, duplicate and conflicting operation UUID, consumed target/history | One identification document and flat aggregate justification with offsetting lot legs; Σ lots, aggregate, WAC and all GL unchanged; source/opening history retained, no merge/rename of used histories, repeated operation adds nothing |
| L4 actual count | Booked short/long 10+10, observed 10+5; also surplus, explicit zero, missing row, 15+5 with same aggregate and variants | Correct lot alone changes, or offsetting lot corrections when appropriate; parent is child sum; no FEFO shortage guess or whole-aggregate DEFAULT top-up; Σ lots equals aggregate at accepted finalization; 15+5 reattribution writes one flat stock row plus −5/+5 lot legs, zero journals and unchanged WAC; true no-op still writes no row |
| L4 drift and timing | Pre-existing DEFAULT inflation, unknown physical lot, legacy active count, sale/transfer during count and late pre-count receipt | Identification/reconciliation blocks false finality; existing late-sync detector reused; no invented lot observations/expiry; repeat finalize has no extra movement/GL; PostgreSQL count/identification versus sale/transfer serializes before FEFO, never silent SKIP LOCKED diversion or a false final count |
| L5 provenance | POS allocation, document_lines/trace document_sales, transfer allocation/detail, absent allocation, current tile suggestion, forward/backward trace, export and return | All three producers and both traceability branches: every FEFO allocation displayed as `system_fefo_estimate`; absent identity `unknown`; nothing relabelled captured; estimates cannot certify physical traceability |
| L6 scheduled detection | Entitled tracked drift/clean cohort, unentitled flagged cohort, missed/failed run, duplicate scheduler and wrong tenant, contained lot-arm failure | Shared-service entitlement filter plus read-only census; non-entitled cohort is non-alerting, alert and last-success visibility; no correction writes; projection can preserve fiscal sale while drift stays visible |
| L7 iteration 1 | Two lots in NearExpirySlot on ProductCard/ProductListRow/ProductTable and cart; L1 depleted by cart/offline sale; server reservations; snapshot coverage after ACK; fractional quantities; quantity exceeds first lot, variant/location switch, expiry rollover, recall/cache refresh, stale/empty cache and offline retry | One exact effective-availability model with intentional next-add versus cart-line semantics, read-only next-lot guidance with number/expiry or available identifier; changes/unavailability honest; no lot selector or capture, no sealed payload bytes changed; server estimate may differ |
| L8 iteration 2, D3/D7 only | Local crash boundaries, duplicate/conflicting evidence, either delivery order for sidecar if selected, cross-terminal forgery, late expiry/recall, disconnected contention and original-lot refund | Atomic local required evidence; immutable fiscal facts; one aggregate/lot effect or durable exception; no FEFO substitution labelled captured |

W-LOT's later handback must map every L1–L9 item to evidence, explicit gated deferral or unresolved finding. T22/OQ-7 is superseded in design by L4, not declared implemented/closed today. A balanced lot ledger is quantity consistency, not proof of physical lot capture.

### W7 — Session reconciliation against fiscal-derived totals

**Finding:** F6; review F-D. **Required for a trusted POS branch close.** Session reconciliation means comparing authored device totals with independently derived fiscal-event totals, not bank reconciliation, orphan-session closure or a Treasury balance comparison. Its canonical noun/write path/surface is registered in [the glossary](../../glossary.md).

Retain signed device close/Z records. Proposed `PosSessionReconciliationService` owns one result store (`pos_session_reconciliations`, proposed) keyed by tenant/company/terminal/session/Z identity, with append-only run history, input fingerprint and states `pending_sync`, `blocked`, `mismatch`, `matched`. The existing shift/Z detail is the primary operator surface. Show each check's coverage and provenance. A fiscal-totals `matched` badge must not claim captured lots or reconciled Treasury custody; lot quantity/evidence status and the separate repository check stay visible. A broad “fully reconciled” badge requires all launch-selected checks and their dependencies, without hiding an incomplete leg.

**Reuse and extend `apps/api/app/Modules/POS/Application/Services/ShiftExpectedCashService.php:27–59,252–299` as the single shared derivation for this work.** It already handles fiscal cash movements, net cash takings, opening float and account collections. Its comments distinguish v3 fiscal-session events from v2 drawer operations and acknowledge remaining legacy consumer boundaries; do not claim all historical report code already shares it. Add any missing membership-aware input selection to the shared derivation rather than copy its arithmetic into a new reconciliation service. The reconciliation service orchestrates completeness/comparison and stores results; it is not a second expected-cash calculator. Preserve explicit bounded shift end and terminal authority (`ShiftExpectedCashService.php:61–80`).

Compare device tender/cash totals against that fiscal derivation. Derive sales and refund VAT separately by rate/category from verified canonical receipts; compare like-for-like headers/buckets using shared precision and rounding rules. Do not simply enable the invalid sum check skipped for refunds at `apps/api/app/Modules/Fiscal/Application/Services/FiscalPayloadConstraintValidator.php:1071–1073`. A changed classification or unresolved source cannot silently turn a value into zero; use W2 authored/cutover classification and block ambiguous history.

**Membership has useful existing checks, not a blank slate:** `apps/pos/src/lib/offline/zReportService.ts:749–752` uses legacy receipt hash/hash-sequence bounds, not canonical operational sequence numbers. `apps/api/app/Modules/POS/Application/Projections/ZReportProjection.php:288–294` documents the **device payload builder’s** exclusion of terminal-settled refunds from receipt_count (`apps/pos/src/lib/offline/zReportService.ts:159–176,640`); the server’s `ZReportProjection.php:305–319` waits for projected verified sales in a device-time window. Preserve these checks, but they do not establish complete membership of refunds, collections and cash-operation streams. A later plan must define a versioned close manifest covering every contributing stream with identities/hashes/counts and explicit sequence namespaces. Reuse proven bounds where sufficient; never treat receipt count as all-event count or timestamp inference as full completeness. Historical insufficient bounds remain explicitly limited/pending.

Late members within a declared close boundary trigger a new fingerprint and recomputation. Outside-boundary events naming the session create exceptions, not deletion of fiscal facts or rewritten Z totals. Projection/evidence incompleteness keeps the relevant check pending/blocked. Concurrent receipt/projection arrivals cannot produce a clean badge from a mixed input set: evaluate a stable input snapshot and invalidate the result when its dependencies change.

**Separate optional check: repository-balance coverage, gated by RD4.** `apps/api/app/Modules/Treasury/Application/Listeners/PostShiftCashVarianceAdjustment.php:50–56` explicitly ships the variance GL listener disabled because Treasury lacks opening-float and mid-shift-operation booking. Do not compare today's repository balance to whole-drawer expected cash as if coverage matched, and do not enable this listener as a shortcut. W7's fiscal comparison can proceed independently after its prerequisites.

If RD4 funds repository coverage, its later plan must book opening float and drawer movements (including drops) through existing Treasury services with stable source identities, correct counter-custody, opening-balance alignment and replay protection. A shift's float count is not automatically new company money: reconcile it to the existing drawer opening or a safe→drawer transfer, never double-book it on every shift. Only then compare repository opening plus covered movements against the corresponding fiscal/physical interval. Include back-office drawer movements once, attribute shared drawers across terminals and avoid counting two opening floats. Bank settlement is a different interval/check. Until coverage is accepted, show repository comparison as unavailable/not enabled, never matched. No new Treasury booking policy is approved by this spec.

**Seams:** shared `ShiftExpectedCashService`; `ZSessionLifecycleProjection`, `ZReportProjection`, fiscal payload validator and `OutboxIngestor`; proposed reconciliation result service/table and existing shift/Z detail; device close manifest/version cutover. Reuse expected-cash derivation, Z authoring and existing exception surfaces. No new orphan-close reconciler, bank-reconciliation surface or automatic variance money writer.

**Acceptance:** taxed sale plus partial refund, multiple rates, rounded cash/card, account collection, opening float and fiscal drops, verified membership gaps, missing projection, late valid member, out-of-manifest receipt, refund-only negative VAT bucket, duplicate Z and legacy insufficient bounds. Sealed hashes with wrong economic totals must mismatch. Lot estimate status remains explicit even when fiscal totals match. Before RD4 coverage, repository comparison is unavailable; after it, actual booked float/drops/back-office movements and two terminals sharing custody reconcile without double entries. Real expected values derived independently of the Z summary are required, not copied fields. This is not external assurance of the device chain: consistently unrecorded sales can evade both summaries. Preserve ingest sequence-conflict quarantine (`apps/api/app/Modules/Fiscal/Application/Services/OutboxIngestor.php:37,48`) and `fiscal:verify-event-chain` as complementary chain-continuity checks; these still do not prove a physical sale was recorded. Include unknown/reclassified historical tender cases producing the explicit `UnknownTenderClassificationException` boundary (`ShiftExpectedCashService.php:250`), never a zero or matched close.

### W8 — Client-specific release proof and migration discipline

**Scope:** integrate W0/W1–W4/W-LOT/W7 without expanding into a new product; W5/W6 are internal W-LOT references. Only owner-selected iteration 2/RD4 capabilities enter release scope. Run against the chosen release artifact, PostgreSQL tenant topology and actual terminal build.

- Register catalogue/evidence classification for every added/touched table in convention-09 manifests. `product_batches` already has company-scoped uniques (`apps/api/database/migrations/tenant/2026_06_02_100008_add_variant_id_to_product_batches.php:27–31`); add its missing catalogue-manifest entry rather than change those keys. Count observations, lot-identification documents, binding resolutions and session reconciliations are evidence tables with explicit scope/source identities; classify them deliberately.
- Additive tenant migrations first, then compatible server readers, then explicit per-company/terminal capability enablement. Keep old fiscal readers. Do not enable new tender/lot/close authoring before its server acceptance and recovery paths exist.
- Census existing repository routing, sealed documents without GL, missing projections, DEFAULT/unknown lots, in-progress counts and unresolved closes; include retail/fashion flagged products with unentitled historical lot legs, and record their non-alerting entitlement cohort/cutover disposition. Backfills may annotate provenance/capabilities; financial corrections require explicit compensating operations. Never fabricate captured lots or approvals.
- Back up and rehearse restore before cutover. Rollback disables new authoring capability while retaining ingestion/readers for already emitted new versions and all outbox data. No destructive migration rollback over financial history.
- Extend the existing onboarding campaign with two-branch/two-terminal journeys and real device smoke. Synthetic fiscal vectors are useful but do not replace Tauri/SQLite/printer/offline/crash testing.
- Gate on every required leg's result and candidate revision, not an empty findings list or process exit alone. Any required skipped/blocked leg means incomplete evidence.
- Record the Tunisia accounting/configuration review and existing owner launch gates on the actual release. Do not copy an old blank checklist as fresh approval.

## 5. Dependencies and review boundaries

This table describes dependencies, not authority to dispatch work or a fixed parallel schedule. Fable alone sequences lanes from accepted specs and briefs. W-LOT is one package even if Fable breaks its ordered work into implementation tasks.

**One D7/D8 SALE_RECEIPT version roadmap before either authoring lane starts:** Fable records both owner choices, exact canonical version/capability matrix and one cutover. If either choice needs sealed fields, combine the selected tender and captured-lot fields in **one** future version rather than two independently named vN+1 releases; if the second decision is unresolved, hold sealed authoring instead of minting a speculative version. If both choices use nonsealed evidence, no SALE_RECEIPT bump is needed. Keep old readers/vectors and explicit legacy policy; test optional/required fields by selected capability and non-money-leg semantics. R3 iteration 1 remains unchanged sealed payload. An owner-approved exception to that timing must be recorded with D8 before dispatch, not assumed by W2. This roadmap governs W2, W-LOT L8, W4 readers/recovery and W7 membership together.

| Package | Prerequisites and plan gates | Completion boundary |
|---|---|---|
| W0 | Fable r3 and guard/baseline review | Seven live guards/standing acceptance obligations linked to their mechanisms; unrelated legacy debt remains explicit |
| W1 | D1/D5; G1/G2 route/custody and device-consumer census; W2 terminal binding projection and cache transition before human scoping | Consistent repository reads, transfers and adjustments under approved scope |
| W2 | D2/RD3/D8; every-leg census and non-money class; W1 terminal projection; joint D7/D8 roadmap, repair and drained-classification cutover | One binding from authoring through first projection/replay; explicit seed behavior reversal only after ruling |
| W3 | B2B selected by D2; complete writer/lock and duplicate-source censuses | Atomic seal/GL/advance state, shared allocator and PostgreSQL concurrency proof |
| W4 | Activation/cutover contract shared with W-LOT; G6 standing scenarios | Registry failure cannot erase work; lost enqueue and abandoned running recover |
| W-LOT L1–L6 plus L9 / W5 | R1/R2 worker/cache fix; RD2 role ruling, D9 module policy; L9 before L4 and alongside L2 freeze; full float/provenance/lock census; W0 guards | Lot permissions, used-lot history, exact issue/transfer guards, actual counts, provenance and scheduled drift detection |
| W-LOT L7 / W6 iteration 1 | R3; R2 module checks; D4 freshness; branch cache and consistent suggestion rules; L3/L5/L6 and L9 for imported cohorts; NearExpirySlot and exact effective availability | Read-only tile/cart guidance, unchanged sealed lot contract; D3 still governs suitability for launch |
| W-LOT L8 / W6 iteration 2 | D3/D7 owner decisions and joint D7/D8 roadmap, D4 freshness, W4 recovery and reliable lot balances | Captured evidence only through chosen transport, fiscal money preserved on inventory exceptions |
| W7 fiscal comparison | W2 classification, W4 source completeness, manifest contract; W-LOT status integration for lot-enabled launch | Independently derived session totals and honest per-check coverage; no assumed Treasury balance parity |
| W7 repository comparison | RD4 and separately accepted Treasury float/drop coverage | Comparable custody intervals, no duplicate float/variance entries |
| W8 | Every launch-selected package, exclusions recorded under D1–D9/RD2–RD4 | Same-candidate two-company/two-branch/two-terminal acceptance and owner launch gate |

Every later plan must name files/contracts, focused discriminating tests, migration/cutover/backfill order, operational alerts, compatibility and Fable's reviewer gates. Shared permission, GL and module-policy seams must be reconciled by Fable before integration. An approved spec or program dependency table is not implementation authorization.

## 6. Required evidence for later implementation

| Requirement | Evidence | Existing anchors to extend (backend paths under `apps/api/`) |
|---|---|---|
| W0 | Ratchet liveness, coverage baselines, grep-able limitation register and standing recovery rows | `TenantOnlyUniqueOnCatalogueTablesRatchetTest`; archaeology G1–G7 |
| W1 | Real HTTP authorization plus unchanged movement/GL snapshots for denied reads/mutations | `tests/Feature/Identity/UserManagement/UserLocationAccessTest.php`, `tests/Feature/Treasury/RepositoryTransferEndpointTest.php`; adjustment endpoint coverage |
| W2 | Full cash/electronic destination matrix, one binding, remap-before-sync and legacy replay | `tests/Feature/Treasury/BranchCashRepositoryRoutingTest.php`, `TenderRepositoryResolverTest.php`, `PaymentRepositoryCompanyScopeTest.php`; `tests/Feature/Tenant/CleanRegistrationDownstreamAssumptionsTest.php` H-3, RD3 open |
| W3 | Fault injection inside document transaction; PostgreSQL concurrent writers, unique source and chain continuity | `tests/Feature/Accounting/DocumentGlPreflightTest.php`, `InvoicePostedListenerTest.php`; new PostgreSQL concurrency leg after census |
| W4 and every projection lane | Registry throw; enqueue lost after commit with attempts=0; worker dies in running/between effect and status; dependencies, quarantine and training | `tests/Feature/Fiscal/FiscalEventProjectionRegistryTest.php`, `ApplyFiscalEventProjectionJobTest.php`, `FiscalEventProjectionDispatcherTest.php`, `RetryFiscalProjectionsCommandTest.php` |
| W-LOT L1–L6 plus L9 / W5 | Complete acceptance matrix above; real-lot count meaning, exact PostgreSQL availability, provenance and scheduled read-only drift | `tests/Feature/Security/BatchExpiryModuleAccessControlTest.php`, `tests/Feature/Inventory/InventoryCountingDefaultBatchTest.php`, `tests/Feature/BatchExpiry/BatchStockServiceVariantTest.php`; existing drift and late-sync detectors |
| W-LOT W6 iteration 1 | Actual tile/cart rendering, module on/off, shared cache age/eligibility, no selection or sealed-lot change | `apps/pos/src/stores/__tests__/productStore.hasModule.test.ts`, existing NearExpirySlot/ProductCard/ProductListRow/ProductTable/CartLineItem tests and SQLite/device integration; `tests/Feature/Fiscal/PosCoreReceiptProjectionBatchLotTest.php` |
| W-LOT W6 iteration 2 only | D7-dependent local atomicity/cutover, single aggregate writer and evidence conflict recovery | Same receipt projection anchors plus new chosen-transport/device tests; not an iteration 1 requirement |
| W7 | Independent economic expected values, stream completeness, coverage labels; RD4 separate check | `tests/Feature/Fiscal/ZReportProjectionTest.php`, `FiscalPayloadConstraintValidatorTest.php`, `tests/Feature/POS/ZReportRefundVatDisclosureTest.php`; shared expected-cash and new reconciliation tests |
| W8 | Two-branch business journey and recovery on actual candidate | `docs/qa/ONBOARDING-CAMPAIGN.md`, `scripts/campaign-onboarding.sh`, existing real-device smoke and restore runbooks |

SQLite-only passing results cannot establish PostgreSQL row-lock/precision/constraint guarantees. Required skipped or blocked legs are incomplete evidence. During future implementation use focused tests and repository preflight/CI before Fable promotion, including React diagnostics for actual React changes. **No tests, code, migrations, deployment or runtime configuration changes are performed by this spec fix round.**

## 7. Handoff and stop condition

Round 2 authority is [the owner’s handover](../../handoff/HANDOVER-parapharmacy-remediation-codex-spec-fix-round-2-2026-09-05.md). Inputs read: [r2 synthesis](../reviews/2026-09-05-parapharmacy-remediation-gate-r2-synthesis.md), its four linked fiscal/inventory/treasury/authz lenses, and [round-1 R1–R4](../../handoff/HANDOVER-parapharmacy-remediation-codex-spec-fix-round-2026-09-05.md). The synthesis owns the correction to r1; old reviews are not edited. The older audit and archaeology remain historical inputs.

Deliverables are this v3, the glossary updates and [round-2 notes](../reviews/2026-09-05-parapharmacy-remediation-codex-fix-round-2-notes.md), including per-blocker/per-major disposition and the full twelve-row OPEN decision register. **Codex stops after these working-tree documents. No product code, tests, migrations, runtime configuration, reviewer calls, commits, merges, rebases or pushes. Fable commits, runs gate r3 and sequences any subsequent work.** Spec acceptance is not implementation or launch authorization.
