> Audit lane: roles & permissions, 2026-09-09. Produced by a Sonnet research agent from HEAD e85641a66 (dev); read-only. Verification verdicts: see 06-synthesis.md §0.

# HISTORY dossier — roles & permissions in the ERP repo

Read-only research. No edits, no git writes made. Compiled 2026-09-09.

**HEAD at task start:** `git rev-parse --short HEAD` = `e85641a66` (full: `e85641a667f97c96b2574a967defef8a3c156f91`), branch `dev`.
**Note:** `apps/erp` is a shared, actively-worked checkout (CLAUDE.md rule 21 — sessions merge to local `dev` continuously). During this audit HEAD advanced on its own to `bd986f898` ("review(2026-09-09): lane T-1 gate r3 stock-gl-interaction — MERGE"), confirming a parallel session is live right now on branches that touch permissions (see §3). Two untracked files were present at start and remained untouched: `apps/api/docs/sessions/2026-08-04-cross-tenant-scheduled-jobs-plan.md`, `docs/handoff/HANDBACK-enforcement-p1-2026-08-19.md`.

---

## 1. Chronological table

| Date | Source | Finding / decision | Status |
|---|---|---|---|
| 2026-05-14 | `docs/security/audit-log-dispatch-review-2026-05-14.md` | `RoleController::assignRole()`/`::removeRole()` called Spatie's `assignRole()`/`removeRole()` directly — no domain event, so a `model_has_roles` row was written with no actor, no timestamp, no removal record. | **FIXED** — `RoleAssigned`/`RoleRemoved` domain events + `handleRoleAssigned`/`handleRoleRemoved` listeners added (G1 fix), tests `tests/Feature/Identity/RoleAssignmentAuditTest.php`. Marked 🟢 fixed in the review doc itself. |
| 2026-06-12 | `apps/erp/.claude/memory` `project_sidebar_regroup_module_hardening.md` | Module gating hardened fail-closed (nav-level); routes still only role-gated (`RequirePermission`), no route-level module gate at the time. `usePermissions` hardcoded role map flagged as pending. | Module gating: **FIXED** then (later formalised as `RequireModule`/`hasModule`, see `docs/architecture/vertical-module-gating.md`). `usePermissions` hardcoded map: carried forward, see 2026-06-13 row and TD-005 below. |
| 2026-06-13 | memory `feedback_usePermissions_hardcoded_map.md` | `apps/web/src/hooks/usePermissions.ts` uses a hardcoded `ROLE_PERMISSIONS` map instead of reading the auth payload's real Spatie permission list — custom roles with granted permissions are silently denied on the frontend. | **OPEN.** Still documented as the live pattern in `docs/conventions/03-AUTHORIZATION.md` (Last Updated 2025-12-30, itself stale) and tracked as **TD-005** in `docs/PRODUCT-BIBLE.md` §8 ("Hardcoded FE permissions map (`usePermissions.ts`)", Priority Medium, Open). |
| 2026-06-14 | `docs/superpowers/audits/2026-06-14-go-live-security-audit.md` (per memory `project_go_live_security_audit_2026_06_14.md`) | Full go-live security audit, 6 parallel specialist agents. **#1 HIGH launch-blocker privesc:** `RoleController::assignRole/store/update` + `Identity/routes.php` had no `permission:` middleware and no `can()` — a cashier could `POST /api/v1/users/{self}/roles {"role":"admin"}` for full tenant admin. `roles.store` also minted arbitrary-permission roles. **#2 HIGH:** `UserController::store/update` `role` field validated only `exists:roles,name` (no rank check) — a user with `users.create` could mint an `admin`. | **FIXED.** Owner decision 2026-06-14: defer fix until running sessions finish, then a dedicated branch. Lane A commit `b4a603357` "fix(identity): gate role management & assignment (audit #1)" (2026-06-18) gated via `StoreRoleRequest`/`UpdateRoleRequest`/`AssignRoleRequest`; commit `d9d33649a` added the `AssignableRole` subset-of-own-permissions rule. Merged to `origin/dev` 2026-06-19 (`5bbf93f5f`). The residual DELETE `roles/{id}` hole was closed later — see 2026-07-04 row. |
| 2026-06-14 | same audit, finding **#4 MEDIUM** | `PaymentController::index` scoped only `tenant_id`, not `company_id` (unlike `show()`) — cross-company payment leak in multi-company tenants. | **FIXED** — commit `ce8132e8c`, merged with the Lane A batch 2026-06-19. |
| 2026-07-03 / 2026-07-05 | memory `project_spatie_permission_cache_tenant_blind.md` | Spatie's permission registry cache key (`spatie.permission.cache`) is global in Redis, not scoped per tenant DB and not per app instance, under database-per-tenant. Whichever tenant/app last rebuilt the cache "wins" for everyone sharing Redis — reproduced twice (RFQ 403s from a stale demo-app cache; `goods-receipt.edit-price` grant ignored because a second app kept rebuilding the shared cache). Flagged as launch-relevant correctness bug: tenant A's request can rebuild the registry from tenant A's DB and tenant B then evaluates against it. | **OPEN** as of 2026-09-09 — see §4 item D1 and §3 (Task T1 of request-hygiene Phase A). Mitigations shipped (deploy-time `permission:cache-reset` step folded into many deploy checklists; boot-time flush commit `9a56149cf feat(deploy): api entrypoint rolls pending tenant migrations + flushes spatie permission cache at boot`) but the underlying tenant-scoping fix was never merged — confirmed by `git log` (no `HANDBACK-request-hygiene-T1-2026-09-03.md` exists in `docs/handoff/`, no commit touching `TenancyServiceProvider`/`PermissionRegistrar` for this). |
| 2026-07-04 | memory `project_go_live_security_audit_2026_06_14.md` / ARCHIVE.md / `docs/PRODUCT-BIBLE.md` TD-015 | Re-verification of the go-live audit's role findings: store/update/assignRole/removeRole were already gated by the Lane-A fix; the residual hole was the **ungated DELETE `roles/{id}`**. | **FIXED** — commit `f5aabc56e` "fix(identity): require role-management authorization on RoleController mutations (closes go-live privesc)" (2026-07-04, verified via `git log`; ancestor of merge `4e8593179` which ARCHIVE.md cites). `DeleteRoleRequest` → `roles.manage`; deny-path tested; tenancy-authz-reviewer APPROVED with a negative control reproducing the cashier-delete on pre-fix code. Product Bible TD-015 = **Resolved (2 minor follow-ups)**: `RoleController::update` still lacks a subset check on written permissions (latent only if `roles.manage` is ever granted below admin); read endpoints (`index`/`show`) not gated on `roles.view`. Both follow-ups remain **OPEN** (not re-mentioned as closed anywhere in the corpus). |
| 2026-06-20 → 2026-08-08 | `docs/superpowers/specs/2026-06-24-tenant-impersonation-support-access-design.md`; memory `project_tenant_impersonation_support_access.md`; `docs/handoff/HANDBACK-tenant-impersonation-2026-08-07.md` | Consent-gated support impersonation design locked 2026-06-24 (post-launch). Implemented by Codex 2026-08-07, **FAILED final gate 3× REJECT** — 5 blockers: (B1) `*.manage` elevation pattern matched `support-access.manage`, letting an elevated session mint itself a 5-year window (PROVEN); (B2) elevated session could approve its own pending grant (PROVEN); (B3) fiscal writes (receipts/return, reports/z, audit-events/sync, shifts/close) were only `RequiresElevation`, not `HardBlocked`; (B4) denied attempts + lifecycle events never hit the hash chain; (B5) persistent banner missing from POS terminal + `/admin`. | **FIXED then MERGED.** Fix pass complete 2026-08-08, all 3 reviewers ACCEPT. `RolesAndPermissionsSeeder` adds `support-access.view`/`support-access.manage`; central roles `super_admin` (requests/operates support access) vs `support_approver` (four-eyes business-partner account). Merged local dev `c42a202ae` 2026-08-08. Promoted to `origin/dev` in the 2026-08-19 enforcement-layer batch (`c6d6308ae`, per ARCHIVE.md). **Usage-gate still OPEN:** must not be used on a real Tunisian tenant until Law 2004-63/INPDP validation (E-4) is recorded — see §2 ruling B3 (2026-09-05: owner ruled "allow, properly logged" — unblocks *usage policy*, but the E-4 legal-position confirmation itself is still listed as owed to TN legal in `docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md` item B3). Super-admin MFA lane also still owed (pre-production). |
| 2026-08-05 | commit `4324a637a` "reviews: [Session B2] B2-6 tenancy gate r1 APPROVED-w/-conditions" | POS held-order routes were reachable with no per-verb permission check; gating them per the reviewer's conditions caused **5 roles to lose held-order access** (absent permission rows → 403 for all, including admin, until seeded). | **FIXED** — commit `b91677396` "fix(sb2-b26/C-16iii): gate the POS held-order routes per verb with can: middleware"; deploy note (permission seeding) carried in the tenancy gate review. |
| 2026-08-09 | memory `project_country_defaults_super_admin.md` | `company.country_code` mutable via `settings.update`, `tax_configurations` company-unscoped ⇒ cross-company stamp-duty-capability escalation possible in multi-company tenants. Ruling: the stamp-duty capability check is a **usability guard, not a security boundary** — must not be cited as an authorization control until closed. | **OPEN** — ticket `docs/superpowers/tickets/2026-08-10-country-code-mutable-authz-authority.md` (owner ruling needed). |
| 2026-08-10 → 2026-09-09 | memory `project_country_defaults_super_admin.md` | Country-defaults super-admin panel: a scoped central-admin role `defaults_editor` (chartered-accountant-editable seeded country templates). Owner-locked: `defaults_editor` role scoped to template edits only; four-eyes deferred. | `defaults_editor` role **shipped, feature-flagged OFF** pending the central-admin MFA lane (owner ruling). Phase A (M0-M7) dispatched 2026-08-11, M1 passed; gaps C/G/H/I merged+promoted `7d85232cc`. |
| 2026-08-12 → 2026-08-21 | memory `project_enforcement_layer_2026_08_12.md` | Retrospective enforcement layer added new adversarial reviewers, incl. `tenancy-authz-reviewer` (gates every permissions/module-gating/multi-tenancy diff) and a mandatory Round-0 mechanical precheck ("grep-verified permission keys" is one of its checks before any spec/brief gate). | **SHIPPED** — landed + promoted `origin/dev` `c6d6308ae` 2026-08-21; all sessions since operate under these gates. |
| 2026-08-29 | commit `4645f72f1` "h1-cleanup G1.8: gate the partner create/list/detail routes on real permissions" | Session H (party/contact model) found partner routes gated only loosely; gated create/list/detail on real permissions as part of the merge-gate conditions. | **FIXED** — merged as part of `8d629ac89` "Session H Phase 1 M1–M3" merge to dev; tenancy-authz gate ACCEPT-WITH-CONDITIONS, conditions landed. |
| 2026-09-02 → 2026-09-09 | memory `project_request_hygiene_scale_audit_2026_09_02.md`; `docs/superpowers/audits/2026-09-02-request-hygiene/05-synthesis.md` finding **S-1** | Request-hygiene/scale audit re-confirmed the tenant-blind Spatie permission cache (same defect as the 2026-07-03 row) as finding S-1, Phase A Task 1. Plan gate confirmed Task 1 scoped to database-per-tenant mode only (compatibility mode intentionally keeps the shared key). | **STILL OPEN.** Dispatched to the owner via Codex Desktop 2026-09-03 (`docs/handoff/HANDOVER-request-hygiene-T1-permission-cache-2026-09-03.md`), worktree `.worktrees/rh-t1`. No `HANDBACK-request-hygiene-T1-2026-09-03.md` exists in the repo and no corresponding code commit was found in `git log`, unlike Tasks 2–14 of the same plan (all of which have handback files and were merged). Every later status update in the memory file (through 2026-09-09) still lists "T1 = owner Codex Desktop" as outstanding. |
| 2026-09-05 | `docs/superpowers/specs/2026-09-05-parapharmacy-readiness-remediation-design.md`; `docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md` | Batch-recall permission model was a single company-wide boolean action with **no permission check at all** (`BatchController:176-210`); the seeded `manager` role held `batches.recall` with no branch scoping. | **RULED, not yet shipped** — see §2 ruling RD2/Q4/Q5/Q10 and §3 (W-LOT-A-1/A-1a in flight). |
| 2026-09-05 | `docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md` §"What is actually in place for float and drops today" | Treasury has no consumer of shift float/cash-in/cash-out events; only the device + fiscal projection know them; variance-GL leg (`PostShiftCashVarianceAdjustment`) ships **disabled** for exactly this reason. Not a permissions bug per se, but the W-CASH lane it spawned (see below) also touches `treasury.manage_all_locations`, a permission scoped by this same program. | RULED **CRITICAL, into launch scope** (RD4) — W-CASH lane, separate from roles/permissions proper but the general_manager role (below) is the authority for `treasury.manage_all_locations`. |
| 2026-09-05 | Dhouha PR #210 gate r1, `docs/superpowers/reviews/2026-09-05-dhouha-pr-210-gate-r1.md`; commits `89d77eb31`, `ad0292ef8`, `4d7dccbd3`, `e55ddbb0a` | Supplier-invoice posting (create/match/post) had no dedicated permission; F-W2-14 gated it behind `supplier-invoices.manage` — the `operator` role held `documents.update` but not the new permission, so it silently lost an API-only capability (never exposed in the web UI to `operator` anyway). Gate r1 flagged this as CHANGES + owner ruling needed (owner row **A9**, see §2). | **FIXED + MERGED + PROMOTED.** PR #210 gates r1 CHANGES → r2/r3 MERGE-WITH-FIXES → r4 delta MERGE; merged to dev `70a4fdbf0` and promoted to `origin/dev` (tip `ad1d6ceb1`) on 2026-09-09. Deploy row: `tenants:seed RolesAndPermissionsSeeder` + `permission:cache-reset` (API env has `SYNC_PERMISSIONS_ON_BOOT=true`). |
| 2026-09-06 | memory `project_country_defaults_super_admin.md` / `project_parapharmacy_remediation_program_2026_09_05.md` | Ticket `docs/superpowers/tickets/2026-09-06-gl-and-repository-balance-columns-scale-2-precision-debt.md` — pre-existing precision debt on `payment_repositories.balance`/`journal_lines.debit/credit`; unrelated to permissions directly but blocks the W-CASH-1 slice that shares dispatch with W-LOT-A-1 (the permissions/general_manager slice). | Downgraded P1→P3 after correction (columns already `decimal(15,3)`, not `(15,2)`); still an open P3 ticket, referenced in MEMORY.md "Common Pitfalls" line. |
| 2026-09-06 → 2026-09-09 | `docs/handoff/CODEX-PROMPT-slice-plan-WLOTA-1-permissions-hold-2026-09-06.md` → `CODEX-PROMPT-WLOTA-1a-ruling-legacy-null-team-roles-2026-09-09.md` | New seeded role **`general_manager`** (manager permission set, no location restriction, `batches.recall` + `treasury.manage_all_locations`; seeded `manager` **loses** company-wide `batches.recall`); new permission `batches.recall.request` for branch-scoped recall holds; body-less BatchExpiry routes (recall/destroy/traceability) had **no** action-permission checks at all, reusing only module + company checks. | **IN PROGRESS**, not yet merged — see §3. |
| 2026-09-09 | `docs/handoff/CODEX-PROMPT-WLOTA-1a-ruling-legacy-null-team-roles-2026-09-09.md` | Owner-adjacent architectural ruling: legacy Spatie roles seeded via `Role::firstOrCreate` carry `tenant_id = NULL`; under Spatie teams a NULL-team role matches every lookup (`Role::findByParam`). Under database-per-tenant this is **not** an ownership ambiguity (every row already belongs to one tenant DB) — re-homing/scoping those rows to `tenant_id` would break console/queued-job contexts that run without `SetPermissionsTeam` (per CLAUDE.md rule 20). Decision: **NO ownership adoption** of legacy NULL-team roles; only the new `general_manager` row gets `tenant_id` set, marked with `provisioning_source = 'w-lot-a-1a'`. | RULED (architectural decision, not an owner business ruling) — binding for the in-flight W-LOT-A-1a slice; a side ticket was filed: `docs/superpowers/tickets/2026-09-09-role-controller-team-scope-annotations.md` — `RoleController`'s `CrossTenantRoute` annotations **overstate** an automatic team-global scope; not fixed in this lane. |

---

## 2. Owner rulings that constrain the roles/permissions design (verbatim)

**RD2 — general-manager escalation model** (`docs/handoff/OWNER-RULINGS-parapharmacy-remediation-2026-09-05.md`):
> "A manager is branch-linked, or is a **general manager** (explicit company-wide role). Recall is a safety action: a branch manager **initiates** a recall for a lot present in their branch; the recall **escalates** to the general manager, who executes it company-wide. Permissions stay tight; escalation, not denial."
> Consequence: "W-LOT L1 becomes a two-step recall: `batches.recall.request` (branch scope...) and `batches.recall` (company-wide execution, general-manager role). Seeded manager loses company-wide `batches.recall`; new general-manager role or explicit grant carries it."

**D5 — location-restricted treasury custody** (same doc):
> "Managers move **only their branch custody**. Only the **central authority** (an explicit company-wide manager role, not any manager) has all-treasury reach." Consequence: "`treasury.manage_all_locations` granted to admin and the general-manager role only."

**Q4 — general_manager role, confirmed** (same doc, second ruling pass):
> "CONFIRMED: new seeded `general_manager` role (manager set, no location restriction, `batches.recall`, `treasury.manage_all_locations`)."

**Q5 — recall hold, confirmed:**
> "CONFIRMED: branch request = immediate local hold on sale and transfer."

**Q10 — recall hold lifecycle** (owner, 2026-09-06 morning, same doc §"Q10–Q13 RULED"):
> "Hold lifecycle `requested → recalled` or `requested → released`; only the general manager may release or reject; mandatory reason and append-only evidence; the requesting branch never lifts its own hold."

**Q11 — shared drawer / one cash-bearing shift:**
> "One cash-bearing shift per drawer at a time; a second terminal joins the open drawer session without a second float, or is refused. The drawer session is the custody unit; terminal sessions attribute sales."

**Benchmark-derived default behind Q4/Q5 (Recall escalation and general-manager role)**, same doc §"Benchmarks for the open questions":
> "Two-step as ruled is **stricter than both benchmarks** [Odoo `quality_hold`/OCA `stock_lock_lot`, no approval chain; ERPNext Batch disabled + User Permission on Warehouse] and is defensible for pharmacy safety... Recommendation: **new seeded role `general_manager`** = manager permissions with no location restriction plus `batches.recall` and `treasury.manage_all_locations`; `admin` keeps both too."

**B3 — impersonation logging** (`docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md` §Owner follow-up):
> "Impersonation: **allow**, properly logged; needed for support." Note: "Ruled. Unblocks the merged impersonation feature; audit log already required by its spec." Separately, item B3 in the earlier table (§A/B) still lists: "**E-4 legal position** on support impersonation (blocks impersonation usage, merged since 2026-08-08)" as owed to **TN legal**, distinct from the "allow + log" business ruling.

**A9 — PR #210 operator-role ruling** (`docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md`):
> Question as posed: "PR #210 (F-W2-14, cashier supplier-invoice perms): may the `operator` role lose supplier-invoice create/match/post?" Recommendation adopted: "accept the secure default (operator loses it; nobody could reach it in the UI), grant `supplier-invoices.manage` to `manager`/`purchases`; then fix round for the remaining ⅔ of F-W2-14." (Owner ruling column recorded as accepted — PR #210 subsequently merged/promoted per §1.)

**Discount permissions** — no direct 2026 owner ruling text was found beyond the 2026-03-23 architecture note (`project_discount_permissions.md`); design remains: `users.can_discount` (bool, default false) + `pos_terminals.max_discount_percent` (ceiling) + optional `users.max_discount_percent`, `effective limit = min(terminal_limit, user_limit)`. No ruling supersedes this; item is carried as unfinished wiring (POS never calls `/pos/discount-permissions`), not as an open policy question.

**Cashier price override** — owner ruling to **defer** (2026-07-09), verbatim from memory `project_cashier_price_override_deferred.md`:
> Owner: "I want to understand a bit more from the tenant." Deferred pending tenant discovery; research (industry pattern: per-cashier capability flag + max-% ceiling, absolute floor binding even managers, manager-PIN escalation, Price Override Report) already done and captured for resumption.

**RolesAndPermissionsSeeder tech-debt / role-composition rulings elsewhere in git log** (not owner quotes but binding commit-level rulings referenced by commit message):
- `9012d804a fix(taxation): drop reports.manage from manager — gate finding I-1`
- `1f425f5f4 fix(accounting): split reports.view into reports.financial/reports.operational per W-6 D5 owner ruling`
- `1a8206d9e fix(taxation): seed taxation.tax_configurations.manage in per-tenant seeder (W-X)`
- `4bf1935e4 fix(treasury): grant payments.refund to manager+accountant (reverse stays admin-only by decision)`

---

## 3. In-flight lanes touching permissions right now (as of HEAD `bd986f898`, 2026-09-09)

**W-LOT-A-1a — lot permissions, general-manager role, policy-neutral hold** (`docs/handoff/CODEX-PROMPT-slice-plan-WLOTA-1-permissions-hold-2026-09-06.md`, and its successor plan `docs/superpowers/plans/2026-09-06-WLOTA-1-permissions-hold-rev-1.md`, currently at rev ≥8 per memory `project_dhouha_pr_gates_2026_09_08.md`/W-LOT session file):
- Adds action-permission checks to the body-less BatchExpiry routes (recall, destroy/deactivate, traceability, read), reusing seeded `batches.view`/`batches.traceability`/`batches.delete`/`batches.recall`.
- Web-layer gating: `Sidebar.tsx:222-223` currently gates batch nav on module only, not permission — will add `RequirePermission`/`usePermissions` gating and regenerate the frontend permission map.
- New seeded role **`general_manager`** = manager permission set + `batches.recall` + `treasury.manage_all_locations`, assigned only when `user_company_memberships.allowed_location_ids IS NULL`; seeded `manager` **loses** `batches.recall`. Delta migration must **preserve custom roles** (no blind `syncPermissions`); reseed + `permission:cache-reset` per tenant is a deploy step.
- New permission `batches.recall.request` (branch-scoped hold creation), granted to `manager`.
- 2026-09-09 architectural ruling: legacy NULL-team roles are **not** re-homed; only the new `general_manager` row gets `tenant_id` set (see §1 last row and §2's Q4 material). A ticket was filed on `RoleController`'s cross-tenant-route annotations overstating automatic team scoping.
- Status: gate loop still running (rounds r1–r8+ per the memory files); **not yet merged to dev**. Roadmap collision risk: anything else touching `RolesAndPermissionsSeeder`, the `manager`/`admin` permission sets, or `Sidebar.tsx` batch gating should sequence after this lane lands, since it changes `manager`'s permission set and adds a brand-new role.

**T-1 / T-2 / T-3 — transfers & blind-receiving edge campaign** (`docs/handoff/BRIEF-lane-T1-transfers-requests-edge-campaign-2026-09-08.md`, `docs/superpowers/specs/2026-09-08-transfer-destination-receipt-and-blind-receiving-design.md`, spec rev 11 **ACCEPTED** at gate r11 as of the commit at current HEAD):
- New permission `inventory.transfers.reconcile` — deliberately **not** folded into the broadly-seeded `inventory.view`, because `inventory.view` is seeded into most roles and reconciliation/expected-quantity visibility must be gated more narrowly to preserve "blind" receiving (D1 deviation from the original brief).
- Close authority ruled as **reconcile AND close permission** (both required) — this is a two-permission gate pattern, notable because it diverges from most existing single-permission gates in the codebase.
- Blind-receiver masking: current-stock/quantity leakage across `StockMovementController`, `EntryExitNoteController`, product/batch/counting-reconciliation/rebalance surfaces was ruled as accepted residual risk (owner-approved "Oracle approach" — mask only *transfer document* quantities, not general on-hand stock) rather than masking every stock-visibility surface.
- T-1 implementation lane (separate Codex thread) is mid fix-round-2 as of current HEAD's last commit (`bd986f898`, "lane T-1 gate r3 stock-gl-interaction — MERGE"); execution plan (rev 1) for the full T-2/T-3 spec has not yet been written.
- Roadmap collision risk: this lane's permission model (two-of-two gate on Close, new `inventory.transfers.reconcile` permission) should be checked against any concurrent transfers/inventory permission work before merging either.

**Dhouha PRs** (`docs/superpowers/reviews/2026-09-*-dhouha-pr-*`, memory `project_dhouha_pr_gates_2026_09_08.md`): the active backlog is drained through #221 as of 2026-09-08 afternoon; **#210** (the supplier-invoice `operator`-role permission change, §1/§2 A9) is the one PR in this stream that materially changed a role's permission set, and it is now merged+promoted. No further Dhouha PRs are flagged as touching roles/permissions as of the last update read.

**W-CASH-1** (`docs/handoff/DISPATCH-W-CASH-1-implementation-2026-09-06.md`): does not add roles/permissions directly, but its `treasury.manage_all_locations` grant target is defined by the same W-LOT-A-1a general_manager-role ruling — the two lanes must land in a coherent order (W-LOT-A-1a's role migration should not leave `treasury.manage_all_locations` ungranted for the interim).

**Request-hygiene Task 1 (S-1 permission-cache fix)**: dispatched, not executed (§1). If it resumes, it is independent of the W-LOT/T-1 lanes above (different subsystem — Spatie cache scoping vs. permission catalogue), so it should not collide, but it does share the deploy-step pattern (`permission:cache-reset`) that several of the in-flight lanes also rely on as a manual mitigation.

---

## 4. Known open defects (deduplicated), with original evidence path

1. **Spatie permission-registry cache is tenant-blind under database-per-tenant** (global Redis key `spatie.permission.cache`, `PermissionRegistrar::CacheManager::store()` bypasses Stancl tenant tagging). First found: memory `project_spatie_permission_cache_tenant_blind.md` (2026-07-03/05). Re-surfaced as audit finding S-1: `docs/superpowers/audits/2026-09-02-request-hygiene/05-synthesis.md`. Dispatch brief: `docs/handoff/HANDOVER-request-hygiene-T1-permission-cache-2026-09-03.md`. **Still open** as of 2026-09-09 (no handback, no fix commit found).

2. **Frontend `usePermissions.ts` uses a hardcoded `ROLE_PERMISSIONS` map, ignoring real Spatie grants for custom roles.** Evidence: memory `feedback_usePermissions_hardcoded_map.md`; documented (as current behavior, i.e. still unfixed) in `docs/conventions/03-AUTHORIZATION.md`; tracked as `docs/PRODUCT-BIBLE.md` TD-005 (Open, Medium priority).

3. **`RoleController::update` lacks a subset-of-own-permissions check on written permissions** (latent risk only if `roles.manage` is ever granted below `admin`) — `docs/PRODUCT-BIBLE.md` TD-015 follow-up, never closed by a later commit found in this audit.

4. **`RoleController` read endpoints (`index`/`show`) are not gated on `roles.view`.** Same TD-015 follow-up list, never closed.

5. **`RoleController`'s `CrossTenantRoute` annotations overstate an automatic Spatie team-global scope.** Filed as ticket `docs/superpowers/tickets/2026-09-09-role-controller-team-scope-annotations.md` during the W-LOT-A-1a ruling (2026-09-09); explicitly deferred out of that lane's scope.

6. **`company.country_code` is mutable via `settings.update`, and `tax_configurations` is company-unscoped**, enabling cross-company stamp-duty-capability escalation in multi-company tenants; the stamp-duty capability check must not be treated as a security boundary until this closes. Ticket: `docs/superpowers/tickets/2026-08-10-country-code-mutable-authz-authority.md` (owner ruling needed).

7. **BatchExpiry body-less routes (recall/destroy/deactivate/traceability) have no action-permission checks at all** (module + company checks only) — `apps/api/app/Modules/BatchExpiry/Presentation/routes.php:12-48` per `docs/handoff/CODEX-PROMPT-slice-plan-WLOTA-1-permissions-hold-2026-09-06.md`. Being fixed by the in-flight W-LOT-A-1a lane (§3) — **not yet merged**.

8. **Discount-permission wiring is incomplete on the POS device**: `fetchDiscountPermissions()` in `discountApi.ts` exists but is never called; `HomePage.tsx` reads `operator.can_discount` from the cached verify-pin response and ignores terminal-level `max_discount_percent` limits entirely. Evidence: memory `project_discount_permissions.md` (2026-03-23 architecture note; no later memory records this as fixed).

9. **Cashier price-override feature is deferred** pending tenant discovery (owner, 2026-07-09) — not a defect but an explicitly incomplete/paused capability whose permission model (manager-PIN escalation, per-cashier capability flag) is designed but unbuilt. Memory: `project_cashier_price_override_deferred.md`.

10. **Impersonation usage on a real Tunisian tenant remains legally blocked** pending TN legal's E-4 (Law 2004-63/INPDP) confirmation, despite the owner's "allow, properly logged" business ruling (B3) and the feature being merged since 2026-08-08. `docs/handoff/HANDBACK-tenant-impersonation-2026-08-07.md` states this is "an operational prohibition, not a soft recommendation." Tracked open in `docs/handoff/OWNER-QUESTIONS-consolidated-2026-09-06.md` item B3.

11. **Super-admin MFA lane is still owed pre-production** — referenced both in the tenant-impersonation handback and in `project_country_defaults_super_admin.md` as the gate blocking activation of the `defaults_editor` central-admin role.

12. **`docs/conventions/03-AUTHORIZATION.md` itself is stale** (Last Updated 2025-12-30): it documents the hardcoded `usePermissions.ts` `ROLE_PERMISSIONS` map (defect #2 above) as the *current* pattern, with no note that this is known-wrong, and does not mention `RequireModule`/module gating (a materially later, separate enforcement layer documented instead in `docs/architecture/vertical-module-gating.md`). This is a documentation/reality drift, not a code defect, but it means any reader of convention 03 alone will design new permission-gated frontend code against a known-broken pattern.

---

## 5. Glossary coverage for roles/permissions/users/memberships

`docs/glossary.md` (created 2026-08-29, Session I) defines, verbatim:

> **Membership** | A user's access to a company (roles are Spatie team-scoped by tenant). | `user_company_memberships` / `Identity` | Settings → Users | — |

That is the **only** row in the glossary that touches roles/permissions/users. The glossary's "Tenancy and organisation" section otherwise defines **Tenant**, **Company**, **Location**, but not **User**, **Role**, or **Permission** as first-class terms — no row defines what a "Role" is (Spatie team-scoped? seeded vs custom? relationship to `tenant_id`), what a "Permission" string convention is (`{resource}.{action}`, per `naming.md`, is documented only in a stale 202-day-old memory file, not the glossary), or what "User" means as distinct from "Membership" (a `User` can have memberships in multiple companies of a tenant — this relationship is not glossed). Given the glossary's own rule ("Adding a concept = adding a row here in the same lane," convention 11-ONE-SURFACE-PER-CONCEPT), **Role**, **Permission**, and **User** are conspicuously undefined terms despite being actively designed right now (the W-LOT-A-1a slice plan explicitly asks the Codex agent to "reconcile with `docs/glossary.md`" and propose new rows for "Recall request, Branch hold, General manager" — implying the glossary does not yet have a Roles/Permissions section for these to slot into).

**Terms proposed but not yet landed in the glossary** (per `docs/handoff/CODEX-PROMPT-slice-plan-WLOTA-1-permissions-hold-2026-09-06.md`, item "Convention-11 Vocabulary line"): *Recall request*, *Branch hold*, *General manager* — to be reconciled with `docs/glossary.md` as part of the in-flight W-LOT-A-1a lane (§3), not yet merged.

**Missing terms** (no glossary row found for any of): Role, Permission, User (as distinct from Membership), Super admin, Support approver, Impersonation grant/session, general_manager (new role, in flight), Operator role (PR #210 discussion refers to an "operator" role informally with no glossary definition of what "operator" means as a role vs. permission holder).
