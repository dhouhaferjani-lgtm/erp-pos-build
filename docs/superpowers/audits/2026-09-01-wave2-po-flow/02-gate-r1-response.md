# Wave-2 PO spec — gate r1 response (rev 1 → rev 2)

**Session:** L · **Date:** 2026-09-01 · **Base commit:** `62964e5cc`
**Reviews answered:** `docs/superpowers/reviews/2026-09-01-wave2-po-spec-gate-r1-stock-gl.md` (G1-S1-01..17 + missing M1–M9) and `…-gate-r1-costing.md` (G1-C1-01..24 + missing M1–M14).
**Documents revised:** [`01-research.md`](./01-research.md) and [`02-scenario-matrix.md`](./02-scenario-matrix.md), both now rev 2.

## Summary

| | r1 | r2 |
|---|---|---|
| Scenarios | 130 / 21 classes | **151 / 21 classes** |
| Findings | 28 | **36** (8 new; 2 re-graded; 3 dispositions corrected) |
| Owner questions | 12 | **10** (Q-7 withdrawn as a confirmed defect, Q-10 as blocked-on-evidence; Q-4/Q-5/Q-6 reframed) |
| Findings by severity | 1 P0 / 7 P1 / 12 P2 / 8 P3 | **2 P0 / 8 P1 / 16 P2 / 10 P3** |

**Every BLOCKER and MAJOR is FIXED.** One MINOR is **partially rebutted with a file:line** (G1-S1-16's renumbering of `CreateDocumentRequest.php`), and one reviewer *worry* (not a numbered finding) is rebutted with arithmetic (G1-C1-12's prediction that W2-WDIL-3 would trip over LAND-4). Nothing is "acknowledged" and nothing is deferred without a code-cited reason.

**Structural changes both reviewers required:**
1. **THE COSTING RULE** — every WAC-asserting class now owns a **virgin product** (`P-<CLASS>-n`, 30 SKUs seeded in W2-SETUP-3) and **no PO is reused across classes**. r1's figures were computed against virgin products while the run reused `P1`/`P4` across six classes serially; option (a) was chosen over restating costs as measured deltas precisely so **every figure both reviewers independently re-derived stays byte-identical**.
2. **Schema verification** — every table and column in every SQL statement was checked against `apps/api/database/migrations/tenant/**` and the model `$table`; the verification paragraph is in `01-research.md` §Corrections. This retires the `UNVERIFIED` marker on `01c §8` query 5.
3. **Three browser contexts** — admin (shared page) + cashier + `wave2-receiver`, resolving r1's direct self-contradiction between "one shared page" and "a second browser context for W2-PERM".
4. **Second-of-everything** — the blanket claim is withdrawn and replaced by a per-arm table of row ids, with the classes that carry **no** second arm and the code reason why.

---

## Stock↔GL lens — G1-S1-01..17

| Finding | Verdict | Where in r2 |
|---|---|---|
| **G1-S1-01** [BLOCKER] `check-cogs-coverage` cannot detect a missing GR-IR entry; "the only signal" is false | **FIXED** | `02` **W2-WDIL-4 rewritten** around a direct two-part orphan query (paid legs must have exactly one entry; free legs must have **none**, since `GeneralLedgerService.php:2069-2072` returns null at amount ≤ 0), with the reason the command cannot cover it spelled out (`CheckCogsCoverageCommand.php:169-179`, `:203`, `:431-473`). The sentence "this detector is the only signal" is deleted. **F-W2-03 re-graded P1 → P0** in `01` §5. Tolerances gain a line: the command may be run for its own D-f arm, but it is not the assertion. |
| **G1-S1-02** [BLOCKER] every batch SQL names non-existent tables | **FIXED** | Global rename to **`product_batches`** / **`inventory_batch_stock`** in `02` W2-HP-4, PART-4, LOT-2/4/6/7/12, WDIL-1, LOC-5. `01` §Corrections item 5 records the real schema, including that `inventory_batch_stock` has **no `company_id`**. The per-location invariant query (joining `product_batches` to scope by company) is written out under W2-LOC-5. |
| **G1-S1-03** [BLOCKER] W2-HP-5's query returns zero rows and passes vacuously | **FIXED** | `02` **W2-HP-5 gets its own query**, joined `journal_entries.source_id = goods_receipt_lines.movement_id` (the GR-IR source key is the **stock-movement id**, `GeneralLedgerService.php:2044-2050`), with `COUNT(*) = 3` asserted explicitly and "an EMPTY result FAILS the row" written into the SQL comment. |
| **G1-S1-04** [MAJOR] LAND-4's oracle is dead code; the post-`Received` arm is missing | **FIXED** | Oracle re-cited to `DocumentAdditionalCostController.php:41-73` (**no guard**) in `02` W2-LAND-4; **W2-LAND-10 added** (cost after `Received`: accepted, never reaches `landed_unit_cost`/WAC/408, still visible to the float preview). **F-W2-33** records `canModifyCosts()` as dead code so a fix lane does not "wire up the existing guard". Item 2 of the finding → **F-W2-35** (the controller sits outside the hexagon at `app/Http/Controllers/Api/`). |
| **G1-S1-05** [MAJOR] VAT-4's drift premise describes a replaced code path | **FIXED** | `02` **W2-VAT-4 rewritten as a positive assertion**: it posts deterministically and `Σ document_tax_details.tax_amount == documents.tax_amount == documents.line_tax_amount == 0.192`, with `Dr 4456 = 0.192`. The `0.191` bucket figure and the two-outcome hedge are withdrawn, and the reviewer's correction is carried in full (the engine would truncate to `0.190`, not `0.191`). **F-W2-20 downgraded P2 → P3 / not reproducible via HTTP**, with the reason (the snapshot is built from persisted line amounts) and the condition under which it would become live (a write path mutating `line_total` or `recoverable_tax_amount` after creation — none exists). |
| **G1-S1-06** [MAJOR] three column names do not exist | **FIXED** | `document_tax_details.tax_amount`/`tax_base` (never `.amount`), `repository_movements.payment_repository_id` (never `.repository_id`), `products.sale_price` (never `.selling_price`) — corrected in `02` W2-VAT-2, WDIL-2, PART-7, WDIL-5, HP-6 and in `01` §1 row 12, §5 F-W2-25, §6.6. All three re-verified in the migrations, plus the whole schema-verification paragraph. |
| **G1-S1-07** [MAJOR] REV-6 asserts stale `landed_unit_cost`; PATCH nulls it | **FIXED** | `02` **W2-REV-6 reshaped**: assert `landed_unit_cost IS NULL` on every recreated Unit-mode line (`:611` takes `?? null`; `normalizePurchaseLine:125-127` only populates it in Total mode or with a free quantity), and assert the genuinely stale artefacts — `document_tax_details` still carrying the **pre-patch** bases, and `documents.payload.costs_allocated_*`. **F-W2-06** rewritten the same way. |
| **G1-S1-08** [MAJOR] second-of-everything is decorative | **FIXED** | Blanket claim withdrawn from both documents. `01` §3 now carries a **per-arm table of real row ids** plus the classes with no second arm and the code reason. The three named gaps are added: **W2-LAND-9** (second-company landed cost resolving c2's own accounts), **W2-LOC-5 + W2-WDIL-1(b)** (per-location batch invariant), **W2-IDEM-7** (supplier-invoice create re-run). Plus **W2-SEC-7** (second-company WAC isolation) from the costing lens. |
| **G1-S1-09** [MAJOR] F-W2-01 (P0) has no operator-reachable probe | **FIXED** | **W2-IDEM-2b added** — UI double-submit / in-flight retry of the receive dialog, with the explicit instruction that if the dialog's disabled-while-submitting state makes it unreachable, the evidence must say so and **F-W2-01 is re-graded to P1 with that reason**. The P0 no longer stands on an unproven premise. |
| **G1-S1-10** [MAJOR] negative receive quantity passes validation, silently swallowed | **FIXED** | **W2-OVER-5 added** (all-negative → misleading `No items to receive`; mixed payload → the negative line silently produces no movement and no stock change). **F-W2-29** added (P2), citing the regex divergence from the sibling `received_unit_prices.*` three lines away and from `CreateDocumentRequest.php:120`'s `gt:0`. Also noted on baseline row B21. |
| **G1-S1-11** [MAJOR] DISC-2/3 cite a docblock | **FIXED** | Re-cited to `PurchaseOrderController.php:465-466` (`store`) and `:607-608` (`update`) in `02` W2-DISC-2/3 and in `01` §5 F-W2-08 — both verified in this worktree. The reviewer's supplementary point is adopted: a note under W2-DISC records that `DiscountPolicyDocumentValidator` does **not** apply to purchase orders (`:23-28`, `:37-39`), so there is no phantom 422 to chase. |
| **G1-S1-12** [MAJOR] cashier fixture under-specified | **FIXED** | `02` W2-SETUP-6 and `01` §6.5 now state: the membership is **single-company** (`UserController.php:241-243`) so **every W2-PERM row runs in company 1** — a c2 403 would be a membership refusal and would be misattributed; and the mechanism is a **second `browser.newContext()`** with its own storage state, the admin context untouched. The run plan's "three browser contexts" paragraph resolves the r1 contradiction explicitly. |
| **G1-S1-13** [MINOR] VAT-2's B13 is already answered; `tax_code` is wrong | **FIXED** | **B13 re-decided PARTIAL/DEFER → ALREADY** in `01` §3, with the mechanism (empty `$applicableTaxes` for a `NonFiscal` document ⇒ the UNCONFIGURED branch, whose own comment carries the V3 ruling). `02` **W2-VAT-2 becomes a positive assertion**: the 0 % row exists at `tax_base 20.000` / `tax_amount 0.000`, and every row carries **`tax_code = 'UNCONFIGURED'`, `tax_name = 'VAT 19%'`** — a query keyed on the seeded `TVA_19` returns nothing. Also added to the fixture notes (`01` §6.3). |
| **G1-S1-14** [MINOR] LOT-8 hedges on a determinable status | **FIXED** | `02` W2-LOT-8 now asserts **422 `GOODS_RECEIPT_FAILED`** with the exact message, since `App\Shared\Domain\Exceptions\MissingVariantException extends \DomainException` (verified) and the 500 arm is the `\RuntimeException` catch. **F-W2-12**'s disposition corrected; `01` §Corrections item 8 records the class and message. |
| **G1-S1-15** [MINOR] SEC-5 will read NULL | **FIXED** | `02` **W2-SEC-5 restated**: assert `payments.location_id IS NULL` and record that no location comparison exists — because `CreateSupplierInvoiceService::create` never writes `location_id` on the invoice (`:118-146`) and the payment inherits from the first allocated document. **F-W2-22** rewritten to the same shape. |
| **G1-S1-16** [MINOR] citation drift | **FIXED in part · REBUTTED in part** | **FIXED:** `PurchaseOrderController.php:414 → :431` (currency) and `:411-414 → :415-418` (location resolve) — both confirmed by reading. **REBUTTED with file:line:** the proposed renumbering of `CreateDocumentRequest.php` is off by one. `sed -n '118,126p'` gives `:119 = lines.*.description`, **`:120 = lines.*.quantity`**, `:121-123 = free_quantity`, **`:124 = unit_price`**, **`:125 = lines.*.line_total`**, `:126 = price_entry_mode`. r1's claim that quantity is `:119` and that `:125` is `price_entry_mode` is wrong, and its objection to W2-TOT-5 (which cites `:125` as the 3-dp `line_total` ceiling) therefore dissolves. r1's citations are kept unchanged in W2-TOT-5 and W2-EDGE-3, both now carrying the verification note. Recorded in `01` §Corrections item 10. |
| **G1-S1-17** [MINOR] the movement→GRN link is never asserted | **FIXED** | Added to `02` **W2-WDIL-1(c)**: every posted receipt line with `received_qty > 0` has a non-null, tenant-distinct `movement_id` resolving to a `movement_type='receipt'` row — the only link, since `stock_movements` carries no `goods_receipt_id`. **W2-LOC-6** adds the per-movement `avg_cost_before`/`avg_cost_after` audit (also stock-GL M8). |

### Stock↔GL missing scenarios M1–M9

| # | Landed as | Note |
|---|---|---|
| M1 orphan GR-IR query | **W2-WDIL-4** | both parts (paid must have one, free must have none) |
| M2 landed cost after `Received` | **W2-LAND-10** | + F-W2-33 |
| M3 UI double-submit receive | **W2-IDEM-2b** | with the re-grade instruction |
| M4 negative receive quantity | **W2-OVER-5** | + F-W2-29 |
| M5 per-location batch invariant | **W2-LOC-5** + **W2-WDIL-1(b)** | query written out |
| M6 second-company landed cost | **W2-LAND-9** | asserts c2's own account ids (the C-27 shape) |
| M7 re-POST `POST /supplier-invoices` | **W2-IDEM-7** | two `SI-` numbers burned |
| M8 `avg_cost_before/after` per movement | **W2-LOC-6** | |
| M9 additional cost on a non-PO document | **W2-EDGE-12** | **the reviewer could not confirm the type filter; I did — `resolveDocument` (`:166-181`) scopes by tenant + company + id and applies NO type filter.** New finding **F-W2-31**. |

---

## Inventory / WAC / batch lens — G1-C1-01..24

| Finding | Verdict | Where in r2 |
|---|---|---|
| **G1-C1-01** [BLOCKER] running company-wide WAC × serial single-tenant run ⇒ every cost figure wrong | **FIXED — option (a)** | `02` **THE COSTING RULE** section + a 30-SKU `P-<CLASS>-n` fixture table, seeded in W2-SETUP-3, so every class asserts against a virgin `cost_price`. The standing line the reviewer asked for is quoted verbatim as a block quote. `01` §6.2 carries the rule with the three code citations (`:100-128` denominator, `:241-253` blend, `:291` write-back). **Consequence: every figure the reviewers re-derived is unchanged** — `11.900000`, `10.500000`, `11.000000`, `12.000000`, `11.500000`, `8.333333` all stand. |
| **G1-C1-02** [BLOCKER] W2-LOC-1..3 cannot run — PO-C is already fully received | **FIXED** | `02` **W2-LOC gets its own `PO-L`** (`P-LOC-1 × 10.0000 @ 10.500`, untouched by any other class) and W2-LOC-5 gets `PO-L5`. The class header states why. The no-PO-reuse rule is generalised in THE COSTING RULE and enumerated in `01` §6.2. |
| **G1-C1-03** [BLOCKER] LAND-4 asserts a guard with no callers; Q-5 is framed on it | **FIXED** | `02` W2-LAND-4's expectation now reads "**because nothing checks**", oracle `DocumentAdditionalCostController.php:41-73`; **Q-5 reframed** ("there is NO guard, not a permissive one") in both documents; **F-W2-33** records the dead code. The row's numbers were confirmed correct by the reviewer and are unchanged. |
| **G1-C1-04** [BLOCKER] batch table names | **FIXED** | Same fix as G1-S1-02. The reviewer's extra detail is adopted: `product_batches (company_id, product_id, batch_number) WHERE variant_id IS NULL` is company-scoped, i.e. convention-09 clean — stated in `01` §6.6. |
| **G1-C1-05** [MAJOR] PRICE-4 has no producible actor | **FIXED** | **W2-SETUP-7 added** — `POST /roles` (verified: creates on guard `sanctum` and syncs permissions, `RoleController::store:187-198`) mints a `wave2-receiver` role holding `purchase-orders.receive` **without** `goods-receipt.edit-price`, assigned to a third user in a third context. W2-PRICE-4 points at it and asserts the exact envelope (422 `GOODS_RECEIPT_POST_FAILED` / `User is not allowed to apply goods receipt price overrides.`). |
| **G1-C1-06** [MAJOR] LAND-5's "reverse a cost" is not producible | **FIXED** | Split into **W2-LAND-5a** (producible: preview vs persisted after a second plain cost — the float + rounding divergence) and **W2-LAND-5b** (code truth, explicitly **not executable**: `reversed_at` / non-`LandedCost` `application_path` are written only by the Expense module, `ExpenseService.php:987` / `:176`, and there is no reverse route on `document_additional_costs`). F-W2-16 updated with both halves. |
| **G1-C1-07** [MAJOR] LAND-6's NON_REGISTERED company is not produced | **FIXED** | **W2-SETUP-8 added** — `PATCH /companies/{c2} {tax_status:'non_registered'}`, with the ordering constraint ("before any company-2 document is posted, i.e. before W2-SEC-2") stated in the row, in the run plan and in `01` §6.5. Creation's hardcoded `REGISTERED` (`CompanyController.php:107`) is recorded in the day-one table. |
| **G1-C1-08** [MAJOR] FEFO is never exercised; LOT-6's oracle is a docblock | **FIXED** | Oracle repointed to **`FEFOInventoryService.php:105-113`, `:270`** (exclusion by `expiry_date >= startOfDay`, not `is_expired`) in W2-LOT-6 and F-W2-09; **W2-LOT-9** (FEFO consumption across `LOT-T1`/`LOT-T2`, earlier expiry drained first, per-lot allocation) and **W2-LOT-10** (over-consume ⇒ refusal, no negative row) added on the fixture W2-PART-4 already builds. **The consumption driver was located and cited**: `POST /delivery-notes` → `POST /delivery-notes/{id}/confirm` → `DeliveryNoteService::issueStock:297` → `consumeBatchesAtomically:389` (routes `Document/Presentation/routes.php:330-336`); `01` §6.7 explains why it is the only Playwright-drivable option (POS is device-event driven; `POST /batches/{uuid}/write-off` targets one named lot and proves nothing about selection) and **bounds the scope**: only the lot ledger and the aggregate are asserted; the delivery note's COGS/GL/lifecycle are recorded, not asserted. |
| **G1-C1-09** [MAJOR] a null-expiry lot cannot be received | **FIXED** | **W2-LOT-11 added** (omitted and explicit-null `expiry_date`, both 422). **F-W2-32** added (P2) citing `ReceiveGoodsRequest.php:33` against `2026_08_26_100000_make_batch_expiry_date_nullable.php:34`, `BatchStockService.php:345` and `FEFOInventoryService.php:105-113`. **Baseline row B24 re-decided PARTIAL → MISSING** and its guarantee text extended to "and a genuinely unknown expiry is representable". |
| **G1-C1-10** [MAJOR] no second-location arm for lots | **FIXED** | **W2-LOC-5 added** — the same `batch_number` received into two locations: one `product_batches` row, two `inventory_batch_stock` rows, two `stock_levels` rows, Σ equal per location. |
| **G1-C1-11** [MAJOR] no second-company WAC isolation | **FIXED** | **W2-SEC-7 added** — `P-SEC-7` under the **same SKU** in both companies; c1's `cost_price`/`Σ stock_levels` captured before and asserted byte-identical after a c2 receipt at a different price; c2 blends only against c2 stock. |
| **G1-C1-12** [MAJOR] LAND-4 stops at the receipt; GR-IR figures missing | **FIXED (with one sub-claim rebutted)** | **W2-LAND-7 added** — the supplier invoice on `PO-E`, with the full derivation: accruedHt `115.000`, billedHt `100.000`, Cr 7585 `15.000`, Dr 408 `115.000`, Dr 4456 `19.000`, Cr 401 `119.000`, balanced at `134.000`. GR-IR figures `50.000` / `65.000` / Σ `115.000` added to W2-LAND-4's expected column, and W2-WDIL-3 cross-references it. **Rebutted sub-claim:** the reviewer predicted W2-WDIL-3 "will trip over" the `115.000` vs `100.000` gap. It will not — the receipts credit 408 by `50.000 + 65.000 = 115.000` and the invoice debits exactly `115.000`, so **408 nets to zero**; the lost `15.000` of freight surfaces as inventory under-valuation, not as a 408 residue. W2-WDIL-3 now says so for both `PO-D` and `PO-E`. |
| **G1-C1-13** [MAJOR] the price-override × landed-cost branch is untested | **FIXED** | **W2-LAND-8 added** on `PO-F`, with the derivation independently recomputed and matching the reviewer's: allocator pool `30.000000` weighted by the override, `baseUnitCost = 11.000000`, receipt `landed_unit_cost = 14.000000`, `cost_price = 14.000000`, GR-IR `140.000`, `price_override_old_basis = 13.000000` (the **landed** cost, not `unit_price`). |
| **G1-C1-14** [MAJOR] the `S1` fixture is under-specified; one shape creates an orphan receipt line | **FIXED** | The fixture table pins **both** shapes: `S1a` (a `Service`, line carries `service_id`) for W2-UNDER-3, and **`P-UNDER-4`** (a **non-physical `Product`**) for the new **W2-UNDER-4** — `receiveAll` on a `P-OVER-1 + P-UNDER-4` PO, asserting a `goods_receipt_lines` row with `movement_id IS NULL` and whether it is visible to `receipt-lines?uninvoiced=1`. **F-W2-34** added (P2): `createDraft:153-155` lacks the `isPhysical()` filter that `processReceiptLines:503-505`/`:519-521` has. |
| **G1-C1-15** [MAJOR] a 101–255 char `batch_number` produces a 500 | **FIXED** | **W2-EDGE-11 added** (150-char batch number; expected fix 422, the 500 recorded as the finding). **F-W2-30** added (P2). The Tolerances section gains the general note that `PurchaseOrderController.php:857-865` converts **every** `QueryException` in the receive path into a 500, and W2-EDGE-11 joins W2-EDGE-1 as a row permitted to *record* a 5xx while still failing its leg. |
| **G1-C1-16** [MINOR] `allocated_costs` is currency scale, not 6 dp | **FIXED** | `02` W2-LAND-1 now reads **`allocated_costs = 20.000 / 10.000`** with the reason (`allocatePositiveShares` returns at `$this->scale()`, assigned straight through at `:264`) and the pin (`LandedCostBcmathTest.php:298-300`); `landed_unit_cost` stays at 6 dp. W2-HP-2's `0.000` was already right and is unchanged. |
| **G1-C1-17** [MINOR] LOT-8 hedges | **FIXED** | Same as G1-S1-14. |
| **G1-C1-18** [MINOR] PRICE-3 payloads need `quantities` | **FIXED** | `02` W2-PRICE-3 states that each payload carries a valid `quantities` entry for the same line (`required_with:received_unit_prices`, `:27`) and labels which 422 is a **validation** failure (`-1.000`, `11.0001`) versus a **service** failure (`'0'`). |
| **G1-C1-19** [MINOR] negative quantities | **FIXED** | Same as G1-S1-10 → W2-OVER-5, both arms (all-negative and mixed). |
| **G1-C1-20** [MINOR] a draft can persist an invalid destination | **FIXED** | `02` **W2-LOC-4 gains arm (c)** — `save_as_draft: true` with a foreign-company `location_id` is persisted verbatim (`createDraft:129` never resolves) and only `post()` refuses (`:222`, `:984-1008`), pairing with W2-DRAFT-4's uncorrectable-draft finding. |
| **G1-C1-21** [MINOR] no quantity-display-precision assertion | **FIXED** | Added to `02` **W2-HP-3** and **W2-LOT-1**: the prefilled remainder must render as `6`, not `6.0000`, for the 0-dp `pc` unit (house rule 19 display clause). |
| **G1-C1-22** [MINOR] HP-4's WAC oracle range is imprecise | **FIXED** | `02` W2-HP-4 now cites `:100-128` (denominator), `:241-253` (blend), `:263-284` (movement), `:291` (cost write). |
| **G1-C1-23** [MINOR] receipts carry no `MovementReason` | **FIXED** | Stated in `02` W2-WDIL-4 (part of why `check-cogs-coverage` cannot see them) and in `01` §6.6: **goods-receipt movements are identified by `movement_type = 'receipt'`, never by `reason`** — `recordPurchase` writes no `reason`, unlike `recordSale:477` / `recordReturn:630`. |
| **G1-C1-24** [MINOR] TOT-2 is internally inconsistent about `unit_price` | **FIXED** | `02` W2-TOT-2 now states both are true and not in conflict (the editor displays a net-derived unit while the payload ships `line_total`), and that the **costing consequence flows from `line_total` alone** — `cost_price = 11.900000` follows from `line_total = 119.000` regardless of what `unit_price` holds. |

### Costing missing scenarios M1–M14

| # | Landed as | # | Landed as |
|---|---|---|---|
| M1 FEFO consumption | **W2-LOT-9** | M8 `receiveAll` on a non-physical Product line | **W2-UNDER-4** |
| M2 over-consume a lot set | **W2-LOT-10** | M9 101–255 char `batch_number` | **W2-EDGE-11** |
| M3 null / omitted `expiry_date` | **W2-LOT-11** | M10 `save_as_draft` with a foreign location | **W2-LOC-4 arm (c)** |
| M4 same lot at two locations | **W2-LOC-5** | M11 negative `quantities` entry | **W2-OVER-5** |
| M5 second-company WAC isolation | **W2-SEC-7** | M12 `batches` keyed by a foreign line id | **W2-LOT-12** |
| M6 price override + additional cost | **W2-LAND-8** | M13 `manufacturing_date` later than `expiry_date` | **W2-LOT-13** |
| M7 supplier invoice on `PO-E` | **W2-LAND-7** | M14 quantity display precision | **W2-HP-3 / W2-LOT-1 arms** |

---

## Findings register — r2 delta

**Re-graded:** `F-W2-03` P1 → **P0** (G1-S1-01: no detector exists) · `F-W2-20` P2 → **P3 / not reproducible via HTTP** (G1-S1-05).
**Dispositions corrected:** `F-W2-12` (deterministic 422, never a 500) · `F-W2-22` (`payments.location_id` is NULL, not a mismatched location) · `F-W2-06` (the stale artefact is `document_tax_details`; `landed_unit_cost` comes back NULL).
**New:** `F-W2-29` negative receive quantity silently swallowed (P2) · `F-W2-30` over-long `batch_number` → 500 (P2) · `F-W2-31` additional cost attachable to any document type (P2) · `F-W2-32` null expiry unreceivable despite a nullable-by-design column (P2) · `F-W2-33` `canModifyCosts()` dead code / no lifecycle guard (P3) · `F-W2-34` orphan `goods_receipt_lines` row from the missing `isPhysical()` filter in `createDraft` (P2) · `F-W2-35` `DocumentAdditionalCostController` outside the hexagon (P3) · `F-W2-36` costing services use a no-argument `getScale()` — known non-coverage (P3).

**Baseline decisions changed:** **B13** PARTIAL/DEFER → **ALREADY** (G1-S1-13) · **B24** PARTIAL → **MISSING** with the guarantee text extended (G1-C1-09) · **B21** gains the negative-quantity note · **B31** gains the "no guard exists" fact and its reframed ruling · **B48** marked a confirmed defect rather than an owner ruling.

## Owner questions — r2 delta

| Change | Reason |
|---|---|
| **Q-7 withdrawn** | Not a ruling — F-W2-14 is a confirmed defect with citations (`Procurement/routes.php:99-120`, `Document/routes.php:84-87`, `RolesAndPermissionsSeeder.php:121,671,682`). The only owner input is approving the replacement permission names at lane `L-14`'s gate. Both reviewers said so. |
| **Q-10 withdrawn** | Blocked on evidence, not on the owner — W2-EDGE-8 must measure what `POST /payments/{id}/refund` writes first. Listed under "not owner questions" so it is not silently dropped. |
| **Q-4 narrowed** | The hard-qty / advisory-price split is already a deliberate, commented design and is no longer re-asked. The ruling is only the `2.00 %` **AND** `1.000` TND pair, and the question now states the consequence: the TND cap dominates every line above 50 TND, making the percentage effectively decorative. |
| **Q-5 reframed** | On the correct premise: there is **no** guard (`canModifyCosts()` is dead), not a permissive one — which changes the answer space from a one-line fix to a lane. |
| **Q-6 reframed** | Must cover **both** halves — the optional `expense_document_id` **and** the absent lifecycle guard — or a fix lane closes half the hole. |

**Remaining: 10 questions** — Q-1, Q-2, Q-3, Q-4, Q-5, Q-6, Q-8, Q-9, Q-11, Q-12.

## Lane collision ruled (both reviewers' "double-fix check")

`F-W2-08`'s lane `L-4` and K-1 task 4.2 would both edit `PurchaseOrderController::normalizePurchaseLine`'s Total branch (`:100-105`). **Ruling, recorded in `01` §4 and in the run plan: K-1 task 4.2 owns that hunk exclusively.** `L-4` is strictly downstream of K-1's merge and adds only the regression test that a Total-mode discount is honoured; it touches no line inside `:100-127`. If K-1's fix makes the discount case correct on its own, `L-4` reduces to the test and closes.
