# IMP-1 follow-ups

- **G-8 CSV convention hookup:** `ImportRowExportService` emits UTF-8 BOM, comma, CRLF and unchanged stored strings. When the company convention writer lands, inject it here; retain the source-cell and SQLite/PostgreSQL parity tests. Do not reformat numeric values.
- **Supplier finalization currency context — FIXED on dev by `477c877a3`** ("Phase 0.1.6: Fix queued supplier balance currency scaling", cherry-picked into this lane). `ArApOpeningService` now resolves the money scale from the entity currency (`moneyScale(string $currency)`) instead of a bare no-arg `getScale()`, so `imp1-suppliers-balances-200.xlsx` finalizes cleanly (200 imported / 0 failed, `completed`, `error_message = NULL`). The lane's browser scenario was re-pointed to that outcome in fix round 1. Baseline evidence `docs/superpowers/reviews/2026-09-10-imp1-evidence/supplier-finalization.json` is **historical** — it records the pre-fix failure and must not be read as current behaviour (see `supplier-finalization.SUPERSEDED.md` beside it).
- **All-invalid history state:** upload returns 201 with status validated and all rows rejected; execute is disabled/refused. Decide whether validation with zero usable rows should be terminal failed, including retry semantics. The missing-header case is already failed/422. Not changed by IMP-1.
- **Optional full report All rows sheet:** the separate Imported / Skipped / Rejected workbook stays as-is. Add a fourth sheet only after a separate product decision.
- **Staging confirmation:** the local failure is reproduced, but the specific PharmaBio staging job has not been queried. The linked runbook contains no verified SSH/database access recipe. An operator with the staging access recipe should compare that job's `error_message` and `error_code`; never paste credentials into the evidence.
- **Historical source spelling:** older wizard jobs may have lowercase source names persisted by the former parser. New uploads preserve source spelling and order. Once the original file is purged, original capitalization cannot be recovered from the old mapping alone.

- **Existing Arabic history translation gaps:** history title, column headings and older status labels fall back to English; translate that pre-existing surface separately. New correction controls and partial status are translated in this lane.

- **`import.warnings.*` catalogue (opened by fix round 1, m2):** `ImportRowExportService::diagnostic()` now resolves a warning row's `_message` through `__('import.warnings.<code>')` with the stored per-row `detail` as the fallback, and no longer emits the magic string `'internal_error'`. No translation keys are seeded yet, so every code still falls back to the detail — which is deliberate, because today's stored details are row-specific ("provided 12.00 vs derived 11.90") and strictly more useful in a correction file than generic per-code copy. Seed `lang/{en,fr,ar}/import.php` `warnings` only with copy that carries at least as much row context, one code at a time.
- **Deferred gate r1 minors (not taken in fix round 1):** m3 (no test pins the coded `no_rows_to_fix` 404, the `opening_locked` selection arm, `reimport_type_mismatch`, or the entitlement re-check on the referenced job), m5 (auto-derived lowercase mapping can now 422 `mapping_not_injective` on a file with `Prix`/`PRIX` that previously imported last-wins), m6 (six untranslated AR status labels — already covered by the Arabic bullet below), m7 (`failed_rows_csv_url` is now unconditionally non-null even when the endpoint 404s, and carries no `/api/v1` prefix), m8 (export cells echo the *normalized* stored string, not the operator's original spelling; no FR-locale round-trip case). Register: `docs/superpowers/reviews/2026-09-12-imp1-impl-gate-r1-imports.md`.
- **Third raw-`error_message` surface (found while fixing gate r1 M2, deliberately out of scope):** `apps/web/src/components/organisms/GlobalImportProgress/GlobalImportProgress.tsx:103-105` renders the WebSocket `progress.errorMessage` verbatim, the same defect M2 closed on `ImportHistoryPage` / `ImportWizardPage`. Fixing it properly means carrying `error_code` on the `ImportCompleted` broadcast payload and through `importProgressStore`, then reusing `importJobErrorMessage()` — a change to the broadcast contract, so it needs its own lane. The same gap makes the wizard's realtime merge fall back to the generic sentence while a job is finishing, because the merged object keeps the API's (possibly still null) `error_code`.
- **Job-level copy reuses the row-scoped `errors.*` catalogue:** `importJobErrorMessage()` renders `errors.<code>`, whose copy is written for a row ("The row did not pass validation."). On a job-level failure — e.g. the missing-required-columns upload refusal, which stamps `validation_failed` — that reads oddly. Either add a job-scoped variant block or re-word the shared sentences to be scope-neutral; a wording decision, not a safety one (no raw text reaches the screen either way).

## Opened / closed by fix round 2 (gate r2 imports + gate r1 frontend)

**Closed in fix round 2** — the two bullets above about the third raw-`error_message` surface and about job-level copy reusing the row-scoped catalogue are now **done**, with one residual each, recorded below.

- **`ImportCompleted` still broadcasts no `error_code` (residual of the third-surface bullet).** `GlobalImportProgress` no longer renders raw text: it resolves both feeders through `importJobErrorMessage()`, so the WebSocket feeder — which carries only `error_message` — lands on the generic `errors.unknown` sentence. Carrying `error_code` on the broadcast payload (`ImportCompleted::toArray()`, `useImportProgress`, the store's `completeImport` input) would let the toast name the actual reason for an async job. It is a pure addition to a broadcast contract, so it belongs in its own lane (rule 8: the event class must not be restructured). The wizard's own API feeder already forwards `error_code`.
- **`ImportErrorCode::CompanyContextMissing` has no per-code operator remedy beyond one sentence.** The code now exists and is stamped; `errors.company_context_missing` says to re-upload. If the ProductImages re-upload flow ever changes, that sentence is the only place it is stated.
- **The missing-artefact 404 guard has no automated test.** `ImportController::downloadFailedRows` refuses a vanished or zero-byte artefact with `404 correction_export_unavailable` instead of streaming an empty 200. Forcing that condition in a test needs a seam: `ImportRowExportService` is `final` with no interface, so neither Mockery nor a subclass can make `generate()` return a path that is then removed. Either introduce a narrow port for the export service or drive the disk through a fake adapter, then pin the refusal.
- **m5 (frontend gate r1) — no `ImportJobData` DTO.** `apps/web/src/features/import/types.ts` keeps a ~25-field hand-rolled `ImportJob` beside a payload assembled as an inline array in `ImportController::formatJob()`. No generated DTO is shadowed today (convention 11 is not violated), but the two drift silently and `extends Partial<ImportCorrectionData>` makes `column_mapping` optional where the backend always emits it. Introduce `ImportJobData`, run `php artisan typescript:transform`, and re-export it. Explicitly out of scope for fix round 2.
- **m-B (imports gate r2) — the sync finalize arm has no test.** `ImportService.php:616-627` reports the exception before swallowing it and preserves a `CodedImportRowException`'s own code. Nothing pins either. One feature test on the sync path (force `finalizeImport()` to throw the opening-locked coded exception, assert `import_jobs.error_code` is that code, status `partially_completed`, counters intact, and that the exception was reported).
- **m-C (imports gate r2) — the SQL half of `ImportJobOutcome` is pinned in one quadrant.** `ImportRowExportTest` now covers stored-`failed`+committed+message (answers `?status=partially_completed`, not `?status=failed`). The other two quadrants — stored `completed` with `failed_rows > 0` must NOT answer `?status=completed`, and a clean `failed` must NOT answer `?status=partially_completed` — are covered only by the Playwright lane, which does not run in CI.
- **m-D (imports gate r2) — pre-fix browser evidence is unannotated.** `docs/superpowers/reviews/2026-09-10-imp1-evidence/phase-b/browser-green.txt` and the phase-a suppliers `-history.txt` / `-completion.png` are pre-fix artefacts sitting beside refreshed ones; only `supplier-finalization.json` has a SUPERSEDED sibling.
- **m-E (imports gate r2) — `error_message` is still disclosed to the client.** `formatJob()` and the `errors` / `error-summary` payloads publish the raw text (exception class names, absolute paths, full SQLSTATE including key values) to every `imports.manage` operator. No surface renders it and `docs/modules/imports.md` now says "disclosed but never rendered", but a DevTools tab still shows it. Decide between gating it on a support role, truncating it, or accepting the disclosure.
- **m-F (imports gate r2) — the wizard's converted panels have no Vitest case.** Fix round 2 added the SQLSTATE pin for `ValidationResults` and for `GlobalImportProgress`; the wizard's own execute/completion alerts (`ImportWizardPage.tsx`) are still covered only indirectly. `ImportWizardPage.options.test.tsx` even fixtures `error_message: 'Import failed'` with `error_code: null` and asserts nothing about what is rendered.
