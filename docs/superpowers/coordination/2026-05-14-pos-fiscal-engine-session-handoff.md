# POS Fiscal Event Engine — Session Handoff / Continuation Anchor

**Created:** 2026-05-14
**Last refreshed:** 2026-05-20/21 — Phase 1 implementation complete through Task 33 on branch `feat/pos-fiscal-event-engine-phase1` (PR #124). Latest pushed HEAD before this handoff-refresh commit: `e070a6fed` (`Phase 1.33.1: Record Task 33 reviews`). Tasks 1–33 are shipped or absorbed: 27B Pass 2 completed as Task 27B Pass 2B, Task 28 absorbed by Pass 2B (`/pos/receipts/sync` retired), and Task 33 full-flow verification shipped with dual review. Next work: Phase 1.5 cleanup / dev merge preparation, not more Phase 1 implementation.
**Why this exists:** the originating session hit the context window; subsequent sessions refresh this in place. This is the durable anchor — read this first, then the artifacts it indexes.

---

## 1. Where we are in the arc

Goal: bring B2C customer-account flows (on-account payments, receipts, compliance) to the offline-first POS — which surfaced that the **POS fiscal layer itself** needs rebuilding on the correct (device-authority) architecture first.

The journey, compressed:
- Customer-accounts spec hit 4 Codex BLOCKs → revealed the architecture was mis-placed (server-authored instead of device-authored).
- Pivoted: codebase reality audit + receipt-chain trace + clean-rebuild scoping + 6 primary-source research passes.
- Consolidated into the **source-of-truth document**: v1 UNSOUND → v2 MAJOR-REVISION → **v3 SOUND-WITH-CORRECTIONS → corrections applied → LOCKED**.
- **SoT v3 amended 2026-05-14:** §13 guardrail 6 + D16 added — the **bounded-modules / composable-activation guardrail** (carried in roadmap v1, lost in the architecture pivot) restored, then **refined the same day** with the inbound-reference-data / outbound-operational-dependency distinction (see §2.2).
- **Roadmap v2** written → Codex MAJOR-REVISION → 6 corrections applied → annotated for the bounded-modules seam (Treasury touchpoints marked module-integration; web-POS parity added to out-of-scope).
- **Phase 1 spec**: v1 → Codex BLOCK (3B/3P1/3P2) → v2 → Codex BLOCK (2B/1P1/1P2) → v3 → Codex BLOCK (1B/3P1/2P2) → v4 (resolves all 6) → Opus review APPROVE-WITH-MINOR-EDITS (1P1/2P2/2P3) → v5 (applies all 5) → Codex BLOCK (1B/1P3) → v6 (resolves both) → Codex BLOCK (1B) → **v7 (current) resolves the v6 finding + adds a structural fix (the §14.3 two-chokepoint completeness rule)**.
- v7 **cleared Codex re-review — APPROVE, 0 findings** (the §14.3 chokepoint enumeration independently grep-verified as exhaustive; no regressions). Next: **`writing-plans` for the Phase 1 implementation plan → execute.**

---

## 2. The locked architecture model

### 2.1 Device-authority fiscal pattern (LOCKED — SoT v3 §1)
The offline-first POS **device** is the fiscal source of truth: it authors, canonically serializes (once), hashes, chains, and seals fiscal events locally. The **server** is a verify-only mirror — it re-hashes the device's exact `canonical_bytes`, verifies linkage, stores verbatim; it **never re-serializes, never recomputes-and-replaces**. One fiscal pattern everywhere; the existing receipt chain is rebuilt clean on it (no migration — gated by a preflight check).

### 2.2 Bounded modules, composable activation — the asymmetric seam (LOCKED — SoT v3 §13.6 + D16; amended + refined 2026-05-14)
**Not** a "base POS → upgrade tier" ladder. Bounded modules compose per use case, and **the dependency is asymmetric — direction is what matters:**

- **Inbound — setup/reference data is mirrored and may be referenced (PERMITTED).** Payment methods, tenders, payment repositories, products are defined once in the web during setup and synced to the POS local mirror; the POS already operates offline-first against that mirror. A POS record referencing this mirrored reference data — e.g. `ReceiptPayment.payment_method_id` FK to `payment_methods` — is a *permitted inbound setup dependency*, **not** a coupling to the Treasury operational module, and does not block a POS-only deployment.
- **Outbound — the fiscal engine depends on no other module's operations (FORBIDDEN to couple).** The `FiscalEventEngine`, the authoring/sealing/chain, and the server-side `OutboxIngestor` + ingestion path have **zero** dependency on the Treasury *operational* module (Treasury `Payment` rows, GL postings, `PaymentAllocationService`/FIFO allocation), accounting, or the B2B sales module.

**The mechanism:** the POS fiscal engine *publishes* fiscal events; each active module *consumes/projects* them via a **pluggable projector registry** (`FiscalEventProjectionRegistry`) resolved per `(tenant, company)` by a **`ModuleActivationResolver`**. The receipt business projection = a **POS-core projection that always runs** + **pluggable module bridges** (the **Treasury bridge** runs only when Treasury is active). The POS-core projector owns `pos_receipts`/lines/VAT, `ReceiptPayment` rows, vouchers, stock; the Treasury bridge owns Treasury `Payment` rows + GL.

POS-only is a **supported standalone deployment**; its transactions are viewable via the existing web shop-management (POS) section as a read projection over the synced mirror. **The actual web POS** (`TerminalType::Web`, a server-authoring browser path) is **out of scope** as a device-authority target — dispositioned (hidden/disabled or confirmed-unused) in the Phase 1 preflight gate; parity deferred.

This guardrail was in roadmap v1, lost in the architecture pivot, **re-locked + refined this session. Do not reopen it.**

---

## 3. Artifact index

| Artifact | Path | Status |
|---|---|---|
| **Source-of-truth v3** | `docs/superpowers/research/2026-05-14-offline-first-fiscal-source-of-truth-v3.md` | **LOCKED** — amended 2026-05-14 (§13.6 guardrail 6 + D16, the asymmetric bounded-modules seam). Do not reopen. |
| Codebase reality audit | `docs/superpowers/research/2026-05-14-pos-fiscal-codebase-reality.md` | Current — the grounding reference for every codebase claim. Do not reopen. |
| Roadmap v2 (phased plan) | `docs/superpowers/specs/2026-05-14-pos-customer-accounts-roadmap-v2.md` | Current — annotated for the bounded-modules seam (Treasury touchpoints = module-integration; web-POS parity out of scope). Phase structure locked. |
| **Phase 1 spec v7** | `docs/superpowers/specs/2026-05-14-pos-phase1-foundation-spec-v7.md` | **CURRENT DRAFT** — resolves the v6 Codex BLOCKER + adds the §14.3 two-chokepoint completeness rule. Self-reviewed + owner-reviewed. Awaiting Codex re-review. |
| Codex review of Phase 1 spec v3 | `docs/superpowers/reviews/2026-05-14-pos-phase1-foundation-spec-v3-codex-review.md` | The 6 findings v4 resolved — see §5. |
| Opus review of Phase 1 spec v4 | `docs/superpowers/reviews/2026-05-14-pos-phase1-foundation-spec-v4-opus-review.md` | APPROVE-WITH-MINOR-EDITS — the 5 findings v5 resolved — see §5. |
| Codex review of Phase 1 spec v5 | `docs/superpowers/reviews/2026-05-14-pos-phase1-foundation-spec-v5-codex-review.md` | BLOCK — the 2 findings v6 resolved — see §5. |
| Codex review of Phase 1 spec v6 | `docs/superpowers/reviews/2026-05-14-pos-phase1-foundation-spec-v6-codex-review.md` | BLOCK — the finding v7 resolved — see §5. |
| Codex review of Phase 1 spec v7 | `docs/superpowers/reviews/2026-05-14-pos-phase1-foundation-spec-v7-codex-review.md` | **APPROVE — 0 findings.** The spec gate is passed. |
| **Phase 1 implementation plan** | `docs/superpowers/plans/2026-05-14-pos-phase1-fiscal-event-engine.md` | **CURRENT — v4, 2026-05-14.** 33 TDD tasks; preflight gate first. v1 → BLOCK (1B/3P1/3P2/1P3) → v2 → BLOCK (1P1) → v3 → BLOCK (1P1/1P3) → v4 resolves both. Awaiting v4 re-review. |
| Codex review of the Phase 1 plan (v1) | `docs/superpowers/reviews/2026-05-14-pos-phase1-fiscal-event-engine-plan-codex-review.md` | BLOCK — the 8 findings plan v2 resolved. |
| Codex review of the Phase 1 plan (v2) | `docs/superpowers/reviews/2026-05-14-pos-phase1-fiscal-event-engine-plan-v2-codex-review.md` | BLOCK — 1 P1 (deferred provider wiring); plan v3 resolved it. |
| Codex review of the Phase 1 plan (v3) | `docs/superpowers/reviews/2026-05-14-pos-phase1-fiscal-event-engine-plan-v3-codex-review.md` | BLOCK — 1 P1 (Task 19 needs projectors before 21/22) + 1 P3; plan v4 resolved both. |
| Phase 1 spec v1–v6 + their reviews; SoT v1/v2 + assessments | `docs/superpowers/specs/` + `research/` + `reviews/` | Superseded — audit history. |
| 6 research reports (NF525, German TSE, integrity/recovery, Tunisia, B2B/POS investigation) | dispersed in `research/` + agent outputs | Inputs, consolidated into SoT v3. |
| Owner strategy docs | `~/Downloads/fiscal_chain_architecture_strategy.md`, `~/Downloads/pos_printable_documents_architecture.md` | Owner-provided grounding inputs. |

---

## 4. Current open work

Phase 1 spec **v7 is APPROVED** — Codex re-review 2026-05-14, 0 findings. The spec passed through v1→v7 (Codex BLOCK ×4, Opus APPROVE-WITH-MINOR-EDITS ×1) and the §14.3 two-chokepoint completeness rule closed the recurring "missed server-authoring caller" class. **The Phase 1 spec gate is passed.**

**Plan v4 is re-reviewed and cleared.** Review file: `docs/superpowers/reviews/2026-05-14-pos-phase1-fiscal-event-engine-plan-v4-codex-review.md` (verdict APPROVE-WITH-MINOR-EDITS; only P3 wording cleanup applied).

**Phase 1 implementation is complete on branch `feat/pos-fiscal-event-engine-phase1` (PR #124).** Latest pushed HEAD before this handoff-refresh commit: `e070a6fed` after Task 33 review artifacts. Owner sign-off on the schema-destructive preflight gate is recorded at `apps/api/docs/sessions/2026-05-14-fiscal-preflight-signoff.md` (attested 2026-05-15).

**Phase 1 progress: 33 of 33 task slots shipped or absorbed.** Task 27B Pass 2B (`b4faf11e6`) refactored POS receipts through the fiscal event engine, removed the legacy sync/chain path, deleted the `.PASS_2B_PENDING` marker atomically with engine wiring, and absorbed Task 28's `/pos/receipts/sync` retirement. Task 33 (`b713063d1` + review audit `e070a6fed`) added endpoint-driven full-flow verification and updated roadmap v2 status. Remaining work is Phase 1.5 cleanup and merge/PR disposition.

### 4.1 Tasks shipped (1–20 inclusive)

| # | Commit | Summary | Reviews |
|---|---|---|---|
| 1 | `fc69ee27` (promoted to `dev` via PR #123) | preflight + minimal Fiscal module provider | Opus APPROVE; sign-off attestation `d695e9cf` |
| 2 | `84ff133d` | FiscalEventType enum (Appendix A reserved values) | Opus APPROVE |
| 3 | `0f0e0271` | Supporting status enums (integrity/parse/projection/signature) | Opus APPROVE |
| 4 | `7e469e0a` | Canonical golden vectors + PHP hash-only golden test | Opus APPROVE |
| 5 | `296d9d61` | FiscalEventCanonicalEncoder TS (cross-language reproduction) | Opus APPROVE; **two deferred P2s** (vet hand-rolled SHA-256 OR add padding-boundary vectors at 55–57, 63–65, 119–120, 127–128 bytes; extract shared JCS core) — handle BEFORE Task 15 |
| 6 | `3ad6fe49` | FiscalIntegrityProvider + HashChainIntegrityProvider + SignatureProviderInterface | Opus APPROVE |
| 7 | `245f5e85` + `097c21a0` | `fiscal_events` table + Task 7 P3 un-skip | Opus + Codex both APPROVE-WITH-MINOR-EDITS-APPLIED |
| 8 | `2e036485` + `9577dc62` + `73066080` | `fiscal_events` immutability triggers + 2 BLOCKER closures | Codex round-2 BLOCKER (parse-failure resolution bypass) + round-3 BLOCKER (integrity_exception_class write-once) BOTH found by Codex, missed by Opus — both closed |
| 9 | `61444f56` + `223577ff` + `55027c0b` | `fiscal_event_projections` mutable table + reconciliation (P1 PG-gate, P2 $fillable narrowing, P2 PHPDoc) | Opus APPROVE-WITH-MINOR-EDITS; Codex APPROVE-WITH-MINOR-EDITS; consensus reconciliation applied |
| 10 | `2e8aec56` + `c3e448d2` + `9014019b` | `fiscal_event_quarantine` (§8 non-admissible partition) + reconciliation (P1 CI-gate, 4×P2 + index widen + Phase-2 trigger note) | Opus APPROVE-WITH-MINOR-EDITS; Codex REQUEST-CHANGES (file says APPROVE-WITH-MINOR-EDITS — wrapper-summary diverged); reconciliation applied |
| 11 | `ddc42d5c` + `00670adf` + `4aa7f9be` | `pos_receipts` gains `canonical_bytes` + `fiscal_event_id` UNIQUE FK; chain columns become mirrors. Receipt model `$fillable` extended; @property annotations added; prevent_receipt_modification trigger interaction documented; insert-based duplicate-rejection test (via explicit factory chain) added | Opus APPROVE-WITH-MINOR-EDITS; Codex APPROVE-WITH-MINOR-EDITS; 3 P2 reconciliation applied |
| 12 | `49fb63e3` + `a544d566` + `0d5da5b0` | Treasury `payments` gains `origin` + `fiscal_event_id`; `PaymentOrigin` enum (5 cases). FK direction `payments → fiscal_events` (asymmetric bounded-modules seam). Partial index on `fiscal_event_id WHERE NOT NULL` for projector replay. Payment model `$fillable` + cast + @property extended. No UNIQUE on `fiscal_event_id` (one event → N Payment rows; idempotency at projector-level per Task 22) | Opus APPROVE-WITH-MINOR-EDITS (3 P2); Codex APPROVE-WITH-MINOR-EDITS (1 P2 + 11 CLEAN); 4 P2 reconciliation applied (partial-index test, all-5-cases cast, runtime FK rejection, VARCHAR(32) length pin) |
| 13 | `442ea109` + `3fdb7a41` | **First Tauri/device-side task.** Device SQLite migration v37: `fiscal_events` table (spec §3.1 column set, NOT GLOB hash-format CHECK, sequence/enum/source-pair CHECKs), append-only triggers (BEFORE UPDATE OF all 33 non-sync columns; BEFORE DELETE), `terminal_state` +3 chain head columns (legacy mirrors retained), `offline_receipts.canonical_bytes` (nullable per Task 15 deferral). | Opus REQUEST-CHANGES (1 BLOCKER + 5 P2 + 1 P3); Codex BLOCK (1 BLOCKER + 1 P2 + 1 P3) — BOTH caught the same hash-format GLOB BLOCKER (`[0-9a-f]*` only validated first char). Round-2 commit fixes the CHECK with `NOT GLOB '*[^0-9a-f]*'` and grows the test from 12 → 49 cases (BLOCKER reproducer + sequence>0 + enum CHECKs + source-event pair CHECK + all-33-columns data-driven trigger coverage + post-failure atomicity + ALTER TABLE backfill on pre-v37 rows). |
| 5†| `b561bcd7` | Task 5 deferred P2s closed BEFORE Task 15: extracted `canonicalCore.ts` (shared JCS structural encoder parameterized by a `StringNormalizer`); receipt-V3 hash output remains byte-identical (47 v3 fixture tests stay green). SHA-256 padding-boundary vectors added — every block-edge length (55-57, 63-65, 119-121, 127-128) hashed against `node:crypto.createHash('sha256')`; 19 vectors total. | Closes the two Task-5 round-1 deferred P2s; gate to Task 15 lifted. |
| 14 | `57b8fda9` + `023f4a93` | `FiscalEventPayloadRegistry` + 5 payload DTOs (PHP) + TS device-side mirror. 4 implemented (`SaleReceiptPayload`, `ChainBreakDetectedPayload`, `ChainRestartPayload`, `TerminalRegistrySnapshotPayload`) + 1 reserved schema-only (`CompanyDayClosureManifestPayload`). Registry-vs-enum drift gate walks all 28 cases. TS `FISCAL_EVENT_TYPES` const tuple — 28-case mirror, verified by diff. | Opus APPROVE (0 BLOCKER / 0 P1 / 2 P2 / 2 P3); Codex BLOCK (2 BLOCKERs in `fromArray()`: silent key-missing data loss + float monetary field leak — both via the loose `(string) $data['key']` cast pattern). Round-2 commit introduces `FiscalPayloadArrayGuards` (requireString / requireInt / requireArray / optionalString with explicit `array_key_exists` + `is_*` checks); hardens all 4 implemented DTOs; aligns `CompanyDayClosureManifestPayload` to throw `FiscalEventTypeNotImplemented` (Opus P2-1). 11 → 20 tests (83 assertions). |
| 15 | `9a9a94b3` + `b888aa9e` | **First Opus-subagent delegation.** `FiscalEventEngine.append()` — the single device chain authoring path per spec §6.1. Runs inside caller's SQLite transaction; never commits. Resolves payload DTO + event_version via the registry; idempotency check on `(tenant_id, terminal_id, source_event_class, source_event_id)`; reads chain head; builds canonical_bytes; INSERTs `fiscal_events`; advances `terminal_state` chain head. Defends against the v37 empty-string seed sentinel from Task 13 Codex P3 (now hardened to require 64-char lowercase hex per Task 15 round-2 Codex P3). | Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 / 3 P3); Codex BLOCK (1 BLOCKER / 2 P1 / 2 P2 / 1 P3 — opaque `payload: unknown` lets non-string money into canonical bytes + envelope timestamp / date format not enforced + cross-terminal source-id pollution + raw UNIQUE on parallel chain advance + genesis-seed only rejected `''`). Round-2 commit adds: typed payload input interfaces (compile-time defense) + runtime `validateRequestEnvelope` + `validateRequestPayload` (rejects non-string monetary fields in SALE_RECEIPT); terminal-scoped `findBySource`; typed `ConcurrentChainAdvanceError` on chain UNIQUE; `FiscalEventPayloadValidationError`; hex-validated genesis seed. 11 → 22 tests. Opus P3-1/P3-2/P3-3 (defaultDb cleanup, union → single type, findBySource comment) deferred. |
| 16 | `7683ce666` + `ecbe7cd84` | `StrictCanonicalParser` (server PHP) — derives the structured `payload` JSONB from already-hash-verified `canonical_bytes` per spec §7.6. Hand-rolled JSON tokenizer (no whitespace tolerance — JCS canonical bytes) detects duplicate keys at every depth + rejects out-of-grammar numbers (fractional/exponent/leading-zero/`+`/`-0`/overflow) + rejects invalid UTF-8 + rejects unescaped control bytes + rejects U+2028/U+2029 line/paragraph separators. Asserts the §4 fourteen-field envelope shape (exact key set + canonical regex for hash / ISO 8601 / business_date + event_version cross-check against registry). Rejects payload extras per event type via `PAYLOAD_KEYS` map. Per-event constraints: sub-array shape (Task 14 P2-2 carry-forward), scale-aware bcformat money regex, 64-char lowercase hex on payload-level hash fields (analogue of Task 15 P1-1 on the server boundary). Never throws — returns `ParseResult { ok, payload, failureReason }` with stable `<snake_case>:<context>` failure prefixes so OutboxIngestor (Task 19) can route on the prefix. 2 files (parser + ParseResult). | Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 1 P1 / 4 P2 / 4 P3); Codex BLOCK (4 BLOCKER — envelope shape not asserted, payload extras accepted, whitespace tolerated, money fields not regex-validated; 2 P1 — list-vs-assoc container, hex hash format; 2 P2 — U+2028/U+2029, test coverage). **Codex BLOCKER-catch streak now 5 tasks** (Tasks 8 ×2, 14, 15, 16); Opus had blessed the same commit. Round-2 commit closes every BLOCKER + both P1s + U+2028/U+2029 + Opus P2-1/P2-2/P2-3/P3-1/P3-2. 34 → 82 tests (175 → 618 assertions). Opus P2-4 (envelope-empty-list error string), P3-3 (mb_chr false check), P3-4 (payload-list forensic prefix), Codex P2-7 NFC normalization deferred. |
| 17 | `3cff98285` + round-2 | `ModuleActivationResolver` interface + `DefaultModuleActivationResolver` (server PHP) — the per-`(tenant, company)` activation seam per spec §7.3 + SoT §13.6/D16. Interface in `app/Shared/Contracts/Fiscal/`; default impl delegates to `CompanyConfigService` → `CompanyConfig::hasModule()` over `allEnabledModules` (the same surface `RequireModule` reads). Canonical PascalCase token contract (`'Treasury'`) test-locked per §17.3; lowercase strict-compared false. companyId parameter held for future per-company branching but unused in Phase 1 — §18 caveat documented in both interface + impl. Defensive contract: missing tenant OR malformed-UUID tenantId resolves to false, never throws (round-2 wrapped `Tenant::find()` in `try/catch (QueryException)` for the genuinely-never-throws guarantee on PG `uuid`-typed column). Provider binding alongside FiscalIntegrityProvider in FiscalServiceProvider. | Opus APPROVE (0 BLOCKER / 0 P1 / 0 P2 / 7 P3 doc-nits); Codex APPROVE (0 BLOCKER / 0 P1 / 0 P2 / 3 P3 — one of which Codex got wrong, `test_company_id_parameter_is_accepted` already existed). **First single-shot dual APPROVE of the streak** — Codex BLOCKER-catch streak paused at 5 tasks (Tasks 8 ×2, 14, 15, 16). Round-2 closed the convergent doc nits (cache-key shape + 24h TTL caveat in impl docblock; Opus P3-7 whitespace-padded token test) + Opus P3-3 (genuinely-never-throws via QueryException wrap). 6 → 8 tests. |
| 18 | `1151ca94e` + round-2 | `FiscalEventProjector` interface + `FiscalEventProjectionRegistry` (server PHP) — the pluggable-bridge seam per spec §5.0 + §7.3. Interface contract: `name() / handlesEventType() / requiresModule() / apply()`. Registry filters by event-type first, then gates on `ModuleActivationResolver` using the **event's** `tenant_id` + `company_id` (call-site lock-test prevents request-context leakage). Determinism via registration-order preservation. Singleton provider binding over `app->tagged(FiscalEventProjector::class)` — Tasks 21/22 will register the real POS-core + Treasury bridge projectors. Boundary-clean: zero imports of Treasury/Accounting/Sales operational code (SoT §13.6/D16 enforcement). | Opus APPROVE (0 BLOCKER / 0 P1 / 0 P2 / 3 P3 doc-nits — second consecutive single-shot APPROVE-eligible); **Codex BLOCK (1 BLOCKER + 2 P2 — resumes the streak at 6 tasks): F1 resolver-throw escape violates §7.2/§7.5 fail-closed contract; F2 duplicate projector names would violate §7.5 UNIQUE constraint downstream; F3 empty/whitespace `requiresModule()` token silently routes to strict-compare-false instead of fast-failing.** Round-2 closes all three: try/catch around the resolver call with structured Log::error + projector exclusion; constructor-time uniqueness check on `name()` throwing `LogicException`; constructor-time `trim() === ''` rejection on non-null requiresModule(). 9 → 13 tests. |
| 19 | `f202f5ef9` + `0435391b9` (round-2) | `OutboxIngestor.ingest()` — the **verify-then-insert server-side core** per spec §7.2 (the most load-bearing service in Phase 1). Standalone operation (NOT in a business transaction). Validate envelope shape + UTF-8 + hashes/UUIDs/timestamps via new `FiscalEventEnvelope::fromArray()` (round-2), then `hash_ok` (SHA-256 vs current_hash), `linkage_ok` (numeric continuity AND hash chaining — first events compare `previous_hash` against `pos_terminals.genesis_seed` per round-2 T19-B3), `clock_ok` (rollback-vs-prior + config-driven drift threshold per round-2 P1), `parse_result` (StrictCanonicalParser). Derive `integrity_status` + `integrity_exception_class` (with new `MalformedEnvelope` case + DB CHECK widened in migration `2026_05_14_100007`) + mandatory structured reason. Atomic insert via raw `INSERT … ON CONFLICT ON CONSTRAINT fiscal_events_tenant_terminal_sequence_unique DO NOTHING RETURNING id` (the spec-mandated primitive — round-2 T19-B1 closure). Conflicts scoped to the named slot-constraint; `fiscal_events_source_event_unique` violations re-throw via `Log::critical` (round-2 T19-B2). On insert success, one `fiscal_event_projections` row per active projector (suppressed for `canonical_parse_failure`), enqueued via `DB::afterCommit()`. `server_received_at` from DB `NOW()` not PHP wall-clock (round-2 P1 T19-P2). First Opus-subagent delegation since Task 15. | **Round-1 (`f202f5ef9`):** Opus REQUEST-CHANGES (1 BLOCKER B1 = try/catch-aborts-PG-tx **regression of spec round-4 closed BLOCKER**, 1 P1 F1 = genesis-seed bypass, 4 P2 F2-F5, 2 P3 F6-F7); **Codex BLOCK (4 BLOCKERs: T19-B1 converged on PG-abort, T19-B2 source-event-id constraint misclassification missed by Opus, T19-B3 elevated genesis-seed to BLOCKER because `pos_terminals.genesis_seed` actually exists server-side — subagent's deferral premise was wrong, T19-B4 envelope-layer invariants missing; 2 P1, 3 P2)**. **Codex BLOCKER-streak now 7 tasks** (8 ×2, 14, 15, 16, 18, 19). **Round-2 (`0435391b9`):** all 4 BLOCKERs + both P1s + 3 P2s closed in a single commit; new DB migration to widen `fiscal_event_quarantine` CHECK constraint for `malformed_envelope` class; new `config/fiscal.php` for clock-drift threshold; `FiscalEventEnvelope::fromArray()` + `assertWireShape()` regex-validates hashes/UUIDs/timestamps at the ingestor boundary; genesis-seed lookup with defensive `QueryException` wrap (Task 17 standing pattern); rollback-suppresses-dispatch test + real after-commit dispatch test; `buildConflictReason()` enumerates all 5 diff fields. 11 → 20 tests (98 assertions). Full Fiscal suite 217/217. |
| 22 | `18df84988` + `52ff3166f` (round-2) | **First module bridge** (Treasury-active-only projector per `requiresModule()='Treasury'` per spec §17.3 canonical PascalCase token) per spec §5.0 + §7.3 + §7.4 split + §13 writer inventory + plan §1641+. Fourth Opus-subagent delegation. Sibling to Task 21's `PosCoreReceiptProjection`. Owns ONLY Treasury operational portion of `SALE_RECEIPT`: stamps `origin=Pos` + `fiscal_event_id` on Treasury `Payment::create`; calls `GeneralLedgerService::createPOSPaymentEntry()`. Idempotency = check-then-insert at projector level (no UNIQUE on `payments.fiscal_event_id` per Task 12 deliberate choice — one fiscal event → N Payment rows for split payments). Round-2 added `pg_advisory_xact_lock(hashtext(event_id || ':treasury_receipt_bridge'))` inside the wrapping transaction (SQLite-driver-guarded) for race-safety, replacing the round-1 docblock that incorrectly claimed Task 23 would own a `lockForUpdate()` on `fiscal_event_projections` (premise verified false against Task 23 plan §1705+ in Codex F1). Cross-tenant `payment_method_id` enforcement = fail-closed `RuntimeException` inside transaction (Task 21 R2 F3 pattern; round-1 missed it, Opus F1 caught). §13 writer inventory complete: 10 writers stamped + tested in `PaymentOriginWriterInventoryTest` (PaymentController::store/storeMultiple → web_admin; MultiPaymentService::createSplitPayment/recordDeposit/recordPaymentOnAccount → web_admin; PaymentRefundService::refundPayment/partialRefund/refundReceiptPayments → inherit origin via new `originForRefund()` helper falling back to `PaymentOrigin::UnknownLegacy` on NULL — round-2 B2 closure; VendorRefundService::refundPrepayment → web_admin). New backfill migration `2026_05_17_120000_backfill_legacy_payment_origin.php` closes Task 12 spec drift (legacy NULL rows → `unknown_legacy`). Provider wiring via `TreasuryServiceProvider::register()` tag → Task 18's `app->tagged(FiscalEventProjector::class)` consumption (now priority-sorted per round-2 B1). | **Round-1 (`18df84988`):** Opus REQUEST-CHANGES (1 BLOCKER F1=cross-tenant payment_method_id missing + 2 P1 F2/F3 + 2 P2 + 3 P3); **Codex BLOCK (2 BLOCKER T22-B1=provider-order silent-applied + T22-B2=NULL-origin refund inheritance + 1 P1 = Task 23 lockForUpdate premise fails + 1 P3 stale docblocks)**. **Convergent on the provider-order BLOCKER from different angles** (Codex caught the Task 23 lifecycle silent-applied consequence; Opus caught the wrong docblock claim). §13 writer inventory exhaustive confirmation: both reviewers grep-verified no missing writers. **Round-2 (`52ff3166f`):** all 3 BLOCKERs + both P1s + 5 P2/P3s closed in a single commit + dual review artifacts committed at `1cdedae77`. `priority(): int` added to `FiscalEventProjector` interface; registry sorts by `(priority ASC, name ASC)` at construction with negative-priority `LogicException` boot validation (Task 18 standing pattern); 271 → 282 Fiscal tests (11 new = 47 targeted-three total across TreasuryReceiptBridgeTest/PaymentOriginWriterInventoryTest/FiscalEventProjectionRegistryTest); Treasury suite 355 green; phpstan + pint clean; CI PG-merge-gate filter extended at `ci.yml:~404` adding `TreasuryReceiptBridgeTest|PaymentOriginWriterInventoryTest`. **Codex BLOCKER-streak now 9 of 22** (8 ×2, 14, 15, 16, 18, 19, 21, 22). |
| 21 | `77f4674f0` + `25e0314f4` (round-2) | **First always-running projector.** `PosCoreReceiptProjection` (POS module — `requiresModule()=null`) per spec §5.0 + §7.3 + §7.4 split + plan §1561+. Third Opus-subagent delegation. Implements `FiscalEventProjector` for `SALE_RECEIPT`; idempotency via raw `INSERT … ON CONFLICT ON CONSTRAINT pos_receipts_fiscal_event_id_unique DO NOTHING RETURNING id` (round-2 T21-B3 closure — regression of Task 19 standing pattern). Owns `ReceiptPayment::create` per the §7.4 split (Treasury `Payment` + GL lives in Task 22's bridge). Relocates voucher redemption + stock movement from `ReceiptSyncService::sync*` and `ReceiptPayment::create` from `ReceiptPaymentService:306` — legacy bodies retained with deprecation docblocks citing §14 (knowingly-bounded intermediate state; `/pos/receipts/sync` retirement routed to Task 28; new-sale server-authoring disposition routed to Task 29). Real per-section hashes (`hashVATBreakdown` + `hashPaymentMethods`) computed AND `VerifyPosChainCommand` widened to skip `whereNotNull('fiscal_event_id')` rows so legacy `pos:verify-chains` stays green on mixed terminals (round-2 B5 closure — both mitigations applied). Cross-tenant `payment_method_id` enforcement = fail-closed `RuntimeException` (round-2 F3 closure — round-1 docblock claim "FK rejection" was inaccurate; now actually enforced application-side). Payment-line shape violations now raise typed `FiscalPayloadValidationException` (round-2 B2 closure). Provider wiring via `POSServiceProvider::register()` tag → Task 18's `app->tagged(FiscalEventProjector::class)` consumption. | **Round-1 (`77f4674f0`):** Opus REQUEST-CHANGES (2 BLOCKER F1=verify-chains chain-broken + F2=stock/voucher untested; 1 P1 F3=cross-tenant FK untested; 3 P2 F4-F6; 3 P3 F7-F9); **Codex BLOCK (4 BLOCKER: T21-B1 D8 two-model coexistence + T21-B2 payment-line schema drift + T21-B3 check-then-insert race + T21-B4 test matrix variants; 2 P1: PG-merge-gate filter + zero-sentinel; 1 P3 stale docblocks)**. Convergent finding: both flagged the PG-merge-gate filter omission (Tasks 10/11/19/20 standing pattern). Divergent adjudication on C1 (D8 coexistence) resolved in round-2 by re-reading spec v7 §14 — table tags `ReceiptSyncService` business logic as "REUSE (relocated → `PosCoreReceiptProjection`)" with retirement explicitly routed to Task 28/29 → Opus's accept stood. **Round-2 (`25e0314f4`):** all 6 BLOCKERs + both P1s + 5 P2/P3s closed in a single commit; 8 → 21 tests (29 → 78 assertions); full Fiscal suite 235 → 248 green; phpstan + pint clean; CI PG-merge-gate filter extended at `ci.yml:380` adding `PosCoreReceiptProjectionTest`. Test matrix added: split payments, voucher redemption, stock decrement, voucher+stock combo, zero-payment, voucher-rollback. **Codex BLOCKER-streak now 8 of 21** (8 ×2, 14, 15, 16, 18, 19, 21). |
| 20 | `841c3d781` + `db62c469a` (round-2) | `POST /api/v1/pos/sync/fiscal-events` — the single device→server fiscal-event ingestion endpoint per spec §7.1 + plan §1484. Inline implementation (NOT subagent). Wire envelope shape: outer `{ envelope_id, type: 'FISCAL_EVENT', payload_version, idempotency_key, payload }` with the inner spec §4 envelope + `canonical_bytes` + `current_hash`. Middleware tuple byte-equal to the existing POS surface (`['api', 'auth:sanctum', SetPermissionsTeam::class, EnforceTokenTenantClaim::class]`). Two-layer tenant boundary: `EnforceTokenTenantClaim` (middleware) checks token-vs-user; controller checks envelope-vs-user. Two-stage rejection model: malformed-field (422) or cross-tenant (403) anywhere in the batch aborts the ENTIRE batch BEFORE any ingest call — zero rows persist on either rejection. `FiscalServiceProvider::boot()` gains `loadRoutesFrom(__DIR__.'/../routes.php')`; CI PG-merge-gate filter extended in the same commit. | Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 / 3 P3); Codex APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 — **both reviewers convergent on the SAME 2 P2s: `IngestionResult::idempotent` HTTP mapping untested + `IngestionResult::quarantined` (`canonical_hash_mismatch`) HTTP mapping untested**); **Codex BLOCKER-streak holds at 7 of 20** (the second single-shot clean dual review of the streak after Task 17). Codex sandbox could not write the review file — content transcribed inline by parent session. Round-2 (`db62c469a`) added the 2 missing endpoint tests + 1 precedence test (combined cross-tenant + malformed UUID → 422 wins over 403 because Stage 1 runs before Stage 2) + rewrote stale controller comment block at lines 107-114 (the prose promised a defense-in-depth catch around `ingest()` that didn't exist; rewritten to honestly document the Task 19 round-2 T19-B2 QueryException re-throw → 500 contract instead) + cited the `SyncReceiptsRequest:64` precedent for the `envelopes.*.max:100` cap. 7 → 10 endpoint tests (30 → 53 assertions). Full Fiscal suite 227/227. |
| 23 | `34f1b6113` + `6c03bac5e` (r2) + `149ac8bc4` (r3) + `8fed9d87f` (r4) + `e2cc626b1` (review artifacts) | **First four-round task.** `ApplyFiscalEventProjectionJob` — Horizon-owned retry + dead-letter contract per spec §7.5 + plan §1705+. Fifth Opus-subagent delegation. Plan amended pre-dispatch to add row-level `lockForUpdate()` lifecycle lock + idempotent terminal-state short-circuit per Task 22 cross-task implication. Wires `OutboxIngestor` enqueue (round-1 replaced the Task 19 `TODO(Task 23)` no-op closure with real `ApplyFiscalEventProjectionJob::dispatch($row['id'])`). Two-transaction shape: T_lock (lockForUpdate + flip-to-running, committed BEFORE apply) + T_apply (projector's own). `byName(string $name)` added to registry — ignores activation gating (orphan-avoidance: module deactivated between ingest and run still resolves). `WithoutOverlapping` middleware keyed by `projectionRowId` with `expireAfter($timeout) + releaseAfter($timeout + 30)`. Symmetric reset-to-Pending in both `advanceFailureAccounting` + `recordHardFailure`. T_lock stamps `last_attempted_at` at Running flip. | **Round-1 (`34f1b6113`):** Opus APPROVE-WITH-MINOR-EDITS (2 P1 + 1 P2 + 2 P3); Codex BLOCK (2 BLOCKER T23-B1 + T23-B2 + 1 P1 + 3 P3). T23-B1 = concurrent-delivery race (lockForUpdate only serializes the brief status flip, not the long apply); T23-B2 = Treasury silently returns clean when pos_receipts missing → marked applied with zero effects under multi-worker race. Codex BLOCKER-streak now 10 of 23. **Round-2 (`6c03bac5e`):** closed all 6 round-1 findings cleanly with `WithoutOverlapping` middleware + cross-task touch on TreasuryReceiptBridge (Task 22) — replaced silent return with new `ProjectionDependencyMissingException`. New Fiscal Domain exception class added. Cross-task touch necessary + documented. **Codex round-2 re-review caught 2 NEW BLOCKERs in the fix itself**: R2-B1 (stale-running guard killed Horizon retry path — advanceFailureAccounting left status=Running + fresh last_attempted_at → next retry's T_lock short-circuited silently; Treasury retry pattern was dead in production) + R2-B2 (`WithoutOverlapping->dontRelease()` permanently dropped duplicate deliveries on stale-held lock with Redis `retry_after=90s` < `expireAfter=120s`). Opus round-2 APPROVE missed both. Codex sandbox read-only — verdict transcribed by controller. **Round-3 (`149ac8bc4`):** closed both R2 BLOCKERs via Codex's prescribed options (c) + (b). Symmetric scope-expansion to `recordHardFailure` (same bug class). Codex round-3 REQUEST-CHANGES with R3-P1-1 (T_lock didn't stamp `last_attempted_at` on Running flip → in-flight guard's null-path returned false → if cache lock failed open, duplicate could enter apply concurrently). Opus round-3 APPROVE with 2 P3s (R3-F1 docblock + R3-F2 try/finally for Carbon::setTestNow). **Round-4 (`8fed9d87f`):** closed R3-P1-1 with 1-line stamp at T_lock + 2 docblock updates + 2 try/finally wrappers. Codex round-4 **APPROVE — 0 new findings**, defect-surface sweep clean, operator-visibility audit clean. **Codex BLOCKER-streak now 10 of 23** (this task = 2 round-1 BLOCKERs + 2 round-2 BLOCKERs counted as the same task's regression class; lifecycle-state-machine class). Full Fiscal suite 187/187 (+11 vs round-2 baseline). 18 ApplyFiscalEventProjectionJobTest tests. CI PG-merge-gate filter unchanged (no PG-specific behavior asserted). |

| 24 | `71eaa838c` + `5bb9216ce` (r2) + `48c261320` (r3) + `dc3b2f322` (review artifacts) | `ParseFailureResolutionService` + `EnqueueResolvedEventProjectionsCommand` (Fiscal — atomic parse-failure resume contract per spec §7.5 + §15.2 + plan §1812+). Sixth Opus-subagent delegation. ONE-transaction shape: `payload` + `payload_parse_status='parsed'` + `integrity_status='verified'` + `integrity_resolved_at/by` written in single UPDATE per Task 8 trigger Step 3 requirements; projection rows inserted in same transaction; `DB::afterCommit()` dispatches `EnqueueResolvedEventProjectionsCommand` to enqueue `ApplyFiscalEventProjectionJob` per pending row. Command idempotent + permission-gated by `fiscal.events.resolve_quarantine` (new permission added to `RolesAndPermissionsSeeder` alongside `fiscal.events.verify_chain` for Task 31). New `FiscalPayloadConstraintValidator` extracted from `StrictCanonicalParser` (round-2 cross-task touch to Task 16) so both parser AND resolver enforce identical per-event grammar (extras-rejection, money scale-aware regex, sub-array shape). Spatie team-scoped permission check via `PermissionRegistrar::setPermissionsTeamId($actor->tenant_id)` in try/finally. `--actor-id` flag carries the actor identity (spec drift documented in docblock — Phase 1 implementation choice). | **Round-1 (`71eaa838c`):** Opus APPROVE-WITH-MINOR-EDITS (2 P1 + 2 P2 + 2 P3); Codex REQUEST-CHANGES (T24-P1 payload validation gap + T24-P2 Spatie team context bug — Codex-only, missed by Opus). **Convergent P1** (Opus F2 / Codex T24-P1). **Implementer flagged a brief↔code conflict on `integrity_exception_class`** — brief said null out; trigger forbids (write-once forensic metadata per Task 8 lines 128-136); implementer's interpretation correct + documented. **Round-2 (`5bb9216ce`):** closed convergent P1 + Codex T24-P2 + Opus F3 + F6; deferred F1/F4/F5 with docblock notes. Cross-task touch: extracted `FiscalPayloadConstraintValidator` from `StrictCanonicalParser`. **Codex round-2 caught new P1** in the fix itself (T24-R2-P1): parser constructor used `new in body` anti-pattern violating CLAUDE.md rule 13 (the brief explicitly warned against this). **Round-3 (`48c261320`):** closed Codex P1 with required non-nullable constructor parameter + updated test call site + deleted dead `ResumeThrowingResolver` class. No round-3 re-review dispatched (mechanical fix matching Codex's spec; verified by 201/201). Codex sandbox read-only across all 3 rounds — review file transcribed by controller. **Codex BLOCKER-streak now 11 of 24** (counts the new P1 in round-2's fix as Codex-caught). Full Fiscal feature suite 201/201 (+14 vs Task 23 baseline). 14 ParseFailureResumeTest tests + 82 StrictCanonicalParserTest tests still green. PHPStan + Pint clean. CI PG-merge-gate filter extended at `ci.yml:382` adding `ParseFailureResumeTest`. |

| 25 | `7cccfce8b` + `a73ecdbd8` (r2) + `2c0425f56` (r3) + `5638f9d19` (review artifacts) | **First device-side task since Task 13** (mixed PHP + TS). `ChainRecoveryService` (TS) + `ClockAnomalyDetector` (PHP) + `TerminalFiscalConfig` (PHP DTO) + new v38 SQLite migration for `terminal_state.fiscal_chain_status`. Seventh Opus-subagent delegation. Extracts `OutboxIngestor::verifyClock` into dedicated detector per Task 24 round-2 pattern. ChainRecoveryService emits ordinary `CHAIN_BREAK_DETECTED` + `CHAIN_RESTART` fiscal events through `FiscalEventEngine.append()` inside one SqlSurface transaction (round-2 atomicity fix). Records degraded terminal state in same tx. Round-2 added strict IANA timezone validation in TerminalFiscalConfig (rejects `+02:00`, `GMT+02:00`, `Etc/GMT+2`). Round-2 + round-3 added cross-language drift gate: TS validators for both event types mirror PHP `FiscalPayloadConstraintValidator::PAYLOAD_KEYS` byte-for-byte (hash regex + non-empty-object + extra-key rejection). | **Round-1 (`7cccfce8b`):** Opus APPROVE-WITH-MINOR-EDITS (1 P1 IANA-strict + 2 P2 + 2 P3); **Codex REQUEST-CHANGES (1 BLOCKER T25-B1 atomicity — appends not in transaction; 2 P1 — degraded mode unrecorded per spec §9, TS skips runtime validation; 1 P2; 1 P3)**. Codex caught all 3 critical issues Opus missed — load-bearing reviewer for this task class. **Round-2 (`a73ecdbd8`):** closed all r1 findings (BLOCKER + 3 P1 + 2 P2 + 3 P3); new v38 migration; cross-task touches: OutboxIngestor + FiscalEventEngine + db migrations. 21 TS + 35 PHP tests (+13 + 10 vs round-1). 140/140 TS + 201/201 PHP green. **Codex round-2 caught 1 new P1** (T25-R2-P1) — TS still missing extra-top-level-key rejection that PHP's `validatePayloadKeySet()` enforces (round-2 added hash + non-empty-object but missed extras gate) + 1 P3 stale v37 docblock. **Round-3 (`2c0425f56`):** closed both with `assertNoExtraTopLevelKeys()` helper + byte-mirrored `PAYLOAD_KEYS` constants + docblock update. 142/142 TS vitest (+2 new); no PHP changes. Codex 2 verification-data discrepancies were false positives (clockDriftLimitSeconds 4 hits all in detector legitimately; vitest 103 vs 140 was scope mismatch). **Codex BLOCKER-streak now 12 of 25** (counts T25-B1 atomicity BLOCKER + T25-R2-P1 round-2 new P1 escalation). Full Fiscal suite stable 201/201 across all 3 rounds. CI PG-merge-gate filter unchanged (Unit tests + TS only). |

| 26 | `31d2c94a6` + `f2449b638` (r2) | **First task to amend a LOCKED spec** (spec §11.0 carve-out added). `TerminalRegistrySnapshotService` (PHP server-authoring path for company-integrity events). Eighth Opus-subagent delegation. Plus `FiscalEventProjectionRegistry::all()` (Generator) addition; new TS `ServerAuthoredEventTypeError` + `isServerOnly()` registry classifier; new Fiscal Domain `InvalidServerAuthoredPayloadException`. The implementer initially proceeded on the brief's premise that §11 carved out server-authoring; Codex caught that §11 did NOT explicitly authorize it (§3/§4 forbid). | **Round-1 (`31d2c94a6`):** Opus APPROVE-WITH-MINOR-EDITS (3 P2 + 3 P3); **Codex REQUEST-CHANGES (1 BLOCKER T26-B1 architectural carve-out missing + 1 P1 race + 2 P2 + 1 P3)**. T26-B1 architectural BLOCKER: spec §11 lines 535-542 confirms event type + emission use case but doesn't say "server-authored" — §3 "Device SQLite is authoritative; server is verify-only mirror" + §4 "server never re-serializes" stand. Codex sandbox **wrote review file successfully this time** (manual user-piped prompt with explicit file path + VERDICT sentinel). **Round-2 (`f2449b638`):** controller chose Path A (amend spec) over Path B (rewrite as device-authored). Added spec §11.0 (LOCKED carve-out with 5 invariants) + §11.1 restating implemented/reserved entries. Closed T26-P1 via DB::transaction + lockForUpdate on pos_terminals. Closed T26-P2 via `FiscalPayloadConstraintValidator` constructor injection + `validateServerAuthoredPayload()` helper. Closed T26-P3 via TS `ServerAuthoredEventTypeError` at Step -1 of `append()` + `isServerOnly()` classifier on registry. Closed T26-P4 via 3 new tests including `test_third_snapshot_links_to_second` + `test_concurrent_emission_serializes_via_terminal_row_lock` + validator-rejection tests. Implementer's defensive precedence test (Step -1 §11.0 reject wins over not-implemented) for `COMPANY_DAY_CLOSURE_MANIFEST` future-proofs the boundary. Full Fiscal suite 220/220 + 19/19 TerminalRegistrySnapshotTest + 149/149 TS fiscal vitest. **Codex BLOCKER-streak now 13 of 26**. No round-2 re-review dispatched (Codex backgrounds timed out repeatedly; user-piped runs were the only reliable channel). |

| 27 Pass 1 | `c1c30ea86` | **Split into Pass 1 + Pass 2 (Task 27B).** Ninth Opus-subagent delegation reported scope concerns (no production wiring of FiscalEventEngine; tenant_id/company_id flow gap; ~1300 LOC test migration; PHP payload shape contract). Controller chose Option A (split). Pass 1 = `executeCheckout` connectivity-independent + `receiptApi.createReceipt`/`processReceiptPayments` deleted + source-level guards in test (§14.3 chokepoint disposition). Pass 2 = `receiptService` assembler with full engine wiring (DEFERRED, BLOCKED on owner design Q1-Q6). 5 cascading test files updated for mock stripping. Source-level guard uses lookbehind regex `(?<!Offline)createReceipt\b` to distinguish kept `createOfflineReceipt`. 1412/1412 pos vitest green; typecheck + lint clean. No formal dual review (mechanical deletion, low risk). Task 28 precondition updated to require Pass 2. |
| 27B Pass 2 | DEFERRED | **NEW TASK** carved out from Task 27 (§14.3 chokepoint extension). Status: BLOCKED ON OWNER DESIGN INPUT (Q1-Q6 in plan §2087+). `receiptService.ts` → assembler refactor calling `FiscalEventEngine.append()`. Requires: production engine singleton wiring, threading tenant_id/company_id through `OfflineReceiptInput`, building canonical SALE_RECEIPT payload-translation layer (currency_scale + bcformat money strings per PHP `FiscalPayloadConstraintValidator::validateSaleReceiptPayload`), ~1300 LOC test migration. Blocks Task 28 (sync wiring) + Task 33 (full-flow verification). Recommend dedicated session. |
| 28 | BLOCKED on 27B Pass 2 | `/pos/receipts/sync` retirement — backend route removal + 7 PHP feature suite migrations + POS `syncService.ts` rework to push fiscal-event envelopes to `POST /api/v1/pos/sync/fiscal-events`. Cannot start until Task 27B Pass 2 ships the canonical SALE_RECEIPT payload assembler. |
| 29 | `eb85fb155` + `314dbc781` (r2) | **§14.2 new-sale server-authoring disposition** (web POS + Tauri-online + order-close). Eleventh Opus-subagent delegation. Closes one of the two v5/v6 BLOCKER root-cause classes ("a missed server-authoring caller"). Backend: 3 route-level closures returning HTTP 410 + `NEW_SALE_AUTHORING_RETIRED` for `POST /api/v1/pos/receipts`, `POST /api/v1/pos/receipts/{id}/payments`, `POST /api/v1/pos/orders/{id}/close`. Chose route-level closures over controller early-returns so the disposition code precedes FormRequest validation; controller methods retained with `[RETIRED §14.2]` docblock anchors for Task 30's §14.3 chokepoint grep manifest. Web frontend: `POSTransactions.tsx` collapsed 735→47 lines (retirement-notice page); `CloseOrderButton.tsx` deleted; `OrderPanel.tsx` close affordance + `canClose` predicate removed; `useOrders.useCloseOrder` removed; `orderApi.closeOrder` removed; `receiptApi.createReceipt`/`processReceiptPayments` deleted (mirrors Task 27 Pass 1's apps/pos deletion). Knowingly retained per §14.2: `void` + `processReturn` (Phase 2+ reserved event types, shared with offline Tauri POS via `VoidReturnModal.tsx`); `/pos/receipts/sync` (Task 28). Preserved (read-only): GET `/pos/receipts`, `/pos/receipts/{id}`, PDF endpoints. routes.php:45 verified unrelated (current `POST /pos/terminals/web` — terminal acquisition, not new-sale). 30 obsolete test methods across 8 PHP feature files skipped per round-1 (later converted to per-method skips in round-2 to preserve non-route assertions). Preflight signoff updated. CI PG-merge-gate filter extended with `NewSaleServerAuthoringDispositionTest` in same commit. | **Round-1 (`eb85fb155`):** Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 / 3 P3: F1 FK coverage gap on 6 non-payment-method axes; F2 route closures inherit auth:sanctum → 401 not 410 for anon; F3-F5 doc-nits); Codex APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 4 P2: T29-F1 void/return tests only assert "not 410" not happy-path = Task 22 deferred-bail-out smell; T29-F2 class-level skip drops non-route seeder permission assertion; T29-F3 instrument-binding skip citation overstates `FiscalPayloadConstraintValidator` coverage; T29-F4 class-level skip drops service-bypass tenant-isolation test where `ReceiptPaymentService` still reachable via `ExchangeService::processExchange`). Codex review run via the user-piped sandbox fallback (Codex sandbox reliability lesson). **Codex BLOCKER-streak holds at 13 of 27** — Task 29 was a single-shot dual APPROVE-WITH-MINOR-EDITS, joining Tasks 17 + 20 as the three clean-round-1 outliers. **Round-2 (`314dbc781`):** closed all 4 Codex P2s + 3 Opus P3s in a single commit (11 files / +267 / −47). T29-F1: void/processReturn now exercise happy paths with eligible-receipt seeding + actual payload shape, asserting 2xx + DB side effects (`voided_at`, `voided_by`, `receipt_type=return`, `original_receipt_id` mirrored). T29-F2: class-level → 4 per-method skips on route-dependent methods; `test_seeder_grants_apply_tolerance_to_cashier_role` un-skipped. T29-F3: skip citation now correctly points to `PosCoreReceiptProjection::writePayments` (defense-in-depth via `InstrumentRequiredException` + `PaymentInstrumentKind::requiresInstrumentForMethodCode`); `PosCoreReceiptProjectionTest` extended with 2 new test methods pinning voucher-instrument missing-type + missing-serial rejection. T29-F4: class-level → 5 per-method skips; `test_service_rejects_cross_tenant_customer_id_when_form_request_is_bypassed` un-skipped (LIVE — `ReceiptPaymentService` still production code). Opus F1 + F2 deferred with citations in preflight signoff + inline route comments. Opus round-2 re-review **APPROVE — 0 new findings** (defect-surface sweep clean, no Task 23 r2/r3 anti-pattern recurrence — assertion-strong typed `catch (InstrumentRequiredException $e) + fail()` + 3-table rollback on new projection tests, no new mocks, no `markTestIncomplete`). 234 → 244 Fiscal feature tests / 752 assertions. PHPStan + Pint clean. TypeScript + lint + vitest clean. |
| 30 | `cb1ab6eda` (r1) + `a005b1b71` (r2) + `660f7a28b` (r3) | **§14.3 two-chokepoint CI grep gate + `ReceiptHashService`/`Nf525DataProvider` rebuild.** Twelfth Opus-subagent delegation. Three-round task. R1 shipped `apps/api/scripts/saleReceipt-chokepoint-manifest.json` (8 entries / 9 grep hits) + `check-saleReceipt-chokepoints.sh` shell gate + `ChokepointCompletenessTest` (PHPUnit mirror per Task 20 defense-in-depth pattern) + `ReceiptChainRebuildTest` + dedicated `chokepoint-gate` CI job + PG-merge-gate filter extension + `ReceiptHashService::verifyTerminalChain` rebuilt to re-hash stored `canonical_bytes` (legacy `whereNotNull('fiscal_event_id')` carve-out per Task 21 R2) + `Nf525DataProvider` gained NEW `buildExport()` with `quarantine_section` per spec §8. | **R1 (`cb1ab6eda`):** Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 / 4 P3); **Codex BLOCK (1 BLOCKER T30-B1 + 1 P1 T30-P1 + 1 P2 T30-P2).** T30-B1 = NEW `buildExport()` is dead-path; live NF525 export still calls `buildExportSnapshot()` (recomputes from mirror); `Nf525ExportController:88` calls `verifyReceiptChain()` (still uses `calculateHash` recompute) — the rebuild added a parallel code path instead of rewriting the live one. Opus missed this as "cohabiting"; Codex caught as "dead path". T30-P1 = shell+PHPUnit gate reconcile by file+line_anchor substring only, no validation of receiver_type/chokepoint/calling_class. T30-P2 = bare-bool failure mode without structured context. **Codex BLOCKER-streak now 14 of 28 (Task 30 extends).** **R2 (`a005b1b71`):** closed 9 findings (1 BLOCKER + 1 P1 + 3 P2 + 4 P3) in single commit; introduced `Nf525ExportSnapshot.quarantineSection` field + `Nf525XmlBuilder::addQuarantineSection` + `Nf525JetExportService` pipeline integration + `verify-chokepoint-manifest.php` PHP reflection helper + `retired_in_task` markers in manifest + manifest schema bump 1.0→1.1 + `Log::error('chain_verification_failed', ...)` structured failure mode + 64-char genesis_seed defensive guard. Both reviewers found NEW defects (Task 23/24/25 round-2 pattern): **Codex R2 BLOCK (1 BLOCKER T30-R2-B1 + 3 P1 + 1 P2): dead-path rebuild STILL not closed — `buildExportSnapshot` mapSaleReceipt/mapVoidedReceipt/mapReturnReceipt still hydrate DTOs from `pos_receipts` mirror; canonical_bytes only read for a SIDE-CHANNEL LOG via `verifyCanonicalBytesForReceipt` which itself compares against `pos_receipts.fiscal_hash` (mirror) not `fiscal_events.current_hash` (authoritative). Plus CI gate broken (no PHP+composer in chokepoint-gate job); receiver-type validator too permissive (passes wrong receiver_type for multi-dependency classes); quarantine_section incomplete (only quarantine_table queried, in-table quarantined fiscal_events omitted); Log::shouldReceive Mockery anti-pattern.** Opus R2 APPROVE-WITH-MINOR-EDITS missed BLOCKER classification but caught the same mirror-source as P1. **R3 (`660f7a28b`):** closed all 5 R2 findings cleanly. `mapSaleReceipt`/`mapVoidedReceipt`/`mapReturnReceipt` now BIFURCATE — fiscal_event-backed receipts source data from `fiscal_events.payload` via `StrictCanonicalParser` (Task 16); tampered receipts (hash-mismatch) route to NEW `tamperedSection` array on `Nf525ExportSnapshot` and are EXCLUDED from `sales`. Legacy receipts (`fiscal_event_id IS NULL`) keep model-based path. CI gate now `SKIP_RECEIVER_TYPE_VALIDATOR=1` on the lightweight `chokepoint-gate` job + PHP validator runs in `backend-test-pgsql` (has vendor). Receiver-type validator now parses receiver expression `$this->propertyName->` and reflects on THAT specific property. Quarantine query UNIONs `fiscal_event_quarantine` + `fiscal_events WHERE payload_parse_status='failed' OR integrity_status != 'verified'`; XML emits all spec §8 fields. `Log::spy()` + `shouldHaveReceived('error')->withArgs(...)` migration; defense-in-depth no-sensitive-data audit (no canonical_bytes / payload in log context). **Opus R3 re-review APPROVE — 0 new findings**, 3 P3 polish items (line-level data still mirror-sourced — out of scope; `Log::shouldNotHaveReceived` re-introduces `Mockery::any()` cosmetically; `conflicting_event_id` for sequence_conflict spec §8 ambiguous). Codex R3 re-review skipped (Codex sandbox-reliability lesson; round-3 is mechanical closure). 16/16 Task 30 tests + 260/260 full Fiscal feature suite + JET XML byte-stability fixtures preserved + PHPStan L8 + Pint clean. CI gate (3 invocations: default, SKIP env var, --phase1-complete) + PHP validator all PASS. **3 NEW standing patterns surfaced** (folded into §4.2): dead-path-rebuild-needs-actual-rewire / receiver-type-validation-needs-specific-call-site-not-just-class-has-type-X / CI-gate-extension-must-update-job-deps-in-same-commit. |
| 32 | `b09665a33` (r1) + `3995575e0` (r2) | **Off-device durability controls + device-loss incident register (§12)** — Phase 1 gate before Phase 2 customer-facing deployment. 13th Opus-subagent delegation. New TS service `OffDeviceDurabilityService` + `UnsyncedRiskIndicator` component + `DurabilityGateModal` + `useFiscalDurabilityPolling` hook + `durabilityServiceFactory` + Zustand `durabilityStore` + new PHP `DeviceLossIncident` model + `DeviceLossIncidentStatus` enum + migration `2026_05_20_100001_create_device_loss_incidents_table.php` + (round-2) `2026_05_20_100002_add_composite_fk_to_device_loss_incidents.php`. **R1 (`b09665a33`):** Opus APPROVE (0 BLOCKER / 0 P1 / 0 P2 / 2 P3 + 1 NOTE); **Codex REJECT (2 BLOCKER + 1 P1 + 2 P2): T32-B1 DeviceLossIncidentTest inserts orphan FK refs (random UUIDs for tenant/company/terminal/users — on PG would FK-violate before CHECK; tests "pass" on SQLite because FKs aren't enforced); T32-B2 dead control surface (UnsyncedRiskIndicator + shouldForceArchive mounted nowhere — Task 30 dead-path-rebuild pattern recurrence on a FRESH NET-NEW component class); T32-P1 cross-tenant terminal_id allowed by independent FKs; T32-P2 thresholds required without defaults; T32-P3 vitest skipped on Node 20 because node:sqlite needs 22.5+. Codex BLOCKER-streak now 15 of 29 (Task 32 extends).** Opus missed the dead-path BLOCKER again (Task 30 lesson exact recurrence). **R2 (`3995575e0`):** closed all 5 findings; 21 files / +1871 / -113. T32-B1: factory-seeded real tenants/companies/pos_terminals/users in setUp(); invalid-enum test asserts SQLSTATE 23514 + constraint name `device_loss_incidents_recovery_status_allowed`. T32-B2: `UnsyncedRiskIndicator` + `DurabilityGateModal` mounted in `AppShell` (existing ChainBreakAlert sibling-pattern); `useFiscalDurabilityPolling` polls every ≤30s, writes to Zustand `durabilityStore`; modal blocks UI when `forceArchiveRequired=true`; 1h grace period after operator acknowledgment; `durabilityServiceFactory` module-scope singleton mirrors Task 30 `getFiscalEventEngine` pattern. T32-P1: composite FK migration `2026_05_20_100002` adds unique key on `pos_terminals(id, tenant_id, company_id)` + composite FK `device_loss_incidents(terminal_id, tenant_id, company_id) → pos_terminals`; new PG test asserts SQLSTATE 23503 cross-tenant rejection. T32-P2: thresholds optional in `OffDeviceDurabilityConfig`; constructor normalizes via `normalizeThreshold()` to 50/500/3600; validates positive finite + escalated ≥ forced; `InvalidDurabilityThresholdError`. T32-P3: `sqliteTestAdapter` migrated `node:sqlite` → `better-sqlite3`; `describe.skip` guard removed; 342 vitest tests run unconditionally on Node 20. **Opus R2 re-review APPROVE — 0 new findings (0 BLOCKER / 0 P1 / 0 P2 / 4 P3 + 1 NOTE):** P3-A11Y modal needs focus trap + Escape handler; P3-GRACE-LAG selector reads Date.now() pure, grace expiry needs next polling tick (≤30s); P3-CLEAN dead `const d = describe;` indirection in test; P3-CONFIG-SOURCE Phase 2 will wire config from terminal/company. All non-gating. Tests: 13 PHP DeviceLossIncidentTest (8 PG-only skipped on SQLite) + 27 vitest (UnsyncedRiskIndicator + DurabilityGateModal + useFiscalDurabilityPolling + OffDeviceDurabilityService) + 273/273 full Fiscal feature suite + 342/342 POS fiscal-lib + 353/353 POS broader suite. PHPStan + Pint + typecheck + lint clean. §14.3 chokepoint gate still PASS. CI PG-merge-gate filter extended with `DeviceLossIncidentTest`. Codex round-2 re-review skipped (Codex sandbox-reliability lesson; R2 was mechanical-cleanup + new architecture passed Opus). |
| 31 | `a6be471ff` | `fiscal:verify-event-chain` command — walks `fiscal_events` for terminal in sequence_number order, re-hashes canonical_bytes via `HashChainIntegrityProvider` (Task 6), asserts current_hash + previous_hash linkage (first event → terminal genesis_seed), reports `fiscal_event_quarantine` rows as incidents. Permission-gated by `fiscal.events.verify_chain` (seeded in Task 24). Tenth Opus-subagent delegation. `--actor-id` + Spatie team-scoped check pattern from Task 24 R2. Inline fixture helpers (valid chain / tampered / quarantine_conflict). Plus flipped Task 26's defensive guard test (`test_fiscal_service_provider_does_not_yet_wire_…` → `…wires_verify_event_chain_command`). 12 tests (4 plan + 8 edge); 232/232 Fiscal feature suite green. PHPStan + Pint clean. CI PG-merge-gate filter extended adding `VerifyEventChainCommandTest`. No formal dual review (clean mechanical implementation, well-tested). |

### 4.2 Per-task ground rules (carried forward from Tasks 7–29)

1. **Per-task TDD + dual review is non-negotiable.** Codex has now caught BLOCKERs Opus missed on **fifteen of twenty-nine** tasks: 8 (×2), 14, 15, 16, 18, 19, 21, 22, 23, 24, 25, 26, 30, 32. **Task 32's dead-control-surface BLOCKER T32-B2 is the EXACT Task 30 dead-path-rebuild pattern recurrence on a fresh net-new component class** — UnsyncedRiskIndicator + shouldForceArchive shipped with no caller mount. Opus missed it again as "the component exists, it's good"; Codex caught it via the same "grep for callers" attack pattern. The dead-path-rebuild standing pattern (§4.2) has now been triggered TWICE on this branch — once on a rebuild (Task 30 `buildExport`) and once on net-new code (Task 32 `UnsyncedRiskIndicator`). **Tasks 17, 20, and 29 remain the three single-shot clean dual reviews** (Task 17 dual APPROVE; Task 20 dual APPROVE-WITH-MINOR-EDITS with both reviewers convergent on the same 2 P2 coverage gaps; Task 29 dual APPROVE-WITH-MINOR-EDITS on round-1, all P2s + P3s closed in one round-2 commit, Opus round-2 re-review APPROVE — 0 new findings). **Task 30 was the second three-round task in the streak (Task 23 was the first four-round task)** — round-2 introduced NEW defects under the Task 23/24/25 round-2 pattern (dead-path rebuild via parallel code, CI gate dependency drift, receiver-type validator too lax, quarantine partial); round-3 closed cleanly. Task 13 had both reviewers independently converging on the same hash-format GLOB BLOCKER; Task 19 had two independent convergences (PG-tx-abort + envelope-shape); Task 21 was heavy (6 BLOCKERs total, 3 closed-class regressions). **Task 22 contained the first cross-angle convergent BLOCKER** — both reviewers caught the provider-order silent-applied bug from different angles (Codex from the Task-23-lifecycle consequence; Opus from the wrong docblock claim about tagged-set order). Convergent-finding tally now: T13, T19, T20, T21, T22 (5 of 22). Convergent on PG-merge-gate filter (now 5 tasks: 10, 11, 19, 21, 22). **Standing code-smell patterns:**
    - `(type) $array['key']` PHP casts → silent coercion (Task 14 BLOCKER)
    - `payload: unknown` TS seams without runtime validation (Task 15 BLOCKER)
    - Free-form string fields without regex format validation (Task 15 P1-1, Task 16 P1 F6, **Task 19 BLOCKER T19-B4** — `FiscalEventEnvelope::fromArray()` needed regex-validation at the ingestor boundary)
    - **Wider-than-canonical parse / accept grammar** (Task 16 4-BLOCKER pattern, Task 19 BLOCKER T19-B4 — malformed inputs must be rejected at the typed-boundary, not at PG CHECK)
    - **DTO::fromArray as a "complete schema check"** (Task 16 BLOCKER F2)
    - **Resolver / downstream-service exceptions must fail-closed** (Task 18 BLOCKER F1)
    - **Boot-time invariants must be constructor-asserted** (Task 18 P2 F2/F3)
    - **DB primitives that the spec names by-the-statement are load-bearing — DO NOT substitute "functionally equivalent" alternatives** (Task 19 BLOCKER B1/T19-B1 — `try/catch (QueryException)` aborts the PG transaction; the spec mandated raw `INSERT … ON CONFLICT DO NOTHING RETURNING id` specifically. The same BLOCKER was caught and closed in spec round-4; reintroducing it on the implementation side is a regression of a closed BLOCKER.)
    - **Multi-constraint tables need constraint-name-targeted ON CONFLICT** (Task 19 BLOCKER T19-B2 — `fiscal_events` has two unique constraints; an untargeted `ON CONFLICT` would catch the wrong one for `source_event_class/id` violations)
    - **Verify the premise of every deferral before accepting it** (Task 19 BLOCKER T19-B3 — round-1 subagent claimed no server-side genesis-seed mirror; Codex verified by reading the actual migration that `pos_terminals.genesis_seed` exists. The premise was wrong; the deferral was wrong.)
    - **Legacy verify/audit infrastructure must keep working on relocated rows** (Task 21 BLOCKER F1/B5 — `pos:verify-chains` was still wired in `POSServiceProvider::boot()` and `ReceiptHashService::serializeForHashing` folded `vat_breakdown_hash` + `payment_methods_hash` into chain input; round-1 wrote `str_repeat('0', 64)` sentinels → every projection-written row would trip the chain-broken alarm in production). When relocating logic from a legacy write path to a new one, audit the legacy verify/integrity infrastructure that still inspects the relocated columns. Either compute the real values the verifier expects, OR carve out the new rows from the verifier — **never ship known-fake sentinels into columns the verifier reads.** Task 21 round-2 closure: both mitigations applied — real per-section hashes computed AND the verifier widened to skip `whereNotNull('fiscal_event_id')`. Sweep pattern: grep the touched column names against the entire module's command/service surface before shipping.
    - **D8 two-model coexistence requires the spec §14 disposition as the load-bearing test, not the implementer's intuition** (Task 21 BLOCKER T21-B1 round-2 adjudication — Codex flagged the active-projector + still-live-legacy-endpoint pair as D8; Opus said acceptable; round-2 resolved by re-reading spec v7 §14 table, which explicitly tags `ReceiptSyncService` business logic as "REUSE (relocated → `PosCoreReceiptProjection`)" with §14.1 retirement routed to Task 28 and §14.2 new-sale disposition routed to Task 29. D8 forbids two *canonical* paths; here the canonical path is the projector, the legacy is a retiring transport. Pattern: when D8 coexistence is alleged, the first check is spec §14's disposition table — if the legacy path is dispositioned as "knowingly retained no-new-writers" with retirement routed to a later task, deprecation docblocks suffice; otherwise physical removal or feature-flag is required.)
    - **Projector dispatch order must be explicit, not inherited from provider registration** (Task 22 CONVERGENT BLOCKER T22-B1 / Opus F3 — Codex caught the Task-23-lifecycle consequence + Opus caught the wrong docblock claim). `bootstrap/providers.php` registers Treasury before POS (insertion-order Treasury → Fiscal → POS); Laravel's `app->tagged()` returns the tagged set in insertion order; round-1 Treasury bridge therefore fired BEFORE the POS-core projector → bridge found no `pos_receipts` row → bailed silently → Task 23 would have marked the projection row applied → Treasury Payment + GL never created (production data loss). The round-1 deferred-bail-out test masked the bug by asserting "no crash" instead of asserting the lifecycle state machine outcome. **Fix pattern, now LOCKED in the interface:** `FiscalEventProjector::priority(): int`. Registry sorts by `(priority ASC, name ASC)` at construction with negative-priority `LogicException` boot validation (Task 18 constructor-asserted-invariants pattern). POS-core projectors = priority 50; module bridges = priority 150. **Going forward:** any projector that depends on another projector's writes must declare a higher `priority()` value. Do NOT rely on provider order or alphabetic sorting as the load-bearing contract.
    - **Projector idempotency on non-UNIQUE keys requires a PG advisory lock, not just check-then-insert** (Task 22 BLOCKER B2 / Codex F1 — round-1's docblock claimed Task 23 would own a `lockForUpdate()` on `fiscal_event_projections`; premise verified false against Task 23 plan §1705+). When a projector writes N rows keyed on `(fiscal_event_id, projector_name)` to a table with NO UNIQUE constraint on `fiscal_event_id` (deliberate per Task 12 — one event → N Payment rows for split payments), the check-then-insert race window is real and must be closed at the projector level. **Canonical pattern**: wrap `apply()` body in `DB::transaction(fn () => { DB::statement('SELECT pg_advisory_xact_lock(hashtext(?))', [$event->id . ':' . $this->name()]); /* check-then-insert */ })`. SQLite-driver-guard via `DB::getDriverName() === 'pgsql'` for the test runner. The advisory lock auto-releases on transaction end. Task 23's row-level lock (when it ships) layers on top — two-layer defense.
    - **NULL-origin inheritance fails closed to a canonical sentinel** (Task 22 BLOCKER B2 / Codex T22-B2 — round-1's refund writers inherited `origin` from the original `Payment` row; pre-Task-12 legacy rows have `origin = NULL` → refund row silently NULL; untested. Canonical sentinel: `PaymentOrigin::UnknownLegacy` (the enum case already existed in Task 12). Pattern: `$originalPayment->origin ?? PaymentOrigin::UnknownLegacy`. Combine with a backfill migration when the column is added so steady-state data is clean. Pin every inheritance writer with a test for the NULL case. **Going forward:** any "inherit from upstream" semantic on an enum column needs an explicit NULL-fallback sentinel + a test pinning it; never silently propagate NULL.
    - **Class-level `markTestSkipped` is too broad when the class contains orthogonal assertions** (Task 29 Codex T29-F2 + T29-F4). When a test class is skipped on the rationale "the route this test exercised is retired", the class-level skip drops EVERY assertion in the class — even ones that don't hit the retired route (e.g. seeder/permission contracts, direct service-bypass assertions). Per-method skips are the correct granularity. Pattern: `markTestSkipped` at the top of each route-dependent method body; leave non-route assertions LIVE. Going forward: when dispositioning a route, every method in a class that exercised the route gets reviewed for whether it was *actually* exercising the route or testing an orthogonal contract that happened to use the same class as a scaffold.
    - **Skip-citation accuracy — the cited surviving owner must actually own the assertion** (Task 29 Codex T29-F3). When a test is skipped because "the assertion moved to X", grep-verify X actually enforces the assertion. Round-1 cited `FiscalPayloadConstraintValidator` as the surviving owner of instrument-binding validation, but the validator only enforces monetary shape — the actual defense lives at `PosCoreReceiptProjection::writePayments` (via `InstrumentRequiredException` + `PaymentInstrumentKind::requiresInstrumentForMethodCode`). Pattern: when writing a skip rationale, the reviewer's first job is to open the cited owner and confirm the assertion is grep-discoverable there. Combine with: when the cited owner's coverage is partial, ADD the test (don't claim coverage that doesn't exist).
    - **Test-coverage assertion: "not 410" is not "happy path"** (Task 29 Codex T29-F1, a specialization of Task 22 deferred-bail-out test smell). When a route is dispositioned as knowingly-retained, the carve-out test must EXERCISE the happy path (seed eligible state + send actual payload + assert 2xx + assert DB side effects), not just assert the absence of the disposition code. Pattern: any test in a disposition-completeness matrix that uses `assertSuccessful` on an empty/minimal payload is suspect — the implementer likely shaped the test to prove "not retired" rather than "still works". Reviewer rule: search for the absence-of-symptom shape (`->status() !== 410`, `assertSuccessful` with empty payload, `error.code !== 'X'`) and require the presence-of-correct-behavior shape instead.
    - **Dead-path rebuild — adding NEW code without rewiring the LIVE path is a rebuild masquerading as a rewrite** (Task 30 Codex T30-B1 + T30-R2-B1). When the spec calls for a rebuild (e.g. "read canonical_bytes, not model fields"), the implementer must identify the LIVE entry points (the controller / service / job that runs in production) and re-route THOSE to the new logic. Adding a new method with the right behavior that has zero callers is a dead-path rebuild — the production data path still flows through the old, unchanged code. Side-channel verification (verify the data but don't use it) is not enough — the data path itself must source from the new place. Reviewer rule: when a task ships a "rebuild" or "rework", grep for every caller of BOTH the new method and the legacy method; if any production caller still reaches the legacy method without an explicit retirement disposition, the rebuild is not closed.
    - **Receiver-type validation needs the specific call-site receiver expression, not just "calling_class has SOME property of type X"** (Task 30 Codex T30-R2-P2). Multi-dependency classes (e.g. `ExchangeService` with both `ReceiptCreationService` and `ReceiptFinalizationService`) can pass a permissive "any property of declared type" check while having a manifest entry with WRONG receiver_type for the specific call site. Pattern: parse the receiver expression (e.g. `$this->receiptCreationService->createReceipt(`), look up THAT specific property on `calling_class` via reflection, assert ITS type matches manifest's `receiver_type`. Negative test fabricating a wrong receiver_type for a multi-dep class MUST be present.
    - **CI gate extension must update the job's dependency setup in the same commit** (Task 30 Codex T30-R2-P1). When extending a CI gate's mechanism (e.g. adding a PHP reflection helper to a previously shell-only gate), update the CI job's `setup-*` + dependency install steps IN THE SAME COMMIT. A clean checkout has no vendor dir / no PHP / no jq / etc.; verify the gate would actually run on a fresh runner. Reviewer rule: when a gate calls a new binary or runtime, grep the CI workflow file to confirm the job's `runs-on` + `steps` include the prerequisites.
    - **§8 quarantine completeness — BOTH the partition table AND in-table quarantined rows count** (Task 30 Codex T30-R2-P3). The non-admissible quarantine partition (`fiscal_event_quarantine`) is one source; admissible-but-quarantined `fiscal_events` rows (`payload_parse_status='failed'` or `integrity_status != 'verified'`) are the other. Auditor visibility per spec §8 requires both. Pattern: UNION both queries with a `source` tag (`'quarantine_table'` vs `'fiscal_events_in_table'`) when assembling the quarantine_section.
    - **Mockery `Log::shouldReceive` is an anti-pattern in this codebase — use `Log::spy() + shouldHaveReceived` instead** (Task 30 Codex T30-R2-P4). Project memory pins: "Backend tests use RefreshDatabase + real Eloquent models + RolesAndPermissionsSeeder; never fake API responses." `Log::shouldReceive` brings Mockery into the test setup needlessly. `Log::spy()` + `Log::shouldHaveReceived('error')->withArgs(callable)` is the Laravel-native equivalent and integrates cleanly without the Mockery cognitive load.
2. **CI PG merge-gate filter must be extended at the same commit any new fiscal table-shape test ships — server-side PHPUnit only.** Tasks 10/11/19/21/22 reviewers all flagged this regression. Pattern: add the test class name to `.github/workflows/ci.yml` line ~404 + extend the comment block. **Device-side Vitest tests (Task 13+) are picked up automatically by `apps/pos pnpm test` in the existing pos-test job (`.github/workflows/ci.yml:461`); no filter change needed for device tests.**
3. **`$fillable` boundary discipline.** Lifecycle/state-machine columns belong out of `$fillable` (Task 9 lesson). Identity / insert-time FK columns are fine to include (Task 11 CLEAN-2 verified — both Receipt and FiscalEventProjectionRow follow this pattern).
4. **Codex wrapper-summary may diverge from the actual review file.** Trust the file (`docs/superpowers/reviews/...-codex-review.md`) as source-of-truth; the wrapper summary returned by the agent runtime is generated from a different prompt path and can mis-classify severity.
5. **Plan-vs-code wording drift on `hash_sequence` vs `chain_sequence`.** Plan says `hash_sequence`; actual `pos_receipts` column is `chain_sequence`. Migration / commit message correctly use `chain_sequence`; do not propagate the plan's wording. (Task 11 Codex CLEAN-6.)
6. **Schema-destructive preflight gate is OPEN.** No further human checkpoints in the remaining tasks unless a new schema-destructive surface surfaces.

### 4.3 Open items going into the next session

- **Task 14 standing pattern:** the `FiscalPayloadArrayGuards` helper is the canonical pattern for DTO `fromArray()`-style construction (server PHP). The `(type) $data['key']` cast pattern is treated as a code smell going forward.
- **Task 15 standing pattern:** at the device-TS boundary, `payload: unknown` seams MUST be paired with per-event-type runtime validation. The new `FiscalEventEngine.validateRequestPayload` switch is the canonical pattern. Typed-input interfaces (`SaleReceiptPayloadInput` etc.) provide compile-time defense; the runtime validator is the second line. Free-form string fields (`event_time_device`, `business_date`) MUST be regex-validated at the boundary.
- **Task 16 standing patterns (3 new):**
  - **DTO::fromArray is a partial schema check, not a complete one.** It validates required keys + types; the parser at the canonical-bytes boundary additionally rejects EXTRA keys via a `PAYLOAD_KEYS` map. (Codex BLOCKER F2.) Any future parser that sits at a canonical boundary MUST mirror this.
  - **The accepted grammar of a strict parser cannot be wider than the canonical contract.** Whitespace tolerance, optional fields beyond the spec, weakly-typed numbers, etc., all widen the parser past what canonical bytes should contain — they turn what should be `canonical_parse_failure` quarantines into accepted parsed payloads. (Codex BLOCKER F1+F3+F4.)
  - **Scale-aware money regexes** are the canonical PHP-side enforcement of spec §4's "money as `CurrencyScale::bcformat()` decimal strings". The `moneyRegex($scale)` pattern is `^-?(0|[1-9]\d*)(\.\d{N})?$` (no fraction part when scale=0). `is_string()` alone is insufficient — `"not-money"` is a string.
- **Task 15 deferred Opus P3 follow-up:** unused `defaultDb` ctor param + `db` getter (Opus P3-1); `Database | SqlSurface` union → single structural `SqlSurface` (Opus P3-2); `findBySource` could use the same tenant-scope comment `readChainHead` already has (Opus P3-3). Group into a single cleanup commit when convenient (no urgency — none of them change behavior).
- **Task 16 deferred items (low urgency):** Opus P2-4 (forensically clearer error string on envelope-as-empty-list), Opus P3-3 (belt-and-braces mb_chr false check — phpstan stub says return is `string`, check would be unreachable on validated range), Opus P3-4 (forensic prefix on payload-as-list — already covered by `F1 payload_not_object:got=list`), Codex P2-7 NFC normalization (left as producer-side per spec §4 wording — "stripped at the producer"). Tail-end cleanup commit.
- **Task 17 standing pattern:** PHP-side defensive contracts must wrap `Eloquent::find($uuid)` in `try/catch (QueryException)` when the docblock pins "never throws". On PostgreSQL the `uuid`-typed primary-key column rejects malformed-UUID strings with a driver-level `QueryException`.
- **Task 18 standing patterns (2 NEW):**
  - **Fail-closed on resolver / downstream-service exception.** Any code path that's part of (or feeds into) the ingest path must catch downstream exceptions and apply the documented "missing" / "inactive" sentinel — never propagate. Spec §7.2 line 380 + §7.5 line 431 require the fiscal event row to always be persisted and the device never blocked. Pattern: `try { … resolver->isActive(…) … } catch (\Throwable $e) { Log::error(…, [structured context]); /* fail-closed action */ }`. Task 18 BLOCKER F1 closure.
  - **Registry / config-shape invariants must be constructor-asserted.** Anything that would surface as a runtime DB constraint violation, silent-no-op, or `false`-for-bad-input downstream should fast-fail at construction time with a `LogicException` naming the violation. Tasks 18 P2 F2 (duplicate projector names → UNIQUE constraint violation at Task 24) and F3 (empty `requiresModule()` → silent strict-compare-false) closures.
- **Task 19 standing patterns (3 NEW — these are HOT, they each came from a Codex BLOCKER missed by Opus AND missed by the round-1 subagent):**
  - **DB primitives the spec names by-the-statement are load-bearing.** Don't substitute a "functionally equivalent" alternative without verifying the equivalence on the target driver (the spec mandates raw `INSERT … ON CONFLICT … RETURNING id` precisely because the try/catch alternative aborts the PG transaction — empirically reproduced on PG 15.15). When a spec line reads "the atomic primitive," treat it as a contract obligation, not a hint.
  - **Multi-constraint tables need `ON CONFLICT ON CONSTRAINT <name>` targeting.** `fiscal_events` has TWO unique constraints (sequence-slot + source-event). An untargeted ON CONFLICT catches whichever fires first — which is not always the one the conflict-handling code is shaped for. Always name the constraint explicitly.
  - **Verify the premise of every deferral before accepting it.** The round-1 subagent claimed "no server-side genesis-seed mirror exists" — Codex verified by reading the actual migration that `pos_terminals.genesis_seed` was already there. The deferral was unjustified; the BLOCKER was inside the verification gap, not the architecture. Pattern: when a subagent flags an "intentionally deferred" check, the reviewer's first job is to grep-confirm the premise.
- **Task 20 standing pattern (NEW — dual-review-convergent coverage gap):** any controller wrapping a service that returns a discriminated-union DTO MUST have endpoint tests for every variant of the union. `IngestionResult` has five factories (`stored` / `quarantined` / `idempotent` / `sequenceConflict` / `malformedEnvelope`); Task 20's round-1 covered only 3 of 5 at the HTTP layer (stored, sequenceConflict, malformedEnvelope). Both reviewers independently flagged the same 2 coverage gaps. Going forward: when a Task wraps a service returning a discriminated union, write the endpoint-test matrix exhaustively in round-1, not after a review tells you which were missed.
- **Task 20 standing pattern (NEW — stale-comment hazard):** when removing a defensive catch / wrapper during refactoring, also remove or rewrite the comment that referred to it. Task 20 left a stale prose block at the ingest loop that promised a "defense-in-depth catch around `ingest()`" — but the catch had been removed earlier in development. The comment outliving the code is a documentation drift that future readers (and reviewers) will flag. Pattern: comment + behaviour must move together.
- **Task 21 standing patterns (2 NEW — both HOT, each from a Codex BLOCKER missed by Opus OR an Opus BLOCKER missed by Codex):**
  - **Legacy verify/audit infrastructure may inspect relocated columns** (Opus F1/B5 — `pos:verify-chains` + `ReceiptHashService::serializeForHashing`). When relocating from legacy to new write path, sweep the legacy verify/integrity infrastructure for column reads. Either compute the real values OR carve the new rows out of the verifier. Never ship known-fake sentinels into columns the verifier reads.
  - **D8 coexistence adjudication starts with spec §14's disposition table** (Codex T21-B1 round-2 adjudication). When two write paths for the same model class are alleged to violate D8, the load-bearing check is spec §14 — if the legacy path is dispositioned as "knowingly retained no-new-writers" with retirement routed to a later task, deprecation docblocks suffice. Otherwise physical removal or feature-flag required. The implementer's intuition is not the load-bearing test; the spec is.
- **Task 21 standing patterns regressed (these were closed-class patterns that came back):**
  - Task 19 check-then-insert race re-introduced at `PosCoreReceiptProjection::apply` round-1; Codex T21-B3 caught; round-2 closed with atomic `ON CONFLICT ON CONSTRAINT pos_receipts_fiscal_event_id_unique DO NOTHING RETURNING id`.
  - Task 19 untargeted ON CONFLICT principle applied — `pos_receipts` does have multiple UNIQUEs (`fiscal_event_id` + `receipt_number`); constraint named explicitly to avoid swallowing the wrong conflict.
  - Task 20 discriminated-union test matrix gap re-introduced — round-1 covered only happy-path single-payment; round-2 added split / voucher / stock / voucher+stock / zero-payment / voucher-rollback variants. **Meta-lesson:** when a projector or controller consumes a discriminated-union payload, write the test matrix exhaustively in round-1.
- **Task 21 fail-closed cross-tenant FK posture change (round-2 F3):** the projector now raises a fail-closed `RuntimeException` inside the transaction when `payment_method_id` references a different tenant's row. This was a behavior change from round-1's silent-mirror (with an inaccurate docblock claiming "FK rejection"). The next reviewer should validate the security stance — alternative is to route to `fiscal_event_projections` failed status for operator review (deferred — current stance is fail-closed-in-projection, not dead-letter-on-cross-tenant).
- **Task 21 pre-existing SCALE drift (round-2 F6 docblock-only):** `Receipt::$casts` is `decimal:3` but the migration column is `decimal(12, 2)`. Out-of-scope cleanup; docblock disclosure shipped. A future task should align the model cast with the migration column scale.
- **Task 22 standing patterns (3 NEW — all HOT, all from this task's round-2 closures):**
  - **Projector dispatch order must be explicit via `priority()`, not inherited from provider registration.** The `FiscalEventProjector::priority(): int` interface contract is now load-bearing. POS-core = 50; module bridges = 150. Registry sorts by `(priority ASC, name ASC)` at construction with negative-priority boot validation. ANY new projector MUST declare a `priority()` and the value must respect its dependencies on other projectors' writes.
  - **Projector idempotency on non-UNIQUE keys requires `pg_advisory_xact_lock` inside the wrapping transaction.** When a projector writes N rows keyed on `(fiscal_event_id, projector_name)` to a table with no UNIQUE on `fiscal_event_id`, the canonical pattern is `DB::transaction(fn() => { DB::statement('SELECT pg_advisory_xact_lock(hashtext(?))', [$event->id . ':' . $this->name()]); /* check-then-insert */ })` wrapped with `DB::getDriverName() === 'pgsql'` guard for the SQLite test runner.
  - **NULL-origin inheritance fails closed to canonical sentinel.** `PaymentOrigin::UnknownLegacy` is the sentinel; the round-2 backfill migration (`2026_05_17_120000_backfill_legacy_payment_origin.php`) closes Task 12 spec drift. Pattern: `$originalPayment->origin ?? PaymentOrigin::UnknownLegacy` + a test pinning the NULL case.
- **Task 22 deferred items (worth checking on next review session):** none. All P2/P3 items closed in round-2 single commit.
- **Task 22 cross-task wiring implication for Task 23 — RESOLVED IN TASK 23 ROUND-1 + AMENDED PLAN.** Plan §1772 amended pre-Task-23-dispatch to add row-level `lockForUpdate()` lifecycle lock + idempotent terminal-state short-circuit. Two-transaction shape: T_lock (lockForUpdate + flip to Running, SHORT, committed BEFORE apply) + T_apply (projector's own). Codex round-1 caught that the lock alone wasn't enough — added `WithoutOverlapping` middleware in round-2.
- **Task 23 standing patterns (4 NEW — each came from a Codex BLOCKER caught across 4 rounds):**
  - **Row-level lifecycle lock alone doesn't serialize concurrent execution** (T23-B1 round-1). `lockForUpdate()` only serializes the brief status flip — once T_lock commits, sibling worker can acquire the lock, see Running, proceed into apply concurrently. Need queue-level uniqueness (`WithoutOverlapping` middleware keyed by row id). Pattern: when adding a row-level lock for idempotency, ask whether the lock window covers the actual long-running operation; if not, add a queue-level uniqueness primitive.
  - **Cross-task wiring premises must be verified per-round** (T23-B2 round-1). TreasuryReceiptBridge round-1 (Task 22) silently returned clean when pos_receipts missing — survived Task 22 review because the bug only surfaced under Task 23's lifecycle (job marks Apply on clean return). Task 22's `priority()` ordering only fixed enqueue order, not execution order across workers. Pattern: when Task X's behavior depends on Task Y's effects, the test for Task X must drive Task Y's failure mode end-to-end. Bridge-style components should fail-LOUD on missing dependencies (typed exception → retryable) rather than silent return.
  - **Reset-to-Pending on failure is the canonical Horizon-retry pattern** (R2-B1 round-2). Leaving status=Running between Horizon retries trips any stale-running guard that uses time predicates. After every failure, reset to the pre-attempt status so the next retry's T_lock proceeds normally. Apply symmetrically to ALL failure-accounting code paths (both `advanceFailureAccounting` + `recordHardFailure` had the bug; fix both in one commit). Pin with `Carbon::setTestNow` between attempts.
  - **`WithoutOverlapping->releaseAfter()` vs `dontRelease()`** (R2-B2 round-2). With Redis default `retry_after=90s`, `dontRelease()` permanently DROPS a duplicate delivery hitting a stale-held lock (worker crashed mid-job). Use `releaseAfter($timeout + buffer)` to re-queue the duplicate with delay so it survives the lock TTL window. Document a runbook requirement that `retry_after > expireAfter + buffer`.
  - **Cache-lock-fails-open is a real failure mode** (R3-P1-1 round-3). `WithoutOverlapping` is the primary defense; the in-flight guard inside T_lock is belt-and-braces. The guard's null-path on `last_attempted_at` MUST not leave the door open — stamp `last_attempted_at = now()` at the Running flip so the guard correctly identifies in-flight rows even when the cache lock has failed open.
- **Test discipline lesson (round-2 → round-3):** the deferred-bail-out test smell (Task 22 standing pattern) struck again at R2-B1. The cross-worker race test passed sequentially via `runJobInline` without `Carbon::setTestNow` between attempts, masking the time-predicate guard that would have triggered in Horizon's real retry. **Rule:** when testing Horizon retry behavior, ALWAYS use `Carbon::setTestNow` to advance time between attempts. Sequential same-clock calls don't exercise time-based predicates.
- **Task 29 standing patterns (3 NEW — all surfaced from Codex round-1 review).** All folded into §4.2 above; quoted here for emphasis:
  - **Class-level `markTestSkipped` is too broad** when a class contains orthogonal assertions (T29-F2 + T29-F4). Per-method skips are the correct granularity.
  - **Skip-citation accuracy** (T29-F3). Grep-verify that the cited surviving owner actually enforces the assertion.
  - **"Not 410" is not "happy path"** (T29-F1, specialization of Task 22 deferred-bail-out smell). Knowingly-retained disposition tests must exercise the happy path with eligible state + actual payload + 2xx + side-effect assertions.
- **Task 30 standing patterns (5 NEW — all surfaced from Codex round-1 BLOCKER + round-2 NEW findings). All folded into §4.2 above; quoted here for emphasis:**
  - **Dead-path rebuild** (T30-B1 + T30-R2-B1). NEW code with no live callers ≠ rewrite. Identify the LIVE controller/service/job; rewire it.
  - **Receiver-type validation specificity** (T30-R2-P2). Multi-dep classes need property-specific check, not class-has-some-property-of-type-X.
  - **CI gate extensions update job deps in same commit** (T30-R2-P1).
  - **§8 quarantine completeness needs BOTH partition table + in-table quarantined rows** (T30-R2-P3).
  - **`Log::spy + shouldHaveReceived` over Mockery `Log::shouldReceive`** (T30-R2-P4).
- **Task 30 deferred items (3 P3s from Opus round-3, non-blocking — fold into a Task 30B cleanup commit when convenient):**
  - **P3-1:** Line-level data (`mapLine`/`mapVatDetail`/`mapPayment`) still mirror-sourced. Round-3 closed receipt-level (top-line `total` / `vat_total`) sourcing but did not extend to line items + VAT details + payments. Out of scope of round-2's `pos_receipts.total`-level ask, but worth closing for full D1 compliance.
  - **P3-2:** `Log::shouldNotHaveReceived('error', [...])` array-arg form in the structured-log test re-introduces `\Mockery::any()` cosmetically (doesn't undo the spy migration, but a polish miss).
  - **P3-3:** `conflicting_event_id` for `sequence_conflict` quarantine rows is loaded into the DTO array but not emitted as XML element. Spec §8 wording is ambiguous on this — defer until auditor consultation.
- **Task 29 deferred items (worth tracking forward):**
  - **Opus F1 (cross-tenant FK coverage gap on 6 non-payment-method axes).** `PosStabilizationTenantIsolationTest` skips for `product_id` / `customer_id` / `contact_id` / `modifier_group_id` / `modifier_id` / `composite_item_id` claim coverage moves to `PosCoreReceiptProjection` (Task 21) — but `PosCoreReceiptProjectionTest` only covers `payment_method_id` explicitly. Defense exists at DB-FK level (those columns FK to tenant-scoped tables Task 21 verified); the explicit projector-level cross-tenant assertion for the six axes is queued for **Task 22/30 projection-coverage extension**. Not a Phase 1 regression because the route is 410; the attack vector is gone for now.
  - **Opus F2 (route closures inherit `auth:sanctum` → anon gets 401 not 410).** By design: the disposed routes are authenticated POS/API surface. Inline comments shipped in `routes.php` + `routes_orders.php` documenting the contract. Both reviewers acknowledged; not escalated. Future Phase-2 work could move the closures outside the auth group if external HTTP semantics matter.
- **Subagent strategy:** Phase 1 used Opus/Codex second-pass review heavily. Task A/B/C continuation used Codex implementer + Codex first-pass review + Opus-equivalent second-pass review. Task 27B Pass 2B required four defect-fix commits after second-pass findings; Task 33 second pass returned APPROVE-WITH-MINOR-EDITS with wording/fidelity residuals only.
- **Phase 1 task status (33 of 33 shipped or absorbed, post-Task-33):**
  - **Shipped/closed:** 1-27 Pass 1, 27B Pass 2A/2B, 29, 30, 31, 32, 33.
  - **Absorbed:** Task 28 was folded into 27B Pass 2B: `/pos/receipts/sync`, `ReceiptSyncService`, `SyncReceiptPayload`, `SyncReceiptsRequest`, and `SyncReceiptResult` were deleted.
  - **Next:** Phase 1.5 mirror-column audit/drop + other cleanup items before any Phase 2 customer-facing deployment.
- **Recommended next-session order:** finish Phase 1.5 cleanup or create/merge the PR to dev, depending on owner direction. Phase 1 implementation itself is complete and pushed.
- **5-task boundary memory refresh.** `project_pos_fiscal_event_engine.md` refreshed after Task 33 completion in this session. The next memory update should track Phase 1.5 / merge work, not further Phase 1 implementation tasks.

### 4.4 Worktree + CI status

- Working directory: `/Users/houssamr/Projects/syneriva/apps/erp.fiscal-phase1` (dedicated git worktree). Main `apps/erp/` is on detached HEAD; do NOT cd into it (parallel cleanup session may claim branches).
- The worktree has its own APFS-cloned `apps/api/vendor` (the original symlink resolved PSR-4 baseDir through the main worktree, masking new files; replaced with a real copy in this session). Phpstan/pint/phpunit work directly from the worktree.
- Latest Task 33 verification: targeted `Task33FiscalFullFlowVerificationTest` PASS (1 test / 41 assertions), fiscal/POS PHPUnit subset PASS (546 tests / 2005 assertions / 53 skipped), `pnpm typecheck` PASS, `pnpm lint` PASS with existing warnings only, `pnpm test` PASS (POS + web Vitest suites), PHPStan L8 PASS, Pint PASS, Pass 2B sentinel PASS, §14.3 chokepoint gate PASS, dead-path grep PASS.
- PG-only smoke runs only happen in CI (the local SQLite runner skips them with `markTestSkipped`). The CI filter at `.github/workflows/ci.yml:~404` includes (post-Task-32): adds `DeviceLossIncidentTest` to the existing post-Task-30 set. Full filter list post-Task-30: `VoucherLedgerTest|VoucherLedgerAppendOnlyTest|VoucherSchemaTest|FiscalHardeningE2ETest|FiscalEventsTableTest|FiscalEventsImmutabilityTest|FiscalEventProjectionsTableTest|FiscalEventQuarantineTableTest|PosReceiptsCanonicalBytesTest|PaymentsOriginColumnsTest|OutboxIngestorTest|FiscalEventIngestionEndpointTest|PosCoreReceiptProjectionTest|TreasuryReceiptBridgeTest|PaymentOriginWriterInventoryTest|ParseFailureResumeTest|VerifyEventChainCommandTest|NewSaleServerAuthoringDispositionTest|ChokepointCompletenessTest|ReceiptChainRebuildTest`. Plus a new dedicated `chokepoint-gate` CI job (lightweight: `rg + jq + bash` only, `SKIP_RECEIVER_TYPE_VALIDATOR=1` env var; the PHP validator runs in `backend-test-pgsql` which already has vendor). **Task 23 `ApplyFiscalEventProjectionJobTest` NOT added** — the 18 tests assert lifecycle semantics in single-worker scenarios; `lockForUpdate()` + `WithoutOverlapping` cache-lock are exercised but tests don't depend on actual concurrent locking (PG driver behavior identical to SQLite for the assertions). Documented in Task 23 round-1 implementer report + Codex round-1 review CLEAN section.

---

## 5. Findings history (resolved — do not reopen)

- **v1** → Codex BLOCK: 3 BLOCKER / 3 P1 / 3 P2 → v2 resolved all 9.
- **v2** → Codex BLOCK: 2 BLOCKER (projection based on false source; projection failure semantics undefined), 1 P1 (`ON CONFLICT` treated non-identical conflicts as success), 1 P2 (`/pos/receipts/sync` retirement surface) → v3 resolved.
- **v3** → Codex BLOCK: 1 BLOCKER + 3 P1 + 2 P2 → **v4 resolved all 6**:
  - **BLOCKER** — bounded-modules seam not buildable on the current payment-method boundary. Resolved **by correcting the model, not removing the FK**: the asymmetric seam (§2.2) — inbound mirrored reference data is permitted; the `ModuleActivationResolver` gates the outbound Treasury operational dependency. `payment_methods` is deliberately **not** relocated.
  - **P1** — web-POS disposition named no concrete write paths → new §14.2 checklist.
  - **P1** — `sequence_conflict` quarantine not verbatim enough → `fiscal_event_quarantine` expanded with raw envelope + full metadata columns.
  - **P1** — parse-failure projection resume had no mechanism → §7.5 resolver contract + `fiscal:enqueue-resolved-event-projections` command.
  - **P2** — `/pos/receipts/sync` retirement omitted classes → §14.1 expanded with the concrete old-sync classes + integration tests.
  - **P2** — projection retry/dead-letter state underspecified → §7.5 names Laravel/Horizon as owner of transient state; `fiscal_event_projections` gains `running`/`dead_lettered` + operational timestamps.
- **v4** → Opus adversarial review APPROVE-WITH-MINOR-EDITS: 0 BLOCKER / 1 P1 / 2 P2 / 2 P3 → **v5 applies all 5**:
  - **P1** — `ModuleActivationResolver` / `requiresModule()` used lowercase `'treasury'`, but `Vertical::defaultModules()` uses PascalCase `'Treasury'` and `CompanyConfig::hasModule()` strict-compares → bridge would never run. v5 pins the canonical `'Treasury'` token + §17.3 test-locks it.
  - **P2** — §14.2 web-POS disposition omitted the `void` / `processReturn` server-authoring paths → v5 names them as **knowingly retained server-side for Phase 1** (their event types are Phase 2+ reserved), scopes the "does not coexist" claim to new-sale authoring.
  - **P2** — parse-failure resume transaction boundary undefined → v5 §7.5 specifies `payload` write + status flip + projection-row inserts in one transaction, enqueue after commit; `fiscal:enqueue-resolved-event-projections` named the recovery path.
  - **P3** — `ReceiptPaymentService` mis-attributed wholesale to the Treasury bridge (it also creates the POS-core `ReceiptPayment` row at `:297`) → v5 §7.4/§14 specify the **split**: `ReceiptPayment::create` → POS-core; Treasury `Payment` + GL → bridge.
  - **P3** — `/pos/receipts/sync` feature-suite scope was a glob → v5 §14.1 names the concrete suites.
- **v5** → Codex BLOCK: 1 BLOCKER / 1 P3 → **v6 resolves both**:
  - **BLOCKER** — the Tauri POS `executeCheckout()` has an `onlineCheckout()` branch that server-authors `SALE_RECEIPT` (`POST /pos/receipts` + `/pos/receipts/{id}/payments`) whenever connectivity is online — v5 dispositioned only the *browser* web-POS path, leaving the *physical* terminal server-authoring online (two-model coexistence, D8 violation). v6 makes device authority **connectivity-independent + terminal-type-independent** (§5.0), adds §14 disposition rows for `offlineCheckoutService.ts` (REWORK — `onlineCheckout` branch DISCARDED) + `apps/pos/src/api/receiptApi.ts`, reframes §14.2 to cover web *and* Tauri-online callers, adds §17.5 proving tests. Behavior change: online Tauri checkout → device-authored-then-sync.
  - **P3** — §14.1 feature-suite list still not exhaustive → v6 adds `OfflineV3CutoverSyncTest.php` + classifies `PosStabilizationTenantIsolationTest.php` as a broader stabilization suite (migrate its receipt-sync assertions only).
- **v6** → Codex BLOCK: 1 BLOCKER → **v7 resolves it + adds a structural fix**:
  - **BLOCKER** — `POST /pos/orders/{id}/close` → `OrderToReceiptService` → `ReceiptCreationService::createReceipt()` is *another* server-authoring `SALE_RECEIPT` path v6 missed. Verified `apps/web`-only (no Tauri caller) → dispositioned with the web POS (§14.2; order management → §18 deferred parity).
  - **STRUCTURAL FIX** — v5 and v6 were the same class of defect ("a missed server-authoring caller"). v7's new **§14.3** dispositions the **two chokepoints** — `ReceiptCreationService::createReceipt()` + `ReceiptFinalizationService::finalize()` — enumerates every production caller (grep-verified), and makes completeness a **CI grep** of the two methods. This breaks the whack-a-mole: a missed caller now fails CI rather than surfacing in the next review round.
- **v7** → Codex re-review **APPROVE — 0 findings**. Codex independently grep-verified the §14.3 chokepoint enumeration as exhaustive, ran a direct-`Receipt`-construction bypass scan (none found), and confirmed no regression. **Phase 1 spec gate passed.**

---

## 6. Process for the next session

Every adversarial-review prompt this effort uses (Opus or Codex): instruct the reviewer to **write the review to a file path** (not inline), **ground findings in SoT v3 + the two owner strategy docs + the codebase reality audit**, and **verify codebase claims against real code**.

**Do not reopen what is locked:** SoT v3's device-authority architecture; the bounded-modules asymmetric seam (§13.6 / D16); the codebase reality audit; roadmap v2's phase structure; and the v1–v6 findings (Codex v3/v5/v6 + Opus v4) already resolved in later revisions.

---

**End of handoff (refreshed 2026-05-20/21 after Task 33 full-flow verification + review audit).**

**Continuation arc completed:** Task A (Pass 2A.TS dual review) + Task B (Task 27B Pass 2B / Task 28 absorption) + Task C (Task 33 full-flow verification) + this Task D handoff/memory refresh.

- **Task A:** Pass 2A.TS (`8f358d5e6`) received Codex + Opus-equivalent review; audit commits landed through `3e1547d32`.
- **Task B / Task 27B Pass 2B:** `c8cd9fca1` wired POS receipts through fiscal events; `fdb9e2700`, `1b8dfa080`, and `6369d99b3` closed second-pass findings around fiscal sync recovery, null-id sequence conflicts, and fail-loud sync discriminators; `b4faf11e6` recorded review artifacts. Legacy chain/sync surfaces deleted; `.PASS_2B_PENDING` removed atomically with engine wiring; Task 28 absorbed.
- **Task C / Task 33:** `b713063d1` added `Task33FiscalFullFlowVerificationTest` and roadmap status update; `e070a6fed` recorded Codex + Opus-equivalent reviews. The test posts a device-shaped sealed `SALE_RECEIPT` envelope to `/api/v1/pos/sync/fiscal-events`, asserts server ingest/parse/hash verification, POS + Treasury projection effects, NF525 canonical-only fields, and byte-equivalence across device bytes, `fiscal_events`, and projected `pos_receipts`.
- **Verification evidence for Task C:** targeted Task 33 PHPUnit PASS (1 test / 41 assertions); fiscal/POS PHPUnit subset PASS (546 tests / 2005 assertions / 53 skipped); `pnpm typecheck` PASS; `pnpm lint` PASS with existing warnings only; `pnpm test` PASS (POS + web Vitest); PHPStan L8 PASS; Pint PASS; Pass 2B sentinel PASS; §14.3 chokepoint gate PASS; dead-path grep PASS.
- **Review outcomes:** Codex self-review APPROVE; Opus-equivalent second-pass APPROVE-WITH-MINOR-EDITS, with no BLOCKER/REQUEST-CHANGES. Residual note: Task 33 PHPUnit builds a device-shaped envelope in PHP rather than invoking the TS Tauri engine; TS device coverage remains in Pass 2B POS tests and drift gates.

**Current Phase 1 status:** COMPLETE. All Phase 1 task slots are shipped or absorbed. Branch `feat/pos-fiscal-event-engine-phase1` pushed through `e070a6fed` before this handoff-refresh commit. Next owner decision is Phase 1.5 cleanup versus merge/PR disposition.

**End of handoff (refreshed 2026-05-20 after Task 27B Pass 2A complete — synthesis arc + PHP.1 + PHP.2 + TS all shipped/pushed; Pass 2B + Task 33 deferred to Codex-led continuation session per owner directive 2026-05-20.** 

**Session arc 2026-05-20 (this refresh):**
- **Synthesis loop (5 Codex review rounds):** Pass 2 implementer pre-flight (agent `a58368ad0687cb326`) flagged 5 plan §2144 divergences against codebase. Owner directed (D1-D9): commit to spec for all 4 regimes (NF525/ZATCA/DE/IT); implement NF525+Tunisia immediately; ZATCA/DE/IT deferred; v1 rewrite in place atomically (no production tenants); no dual chain; drop feature flag; B2C Simplified only via Tauri; legacy mirror columns retained through Pass 2 with Phase-1.5 audit task. 5 synthesis revisions converged on **APPROVE-WITH-MINOR-EDITS at v5** (`docs/superpowers/research/2026-05-20-sale-receipt-canonical-payload-synthesis-v5.md`). 27-key Candidate C-v3 canonical payload (sorted lex). Audit trail at `e9a94790b`. Plan §2144-2389 + spec v7 §11.2-§11.4 + roadmap v2 §Phase 1.5 amended at `be1d3a687`.
- **Pass 2A.PHP.1 (foundation):** `b6f143e1a` R1 (validator + 9 DTOs + CanonicalPayloadReader + migration + 49-test FiscalPayloadConstraintValidatorTest + D16 grep guard + 15 hand-authored JCS golden vectors F-01..F-15 + 36 per-method markTestSkipped citing PHP.2). Opus R1 APPROVE + 3 P3 (folded to PHP.2); Codex R1 REQUEST-CHANGES (3 P1 + 1 P2 + 1 P3 doc). `ed17e022d` R2 closed all Codex findings (currency_scale allowlist `{0,2,3}` + UUID/ISO8601 format helpers + TRAINING-flag invariant + F-15 BCMath cleanup + synthesis §6.E case 8 doc amendment). Codex R2 APPROVE at `d2319c068`. 488 tests / 1570 assertions / 81 skipped / 0 failures.
- **Pass 2A.PHP.2 (consumer migration):** `d9cc8e250` R1 (PaymentMethodResolver interface at `App\Shared\Contracts\Fiscal\` + EloquentPaymentMethodResolver + PosCoreReceiptProjection migration + Nf525DataProvider bifurcation per v5 §8.B + TreasuryReceiptBridge sibling migration + 9 test helper migrations + 36 PHP.1 skips removed + D16 expansion + F-15 determinism test). Opus R1 APPROVE-WITH-MINOR-EDITS + 2 P3 (Phase 1.5); Codex R1 BLOCK (2 BLOCKER + 2 P1 + 1 P2). R2 `3c1711f4a` closed R1 findings (cross-tenant product FK + REFUND/VOID fail-loud `OriginalReceiptUnresolvableException` + buyer-deletion regression test + D16 `Shared\Contracts\Treasury` pattern + PaymentMethodResolver null contract docblock); Codex R2 BLOCK with new BLOCKER (R2-finds-new-defects standing pattern recurring — impl violated R2's own docblock contract by catching QueryException + returning null instead of propagating). R3 `688878c54` closed (catch removed; buyer-test values differentiated; cross-tenant product test sealed-snapshot reload + new EloquentPaymentMethodResolverTest with 4 cases). Codex R3 re-review SKIPPED per Task 30 R3 precedent (mechanical fix matching Codex spec verbatim; all gates green; sandbox-reliability lesson). 496 Fiscal + 289 Treasury tests / 1805 + 875 assertions / 45 skipped (down from 81).
- **Pass 2A.TS:** `8f358d5e6` (TS engine `SaleReceiptPayloadInput` expanded to 27-key nested shape + `validateSaleReceiptPayload` runtime validator + `SALE_RECEIPT_PAYLOAD_KEYS` constant + cross-language drift gate test + `.PASS_2B_PENDING` marker at `apps/pos/src/lib/offline/` + `apps/pos/scripts/check-pass-2b-pending.sh` CI sentinel wired into chokepoint-gate CI job + 28 new vitest tests + ChainRecoveryService test helper migration). NOT YET dual-reviewed — handoff item for next session. 1467/1467 POS suite passing; CI sentinel positive + negative-tested (drops in evil import → exit 1 with forensic message).

**Codex BLOCKER-streak now 16 of 30+** (Pass 2A.PHP.2 R1 extended: BLOCKER-1 cross-tenant product FK + BLOCKER-2 REFUND/VOID downgrade — both caught by Codex, missed by Opus + implementer). Plus the R2-finds-new-defects pattern struck again in PHP.2 R2 (EloquentPaymentMethodResolver QueryException swallow contradicted the R2 docblock).

**Owner directive 2026-05-20 (mid-session):** transitioning to Codex-led continuation. **Codex acts as primary implementer + first-pass adversarial reviewer; Opus called for second adversarial pass only.** Comprehensive Codex handover brief written to `docs/superpowers/coordination/2026-05-20-codex-handover-brief.md`. Pass 2A.TS dual review + Pass 2B + Task 33 + final handoff refresh deferred to that continuation session.

**Phase 1 status: 32 of 33 task slots shipped or split** (Pass 2A complete; Pass 2B + Task 33 remaining).

Branch HEAD `8f358d5e6` on `feat/pos-fiscal-event-engine-phase1` (pushed). 14 commits this session. Worktree clean.

(Previous "End of handoff" message for archival reference: 2026-05-20 after Task 32 round-2 — `Off-device durability controls + device-loss incident register (§12)` shipped + dual-reviewed. R1 (`b09665a33`): Opus APPROVE, Codex REJECT (2 BLOCKER + 1 P1 + 2 P2) — T32-B2 dead-control-surface (`UnsyncedRiskIndicator` mounted nowhere) is the EXACT Task 30 dead-path-rebuild pattern recurrence on a fresh net-new component class. Opus missed it as "component exists, it's good"; Codex caught via "grep for callers" attack. R2 (`3995575e0`): closed all 5 findings (mount + wire-up via Zustand `durabilityStore` + `useFiscalDurabilityPolling` hook + `DurabilityGateModal`; composite FK rejects cross-tenant terminal; thresholds optional with defaults + InvalidDurabilityThresholdError; `better-sqlite3` swap unblocks Node 20 CI). Opus R2 re-review APPROVE — 0 new findings, 4 P3 + 1 NOTE (modal focus-trap; grace-lag polling-coupling; dead test indirection; Phase 2 config-source). 13th Opus subagent delegation. **Codex BLOCKER-streak now 15 of 29 (Task 32 extends — second triggering of the dead-path-rebuild standing pattern on this branch).** Phase 1 status: **30 of 33 task slots shipped or split**. Remaining: 27B Pass 2 (READY-TO-DISPATCH per Q1-Q6 answered 2026-05-20 at `7a7686817`), 28 (BLOCKED on 27B), 33 (BLOCKED on 27B + 28). Pushed to `feat/pos-fiscal-event-engine-phase1` (PR #124).

(Previous "End of handoff" message for archival reference: 2026-05-20 after Task 30 round-3 — `§14.3 chokepoint CI gate + ReceiptHashService/Nf525DataProvider rebuild` shipped + dual-reviewed across THREE rounds. R1 (`cb1ab6eda`): Opus APPROVE-WITH-MINOR-EDITS, Codex BLOCK (1 BLOCKER dead-path rebuild + 1 P1 gate-too-permissive + 1 P2 bare-bool failure). R2 (`a005b1b71`): 9 round-1 findings closed; both reviewers caught NEW defects (Task 23/24/25 r2 pattern recurrence) — Codex BLOCK (1 BLOCKER T30-R2-B1 = dead-path NOT actually closed, DTO still mirror-sourced + 3 P1: CI gate broken on clean checkout, receiver-type validator too permissive, quarantine partial + 1 P2 Mockery anti-pattern). R3 (`660f7a28b`): closed all 5 R2 findings cleanly — `mapSaleReceipt`/`mapVoidedReceipt`/`mapReturnReceipt` bifurcated to source from `fiscal_events.payload` via `StrictCanonicalParser`; tampered receipts routed to NEW `tamperedSection`; CI gate restructured (lightweight chokepoint-gate + PHP validator in backend-test-pgsql); strict receiver-expression validator; quarantine UNION of partition + in-table; `Log::spy` migration. Opus R3 re-review APPROVE — 0 new findings (3 P3 polish items only, non-blocking). 16/16 Task 30 tests + 260/260 full Fiscal feature suite + JET XML byte-stability fixtures preserved + PHPStan L8 + Pint clean. Codex BLOCKER-streak now 14 of 28. Twelfth Opus subagent delegation; Task 30 = second three-round task in the streak (Task 23 was the four-round). **5 new standing patterns** (folded into §4.2): dead-path-rebuild-needs-actual-rewire / receiver-type-validation-needs-specific-call-site / CI-gate-extension-updates-job-deps-in-same-commit / §8-quarantine-completeness-needs-both-partition-and-in-table / `Log::spy`-over-Mockery-`Log::shouldReceive`. Q1-Q6 for Task 27B Pass 2 answered + committed at `7a7686817` — Pass 2 status changed from BLOCKED → READY-TO-DISPATCH. Pushed to `feat/pos-fiscal-event-engine-phase1` (PR #124). Phase 1 status: **29 of 33 task slots shipped or split**. Next session priority: Task 32 (off-device durability, independent), then 27B Pass 2 (Q1-Q6 locked, dedicated session), then 28 + 33.**

(Previous "End of handoff" message for archival reference: 2026-05-19 after Task 29 round-2 — Round-1 (`eb85fb155`): Opus APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 2 P2 / 3 P3); Codex APPROVE-WITH-MINOR-EDITS (0 BLOCKER / 0 P1 / 4 P2 / 0 P3). **Three single-shot clean dual reviews now: Tasks 17, 20, 29.** Codex round-1 ran via the user-piped sandbox fallback. Round-2 (`314dbc781`): closed all 4 Codex P2s (T29-F1 void/return happy-path coverage replaces "not 410" deferred-bail-out shape / T29-F2 + T29-F4 class-level-skip-too-broad → per-method skips with seeder-permission + service-bypass tenant-isolation assertions un-skipped / T29-F3 skip citation now points to actual defense in `PosCoreReceiptProjection::writePayments` + 2 new projection tests pin voucher-instrument missing-type/missing-serial rejection) + 3 Opus P3s (docblock past-tense, IMPLEMENTATION_SUMMARY annotation, design-token migration on the retirement-notice page). Opus round-2 re-review APPROVE — 0 new findings (no Task 23 r2/r3 anti-pattern recurrence). 2 Opus P2s deferred with explicit citations: F1 (FK coverage gap on 6 non-payment-method axes) → Task 22/30 follow-up; F2 (route closures inherit auth:sanctum) → by design for authenticated POS/API surface, inline comments shipped. 244/244 Fiscal feature suite + 23/23 PosCoreReceiptProjectionTest + PHPStan + Pint clean. CI PG-merge-gate extended with `NewSaleServerAuthoringDispositionTest`. Codex BLOCKER-streak holds at 13 of 27. 3 new standing patterns (folded into §4.2): class-level-`markTestSkipped`-is-too-broad / skip-citation-accuracy / "not 410"-is-not-"happy path". Eleventh Opus-subagent delegation. Pushed to `feat/pos-fiscal-event-engine-phase1` (PR #124). Phase 1 status: 28 of 33 task slots shipped/split. Next session: **Task 30** (CI grep gate + ReceiptHashService rebuild — now unblocked by Task 29) OR **Task 32** (off-device durability controls, independent) if Q1-Q6 still unanswered; **27B Pass 2** as dedicated session if owner answers Q1-Q6.**
