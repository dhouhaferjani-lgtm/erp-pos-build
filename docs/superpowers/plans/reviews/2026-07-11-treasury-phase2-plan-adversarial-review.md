# Adversarial Plan Review — Treasury Phase ② Implementation Plan (Rev 1)

> **Date:** 2026-07-11 · **Reviewer:** treasury-reviewer (Fable-tier, financial-spine rule)
> **Subject:** `docs/superpowers/plans/2026-07-11-treasury-phase2-instruments-echeancier.md` (Rev 1) vs spec Rev 2 + live code.
> **Verdict: APPROVE-WITH-FIXES** — no BLOCKER; 3 HIGH / 7 MED / 7 LOW, all accepted; resolutions in plan Rev 2's reconciliation section.

## Verified CLEAN (mandate answers)

- **§20 coverage:** all 32 spec-review findings trace to a task (F9 pin was the one gap — finding 6).
- **Code anchors:** `PaymentController` doc locks `:673`, GL posts `:781/:794`, advance JE `:838` (builder `GeneralLedgerService.php:330`, debit line `account_id => $paymentMethodAccountId` `:380`), withholding `:630-661` in-tx, `?? 'TND'` at `:604/:623/:1143/:1162`, `storeMultiple :964`; `createPaymentReceivedJournalEntry :981` (debit param `:986/:1023`) — **the debit swap is genuinely parameter threading for both builders**; `createPOSPaymentEntry(Payment, Receipt, PaymentRepository)` `:2569` — override-param seam clean. Bridge anchors `:364/:392-416/:428-444/:456-471/:292-301` ✓. `MultiPaymentController:94/:123/:216/:250/:412` ✓. `ReconcileTreasuryCommand :114/:400-430` ✓. Seeder `:210-213` ✓. `MovementSourceType::Instrument`, `JournalCode` fallthrough, `requiresInstrumentForMethodCode:63`, `forEachTenant:150` (continue-on-throw `:168`) ✓.
- **Document immutability trigger `2025_12_11_054716`:** enforces only on `fiscal_status='SEALED'`; `status` NOT in the immutable list; comment says "Allow operational status updates (e.g., posted -> paid)". **Paid→Posted revert permitted** — Task 9's conditional is a no-op; state affirmatively.
- **Transaction/lock coherence:** `record()` throws at `transactionLevel()===0` (`TreasuryMovementService.php:51-53`); `postEntryNow` throws `<1` (`:2383-2386`); every plan call site is inside a transaction; nested `DB::transaction` = PG savepoint — Tasks 6+13 coexist safely. Task 13's insertion order satisfies the global lock order with zero restructuring.
- **Task ordering:** all produce-before-consume chains hold (`CancellationShape` 6→17, FK 2→5, `handleMaturityLeg` 16→18, buckets 19→23/25). Nothing reads `phase2_cutover_at` before Task 22.
- **Test harness exists:** `tests/Feature/Treasury/PosBridgeSpineTest.php` (real canonical bytes via `canonicalEncode:669`, `seedPriorLegWrite:441` = the pre-cutover fixture Task 16(c) needs, context cleared `:160-162`), `PosRefundReceiptBridgeTest.php`, `tests/Feature/Fiscal/TreasuryReceiptBridgeTest.php`. Task 5's numbering mirrors the real `generateEntryNumber:3414-3441` mechanism.

## Findings

1. **[HIGH] Task 9 — tolerance reversal as written finds NOTHING.** `CloseInvoiceWithToleranceService.php:110-116` creates the allocation with **`payment_id = NULL`** (discriminator per `2026_04_26_120000` docblock: `payment_id IS NULL AND tolerance_writeoff = amount AND tolerance_writeoff > 0`, linked via `document_id` only). A payment-scoped query silently reverses nothing → `balance_due` reopens short (re-introduces spec-review T19). Write-off JE findable via `source_type='payment_tolerance'`, `source_id` = document (`GeneralLedgerService.php:1120`). *Fix:* reversal scope = tolerance allocations **on the documents being reopened**; contre-passation targets the `payment_tolerance`-sourced JE per document.
2. **[HIGH] Task 3/13/16 — seeder code list wrong; `kind=other` has NO posting rule.** Actual `has_maturity=true` seeds (`PaymentMethodSeeder.php`): TN `CHECK`,`TRAITE`; FR `CHECK`, **`DIRECT_DEBIT`** (`:260-263`), **`LCR`** (`:276-279`), **`BILL_EXCHANGE`** (`:324-327`; no "PDC"/"BILL_OF_EXCHANGE"). Plan's mapping leaves LCR/BILL_EXCHANGE/DIRECT_DEBIT NULL-kind on maturity rows — violating Task 3's own 422 invariant — and `InstrumentKind::Other` reaches an undefined posting branch in the cutover/bridge. *Fix:* map LCR/BILL_EXCHANGE→effet; DIRECT_DEBIT→`other`; pin the rule: **the cutover + bridge instrument creation apply only to kind cheque/effet; `other` maturity methods keep Phase-① behavior** (mandate-pull ≠ portfolio paper), tested.
3. **[HIGH] Task 14/File Structure — `PaymentRefundService` path wrong.** Lives at `app/Modules/Treasury/Domain/Services/PaymentRefundService.php` (`refundPayment:78`, `partialRefund:211`, `postRefundGlAndMovement:402`, `canRefund:479`, `reversePayment:523`) — the plan's `Application/Services/` path doesn't exist; a fresh subagent may create an unused class and the T3 BLOCKER guard never wires.
4. **[MED] Task 22 — `companies.phase2_cutover_at` migration referenced but listed nowhere.** Add to Task 22 Files + File Structure.
5. **[MED] Task 26 — the pgsql job has NO path list to extend.** `ci.yml:652` runs whole directories (`tests/Feature/Treasury`, `tests/Feature/Accounting`, `tests/Unit/Treasury`, `:766-776`) — new tests auto-covered; genuinely uncovered: `tests/Architecture/...` (Task 1) + `tests/Feature/Compliance/...` (Task 10). Steps are deliberately serial ("DO NOT PARALLELIZE", `:757-764`). Executor note says "Task 28" for CI — it's Task 26. *Fix:* rewrite as a verification task.
6. **[MED] F9 pin missing** — no task asserts "nothing writes `pos_receipt_payments`". Add to Tasks 16+17.
7. **[MED] Task 14 silently owns the `:1143/:1162` (and `:623`) currency literals** only via a Task-13 aside; a fresh Task-14 subagent won't do it. Make explicit.
8. **[MED] Task 11 — double-dispatch:** controller currently dispatches the four instrument events itself (`:190/:232/:278/:330`); after Tasks 6-10 move dispatch into the service, Task 11 must DELETE the controller dispatches or every transition double-writes audit rows.
9. **[MED] Spec §19.3 (`dishonored_at` on FE payment surfaces) never traced.** Add `dishonored_at` to `formatPayment` + list badge, or record a deliberate deferral.
10. **[MED] Task 6 transaction contract contradicts call sites.** `receive()`/`cancel()` are called INSIDE existing transactions (Tasks 13/17); reword: opens its own transaction only when none is active (nesting = savepoint is intended usage), so a literal subagent doesn't hoist `receive()` out and break atomicity. Task 16: unique-violation SELECT sits OUTSIDE the nested closure (the `PaymentController.php:578-586` lesson).
11. **[LOW] Byte-identical pins are green-first regression pins**, not failing-first TDD — no snapshot mechanism exists; assert explicit JE line accounts/amounts + movement columns. Say so.
12. **[LOW] Name the harness in Tasks 16-18** (`PosBridgeSpineTest` fixtures, `PosRefundReceiptBridgeTest`).
13. **[LOW] File Structure omits** `InstrumentAccountPurpose`, `CancellationShape`, `MissingInstrumentAccountException`.
14. **[LOW] D-7 early-remise FE warning absent from Task 24.**
15. **[LOW] Spec §5.6 no-partial-unique-JE-index migration comment assigned to no task** — attach to Task 5.
16. **[LOW] Dishonor-after-clear lacks an in-test `treasury:reconcile` run** (clear and bounce_fee have one; §16 asks per-transition).
17. **[LOW] Line-ref drift within tolerance** (`refundPayment:78` vs plan `:89`; `resolveDefaultRepository:596`; `createPOSPaymentEntry:2569`) — re-grep note already covers.
